# CANONICAL BASE config — the full, documented, shared configuration. NO secrets: ntfy_topic is a dummy
# (the tests never push), every URL below is a public, unauthenticated endpoint. THREE consumers:
#   - network-smoke (per-PR): only urls.yahoo_chart is exercised.
#   - backtest-gate (nightly + push/PR): runs `backtest 12 universe`, scoring with buy_heuristic below.
#   - the private config/settings.yaml INHERITS this file (config::load deep-merges it as the base), so the
#     gitignored local overlay only OVERRIDES what it changes. By convention it now carries ONLY the
#     watchlist (tickers), the table column layout (widths), and any local secret (a real ntfy_topic) or
#     knob under test — EVERYTHING ELSE lives HERE. Overlay wins field-by-field. Edit shared config HERE.
# Omitted knobs fall back to the code's #[serde(default)] (== the shipped defaults).
#
# A/B'ING A KNOB — WHERE YOU PUT THE ARM FILE CHANGES THE ANSWER (learned the hard way 2026-08-03).
# `config::data_path` (config.rs:707) resolves the on-disk caches from the config file's GRANDPARENT
# directory. `tests/<x>-ci-settings.yaml` has an empty grandparent, so it falls through to the CWD and
# reads the repo-root caches. An arm parked in /tmp or a job dir resolves to THAT tree instead — a cold,
# separate cache. A cold `.long_history_skip.json` makes `screen` fetch the monthly series it would
# otherwise skip, which moved the universe by 42 names and rewrote most of the gate funnel. `backtest` is
# immune (measured: same config, both dirs, byte-identical), `screen` is NOT. So: keep every arm at
# `tests/<something>-ci-settings.yaml`, and bracket the arm with a repeat of the shipped
# config — consecutive same-config screens still drift by a few names, and that drift is the noise floor
# any claimed effect has to clear.
#
# (#125) CORRECTION 2026-08-23. The clause just deleted from the paragraph above was WRONG, and wrong
# in the direction that silently produces a wrong measurement rather than an error. It claimed a
# `tests/<x>-ci-settings.yaml` arm is SELF-CONTAINED, "since `ci_base_yaml` returns an empty base for
# that name". It does not. `ci_base_yaml` (src/config.rs) tests `overlay.ends_with("ci-settings.yaml")`
# and Rust's `Path::ends_with` matches WHOLE PATH COMPONENTS, not string suffixes. The last component
# of `tests/diag-ci-settings.yaml` is `diag-ci-settings.yaml`, which is not the component
# `ci-settings.yaml` — so the test is FALSE, and this canonical base IS loaded and merged underneath
# the arm. Only a file literally NAMED `ci-settings.yaml` takes the empty base, which is CI's own
# `FOLIOMAN_CONFIG=tests/ci-settings.yaml` and precisely the self-merge that guard exists to prevent.
#
# WHY THE ERROR IS EXPENSIVE: merging is what an arm WANTS — carry only the knob under test, inherit
# every other gate from this file. Believing the old sentence, you would either duplicate 5000 lines
# needlessly, or write a small arm trusting it stood alone. A small arm with a non-empty
# `buy_heuristic` still PASSES `gates_configured`, so it would not refuse; it would run with every
# OTHER gate at its CODE DEFAULT and report a plausible-looking table. src/config.rs names that case
# itself: "not a degraded result, it is a wrong one".
#
# HOW TO VERIFY ANY ARM, free and without a test: `heuristic_drift()` prints a startup WARNING listing
# every `buy_heuristic` knob whose value differs from the validated baseline. It must name EXACTLY the
# knobs your arm sets — no more. If it names dozens, the base did not merge and the run is void.
# Measured on tests/diag-ci-settings.yaml: 4 knobs set, 4 knobs named. That check is what settled this,
# and it is the check to repeat, because NOTHING in the test suite pins `ci_base_yaml`'s behaviour.
#
# ============================================================================================
# (#131) THE IN-TREE OVERLAY ARMS ARE GONE (2026-08-23) — WHAT THE CITATIONS BELOW POINT AT
# ============================================================================================
# Six `tests/<x>-ci-settings.yaml` overlay arms were deleted as housekeeping. The repo now keeps
# exactly two settings files: THIS one (the committed base) and the gitignored `config/settings.yaml`.
# `tests/fixture/config/settings.yaml` also stays and is NOT an exception to that rule — it is not a
# tuning arm at all. It carries the golden fixture's universe, and its LOCATION is load-bearing:
# `config::data_path` anchors to that file's GRANDPARENT, which is the only thing making
# `tests/fixture/` the frozen-cache data root. Delete it and the goldens do not fail, they score a
# different dataset and print confident wrong numbers.
#
# NOTHING BELOW IS RETRACTED. Every receipt citing a deleted arm still stands: the arms were
# INSTRUMENTS, the measurements they produced are written down in this file, and the file is the
# record. What is gone is the convenience of re-running one without re-typing it. Each is one
# `git show <sha>:<path>` away:
#     tests/diag-ci-settings.yaml      f26f02b  4 display-only knobs, all score-neutral: print_n_eff,
#                                               print_recall_capture, print_book_deciles,
#                                               print_base_rates. Cited in the header above and in
#                                               several receipts below.
#     tests/gatesoff-ci-settings.yaml  5159105  the (#129) gates-off CEILING arm: 9 binding gates at
#                                               their own documented off-values (min_range_pct 0;
#                                               min_cagr / min_1y_pct / min_5y_pct -1e9; max_above_ma
#                                               / maxdd_cap / max_peg 0; require_peg and
#                                               require_lifetime_uptrend false) + print_recall_capture.
#     tests/beta{050,030,015,000}-     2804808  the (#130) beta ladder: ONE knob each,
#       ci-settings.yaml                        growth_accel_beta at 0.5 / 0.3 / 0.15 / 0.0.
#
# RE-CREATING ONE IS THE POINT, NOT THE COST. An arm is 2-10 lines and the merge rules are directly
# above: carry ONLY the knob under test, let this file supply every other gate, and verify FROM THE
# RUN that `heuristic_drift()` names exactly your knobs and no others. A stale arm sitting in the tree
# is the thing worth avoiding — it invites being re-run against a base that has moved underneath it,
# which is the whole subject of the (#125) correction above.
#
# ============================================================================================
# (#146) WHAT THE LIVE TRACK RECORD IS WORTH (2026-08-27) — MEASURED, AND IT IS ~1 TRIAL
# ============================================================================================
# Not a knob. `track`/`sim`/the screen's trust line grade the LIVE forward record — the screen's own
# past top-10s at today's prices — and that number needed the same honesty `print_n_eff` (#124) bought
# for the backtest columns. It shipped without it, and read as the more confident of the two.
#
#   THE SHAPE OF THE ERROR. `track` grades EVERY journaled snapshot against TODAY. The windows are
#   therefore NESTED on one shared endpoint, not sequential like the backtest's ~6-month entry
#   buckets. The span of entry dates is (oldest - newest); the longest hold graded is `oldest`
#   itself. The second can never be smaller than the first, so the ratio is <= 1 BY CONSTRUCTION.
#   More snapshots do not buy a second trial. A 10,000-row table would still be one observation of
#   one endpoint, and one bad final week moves every row in it together.
#
#   MEASURED on the real journal, 2026-08-27: 40 snapshots, 2026-07-16 -> 2026-08-27, 39 gradeable.
#     entry span    42d
#     longest hold  42d
#     n_eff         1.00        (39 rows, one trial)
#     longest hold  0.115y = 0.6% of the 20y hold the ranking is built for
#
#   WHAT SHIPPED: `track::effective_trials` + `track::trials_note`, appended to the win rate on BOTH
#   surfaces that print it (`track`'s summary and the screen's trust line) and, free, to the `--push`
#   ntfy ping, which is the most decontextualised surface there is. One helper, so the two cannot
#   quote one fold with two different confidences.
#
#   NO KNOB, DELIBERATELY — the SECOND documented exception to non-negotiable #1, alongside
#   `config::Sizing`. That rule protects an off-default lane; `track` has none. The goldens are
#   backtest-only (`tests/fixture/backtest-*.golden`) and nothing covers this output, so there is no
#   byte-identical lane to keep. And a correction gated behind a flag defaulting OFF corrects
#   nothing: (#124)'s own knob has sat off since the day it was measured, which is precisely why
#   every 20y verdict in this file still reads as if it rested on more than half a trial.
#
#   (#124)'s FORMULA MUST NOT BE REUSED HERE. `windows / (2 * years)` counts SEQUENTIAL buckets. That
#   receipt says it itself: applied to a differently-shaped count it "would be a wrong number wearing
#   the right label". Nested windows needed their own derivation and got one.
#
#   THE LINE THAT MATTERS FOR THE FUTURE: the live record is DESCRIPTIVE. It must never become a
#   ship-rule input — the same boundary (#124) draws. At n_eff 1.00 it has nothing to adjudicate
#   with, and the ship rule reading it would import a fitted number wearing an out-of-sample label.
#
#   WHAT WOULD ACTUALLY RAISE IT: grade each snapshot at its own maturity (d_i + H) instead of at
#   today, which makes the windows sequential and lets n_eff grow with the record. It needs
#   per-ticker history rather than the one current-price fetch `track` makes, and on a 42-day record
#   it would grade ZERO rows — so it is named in the code as the upgrade path, not built.
#
#   WHAT DOES NOT NEED FIXING: the recording itself. `screen` has journaled its `growth_score`
#   ranking every run since round 114, so the buy/growth heuristic's forward record IS accruing. Time
#   is the only binding constraint on it, and no code removes that. Still open and unrelated:
#   `journal_core_list` (#103), the buy-and-hold half, which has accrued nothing and cannot be
#   recovered retroactively.
#
#   REVERT: none needed - it cannot move a decision, only describe one. Deleting the note restores
#   the previous output exactly; nothing reads it.
#
# ============================================================================================
# ETF-CLASSIFICATION RECEIPT (2026-08-02) — READ BEFORE TRUSTING ANY PEER-RELATIVE EDGE BELOW
# ============================================================================================
# Every peer-relative edge number written into this file BEFORE this date was measured with ETFs
# pooled into the STOCK peer-mean. `core::backtest_quote` builds `Quote::stub(ticker, "", "", ticker)`
# — name = the ticker, instrument_type empty — and `picks::quote_is_etf` is exactly
# `instrument_type == "ETF" || is_etf(name)`, where `is_etf` substring-matches "etf"/"ucits"/… markers
# that no real fund TICKER carries (VWRA.L, XDWD.L, SEMI.AS). So every fund in the pool classified as a
# single stock. `demean` splits on `picks::asset_class`, so the peer baseline each lane edge is graded
# against mixed funds with companies. Fixed by `stamp_asset_class` (commands/backtest.rs), which fills
# name/instrument_type/sector from the chart response the backtest ALREADY fetches — zero extra requests.
#
# HOW BIG: not the "~87% of the universe" the ticker count suggests. Forward-window survival scales with
# listing age, and UCITS funds are young, so ETF density in the SCORED pool depends on the horizon. The
# per-class census the report now prints, `universe fund <h> stress`:
#     20y-s: crypto 0/4633     ETF 14/4633 (0.3%)    stock 4619/4633
#     12y-s: crypto 0/11758    ETF 2624/11758 (22%)  stock 9134/11758
#      8y-s: crypto 0/20575*   ETF 8650/20575 (42%)  stock 11907/20575        (*18 crypto cutoffs, 0 scored)
#
# WHAT MOVED (GROWTH lane, `universe fund <h> stress`, same session/binary/universe, before -> after):
#     20y-s KING  rho +0.18 -> +0.18   edge +648.2 -> +661.2   winsor +570.7 -> +583.7   OOS +0.21/+0.17 -> +0.22/+0.17
#     12y-s       rho +0.21 -> +0.12   edge +212.9 -> +148.1   winsor +168.3 -> +113.6   OOS +0.17/+0.19 -> +0.18/+0.01
#      8y-s       rho +0.25 -> +0.16   edge  +77.1 ->  +42.9   winsor  +73.6 ->  +41.2   OOS +0.19/+0.19 -> +0.19/+0.08
#
# The damage tracks ETF density EXACTLY — 0.3% density, no change; 22%, edge -30%; 42%, edge -44%. That
# (but see TWO-CAUSE ATTRIBUTION below: the "after" column bundles a second change, now separated)
# dose-response is the evidence the fix is a fix and not a new bug. `windows scored` barely moves
# (380->380, 576->576, 723->717), so no names dropped out: what changed is the YARDSTICK, not the pool.
# The old 12y/8y numbers were inflated because a growth stock was being graded against a peer pool that
# was a fifth-to-half broad index funds. Beating a basket of index trackers is easy for a momentum name;
# beating other STOCKS is the real test, and that is what these lower numbers now measure.
#
# WHAT DID **NOT** MOVE: the held book — the rows actually bought. Identical at all three horizons:
#   20y  growth_score book +13.3%/yr excess +6.1 win 97%   rank1 +6.0|+6.6 win 83% of 36
#   12y  growth_score book +14.1%/yr excess +6.3 win 90%   rank1 +5.6|+5.9 win 69% of 51
#    8y  growth_score book +14.0%/yr excess +6.4 win 88%   rank1 +6.6|+7.2 win 73% of 59
# Held-book excess is measured against a BENCHMARK, not against the de-meaned peer pool, so the
# contamination never reached it. The 12y/8y drop is a diagnostic correcting itself, not a portfolio
# getting worse. The growth lane still beats the on-sale foil at every horizon (661 vs 413, 148 vs 46,
# 43 vs 27), so the lane choice is unaffected too.
#
# TWO-CAUSE ATTRIBUTION (added 2026-08-02, correcting the single-cause reading above)
# ----------------------------------------------------------------------------------------------
# The "after" column bundles TWO changes that shipped in the SAME commit, not one:
#   (1) the de-mean now groups by a CORRECT asset class, so a stock is graded against stocks;
#   (2) `growth_commodity_damp` (0.8 in this file) went from inert to LIVE, because the same stamping
#       that fixed the class also supplies the `sector` and real fund `name` that `picks::is_commodity`
#       reads. Its own doc in config.rs asserted it "can never be swept" — that was true until it wasn't.
# Attributing the whole move to (1) was therefore unproven. A CONTROL run — `growth_commodity_damp: 1.0`,
# every other knob at this file, same binary/universe/session — separates them. GROWTH lane, edge:
#
#   horizon   base (pre-fix)   ctrl (split only)   etf (split+damp)   split alone   damp alone
#    20y-s KING   +648.2            +653.9              +661.2           +5.7         +7.3
#    12y-s        +212.9            +141.2              +148.1          -71.7         +6.9
#     8y-s         +77.1             +42.0               +42.9          -35.1         +0.9
#   rho:  20y +0.18/+0.18/+0.18   12y +0.21/+0.09/+0.12   8y +0.25/+0.15/+0.16
#   winsor ctrl: +576.4 / +106.9 / +40.3     OOS ctrl: +0.21|+0.17 / +0.16|-0.02 / +0.19|+0.06
#
# WHAT THIS CHANGES, and what it does not:
#   - The 12y/8y collapse IS the peer-mean split. The split ALONE costs MORE than the net move
#     (-71.7 vs a net -64.8 at 12y; -35.1 vs -34.2 at 8y) — the damp cushions it. The receipt above
#     is not just directionally right there, it UNDERSTATES the contamination.
#   - The 20y KING improvement is mostly the DAMP (+7.3 of +13.0), not the split (+5.7). The original
#     "the king horizon barely moved" line stands, but its cause was misattributed.
#   - `growth_commodity_damp` is edge-POSITIVE at every horizon measured. This is the FIRST measurement
#     the knob has ever had; it was set to 0.8 by judgement precisely because it was believed unsweepable.
#     At 12y it also lifts OOS-late from -0.02 to +0.01, i.e. off the wrong side of zero.
# DEBT: the damp is now sweepable and has NOT been swept — 0.8 remains an unfitted judgement value that
# happens to measure positive. Sweep it before treating 0.8 as validated.
#
# GATES THAT WERE DEAD AND ARE NOW LIVE in the backtest (they read name/instrument_type/sector):
#   is_commodity_etf (physical-gold/ETC), is_commodity (fund name tokens), sharpe_cap_etf.
# STILL INERT there, for a different and unfixed reason — `backtest_quote` never fills their inputs:
#   growth_min_aum_etf (aum_eur None), is_noneur_etf (quote_currency None), age/TER gates.
# BOTH LISTS ARE NOW PINNED, not prose: `growth_gate_reachability_pin` (commands/backtest.rs) probes
# every growth knob against a stock/ETF/crypto fixture built through the real reconstruction and asserts
# the full LIVE/INERT table per class. It also separates the three reasons a knob reads INERT — field
# never filled, scoped to another class, or reachable-but-dominated (which is what
# `growth_require_lifetime_uptrend` turns out to be: `growth_min_cagr` rejects its cohort first).
# Change a gate's reachability and that test fails, which is the point — these claims rotted silently
# for years precisely because nothing asserted them.
#
# (#155) THE KNOB SURFACE WAS AUDITED FOR DEAD WEIGHT, 2026-08-29, AND NOTHING IS DELETABLE. Recorded
# because "215 knobs, prune the inert ones" is an obvious-looking session that should not be re-opened
# on the same reasoning three times.
# FIRST, THE COUNT IS 152, NOT 215. `config::BuyHeuristic` carries 152 fields; the larger number counts
# yaml keys across every struct in this file, most of which are not buy-heuristic knobs at all.
# 1. ZERO FIELDS ARE UNREAD. Every one of the 152 is referenced somewhere under src/, checked
#    mechanically over every .rs with the struct definition and the `Default` impl excluded (so a field
#    that is only declared and only defaulted would show up as 0). The count came back 0. There is no
#    dead-field tier at all — the easy, provable deletion set is EMPTY.
# 2. `growth_gate_reachability_pin` CANNOT SUPPLY THE DELETION LIST, and this is the trap worth writing
#    down because non-negotiable #3 sends every future session to that test. Twelve knobs read INERT for
#    all three classes: growth_max_peg, growth_max_dilution_pct, growth_min_interest_cover,
#    growth_min_fcf_margin, growth_min_net_cash_rev, growth_require_lifetime_uptrend, crypto_max_mvrv,
#    growth_min_aum_etf, growth_ter_drag, growth_fx_damp, growth_min_age_years, growth_min_range_pct_8y.
#    THEY ARE BACKTEST-BLIND, NOT DEAD. `dim_active` drives each knob to an extreme (1e9 for a floor,
#    1e-6 for a cap) and asks whether any score moves, so INERT is a real structural claim — but it is a
#    claim about THIS PATH. `backtest_quote` fills 18 fields and these read ones it does not: `fund`
#    (the five fundamentals gates), `aum_eur`, `quote_currency`, listing age, the 8Y range. Every one is
#    reachable on a live `screen`. The pin's own header says this ("STILL INERT there, for a different
#    and unfixed reason") and the probe list labels the last two "known-unreachable by construction".
#    (#156) CORRECTS THE `fund` HALF OF THAT SENTENCE, and it is the half that mattered. "Reachable on
#    a live `screen`" UNDERSTATES the five fundamentals gates: `fund_lane_on` (backtest.rs:501) fills
#    `quote.fund` whenever the `fund` keyword is on the command line, so those five are reachable and
#    GRADEABLE in a BACKTEST — `backtest {20,12,8} universe fund pit`. (#156) then graded one of them
#    (growth_max_dilution_pct) exactly there. So the pin's INERT verdict for those five is an artifact
#    of probing at a default that omits the keyword, NOT a statement that the backtest cannot see them.
#    The remaining four (`aum_eur`, `quote_currency`, listing age, the 8Y range) are backtest-blind as
#    written. This matters for the prune question the block below pre-registers: a live-path twin of
#    the reachability pin is NOT needed for the five fundamentals gates, because the fund lane already
#    grades them. It is still needed for the other four.
#    Deleting on the strength of that table would delete WORKING GATES.
#    Note growth_require_lifetime_uptrend is the third category named above — reachable, inputs filled,
#    but `growth_min_cagr` rejects its cohort first. That domination is a property of cagr sitting at
#    19; loosening cagr un-dominates it. A value-dependent inertness is not a dead knob either.
# 3. THE REAL TARGET A PRUNE WOULD FIND IS 62 OF 152, AND IT IS THE WRONG TARGET. That many ship at an
#    off or non-binding sentinel (0.0, false, ±1e9, empty). They are not dead code; they are
#    DELIBERATELY-OFF LEVERS, and each carries a receipt in this file stating what it does and what
#    turning it on measured. Deleting them deletes the options and the measurements together, which is
#    the one thing this file exists to prevent.
# 4. WHAT SETTLES IT — DELETION IS NOT FREE, IT IS A LOAD ERROR. `BuyHeuristic` is
#    `#[serde(default, deny_unknown_fields)]`, by deliberate choice ("a typo'd knob must error, not
#    silently fall back to the default"). So removing a field turns any overlay that still sets that key
#    into a HARD FAILURE at config load. `config/settings.yaml` is gitignored and invisible to CI, so a
#    fully green gate would not predict a broken machine — the failure mode lands on the user, after the
#    merge. Checked 2026-08-29: the overlay on this machine carries no `buy_heuristic:` section, so
#    nothing would break today. That is luck, it is not a guarantee, and it is not checkable for anyone
#    else's overlay.
# WHAT WOULD ACTUALLY JUSTIFY A PRUNE, pre-registered so the next session does not re-argue it: a knob
# shown inert on BOTH paths. The pin covers the backtest path; NOTHING probes the live `screen` path,
# which is precisely where the twelve above come alive. Build a live-path twin of
# `growth_gate_reachability_pin` first, and delete only what both tables call inert. Until that exists
# every deletion is an argument rather than a measurement, and this file does not ship those.
# VERDICT: no knob deleted, no code changed. The surface is 152 wide because the lane is a record of
# options that were measured, not because it accumulated dead code.
#
# (#164) 2026-08-30 — "NOTHING PROBES THE LIVE `screen` PATH" IS FALSE, AND HAS BEEN SINCE 07a941f.
# Recorded because (#155) makes that claim the PRECONDITION for pruning ("build a live-path twin
# first"), so a reader who believes it spends a round building an instrument that already ships.
# TWO THINGS ALREADY COVER THE LIVE PATH, and between them they are the twin (#155) asked for:
#   (1) `screen::funnel_lines` (src/commands/screen.rs) — per gate, how many names FAILED it and how
#       many it SOLE-BLOCKED, split stocks / ETFs / crypto, with the sole-blocked tickers named and a
#       reconciliation line. Called UNCONDITIONALLY at screen.rs:1982 — no knob, no flag; every live
#       `screen` run prints it. This is where `growth_max_peg`'s receipt got "peg fails 411 stocks and
#       SOLE-blocks 17"; that number was read off this table, not measured by hand.
#   (2) `sole_blocking_gates_are_pinned` ((#47), src/commands/backtest.rs) — pins the sole-blocking SET
#       on the fixture, and its comment already RECORDS THE LIVE SET in-tree: "The live funnel
#       sole-blocks `cagr peg maxdd 1Y+`". Its failure message sends the reader to the live funnel's
#       sole-blocked column before touching the pin.
# So the LIVE/INERT question (#155) wanted is answerable today, and the answer for the gate that
# matters most is already written down in two places.
# THE REAL RESIDUAL GAP IS NARROWER, AND THE CODE NAMES IT ITSELF at backtest.rs:7826: "the only place
# that shows up today is a live `screen` funnel NOBODY DIFFS". The instrument exists; what is missing
# is that nothing compares its output across runs, so a bar being promoted from decorative to
# load-bearing is visible only to a human who runs `screen` and reads the column that day.
# NOT CLOSING THAT WITH CODE, deliberately. A live funnel golden would pin a MOVING MARKET — the set
# changes when prices change, with no knob touched — so it would be flaky by construction and would
# train the reader to re-bless it, which is worse than no test. The mechanism this file already uses is
# the right one: (#47) records the live set in a comment and re-reads it when a knob moves. A second
# instrument is not what is missing; DIFFING the one that exists is, and that is a habit, not a diff.
# ONE MORE THING THIS CORRECTS, because it is the load-bearing half. `funnel_lines` only emits a row
# for a gate that failed at least one name, so a gate that is REACHABLE-BUT-BIT-NOBODY and a gate that
# is STRUCTURALLY UNREACHABLE both print nothing. That is exactly the confusion
# `growth_gate_reachability_pin` exists to prevent on the backtest path ("a fixture artifact
# indistinguishable, in the golden string, from a structurally dead gate"). So the live table answers
# "what is load-bearing" honestly and CANNOT answer "what is dead" — do not read an absent row as
# evidence a knob is inert, and do not delete a knob on that basis.
# WHAT WOULD ACTUALLY UNBLOCK PRUNING is therefore unchanged and is NOT a new instrument: it is a live
# funnel that emits a row per gate INCLUDING the zero rows. That is a small change to `funnel_lines`
# (iterate the gate list, not the `fails` keys) and it is not made here, because (#155)'s verdict —
# no knob deleted — stands on its own merits and this round ships no code.
# REVERT RULE: none — receipts only, nothing changed.
#
# (#166) 2026-08-30 — THE ZERO-ROW FUNNEL (#164) PRE-REGISTERED IS REFUSED; THE GAP IT NAMED IS CLOSED
# ANOTHER WAY. (#164) above proposed "iterate the gate list, not the `fails` keys" as a small change.
# It is not small, and it is not wanted, for two reasons found by trying to write it:
#   1. `funnel_semantics` (src/commands/screen.rs) already asserts, in those words, that "a gate
#      nobody failed must not print" — `assert!(row("cagr").is_none())`. The zero-row form does not
#      extend the funnel, it REVERSES a decision the suite states explicitly. Whatever its merits,
#      it cannot be smuggled in as a small change to a loop.
#   2. There is NO canonical list of gate names to iterate. `gate_failures` emits its `&'static str`
#      labels inline, and `ABLATED_GATES` is three GATE SWEEP row labels, not these. Writing one adds
#      a THIRD copy of the gate names. picks.rs justifies exactly ONE such mirror (`gate_failures`
#      against `score_parts`, "drift only mislabels the tail, never the rank") and pays for it with
#      `gate_failures_agrees_with_the_scorer`, which pins the two by DRIVING QUOTES THROUGH BOTH. A
#      bare name list admits no equivalent pin: a gate that no quote in the corpus fails would go
#      silently missing from it — which is EXACTLY the failure the zero rows exist to prevent. The
#      instrument would reproduce its own bug.
# WHAT SHIPPED INSTEAD, one line in `funnel_lines`: the table now states that a gate absent from it
# failed nobody in this scan and that this is not evidence its knob is inert. That is the whole of
# (#164)'s catch — an absent row was ambiguous, and now it is not — at zero drift surface. It is
# pinned directly beside the assertion in 1., so whoever relaxes that one must decide about this too.
# WHAT IT STILL DOES NOT GIVE: per-gate zero COUNTS on the live path. Deleting a knob on reachability
# evidence therefore still goes through the backtest side's `growth_gate_reachability_pin`, exactly
# as before. (#155)'s verdict — no knob deleted — is untouched, and no knob moved here either.
# REVERT RULE: drop the line and its assertion together; report-only, no golden and no pick moves.
#
# DEBT, DELIBERATELY NOT PAID HERE: growth_min_cagr 19, growth_min_range_pct 80, growth_maxdd_cap 84,
# growth_max_peg 1.6, growth_accel_weight 0.65 and every other knob receipt below were FITTED on the
# contaminated peer-mean. The king horizon barely moved so they are not presumed wrong, but they are
# no longer independently validated at 12y/8y. Re-sweep before citing a 12y/8y Δedge as evidence.
# Those same four knobs carry a SECOND, larger debt on top of this one — see PRE-PIT below. They were
# fitted on a pool that could pick a name before it joined the index, and the PIT arm cuts the top-3
# excess they were fitted to explain by more than half at every horizon.
#
# (#137) 2026-08-25 — TWO NOTES ON THE LIST ABOVE. Neither rewrites it; both stop it being miscited.
# (1) `growth_accel_weight 0.65` is the value this block was written against. The knob has shipped
#     0.50 since 2026-08-10 ((#63), confirmed by (#70) on 2026-08-11). The DEBT is real either way —
#     0.50 was fitted on the same contaminated pool — but do not quote 0.65 as the current value.
# (2) THE PIT HALF OF THIS DEBT IS DISCHARGED FOR TWO OF THE FIVE, AND THE OTHER THREE ARE GATES.
#     `growth_min_cagr` was re-swept and lost ((#135); 19.0 stands). `growth_accel_weight` was
#     re-swept and lost ((#137); 0.50 stands) — and that receipt also records why it should be the
#     LAST weight anyone re-sweeps on `pit`: the graded book holds 5.4-6.4 names per window against a
#     basket of 10, so PRIMARY is near-blind to ranking on that lane. The remaining three
#     (`growth_min_range_pct`, `growth_maxdd_cap`, `growth_max_peg`) are GATES; they move admission,
#     so unlike a weight they ARE gradeable there.
# (#138) 2026-08-25 — THE PIT HALF OF THIS DEBT IS NOW CLOSED FOR ALL FIVE. Two of the three gates
#     named above never needed an arm. Their PIT `gate_sweep` rows, read off the (#137) control runs,
#     price the loosening directly, and both are NEGATIVE where it counts:
#       `growth_max_peg` 1.6       20y admits 0 new names | 12y n=11 -156.4|-212.1 | 8y n=37 -2.0|-23.2
#       `growth_min_range_pct` 80  20y n=1 -347.0 | 12y n=1 -155.8 | 8y n=1 -150.8
#     max_peg's loosening loses at both adjudicating horizons and does not bind at 20y. min_range_pct's
#     -10 notch moves EXACTLY ONE NAME, and it loses at every horizon — that knob is not un-re-swept,
#     it is un-re-sweepABLE on this pool, the gate twin of (#137)'s finding for the weight. NEITHER
#     NEEDS A RUN; re-running them to hunt a better answer is the overfitting shape this file bans.
#     `growth_maxdd_cap` DID get the arm, and it is the one of the five that MOVED: it SHIPS OFF as of
#     (#138) — see its own block for the verdict, the stated 20y deviation and the revert rule.
#     P0b IS CLOSED. No shipped gate value now rests only on the lookahead pool.
#     SAME-DAY ADDENDUM, and it matters because it undercuts one sentence above. The rows quoted here
#     were measured with `growth_maxdd_cap` STILL ARMED. On the config (#138) actually ships — cap off —
#     the arm runs re-price every other gate for free, and two rows move:
#       `growth_max_peg` ->off      20y admits 0 | 12y n=14 -18.9|-206.5 | 8y n=42 +45.6|+12.6 TOO TIGHT
#       `growth_min_range_pct` -10  20y n=2 -278.2 | 12y n=2 -25.8 | 8y n=2 -6.3  (still negative; n=2, not 1)
#     So "max_peg loses at BOTH adjudicating horizons" is true of the PRE-(#138) config and FALSE of the
#     shipped one — its 8y row flipped positive. IT STILL DOES NOT REOPEN, for two reasons needing no
#     runs. (1) 12y reads -18.9|-206.5, hard negative, and PRIMARY needs 12y AND 8y; a loosening cannot
#     pass on one horizon alone. (2) The 8y median is +12.6, squarely in the band this file records as
#     PERISHABLE (+57.5 and +7.0 both expired), and max_peg's own block already shows this same 8y flag
#     dying twice before. Written down so the next reader finds the flag ALREADY PRICED, not fresh.
#     The ONLY other TOO TIGHT row on the shipped config is `growth_min_cagr -4` at 8y (+64.7|+61.5) —
#     the flag (#135) already converted in three runs and REFUSED, the 8y book falling +4.5 -> +3.3.
#     NET: on the config that now ships, the gate stack carries NO live lead.
# ============================ SHIP RULE v2 — 2026-08-06 =====================================
# THE PRIMARY METRIC CHANGES FROM rank-1 TO top-3, AND EVERY RECEIPT BELOW PREDATES THE CHANGE.
#
# (#120) 2026-08-22 — THE GRADED BASKET MOVES top-3 -> top-10, AND EVERY RECEIPT IN THIS FILE THAT
#   QUOTES A "top-3" COLUMN WAS GRADED UNDER THE OLD BASKET. Those columns are LEFT AS WRITTEN on
#   purpose: they record what was measured on the day, and rewriting them would forge history. Read
#   any pre-2026-08-22 receipt as "top-3, the basket then shipped". What this DOES mean is that a
#   refusal resting on "it only pays at top-10" no longer rests on anything — `growth_value_floor_pct`
#   (#75) is the one such case in this file, and it is flagged RE-GRADEABLE at its own entry. A
#   refusal that recorded its arm ordering "at every basket size" (#71) survives the move untouched.
#   (#126) 2026-08-23 — THAT RE-GRADE IS DONE and `growth_value_floor_pct` now SHIPS 40.0. The basket
#   move is what made it visible, so (#120) is the reason this knob is on; read the two receipts
#   together. No other refusal in this file was re-opened by the basket move.
#
# PRIMARY AND THE FUNDAMENTAL BLACKOUT — read before grading any knob that reads company financials.
#   PRIMARY says "20y AND 8y". No 20y cutoff carries a `peg_yield` or any other as-of fundamental:
#   SEC XBRL starts 2007 and 20y cutoffs end ~2006. (#71) proved it by ablation, (#75) by a 70% cut
#   that removed ZERO names, and (#126) reproduced it on the PIT pool with control and reject-70
#   byte-identical on every field. So EVERY fundamental knob discharges the 20y half of PRIMARY
#   VACUOUSLY and reads "held at 20y" for free. That is not evidence. (#126) is the worked precedent
#   for what to do about it: substitute 12y for the blind horizon and require the rise at 8y AND 12y,
#   which is STRICTER than the letter, and state the substitution in the receipt as a deviation.
#
#
# (#139) 2026-08-25 — PIT-LANE AMENDMENT TO PRIMARY. Same shape as the blackout rule above, different
#   cause. On the `pit` lane the 20y column cannot adjudicate a MEAN: (#124) puts its n_eff at ~0.2,
#   and the cohorts a knob moves there are routinely n=3..10 against 20 windows. Four receipts have
#   now graded PIT this way rather than by the letter — (#126), (#135), (#137), (#138) — and (#138) is
#   the first that SHIPPED on the substitution instead of refusing anyway. Written down as a rule so
#   it stops being re-argued from scratch every round.
#     ON `pit`:  PRIMARY is top-10 excess (mean AND median) up or held at 12y AND 8y.
#     ON `pit`:  EVERY 20y GUARD STAYS LIVE — rank-1 h2h >= 50% at 20y, and 20y worst window not worse.
#     WIDE and `stress` lanes are UNCHANGED: PRIMARY stays 20y AND 8y there.
#   WHY THE GUARDS SURVIVE WHAT PRIMARY CANNOT. A mean over n=3 is noise; a WORST WINDOW is a MAX and
#   an h2h is a COUNT, and both stay answerable at that n. That is the entire content of this
#   amendment — it drops the horizon only where the statistic is a MEAN, and nowhere else.
#   THIS RAISES THE 12y BAR, IT DOES NOT ONLY LOWER THE 20y ONE. By the letter an arm needed 20y and
#   8y and could be FLAT at 12y; under this rule 12y must carry the rise. Same reasoning (#126) used,
#   and (#126) called its own version STRICTER than the letter for exactly this reason.
#   NOT A LICENCE TO DROP AN INCONVENIENT HORIZON. It applies to `pit` alone, it must be pre-registered
#   BEFORE the runs as every receipt using it did, and the 20y numbers are still reported in full.
#   TO REVERT: delete this block and re-grade (#138) at 20y — that arm reads +0.8|+0.9 -> +0.6|+0.7
#   there, a TIE under the letter, so reverting this rule reverts that ship. The two stand together.
#
# (#185) 2026-08-31 — STRUCTURAL-INERTNESS AMENDMENT TO PRIMARY. Fourth cause, same shape as (#139)
#   and (#149), and PRE-REGISTERED BEFORE ANY RUN of the arm it exists to adjudicate.
#   THE HOLE IT CLOSES. (#149) says a column whose median bucket is saturated cannot adjudicate a
#   RANKING knob, and must be recorded VACUOUS rather than "held". There is a second way a column can
#   fail to adjudicate, and (#178) hit it: the ruler is fine and the pool is fine, but THE KNOB IS
#   NOT CONNECTED. `growth_value_weight` at 20y on `stress` is the case — the run reports
#   `earnings_yield n/a (only 0 cutoffs carry this factor)`, `pe_ratio` is filled only from
#   earnings_yield, so `value_factor` is identically 1.0 and EVERY arm returns the control's numbers.
#   READ BY THE LETTER THAT IS A PASS, AND THAT IS THE BUG. PRIMARY is "up or HELD at 20y AND 8y".
#   A disconnected knob is held at 20y by construction, on every arm, forever. So the letter lets a
#   favourable 8y rung ship on a 20y leg that was never a test. This amendment refuses that.
#     ON `stress`, FOR A KNOB WITH DOCUMENTED STRUCTURAL INERTNESS AT 20y:
#       the 20y PRIMARY leg is VACUOUS, never "held" — exactly (#149)'s word, for the same reason.
#       PRIMARY is therefore top-10 excess (mean AND median) up or held at 12y AND 8y.
#       EVERY 20y GUARD STAYS LIVE. A worst window is a MAX and an h2h is a COUNT; neither is
#       computed from the dead factor, so both still answer and both still refuse.
#   THIS RAISES THE BAR, IT DOES NOT LOWER IT, and that is the whole reason it can be written after
#   (#178)'s favourable 8y number is already known. By the letter an arm needed only a trivially-held
#   20y plus a good 8y. Under this rule 12y must ALSO carry a real rise, on a column where the factor
#   IS present. Nothing that would have passed before fails to pass on a technicality; things that
#   would have passed vacuously now have to earn it. A rule that makes the arm harder is not a bar
#   moved to fit a result.
#   EVIDENCE REQUIRED, IN THE RECEIPT, QUOTED FROM THE RUN — not asserted. A receipt invoking this
#   amendment MUST carry the `only 0 cutoffs carry this factor` line (or its equivalent for whatever
#   factor is dead) for the horizon it drops. Absent that line the amendment does not apply and
#   PRIMARY reads 20y AND 8y as written. "I think it is inert" is not evidence; the run saying so is.
#   NOT A LICENCE TO DROP AN INCONVENIENT HORIZON. It applies to `stress` alone, only to a knob the
#   run itself proves disconnected on that column, it must be pre-registered BEFORE the runs, and the
#   20y numbers are still reported in full. A knob that is merely WEAK at 20y is not inert — weak is
#   a result and it counts against the arm.
#   SURVIVOR BIAS IS NOT WAIVED. `stress` reaches its fat h2h denominators through non-point-in-time
#   membership ((#178)); this amendment changes which HORIZONS adjudicate, and changes nothing about
#   that price. A ship under this rule carries the survivor-bias caveat in its own receipt.
#   TO REVERT: delete this block. NOTHING HAS SHIPPED UNDER IT, AND ON THE EVIDENCE NOTHING WILL —
#   (#185)'s own arms were graded under it and REFUSED (receipt at `growth_value_weight`). The
#   amendment did its job in the strict sense: it stopped a trivially-held 20y leg from carrying a
#   ship, and applying it honestly then exposed that 12y on `stress` is ALSO vacuous, by (#149)
#   saturation, leaving one adjudicating column where PRIMARY needs two. A rule that refuses the very
#   arm it was written for is working, not failing. KEEP THE BLOCK: the reasoning is what is reusable
#   — a knob disconnected from the ruler must never be scored as "held" — and the next structurally
#   inert knob is entitled to the same treatment without re-arguing it from scratch.
#
# (#149) 2026-08-29 — VACUITY AMENDMENT. Same shape as (#139) again, and the third cause: a column
#   that CANNOT MOVE cannot adjudicate either. PRIMARY grades an EQUAL-WEIGHT top-10 basket, which is
#   SET-invariant — reordering names INSIDE the basket moves not one number it prints. So a RANKING
#   knob is only ever measured through names that CROSS the basket boundary, and a bucket whose gated
#   pool is no larger than the basket has NOBODY OUTSIDE TO CROSS IT.
#   Every backtest run now prints a POOL CENSUS line before the top-N table, and tags the column
#     VACUOUS  when the MEDIAN bucket is saturated (pool <= basket) — i.e. a strict majority of
#              graded buckets hold no name outside the basket at all.
#   ON A VACUOUS COLUMN: a RANKING knob is recorded VACUOUS and NEVER "held". A knob that cannot move
#   a column has not passed it; it was never tested. Exactly as (#139) splits by STATISTIC, this
#   splits by WHAT THE KNOB TOUCHES: gates and anything else that CHANGES THE POOL still answer (they
#   move which names exist, not their order), and so do MAX/COUNT statistics on the pool — the worst
#   window and the rank-1 h2h, which `rank_slice_stats` reads off the untrimmed cohort.
#   WHY IT IS A DEFINITION AND NOT A FITTED THRESHOLD, which matters because (#120) forbids selecting
#   a basket off the ladder it is graded on: `pool <= basket` is arithmetic, and "the median bucket"
#   is the same one-line rule (#139) already uses. NO BASKET MOVES. `VERDICT_TOP` stays 10 and no pick
#   changes — re-selecting a rung because it peaks would be (#120)'s forbidden argmax a second time,
#   on the same ladder and the same data. This is a VALIDITY test, not a fitting one.
#   WHAT IT COSTS: four rounds that banked a "held" now read VACUOUS instead — (#144), (#145) and
#   (#147) x2 all graded ranking knobs on this column. None of them SHIPPED on that pass, so no
#   shipped value moves; what changes is that their passes stop counting as evidence. (#147)'s own
#   receipt already carried the warning and (#126)/(#145) named the standard; this makes the tool say
#   it instead of a reader having to notice.
#   MEASURED: on the frozen fixture EVERY lane is vacuous (12y, 8y, 20y, pit, stress — median pool 1
#   or 2 against a basket of 10), which is the first thing this rule establishes: the fixture cannot
#   adjudicate a ranking knob AT ALL, and no receipt should ever have cited it for one.
#   TO REVERT: delete the `basket_vacuity` call in `report_vs_benchmark` and re-bless the goldens.
#   Nothing downstream reads the tag programmatically — it is a report line and a grading rule.
#
#   PRIMARY   top-10 excess (mean AND median) up or held, at 20y AND 8y
#   GUARD     rank-1 h2h >= 50% at 20y, 12y and 8y   (winner's-curse guard, NOT the judge)
#   GUARD     top-10 worst window not worse
#   TIE       horizons disagree -> UNRESOLVED -> NO SHIP. "20y king overrules" is RETIRED.
#   PIT LANE  on `pit` ONLY, PRIMARY and TIE read 12y AND 8y, not 20y AND 8y — see (#139) just above.
#             Every 20y GUARD stays live. The wide and `stress` lanes are unchanged.
#   VACUITY   a column the POOL CENSUS tags VACUOUS cannot adjudicate a RANKING knob — record
#             VACUOUS, never "held" (#149). Gates and the worst window still answer there; the
#             rank-1 h2h answers off the NON-SATURATED buckets only — check its n (#151).
#
#   PRECEDENCE IS GUARD > PRIMARY > TIE, and it is not a formality. A broken guard REFUSES outright;
#   it is never softened into "unresolved" because the primary happened to split. `fixed_cagr_years: 5`
#   is the worked case: its top-3 medians split by horizon (20y +0.1, 8y -0.6) which reads like a tie,
#   but its rank-1 h2h fell to 48/50/50 — the argmax stopped beating ranks 11-20 — so it is REFUSED,
#   full stop. Apply the three tiers in order and stop at the first that fires.
#   "UNRESOLVED" ALSO REQUIRES A REAL DISAGREEMENT: one horizon UP and the other DOWN. A flat horizon
#   is not a disagreeing one, so an arm that is flat at 8y and up at 20y but loses the primary is a
#   plain NO-SHIP (see `fixed_cagr_years: 8`). The three states carry different follow-ups —
#   UNRESOLVED invites a better arm, NO-SHIP and REFUSED close the question — so do not blur them.
#
# (#120) WHY THE BASKET MOVED FROM 3 TO 10 — MEASURED 2026-08-22 AND SHIPPED.
#   THE DEFECT, and it is structural rather than empirical: `VERDICT_TOP: 3` was the ARGMAX over
#   `TOP_LADDER`'s 13 rungs, taken on the same wide run the report then quotes, with no best-of-N
#   haircut, on the 0.5-3.5 effectively independent windows (#91) counts. Its own doc comment said so
#   and the (#92) receipt below called it "THE DEBT" in those words. Worse, the argmax was over a MEAN,
#   and under positive skew small N maximises the mean while wrecking the median and the left tail —
#   so that selection picks the most concentrated book almost by construction. Meanwhile candidate
#   knobs were being COMPARED on `legacy_top()` = 10 as "the wider, better-estimated book". The tool
#   compared on 10 and shipped on 3.
#
#   WHY 10 AND NOT A FRESHLY FITTED NUMBER. Re-running the ladder and taking a new argmax reproduces
#   the same selection bias with a newer date on it — (#92) says this explicitly and it is still right.
#   Re-deriving 3 off the median or the 10th percentile instead would be the same selection wearing a
#   different statistic. 10 is FIXED A PRIORI: it is the basket every candidate knob in this file was
#   already compared on, so the change REMOVES a degree of freedom rather than re-spending it. No
#   ladder run chose it.
#
#   THE PRICE, PAID IN FULL AND ON THE RECORD (`backtest {20,12,8} universe`, wide, 2026-08-22):
#     horizon   top-3 mean|med   top-10 mean|med   win 3 -> 10   worst 3 -> 10   windows
#       20y      +6.3 | +5.6      +6.0 | +6.1       97% -> 97%    -0.5 -> -0.5      36
#       12y      +6.8 | +6.9      +6.2 | +6.1       84% -> 88%    -7.8 -> -7.8      51
#        8y      +7.5 | +8.3      +6.6 | +7.3       80% -> 85%    -9.2 -> -9.2      59
#   Top-10 gives up 0.3-0.9 pts/yr of MEAN excess at every horizon. It buys a higher win rate at the
#   two horizons where the rate could move (12y +4pts, 8y +5pts) and an IDENTICAL worst window at all
#   three — so the rule's worst-window GUARD does not bind in either direction, which is the cleanest
#   possible reading of a basket swap.
#   AND AT 20y THE MEDIAN CROSSES THE MEAN: top-3 is +6.3 mean / +5.6 median, top-10 is +6.0 / +6.1.
#   The concentrated book wins on the mean and LOSES on the median at the longest horizon. That is the
#   skew mechanism above, measured rather than argued, and it is the strongest single line of evidence
#   that the old argmax was reading a selection artifact and not an edge.
#   AND IT REPRODUCES ACROSS TEN DAYS AND A DIFFERENT RUN. The (#75) control ladder, measured
#   2026-08-12 on the 20y fund arm for an unrelated question, printed "top-3 +6.3|+5.7, top-10
#   +5.9|+6.1" — the same crossing, to a tenth, from a run nobody made to settle this. Two independent
#   measurements of the 20y mean/median inversion is not a fluke of one seed.
#
#   WHAT MOVED IN THE TREE: `VERDICT_TOP` 3 -> 10; `markers::VERDICT_ROW` "top-3 " -> "top-10 " with
#   `verdict_row_matches_the_basket` asserting the two can never drift; the 7 `top-3` parses in
#   tests/network.rs; the 5 fixture goldens' `all entries (top-N)` row; and the best-of-13 caveat came
#   OFF the screen footer, since a basket fixed a priori is not a maximum over anything.
#   THE GOLDENS MOVED ON PURPOSE — this is a correction, not a knob, and non-negotiable #1 does not
#   apply. Fixture deltas (top-3 -> top-10): 20y +3.3 -> +3.2, 8y +4.9 -> +5.2, 12y +1.1 -> +0.9,
#   12y-pit unchanged at -2.4. The 200-name fixture disagrees with the wide run about the SIGN of the
#   8y change; the wide run is the evidence, the fixture is only a byte-level pin.
#   REVERT RULE: set `VERDICT_TOP` back to 3 and re-bless. Do it only for a reason that is not "top-3
#   scores higher" — that sentence is the defect, not the argument.
#
# WHY. The old primary, "rank-1 median at the 20y king", grades a ONE-STOCK portfolio. Nobody holds
# one stock; the money goes into a basket. `backtest` already prints top-N for N=1..50, so this costs
# no code — only the discipline to read the row that matches the policy.
# THE YARDSTICK WAS CARRYING THE VERDICTS. Re-grading the 2026-08-06 fixed_cagr_years grid on top-3
# instead of rank-1, same files, zero new runs (20y stress, median excess pts/yr):
#     arm        top-3 med   rank-1 med   worst window t3 / r1
#     control       +6.6        +6.4          -0.5 / -10.6
#     pin 8         +6.9        +4.6          -1.0 / -10.6
#     pin 10        +6.9        +6.0          -0.5 / -10.6
#     pin 5         +6.7        +4.0          -3.8 /  -7.5
#   Rank-1 spreads those arms over 2.4 pts and refused all three the same morning. Top-3 spreads them
#   over 0.3. THE FAILURE WAS CONDITIONAL ON BUYING ONE NAME. Nothing about the arms changed.
# AND top-3 IS THE BETTER-MEASURED SLICE, not merely the friendlier one: rank-1 is ONE name per
#   window, the noisiest cut in the grid. Its worst window is roughly DOUBLE top-3's at every horizon
#   (-10.6 vs -0.5 at 20y, -19.9 vs -7.8 at 12y, -19.2 vs -9.2 at 8y), and top-1 also WINS LESS OFTEN
#   (87/74/76% vs 97/87/82%). Concentrating into the single argmax was never the better book.
# RANK-1 IS DEMOTED, NOT DROPPED. It exists to catch a rotten argmax hidden behind two good names —
#   the (#3k) winner's curse. That is why its h2h stays MANDATORY. A top-3 that passes while rank-1
#   h2h sits under 50% is an inverted ranking wearing a basket as a disguise.
# "20y KING OVERRULES" IS RETIRED, and it should never have been the tie-break: the 20y lane is the
#   BLURRIEST in the grid. Its 90% bootstrap band is +/-78% of its own estimate on 21 h2h pairs;
#   8y is +/-40% on 38 pairs. Structural, not fixable — a 20y forward window needs data from 20y ago,
#   so there are simply fewer of them. Ties now read UNRESOLVED. See `growth_proximity_weight` and
#   `long_trend_cap`, both decided by the retired rule against contrary 12y/8y evidence.
# DISCLOSURE, because the order of events matters: the re-grade above was run BEFORE this rule was
#   written, so `fixed_cagr_years: 10` was already known to improve under it. Choosing a yardstick
#   after seeing whom it favours is the classic overfit path. The defence is that the justification
#   is STRUCTURAL — nobody holds one stock, and that argument stands with the numbers hidden — but
#   the peek is recorded here rather than left for someone to discover.
# WHAT THIS RE-OPENS: every knob below was graded on rank-1, so every rung is re-openable in
#   principle. NOT re-opened wholesale — that is the maximum-overfit move. Re-test one arm at a time,
#   on this rule, and record the UNRESOLVED verdicts too so nobody re-runs them.
# LIMITATION FOUND 2026-08-11 (#71) — THE PRIMARY CANNOT GRADE A FUNDAMENTAL KNOB. It requires 20y AND
#   8y, and at 20y there is no fundamental data at all: all 17 standalone factor probes report zero
#   cutoffs, and the growth_fund_weight ablation reads D+0.00 rho / D+0.0 edge, exactly inert. Twelve
#   runs of `backtest 20 universe fund` across four weights came back byte-identical down to the OOS
#   split. So growth_fund_weight, growth_fund_factor, growth_fund_cap, growth_max_peg,
#   growth_require_peg and quality_weight ALL read "held at 20y" for free, and the primary collapses to
#   a single horizon without saying so. A fundamental arm that "passes" has passed on 8y alone.
#   This is structural and not a bug to fix: a 20y forward window needs cutoffs from ~2005 and the SEC
#   XBRL company-facts era begins later. Grade fundamental knobs on 12y AND 8y, state that the 20y half
#   is unmeasured rather than held, and require the result to survive a basket-size check (top-1/3/5/10)
#   in place of the horizon agreement the rule normally gets. See the (#71) receipt on
#   growth_fund_weight for the worked case.
# THE BUY SURFACE NOW QUOTES THE SAME BASKET (backtest.rs `VERDICT_TOP = 10`). The screen's method
#   footer used to cite top-10 at whatever horizon ran LAST, because `.backtest_verdict.json` held one
#   row and every wide run overwrote it — so a `backtest 8` erased the 20y proof and the footer changed
#   horizon silently. The journal is now keyed by hold-years and MERGES, the footer cites the graded
#   basket at the LONGEST row on file, and it names that basket. If a rule change ever moves the graded
#   basket, move VERDICT_TOP with it or the footer starts advertising a book the rule no longer grades.
#   (#120) DID EXACTLY THAT MOVE, and the pin that makes forgetting impossible is now mechanical:
#   `verdict_row_matches_the_basket` asserts `markers::VERDICT_ROW == format!("top-{VERDICT_TOP} ")`,
#   so the CI gate's parser and the shipped basket cannot drift apart silently again.
# SURVIVORSHIP IS UNCORRECTED IN THIS RULE'S PRIMARY METRIC, AND `stress` DOES NOT FIX IT (2026-08-07).
#   `STRESS_TICKERS` (commands/backtest.rs) is 27 hand-picked crashed/bankrupt names against ~4900
#   survivors — 0.55% of the pool — and it is worth knowing exactly where that correction lands.
#   Clean same-config A/B: the CI gate shells the wide run with the SAME `FOLIOMAN_CONFIG` this file is,
#   differing from the grid ONLY in the `stress` keyword. 12y top-3 held book:
#     with stress     +15.2 book | +7.3 excess | win 87% | worst -7.8 | 54 windows
#     without stress  15.1629    | 7.2584      | 87.037% | -7.7544    | 54 windows
#   Injecting every corpse moves the graded number by less than 0.05pp on every statistic. The run says
#   why in its own output: top-3 "rode 0 zeros/138 holds" while top-10 rode 1/387 (NOK, an injected
#   corpse). Corpses score BADLY — that is the gates working — so they land in the bottom half, widen
#   the top-half-minus-bottom-half LANE EDGE, and never enter the book.
#   CONSEQUENCE, STATED WHERE THE RULE IS STATED: `stress` grades the lane, not the basket. The top-3
#   number this rule ships on — and that the screen footer quotes — carries FULL survivorship bias.
#   Do not cite a `stress` run as if it had corrected the held book; it corrected a different metric.
#   PARTLY FIXED SINCE, and the paragraph above is kept because it named the fix correctly: point-in-
#   time index membership landed 2026-08-19 as the `pit` keyword, and the PRE-PIT block below carries
#   what it measured. Read the two together — `stress` still does not correct the held book, and `pit`
#   corrects only the MEMBERSHIP half of the bias, never the price-availability half.
# HORIZONS ARE NOT COMPARABLE TO EACH OTHER — a cross-horizon CAGR gap is an ERA artifact. A 20y hold
#   can only START 1971..2006-07 (it needs 20 years of future left); an 8y hold starts 1971..2018-07.
#   Those extra 12 years of start dates cover the 2010s run, which is why top-3 prints +16.1%/yr at
#   8y against +13.7%/yr at 20y. It is NOT a shorter-hold premium. The 8y lane's own edge-by-era
#   DECLINES (+46.6 -> +23.5 -> +22.3 -> +14.8), so the richer recent book is market, not method.
#   Compare an arm to its control WITHIN a horizon. Never compare horizons to pick one.
# ============================================================================================
#
# ================= PRE-PIT — POINT-IN-TIME MEMBERSHIP LANDED 2026-08-19 =====================
# EVERY RECEIPT IN THIS FILE WAS MEASURED WITHOUT POINT-IN-TIME INDEX MEMBERSHIP, AND IS THEREFORE
# `PRE-PIT`. Every Δedge, Δexcess, h2h and worst-window figure below means "measured against a pool
# of names that were S&P 500 members ON THE DAY THE SWEEP RAN", never "against the index as it stood
# at the cutoff being scored". A 2003 cutoff could pick a name that did not join until 2015.
#
# `backtest <h> universe fund pit` scores each cutoff against the index AS IT STOOD THAT DAY. Six
# runs, 2026-08-19, this exact config; each PIT arm's control is the identical command without the
# `pit` keyword, so the ONLY thing that differs is which names were eligible to be picked:
#
#     horizon  arm    top-3 excess mean (med)   rank-1 mean|med   rank-1 h2h     windows
#     20y      base        +6.3 (+5.6)             +6.4|+7.2      67% (12/18)       36
#     20y      PIT         +0.1 (+0.9)             -3.3|-0.1      50%  (1/2)        20
#     12y      base        +6.8 (+6.9)             +6.9|+7.6      62% (16/26)       51
#     12y      PIT         +2.6 (+3.4)             +0.6|+4.1      50%  (2/4)        34
#     8y       base        +7.1 (+7.4)             +7.6|+8.2      65% (22/34)       59
#     8y       PIT         +4.7 (+4.9)             +4.6|+6.2      56%  (5/9)        42
#
# MOST OF THE MEASURED EDGE WAS LOOK-AHEAD MEMBERSHIP. Top-3 excess falls 6.2 / 4.2 / 2.4 pts/yr and
# rank-1 goes NEGATIVE at 20y. Nothing was tuned between the two arms of any pair — this is what
# removing the bias costs, and it is the successful outcome of measuring it rather than a regression.
# The gradient across horizons is the tell: the longer the hold, the more of the "edge" was the
# heuristic being handed names whose admission to the index it could not have known about.
#
# PIT CORRECTS MEMBERSHIP, NOT SURVIVORSHIP, AND THE TWO ARE NOT THE SAME HOLE. The 20y PIT run
# reports 387 pool names that were index members Yahoo no longer serves: they fetched nothing and
# scored nothing. So the PIT pool is "members at the cutoff that a data vendor still prices today" —
# no longer look-ahead on WHO WAS IN, still blind to WHO DIED. The `stress` note above stands
# unchanged; neither run corrects the other's half.
#
# (#121) 2026-08-22 — THAT 387 IS NOW 0, AND NOTHING WAS FIXED. Re-running `backtest {20,12,8} universe
#   pit` three days later, the miss count above prints ZERO at every horizon. The hole did not close;
#   it went INVISIBLE. Yahoo now answers for those names — with a payload carrying ZERO BARS. Census of
#   the long history cache against all 1206 members: every one of them HAS a cache entry, so the miss
#   count is structurally 0. Of the 734 members whose span carries an exit date, 387 are served with
#   ZERO BARS; of the 347 that do carry bars, 193 hold fewer than 36 inside their own membership span,
#   leaving 154 scoreable. THAT 387 IS EXACTLY THE COUNT THIS CAVEAT REPORTED ON 2026-08-19 — not close
#   to it, equal to it. The names that used to fetch nothing now fetch an empty answer, one for one:
#   `BSC`, `TWX`, `SVU`, `ROH` and `ENRNQ` all come back as a `MUTUALFUND` stub with a numeric
#   shortName. A served-but-empty name counts as SERVED, so a metric built on "did the fetch answer"
#   reads a warm cache as a clean bill of health. THIS IS WHY A MISS COUNT WAS THE WRONG INSTRUMENT.
#
#   WORSE, THE SERIES THAT DO CARRY BARS DO NOT END WHEN THE COMPANY DID. Count of members, out of all
#   1206, whose series ends more than a year before today: ZERO. `CFC` (Countrywide, out of the index
#   2008-07) carries bars through 2026-08; `MOLX` (acquired 2013) through 2026-07. `LEH`, `WCOM`, `CC`
#   and `ABK` are not in the membership map at all. So this dataset contains no delisting events of any
#   kind, and Bessembinder's median delisted return of -91.95% — which docs/heuristic-v2-spec.md builds
#   its case on — is absent from every number this repo has ever printed. A name that dies simply stops
#   contributing samples, which biases the edge UP by an amount nothing here can size.
#
#   WHAT SHIPS INSTEAD: a COVERAGE RATIO in the PIT caveat — of the index members this pool asked
#   about, how many produced a scoreable cutoff. Measured live, `backtest {20,12,8} universe pit`:
#       20y  291 of 1206  (24%)      12y  399 of 1206  (33%)      8y  481 of 1206  (40%)
#   READ IT WITH ITS CONFOUND, WHICH THE PRINTED LINE ALSO STATES: the ratio has TWO drivers and cannot
#   split them. A {h}y hold needs a full {h}y forward window, so members that joined late are
#   unscoreable for a MECHANICAL reason that is not bias — and that is why the ratio climbs 24 -> 33 ->
#   40 as the horizon shortens. Survivorship is the part that does NOT shrink with a shorter hold.
#   DO NOT quote 76% as "the survivorship hole at 20y". The defensible claim is narrower and still
#   damning: at the 20y horizon three quarters of index membership never reaches the tables, the
#   instrument that was supposed to flag that reports 0, and the 703-member census says a large share
#   of the gap is names that died.
#
#   THIS IS A PRINT, NOT A CORRECTION. It sizes the problem so the far more expensive fix — closing a
#   dead name's forward window at a terminal return, sourced from delisting DATES, since the prices
#   provably do not exist to fetch — can be decided on a number instead of a hunch. It moves exactly
#   one golden (backtest-12-pit) and no other output.
#
# WHAT A `PRE-PIT` MARK OBLIGES. Not a wholesale re-open — re-sweeping every rung against a pool this
# much thinner is the maximum-overfit move, and the receipts below are still the honest record of what
# was measured. What it forbids is one specific claim: no figure below may be cited as evidence that a
# knob holds ON THE PIT POOL, because none of them was measured there. Cite them for what they are.
#
# AND THE PIT POOL COULD NOT SETTLE THE SHIP RULE AS WRITTEN. The rank-1 h2h GUARD demands >= 50% at
# 20y, 12y AND 8y. On the PIT pool those three comparisons rest on 2, 4 and 9 window pairs, and two of
# the three land EXACTLY on the 50% boundary — a guard that one window flip decides is not a guard. Any
# future rule that grades on the PIT pool needs a guard whose sample size it can state, and this is
# the measurement that says so rather than a prediction that it might.
#
#   (#135) 2026-08-24 — THAT GUARD NOW HAS A SAMPLE SIZE, AND THE CAUSE WAS NOT THE PIT POOL.
#   The shipped h2h compares #1 to the 11-20 book, so it needs a window holding >= 11 admitted names.
#   (#124) had already measured that the gates admit no more than ~10. The guard was starved by the
#   very gate values it exists to revalidate — the PIT pool only made it visible. Re-based onto the
#   6-10 book, which a ~10-name window can answer, the PIT denominators go 2/4/6 -> 8/18/26.
#
#   THE RE-BASING IS A DENOMINATOR FIX, NOT A SOFTENING, and that was pre-registered before any run:
#   it counts only if the 6-10 book reproduces the SAME verdict wherever both books are computable.
#   All 12 comparisons agree (rank-1 and the 2-5 book, two lanes, three horizons). Full table and the
#   revert rule at `print_h2h_mid_ladder`. So: WHERE THE 11-20 BOOK EXISTS, IT STILL DECIDES. Where a
#   window cannot hold it, the guard reads the 6-10 book instead of going uncomputable.
#
#   (#135) LIMIT, 2026-08-24, ADDED THE DAY THE RE-BASING WAS MADE — P0b arm 1 broke the agreement
#   at 20y (6-10 reads 46%, 11-20 reads 50%), so the pre-registered revert rule fired and THE
#   RE-BASING NOW HOLDS AT 12y AND 8y ONLY. The 20y rank-1 h2h on the PIT pool is uncomputable
#   again, which is honest: (#124) prices 20y at n_eff 0.5 live and ~0.2 on PIT, so that column
#   could never adjudicate between two knob values anyway. Full record at `print_h2h_mid_ladder`,
#   including why "just prefer the bigger denominator" is the one repair that is NOT available.
#
#   WHAT IT DOES NOT LICENSE: 8 windows at 20y is still ~0.2 effective trials (#124). The guard is now
#   COMPUTABLE at 20y on PIT, not decisive there. PIT knob-grading belongs at 12y and 8y.
# ============================================================================================
#
# (#151) 2026-08-29 — C (WIDEN THE POOL): the gate half MEASURED and REFUSED, the coverage half
#   already answered, and one amendment to the (#149) VACUITY rule at the top of this header.
#
#   AMENDMENT — THE rank-1 h2h IS NOT EXEMPT THE WAY (#149) SAID. (#149) exempts "MAX/COUNT
#   statistics on the pool — the worst window and the rank-1 h2h". The worst window is fine: it is a
#   MAX over every graded name and saturation cannot touch it. The h2h is NOT. `h2h_beats(vv, 10, 20)`
#   returns None exactly when `vv.len() <= 10`, and VERDICT_TOP is 10, so the GUARD's denominator is
#   IDENTICALLY the census's non-saturated bucket count. Not an approximation — the same predicate:
#     12y ctl  35 buckets, 31 saturated -> h2h n=4 .  Printed: "#1 beat the 11-20 book in 2/4".
#      8y ctl  43 buckets, 35 saturated -> h2h n=8 .  Printed: "5/8".  Exact on all 6 grid runs.
#   So on a saturated column the h2h is a COUNT OVER THE RESIDUE. It still ANSWERS (n>0, and >=50% is
#   an absolute bar, not a delta, so it passes on its own terms), but at n=4 it has no power, and a
#   reader who treated (#149)'s exemption as "unaffected" would be wrong. The census line now prints
#   the denominator beside the saturation count so nobody has to subtract, and
#   `the_h2h_denominator_is_the_unsaturated_bucket_count` pins the identity — if VERDICT_TOP ever
#   moves off the 10 hardcoded in `rank_slice_stats`'s SLICES that test FAILS, and the census must
#   stop claiming the identity rather than be re-blessed around it.
#   NOTE the remedy already exists and ships OFF: `print_h2h_mid_ladder` (#135) tallies the same
#   question against the 6-10 book, which a ~10-name pool CAN answer. Not turned on here — that is a
#   grading-rule change and wants its own round — but it is where a thin-pool GUARD should go.
#
#   THE BAR THE GATE SWEEP WAS USING WAS THE WRONG BAR. The sweep loosens each gate one notch and
#   grades what it NEWLY ADMITS, flagging TOO TIGHT only when mean AND median are positive on `fwd
#   peer-relative`. That is an ABSOLUTE bar of zero. But the SHIPPED accepted cohort sits at
#   median -73.2 (12y pit, n=249) — peer-relative forward return is strongly negative for EVERY
#   cohort, including the one the gates already keep. Measured against zero, essentially nothing can
#   ever flag, and four rounds of "every gate reads negative, loosening imports junk" was reading a
#   miscalibrated instrument. Against the RIGHT baseline:
#     growth_min_cagr -4          n=151  med  -55.6   vs shipped +17.6  <- admits BETTER than the book
#     growth_min_leg_years ->2    n=68   med  -73.0   vs shipped  +0.2  <- admits EQUAL to the book
#   The sweep now prints `vs shipped` (newly-admitted median MINUS the accepted median) and flags
#   ADMITS NO WORSE THAN THE BOOK. `gate_audit` returns the accepted median for it; None is never
#   faked as 0.0.
#
#   AND THEN THE GRADED RUN REFUSED BOTH ANYWAY. `backtest {12,8} universe fund pit`, 6 runs, one
#   knob each, control through the same path. `windows scored` identical within horizon (2737 @12y,
#   6126 @8y) — same dataset, only the gated pool differs.
#     12y      scored  census                     top-10 excess   worst   rank-1 h2h    rank-1 mean
#     ctl        249   35 bkt 31 sat  16 subst    +3.7 | +3.9     -6.4    2/4  (50%)    +1.8 | +5.1
#     cagr 15    400   36 bkt 22 sat  81 subst    +2.9 | +4.0     -6.4    9/14 (64%)    -0.1 | +3.6
#     leg 2      317   36 bkt 29 sat  38 subst    +2.7 | +2.7     -6.4    5/7  (71%)    -0.3 | +3.1
#      8y      scored  census                     top-10 excess   worst   rank-1 h2h    rank-1 mean
#     ctl        355   43 bkt 35 sat  21 subst    +5.3 | +5.6     -5.5    5/8  (62%)    +5.6 | +6.5
#     cagr 15    690   44 bkt 22 sat 243 subst    +4.1 | +3.8     -3.5   16/22 (73%)    +4.7 | +5.9
#     leg 2      447   44 bkt 32 sat  63 subst    +4.6 | +4.5     -2.8    9/12 (75%)    +4.6 | +5.9
#   GUARDS ALL HOLD. h2h >= 50% at 12y and 8y on every arm, and it RISES on both (50->64/71,
#   62->73/75). Worst window not worse: identical at 12y, strictly BETTER at 8y (-5.5 -> -3.5/-2.8).
#   20y not run: the ladder is not PRIMARY-clean at 12y and 8y, and (#124) puts pit 20y n_eff at ~0.2.
#   PRIMARY FAILS. Top-10 excess needs mean AND median up or held; the MEAN falls at both horizons on
#   both arms (12y +3.7 -> +2.9/+2.7, 8y +5.3 -> +4.1/+4.6). GUARD > PRIMARY > TIE, no guard broke,
#   so PRIMARY decides: NO SHIP. Both gates stay at 19.0 and 5.0.
#   THIS VERDICT IS REAL, NOT VACUOUS, and it is the first substantive adjudication in five rounds.
#   These are POOL knobs, which (#149) explicitly exempts from the vacuity rule, so the fact that
#   five of the six columns are still tagged VACUOUS does not void them.
#   THE ONE THING THAT DID WORK: widening the pool CURES the vacuity. `growth_min_cagr 15` at 8y is
#   the FIRST NON-VACUOUS graded column this heuristic has produced — 22 of 44 buckets saturated is
#   exactly half, not the strict majority the tag needs, and 243 substitutable names against 21 on
#   the control. If a future round needs a column that can grade a RANKING knob, this is the lever;
#   it costs -1.2 pts/yr of top-10 mean at 8y to buy it, and that price must be paid deliberately and
#   pre-registered, never discovered after the fact.
#   HOW BOTH CAN BE TRUE — better admits, worse book. The sweep grades the newly-admitted names as a
#   COHORT against the accepted cohort's MEDIAN name. The book takes the TOP TEN BY SCORE. Names that
#   are no worse than the median holding can still be worse than the tenth-best, and admitting them
#   DISPLACES better names out of an equal-weight basket. So: `vs shipped` is a pool diagnostic, and a
#   green ADMITS NO WORSE flag IS NOT A SHIP SIGNAL. It says the gate is not obviously mispriced; only
#   a graded run says whether the book improves. Read it as permission to MEASURE, never as a result.
#
#   THE COVERAGE HALF OF C NEEDED NO NEW WORK — (#121) ALREADY MEASURED IT. The plan for this round
#   wanted a cause-census of the ~810 index members that produce no scoreable cutoff. It exists, in
#   (#121)'s own report line, measured 2026-08-22 over the 734 members whose span carries an exit
#   date: 387 served with ZERO BARS, 193 more with <36 in-span bars, 154 scoreable, and ZERO of all
#   1206 members with a series ending over a year ago. The recoverable-by-fetching bucket is EMPTY —
#   "closing it needs delisting DATES plus a terminal return, not a price backfill: these prices
#   provably do not exist to fetch." So "widen the pool with better coverage" is not a cheap round
#   being deferred; it is already answered, and the answer is that no fetch reaches it. NOT BUILT,
#   and the reason is measurement, not scope.
#   TO REVERT (#151): drop the `accepted_med` parameter from `gate_sweep` and the `h2h_n` clause from
#   the census line, restore `gate_audit`'s 2-tuple, re-bless the goldens. Report-only throughout —
#   no shipped value, no pick and no gate moved on this commit.
# ============================================================================================

# ============================================================================================
# (#160) 2026-08-29 — THE rank-1 h2h GUARD IS RE-BASED ONTO THE 6-10 BOOK AT 12y AND 8y, AND GAINS A
#   NOT-WORSE BAR THERE. No code, no shipped value, no golden moves: this is a GRADING-RULE change,
#   the one (#151) pre-registered in writing — "that is a grading-rule change and wants its own round
#   — but it is where a thin-pool GUARD should go". CLAUDE.md's Ship rule v2 block is amended to
#   match; THAT FILE IS GITIGNORED, SO THIS BLOCK IS THE SHARED RECORD.
#
#   THE RULE, AS IT NOW READS:
#     GUARD, 12y and 8y — rank-1 h2h read off the 6-10 book (`print_h2h_mid_ladder`, (#135), shipped
#       `true` in this file since (#154)): >= 50% AND NOT BELOW THE CONTROL'S TALLY ON THAT SAME BOOK.
#     GUARD, 20y — UNCHANGED. The 11-20 book, >= 50% absolute, no not-worse bar. (#135)'s revert rule
#       fired at 20y and STAYS FIRED, and (#124) puts pit 20y n_eff at ~0.2, so a delta there is noise.
#   NOTHING IS SOFTENED ANYWHERE: 20y keeps today's bar exactly, 12y and 8y GAIN a condition.
#
#   WHY A SECOND CLAUSE AT ALL — RE-BASING ALONE DOES NOT FIX WHAT (#156) FOUND. (#156) rung 10 reads
#   69% on the 6-10 book at 8y and 61% at 12y. Both clear 50%. Under the re-basing ALONE IT PASSES,
#   and its refusal would still rest on prose judgement rather than on the rule. The defect (#156)
#   exposed is not the denominator by itself: it is that an ABSOLUTE bar cannot see a knob that makes
#   the book worse while staying above the line.
#
#   AND THE TWO CLAUSES ARE NOT INDEPENDENT — THE RE-BASING IS A PRECONDITION FOR THE NOT-WORSE BAR.
#   On the 11-20 book at n=7-8 one window flip is 12-14 points, so a not-worse bar there fires on
#   noise. It would also have REFUSED THE ONE REAL SHIP IN THIS FILE: (#138)'s 8y 11-20 tally FALLS
#   67% -> 62% while its 6-10 tally RISES 65% -> 70%. Adding the bar to the old book breaks (#138);
#   adding it to the re-based book does not. That is the argument for the PAIR, and it is why neither
#   half ships without the other.
#
#   VALIDATED AGAINST THE WHOLE RECORD, NOT FITTED TO ONE ROW. Every graded arm in this file that
#   reports a 6-10 tally, re-adjudicated under the amended rule:
#     (#138) maxdd_cap off  12y 61->63,  8y 65->70   SHIPS, unchanged. Both clauses pass.
#     (#151) cagr 15        h2h RISES on every arm   REFUSED, unchanged — it fails PRIMARY, not this.
#     (#151) leg 2          h2h RISES on every arm   REFUSED, unchanged — same reason.
#     (#156) dilution 10    12y 63->61,  8y 70->69   REFUSED BY RULE.  <- the only verdict that moves.
#   ONE verdict changes, and it changes to match the refusal (#156) had ALREADY written out in prose
#   on three independent reads. A rule that re-derives a judgement already made, and disturbs nothing
#   else in the record, is doing what a rule is for.
#
#   RESOLUTION, so nobody reads a false precision into it: the not-worse bar's minimum trigger is ONE
#   WHOLE WINDOW — 3.8 points at n=26, 5.6 at n=18. It cannot fire on sub-window noise, and it is NOT
#   a percentage-point threshold to be tuned. Do not add a tolerance to it; a tolerance is the argmax
#   (#120) forbids, wearing a different hat.
#
#   READING NOTE, NOT A THIRD CLAUSE: a RISING fraction on a FLAT-OR-FALLING numerator is (#151)'s
#   residue artifact, and (#156) caught rung 10's 5/8 -> 5/7 exactly that way. The bar above is on the
#   FRACTION, because the fraction is what ">= 50%" has always meant and it is the only figure every
#   row of the record prints. When a fraction rises, check the numerator and `windows scored` before
#   believing it — (#156)'s GENERAL FINDING covers why.
#
#   WHAT THIS DOES NOT DO. It does not make 20y adjudicable, it does not touch PRIMARY or the worst
#   window, and it grades no knob. (#151)'s identity test
#   `the_h2h_denominator_is_the_unsaturated_bucket_count` still describes the 11-20 tally, is
#   untouched, and MUST STAY — but that identity NO LONGER DESCRIBES THE GUARD at 12y/8y, because the
#   6-10 book's denominator is a different predicate (>= 6 names, not > 10). Read that test as pinning
#   the OLD book, which still prints beside the new one.
#
#   TO REVERT: delete the not-worse clause and clause 1 stands alone exactly as (#151) pre-registered
#   it; delete both and the rule is what it was on 2026-08-29. Two code comments were corrected to
#   match — `rank_slice_stats`'s "the ship rule still reads `mid`" and `print_h2h_mid_ladder`'s "it
#   does NOT change which number the ship rule reads" — both true when written, both false under this
#   block. IF (#135)'s REVERT RULE EVER FIRES AGAIN AT 12y OR 8y, THIS BLOCK GOES WITH IT: the
#   not-worse bar would have no licensed book left to stand on.
# ============================================================================================
# (#186) 2026-08-31 — A RESOLUTION FLOOR UNDER THE rank-1 h2h GUARD. Fifth cause of a non-answer,
#   same shape as (#139), (#149), (#178)/(#185): no code, no shipped value, no golden moves. A
#   GRADING-RULE change, PRE-REGISTERED BEFORE THE (#187) RUNS IT WILL ADJUDICATE.
#
#   THE RULE: a rank-1 h2h GUARD column whose denominator n <= 2 is recorded VACUOUS BY RESOLUTION.
#   It supplies no evidence in either direction — it can neither refuse an arm nor license one. The
#   n that counts is the ARM's, since at 20y the bar is absolute on the arm's own tally.
#
#   THE DEFECT. (#160) reads the guard off the 11-20 book at 20y, at >= 50% absolute. On `pit` that
#   book's denominator is 2. The SHIPPED CONTROL, re-run twice on 2026-08-31, reads 1/2 = 50% there:
#   the shipped lane sits exactly ON its own guard, and one window flip fails it. `h2h_beats` uses a
#   strict `>`, so 1/2 is "#1 won one window and lost one" — the GUARD is passing a 1-1 coin flip as
#   evidence of no harm.
#
#   THE HARM IS ALREADY IN THIS FILE, not hypothetical. (#175)'s block: its arm's 20y guards PASS on
#   the 11-20 book at 5/10, and it notes "its 6-10 tally is 37% — well under 50% — which does not
#   bite only because 20y reads the 11-20 book by (#160)". A thin book passed a column the fatter
#   book refuses. (#173) reasons the same way from the other side, about these same denominators:
#   "the CONTROL h2h rests on 2, 4 and 6 windows, barely a statistic".
#
#   NO FREE PARAMETER, AND THAT IS THE POINT. (#160) established the principle and refused to let it
#   become tunable — "the not-worse bar's minimum trigger is ONE WHOLE WINDOW ... Do not add a
#   tolerance to it; a tolerance is the argmax (#120) forbids, wearing a different hat." The guard's
#   resolution is 1/n and its bar is 50%. Require the resolution to be FINER than the bar it
#   enforces: 1/n < 1/2 <=> n > 2. At n=2 one window flip moves the tally by the full 50-point width
#   of the bar, so the statistic can only land ON the bar or 50 points away; the attainable set is
#   {0%, 50%, 100%}. Nothing is chosen here — 50% is the bar's own number.
#
#   IT IS NOT A FREE PASS. VACUOUS AT A GUARD IS NOT A PASS. The arm must still clear every remaining
#   live guard on that column — at 20y on `pit` that is the worst window, which is a MAX and answers
#   at any n, exactly (#139)'s reasoning ("a mean over n=3 is noise; a WORST WINDOW is a MAX and an
#   h2h is a COUNT"). (#186) is the amendment to that sentence: a COUNT stops answering too, once its
#   denominator is 2. The tally and its n are still REPORTED IN FULL, as (#139) requires of 20y.
#
#   WHY NOT THE OBVIOUS REPAIR. Re-basing 20y onto the 6-10 book is what (#135)'s revert rule forbids:
#   it fired at 20y and STAYS FIRED, and (#160) says "IF (#135)'s REVERT RULE EVER FIRES AGAIN AT 12y
#   OR 8y, THIS BLOCK GOES WITH IT". The move available is a floor, not a re-base.
#
#   RE-ADJUDICATED AGAINST THE WHOLE RECORD, the standard (#160) set itself. Every row in this file
#   that reports a 20y 11-20 tally, checked for whether the floor moves it:
#     (#138) maxdd_cap off   20y 50%->50%, admits 117->120, arm n=2   VACUOUS. STILL SHIPS: (#139)
#            makes 20y guard-only on pit, its 20y worst window HELD (-10.0 -> -10.0), PRIMARY passes
#            at 12y and 8y, both short-horizon h2h books pass. The one real ship survives.
#     (#126) corr_cap 0.4    20y 50%->50%, holds 110->102, arm n<=2   VACUOUS. REFUSED ANYWAY, and
#            its own block says "any ONE of these is sufficient"; its 20y worst window WORSENS
#            -10.0 -> -10.6, which is a broken 20y guard on its own.
#     (#185) value_weight    20y 1/2 50%                              already VACUOUS by (#149).
#     (#135) cagr 15         20y 50%->50%, admits 117->183, arm n>2   not vacuous; REFUSED on the
#            6-10 book at 20y (46%) and on PRIMARY at 8y. Untouched.
#     (#175) cagr 12         20y arm 5/10                             n=10. Untouched.
#     (#173) wider pond      20y ctl 1/2 -> arm 7/20 (35%)            ARM n=20. Refusal STANDS — and
#            the refusal never rested on the control's 2 in the first place.
#     (#178) trend_weight    20y column reported and NOT graded       nothing to move.
#     (#179) sharpe/accel    stress lane, 20y n=19                    Untouched.
#     every `universe` row   n = 19/21/22/30/38                       Untouched.
#   THREE COLUMNS BECOME VACUOUS. ZERO VERDICTS FLIP. That is (#160)'s own test of a good rule — "a
#   rule that re-derives a judgement already made, and disturbs nothing else in the record".
#
#   AND IT REMOVES NO REFUSAL. At n=2 the only sub-50% value attainable is 0/2 = 0%; at n=1 it is
#   0/1. No row in this file reports either. So the floor cannot retroactively rescue an arm — it can
#   only stop a 1-1 split being quoted as a pass. CHECK THIS AGAIN if a future row reads 0%.
#
#   STATE IT PLAINLY: THIS IS INSURANCE, NOT A GAIN. It changes no verdict, it grades no knob, and it
#   buys no edge. It exists so the next de-saturating arm — which GROWS its own 20y denominator, as
#   (#175) and (#173) both did — cannot be adjudicated partly on a column that was a coin flip.
#
#   TO REVERT: delete this block; every verdict in the file stands exactly as written either way,
#   which is the whole argument for it and also the reason reverting costs nothing. If the 20y pit
#   denominator ever rises above 2 on the shipped config, this block goes DORMANT rather than wrong —
#   re-read it before assuming it still binds.
# ============================================================================================
# (#181) LANDSCAPE — WHERE THE BUY/GROWTH HEURISTIC STANDS AS OF 2026-08-31, and what is left. Written
# so the next round does not re-walk ground that is already closed. Each line names the receipt that
# closed it; go read that receipt before re-opening anything here.
#
# CLOSED — GATES AND POOL
#   spec P1 four / P2 / P3 gates ... refused in three separate shapes; knobs exist and ship OFF
#   (#159)/(#165) AUC factor table . SPENT. A new gate proposal needs a NEW measurement, not this table
#   pool SUPPLY .................... (#173)/(#174) a +65% wider pond moved the median gated pool by ZERO names
#   the gate stack ................. (#174) no gate carries TOO TIGHT at both 12y and 8y; no live lead
#   de-saturating the pool ......... (#175) growth_min_cagr 19->12 is the smallest rung that works, and it REFUSED
# CLOSED — RANKING WEIGHTS (all on `universe fund stress`, the one non-vacuous lane)
#   growth_trend_weight 0.15->0.35   (#178) REFUSED on PRIMARY at both horizons AND on the (#160) bar. Dead.
#   sharpe_weight 0.05->0.15 ....... (#178) REFUSED; answers (#70)'s unasked raise question with a no. Dead.
#   growth_accel_weight 0.35 / 0.65  (#179) REFUSED IN BOTH DIRECTIONS; 0.50 is bracketed on a lane that answers
#   growth_accel_beta .............. (#130) 1.0 is the best rung on every voting column
#   the trend/accel SUBSTITUTION ... (#130)(d) asked it, (#178)+(#179) close it: both halves of the one slope die
# CLOSED — MECHANICS AND COSTS
#   (#97) vol_daily_equivalent ..... (#176) bug check passes end-to-end; the knob is structurally ungradeable
#   turnover costs ................. (#177) the edge survives a 40x charge at every horizon; churn flag fires nowhere
#   the hold-period sweep .......... (#177) NOT OWED — it was conditional on the churn flag firing, and it did not
#   basket size / VERDICT_TOP ...... (#120) re-fitting it is forbidden; (#124)+(#180) measured the distribution
#   live book size `top_picks` ..... (#180) NO CHANGE. 5 sits on the flat part of the curve and the pool cannot
#                                    supply 10 anyway (top-10 == top-15 byte-identical at every horizon)
#
# THE ONE GENUINELY OPEN ITEM IS NOW CLOSED — see the (#185) receipt at `growth_value_weight`. It was
# re-opened under a pre-registered (#185) structural-inertness amendment and REFUSED: 20y is vacuous
# because `earnings_yield` reaches 0 cutoffs, 12y is vacuous because median gated pool 10 meets a
# basket of 10, and one adjudicating column cannot satisfy a two-horizon PRIMARY. The 8y column moves
# rank-1 h2h not at all (30/44 on every arm) and buys its only median gain by admitting a -100% name.
# Closed as PERMANENTLY UNGRADEABLE ON TODAY'S LANES. The paragraph below is the pre-(#185) framing,
# kept because its diagnosis was right and only its "open" status changed:
# `growth_value_weight`. (#178) found it UNGRADEABLE, not refused — it is
# structurally inert at 20y (the run reports earnings_yield on 0 cutoffs, `pe_ratio` is filled only from
# earnings_yield, so `value_factor` is identically 1.0), while `pit`, which could drop 20y under (#139),
# is VACUOUS for ranking knobs. It falls between the two lanes. Re-opening it requires pre-registering a
# (#139)-style 12y-AND-8y amendment for the stress lane FIRST, before any run — the favourable 8y number
# is already known, so proposing the amendment afterwards would be moving a bar after seeing a result.
# Its live 8y rung also drags a -100% name into the book while every ship-rule statistic holds or
# improves; read the zero-names line if it is ever re-run.
#
# THE LANE MAP, which is the most reusable thing this round produced. `universe fund pit` is the HONEST
# lane and the only one that should decide a GATE, but its median gated pool is 5-7 against a basket of
# 10, so it is VACUOUS for any RANKING knob and (#137) measured that it cannot resolve a weight arm
# better than ~0.1pt. `universe fund stress` is the only NON-VACUOUS ranking lane (median gated pool 11
# > basket 10, h2h denominators 19 and 31, byte-reproducible controls) — but its fat denominators come
# from membership not being point-in-time, NOT from the 27-name loser injection, so SURVIVOR BIAS IS THE
# PRICE OF GRADING THERE. Hence the standing reading rule: a REFUSAL on stress is permanent and
# publishable; a PASS on stress proves nothing and earns only an instrument run whose pit column is
# printed beside it. Every frozen-fixture lane is vacuous and can never grade a ranking knob.
#
# METHOD RULES EARNED HERE, all cheaper to obey than to re-derive: (1) an equal-weight top-N book is
# SET-INVARIANT, so an intra-book reorder reads "held" vacuously — grade a ranking knob on the rank
# slices and the h2h, never on the top-10 mean ((#179) shows rank-1 collapsing 1.8pt while the top-10
# mean does not move at all). (2) A byte-identical column is NOT evidence: it means either the ruler
# cannot move ((#149) saturation) or the knob never touches the ruler ((#178) structural inertness).
# Record both as NOT TESTED. (3) Settle same-or-different with `md5sum` or `diff -q`; `diff | grep -c`
# has twice reported 0 for files that differ ((#176) part 4, (#179)). (4) Run the control TWICE before
# attributing any diagnostic delta to a knob.
#
# BOTTOM LINE: the knob space is exhausted. Every weight and every gate in the buy/growth lane has now
# been swept, and the shipped value won or the arm refused. The next real gain will come from NEW DATA
# or a NEW MECHANISM — not from another rung of an existing knob, and not from another lane weaker than
# the two mapped above.
# ============================================================================================

tickers: []

# ============================================================================================
# (#114) THE LIVE DISPLAY BLOCK — moved here when config/web-settings.yaml was DELETED.
#
# There used to be two overlays over this file: the gitignored config/settings.yaml (the author's
# terminal) and the committed config/web-settings.yaml (the GitHub Pages run). They carried the same
# `history_proxy`, the same `widths`, the same `prefer_eu_listing: true` and the same twelve
# off-validated `buy_heuristic` knobs. The page therefore published a lane no receipt in this file had
# ever graded, and web/index.html had to say so out loud.
#
# The collapse went ONE WAY ONLY, and the direction is the whole point. The display half (this block)
# moved DOWN into the base, because it cannot move a backtest score. The heuristic half was DELETED
# from both overlays rather than promoted, because promoting it would have made `use_life_cagr: true`
# — twice-condemned at (#3j) and (#3k) — the graded default and invalidated every receipt below it.
# Pages now runs FOLIOMAN_CONFIG=tests/ci-settings.yaml: the page, the terminal and the test suite
# rank on ONE lane, and it is this one.
#
# Everything in this block is DISPLAY OR VENUE, never score. No golden covers it and no horizon can be
# quoted for it — these are shipped-policy values, not fitted ones.
# ============================================================================================

# Young listing -> older SAME-strategy, SAME-currency twin: the twin's closes are rebased and prepended
# so the young wrapper is scored on the strategy's proven history (marked ~ in tables). SXR8.DE =
# iShares Core S&P 500 acc (Xetra, EUR, since 2010) — twin for the young S&P wrappers.
#
# UNMEASURED at every horizon, and UNMEASURABLE here: no fixture carries any of these four symbols, so
# every golden is byte-identical with the block present or absent (checked before it landed). It DOES
# move a live rank — a spliced name is scored on the strategy's age, not the wrapper's — which is why
# `picks` marks those rows `~` instead of letting the longer history pass silently.
# REVERT RULE: drop an entry the moment its twin stops tracking the same index, or trades in another
# currency; `fetch` already refuses the splice in both cases and says so on stderr.
history_proxy:
  VUAA.DE: SXR8.DE
  SPYL.DE: SXR8.DE
  AUM5.DE: SXR8.DE

# Table column widths (chars) — each value truncated AND padded to this. All optional; omit the block
# for the built-in narrow layout (config.rs `Widths::default`). Pure display: `screen`, `check` and
# `picks` render through these and nothing else reads them.
widths:
  name: 28      # NAME column (check/screen/picks)
  name_etf: 53  # NAME column for ETFs (check/screen/picks) — 0 = use `name` for both lanes
  ticker: 10    # TICKER column (8 fits the longest EU symbols like CSNDX.SW; 7 truncated them)
  market: 7     # MARKET column (check/picks)
  price: 9      # PRICE(EUR) column (check/screen/picks) — lower to tighten the gap before it
  headline: 27  # HEADLINE column (check)
  score: 5      # SCORE column (picks) — shown to 1 decimal
  # screen/picks table columns shown, IN THIS ORDER. Comment out a line to HIDE that column; reorder
  # freely. Remove the whole `columns:` block (or set `columns: []`) to fall back to the built-in
  # default layout. Display-only — never affects scoring. (Listed here = ALL available columns.)
  columns:
    - rank        # position # (+ "*" pinned / "#" used live fundamentals)
    - name        # instrument name
    - ticker      # Yahoo symbol
    - market      # listing country / venue
    - price       # current close (EUR)
    - cagr        # whole-life CAGR (%/yr since listing) — display only; the growth rank scores `leg`, not this
    # - leg         # the CAGR the growth rank DOES score: the 20/8/5Y ladder rung, capped at long_trend_cap
    - yrs         # real listing age in years — also picks the rung above (≥20y→20Y, ≥8y→8Y, else 5Y)
    - 1h          # intraday % (1 hour)
    - 6h          # intraday % (6 hours)
    # - 12h         # intraday % (12 hours)
    - 1d          # % vs 1 day ago
    - 1w          # % vs 1 week ago
    - 1m          # % vs 1 month ago
    - 2y          # % vs 2 years ago
    - 5y          # % vs 5 years ago
    - 8y          # % vs 8 years ago
    - 20y         # % vs 20 years ago
    - vol         # daily-return stdev (~1y) — risk / "normal swing"
    - maxdd       # worst peak-to-trough drop ever — pain endured
    - r2          # log-trend steadiness 0..1 — smooth compounder vs lumpy
    - abv-ma      # % above the 200wk SMA — overextension / blow-off gauge
    - pe          # trailing P/E (needs FMP key; else n/a)
    - peg         # P/E ÷ long CAGR — valuation-vs-growth (<1 cheap for its growth; needs FMP key)
    - mvrv        # crypto MVRV ratio (needs FMP key; else n/a) — valuation-vs-growth (<1 cheap for its growth)
    - roe         # trailing return-on-equity (needs FMP key; else n/a) — quality
    - ter         # ETF expense ratio %/yr — the one cost that compounds against a 40y hold (needs FMP key; ETFs only)
    - aum         # ETF fund size (EUR-approx, Börse Frankfurt) — sub-€100M funds risk liquidation/merge (ETFs only)
    - use         # ETF share class: Acc/Dist — Dist payouts taxed yearly; Acc compounds tax-deferred (ETFs only)
    - repl        # ETF replication: Swap/Full/Opt — counterparty structure; swap-based US funds dodge dividend WHT (ETFs only)
    - rev-yoy     # newest complete-FY revenue growth vs prior FY (stocks only; report pipeline, top rows enriched)
    - eps-yoy     # newest complete-FY EPS growth vs prior FY (stocks only) — profit follow-through
    - net         # newest complete-FY net margin % (stocks only) — profitability quality
    - div         # trailing-1Y dividend yield
    - buyback     # buyback yield (stocks only)
    - off-hi      # % below its OFF-HI high
    # - upside      # room back to that high (NOT a forecast)
    - turnover    # avg daily turnover (EUR) — liquidity
    - score       # the growth ranking score
    - score8y     # the 8-year growth ranking score

# --- `check` / `alert` / `perf` display + scan knobs (display/report only — NOT scored by the backtest gate) ---
# OFF-HI anchor: 0 = drawdown vs the ALL-TIME high over the fetched ~10y history (recommended for a
# decades buy-and-hold: "on sale vs the proven peak", pairs with the below-200wk-SMA cheap reward).
# Or set N days for a trailing-N-day high (365 = last year only, recency-biased; 1825 = ~5y cycle).
high_days: 0
drawdown_pct: 1000.0
dip_days: 30       # trailing window (days) for the vs-high dip
drop_pct: 5.0      # `alert` fires when a name is >= this % below that window high
top_picks: 20      # how many "buy candidate" rows `check` lists after the table
# deploy-math base (€ you put in per month): `screen` prints "DEPLOY THIS MONTH: €base × entry-state
# multiplier" (near-high 1×, pullback 1.5×, drawdown 2× — the backtest's deploy-faster shape). 0 = line
# off. PERSONAL number — set it in the private config/settings.yaml overlay, never here.
monthly_deploy_eur: 0

# Per-horizon ±days averaged around each horizon's anchor date when computing 1D/1W/.../20Y %
# changes (smooths a single outlier day; 0 = exact day). All optional — omit the block or any
# label to use the built-in defaults (core::default_anchor_half): 1D 0, 1W 7, 1M 30, 6M/1Y 90,
# 5Y/10Y/20Y 365. Override only the labels you want to change.
anchor_windows:
  1D: 0
  1W: 3
  1M: 15
  6M: 91
  1Y: 182
  5Y: 365
  10Y: 365
  20Y: 365

# Show REAL (inflation-adjusted) returns on the 1Y/5Y/10Y/20Y % columns instead of nominal. A
# coin/stock up +50% over a span that saw +10% inflation is really only ~+36% in purchasing power.
# Deflates by the ACTUAL cumulative EU HICP inflation over each horizon, fetched live (same source
# as the check footer) — no rate to guess. Shorter horizons (1D/1W/1M/6M) are never adjusted.
inflation_adjust:
  enabled: true       # true = deflate long-horizon returns by live EU HICP; false = raw nominal %
                      # (#88) THIS KNOB IS NOT ABOUT COLUMNS, which is what the paragraph above and its
                      #   own comment both claim. `core::horizon_changes` writes its legs into
                      #   `Quote.perf`, and `Quote.perf` is what `picks::perf_pct` hands to EVERY gate
                      #   and score term, so at `true` the whole growth lane is scored in REAL terms.
  score_on_nominal: false # (#88) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                      #   today's behaviour byte-for-byte. false = the score keeps reading whatever
                      #   `enabled` produced; true = the score reads a nominal copy while the % columns
                      #   keep the real one, i.e. `enabled` finally means only what it says.
                      #   THE SKEW IT CLOSES. Every threshold in this file was fitted by `backtest`,
                      #   which builds its quotes through `core::backtest_quote` and passes `infl: None`
                      #   — nominal, always, at every horizon. The live `screen` runs with `enabled:
                      #   true`. So a nominal bar is being applied to real numbers. Measured off the
                      #   cached EU HICP feed (.fmp_cache/eu_hicp_old + eu_hicp2, 2000-2026, the same
                      #   merge `fetch_eu_inflation` does), compounded exactly as
                      #   `core::inflation_compounded` compounds it:
                      #        1Y cum  +2.9%    5Y cum +23.4%    8Y cum +32.3%
                      #       10Y cum +36.3%   12Y cum +38.1%   20Y cum +60.0%
                      #   As a CAGR that is 2.38 pts/yr off the 20Y rung, 3.56 off the 8Y and 4.30 off
                      #   the 5Y (the 2021-23 spike sits inside the short windows: 5.3 / 10.4 / 3.4).
                      #   `growth_min_cagr: 19.0` therefore behaves like ~21.4 on a 20Y-rung name and
                      #   ~23.3 on a 5Y-rung one — a gate materially tighter than the one the 20y/12y/8y
                      #   grids measured, and tighter by a DIFFERENT amount per rung, so it also tilts
                      #   the ranking between old and young names. Same story for every other perf-fed
                      #   bar: the 5Y/20Y rung floors, `growth_min_1y_pct`, `growth_min_20y_pct`.
                      #   AND IT IS INCONSISTENT INSIDE THE LIVE RUN, which is the part no horizon can
                      #   excuse. `quote.life_cagr` (`core::life_cagr`) and `quote.trend_cagr` are built
                      #   from raw closes and are NOMINAL always. So:
                      #     - `long_cagr_from`'s three arms are not in the same units — flipping
                      #       `use_life_cagr` changes the basis as well as the window, worth ~2-4 pts/yr
                      #       that has nothing to do with what that switch is for;
                      #     - `growth_min_cagr` has TWO legs (the rung, and the whole-life bar via
                      #       `picks::life_leg_cagr`) and reads ONE number, 19.0, against one real value
                      #       and one nominal one.
                      #   That is a direct breach of the ONE-DEFINITION-PER-NUMBER rule, and it is why
                      #   this is filed as a defect and not as a tuning question.
                      #   WHY NO NUMBER IS QUOTED FOR THE FIX, i.e. why it ships OFF. No golden can grade
                      #   it: `backtest_quote` passes `infl: None`, so `perf_nominal` is never filled on
                      #   the backtest path and the fixture output is byte-identical at BOTH settings.
                      #   The change is live-tables end to end. Grading it properly needs an as-of HICP
                      #   series inside the backtest — real work, not done here — and until then "off"
                      #   is the only position that is measured, because off is what every receipt in
                      #   this file was measured under.
                      #   REVERT RULE: it is already at the revert value. The cheaper alternative, if the
                      #   real columns are not worth this, is `enabled: false` — that puts the score and
                      #   the display back on one number with no new field at all. Turn THIS on instead
                      #   only if the real % columns are wanted, and then check a live `screen` both ways
                      #   at the BOTTOM of the stock table: names should be ADMITTED, never dropped,
                      #   since every perf-fed bar gets easier when the deflation comes off.

# (P5) RISK BUDGET for the `size` table. Every value here is UNMEASURED AND UNMEASURABLE IN THIS REPO,
# and unlike the unfitted gate values elsewhere in this file that is a permanent fact, not a to-do:
# `size` is a live read-only command, no backtest path reaches `picks::size_weights`, and no golden
# covers it. These are POLICY. State them, do not sweep them, and do not let a future receipt claim a
# horizon for them. Written out rather than left to the code defaults so the shipped policy is legible
# in the same file as everything else the operator reads.
# REVERT RULE: delete the block — the code defaults are these exact numbers.
sizing:
  budget_stock: 70    # share of gross for single stocks, RENORMALISED over the classes actually present
  budget_etf: 25      # funds: already diversified internally, so they need less name-level spreading
  budget_crypto: 5    # WHAT THIS REPLACED: the old split was 100/k over the classes present, so ONE coin
                      #   scoring positive drew 33% — the whole stock class's share — for no reason but
                      #   being the only coin in the table. 5 is the standing answer to "how much may sit
                      #   in the class that has drawn down 80%+ in every cycle it has had".
  max_name_pct: 4.0   # no single row over this % of gross; excess is redistributed WITHIN its class
  max_sector_pct: 25.0 # no GICS sector over this %, stock class only (an ETF's sector label means something
                      #   else and a coin has none). CONSEQUENCE: a class too small to absorb its budget
                      #   under these caps leaves the remainder UNALLOCATED and the table's total prints
                      #   below 100. That is the intended reading, not a rounding bug.

# ============================================================================================
# BUY HEURISTIC (src/picks.rs). Acronyms (CAGR, ROE, P/E, NUPL, SMA, FX, ...): see Glossary in README.md.
#
# This is the COMMITTED, canonical base. BOTH the backtest-gate scores AND the live `screen` read it:
# the gitignored config/settings.yaml deep-merges OVER this file field-by-field, so the local overlay
# only needs to carry what it OVERRIDES (a secret, the watchlist, or a knob under test). Edit a value
# here to move the validated default for everyone; the rich "why" comments below are the shared record.
#
# There are TWO scoring lanes, and `screen` prints ONLY ONE of them:
#
#   • GROWTH lane (`growth_score`)  --> THIS is what `screen` prints. Proven compounders at/near
#       their own ~10y high, still climbing — the only lane with a validated forward edge. Live score:
#         base  = growth_trend_weight·CAGR + growth_accel_weight·accel + risk + quality + dividend
#         score = base · proximity · value(P/E tilt) · geomean(trust, overext_brake) + liquidity
#       (CAGR = Compound Annual Growth Rate; accel = how much the last 1Y outpaced that CAGR.)
#
#   • ON-SALE lane (`buy_score`)    --> BACKTEST FOIL ONLY. `screen` does NOT print this. It exists so
#       `cargo run -- backtest` can demonstrate that dip-buying has NEGATIVE edge over a 20yr+ hold.
#       Every knob tagged "[FOIL]" feeds ONLY this lane: changing it does NOT change `screen` output.
#
# So to tune what `screen` shows, touch ONLY the knobs NOT tagged "[FOIL]". GATES drop a candidate
# outright (and reshape the ranking the most — tune these first); SCORE knobs only re-rank the
# survivors. All knobs optional (omit any -> built-in default). See README.md "Tuning the buy heuristic".
# ============================================================================================
buy_heuristic:
  # --- GATES (fail any one -> excluded before scoring) ---
  min_1y_pct: -25.0              # [FOIL] on-sale only: reject if 1Y % <= this (the growth lane reads growth_min_1y_pct below, not this)
  growth_min_1y_pct: 0.0         # RE-MEASURED AND RE-CONFIRMED 2026-08-03 (5-rung stress ladder). Loosening this is the single most
                                 #   expensive thing in the whole grid, and it is MONOTONE — every step down costs 20y lane edge:
                                 #     +10 (tighter) +397.4 | 0.0 SHIPPED +410.8 | -10 +331.9 | -25 +316.2 | off(-1e9) +287.5
                                 #   rho falls with it (+0.16 / +0.14 / +0.13 / +0.12 / +0.12). Tightening to +10 does not rescue it
                                 #   either: 20y edge -13.4 and rank-1 median +6.0 -> +5.3, so 0.0 is a genuine peak, not an endpoint.
                                 #   This reproduces the round-5 (2026-07-03) receipt independently on current data — do not re-propose
                                 #   loosening it without a NEW mechanism; two grids five weeks apart now say the same thing.
                                 #   ---- original note ----
                                 #   LIVE (equities): the growth lane's 1Y floor — reject if 1Y % <= this. 0.0 = must be climbing this year.
                                 #   Was a hardcoded 0.0 until the knob was restored; this value is the constant it replaced, so shipping
                                 #   it changes nothing. It is written down because the number is EARNED, not conservative-by-default.
                                 #   MEASURED AND REVERTED ONCE (round 5, 2026-07-03, `.claude/growth_heuristic_plan.md:205`), on the same
                                 #   question that brought the knob back — "aren't down-1Y-but-up-5Y names good growth candidates?":
                                 #     same-batch triple: baseline edge +106.6 (OOS +0.11|+0.06) | floor -10 -> +101.1 | floor -20 -> +94.4
                                 #     decisive: the n=284 names a -10 floor NEWLY ADMITS average -108.1 pts forward peer-relative.
                                 #   Down-1Y-near-high names are falling out of favour, not resting — the on-sale foil's profile, and it
                                 #   loses here too. README lists removing this floor under "measured dead — don't re-run these".
                                 #   WHY THE KNOB EXISTS ANYWAY: so the cohort is re-measurable instead of quoted. `backtest universe`
                                 #   prints a `growth_min_1y_pct -10` GATE SWEEP row (restored with the knob) giving n + mean fwd return
                                 #   on TODAY's sample; `screen` prints a `Down year` tail naming the live names the floor drops. Read
                                 #   both, then leave this at 0.0 unless the sweep flips POSITIVE — and even then, only after a full
                                 #   `backtest universe` re-validation with both OOS halves positive. Edge-affecting.
                                 #   RE-MEASURED 2026-07-27 (`backtest universe`, 8445 cutoffs / 4881 tickers, growth lane 249 windows,
                                 #   edge +49.7 OOS +0.10|+0.21):
                                 #     growth_min_1y_pct -10   n=4   mean fwd peer-relative -85.0 pts
                                 #   Same SIGN and magnitude class as round 5's -108.1, so nothing here reopens the question — but say
                                 #   what it is: n=4 is far too small to confirm or overturn anything on its own. It is corroboration,
                                 #   not a replication. The n collapsed 284 -> 4 because the LANE is much stricter now than in round 5
                                 #   (growth_min_cagr 16.0 + use_life_cagr), so hardly any name reaches the point where the 1Y floor is
                                 #   its SOLE blocker — the same reason the growth lane scores only 249 windows here.
                                 #   READING THE SWEEP: its "<- TOO TIGHT" flag is NOT sample-size aware. The same run flags
                                 #   `growth_min_range_pct -10` at n=1 (+163.8) and `growth_maxdd_cap ->off` at n=1 (+75.7). Check n
                                 #   before believing any row in that table, including this one.
                                 #   PARTLY FIXED 2026-08-01: the sweep and the GATE AUDIT were reporting BARE MEANS of forward
                                 #   peer-relative return — the most skew-prone quantity in the tool — while every other slice in
                                 #   the report prints mean|median for exactly that reason. Both now print both, and the TOO TIGHT
                                 #   flag needs BOTH positive; disagreement prints SPLIT instead of a verdict. This killed most of
                                 #   the historical flags outright (see the PEG receipt at growth_max_peg for the full list). It
                                 #   does NOT make the flag sample-size aware — small n is still small n — but it does mean a row
                                 #   flagged TOO TIGHT is no longer one survivor carrying the average.
                                 #   SWEPT SOLO 2026-07-30 (per-knob stress grid, floor removed at -1e6): the floor is HORIZON-SPLIT.
                                 #   8y-s: removal WINS (edge +100.0 vs +79.2, top-10 +7.0 vs +6.5) — dip-buying pays inside a decade.
                                 #   20y-s KING: lane edge CRASHES +623.5 -> +429.8 (−31%) — down-year names admitted into the top half
                                 #   are secular decliners over 20y, the round-5 −108-pts cohort seen at lane level. The top-10 BOOK
                                 #   actually ticks up (+6.4/+6.1 vs +6.1/+5.8), so the wound is below the buy line — but the 20y+ hold
                                 #   goal keeps the floor. VERDICT: 0.0 stays; an overlay running -1e6 should return to 0.0.
  min_1y_pct_crypto: -50.0       # LIVE (crypto/FX -EUR/-USD): 1Y floor for the crypto growth lane — coins swing far harder than
                                 #   stocks, but a coin down worse than -50% on the year is a crash, not a swing (BTC's own bad
                                 #   year is -33.5%, comfortably inside; GT -62 / BGB -60 out). Was -75, which let deep crashers rank
  max_1m_drop_pct: -1000000.0    # LIVE: reject if 1M % <= this (a hard monthly crash = falling knife) — both lanes
                                 #   SWEPT SOLO 2026-07-30 (off at -1e6, per-knob stress grid): read as noise (20y-s +629.8 vs
                                 #   +623.5, 12y-s −4.4, 8y-s −1.2, book metrics flat) and receipted "FREE in backtest".
                                 #   THAT READING WAS WRONG — it came off the mean-only gate audit ca8e404 retired, and it
                                 #   looked only at lane/book aggregates, which is precisely where this gate does NOT show up.
                                 #   RE-SWEPT SOLO 2026-08-02 at -100 (= off for an equity: a stock cannot fall 100% in a
                                 #   month), post-accel-0.65, same-session baselines at all three horizons, medians read:
                                 #     the knife BINDS — it admits +5 windows at EVERY horizon (20y 380->385, 12y 576->581,
                                 #     8y 723->728), so it was never a no-op; the 2026-07-30 aggregates just could not see it.
                                 #     the cost lands on RANK 1 — the row actually bought — while the top-10 book is FLAT at
                                 #     all three (20y +6.1 med +6.1 win 97%, 12y +6.3/+6.5 90%, 8y +6.4/+6.2 88%, unmoved):
                                 #       20y-s rank-1 med +6.6 -> +6.2   12y-s +5.9 -> +5.5   8y-s +7.2 -> +7.5
                                 #     bought with a +3.0 lane edge at 20y (+648.2 -> +651.2) and −4.3 at 8y (+77.1 -> +72.8).
                                 #   Fails "rank-1 quality up or held at the king horizon" -> -20 KEPT. The gate sweep says the
                                 #   same from the other side: loosening ONE notch admits n=4 whose median is -125.2 pts.
                                 #   And "the backtest can't see the live mid-crash month this exists for" is RETIRED as an
                                 #   argument — core::backtest_quote rebuilds a real 1M leg from the [..=as_of] slice at every
                                 #   cutoff, so 2008/2020/2022 crash months ARE scored. The gate was measured over exactly the
                                 #   months it exists for and earned its keep there. An overlay running -1e6 pays rank-1 for it.
                                 #   Live 2026-08-02 the knife is binding, in the near-miss list: VRT -23.4% vs the -20.0 floor.
                                 # (#64) THE SHIPPED VALUE IS -1e6 AND THIS BLOCK ARGUES FOR -20. That contradiction is real,
                                 #   it is seven days old, and this receipt resolves it as "unresolved", which is the honest
                                 #   answer. `d58db7a` (2026-08-03) set -20.0 -> -1000000.0 BY HAND in a commit about wiring a 2Y
                                 #   CAGR rung, and flagged it in its own message as ungraded. The gate sweep could not catch it
                                 #   because the sweep only LOOSENS, and this was a loosening. No test caught it because
                                 #   `shipped_config_pin` did not cover it. It does now, pinned at the shipped -1e6 with this
                                 #   receipt named, so the next person to move it has to argue with these numbers first.
                                 #   RE-MEASURED 2026-08-10 as one arm of the nine wide runs described at `growth_max_above_ma`,
                                 #   under the rule fixed before any run: PRIMARY is the EXCLUDED cohort, which must be negative on
                                 #   BOTH mean and median to justify excluding it; GUARD is that rank-1 MEDIAN at the 20y king must
                                 #   not fall. Restoring -20 PASSES THE GUARD and FAILS THE PRIMARY.
                                 #     guard, rank-1 mean|MED: 20y +6.6|+7.0 -> +6.6|+7.3 with h2h 76% -> 81%; 12y +6.7|+7.8 ->
                                 #       +6.8|+7.8 with h2h 60% -> 63%; 8y +7.6|+8.4 -> +7.3|+8.2, the one horizon that falls.
                                 #     primary, the excluded cohort: 20y n=4 mean +118.6 | med -143.1 — SPLIT, and split the wrong
                                 #       way at the king. 12y n=4 -98.9 | -192.6 and 8y n=4 -25.0 | -49.5 both pass. One name in
                                 #       four is a large enough winner to carry the 20y mean on its own, which is the exact failure
                                 #       mode the sweep's own note warns about and the reason this bar reads BOTH moments.
                                 #   READ THE n. Four names is not a cohort, and the sweep row that prints them is the -20..-30
                                 #   band only (the probe subtracts 10 from the shipped value), so everything below -30 — the
                                 #   2008/2020-shaped months this gate exists for — is excluded by BOTH arms and never observed.
                                 #   Under (#60)'s symmetry rule that makes -20 UNRESOLVED, not disproved. -1e6 stands because
                                 #   nothing cleared the bar to move it, NOT because -1e6 was shown to be right.
                                 #   TWO COHERENCE CHECKS, both clean, which is why the numbers above are worth anything at n=4.
                                 #   Footprint: the knife costs exactly 5 windows at every horizon (481->476, 668->663, 852->847),
                                 #   reproducing the "+5 windows at EVERY horizon" the 2026-08-02 solo sweep found. Sign by horizon:
                                 #   that sweep had off worse at 20y and 12y and BETTER at 8y; this batch has the knife better at
                                 #   20y, flat at 12y and worse at 8y — the same three signs from the opposite direction, eight days
                                 #   and three re-fits apart. Everything else is invisible: rho +0.15 both, 20y top-3 +6.4 (+6.6)
                                 #   identical, and every edge delta (-5.2 / +1.3 / +2.6) sits under its own null floor (185.3 /
                                 #   56.5 / 8.7). A gate touching 1% of windows cannot move a lane aggregate, so those flats are
                                 #   the instrument's silence, not evidence.
                                 #   TO REOPEN: this needs an instrument that scores the below--30 tail, not another nine runs of
                                 #   the same shape. Until then the live argument above — VRT caught at -23.4% on 2026-08-02 — is
                                 #   the only thing on the -20 side, and it is a live eyeball, not a grade.
  max_1m_drop_pct_crypto: -35.0  # LIVE (crypto/FX): looser knife — a -20%/month alt is normal — both lanes
                                 #   UNMEASURABLE by backtest (2026-08-02) — but NOT for the reason first written here.
                                 #   CORRECTED 2026-08-02: the original text said "crypto is absent from the backtest pool".
                                 #   That is FALSE. fetch_universe pushes CoinGecko SYMBOL-EUR tickers and the backtest applies
                                 #   no crypto filter; coins are in fact the ONE class the backtest always classed correctly
                                 #   (is_currency_quoted reads the ticker suffix, which Quote::stub preserves). The real
                                 #   constraint is SAMPLE COUNT: coins carry ~11y of history, so almost none survives a long
                                 #   FORWARD window. Now measured, from the per-class census line the report prints:
                                 #     20y-s: crypto 0 cutoffs of 4633   12y-s: 0 of 11758   8y-s: 18 of 20575, 0 growth-scored.
                                 #   So the conclusion stands and is now load-bearing on a number rather than folklore: zero
                                 #   crypto cutoffs reach a scored forward window, so no sweep can vote on this knob.
                                 #   Same class as growth_maxdd_cap_crypto / growth_max_vol_crypto (both corrected likewise).
                                 #   Settled by live screen A/B instead (-35 vs -100, everything else HEAD): the two crypto
                                 #   tables are IDENTICAL — same names, same order, same scores; the gate binds on NONE of the
                                 #   20-coin pool today. That is "today is not a crash month", NOT "the gate is dead", so it is
                                 #   not evidence to loosen. Kept at -35 on the equity twin's result above: loosening a 1M knife
                                 #   buys nothing at the top of the book and costs rank-1 quality where it can be measured.
  min_long_pct: 30.0             # [FOIL] on-sale only: reject unless EVERY 5Y/10Y/20Y leg is > this (the growth lane has its own per-rung floors below)
  min_long_pct_crypto: -70.0     # [FOIL] on-sale only: reject if the >2Y leg <= this CUMULATIVE % (a corpse, e.g. -95%)
  growth_min_5y_pct: 75.0        # LIVE (EQUITIES + ETFs ONLY since 2026-08-03 — coins answer to growth_min_5y_pct_crypto below).
                                 #   MEASURED AND SHIPPED 2026-08-03, 9-rung stress ladder, `backtest {20,12,8} universe stress`, one
                                 #   verified field per arm. THE ONLY WIN IN A 78-RUN GRID over five gate knobs, and it is a TIGHTENING:
                                 #     rung   20y edge   rho    rank-1 med   h2h    n
                                 #     off     +408.0   +0.14     +6.0       67%   523
                                 #     -25     +408.1   +0.14     +6.0       67%   523
                                 #     0 (was) +410.8   +0.14     +6.0       67%   520
                                 #     +25     +419.6   +0.14     +6.0       67%   514
                                 #     +50     +417.5   +0.15     +6.4       71%   503
                                 #     +75     +459.3   +0.17     +6.4       76%   490   <- SHIPPED (single peak)
                                 #     +100    +396.0   +0.14     +6.4       81%   468
                                 #     +150    +232.1   +0.10     +4.4       67%   407
                                 #   Repeats at the other horizons: 12y edge +123.0 -> +133.6, 8y +47.8 -> +61.9, 8y rank-1 +6.9|+7.8 ->
                                 #   +7.2|+8.2. Clears the 2026-08-02 ship rule (rank-1 median UP at the 20y king, h2h >=50% at all three,
                                 #   king edge up) AND neighbour consistency (+50 moves the same way on every clause). The MECHANISM is
                                 #   legible rather than a lucky rung: h2h climbs monotonically 67 -> 71 -> 76 -> 81 across the tightening
                                 #   rungs, i.e. the bar strips weak names out of the MIDDLE of the list, which is what should widen the
                                 #   #1-vs-11-20 gap. Edge peaks at +75 and falls after as the sample thins (n 520 -> 407).
                                 #   DRIFT CONTROL: the baseline was re-run at the END of the 3h13m grid and is byte-identical to the start
                                 #   (same n, edge, rho, rank-1, h2h at all three horizons), so none of the above is universe drift.
                                 #   WHAT IT DOES NOT DO: today's live equity/ETF tables are IDENTICAL at 0.0, +50 and +75 (DELL NVDA /
                                 #   IITU.L XLKS.L SMH.L SEMI.AS LYBK.DE). The gain is selection quality across 490 historical cutoffs,
                                 #   NOT a change to tonight's picks. Do not expect to see this knob in the table.
                                 #   WHY THE CRYPTO TWIN EXISTS: at +75 the shared knob printed "Top 0 of 20 max crypto — (none pass the
                                 #   gates)" (BTC 5Y +51.5%, BNB +63.1% that day). See growth_min_5y_pct_crypto below.
                                 #   ---- the pre-2026-08-03 history of this knob, kept because it explains the shape above ----
                                 #   LIVE: growth-lane 5Y CUMULATIVE-return floor — reject if the 5Y leg <= this. Like growth_min_1y_pct above,
                                 #   this is the constant it replaced (a hardcoded 0.0 at both gate sites), so shipping it changes nothing
                                 #   FOR EQUITIES. It is written down because ONE thing did change with it: the gate used to carry a
                                 #   `!crypto` guard and no longer does — a coin now answers to this bar too (round 2026-07-27, by request).
                                 #   THE EQUITY HALF IS MEASURED NEUTRAL, not assumed: `backtest universe fund 12` run on the pre-change and
                                 #   post-change binaries is IDENTICAL line for line — 1642 windows, Spearman +0.18, edge +146.2, net +146.0,
                                 #   winsorized +118.0, OOS +0.11|+0.11. The only line that moves is the bootstrap band, and that is
                                 #   resampling noise: three runs of the SAME binary gave [+79.7…+179.1], [+82.1…+176.5], [+84.3…+180.5]
                                 #   while every deterministic number stayed pinned. Do not read a moved band as a moved edge.
                                 #   THE BACKTEST CANNOT GRADE THE CRYPTO HALF. Every edge metric drops crypto before scoring (backtest.rs:1058 "equal-
                                 #   weight, non-crypto"; :1075 `continue` on a coin; the `gated, non-crypto` pools at :1224/:1468/:1522/:1625),
                                 #   so an unchanged edge here is NOT evidence the crypto half is harmless — it is evidence the metric never
                                 #   looked. The check that means something is the live `screen` crypto lane, read by eye.
                                 #   THE VIEW THIS OVERRODE, so it isn't rediscovered from scratch: both lanes had independently concluded a
                                 #   -EUR 5Y is peak-anchored noise and the range gate is the right tool for bled coins. The foil lane STILL
                                 #   holds that view — see its separate, much looser min_long_pct_crypto -70.0 two lines up. If the crypto
                                 #   lane thins out unexpectedly, this knob going negative is the fix, and it loosens equities at the same
                                 #   time, because it is now one shared number. Edge-affecting for equities -> re-validate before moving it.
                                 #   SWEPT SOLO 2026-07-30 (off at -1e6, per-knob stress grid): noise at every horizon (20y-s +614.1
                                 #   vs +623.5, 12y-s +224.4 vs +221.7, 8y-s +82.4 vs +79.2; book metrics byte-identical). At 0.0 the
                                 #   bar almost never binds once growth_min_cagr is in force. TIE — 0.0 stays.
  growth_min_5y_pct_crypto: 0.0  # CRYPTO twin of the 5Y floor above (added 2026-08-03). 0.0 = EXACTLY what the shared knob served before
                                 #   the split, so adding the twin changed no coin's standing — the split exists so the EQUITY bar could go
                                 #   to its measured +75 optimum without pricing coins against an equity-tuned number.
                                 #   THE COST IT AVOIDS, measured live: at a shared +75 the crypto table read "Top 0 of 20 max crypto —
                                 #   (none pass the gates)". BTC-EUR 5Y was +51.5% and BNB-EUR +63.1% on 2026-08-03, so an equity floor set
                                 #   anywhere above ~+51 empties the lane. Even +50 clears BTC by only 1.5 points.
                                 #   UNGRADEABLE BY THE BACKTEST, like every crypto knob: each edge metric drops coins before scoring
                                 #   (backtest.rs:1224/1456/1519/1560/1610/1728/1781/1872), so no run can vote on this number. `GATE_PROBES`
                                 #   pins it crypto-LIVE / etf+stock-INERT, which is the reachability claim, not an edge claim. If the crypto
                                 #   lane thins unexpectedly, THIS is the knob to take negative — and it no longer loosens equities when you do.
  growth_min_8y_pct: -1e9        # MEASURED AND REJECTED 2026-08-03 (5-rung stress ladder, +25/+50/+100/+150 vs off): stays OFF.
                                 #   Standalone it is noise — rank-1 is IDENTICAL to baseline at every rung AND every horizon, and at the
                                 #   20y king the pool barely moves (n 520 -> 520/520/520/518). Best rung (+150) bought 8y edge +47.8 -> +54.8
                                 #   while changing nothing about who ranks where.
                                 #   COMBINED-ARM VERDICT (the reason it ships off rather than "harmless"): stacked on top of the shipped
                                 #   growth_min_5y_pct 75.0 it SUBTRACTS at the horizon that matters — 20y edge +459.3 (5Y alone) -> +454.1
                                 #   (both), rank-1 and h2h unchanged, and it trims exactly ONE extra 20y window (490 -> 489). The 5Y floor
                                 #   already rejects almost everything this rung would; a second near-redundant gate is cost without gain.
                                 #   Do not re-propose without a NEW mechanism — a rung that moves n by 1 cannot carry an edge claim.
                                 #   ---- earlier note, still true ----
                                 #   OFF here, but it is the rung that BITES — arm it in the local settings.yaml if wanted. MEASURED live
                                 #   2026-07-27 at growth_max_peg 2.0, +50% bar: it rejects FOUR names, and all four sit against the fence —
                                 #     AMGN 8Y +44.0% · GILD +40.7% · SCHW +48.2% · ULTA +49.2%
                                 #   That is 4.4-5.1%/yr over 8 years from names that all clear a 16%/yr LIFE CAGR — precisely the pattern
                                 #   this gate was asked for: a long record flattering a mediocre recent decade. Nothing else reads that
                                 #   window (under use_life_cagr the leg check and the life check are the same number).
                                 #   HOW MUCH IT BITES DEPENDS ON A KNOB 60 LINES AWAY. The same bar rejected NOBODY when first measured,
                                 #   because that run had growth_max_peg 1.0 and only 5 stocks reached the ranking at all. Raising the PEG
                                 #   ceiling to 2.0 admitted the wider cohort these four live in. Re-measure after ANY move to growth_max_peg
                                 #   rather than quoting this line — the two gates compose, and the 8Y bar reads as dead until it isn't.
                                 #   Note it is still ~4x slacker than a gate already in force: +50% over 8y is 5.20%/yr, while
                                 #   growth_min_cagr 16.0 implies +228%. It bites anyway because it constrains a DIFFERENT window.
                                 #   Ships off because the number is unvalidated, not because it is inert. The legs ARE backtest-visible for
                                 #   equities (backtest_quote fills perf from the full [..=as_of] slice), unlike the age/AUM/turnover gates —
                                 #   so sweep it before arming it in anger.
                                 #   SWEPT 2026-07-30 as asked (+50 bar, per-knob stress grid): NEAR-INERT there — 20y-s +624.2 vs
                                 #   +623.5 with the book byte-identical, 12y-s −6.0 edge, 8y-s +1.4. The AMGN/GILD/SCHW/ULTA pattern
                                 #   never assembles in the survivors-only pool (a name failing +50%/8y while clearing the CAGR and
                                 #   range bars barely exists there). The backtest neither convicts nor acquits: arming it live at 50
                                 #   stays a judgement call riding the live receipt above.
  growth_min_20y_pct: -1e9       # MEASURED INERT 2026-08-03 (5-rung stress ladder: +200/+400/+800/+1500 vs off). All four rungs returned
                                 #   BYTE-IDENTICAL numbers to baseline at all three horizons — edge delta +0.0, n unchanged 520/735/925,
                                 #   every book metric the same. Even a +1500% 20-year cumulative bar rejects nothing, because a 20Y leg the
                                 #   quote does not have is SKIPPED, never failed, and essentially nothing in the backtest pool carries one.
                                 #   So this knob cannot be graded by ANY value: it is not "measured neutral", it is unreachable. Setting it
                                 #   is harmless and pointless. Leave it off unless the fetch window ever grows real 20Y legs.
                                 #   ---- original note ----
                                 #   OFF, and unlike the 8Y rung above this one really does reject NOBODY at the +200 requested — confirmed on
                                 #   the same live run (5.65%/yr; growth_min_cagr 16.0 implies +1846% over 20y).
                                 #   READ THIS BEFORE ARMING IT: the bar reaches LONG-LISTED STOCKS ONLY. No UCITS ETF and no coin in the
                                 #   universe has 20 years of history, so their 20Y leg is `n/a` and is SKIPPED — a missing leg is never read
                                 #   as a failed one (that rule is the request's "if value is not N/A", pinned by test). Consequence worth
                                 #   stating plainly: this gate is harshest on the longest records and silent on the youngest, which is the
                                 #   opposite of the intuition. It was asked for to catch a real pattern the CAGR gates miss — under
                                 #   use_life_cagr the leg check and the life check are the SAME number, so nothing reads the 20Y window at
                                 #   all, and a name whose great decades are early clears every bar on history it will not repeat. PGR is the
                                 #   live case: +418% over its last 20y = 8.6%/yr, behind a 16%/yr life CAGR. At +200 it clears easily —
                                 #   catching PGR needs a bar near the CAGR-implied level, which is a much bigger change and unmeasured.
                                 #   SWEPT 2026-07-30 (+200, per-knob stress grid): inert there too (+0.7 edge at king = a bottom-half
                                 #   sliver; 8y-s byte-identical). Live and backtest agree the bar rejects ~nobody — cosmetic at 200.
  perf_fill_coverage_pct: 90.0   # DISPLAY ONLY — this knob cannot move a rank, a score or a gate. 100.0 = off (the code default).
                                 #   The problem: YRS rounds to nearest, a leg needs its full span minus 31d, so each rung has a ~5-month
                                 #   band where the row contradicts itself — WTAI.MI at ~7.7y printed "YRS 8" beside "8Y n/a" (8Y needs 7.91y).
                                 #   YRS now carries a decimal (7.7), and a cell inside that band prints the WHOLE-LIFE MEASURED return marked
                                 #   `≈`. NOT a projection: `(1+CAGR)^N` re-renders two columns already on screen while READING as a
                                 #   measurement, and on WTAI it swings 7.6x on which CAGR feeds it (+3141% at the +19% headline vs +414% at
                                 #   the real recent 8.5%/yr). The value is deflated by live EU HICP like every other >=1Y cell (inflation_adjust
                                 #   is on) — a nominal fill in a real column would be ~25pp wrong on an 8-year span.
                                 #   MEASURED live 2026-07-28 at 90 — six cells fill, and note the count only became visible once YRS carried a
                                 #   decimal (a rounded-YRS reading of the same table predicted three):
                                 #     8Y  — WTAI.MI 7.7y (96%) ≈+176.7% · XAIX.DE 7.5y (94%) ≈+206.6% · PANX.PA 7.3y (91%) ≈+180.7%
                                 #     20Y — NASD.L 18.5y (93%) ≈+1459% · EQQQ.MI 18.5y (93%) ≈+1233% · V 18.3y (92%) ≈+924.6%
                                 #   The bar is doing real work at the boundary: XMTD.L 17.6y and XMUS.L 17.5y are both 88% of the 20Y rung and
                                 #   stay n/a, as do XACT-BULL 16.7y, XLKS.L 16.6y, CSNDX.SW 16.5y and ANX.PA 16.1y.
                                 #   WHY 90 AND NOT LOWER: 0A08.L (iShares JPM EM Bond, ranked in the 2026-07-27 run) sits at 87.6% of the 8Y
                                 #   rung and its first close is a data artifact — CAGR +62%/yr against a 5Y of +17.3%, so its whole-life return
                                 #   is ~+3900%. 90 blocks that by 2.4 points; anything under ~88 prints it as if it were an 8-year number.
                                 #   PROOF IT CANNOT MOVE A RANK, both ways: the same binary run at 100.0 (off) and at 90.0 produced an
                                 #   IDENTICAL ranking in all three lanes; and statically, `perf_fill` has exactly two callers (the perf cell and
                                 #   the legend collector) while `life_return_pct` has exactly one reader (`perf_fill`) — no path into scoring.
  min_avg_turnover_eur: 500000   # LIVE: reject names with < this avg daily turnover (EUR) on THIS listing — both lanes.
                                 #   Set 2026-07-02 at 100K after zeroing the turnover SCORE tilt (which never docked
                                 #   junk): a hard gate is the honest tool for untradeable listings. 100K killed the real
                                 #   junk (Expat Romania €11K/day, Expat Greece €0K, GraniteShares FAANG €0K).
                                 #   Raised to 500K 2026-07-03 (live full-screen A/B vs 100K): drops exactly 4 thin ETF
                                 #   listings — both silver ETCs (XSLE.DE €327K, XSLR.MI €125K, commodity spikes ranked
                                 #   #1-2 on sub-5y history) + CHIP.SW €266K + WTEC.SW €164K (side listings; semis/tech
                                 #   coverage kept via VVSM.DE €38M, SEMI.AS €6M, XDWT.L €3M). Stocks + crypto tables
                                 #   byte-identical. Turnover is PER LISTING, not fund AUM — 500K still spares any
                                 #   listing a retail order can actually exit. Pinned watchlist names bypass all gates
                                 #   (sentinel score). BACKTEST-BLIND (backtest quotes carry no turnover) — the
                                 #   validated edge and CI gate are untouched. 0 = off
  endpoint_smooth_days: 105      # LIVE+BACKTEST: the "current price" used by the LONG-horizon MEASUREMENTS (>=1Y
                                 #   perf %/long CAGR legs, range position, drawdown — so the hard range gate) = mean
                                 #   of the closes in the last N TRADING DAYS. 105 ≈ 5 months. The span converts to
                                 #   bars by the run's cadence (105 daily closes live; 5 monthly bars in the 12y
                                 #   backtest) so the smoothing covers the SAME calendar time both sides — train ==
                                 #   serve. What stays RAW on purpose: the displayed price, the short legs (1D/1W/1M
                                 #   incl. the 1M knife — a months-wide average would make "this month" meaningless),
                                 #   and the overext brake (smoothing it measured WORSE: it under-reads fresh spikes).
                                 #   VALIDATED 2026-07-02, window swept on the monthly engine (bars: edge):
                                 #   1: +109.7 | 2: +103.3 | 3: +109.4 | 5: +115.7..+120.2 | 8: +103.0 — clean hump
                                 #   at 5 bars ≈ 105 trading days, all OOS halves +. 1 = raw last close (old
                                 #   behaviour). Edge-affecting: re-validate `backtest 12 universe` before changing.

  # --- SCORE — ON-SALE LANE (`buy_score`) — [FOIL] ALL of this section is backtest-only; `screen` ignores it ---
  normal_volatility_pct: 2.0     # [FOIL] a "typical" daily swing %; the dip is scaled by normal/asset vol,
                                 #   so a calm name's pullback counts for more than a wild one's
  discount_cap: 35.0             # [FOIL] cap on that volatility-scaled dip (one very deep name can't dominate)
  discount_weight: 0.35          # [FOIL] (#4) multiplier on the dip reward — on-sale dip-depth carries no selection skill (rho
                                 #   negative on 53 names, ~0 on the 354-name wide sample), so 0.35 shifts weight to the
                                 #   CAGR/sharpe terms WITHOUT gutting on-sale scores (0.15 dropped normal names below
                                 #   min_score for only a noise-level rho gain). 1.0 = old behaviour, 0 = drop it entirely.
  # Goal = multi-decade buy-and-hold, so momentum is NEUTRAL (1.0/1.0): a green/red WEEK is noise
  # over a 10-50y hold and must not reorder the ranking. Want a short-term timing view (e.g. for
  # `check`)? Set bounce ~1.3 / knife ~0.6 to reward a confirmed turn and dock a falling knife.
  momentum_bounce: 1.0           # [FOIL] discount ×this on a green week off a dip — 1.0 = ignore weekly timing
  momentum_knife: 1.0            # [FOIL] discount ×this while still falling — 1.0 = ignore weekly timing
  # (A) Long-trend reward = the proven-compounder signal for a decades hold, now on the long leg's
  # CAGR (Compound Annual Growth Rate, %/yr — annualized so a 10Y and a 20Y leg compare fairly).
  long_trend_weight: 0.5         # [FOIL] on-sale reward per %/yr of long-leg CAGR (×cap below = max bonus, ~15)
  long_trend_cap: 30.0           # LIVE+FOIL (SHARED). (#3k) 2026-07-29 RESTORED at 30 on the first-ever
                                 #   20y-STRESS ladder (the goal horizon, corpses injected), trend weight at
                                 #   its new 0.15: 15 +559.3 | 20 +579.7 | 25 +593.0 | 30 +623.5 (peak) |
                                 #   40 +589.1 | uncapped-at-0.35 +552.5. Clean hump, peak 30. The old 12y
                                 #   curve preferred 25 (+191.2 vs 30 +181.3) — measured at trend 0.35, on
                                 #   the advisory horizon; 20y king overrules [RETIRED TIE-BREAK — see
                                 #   SHIP RULE v2 at the top of this file. This rung was chosen by a rule
                                 #   that no longer exists, on the blurriest lane in the grid, against a
                                 #   12y curve pointing the other way. Under v2 that reads UNRESOLVED.
                                 #   RE-OPENABLE, deliberately NOT re-opened here: the receipt below says
                                 #   the cap tunes lane edge only and leaves rank-1 identical at every
                                 #   rung, so it is a weak lever — re-test 25 only if something else
                                 #   makes it worth 6 runs]. Rank-1 med/h2h IDENTICAL
                                 #   (+4.4 / 41%) across the whole ladder — the cap tunes lane edge only,
                                 #   the argmax cure lives in the WEIGHT (see growth_trend_weight).
                                 #   (#47) 2026-08-03 arm C1 pushed it the OTHER way, 30 -> 60, on the same 20y-stress king:
                                 #   lane edge -115.5 (+337.3 vs +452.8), 12y -1.2, 8y -13.1. Rank-1 median IDENTICAL at all
                                 #   three (+6.4/+7.2/+8.2), which reconfirms the "cap tunes lane edge only" line above on a
                                 #   THIRD independent pool. King h2h did rise 76% -> 81% (the best reading in that 27-run
                                 #   grid) — recorded, not shipped: it is one metric moving against edge, cap, AND the (#3g)
                                 #   curve that already found the optimum BELOW 30. Uncapping is now measured in both
                                 #   directions and loses in both. 30 STANDS.
                                 #   history: #16 sweep 2026-06-28: 20/30/40/50/1000 all edge ≈ -100337360 (flat) → cap inert on that run; 2026-07-02 re-test 30→50 on `backtest 12 universe` DROPPED growth edge +103.2→+87.7 → 30 read protective (parabolic names mean-revert), kept 30; (#3g) 2026-07-26 full curve confirmed the ceiling wanted to go DOWN (25 +191.2) not up; (#3h) same day removed it anyway on request, measured cost -11.5 edge at 12y; (#3k) restored on 20y-stress measurement
  health_zero_cagr: -10.0        # [FOIL] long-leg CAGR (%/yr) where on-sale trend_health hits 0; health=1 at flat/rising

  # --- (B) total-return inputs + (E) endpoint-robust CAGR + (#8) geomean shape — EDGE-AFFECTING, validate before trusting ---
  # All flow to LIVE screen AND the backtest (train==serve), so a `backtest 12 universe` measures them
  # honestly. UNVALIDATED at flip time — run .claude/run_me.sh steps B/E and REVERT any that drops rho/edge
  # or pushes an OOS half negative. Reversible (set back to false).
  splice_max_weekly_rate: 2.0    # (splice) 2026-07-29: trim history BEFORE the last step whose implied
                                 #   WEEKLY growth factor exceeds 2.0 (or drops under 0.5) — vendor series
                                 #   that glue two denominations (0A08.L: MXN head, GBP tail, ×19.59 in 28d)
                                 #   otherwise print fictional life CAGRs (+62%/yr vs the fund's true +6.7)
                                 #   and fake −99% MAXDDs on the ÷100 GBp→GBP cluster. 2.0 sits in a MEASURED
                                 #   VALLEY: fastest real survivor 3USL.L 1.87×/wk (3× leveraged ETP), slowest
                                 #   splice COFF.L 2.06×/wk, nothing between; on the down half every real
                                 #   crash clears the 0.5 mirror (NCLH 0.744 Mar-2020, CVNA 0.576) while the
                                 #   redenominations land 0.32–0.35. Weekly RATE not raw ratio (NVR ×26/92d =
                                 #   1.28×/wk survives); sub-week gaps clamp to 7d (a lone −10% day is 0.9,
                                 #   not 0.9^7); crypto exempt (SHIB really did 13×/wk). Trims 138/3346
                                 #   non-crypto series (4.1%); median trimmed series loses 72% of its record —
                                 #   correct: leveraged ETPs that fell ~99% then reverse-split are two
                                 #   instruments glued together, and growth_min_age_years gates the stubs.
                                 #   BACKTEST-BLIND: changes the INPUT universe, so before/after edges are two
                                 #   different measurements, never an A/B. 0.0 = off.
  growth_proximity_weight: 1.0   # (#48) SLOPE of the distance-below-own-high dock: score ×(1 + w·(range_pct/100 − 1)).
                                 #   1.0 = the raw `range_pct/100` multiply the lane has always shipped. Written here
                                 #   EXPLICITLY (it is also the code default, so this line changes no behaviour) because
                                 #   until 2026-08-05 this was the last backtest-reachable term in the growth lane with
                                 #   NO KNOB — it appeared in no ablation table and no curve ever run, while being worth
                                 #   up to 20% of the whole score. It now has a knob, an ablation row and a curve.
                                 #
                                 #   THE QUESTION IT WAS BUILT TO ANSWER: "shouldn't a name FURTHER below its high rank
                                 #   HIGHER?" — i.e. should w be NEGATIVE. There was real evidence on both sides. FOR:
                                 #   entry windows classed by the BENCHMARK's drawdown, same top-10 book held 20y, give
                                 #   near-high +11.6%/yr (excess +5.2, n=21) vs drawdown ≤−15% +14.0%/yr (excess +6.0,
                                 #   n=11) — but that is a DEPLOYMENT finding (identical book in every row), not a
                                 #   selection one. AGAINST: the range-GATE ladder is monotone the other way (90 +428.7 |
                                 #   80 +410.8 [shipped] | 70 +381.8 | 60 +380.9 | 50 +381.0), and the on-sale lane's
                                 #   `discount_weight` ablation is Δ+35.8 — zeroing the dip reward IMPROVES that lane.
                                 #   So the GATE stayed at 80 (measured) and only the SLOPE was swept (unmeasured).
                                 #
                                 #   METHOD, 9 runs `{20,12,8} universe stress`: an 8-rung curve [−1.0 −0.5 0.0 0.5 1.0
                                 #   1.5 2.0 3.0] as a screen, then ARMS at −1.0 and 0.0 for the rank-1/head-to-head the
                                 #   curve cannot print (it emits only rho/edge/winsor/OOS — a curve-only design could
                                 #   not have judged a rank-1 ship rule at all). SOUNDNESS: weight_curve re-scores a set
                                 #   fixed at the shipped tuning, valid only if the swept knob cannot change WHO is
                                 #   admitted — it cannot, the range gate reads `quote.range_pct` directly, before and
                                 #   independently of this multiplier. TIE-BACKS, all six exact: each arm's edge equals
                                 #   its curve rung (off +404.6/+151.7/+63.8, −1.0 +413.7/+150.4/+62.9), the curve's 0.0
                                 #   row equals the ablation `proximity` row, and its 1.0 row equals the lane headline.
                                 #
                                 #   VERDICT: NOTHING SHIPS, 1.0 stands — and the reason is not that the alternatives
                                 #   lose narrowly, it is that THIS KNOB HAS ZERO RANK-1 AUTHORITY. At the 20y king the
                                 #   served top-10 AND top-50 books are IDENTICAL at w = −1.0 / 0.0 / 1.0: +12.6%/yr,
                                 #   excess +5.4 (med +5.2), win 97% of 39, worst −0.5, OOS +5.1/+5.7, 4 zeros/481 holds.
                                 #   So is rank-1: +6.4 median, win 87% of 39, h2h 16/21 (76%) in all three arms. The
                                 #   knob is NOT inert — ranks 2-50 visibly reorder (the 21-50 slice moves +0.9 -> +0.2)
                                 #   — it just never reaches the argmax. Same trap `growth_overext_floor` records ("ZERO
                                 #   rank-1 authority") and `growth_max_peg` states in words ("the king CANNOT vote").
                                 #
                                 #   arm          20y r1 med / h2h / edge      12y edge      8y edge
                                 #   1.0 SHIPPED  +6.4  76%  +452.8            +127.9        +65.0
                                 #   0.0 off      +6.4  76%  +404.6 (−48.2)    +151.7 (+23.8) +63.8 (−1.2)
                                 #   −1.0 invert  +6.4  76%  +413.7 (−39.1)    +150.4 (+22.5) +62.9 (−2.1)
                                 #
                                 #   Two things that table says beyond the null. (a) HORIZON-CONFLICTED on lane edge:
                                 #   the term PAYS +48.2 at 20y and COSTS 23.8 at 12y. The 20y king decides, so it stays
                                 #   on — but nobody should quote "proximity is worth +48" without the 12y sign flip.
                                 #   [SHIP RULE v2, 2026-08-06: "the 20y king decides" IS RETIRED. This is the clearest
                                 #   case in the file of the retired tie-break doing real work — the term pays at 20y
                                 #   and COSTS at BOTH shorter horizons (-23.8 at 12y, -1.2 at 8y), which under v2 is
                                 #   UNRESOLVED, i.e. it would not ship today on this evidence. It is NOT reverted here,
                                 #   for the reason clause (b) below already gives: the whole ladder is inside the
                                 #   bootstrap band and rank-1 med/h2h are IDENTICAL (+6.4 / 76%) at 1.0, 0.0 AND -1.0.
                                 #   A knob with zero rank-1 authority and a coin-flip lane edge is not worth 6 runs to
                                 #   revert. Recorded so the NEXT reader knows this rung rests on a retired rule, not on
                                 #   a measurement that survives the current one. Re-open only alongside a real cause.]
                                 #   (b) INSIDE THE NOISE anyway: the whole 8-rung ladder spans ~50 edge at 20y against a
                                 #   bootstrap band [+201.5 … +673.7] that is 472 wide; 24 against 102 at 12y; 10 against
                                 #   56 at 8y. rho is flat (+0.17/+0.15/+0.21) at essentially every rung and both OOS
                                 #   halves stay positive throughout. Every rung is a coin-flip on edge alone.
                                 #
                                 #   ON THE NEGATIVE RUNGS SPECIFICALLY: inverting is uniformly slightly WORSE than
                                 #   simply neutralizing, at all three horizons. If the 12y result were ever acted on,
                                 #   the move would be w -> 0 (stop paying for proximity), never w < 0 (pay backwards).
                                 #   And a cost no metric here can see: at w=−1 the #1 pick is by construction the name
                                 #   sitting CLOSEST to the rejection line (81% of range against an 80% gate), one bad
                                 #   week from leaving the table — an inverted slope concentrates the book at the
                                 #   boundary of its own admitted set. Prediction was recorded BEFORE the run that the
                                 #   negative rungs would lose; they did.
                                 #
                                 #   TO REOPEN: a run where rank-1 median RISES at the 20y king with h2h ≥50% at all
                                 #   three horizons. Lane edge alone cannot carry it — the king cannot vote on this knob.
  growth_geomean_fold: false     # (#8) REVERTED 2026-07-02: folding proximity+value into the geomean DROPPED edge +103.2->+79.2 (Δ-24) and flattened OOS-early +0.06->-0.00. The raw proximity multiply carries real selection weight; the geomean's 4th-root softens it too far. Golden rule reverts. Knob kept inert in code. NOTE (#48): that Δ-24 is the ONLY prior evidence this term carried weight, and the 2026-08-05 sweep at growth_proximity_weight above re-reads it — softening proximity does cost 20y edge, but the served book and rank-1 do not move at any slope, so what the fold cost was list ORDER below #1, not the pick
  print_acquisition_hazard: false # (#112 / round 3 §9) UNMEASURED, and unmeasurABLE from this data —
                                 # which is the point. Ships OFF, which is the caveat block every
                                 # golden holds. Prose only; no gate, no score term, no journal field.
                                 #
                                 #   THE HAZARD NOBODY NAMES. `#5` and `#6` are both about holds that
                                 #   ended BADLY and never entered the pool. This is the opposite
                                 #   corner and it is more common: the usual way a 20-year single-name
                                 #   hold actually ENDS is neither bankruptcy nor a sell decision, it
                                 #   is being ACQUIRED. Two things happen at once, and the second is
                                 #   the one this repo has a stake in. Compounding stops at a one-time
                                 #   premium — and the deal CRYSTALLISES the gain, paying on a date
                                 #   the holder never chose the exact tax that `capital_gains_tax_pct`
                                 #   and the whole after-tax deferral footer (#108) assume stays
                                 #   deferred for the full horizon. A takeout at year 7 does not just
                                 #   truncate the return; it converts the hold arm into something much
                                 #   closer to the rotate arm, which is the comparison that footer
                                 #   exists to make.
                                 #
                                 #   WHY IT IS PROSE AND NOT A GATE. Nothing in the walk models a
                                 #   takeout, so every multi-year hold in every table is one that was
                                 #   ALLOWED to run — optimistic in the same direction as `#5`, by an
                                 #   amount this pool cannot size, because an acquired name simply
                                 #   leaves the survivor universe and takes its own base rate with it.
                                 #   Pricing it at a guessed hazard rate would be inventing the number
                                 #   the footer is admitting it does not have. A hazard that cannot be
                                 #   quantified from the data should be NAMED, not silently priced at
                                 #   zero, and silently-at-zero is what OFF does — hence a knob whose
                                 #   only defect is that it starts off.
                                 #
                                 #   THE ONE DIRECTIONAL READ it does support: it argues mildly toward
                                 #   LARGER names, which are harder to swallow. That is not a new
                                 #   principle here — `growth_min_aum_etf` already accepts exactly that
                                 #   reasoning on the fund side, for the sibling reason (liquidation
                                 #   risk). Deliberately NOT wired into any stock-side size floor: the
                                 #   fund knob has a receipt and this argument has none.
                                 #
                                 #   THE RUN THAT WOULD SETTLE IT: an acquisition-date feed (delisting
                                 #   reason, not just delisting date) over the PIT pool, re-running the
                                 #   after-tax footer with holds truncated at the deal and taxed there.
                                 #   Until that exists the honest output is a named caveat.
                                 #   REVERT: set false.
  print_currency_mix: false      # (#112 / round 3 §9) UNMEASURED — display only, and deliberately
                                 # NOT a score input. Ships OFF, which is today's footer set.
                                 #
                                 #   THE UNDISPLAYED POSITION. A EUR investor holding a US-heavy book
                                 #   for twenty years carries a large unhedged currency bet, and no
                                 #   line in this tool shows it. The nearest thing is `print_lane`'s
                                 #   `market mix`, which counts LISTING COUNTRY across the stocks lane
                                 #   only. For a fund that is not merely incomplete, it is BACKWARDS: a
                                 #   Xetra-listed S&P 500 tracker reads "Germany" and is 100% dollar
                                 #   exposure. The book is mostly funds, so the one surface that talks
                                 #   about currency is silent about most of the position and wrong
                                 #   about the rest.
                                 #
                                 #   WHAT IT PRINTS: one line, currency shares over the top-10 book,
                                 #   funds looked THROUGH to the holdings already fetched for
                                 #   `fund_pe` (zero extra requests). Equal weight per row, matching
                                 #   `market mix`'s counts — nothing on this surface knows position
                                 #   sizes (`size` decides those separately), so a weighted mix would
                                 #   be reporting weights it invented.
                                 #
                                 #   THREE HONESTY RULES, each pinned by a test: a fund with no
                                 #   look-through counts as `?`, NEVER as its own listing currency —
                                 #   an honest blank beats the confident wrong number this footer
                                 #   exists to correct. A coin is its own bucket, because a `-EUR`
                                 #   suffix is a quote convention and a EUR holder of BTC carries
                                 #   bitcoin risk. And the line quotes the WORST per-fund top-10
                                 #   coverage as `≥N%`, so the reader knows how much of the mix is
                                 #   measured rather than extrapolated from the mega-caps.
                                 #
                                 #   WHY IT SCORES NOTHING. Whether currency concentration should tilt
                                 #   the rank is a measured question, and it has never been asked
                                 #   because the exposure was invisible. Making it visible is how the
                                 #   data to answer it starts existing. Gating on it first would be
                                 #   the same mistake `growth_fund_scope_by_class` records: paying for
                                 #   a factor before knowing its sign.
                                 #
                                 #   THE RUN THAT WOULD SETTLE IT: a `backtest universe` arm scoring
                                 #   holdings-weighted currency concentration as a tilt, graded on the
                                 #   ship rule. Note the pool is all-USD, so the term is backtest-blind
                                 #   exactly like the TER/FX/commodity docks — it would need a
                                 #   multi-currency pool before it could be swept at all, which is the
                                 #   real reason this ships as a printed line and not a weight.
                                 #   REVERT: set false.
  print_base_rates: false        # (#111 / round 3 §8) UNMEASURED — the instrument has not been read
                                 # yet. Ships OFF, which is the report every golden holds. Display
                                 # only; no score, no gate, no journal field.
                                 #
                                 #   THE MISSING DENOMINATOR. This lane's whole thesis is that a long
                                 #   compounding record predicts the next one, and `growth_min_cagr`
                                 #   does not merely TILT on that — it DELETES everything below the
                                 #   bar, so the bar is the universe. Nothing in this tool has ever
                                 #   asked what that bar's forward hit rate actually is. It prints:
                                 #   of the samples whose trailing leg had already cleared the bar,
                                 #   how many cleared it again over the forward window, BESIDE the
                                 #   fraction of the whole pool that did. The pair is the finding —
                                 #   30% is bleak against an unconditional 34% and strong against an
                                 #   unconditional 12% — and printing the conditional number alone
                                 #   would be a rhetorical device, which is why the fn returns both or
                                 #   nothing. One pass over samples already in memory.
                                 #
                                 #   THE BAR IS THE SHIPPED GATE, not a new threshold: a private one
                                 #   would answer a question nobody is asking. Note this can come back
                                 #   ANTI-predictive, and the printed read says so in those words
                                 #   rather than softening it. If it does, that is a finding about
                                 #   `growth_min_cagr` itself and belongs in a sweep, not here.
                                 #   REVERT: set false.
                                 #
                                 # (#124) MEASURED 2026-08-23, `universe fund pit` at 20y/12y/8y. The instrument has
                                 # now been read, and at the horizon this tool exists for the bar carries the WRONG
                                 # SIGN:
                                 #     20y     3/476  ( 1%) cleared the bar again   vs   2% of the whole pool
                                 #     12y    63/840  ( 8%)                         vs   5%
                                 #      8y   170/1157 (15%)                         vs   6%
                                 # The report prints its own verdict at 20y: "the record carries NOTHING - a
                                 # trailing bar is not a forecast here". Conditional BELOW unconditional is not a
                                 # weak result, it is the wrong direction: over 20y a name that had cleared 19%/yr
                                 # was LESS likely to clear it again than a name drawn at random from the pool. The
                                 # lane's whole thesis is that a long record predicts the next one, and
                                 # `growth_min_cagr` does not merely tilt on that record - it DELETES everything
                                 # below it, so the bar IS the universe. At 12y and 8y the pair is the right way
                                 # round and the thesis holds.
                                 # WHAT THIS LICENCES AND WHAT IT DOES NOT: it does NOT license a new value for
                                 # `growth_min_cagr`. This is one reading of one bar, at 20y n_eff 0.5 (#124 on
                                 # print_n_eff), and picking a value is a sweep this did not run. It DOES establish
                                 # that the bar is the thing to sweep, ahead of adding any new factor - which is
                                 # what the round that read it concluded, against its own prior.
                                 # Knob stays false. REVERT: set false.
  implied_growth_years: 0        # (#111 / round 3 §8) UNMEASURED — an unfitted judgement value,
  implied_growth_required_pct: 8.0 # shipping OFF (0 = no line), which is today's ranking footer.
                                 #
                                 #   WHAT IT PRINTS: for each of today's top-10 with a P/E, the EPS
                                 #   growth rate the CURRENT price already pays for, beside what the
                                 #   name has actually delivered. The most reliable human defence
                                 #   against buying hype is not a gate, it is being shown the claim
                                 #   you are implicitly making — and the tell is never the growth a
                                 #   company has posted, it is the growth the price REQUIRES. A
                                 #   ranking says "this one is best"; this turns it into a prediction
                                 #   someone can be wrong about, which is the whole point of printing
                                 #   it next to `print_base_rates`.
                                 #
                                 #   THE MODEL is the smallest one that inverts cleanly: hold N years,
                                 #   let the multiple revert to `ref_pe` (the same reference multiple
                                 #   `value_factor` tilts against — one definition of normal, not a
                                 #   second opinion), require `implied_growth_required_pct` %/yr:
                                 #     g = (P/E_now / ref_pe)^(1/N) × (1 + r) − 1
                                 #   DIVIDENDS IGNORED, which makes the printed number CONSERVATIVE
                                 #   for a payer. Stated rather than corrected: folding in the payout
                                 #   ratio would cost the one property that makes the line useful,
                                 #   that a reader can check its arithmetic in their head.
                                 #
                                 #   NEITHER VALUE IS FITTED. The horizon is the reader's holding
                                 #   period and the required return is the reader's hurdle rate; the
                                 #   printed answer moves one-for-one with the latter, which is why
                                 #   the line prints both assumptions beside the result instead of
                                 #   presenting a number. 8.0 is a plain long-run equity hurdle.
                                 #   REVERT: set implied_growth_years to 0.
  entry_excess_yield_band: 0.0   # (#110 / round 3 §7) UNMEASURED — an unfitted judgement value,
                                 # shipping OFF, which is the one-axis footer every golden holds.
                                 # Display-only; it does NOT touch the deploy multiplier.
                                 #
                                 #   THE GAP: the deploy rule is right for the wrong reason at both
                                 #   ends. `entry_state_class` keys on distance from the S&P high and
                                 #   its receipt (+9.1 drawdown / +6.0 pullback / +5.9 near-high, 12y
                                 #   multi-regime) is real — but distance-from-high is a PATH signal.
                                 #   After a decade-long grind up a market can be at a nosebleed
                                 #   valuation AND at its high, where the rule says deploy slowly,
                                 #   correctly, by accident. After a fast crash from a cheap base it
                                 #   says deploy fast — also correct, also by accident.
                                 #
                                 #   THE SECOND AXIS: starting valuation is the LEVEL signal with the
                                 #   strongest long-horizon evidence, and the cheapest honest version
                                 #   is already on disk — index earnings yield minus Euribor 3M, i.e.
                                 #   Shiller's excess CAPE yield in one subtraction. The anchor is the
                                 #   first CORE name carrying a look-through P/E, so the line cannot
                                 #   quote a valuation for a market the CORE shortlist would not buy,
                                 #   and the rate is the one `print_macro_footer` just printed rather
                                 #   than a second fetch that could differ from it. TRAILING, not
                                 #   cyclically adjusted — a real difference from Shiller, which is
                                 #   why the printed line names its inputs and not just its verdict.
                                 #
                                 #   VALUE: the band is symmetric, in percentage points of excess
                                 #   yield, inclusive at both ends. There is NO measured value for it,
                                 #   and picking one from a chart of the last decade would be fitting
                                 #   a threshold to the sample it will be judged on.
                                 #
                                 #   THE RUN THIS EXISTS TO FEED: bucket entries by the 2x2 (level ×
                                 #   path) on the same backtest that produced the +9.1/+6.0/+5.9 row,
                                 #   and read the two DISAGREEING corners — "cheap at the high" and
                                 #   "rich and falling". The agreeing corners tell you nothing either
                                 #   axis did not already say. Only after that does moving
                                 #   `deploy_multiplier` onto the 2x2 become a decision rather than a
                                 #   guess, and that move is ship-rule graded, not a print knob.
                                 #   REVERT: set 0.
  growth_acc_drag: false         # (#109 / round 3 §6) UNMEASURED, and UNMEASURABLE on this pool —
                                 # `use_of_profits` is None for every backtest quote, so this is
                                 # backtest-blind by construction like the TER and commodity docks
                                 # and no sweep can ever price it. Ships OFF, which is today's lane.
                                 #
                                 #   THE FINDING: the right answer, from the wrong mechanism. For a
                                 #   Portuguese resident an ACCUMULATING UCITS defers everything to
                                 #   one final sale while a DISTRIBUTING one pays the payout tax every
                                 #   year for twenty consecutive years, and the compounding of what
                                 #   was taken out never happens. That is plausibly a larger drag than
                                 #   any TER difference `growth_ter_drag` models to four decimals.
                                 #   FolioMan tracks Acc/Dist, ALERTS when a share class changes, and
                                 #   has no score term for it. The Acc twin still wins — but only
                                 #   because `use_adjusted_close: false` makes the ranking's CAGR
                                 #   price-only, so a Dist fund's payouts leave the NAV and reach the
                                 #   score as a depressed price series. fetch.rs says so outright:
                                 #   "Acc twins win by construction."
                                 #
                                 #   WHY OFF IS CORRECT AND NOT MERELY SAFE. While the CAGR is still
                                 #   price-only, turning this on DOUBLE-COUNTS the drag: the price
                                 #   series charges it once and the damp charges it again. The term
                                 #   becomes the right mechanism exactly when the accident is removed
                                 #   — `use_adjusted_close: true` WITH the benchmark switched to a
                                 #   total-return index in the same change (round 2 C2, and that
                                 #   pairing is itself load-bearing: the old "no effect" verdict on
                                 #   use_adjusted_close was measured with both legs price-only, i.e.
                                 #   on a broken instrument). These two knobs move together or the
                                 #   lane is wrong in one direction or the other.
                                 #
                                 #   COUPLED TO tax_keep, deliberately: the payout tax is the same
                                 #   keep-fraction the dividend reward nets at, so at the code default
                                 #   `tax_keep_other: 1.0` the dock is inert even when switched on. A
                                 #   private second rate here would have let this term charge tax on a
                                 #   payout the dividend reward was scoring as untaxed.
                                 #   REVERT: set false.
  capital_gains_tax_pct: 28.0    # (#108 / round 3 §5) NOT A MEASUREMENT — a tax rate, and the exact
                                 # constant the hardcoded `CAPITAL_GAINS_TAX = 0.28` carried. Its old
                                 # comment said "add a knob only if a second rate is ever needed";
                                 # `cgt_hold_schedule` below is that second rate. Reaches ONE printed
                                 # line in `report_lane` — no score, no gate, no journal field.
                                 # REVERT: nothing to revert; changing it changes only that line.
  cgt_hold_schedule: []          # (#108 / round 3 §5) UNMEASURED and, more to the point, UNVERIFIED
                                 # as a matter of law. Ships EMPTY = one flat rate at every horizon,
                                 # which is the after-tax line every golden holds.
                                 #
                                 #   THE DEFECT. `after_tax_pair` compares never-sell against yearly
                                 #   rotation on the same pre-tax path, and it taxed BOTH arms at the
                                 #   same flat rate. Where a jurisdiction's effective rate falls with
                                 #   the holding period, that is wrong in one specific direction: it
                                 #   UNDERSTATES the deferral edge, which is the single number in this
                                 #   whole report that most supports the tool's own buy-and-hold
                                 #   thesis. An error that flatters the conclusion would be the
                                 #   dangerous one; this is the opposite, which is why it survived.
                                 #
                                 #   WHAT SHIPS: the mechanism, not a jurisdiction. Each rung is
                                 #   `{min_years, excluded_pct}`, the widest satisfied rung wins
                                 #   (yaml order irrelevant), and the exclusion applies to the GAIN
                                 #   before the headline rate. The rotation arm holds for a year by
                                 #   definition, so it is pinned to the bottom rung while the held arm
                                 #   climbs — the schedule can therefore only ever widen the printed
                                 #   deferral edge, never narrow it. `a_holding_period_schedule_can_
                                 #   only_widen_the_deferral_edge` pins all three claims.
                                 #
                                 #   WHY NO NUMBERS. NO TAX LAW IS ENCODED IN THIS CODEBASE — the
                                 #   knobs ARE the tax model, and that rule is exactly why a value is
                                 #   not filled in here. The round-3 note reads Art. 43 CIRS as
                                 #   amended by Lei n.º 31/2024 to give listed shares and open-ended
                                 #   UCITS a 10/20/30% exclusion at >2y / >5y / 8y+ — 28% becoming an
                                 #   effective 25.2 / 22.4 / 19.6% — with englobamento optional. It
                                 #   also records that two secondary sources agreed and a third
                                 #   described only the older SME-specific regime, and says plainly
                                 #   that this is the kind of thing a budget law changes. That is a
                                 #   citation, not a verification. To turn it on:
                                 #     cgt_hold_schedule:
                                 #       - { min_years: 2, excluded_pct: 10 }
                                 #       - { min_years: 5, excluded_pct: 20 }
                                 #       - { min_years: 8, excluded_pct: 30 }
                                 #   CONFIRM WITH AN ACCOUNTANT FIRST. Nothing in this repo has, and
                                 #   the shipped empty list is that fact written down rather than
                                 #   assumed away. REVERT: set [].
  rank_perturb_k: 0              # (#107 / round 3 §3) UNMEASURED — an unfitted judgement value,
                                 # shipping OFF, which is the point-estimate ranking every golden
                                 # holds. Display-only and it does NOT reorder the book.
                                 #
                                 #   THE GAP. McLean & Pontiff put a published predictor's decay at 26%
                                 #   out-of-sample and 58% post-publication. The defences here are real
                                 #   — walk-forward, chronological OOS halves, ship rule v2, an honest
                                 #   `tune` that searches on TRAIN and scores on a TEST it never saw —
                                 #   and they all share one limit: they produce exactly ONE knob vector.
                                 #   Under it every name's printed rank is a point estimate with no
                                 #   error bar. Nothing in the tool has ever asked whether a name ranks
                                 #   #2 because it is good or because `sharpe_weight` happens to sit
                                 #   where it sits.
                                 #
                                 #   WHAT IT DOES: k re-scores of the universe, each with all 14 tilt
                                 #   weights (`picks::WEIGHT_DIMS`) scaled by an independent U(0.8,1.2),
                                 #   then prints today's top-10 by MEDIAN rank with its IQR:
                                 #   `NVDA #3 (IQR 2–6)` vs `XYZ #4 (IQR 4–71)`. The second name is a
                                 #   knob artefact and the score alone cannot say so.
                                 #
                                 #   SCOPE, deliberately: weights only, no gates. A gate is a reject,
                                 #   not a tilt, so perturbing one changes the ranked POPULATION rather
                                 #   than the order inside it, and the honest statistic for that is
                                 #   P(name survives), not a rank spread. Keeping gates fixed is also
                                 #   what makes the output readable — every rank vector has exactly k
                                 #   entries, so a wide IQR is always about order and never about a
                                 #   name dropping out of the pool. A weight shipping at 0 stays 0
                                 #   under a multiplicative draw, which is correct: a term that is off
                                 #   cannot be a source of fragility.
                                 #
                                 #   COST: k full universe re-scores per `screen`, which is why 0 is
                                 #   the default rather than a small always-on number. 200 is the value
                                 #   the round-3 note proposes; nothing here has measured what k the
                                 #   IQR stabilises at, so pick one by watching the printed IQR stop
                                 #   moving, not by trusting 200.
                                 #
                                 #   NOT DONE HERE, and it is the bigger half: ranking the BOOK by
                                 #   median rank instead of by point score. That ships a different book
                                 #   — it quietly drops exactly the population that decays after
                                 #   publication, which is the point — so it is a ship-rule decision
                                 #   graded on `backtest universe` at 20y/12y/8y, not a print knob.
                                 #   REVERT: set 0.
  print_book_deciles: false      # (#106 / round 3 §4) UNMEASURED — an unfitted judgement value,
                                 # shipping OFF, which is the mean-only ladder every golden holds.
                                 # Display-only, so "unmeasured" means the instrument has not been read
                                 # yet, not that a value is unfitted.
                                 #
                                 #   THE DEFECT, and it is arithmetic rather than an empirical claim.
                                 #   The per-bucket statistic this report computes is right: a held book
                                 #   earns ann(MEAN OF TERMINAL MULTIPLES), because a 20x winner covers
                                 #   twenty -100% zeros. But those per-bucket numbers are then
                                 #   ARITHMETICALLY AVERAGED ACROSS BUCKETS into the headline, and
                                 #   VERDICT_TOP: 3 is the argmax of that headline over TOP_LADDER's 13
                                 #   rungs. Under positive skew small N maximises the MEAN while
                                 #   wrecking the median and the left tail — so selecting a basket size
                                 #   by mean excess selects the most concentrated book almost by
                                 #   construction. The ladder is not measuring which N is best; it is
                                 #   partly measuring which N is most concentrated.
                                 #
                                 #   WHAT IT PRINTS: one row per ladder rung — d10, median, d90 of the
                                 #   book's terminal multiple across windows, plus P(book < index) and
                                 #   P(book < 1.0x). That last one is the number a 20-year holder
                                 #   actually feels: not "did I trail the index" but "did I end with
                                 #   less than I started", and nothing in this report has ever printed
                                 #   it. Shares `book_multiples` with `book_stats`, so the distribution
                                 #   and the headline mean cannot be two different arithmetics.
                                 #
                                 #   WHAT IT DOES NOT DO: re-derive VERDICT_TOP. That needs the wide run
                                 #   with this on, at 20y/12y/8y, reading the MEDIAN and the 10th
                                 #   percentile rather than the mean. The round-3 author's prediction —
                                 #   recorded as a prediction, worth nothing until run — is that N moves
                                 #   well above 3, and that the mean-vs-median gap between N=3 and N=15
                                 #   is the clearest picture of skew this tool could produce. Changing
                                 #   VERDICT_TOP is a ship-rule decision and does not ride on a print
                                 #   knob. REVERT: set false.
                                 #
                                 # (#124) MEASURED 2026-08-23, `universe fund pit` at 20y/12y/8y. The skew
                                 # arithmetic this knob was written to expose is real and large at 20y:
                                 #     top-1    d10 0.05x   median 3.78x   P(book<1.0x) 40%   P(book<index) 50%
                                 #     top-3    d10 0.88x   median 4.59x   P(book<1.0x) 15%   P(book<index) 40%
                                 #     top-10   d10 1.30x   median 5.51x   P(book<1.0x)  5%   P(book<index) 45%
                                 # Concentration wrecks BOTH tails: the top-1 book ends below where it started 40%
                                 # of the time and its 10th percentile is a 95% loss, while the median IMPROVES all
                                 # the way out to top-10. That is the pattern (#106) predicted and an independent
                                 # confirmation of the basket (#120) already moved to.
                                 # THE UNPLANNED FINDING, and the more important one: the deciles are IDENTICAL from
                                 # top-10 through top-50, at EVERY horizon. The ladder is not flat because wider
                                 # baskets stop helping - it is flat because THE BOOK NEVER HOLDS MORE THAN ~10
                                 # NAMES. The gates admit too few survivors to fill a wider basket, so every rung
                                 # above 10 is the same book relabelled, and any past reading of the top-N ladder
                                 # that treated rungs 15-50 as distinct baskets was reading one basket 9 times.
                                 # Read beside (#104)'s capture numbers from the same runs: a book that cannot be
                                 # widened cannot hold winners it was never allowed to admit.
                                 # Knob stays false. REVERT: set false.
                                 # (#136) MEASURED 2026-08-25 — the basket question, put to the DISTRIBUTION and REFUSED.
                                 #   WHY THE ROUND RAN. Round 10's mean ladder showed top-5 beating top-10 on the PIT lane at
                                 #   every horizon, and at 20y/12y beating the MORE concentrated top-3 too. An interior knee is
                                 #   not something concentration can manufacture, so it was worth one reading. (#124) had already
                                 #   turned this knob on, but tabulated rungs 1/3/10 at 20y ONLY — rung 5 and the two shorter
                                 #   horizons were the gap.
                                 #   METHOD. Six runs off ONE overlay (historically tests/basket-probe.yaml, since DELETED —
                                 #   instrument overlays are recreated on demand, see the rule directly below):
                                 #       buy_heuristic:
                                 #         print_book_deciles: true
                                 #   `heuristic_drift` named that key and NOTHING else on all six.
                                 #   HOW TO RECREATE ANY INSTRUMENT OVERLAY CITED IN THIS FILE: write those lines to a file
                                 #   under tests/ whose name does NOT end in `ci-settings.yaml` — `ci_base_yaml` returns EMPTY
                                 #   for that suffix, which would make it a PURE fixture and silently swap ~200 tuned gates for
                                 #   code defaults — then point FOLIOMAN_CONFIG at it. It must live under tests/ because
                                 #   `config::anchored_path` resolves data paths from the config file's GRANDPARENT. That
                                 #   `heuristic_drift` names exactly the intended keys and no others IS the proof of the arm.
                                 #   `backtest {20,12,8} universe fund pit` and `backtest {20,12,8} universe stress`.
                                 #   windows scored 1123/2731/5941 (PIT) and 2374/4712/8389 (stress) — no OFFLINE collapse.
                                 #   Runs authenticate against round 10: 12y top-5 +3.2|+3.6 worst -6.4 OOS +2.1/+4.2 and
                                 #   8y top-5 +4.7|+4.7 worst -5.5 reproduce exactly.
                                 #
                                 #   THE BAR, FIXED BEFORE THE FIRST RUN. Mean excess is the statistic (#106) disqualified for this
                                 #   question, so it is not in the bar. Skew predicts small N buys a higher MEAN with a worse median
                                 #   and a fatter left tail, so the bar is purely distributional: top-5 beats top-10 only if, at
                                 #   12y AND 8y (the horizons (#124) says can adjudicate at all), median >= AND d10 >= AND
                                 #   P(book < 1.0x) <=. Any one clause failing REFUSES.
                                 #
                                 #   PIT, top-5 vs top-10:
                                 #     horizon   median          d10             P(book<1.0x)     verdict
                                 #     20y       5.14 vs 5.51 X  1.32 vs 1.30 ok 5%  vs 5%  ok    reported, not decisive (n_eff ~0.2)
                                 #     12y       3.68 vs 3.47 ok 0.83 vs 0.82 ok 18% vs 15% X     FAIL
                                 #     8y        2.69 vs 2.47 ok 1.09 vs 1.00 ok 7%  vs 12% ok    PASS
                                 #   VERDICT: REFUSED at 12y, on the ruin clause the bar existed to protect. Nothing changes.
                                 #   AND IT IS A THIN REFUSAL, said plainly: 18% vs 15% of 34 windows is SIX windows vs FIVE — one
                                 #   window. The 8y pass is two windows (3 vs 5 of 42). At this sample size the ruin clause is
                                 #   decided by single windows, and the median/d10 clauses at 12y are +0.21x and +0.01x apart, i.e.
                                 #   indistinguishable. The honest reading is not "top-10 wins" but "top-5 and top-10 are the same
                                 #   book wearing different labels, and the pre-registered bar refuses a change it cannot justify".
                                 #   Note the outcome does not turn on bar-versus-ship-rule wording: read as a v2 TIE (one horizon
                                 #   up, one down) it is UNRESOLVED -> NO SHIP, the same answer.
                                 #
                                 #   THE FALSIFIER DID NOT FIRE, so the bar is discriminating rather than merely rewarding size.
                                 #   Pre-registered: if the same three clauses passed monotonically down to top-1 on `stress`, the
                                 #   bar would be measuring concentration and the whole reading would be void. It does not — top-1's
                                 #   d10 is WORSE than top-10's at every stress horizon (3.79 vs 6.54, 1.86 vs 2.54, 1.23 vs 1.60).
                                 #
                                 #   THE FINDING THAT ACTUALLY SETTLES IT, and it is stronger than the bar. The optimum has NO
                                 #   STABLE LOCATION. Where each statistic peaks on PIT:
                                 #     statistic       20y          12y      8y
                                 #     mean excess     top-5/15/20  top-5    top-3
                                 #     median multiple top-10/15    top-3    top-2
                                 #   The two statistics disagree with each other AND the peak walks with the horizon. That is the
                                 #   signature of noise, not of an optimal basket size, and it is a direct independent vindication
                                 #   of (#120) fixing VERDICT_TOP a priori: there was never a stable argmax over these 13 rungs to
                                 #   find, so the best-of-13 search (#120) closed would have been fitting window-level luck.
                                 #
                                 #   WHAT CONCENTRATION COSTS, now printed at every horizon (PIT). The number (#106) said nothing
                                 #   had ever printed:
                                 #     horizon  top-1 d10 / P(book<1.0x)      top-10 d10 / P(book<1.0x)
                                 #     20y      0.05x / 40%                   1.30x / 5%
                                 #     12y      0.11x / 29%                   0.82x / 15%
                                 #     8y       0.68x / 14%                   1.00x / 12%
                                 #   A top-1 book ends below where it started 40% of the time at 20y and its 10th percentile is a
                                 #   95% loss. (#106)'s prediction that N moves "well above 3" is CONFIRMED on the left tail at
                                 #   every horizon, even though it is not confirmed as a knee at 5.
                                 #
                                 #   CORRECTION TO (#124), which recorded "the deciles are IDENTICAL from top-10 through top-50, at
                                 #   EVERY horizon". One rung too strong. Exactly flat from top-15 up at all three horizons; from
                                 #   top-10 only at 12y. At 20y top-10 -> top-15 moves P(book<index) 45% -> 40%, and at 8y d90
                                 #   moves 4.25x -> 4.23x. The mean ladder shows the same: holds rise 107 -> 114 (20y), 196 -> 209
                                 #   (12y), 267 -> 282 (8y) between top-10 and top-15, so a few windows DO admit 11-15 names.
                                 #   (#124)'s substantive point stands unchanged and is reconfirmed: the book cannot be widened
                                 #   past ~15 because the gates never admit that many, so rungs above it are one basket relabelled.
                                 #   The stress lane saturates a little later (~15-20) because it injects extra names.
                                 #
                                 #   THE THREE-BASKET MISMATCH, and it is SMALLER than it looked. Gates are tuned against top-10
                                 #   (VERDICT_TOP), the user is shown top-20 (top_picks), and round 10 measured the mean knee at
                                 #   top-5. But on PIT the top-20 and top-10 decile rows are identical at 12y and differ only in
                                 #   P(book<index)/d90 at 20y/8y — so two of the three "different baskets" are the same book.
                                 #   top_picks IS NOT CHANGED HERE and this receipt is not an argument to change it: it is a display
                                 #   cap on `check`'s candidate list, not a portfolio rule, and the backtest cannot grade it.
                                 #   What the admission ceiling does imply is that `check` will usually have fewer than 20 gated
                                 #   candidates to list at a cutoff, which makes top_picks: 20 harmless rather than wrong.
                                 #
                                 #   REVERT: set false (it already is — this knob ships OFF and stays OFF; it was read through an
                                 #   overlay, never shipped true). If a future round wants to re-open basket size, the bar above and
                                 #   the no-stable-peak table are the prior: bring a mechanism, not an argmax.
                                 #
                                 #   (#180) THE TOP-5 COLUMN, READ 2026-08-31 TO PRICE THE ONE NUMBER IN THE STACK
                                 #   THAT IS NOT 10 — `settings.top_picks`, which is 5 and is what
                                 #   `check`/`screen` actually shows a human. (#124) above priced top-1, top-3 and
                                 #   top-10 but never top-5, so the gap between the VALIDATED basket and the
                                 #   DELIVERED one had never been measured. This knob was turned on for three
                                 #   diagnostic runs, `backtest {20,12,8} universe fund pit`, then off again. Not
                                 #   a ship and not an argmax: `VERDICT_TOP` stays 10 ((#120)'s forbidden re-fit
                                 #   is untouched), and nothing below chooses a basket by maximising anything.
                                 #
                                 #   horizon  rung     d10     median   d90     P(book<index)  P(book<1.0x)   windows
                                 #     20y    top-3    0.88x   4.36x    9.59x       43%            14%          21
                                 #     20y    top-5    1.32x   5.01x    9.59x       38%             5%          21
                                 #     20y    top-10   1.30x   5.23x    9.59x       48%             5%          21
                                 #     12y    top-3    0.54x   4.57x   10.70x       26%            17%          35
                                 #     12y    top-5    0.83x   3.71x    9.27x       23%            17%          35
                                 #     12y    top-10   0.82x   3.68x    7.76x       26%            11%          35
                                 #      8y    top-3    0.94x   2.78x    7.20x       14%            12%          43
                                 #      8y    top-5    1.09x   2.61x    6.18x       19%             7%          43
                                 #      8y    top-10   1.05x   2.40x    4.64x       14%             9%          43
                                 #
                                 #   VERDICT: NO CHANGE. 5 IS ALREADY ON THE FLAT PART OF THE CURVE. The
                                 #   concentration cliff (#124) found is real but it is spent by rank 5, not rank
                                 #   10: from top-3 to top-5 the 20y left tail moves 0.88x -> 1.32x and P(book <
                                 #   1.0x) falls 14% -> 5%, while from top-5 to top-10 it moves 1.32x -> 1.30x and
                                 #   5% -> 5%. At 8y top-5 is the BETTER of the two on both tail measures (d10
                                 #   1.09x vs 1.05x, P(<1.0x) 7% vs 9%); at 12y top-10 wins the left tail (11% vs
                                 #   17%) and top-5 wins the median. Neither dominates. There is no measured case
                                 #   for moving `top_picks`, in either direction, and this entry exists so nobody
                                 #   spends another round looking for one.
                                 #
                                 #   AND IT COULD NOT HAVE HELPED ANYWAY — THE POOL CANNOT SUPPLY 10. The same
                                 #   runs print median gated pool 5 / 6 / 7 at 20y / 12y / 8y against a graded
                                 #   basket of 10, all three columns VACUOUS. The proof is in the table itself:
                                 #   top-10 and top-15 are BYTE-IDENTICAL at every horizon (identical d10, median,
                                 #   d90 and both probabilities), which is what saturation looks like — beyond ~10
                                 #   there is nobody left to add. So raising `top_picks` from 5 to 10 would, in
                                 #   the median window, pad the list with the tail of an admitted set that is
                                 #   barely larger than 5. This is `pool-supply-is-not-the-constraint` seen from
                                 #   the output end: (#173)/(#174) showed a +65% wider pond moves the gated median
                                 #   by zero names, and here that same binding gate stack is what caps the
                                 #   deliverable book.
                                 #
                                 #   A DORMANT INCONSISTENCY FOUND WHILE READING THIS, recorded because it is
                                 #   invisible until someone changes one number. `track::BOOK` is 10 and `track`
                                 #   grades the live book with `.take(BOOK)`, but the journal rows it takes from
                                 #   are produced by `picks::render`, which ends `tickers.into_iter().take(n)` at
                                 #   src/picks.rs:4117 with n = `settings.top_picks` = 5. So BOOK is INERT today:
                                 #   it is a ceiling that `top_picks` already binds well below. Nothing is wrong
                                 #   right now, and the two numbers agreeing is NOT required — but anyone who
                                 #   raises `top_picks` to 10 will silently also change what `track` grades, in
                                 #   the same edit, with no test naming the coupling. Read this before touching
                                 #   either.
  growth_er_weight: 0.0          # (#145) MEASURED AND REFUSED 2026-08-26 — stays OFF, where the term
  growth_er_cap: 20.0            # contributes exactly 0.0 for every row and the lane is the one every
                                 # golden holds. The cap is inert while the weight is 0. This receipt
                                 # SUPERSEDES (#105)'s "UNMEASURED"; (#105)'s description of the term is
                                 # kept below because it still describes the code, and its ARGUMENT is
                                 # kept because the run below is what answered it.
                                 #
                                 #   WHAT IT IS: the Grinold-Kroner decomposition of long-horizon equity
                                 #   return, composed from fields this tool ALREADY computes as-of on
                                 #   both paths (`picks::expected_return_pct`) —
                                 #     E[r] = D/P + (g - dS) + d(P/E)
                                 #          = dividend_yield_1y                      (income)
                                 #          + clamp(eps_growth, 0, cap)              (growth)
                                 #          + clamp(buyback_yield, -cap, cap)        (dilution, + = shrinking)
                                 #          + (value_factor ^ (1/20) - 1) x 100      (re-rating)
                                 #   It is an ADDITIVE term in `base`, and the ONE term in the lane that
                                 #   is not floored at 0: a negative expected return is information, and
                                 #   docking for it is the thing no other term can do.
                                 #
                                 #   WHY IT WAS MEASURED NOW. (#129) closed the gate family and named the
                                 #   SCORE as the next honest move; (#144) took it, rank-normalised all
                                 #   eleven additive terms, and refused. This was the only knob still
                                 #   marked UNMEASURED that changes the score's ORDER — every other one is
                                 #   display-only (`entry_excess_yield_band`, `implied_growth_years`,
                                 #   `rank_perturb_k`, the `print_*` flags) or already recorded as
                                 #   unmeasurABLE. No source code was written for this round: the term,
                                 #   its `TERM_DIMS`/`WEIGHT_DIMS` entries, its `render` branch and its
                                 #   unit tests were all already in tree with only the weight at 0.
                                 #
                                 #   THE ARM. One knob, no gates touched. Body, for re-creation as
                                 #   `tests/er-ci-settings.yaml` (recreate per (#131) — an arm overlay,
                                 #   not a tracked lane; it MUST sit at tests/<name>-ci-settings.yaml so
                                 #   `config::data_path` resolves the same caches, and only a file
                                 #   literally named ci-settings.yaml takes an empty base — (#125)):
                                 #     buy_heuristic:
                                 #       growth_er_weight: 0.15   # the dose; 0.0 for the control
                                 #   `heuristic_drift()` named exactly ONE knob on every arm run, and the
                                 #   CONTROL WAS RUN THROUGH THE SAME OVERLAY at 0.0 rather than quoted
                                 #   from a banked shipped run — a probe config resolves its own dataset.
                                 #   Lane `universe fund pit`, shipped gates. `fund` because the term is 0
                                 #   for an uncovered row; coverage was NOT the problem here — 240 of the
                                 #   249 growth-scored cutoffs carry fundamentals (96%), so (#59)'s
                                 #   partial-coverage demotion is not what this measured. windows scored
                                 #   2756 (12y) / 249 growth-lane; never with FOLIOMAN_OFFLINE=1.
                                 #
                                 #   RESULT — PRIMARY IS BIT-IDENTICAL AT EVERY DOSE, AND THAT IS THE
                                 #   FINDING RATHER THAN A PASS. top-10 excess mean|med, win, worst:
                                 #     w      12y                        8y
                                 #     0.0    +3.7|+4.0  74%  -6.4      +5.3|+5.6  86%  -5.5
                                 #     0.05   +3.7|+4.0  74%  -6.4      +5.3|+5.6  86%  -5.5
                                 #     0.15   +3.7|+4.0  71%  -6.4      +5.3|+5.6  86%  -5.5
                                 #     0.30   +3.7|+4.0  71%  -6.4      +5.3|+5.6  86%  -5.5
                                 #   Read by the LETTER this is "held" at 12y and 8y and every guard
                                 #   holds, which under (#139)'s pit substitution is a PASS. It is not
                                 #   one. It is VACUOUS in exactly (#126)'s sense — a column that cannot
                                 #   move reads "held" for free, and that is not evidence.
                                 #
                                 #   WHY PRIMARY CANNOT SEE THIS KNOB. The top-10 MEMBERSHIP is identical
                                 #   at every dose and both horizons — same `top-10 zero names` list to
                                 #   the byte, same `rode 9 zeros/209 holds` (12y) and `3 zeros/284 holds`
                                 #   (8y). `er_term` reorders WITHIN the book without changing which names
                                 #   are in it, and an EQUAL-WEIGHT top-10 book is set-invariant, so its
                                 #   excess, win rate and worst window are all blind to intra-book
                                 #   reordering by construction. The fund-covered baseline in
                                 #   `report_book_by_factor` is blind for the same reason: +3.6 (12y,
                                 #   n=240) and +4.7 (8y, n=332) at every dose. ANY future knob that only
                                 #   permutes the book has this property — grade it on the rank slices,
                                 #   and do not read a flat PRIMARY as a pass.
                                 #
                                 #   THE COLUMNS THAT CAN SEE IT ALL MOVE THE WRONG WAY. Disjoint rank
                                 #   slices, excess mean|med:
                                 #     12y  rank 1   0.0/0.05 +1.8|+5.1   0.15 +1.8|+5.1   0.30 +1.4|+5.1
                                 #          rank 6-10 0.0/0.05 -1.9|-0.1  0.15 -3.8|-0.2   0.30 -3.8|-0.2
                                 #      8y  rank 1   0.0/0.05 +5.6|+6.5   0.15 +5.7|+6.5   0.30 +4.8|+5.8
                                 #          rank 2-5  0.0/0.05 +5.3|+5.7  0.15 +5.4|+5.7   0.30 +5.7|+6.0
                                 #   0.30 drops rank 1's MEAN at BOTH horizons (+1.8 -> +1.4, +5.6 -> +4.8)
                                 #   and fires this round's pre-registered rank-1 rule on its own. 0.15
                                 #   buys +0.1 at 8y rank 1 and pays for it with the 12y 6-10 slice
                                 #   (-1.9 -> -3.8) and the 12y top-10 win rate (74% -> 71%). 0.05 is a
                                 #   NO-OP: every graded column is bit-identical to control at both
                                 #   horizons, so shipping it would be shipping nothing.
                                 #
                                 #   THE H2H GUARD IS INERT ON THIS LANE, recorded so it is not mistaken
                                 #   for a pass either: #1 vs the 11-20 book reads 2/4 (50%) at 12y and
                                 #   5/8 (62%) at 8y, UNCHANGED at every dose. Denominators of 4 and 8 are
                                 #   (#130)'s objection to grading h2h on a shipped-gates pit pool, and it
                                 #   stands — one window is 25 points at 12y.
                                 #
                                 #   AND THE COMPOSITE IS HOLLOW ON THE LANE THAT GRADES IT. This is the
                                 #   half of the finding worth more than the refusal:
                                 #     * THE RE-RATING LEG IS EXACTLY 0 IN EVERY BACKTEST ROW. It routes
                                 #       through `value_factor`, which reads `quote.pe_ratio`; `pe_ratio`
                                 #       appears NOWHERE in src/commands/backtest.rs, so `value_factor`
                                 #       returns 1.0 and (1.0^(1/20) - 1) x 100 is 0.0. core.rs:2265
                                 #       already calls `pe_ratio` "live-only ... backtest-blind". (#105)
                                 #       cited this leg as its THIRD and best argument ("the best
                                 #       long-horizon evidence in the literature") — it is the one leg the
                                 #       grading lane cannot see.
                                 #     * THE GROWTH LEG IS THINLY COVERED. `eps_growth` carries only 250
                                 #       of the 4829 cutoffs with as-of fundamentals in the standalone
                                 #       probe, so `unwrap_or(0.0)` supplies most rows. (Sample-wide, not
                                 #       a gated-pool measurement — read it as an indication.)
                                 #   So the backtest graded roughly `dividend_yield + buyback_yield`, not
                                 #   Grinold-Kroner. THE TERM IS ALSO A DIFFERENT FUNCTION LIVE THAN IN
                                 #   TRAIN — live `pe_ratio` populates and the re-rating leg comes alive —
                                 #   which is the train/serve skew (#1) used to zero `growth_value_weight`
                                 #   for this exact reason. Arming this weight off a backtest number would
                                 #   ship a term the walk-forward never priced.
                                 #
                                 #   WHY NO 20y RUN. The refusal is settled at 12y and 8y on the columns
                                 #   that can see the knob, and 20y guards only bind a SHIP. Continuing to
                                 #   run horizons after the decision is settled is the post-hoc search the
                                 #   pre-registration exists to prevent — (#144)'s reason for not
                                 #   laddering, applied to a horizon instead of a dose.
                                 #
                                 #   WHAT SURVIVES, AND IT IS THE NEXT ROUND. The idea is NOT refused; the
                                 #   composite could not be graded. To make it gradeable, give the
                                 #   backtest an as-of P/E: backtest.rs:1224 ALREADY computes
                                 #   `earnings_yield` = eps_ttm / as-of price, so P/E is derivable from a
                                 #   number the loop holds and simply never reaches `quote.pe_ratio`. That
                                 #   one fill un-blinds TWO things at once — this term's re-rating leg and
                                 #   `growth_value_weight`, which (#1) set to 0 SOLELY because "the
                                 #   walk-forward never saw pe_ratio (None in backtest), so this +-50%
                                 #   reorder was pure guess". It must be pre-registered as its own round,
                                 #   graded on the RANK SLICES (PRIMARY is blind to a pure reorder, see
                                 #   above), and it changes the shipped lane, so it needs its own receipt
                                 #   and re-blessed goldens.
                                 #
                                 #   REVERT: already at the reverted value; 0.0 IS the shipped policy and
                                 #   arming it is a graded change needing its own receipt. Re-run: the arm
                                 #   body above at w in {0.05,0.15,0.30} plus a 0.0 control through the
                                 #   SAME overlay, `backtest {12,8} universe fund pit`, ~2.5 min per run.
                                 #
                                 #   (#147) RE-MEASURED WITH THE COMPOSITE WHOLE — 2026-08-28, AND THE
                                 #   REFUSAL STANDS. (#145) refused this knob and then said its own
                                 #   evidence was compromised: the Grinold-Kroner RE-RATING LEG was
                                 #   identically 0 in every backtest row, so what got graded was
                                 #   `dividend + growth + dilution`, not the composite that ships live.
                                 #   (#147) fills `quote.pe_ratio` on the `fund` lane, so the leg
                                 #   (picks.rs:414 — it calls the same `value_factor`) now carries a
                                 #   real number spanning -3.41 .. +2.05 %/yr. Same ladder, same lane,
                                 #   same overlay discipline, 12y and 8y.
                                 #
                                 #   THE LEG IS LIVE — PROVEN, NOT ASSERTED. (#145)'s own error was
                                 #   reading a leg's state off the code, so repeating that here would be
                                 #   unforgivable. The proof is a PRE/POST pair at IDENTICAL config: at
                                 #   8y, w=0.05 read rank 2-5 mean +5.3 before the fill and +5.4 after
                                 #   it. Same dose, same lane, same knobs — only the fill differs, and
                                 #   (#145) had recorded 0.05 as a bit-identical NO-OP. A dead leg
                                 #   cannot do that. (Independently: the A-ladder's w=10.0 diagnostic
                                 #   moved top-3/top-5, which is only possible if `value_factor` sees a
                                 #   real P/E inside `score_parts`.)
                                 #
                                 #   AND IT CHANGES ALMOST NOTHING ANYWAY. Every other cell of the
                                 #   ladder reproduces (#145) TO THE BYTE — all four doses at 12y, and
                                 #   0.0/0.15/0.30 at 8y:
                                 #     12y      top-10 mean|med win worst | rank1 mean|med win | r6-10 mean|med
                                 #       0.0    +3.7|+4.0 74% -6.4       | +1.8|+5.1 63%      | -1.9|-0.1
                                 #       0.05   +3.7|+4.0 74% -6.4       | +1.8|+5.1 63%      | -1.9|-0.1
                                 #       0.15   +3.7|+4.0 71% -6.4       | +1.8|+5.1 63%      | -3.8|-0.2
                                 #       0.30   +3.7|+4.0 71% -6.4       | +1.4|+5.1 63%      | -3.8|-0.2
                                 #      8y      top-10 mean|med win worst | rank1 mean|med win | r6-10 mean|med
                                 #       0.0    +5.3|+5.6 86% -5.5       | +5.6|+6.5 79%      | -0.0|+1.7
                                 #       0.05   +5.3|+5.6 86% -5.5       | +5.6|+6.5 79%      | -0.0|+1.7
                                 #       0.15   +5.3|+5.6 86% -5.5       | +5.7|+6.5 79%      | -1.6|+0.7
                                 #       0.30   +5.3|+5.6 86% -5.5       | +4.8|+5.8 77%      | -1.6|+0.7
                                 #
                                 #   THE REFUSAL, on the pre-registered rank-1 rule. 0.30 drops rank-1's
                                 #   MEAN at BOTH horizons — +1.8 -> +1.4 at 12y and +5.6 -> +4.8 at 8y,
                                 #   with rank-1's win rate 79% -> 77% at 8y — while its MEDIAN barely
                                 #   moves. (#144) is the standing proof those two move in opposite
                                 #   directions, which is why this round pre-registered reporting the
                                 #   mean beside the h2h. 0.15 buys +0.1 on rank-1 mean at 8y and pays
                                 #   with the 6-10 slice at both horizons (-1.9 -> -3.8 at 12y, -0.0 ->
                                 #   -1.6 at 8y) and the 12y top-10 win rate (74% -> 71%). 0.05 is
                                 #   within one marginal swap of control. REFUSED, same as (#145).
                                 #
                                 #   NOTE what PRIMARY did here, because it is the same trap as the
                                 #   A-ladder: top-10 excess is +3.7|+4.0 at 12y and +5.3|+5.6 at 8y for
                                 #   EVERY dose, so PRIMARY reads "held" at all four rungs while rank 1
                                 #   is visibly degrading. An equal-weight top-10 book is set-invariant
                                 #   and this knob only permutes it. The GUARDs hold too (h2h 2/4 at 12y
                                 #   and 5/8 at 8y, unchanged; worst window -6.4 / -5.5, unchanged), so
                                 #   BY THE LETTER this ladder "passes" Ship Rule v2 at every dose. It
                                 #   is refused on the rank slices instead, which is what (#145)
                                 #   pre-registered and what (#126) means by VACUOUS.
                                 #
                                 #   WHY SO LITTLE SURVIVES. A twentieth root is a brutal compressor:
                                 #   `value_factor`'s full x0.5..1.5 span maps onto just -3.41..+2.05
                                 #   %/yr, so at w=0.30 the whole leg is worth at most -1.02..+0.62
                                 #   points of `base`. (#105) cited this leg as its best argument, and
                                 #   that is not wrong about the economics — it is a statement about how
                                 #   little of a re-rating survives being annualised over a 20-year hold.
                                 #
                                 #   REPRODUCIBILITY, recorded because every identity claim above rests
                                 #   on it: (#145)'s ladder reproduced across a different DATE, a
                                 #   different BINARY and a different FETCH, and two 0.0 controls run
                                 #   hours apart on DIFFERENT universes (6082 vs 6081 tickers, 233 vs 353
                                 #   Yahoo fund-facts fallbacks) printed identical graded columns. The
                                 #   noise floor on this lane's graded columns is 0, so "identical"
                                 #   here means identical, not "within noise".
                                 #
                                 #   VERDICT: 0.0 STANDS, and the refusal is now made on the composite
                                 #   it was always supposed to be made on. Closed on this lane at these
                                 #   doses rather than deferred.
                                 #   REVERT: already at the reverted value.
                                 #   Re-run: `growth_er_weight` in {0.05,0.15,0.30} plus a 0.0 control
                                 #   through the SAME overlay, `backtest {12,8} universe fund pit`,
                                 #   ~2 min per run.
                                 #
                                 #   (#147) TWO CORRECTIONS TO THE TEXT ABOVE, left in place rather
                                 #   than edited away because the reasoning is still the record:
                                 #     (1) "it changes the shipped lane, so it needs its own receipt and
                                 #         RE-BLESSED GOLDENS" — the receipt yes, the re-blessing NO.
                                 #         No golden runs `fund` (tests/backtest_fixture.rs:134 says so
                                 #         outright) and an as-of P/E needs as-of EPS, so `pe_ratio`
                                 #         stays None on every price-only path. All 9 goldens passed
                                 #         WITHOUT FOLIOMAN_BLESS, and that is the fill's INERTNESS
                                 #         PROOF rather than an accident. Do not cite this line as
                                 #         precedent for blessing goldens on a fund-lane change.
                                 #     (2) The 240/249 (96%) fundamentals coverage above was
                                 #         re-measured UNCHANGED by (#147) — confirmed, not stale. It
                                 #         is what rules coverage out as the reason either knob reads
                                 #         flat.
                                 #   NEVER set FOLIOMAN_OFFLINE=1: it cuts the 8y king walk from 699
                                 #   scored windows to 32 and prints the SHIPPED lane at -5.9 excess,
                                 #   exiting 0 with a complete-looking report. Check `windows scored`.
  growth_record_weight: 0.0      # (#133 / round 8) MEASURED AND REFUSED 2026-08-23 — stays OFF, where
  growth_record_full_years: 20.0 # the term contributes exactly 0.0 and the lane is the one every
                                 # golden holds. Kept rather than deleted, on the `growth_trust_ladder`
                                 # precedent: the null must stay reproducible in one config line. The
                                 # full-years bar is an UNFITTED judgement value, inert while w is 0.
                                 #
                                 #   WHAT IT IS: an ADDITIVE penalty in `base`, ahead of every damp —
                                 #     record_term = −weight × max(0, full_years − long_years)
                                 #   `long_years` is the leg `long_leg_fixed` ALREADY found inside
                                 #   `score_parts`; the record length is not re-derived at the term
                                 #   (non-negotiable #4), and `quote.age_years` is deliberately NOT
                                 #   used — it is never filled on the backtest path and reads INERT in
                                 #   `growth_gate_reachability_pin`. Only the SHORTFALL is docked, so a
                                 #   longer-than-full record is never PAID for its length. No gate is
                                 #   added, so `gate_failures` needs no mirror (non-negotiable #2) and
                                 #   the reachability pin does not move.
                                 #
                                 #   WHY IT EXISTS, and this is the whole justification: it is the
                                 #   condition `growth_min_leg_years` PRE-REGISTERED for its own rung to
                                 #   be re-proposed. That receipt refused 5.0->2.0 TWICE, on every
                                 #   clause (20y-s rank-1 +5.9|+6.0 -> +4.5|+4.0, h2h 67% -> 39%, lane
                                 #   edge +411.0 -> +244.2), and named the MECHANISM rather than the
                                 #   quantity: a short record COLLECTS SCORE FOR THE ABSENCE OF MEASURED
                                 #   BAD NEWS. Its above-MA reads 0% and its maxdd is shallow because
                                 #   the drawdown HAS NOT HAPPENED YET, so every risk brake in this lane
                                 #   is blind to it by construction. The refusal's exact terms: "Do not
                                 #   re-propose without a NEW damp aimed at that mechanism (a record-
                                 #   length term in the score itself, not a post-hoc multiplier)."
                                 #   `trust_factor` is the multiplier that receipt rules INSUFFICIENT by
                                 #   name — it already halves these names and does not fix the order.
                                 #   That is why this term is additive and lands in `base`.
                                 #
                                 #   WHY THIS RUNG AT ALL. (#127) measured `out-ranked` — cleared every
                                 #   gate, ranking still missed the winner — at 0.2% / 0.0% / 1.5% of
                                 #   missed wealth at 20y/12y/8y: "SELECTION IS CONFIRMED DEAD, IN ONE
                                 #   NUMBER", at most ~2% of the extreme wealth for a scoring change to
                                 #   win. The same table endorses exactly one direction: "The largest
                                 #   genuinely single-gate cohort at every horizon is `history` (no 5y+
                                 #   record) ... That is the only rung this table endorses looking at
                                 #   next." (#124) prices the ceiling it would lift: the held book NEVER
                                 #   EXCEEDS ~10 NAMES (deciles identical from top-10 to top-50 at every
                                 #   horizon) and captures 5-24% of the extreme wealth its own pool held.
                                 #   "This is an ADMISSION problem, not a ranking problem." This term is
                                 #   not a scoring improvement; it is the brake that has to exist BEFORE
                                 #   the admission rung underneath it can be re-graded.
                                 #
                                 #   THE LADDER, anchored on a measured pair, not a guess: the leg
                                 #   receipt banks its own failure as GEV at 2.3y scoring 26.2 over NVDA
                                 #   at 27.5y on 10.8. Closing a 15.4-point gap over ~17.7 missing years
                                 #   needs ~0.87 points/yr, so the grid spans it: w in {0.25, 0.5, 1.0,
                                 #   2.0}, each paired with `growth_min_leg_years: 2.0`, against the
                                 #   SHIPPED lane (leg 5.0, w 0.0) as control. `universe stress` at
                                 #   {20, 12, 8}: record length is price-derived, so `fund` buys nothing
                                 #   and costs coverage, and `stress` is the lane the banked leg grid
                                 #   used, which keeps the comparison like-for-like.
                                 #
                                 #   GRADED AS AN ADDITION, so it owes the addition ship rule: the arm
                                 #   is judged against the SHIPPED lane it would replace, not against
                                 #   the leg-2.0 control — beating that control only proves the term
                                 #   does something. And a CANDIDATE additionally requires THE BOOK TO
                                 #   HAVE GROWN (cleared-name count up). A term that fixes the ranking
                                 #   but admits nobody has bought nothing, which is the finding (#124)
                                 #   already banked.
                                 #
                                 #   THE VERDICT: REFUSED AT EVERY RUNG, GUARD BROKEN. 14 runs,
                                 #   `backtest {20,12,8} universe stress`, 2026-08-23. Arm verification
                                 #   passed as pre-registered (control A names no drift, control B names
                                 #   `growth_min_leg_years` alone, every arm names exactly it plus
                                 #   `growth_record_weight`), and the pool was IDENTICAL across all 14
                                 #   runs at each horizon (2374 / 4712 / 8389), so no cross-run universe
                                 #   noise is in these numbers.
                                 #
                                 #   rank-1 h2h, the guard, quoted with its denominator:
                                 #                        20y            8y
                                 #     control A     67% (12/18)   65% (22/34)     <- SHIPPED, passes
                                 #     control B     45% (10/22)   46% (19/41)
                                 #     w 0.25        41% ( 9/22)   46% (19/41)
                                 #     w 0.50        36% ( 8/22)   44% (18/41)
                                 #     w 1.00        27% ( 6/22)   39% (16/41)
                                 #     w 2.00        45% (10/22)   34% (14/41)
                                 #   Ship Rule v2 precedence is GUARD > PRIMARY, and a broken guard
                                 #   REFUSES OUTRIGHT — it is never softened into unresolved. Every rung
                                 #   is under 50% at BOTH graded horizons. 12y was not run for the arms
                                 #   because the plan pre-registered it for survivors only, and there
                                 #   were none. PRIMARY fails independently anyway: top-10 median falls
                                 #   at every rung at both horizons (20y +6.1 -> 6.0/5.6/5.4/5.6 vs
                                 #   control A +6.1; 8y +7.1/6.5/6.3/6.1 vs +7.3). The worst-window
                                 #   guard never bound — -0.5 at 20y and -9.2 at 8y at EVERY arm.
                                 #
                                 #   THE PRE-REGISTERED CONDITION IS NOW DISCHARGED, AND NEGATIVE. This
                                 #   term IS the "record-length term in the score itself, not a post-hoc
                                 #   multiplier" that `growth_min_leg_years` demanded before its 2Y rung
                                 #   could be re-proposed. It was built, graded on the ladder its own
                                 #   receipt anchored, and it DOES NOT SAVE THE RUNG. Its best arm only
                                 #   MATCHES control B (20y 45% = 45%, 8y 46% = 46%) and never beats it,
                                 #   so the term buys nothing the bare rung did not already have. DO NOT
                                 #   RE-PROPOSE the 2Y rung on a third damp of this shape; the direction
                                 #   is closed, and closing it was the point of running this.
                                 #
                                 #   TWO THINGS THE ARMS SAY THAT THE VERDICT DOES NOT.
                                 #   (a) THE TERM IS NOT MERELY INERT, IT IS ACTIVELY WRONG-SHAPED. At 8y
                                 #   it degrades h2h MONOTONICALLY as the weight rises (46/44/39/34), so
                                 #   arming it is strictly worse than leaving it off on the same pool. At
                                 #   20y it is non-monotone and incoherent: w 1.00 buys the BEST rank-1
                                 #   excess of any arm (+5.0|+4.6 vs control B's +4.5|+3.7) together with
                                 #   the WORST h2h (27%). It raises what rank 1 earns on average while
                                 #   making rank 1 beat its own book's middle LESS often — a docking rule
                                 #   that reshuffles the argmax without improving it. The same trap
                                 #   `growth_proximity_weight` and `growth_max_peg` both record: visible
                                 #   reordering, no rank-1 authority.
                                 #   (b) THE RUNG'S ADMISSION LIFT IS REAL, LARGE, AND NOT WHAT (#50)
                                 #   SAID IT WAS. King-walk windows go 339 -> 486 at 20y (+43%) and
                                 #   699 -> 977 at 8y (+40%); at 8y the scored book goes 692 -> 938
                                 #   STOCKS and 7 -> 39 ETFs. (#50)'s "the leg rung admits ETFs, not
                                 #   stocks (ETF table 5->20, stocks +1)" is FALSE on today's pool — it
                                 #   admits +246 stocks. Any future citation of (#50) on that point must
                                 #   re-measure first. And the wider book is NOT worse in the basket:
                                 #   control B's top-10 excess RISES at 8y (+6.6|+7.3 -> +7.1|+6.8 mean
                                 #   up, median down) and at 12y (+6.2|+6.1 -> +6.7|+7.2, BOTH up). What
                                 #   collapses is rank 1 ALONE. That localisation is the useful residue
                                 #   of this round: (#124)'s admission ceiling is genuinely liftable, the
                                 #   basket survives the lift, and the only casualty is the single name
                                 #   at the top. A future round should aim at THAT — a rank-1-specific
                                 #   brake, or the guard's own definition — not at another score term.
                                 #
                                 #   REVERT: already at the reverted value. Re-run any row above with
                                 #   `tests/record{025,050,100,200}-ci-settings.yaml` (recreate per
                                 #   (#131) — they are arm overlays, not tracked lanes) and
                                 #   `tests/leg2-ci-settings.yaml` for control B. NEVER set
                                 #   FOLIOMAN_OFFLINE=1 on these runs: it cuts the 8y king walk from 699
                                 #   scored windows to 32 and prints the SHIPPED lane at -5.9 excess,
                                 #   exiting 0 with a complete-looking report. Check `windows scored`.
  growth_base_offset: 0.0        # (#150) MEASURED AND REFUSED 2026-08-29 — stays OFF, which makes the
                                 # term exactly 0.0, so `base` is the shipped sum term for term and every
                                 # golden passes UNBLESSED. This receipt records a REFUSAL and, more
                                 # importantly, THE FIRST APPLICATION OF (#149)'s VACUITY RULE.
                                 #
                                 #   WHAT THE KNOB IS. A CONSTANT added to `base`, the twelfth additive
                                 #   term, ahead of every damp. `score = base × M + liq_bonus`, so an
                                 #   offset c contributes `c × M` — a term that ranks PURELY BY THE
                                 #   MULTIPLIER STACK (proximity, value, trust, overextension, TER,
                                 #   commodity, FX, share class). It is the dial for how much of the order
                                 #   is decided by the additive terms' LEVELS versus by the multipliers.
                                 #   Unlike `liq_bonus`, which lands OUTSIDE the stack and is genuinely
                                 #   rank-inert, this lands inside it and reorders.
                                 #
                                 #   WHY IT WAS WORTH A ROUND. THE SHIPPED LANE ALREADY HAS ONE AND NOBODY
                                 #   CHOSE IT. `growth_fund_extra` ships `roic` at `weight: 0.25`,
                                 #   `neutral: 17.3`, so every row the factor does not cover collects a
                                 #   flat 0.25 × 17.3 = 4.325 inside `fund` — the (#59) mechanism. The 12y
                                 #   ablation prints `fund_extra:roic` and a NULL factor at IDENTICAL edge
                                 #   (+157.0) and IDENTICAL delta (-25.3): the constant IS the whole
                                 #   measured contribution of the shipped fundamental extra. This knob makes
                                 #   that quantity nameable and sweepable instead of a side effect of a
                                 #   missing-data fill. ADDITIVE-ON-TOP, never a replacement for that
                                 #   `neutral`, which would move shipped behaviour at the default.
                                 #
                                 #   RUNS. 8 runs, `backtest {12,8} universe fund pit`, all through the
                                 #   SAME overlay at `tests/off-ci-settings.yaml` ((#125): a file literally
                                 #   named `ci-settings.yaml` takes an EMPTY base, any other name deep-merges
                                 #   over this file — so the control must run through the overlay too, and
                                 #   did). Overlay body, deleted after per (#131):
                                 #     buy_heuristic:
                                 #       growth_base_offset: <c>   # THE ONLY KNOB UNDER TEST
                                 #   Arms c in {0.0 control, -4.0, +4.0, +16.0}; -4.0 chosen to bracket the
                                 #   4.325 the shipped lane already carries, +16.0 as the far rung.
                                 #   Release binary rebuilt from the tree under test FIRST — (#147)'s runs
                                 #   came off a stale mid-round binary and that nearly cost a verdict.
                                 #   `windows scored` 2737 pool / 249 growth-lane at 12y and 355 at 8y,
                                 #   never with FOLIOMAN_OFFLINE=1. The 12y control reproduces the banked
                                 #   (#147) control (top-1 +1.8 exact, top-10 +3.7; median +3.9 vs +4.0,
                                 #   inside the known (#40) cross-run universe drift).
                                 #
                                 #   RESULT — PRIMARY IS VACUOUS AT BOTH GRADED HORIZONS, AND (#149)'s
                                 #   CENSUS SAYS SO IN THE OUTPUT RATHER THAN IN A READER'S HEAD:
                                 #     12y  35 buckets, median gated pool 6, basket 10 -> 31 SATURATED,
                                 #          16 substitutable names in the whole run.  VACUOUS.
                                 #      8y  43 buckets, median gated pool ~6, basket 10 -> 35 SATURATED,
                                 #          21 substitutable names in the whole run.  VACUOUS.
                                 #   And the top-10 column behaves exactly as the census predicts: excess
                                 #   +3.7 at EVERY 12y arm and +5.3 at EVERY 8y arm, worst window -6.4 and
                                 #   -5.5 unchanged, `rode 9 zeros/209 holds` and `3 zeros/284 holds`
                                 #   unchanged. The knob is NOT inert — see the rank slices below — the
                                 #   COLUMN cannot see it. Under (#149) that is recorded VACUOUS and NEVER
                                 #   "held", so PRIMARY cannot be established and the arm CANNOT SHIP.
                                 #
                                 #   THE KNOB IS A PURE RANKING KNOB ON THIS LANE, checked rather than
                                 #   assumed, because (#149) exempts pool-changing knobs. There is NO
                                 #   `min_score` trim in the graded path — `growth_min_score` is display-only
                                 #   and ships -100.0 — so no offset can add or remove a name from the pool.
                                 #   Pool size is identical at every arm, which confirms it.
                                 #
                                 #   GUARDS ALL HOLD, and they are the half that still answers (a worst
                                 #   window is a MAX, an h2h is a COUNT):
                                 #     worst window  12y -6.4 / 8y -5.5 at EVERY arm, to the decimal.
                                 #     rank-1 h2h    12y 2/4 (50%) at every arm — identical, and sitting
                                 #                   exactly ON the >=50% bar with a denominator of FOUR.
                                 #                    8y control 5/8 (62%) -> -4.0 5/8 (62%) -> +4.0 4/8
                                 #                   (50%) -> +16.0 4/8 (50%).
                                 #
                                 #   THE INTERESTING PART, AND WHY IT IS NOT A REASON TO SHIP. The rank
                                 #   slices move a lot, and EVERY non-zero arm beats the control at rank-1:
                                 #     12y rank-1 mean|median  ctl +1.8|+5.1  -4.0 +2.3|+5.7
                                 #                             +4.0 +2.4|+5.1  +16.0 +2.5|+5.1
                                 #      8y rank-1 mean|median  ctl +5.6|+6.5  -4.0 +6.4|+8.5
                                 #                             +4.0 +6.3|+8.5  +16.0 +6.5|+8.5
                                 #   Read that shape carefully: the control is the WORST arm at rank-1 at
                                 #   both horizons, and BOTH SIGNS of the offset improve it by about the
                                 #   same amount. A term whose effect is symmetric in its sign is not
                                 #   carrying a direction — it is reshuffling the argmax, and on a ONE-NAME
                                 #   book over 35 (12y) and 43 (8y) windows that is exactly where noise
                                 #   lives. THE H2H GUARD, which is a same-window COUNT and so carries no
                                 #   averaging artifact, goes the OTHER WAY at 8y: 62%/75% at the control
                                 #   down to 50%/38% at both positive arms, while rank 2-5 falls +5.3 ->
                                 #   +4.4. Rank-1 MEAN up while rank-1 h2h COUNT down is the winner's-curse
                                 #   signature the guard exists to catch ((#144) requires reporting both
                                 #   side by side for this reason). No ship on a number that its own
                                 #   corroborating statistic contradicts.
                                 #
                                 #   THE -4.0 RUNG'S KNOWN HAZARD, pre-registered and discharged by
                                 #   ARGUMENT rather than by an instrumented count, which is stated so the
                                 #   next round knows what was and was not measured. `growth_allow_negative_
                                 #   scores` ships false, so a negative `base` still takes the damps and
                                 #   (#86) records that the ranking INVERTS among such names. The count of
                                 #   rows driven below zero at -4.0 was NOT measured. It does not change
                                 #   this verdict: with `M > 0`, any negative-base name scores below every
                                 #   positive-base one unless its `liq_bonus` exceeds `|base × M|`, so the
                                 #   inversion misorders the BOTTOM of the list and a top-10 basket never
                                 #   reaches it. Consistent with the data — pool, holds, zeros and worst
                                 #   window are all identical at that arm. If a future round grades a
                                 #   NEGATIVE offset on anything but a top-N basket, count those rows first.
                                 #
                                 #   VERDICT: 0.0 STANDS. Not "held", not passed — VACUOUS on PRIMARY at
                                 #   both graded horizons, guards intact, and the one number that moved in
                                 #   the arm's favour is contradicted by its own guard. This is the fourth
                                 #   consecutive ranking-knob round to end this way, and (#149) is why this
                                 #   one says VACUOUS in the output instead of banking a free "held".
                                 #   REVERT: already at the reverted value.
                                 #   RE-OPEN ONLY ON A NON-VACUOUS COLUMN. That is a POOL problem, not a
                                 #   score problem: 31 of 35 buckets at 12y have no name outside the basket
                                 #   at all, so no ranking knob whatsoever is measurable here — this one,
                                 #   `growth_value_weight`, `growth_proximity_weight` or any future one.
                                 #   (#151) is the pool round, and it is the prerequisite for re-running
                                 #   ANY of them. Re-run: the four arms above, ~2.5 min per run.
                                 # (#170) THAT RE-OPEN IS NOW CASHED, AND THE STOP RULE PASSES — the first
                                 # ranking-knob round in five to do so. Measured 2026-08-30, 12 runs,
                                 # `backtest {20,12,8} universe fund pit`, on (#129)'s GATES-OFF INSTRUMENT
                                 # LANE recreated per (#131) from `git show 5159105:tests/gatesoff-ci-settings
                                 # .yaml`. Arms EXACTLY as pre-registered above: c in {0.0 control, -4.0,
                                 # +4.0, +16.0}. No fifth rung was added after seeing the four ((#120)).
                                 #
                                 # THE ARM, and TWO no-ops the base grew underneath it — the (#131) hazard,
                                 # caught by reading the drift WARNING rather than trusting the file:
                                 #   growth_min_range_pct 0.0 / growth_min_cagr -1e9 / growth_min_1y_pct -1e9
                                 #   growth_min_5y_pct -1e9 / growth_max_above_ma 0.0 / growth_max_peg 0.0
                                 #   growth_require_peg false / growth_require_lifetime_uptrend false
                                 #   growth_maxdd_cap 0.0        <- ALREADY SHIPS 0.0 since (#138). NO-OP.
                                 #   print_recall_capture true   <- ALREADY SHIPS true. NO-OP, and NEW since
                                 #                                  (#144), which still saw it drift.
                                 # So `heuristic_drift()` names EIGHT knobs on the control and NINE on each
                                 # offset arm — not (#144)'s ten/eleven. Verified on all 12 runs.
                                 #
                                 # THE LANE IS NON-VACUOUS, WHICH IS THE WHOLE POINT. POOL CENSUS per horizon,
                                 # identical at EVERY arm (so the knob is pure-ranking here, confirming (#150)'s
                                 # claim that no offset adds or removes a name):
                                 #   20y  22 buckets, median gated pool  79, basket 10 ->  1 saturated, 1863 subst.
                                 #   12y  37 buckets, median gated pool 133, basket 10 ->  1 saturated
                                 #    8y  45 buckets, median gated pool 147, basket 10 ->  1 saturated
                                 # Against 31-of-35 and 35-of-43 saturated on the shipped lane. THE COLUMN CAN
                                 # SEE A REORDER HERE.
                                 #
                                 # TIE-BACK: the control reproduces (#144)'s gates-off control rho EXACTLY at
                                 # all three horizons, -0.09 / -0.03 / +0.03. Pool 2079/6036/12256 vs (#144)'s
                                 # 2084/6060/12223 — four days of universe drift, 20y within 5 names.
                                 #
                                 # RESULT. Arms left to right: -4.0 | 0.0 CONTROL | +4.0 | +16.0
                                 #   rho          20y  -0.11 | -0.09 | -0.08 | -0.07     MONOTONE
                                 #                12y  -0.04 | -0.03 | -0.02 | -0.01     MONOTONE
                                 #                 8y  +0.02 | +0.03 | +0.03 | +0.03     flat above control
                                 #   rank-1 mean  20y   -1.5 |  -0.4 |  +0.8 |  +0.5
                                 #                12y   +0.9 |  +1.5 |  +2.4 |  +2.4
                                 #                 8y   +2.4 |  +2.9 |  +3.6 |  +3.7
                                 #   rank-1 h2h   20y    38% |   38% |   52% |   43%   (#1 vs the 11-20 book)
                                 #   (COUNT, no   12y    42% |   44% |   44% |   50%
                                 #    averaging)   8y    43% |   41% |   45% |   48%
                                 #
                                 # PRE-REGISTERED STOP RULE — all three clauses, each needing 2 of 3 horizons:
                                 #   1 DIRECTION  monotone in rho, or single-peaked away from the control.
                                 #                3/3 monotone. PASS — and it SURVIVES DELETING THE -4.0 ARM
                                 #                ENTIRELY (0/+4/+16 is still monotone at 3/3), which is how
                                 #                the (#150) negative-base obligation below is discharged.
                                 #   2 SPEARMAN   rises vs control at the best arm. 20y and 12y yes, 8y flat.
                                 #                2/3 PASS. (Live here, unlike (#144): `lane_metrics` scores
                                 #                through a `fn(&Quote,&BuyHeuristic)` pointer, and this knob
                                 #                is a per-quote CONSTANT (picks.rs:1521 -> :1533) so the
                                 #                pointer sees it. (#144)'s cross-sectional transform needed
                                 #                pool context that pointer cannot supply, which is why its
                                 #                rho came back bit-identical and its clause was unmeasurable.)
                                 #   3 RANK-1     stops being the worst disjoint slice (mean). 20y yes at
                                 #                +4.0 (rank1 +0.8 over 2-5 +0.3); 8y yes at +16.0 (rank1
                                 #                +3.7 over 6-10 +3.3); 12y only ties. 2/3 PASS.
                                 # AND THE (#150) NOISE SIGNATURE IS ABSENT. On the shipped lane BOTH SIGNS
                                 # improved rank-1 by about the same amount, which (#150) read correctly as
                                 # reshuffling the argmax. Here the response is DIRECTIONAL: the negative arm
                                 # is worse-or-equal on rho at all three horizons, the positive arms better-
                                 # or-equal, monotonically. A wide pool is what resolved that ambiguity.
                                 #
                                 # THE (#150) NEGATIVE-BASE OBLIGATION IS DISCHARGED WITHOUT THE EXTRA RUNS.
                                 # (#150) required: "If a future round grades a NEGATIVE offset on anything but
                                 # a top-N basket, count those rows first" — rho IS a whole-pool statistic, so
                                 # this round is that case. It is discharged not by counting but by the finding
                                 # being INDEPENDENT of the -4.0 arm: every clause still passes on {0,+4,+16}
                                 # alone. The -4.0 arm is reported, and its consistently-worst showing is left
                                 # UNATTRIBUTED — it is equally consistent with the (#86) inversion artifact
                                 # and with the real direction, and this round cannot separate them. Do not
                                 # cite -4.0 as evidence for either.
                                 #
                                 # >>> AND THE COUNTER-FINDING, WHICH MATTERS MORE THAN THE PASS <<<
                                 # The graded basket moves AGAINST the arm that wins on rank-1:
                                 #   top-10 excess 20y  +2.8 | +2.4 | +2.7 | +2.1
                                 #                 12y  +4.5 | +4.2 | +4.2 | +4.0   MONOTONE DOWN in c
                                 #                  8y  +5.4 | +5.1 | +4.8 | +4.2   MONOTONE DOWN in c
                                 #   worst window  20y  -3.9 | -3.9 | -3.3 | -3.5
                                 #                 12y  -2.5 | -2.3 | -1.5 | -3.6
                                 #                  8y  -2.1 | -1.7 | -3.4 | -4.0   MONOTONE WORSE in c
                                 # Read it as ONE mechanism, not two results: a larger offset hands the order
                                 # to the MULTIPLIER STACK, which sharpens the single best name and dilutes
                                 # the 10-name book that averages over it. Rank-1 up while top-10 and the
                                 # worst window go down is not a contradiction — it is what "rank by the
                                 # multipliers" buys and costs.
                                 #
                                 # VERDICT: 0.0 STANDS, AND NOTHING SHIPS FROM THIS LANE — pre-registered
                                 # before the runs, not concluded after. The gates-off CONTROL ITSELF fails
                                 # the rank-1 h2h GUARD at every horizon (38% / 44% / 41%), so Ship Rule v2
                                 # cannot adjudicate here for ANY arm; (#129) already calls this file "an
                                 # INSTRUMENT, never a candidate lane". This is a DIAGNOSTIC that passed its
                                 # own stop rule, nothing more.
                                 # REVERT: already at the reverted value.
                                 #
                                 # WHAT IT AUTHORISES, AND THE PREDICTION THAT SHOULD GOVERN IT. The stop rule
                                 # passing authorises a follow-up on a SHIPPABLE lane — (#151)'s
                                 # `growth_min_cagr 15` 8y column, priced there at -1.2 pts/yr of top-10 mean.
                                 # BUT THESE RUNS PREDICT THAT ROUND REFUSES ON PRIMARY: PRIMARY grades top-10
                                 # excess, and top-10 excess falls monotonically in c at both 12y and 8y here.
                                 # Whoever runs it must pre-register that prediction and treat a top-10 gain as
                                 # the surprise requiring explanation. Cheaper first move: a knob that keeps
                                 # the rank-1 sharpening WITHOUT handing the whole order to the multipliers —
                                 # this round says the multiplier stack carries real ordering information at
                                 # the very top, which is the finding, and NOT that a constant offset is how
                                 # to harvest it.
                                 # ARMS DELETED after the round per (#131); body quoted above for re-creation.
                                 #
                                 # (#172) THE (#170) PREDICTION IS CASHED: ITS CONCLUSION HOLDS AND ITS MECHANISM
                                 # IS WRONG. Measured 2026-08-30, 8 runs, `backtest {12,8} universe fund pit`, on
                                 # (#151)'s `growth_min_cagr 15` lane (overlay: that one gate plus the offset;
                                 # everything else shipped). Arms EXACTLY as pre-registered: c in {0.0 control,
                                 # -4.0, +4.0, +16.0}. No fifth rung ((#120)). 20y is not part of this lane and
                                 # was not run. `heuristic_drift()` named ONE knob on the control and TWO on each
                                 # offset arm, verified on all 8 runs.
                                 #
                                 # TIE-BACK: the control reproduces (#151)'s cagr-15 rows EXACTLY at BOTH horizons
                                 # — 12y 400 scored, top-10 +2.9|+4.0, worst -6.4, h2h 9/14, rank-1 -0.1|+3.6;
                                 # 8y 690 scored, top-10 +4.1|+3.8, worst -3.5, h2h 16/22, rank-1 +4.7|+5.9.
                                 # `windows scored` identical within horizon (2737 @12y, 6126 @8y), same dataset.
                                 #
                                 # CENSUS, RE-READ FROM EACH RUN'S OWN OUTPUT (banked flags are perishable), and
                                 # identical at every arm — so the offset is pure-ranking here too:
                                 #   12y  36 bkt, median gated pool  9, basket 10 -> 22 saturated, 81 subst.
                                 #        *** VACUOUS TAG PRINTED *** — a RANKING knob CANNOT be adjudicated here.
                                 #    8y  44 bkt, median gated pool 11, basket 10 -> 22 saturated, 243 subst.
                                 #        NOT tagged: 22 of 44 is exactly half, not the strict majority. It answers.
                                 # (#151)'s claim survives re-measurement four days on. But note what it means:
                                 # exactly ONE of the two columns pit PRIMARY requires can see a reorder at all.
                                 #
                                 # RESULT. Arms left to right: -4.0 | 0.0 CONTROL | +4.0 | +16.0
                                 #   rho           12y  +0.07 | +0.11 | +0.13 | +0.15    MONOTONE UP
                                 #                  8y  +0.09 | +0.12 | +0.15 | +0.17    MONOTONE UP
                                 #   top-10 excess 12y  +2.9|+4.0 | +2.9|+4.0 | +2.9|+4.0 | +2.8|+3.6
                                 #                  8y  +4.2|+4.7 | +4.1|+3.8 | +4.2|+3.8 | +4.1|+3.8   FLAT
                                 #   worst window  12y   -6.4 |  -6.4 |  -6.4 |  -6.4
                                 #                  8y   -3.6 |  -3.5 |  -3.5 |  -4.1
                                 #   rank-1         8y  +4.8|+5.9 | +4.7|+5.9 | +4.4|+5.7 | +3.7|+5.7   FALLS in c
                                 #   rank 2-5       8y  +4.6|+4.5 | +5.2|+5.5 | +5.7|+6.0 | +5.8|+6.0   RISES in c
                                 #   rank 6-10      8y  +1.5|+3.1 | +0.3|+0.6 | +0.0|+0.6 | -0.6|-0.1   FALLS in c
                                 #   h2h #1 vs 6-10 book ((#160)'s book, the GUARD's):
                                 #                 12y  14/26 | 18/26 | 18/26 | 19/26
                                 #                  8y  23/34 | 25/34 | 24/34 | 24/34
                                 #   h2h 2-5 vs 6-10 book, same runs:
                                 #                  8y  24/34 | 26/34 | 29/34 | 30/34             MONOTONE UP
                                 #
                                 # VERDICT: REFUSED ON GUARD, every arm. (#160)'s bar at 12y AND 8y is the rank-1
                                 # h2h read off the 6-10 book AND NOT BELOW the control's tally on it. At 8y every
                                 # arm is BELOW the control: 23, 24, 24 against 25 of 34. A broken guard REFUSES
                                 # OUTRIGHT and is never softened into unresolved, so PRIMARY is never reached.
                                 # (The absolute >=50% half passes everywhere — 54% to 73%. It is the not-worse
                                 # half, which (#160) added precisely so a thin book cannot drift down unnoticed,
                                 # that fails.) Running 20y could not rescue this; a broken guard is broken.
                                 # SECOND, INDEPENDENT REASON: 12y is VACUOUS, so a ranking knob cannot be
                                 # adjudicated there, and the pit lane's PRIMARY reads 12y AND 8y ((#139)).
                                 # REVERT: already at the reverted value. 0.0 STANDS.
                                 #
                                 # THE PREDICTION, GRADED HONESTLY. (#170) wrote: "top-10 excess falls
                                 # monotonically in c at both 12y and 8y here", so the round "REFUSES ON PRIMARY".
                                 # The round DOES refuse — but NOT for that reason, and the stated mechanism is
                                 # FALSE. Top-10 excess does not fall: at 8y it is FLAT (+4.1|+3.8 -> +4.2|+3.8 ->
                                 # +4.1|+3.8) and at 12y it is IDENTICAL across -4/ctl/+4. (#170) generalised from
                                 # the gates-off INSTRUMENT lane, whose median gated pool is 79/133/147, to this
                                 # one, whose median pool is 9/11 against a basket of 10. On a deep pool a reorder
                                 # changes WHICH names fill the basket and dilutes it; on a pool this thin the same
                                 # ~10 names fill it whatever the order. PRIMARY is not refusing the knob here —
                                 # IT IS BLIND TO IT. Treat a pre-registered mechanism as testable in its own
                                 # right, not just its verdict; this one was right by accident.
                                 #
                                 # THE FINDING, WHICH IS THE POINT OF THE ROUND. rho rises MONOTONICALLY in c on
                                 # BOTH horizons, 8 of 8 runs, on a lane whose pool is ~14x THINNER than (#170)'s.
                                 # That is now TWO independent lanes where the multiplier stack carries real
                                 # ordering information. But the gain is entirely INTERNAL TO THE BASKET: at 8y
                                 # ranks 2-5 improve (+5.2 -> +5.8, and the 2-5 book's own h2h against 6-10 climbs
                                 # 26 -> 29 -> 30 of 34) while rank 1 (+4.7 -> +3.7) and ranks 6-10 (+0.3 -> -0.6)
                                 # BOTH degrade. An equal-weight top-10 sums those to zero.
                                 # AND IT INVERTS (#170). There the offset SHARPENED rank 1 (+2.9 -> +3.7 at 8y);
                                 # here it BLUNTS it (+4.7 -> +3.7) while sharpening 2-5. Same knob, opposite sign
                                 # at the very top, and POOL DEPTH is the only thing that changed between the two
                                 # rounds. So (#170)'s "hands the order to the multiplier stack, which sharpens the
                                 # single best name" is a DEEP-POOL statement, not a property of the knob. Do not
                                 # re-cite it as one.
                                 #
                                 # WHAT THIS CLOSES. The score-ordering direction is now measured and refused at
                                 # both dials: growth_rank_normalise 0/3 ((#144)), growth_base_offset on the
                                 # gates-off instrument ((#170)) and on the only shippable non-vacuous column that
                                 # exists ((#172), here). Together with the gate stack carrying no live lead
                                 # (header, (#127)/(#128)/(#129)), growth_max_gate_failures refused do-not-build
                                 # ((#129)) and growth_corr_cap refused three ways ((#140)), every knob-shaped
                                 # lever on this heuristic is closed, and they close for ONE shared reason: the
                                 # gated pool yields ~6-10 names against a 10-name basket, so ranking knobs are
                                 # un-gradeable and de-dup knobs have nothing to de-dup. THE ONE LEVER LEFT IS
                                 # POOL SUPPLY ON MERIT — more names clearing the gates AS THEY STAND — and it is
                                 # not another threshold. (#151) already priced buying it by loosening a gate:
                                 # -1.2 pts/yr of top-10 mean at 8y, which PRIMARY refused. Widening the UNIVERSE
                                 # rather than the gates is the untried half.
                                 # NOT A LEVER, and this is (#120)'s forbidden argmax: the 6-10 slice is negative
                                 # on the shipped lane too, and shrinking VERDICT_TOP to drop it is refitting the
                                 # grader to the result. It is also an artifact — with the median pool at 6-10
                                 # names, ranks 7-10 are frequently EMPTY, so that slice is computed over the
                                 # widest windows only.
                                 # ARMS DELETED after the round per (#131); overlay was `growth_min_cagr: 15.0`
                                 # plus `growth_base_offset: <c>` over tests/ci-settings.yaml.
  growth_rank_normalise: 0.0     # (#144) MEASURED AND REFUSED 2026-08-26 — stays OFF, which is `v' = v`
                                 # for every additive term, so `base` is the shipped sum bit for bit and
                                 # every golden is untouched. This receipt records a REFUSAL, not a value
                                 # to tune. 0.0 = off; 1.0 = each of `score_parts`' eleven ADDITIVE terms
                                 # has its LEVEL replaced by its cross-sectional rank position mapped
                                 # back onto that term's own pool distribution,
                                 # `v' = v + w*(mean + sd*clip(inv_norm(rank_pct(v)), -3, 3) - v)`.
                                 #
                                 #   WHY IT WAS BUILT. (#129) closed the loosen-the-stack family at every
                                 #   arity and then named what was left in writing: "the lever this round
                                 #   actually uncovers is THE SCORE ... A round on the score is the next
                                 #   honest move, and it must be graded on rank-1 and h2h". Three of its
                                 #   instruments pointed the same way — the rank slices invert, Spearman
                                 #   collapses +0.25 -> +0.03 (8y) and +0.10 -> -0.03 (12y), and 87-99% of
                                 #   the ungated missed wealth files under `out-ranked`. The mechanism
                                 #   proposed here is the one `docs/heuristic-v2-spec.md:125-128` names:
                                 #   "Rank->inverse-normal, not raw z-scores. Fundamental distributions
                                 #   are fat-tailed and a single outlier will otherwise dominate the
                                 #   composite." `base` sums eleven UNBOUNDED LEVELS in mutually
                                 #   incommensurable units (CAGR %/yr, accel pts, ROE %, yield %,
                                 #   trend_r2 0..1, underwater YEARS), so one fat-tailed value can take
                                 #   the argmax outright.
                                 #
                                 #   UNIT-PRESERVING BY CONSTRUCTION, which is why no weight was refitted.
                                 #   A plain z would change a term's units from %/yr to sigma and silently
                                 #   invalidate all 14 `WEIGHT_DIMS` values and every cap that has a
                                 #   receipt in this file. Mapping back through the term's own mean/sd
                                 #   leaves all of them meaning what they say, so the arm tests exactly
                                 #   ONE thing: whether the ORDER improves once no single value can
                                 #   dominate. The multiplicative damps (proximity/value/trust/overext/
                                 #   ter/commodity/fx/acc) are deliberately NOT normalised — they are
                                 #   bounded ratios, not levels, so they are not this failure mode.
                                 #
                                 #   THE ARM. One knob on top of (#129)'s gates-off arm, recreated per
                                 #   (#131) from `git show 5159105:tests/gatesoff-ci-settings.yaml` and
                                 #   deleted after the run. Body, for re-creation as
                                 #   `tests/rank-ci-settings.yaml` (comment = the value THIS file ships):
                                 #     growth_min_range_pct: 0.0              # 80.0
                                 #     growth_min_cagr: -1e9                  # 19.0 (also the whole-life leg)
                                 #     growth_min_1y_pct: -1e9                # 0.0
                                 #     growth_min_5y_pct: -1e9                # 75.0
                                 #     growth_max_above_ma: 0.0               # 150.0
                                 #     growth_maxdd_cap: 0.0                  # 0.0 — ALREADY OFF, see below
                                 #     growth_max_peg: 0.0                    # 1.6
                                 #     growth_require_peg: false              # true
                                 #     growth_require_lifetime_uptrend: false # true
                                 #     print_recall_capture: true             # (#104) for the tie-back
                                 #     growth_rank_normalise: 1.0             # 0.0 — THE KNOB UNDER TEST
                                 #   `heuristic_drift()` named TEN knobs, not eleven, on all three runs.
                                 #   `growth_maxdd_cap` is absent because (#138) already moved this file's
                                 #   value to 0.0, so the arm's setting is a no-op — exactly the drift the
                                 #   (#125) correction warns about, caught by reading the WARNING line
                                 #   rather than trusting the arm file. Growth-lane pool 2084/6060/12223
                                 #   vs (#129)'s 2084/6035/11998: identical at 20y, +25 and +225 at 12y/8y
                                 #   from three days of universe growth. The 20y column is therefore an
                                 #   exact same-pool comparison; 12y and 8y carry that small drift.
                                 #
                                 #   WHY THE GATES-OFF POOL. The claim under test is "the score stops
                                 #   ordering a WIDE pool". On the shipped pool the gates admit 117/228/325
                                 #   names and (#129) shows the score orders them adequately — there is no
                                 #   defect there for a normalisation to repair. The gates-off pool is the
                                 #   only place the pathology is measurable.
                                 #
                                 #   PRE-REGISTERED STOP RULE, fixed in writing BEFORE any run: proceed
                                 #   only if Spearman rises at TWO of three horizons AND rank 1 stops being
                                 #   the worst slice at TWO of three. Otherwise dead, no ship-rule grid.
                                 #
                                 #   RESULT — RANK 1 IS STILL THE WORST SLICE AT 0 OF 3. Disjoint rank
                                 #   slices, excess mean|med pts/yr, (#129) arm -> (#144) arm:
                                 #     20y  rank 1 -0.3|+0.1 -> -0.4|+0.1   2-5 +2.0|+1.4 -> +0.6|-0.3
                                 #          6-10 +1.2|+1.1 -> +1.8|+1.1     11-20 +1.4|+1.1 -> +1.7|+2.0
                                 #     12y  rank 1 +1.5|-0.3 -> -0.1|-0.3   2-5 +3.5|+3.3 -> +2.7|+3.1
                                 #          6-10 +2.7|+3.1 -> +2.9|+2.2     11-20 +4.1|+2.9 -> +2.6|+2.8
                                 #      8y  rank 1 +2.9|+1.8 -> +1.2|+1.8   2-5 +4.3|+3.7 -> +3.4|+3.3
                                 #          6-10 +4.2|+3.8 -> +2.2|+1.4     11-20 +3.6|+2.8 -> +3.2|+2.9
                                 #   Rank 1's MEAN falls at all three horizons and it remains the lowest
                                 #   slice at all three. The stop rule fails 0/3, not 2/3.
                                 #
                                 #   AND THE RANK-1 H2H GUARD BREAKS AT 20y AND 12y -> REFUSE OUTRIGHT
                                 #   (GUARD > PRIMARY). #1 vs the 11-20 book, same window:
                                 #     20y  (#129) 7/20 (35%)  ->  (#144)  8/20 (40%)   GUARD BROKEN
                                 #     12y  (#129) 16/36 (44%) ->  (#144) 17/36 (47%)   GUARD BROKEN
                                 #      8y  (#129) 18/44 (41%) ->  (#144) 22/44 (50%)   passes, barely
                                 #   A broken guard REFUSES OUTRIGHT and is never softened to UNRESOLVED,
                                 #   so the verdict is settled twice over and the ship-rule grid on
                                 #   `universe stress` was never run.
                                 #
                                 #   THE SPEARMAN HALF OF THE STOP RULE WAS UNMEASURABLE, and this is the
                                 #   honest defect in the pre-registration. rho reads -0.09/-0.03/+0.03,
                                 #   BIT-IDENTICAL to (#129), because `lane_metrics` scores through the
                                 #   `fn(&Quote, &BuyHeuristic) -> Option<f64>` pointer, which this change
                                 #   deliberately did NOT widen (~20 call sites, and a wide backtest.rs
                                 #   diff reds the mutation gate). Only `report_vs_benchmark` — rank
                                 #   slices, h2h, the TOP_LADDER books, the worst window — sees the
                                 #   normalised score. It does NOT change the verdict: the stop rule is an
                                 #   AND whose other clause fails 0/3, and the h2h GUARD refuses on its
                                 #   own. Recorded so nobody re-reads that rho as evidence the knob is
                                 #   inert on the graded path — the BOOKS moved (20y top-10 +2.7|+2.2 ->
                                 #   +1.9|+1.3, worst -3.3 -> -2.7), which is the proof the wiring is live.
                                 #
                                 #   WHAT IT ACTUALLY MEASURED, AND IT IS NOT NOTHING. h2h rose at ALL
                                 #   THREE horizons (+5, +3, +9 points) while rank 1's MEAN excess fell at
                                 #   all three. Read together: the normalisation makes rank 1 beat its own
                                 #   middle MORE OFTEN but by LESS. That is the transform working exactly
                                 #   as designed — it damps the fat tail at the top — and it is the design
                                 #   PREMISE that is false on this pool: the fat tail at rank 1 was
                                 #   carrying the mean, so it is signal, not the outlier noise
                                 #   `spec:128` assumes. Do not re-file this as "the transform is broken".
                                 #
                                 #   WHY NO LADDER WAS RUN. The plan's ship grid was w in {0.25,0.5,1.0}.
                                 #   w = 1.0 is the MAXIMUM-DOSE probe: if full normalisation does not
                                 #   restore the ordering, a partial dose is not the reading that rescues
                                 #   it, and laddering after a failed pre-registered stop rule is the
                                 #   post-hoc search the rule exists to prevent. A future round wanting a
                                 #   partial dose must pre-register it as its own hypothesis.
                                 #
                                 #   WHAT SURVIVES. The direction is NOT closed the way (#129) closed the
                                 #   gate family — h2h moved the right way at 3/3. What is refused is THIS
                                 #   target: normalising ALL eleven terms uniformly, at full dose, on the
                                 #   gates-off pool. Two untried shapes remain and both must be
                                 #   pre-registered and graded on rank-1 + h2h: normalising the MIDDLE of
                                 #   each term's distribution while leaving the top decile at its level,
                                 #   and the sector-neutral form `spec:128` actually asks for (ungradeable
                                 #   today — the backtest pool carries no GICS sector, see
                                 #   `commodity_damp`'s "sector unknown (backtest)").
                                 #
                                 #   REVERT: already at the reverted value; 0.0 IS the shipped policy and
                                 #   arming it is a graded change needing its own receipt, pinned by
                                 #   `shipped_config_pin`. Re-run: three `backtest {20,12,8} universe fund
                                 #   pit` on the arm body above, against (#129)'s banked gates-off column —
                                 #   the control needs no re-run. NEVER set FOLIOMAN_OFFLINE=1 on these:
                                 #   it cuts the 8y king walk from 699 scored windows to 32 and prints the
                                 #   SHIPPED lane at -5.9 excess, exiting 0 with a complete-looking report.
                                 #   Check `windows scored` (2084/6060/12223 growth-lane here).
  print_recall_capture: true     # (#104 / round 3 §1) UNMEASURED — an unfitted judgement value,
                                 # shipping OFF, which is today's report and what every golden holds.
                                 # It is display-only, so "unmeasured" here means the INSTRUMENT has
                                 # not been read yet, not that a value is unfitted: turning it on
                                 # measures something nothing else in this file can see.
                                 #
                                 #   THE GAP, and it is the largest one round 3 names: every number
                                 #   this report prints is a PRECISION metric. rho, edge, top-N excess,
                                 #   rank-1 h2h — all of them answer "of what I picked, how much was
                                 #   good?". Under the skew a 20-year hold actually lives in
                                 #   (Bessembinder: 4.3% of firms produced all net wealth creation, the
                                 #   top 0.33% half of it), terminal wealth is decided by the OPPOSITE
                                 #   question: of what was good, how much did I pick? A lane can post an
                                 #   excellent rho and miss every 20-bagger, and its book will badly
                                 #   trail a sloppier lane that caught two.
                                 #
                                 #   WHY `gate_audit` DOES NOT ALREADY COVER IT: that audit prices a
                                 #   gate by the MEAN forward return of the names it rejected. A mean is
                                 #   blind to skew by construction. A gate that rejects 300 names
                                 #   averaging -40 pts scores as a GOOD gate even if three of those 300
                                 #   were the decade's 30-baggers — which is the single most expensive
                                 #   mistake this tool could be making, and it currently has no
                                 #   instrument that could detect it.
                                 #
                                 #   WHAT IT PRINTS: per ~6mo bucket, against the FULL pool including
                                 #   gate-rejected rows, recall = |winners held| / |winners| and
                                 #   capture = the same intersection weighted by realized multiple.
                                 #   Capture is the one that matters: recall counts a 30-bagger and a
                                 #   3-bagger the same. Two baskets (top-3, the basket the footer sells;
                                 #   top-10, the book `track` grades) x two winner cuts (top 1% of the
                                 #   bucket; >=10x outright).
                                 #
                                 #   THE GATES THIS EXISTS TO RE-PRICE, and none of them is asserted
                                 #   wrong here — the claim is only that no instrument could currently
                                 #   tell you if one is:
                                 #     growth_min_range_pct: 80  - must sit in the top 20% of its own
                                 #       10y range. Most extreme compounders spent part of their life
                                 #       outside that band.
                                 #     growth_min_1y_pct: 0      - deletes every compounder having one
                                 #       down year. Its receipt's "-108 pts forward" defence is a
                                 #       PRECISION argument, which is exactly what a recall metric is
                                 #       for challenging.
                                 #     growth_min_cagr: 19.0     - a name is admitted only after it has
                                 #       already compounded at 19%/yr.
                                 #     growth_max_above_ma: 150 and growth_maxdd_cap: 84.
                                 #
                                 #   THE RUN THAT SETTLES IT: `backtest universe 20 --wide` with this
                                 #   on, then the same run with each of the five gates above widened one
                                 #   rung, comparing CAPTURE rather than edge. A gate whose capture cost
                                 #   is large and whose edge cost is small is a gate paying precision
                                 #   for terminal wealth. Nothing here proposes acting on that before it
                                 #   is measured. REVERT: set false; no score, gate or journal reads it.
                                 # #
                                 # #   (#154) FLIPPED ON, 2026-08-29 — and the reason is NOT "the instrument is
                                 # #   worth reading". (#91) settled that one the other way for print_n_eff
                                 # #   ("reading an instrument is not a reason to ship its output into every
                                 # #   golden") and that precedent stands. The reason is that the output is a
                                 # #   REGRESSION PIN, which is a thing the goldens can do and a per-run overlay
                                 # #   cannot.
                                 # #   MEASURED COST, exactly: +143 lines across all five goldens, 0 deletions,
                                 # #   and NO number outside the new block moves. Re-blessed with FOLIOMAN_BLESS=1
                                 # #   under FOLIOMAN_CONFIG=/nonexistent/none.yaml; suite 368/9(1 ignored)/21.
                                 # #   WHAT IT NOW PINS, and nothing in the suite could catch this before: the
                                 # #   (#127)/(#128) attribution itself — per lane, per horizon, which gate owns
                                 # #   how much of the missed wealth, the sole-blocker column, and the "at most
                                 # #   X% is reachable by loosening ONE gate" ceiling every admission refusal in
                                 # #   this file cites. Those numbers were previously reproducible only by
                                 # #   remembering an overlay, so a change that silently moved them was invisible.
                                 # #   The tie-back the print site documents (this table's % must equal 100 − the
                                 # #   capture printed above, ±1) is now enforced by the goldens rather than by a
                                 # #   reader checking it.
                                 # #   READING THE OUTPUT — TWO BLOCKS ARE TWO LANES, NOT TWO BASKETS. The "two
                                 # #   baskets (top-3 ...; top-10 ...)" sentence above is STALE: (#120) collapsed
                                 # #   that to ONE row against VERDICT_TOP, because the two had become the same
                                 # #   number. The header appears twice per run because `report_lane` runs once
                                 # #   per lane, and the section header above it (`── ON-SALE (buy_score) ──` /
                                 # #   the GROWTH one) is what names it. Only the GROWTH block carries the "why
                                 # #   the misses were missed" tables — `gate_failures` reports the GROWTH gates,
                                 # #   so attributing them under the other lane would blame one lane's misses on
                                 # #   the other lane's gates.
                                 # #   A NUMBER THIS IMMEDIATELY PINS, worth reading beside (#152): at 20y,
                                 # #   `history` is the SOLE blocker for 32 of the missed >=10x winners = 33.8% of
                                 # #   that missed wealth — the largest single-gate share anywhere in these
                                 # #   tables. (#152) measured that same gate CORRECT (0 of 138 short-history
                                 # #   equities are truncated feeds; the rejects are genuinely young companies,
                                 # #   median trading life 0.53y). Both hold at once, and the pair is the point:
                                 # #   a third of the extreme wealth sits behind a gate that is not wrong, which
                                 # #   is what "the ceiling is structural" means in numbers.
                                 # #   NOT A VERDICT ON ANYTHING. Per (#149) every frozen-fixture lane is VACUOUS
                                 # #   (gated pool <= graded basket), so these fixture figures grade no knob. They
                                 # #   are a PIN on the report's arithmetic, read on real runs.
                                 # #   REVERT RULE: set false and re-bless; no score, gate or journal reads it, so
                                 # #   reverting reverts nothing that was decided. Revert if the block's churn
                                 # #   starts drowning the diff it is meant to make reviewable.
                                 #
                                 #   (#127) THIS FLAG NOW PRINTS A SECOND TABLE, 2026-08-23: MISSED-WINNER
                                 #   ATTRIBUTION, on the GROWTH lane only. Recall/capture count what the
                                 #   lane missed; they never say WHY, and the three answers need three
                                 #   different fixes — a gate name (a threshold), `unassessable` or
                                 #   `history` (DATA COVERAGE, which no knob moves), or `out-ranked` (the
                                 #   only bucket a scoring weight can touch). "THE RUN THAT SETTLES IT"
                                 #   above proposes widening five gates one rung each and comparing
                                 #   capture; the attribution answers the same question in ONE run by
                                 #   naming the blocking gate directly, and it does it without moving a
                                 #   gate at all. Reasons come from `picks::gate_failures`, which
                                 #   `gate_failures_agrees_with_the_scorer` pins to the scorer, so the
                                 #   named gate is the gate the lane actually applied. GROWTH-only for
                                 #   that same reason — beside `buy_score` it would blame one lane's
                                 #   misses on the other lane's gates. Rows rank by missed WEALTH, not
                                 #   headcount, for the reason capture exists. EACH ROW CARRIES TWO
                                 #   COUNTS, and reading only the first inverts the table's meaning: a
                                 #   blocked name usually fails several gates and is attributed to the
                                 #   FIRST in gate order, so an early gate inherits the blame of every
                                 #   later one. The SOLE-BLOCKER count is the winners failing EXACTLY one
                                 #   gate — the only cohort a single-knob loosening can buy back — and the
                                 #   summary line sums it into a hard ceiling on what any one knob could
                                 #   ever recover. (#127) is that reading; on the shipped gates the first
                                 #   column's largest row has a sole-blocker count of ZERO. It reconciles with the
                                 #   recall line by construction (same winner cut, same buckets, same n)
                                 #   and PRINTS that tie-back: the missed-wealth % must be the complement
                                 #   of the capture % on the line above. No new knob was added for it —
                                 #   this flag already asked the question, and it still ships false, so
                                 #   the goldens are untouched. Same REVERT: set false.
                                 #
                                 #   (#128) AND A THIRD AND FOURTH, 2026-08-23, for the question (#127)'s own
                                 #   ceiling raises: if one gate reaches at most 10-16% of the missed wealth,
                                 #   which TWO would have to move together? The pair table tallies, per missed
                                 #   winner failing EXACTLY two gates, the pair it failed — exactly two and not
                                 #   every pair drawn from a k-failure name, because five failures make ten pairs
                                 #   and a two-knob arm buys none of them, so enumerating those would credit each
                                 #   row with wealth no arm can reach. Its BOTH-NEAR-MISS column reuses
                                 #   `gate_failures`' third field, the per-gate slack the codebase already
                                 #   committed to ("nearly passed"), which until now was computed at every push
                                 #   site and discarded: it separates the part of a pair a shippable loosening
                                 #   admits from the part that misses by miles. The GATE-COUNT HISTOGRAM is the
                                 #   honesty check on the pair table — if the missed wealth sits at 3+ gates, no
                                 #   two-knob arm reaches it and the pair rows are a rounding error. It excludes
                                 #   `unassessable` by construction: `None` is not "zero gates failed", it is no
                                 #   gate list at all, and folding it into bucket 0 would read as "cleared
                                 #   everything". Read the (#128) receipt below for what all three said; the short
                                 #   version is that the histogram refused the arm. Still no new knob, still ships
                                 #   false, goldens untouched. Same REVERT: set false.
                                 #
                                 # (#124) MEASURED 2026-08-23, `universe fund pit` at 20y/12y/8y, held book
                                 # (VERDICT_TOP=10), against the FULL pool including every gate-rejected row:
                                 #             vs top-1% winners          vs >=10x winners
                                 #     20y   recall 14%  capture 24%    recall 14%  capture 21%   (n=28 / 318)
                                 #     12y   recall 12%  capture  7%    recall  4%  capture  5%   (n=90 / 241)
                                 #      8y   recall  5%  capture  6%    recall  5%  capture  6%   (n=174 / 77)
                                 # THE ANSWER IS LOW, and capture is the half that matters: the ranking held 5-24%
                                 # of the extreme wealth its own pool contained, single digits at 12y and 8y. Every
                                 # precision metric in this report can look healthy while that is true - and they
                                 # do, which is exactly the gap (#104) was written to expose.
                                 # WHY IT IS LOW, from (#106) read on the same runs: the book never holds more than
                                 # ~10 names, because the gates admit no more than that. This is an ADMISSION
                                 # problem, not a ranking problem, and it inverts the standing plan - every open
                                 # task in docs/heuristic-v2-tasks.md (P1-P4) proposes adding MORE exclusions, each
                                 # to be graded by `gate_audit`, whose mean is blind to precisely the rejected
                                 # 30-bagger this line counts. Those tasks are not refuted by this reading, but they
                                 # can no longer be started on their stated priors alone.
                                 # THE CHANGE THAT WOULD MOVE THIS NUMBER is a LOOSER funnel, not a tighter one.
                                 # Nothing here says WHICH gate to loosen (the gate-sweep flags in the same runs
                                 # disagree across horizons), and no gate was changed on the strength of it.
                                 # Knob stays false. REVERT: set false; no score, gate or journal field reads it.
                                 #
                                 # (#127) WHY THOSE MISSES WERE MISSED — MEASURED 2026-08-23, same `universe fund pit` runs at
                                 # 20y/12y/8y, GROWTH lane, held book (VERDICT_TOP=10). (#124) above ends on "Nothing here says
                                 # WHICH gate to loosen". This is that answer, and it is not the answer the shape of (#124)
                                 # suggested. Share of MISSED WEALTH by the gate that blocked it, >=10x cut:
                                 #             20y            12y            8y
                                 #   range     49.5% (101)    64.0% (113)    37.7% (32)
                                 #   cagr      36.1% (140)    24.3% ( 71)    32.6% (20)
                                 #   history    9.3% ( 28)     4.1% ( 19)     8.2% ( 5)
                                 #   1Y+        2.3% ( 13)     3.7% (  9)     5.1% ( 3)
                                 #   stretch    0.5% (  5)       -            8.6% ( 4)
                                 #   maxdd        -            2.2% (  8)     2.9% ( 4)
                                 #   out-ranked 0.2% (  2)     0.0% (  0)     1.5% ( 2)
                                 # THE FIRST-FAILURE COLUMN ABOVE IS A TRAP AND THE TABLE PRINTS ITS OWN ANTIDOTE. A blocked name
                                 # usually fails SEVERAL gates, and this attributes it to the FIRST in gate order — and `range`
                                 # sits ahead of `cagr`, `1Y+` and `stretch`, so it inherits their blame. The SOLE-BLOCKER column
                                 # counts only winners failing EXACTLY ONE gate, which is the only cohort a single-knob loosening
                                 # could ever buy back:
                                 #   range      SOLE BLOCKER FOR ZERO WINNERS AT EVERY HORIZON AND BOTH CUTS — 0 of 361.
                                 #   cagr       >=10x: 8 (1.9%) at 20y, 6 (1.2%) at 12y, 0 at 8y. top 1%: 0 / 1 / 1.
                                 #   history    >=10x: 28 / 19 / 5 (top 1%: 4 / 3 / 13) — 100% sole everywhere, and the one
                                 #              row no threshold can move.
                                 #   maxdd      absent at 20y; 12y 8 (>=10x) / 6 (top 1%); 8y 4 / 6 — also 100% sole, and
                                 #              worth only 2.2-2.9% of the missed wealth either way.
                                 # CEILING ON ANY SINGLE-KNOB LOOSENING (the table's own summary line), share of missed wealth:
                                 #   20y 14.9% (>=10x) / 13.4% (top 1%);  12y 10.1% / 5.9%;  8y 16.2% / 16.2%.
                                 # Subtract `history`, which is DATA COVERAGE and not a threshold, and the THRESHOLD-reachable
                                 # share is 5.6% / 2.0% at 20y, 6.0% / 5.2% at 12y, 8.0% / 7.6% at 8y. That is an UPPER bound: it
                                 # assumes the loosening admits the winner and costs nothing, when in practice a wider gate admits
                                 # losers by the hundred. Everything else fails 2+ gates at once, so it needs a JOINT loosening,
                                 # which is a different and much more expensive experiment than any rung on this file's ladders.
                                 # THE STOP RULE WAS PRE-REGISTERED AND IT REFUSED. The rule was: one reason carrying >=40% of
                                 # missed capture at 8y AND 12y becomes the round's grid target. `range` reads 64.0%/70.6% at 12y
                                 # but 37.7%/35.4% at 8y, so it FAILED the bar as written — and the sole-blocker column then showed
                                 # that the 12y number was an artifact of gate ordering, not a lever. NO GRID WAS RUN. Recording
                                 # that the threshold was missed, rather than relaxing it after seeing the data, is the point.
                                 # SELECTION IS CONFIRMED DEAD, IN ONE NUMBER. `out-ranked` — cleared every gate, ranking still
                                 # missed it — is 0.2% / 0.0% / 1.5% of missed wealth. (#124) inferred "ADMISSION, not selection"
                                 # from book size; this measures it directly. Any future proposal to move `growth_fund_weight`,
                                 # `quality_weight`, `op_margin` or the tilt weights must clear this line first: on 20y/12y/8y PIT
                                 # data there is at most ~2% of the extreme wealth for a scoring change to win, and (#71)/(#72)/
                                 # (#75) each already spent a round finding that out the expensive way.
                                 # WHAT THIS DOES NOT SAY: it does not say the gates are correct, only that no ONE of them is
                                 # individually responsible. The largest genuinely single-gate cohort at every horizon is
                                 # `history` (no 5y+ record), which is a data-coverage ceiling — see `growth_min_leg_years`, which
                                 # prices it at 1949 of 4748 live EU-buyable names. That is the only rung this table endorses
                                 # looking at next, and it is a COVERAGE problem, not a threshold to sweep.
                                 #   (#152) THAT RUNG IS NOW CLOSED, 2026-08-29 — and the endorsement above is the
                                 #   thing it refutes. The `history` rejects are GENUINELY YOUNG, not served short:
                                 #   over the local chart cache, 0 of 138 short-history EQUITIES have a
                                 #   firstTradeDate older than 5y, and their median actual trading life is 0.53y.
                                 #   There is no coverage fix that admits them. Full receipt and the offline
                                 #   reproduction at `growth_min_leg_years`. With it, admission has no open lever
                                 #   left: selection (#127), one gate (#151), two gates (#128), universe (#134),
                                 #   coverage (#152). Cite (#152) before quoting the sentence above as a lead.
                                 # NO KNOB WAS CHANGED ON THE STRENGTH OF THIS. The instrument is `print_recall_capture`, which
                                 # still ships false; the goldens are untouched.
                                 #
                                 # (#128) HOW MANY GATES, AND WHICH TWO — MEASURED 2026-08-23, same `universe fund pit` runs,
                                 # 20y/12y/8y, GROWTH lane, both cuts. THIS IS THE EXPERIMENT (#127) ABOVE SAID WAS THE ONLY ONE
                                 # LEFT, AND IT REFUSES. (#127) closed on "at most 10-16% of the missed wealth is reachable by
                                 # loosening ONE gate; the rest fails 2+ gates at once", which reads as an invitation to loosen
                                 # two. So the instrument now also tallies, per missed winner, the EXACT PAIR it failed (only for
                                 # winners failing exactly two — a five-failure name has ten pairs and a two-knob arm buys none of
                                 # them) and the histogram of HOW MANY gates each miss failed at once. Both printed behind the same
                                 # `print_recall_capture`, which still ships false.
                                 #
                                 #   PRE-REGISTERED BEFORE THE RUN, all three required to grade any joint arm:
                                 #     (1) CONSISTENCY  one pair holds >=25% of missed wealth at 20y AND 12y AND 8y.
                                 #     (2) REACHABILITY that pair's BOTH-NEAR-MISS subset is >=1/3 of its own wealth at 12y and 8y.
                                 #     (3) DEPTH        the gate-count histogram does NOT put the plurality of missed wealth at 3+.
                                 #   ALL THREE FAILED. No arm was graded, no knob moved, no grid ran.
                                 #
                                 #   (1) FAILS BY 3-14x, AND THE PAIR IS NOT EVEN STABLE. Largest pair at any horizon or cut:
                                 #       20y  range+maxdd       6.2% (top 1%)  /  range+maxdd     3.6% (>=10x)
                                 #       12y  cagr+maxdd        1.9% (top 1%)  /  cagr+cagr-life  1.7% (>=10x)
                                 #        8y  stretch+maxdd     6.4% (top 1%)  /  stretch+maxdd   8.6% (>=10x)
                                 #       Nothing reaches 25% anywhere, and the top pair is a DIFFERENT pair at each horizon. There
                                 #       is no joint hypothesis to pre-register; there are three unrelated ones, each worth ~5%.
                                 #
                                 #   (2) FAILS TO ~ZERO. Every both-near-miss column at 20y and 12y is 0 winners. The single
                                 #       non-zero cell in the whole measurement is 8y `stretch+maxdd`, 1 of 4 winners (1.8 of 8.6
                                 #       points), i.e. 21% — under the bar. READ THAT PLAINLY: of the winners that failed exactly
                                 #       two gates, essentially NONE missed both of them narrowly. The near-miss flag is
                                 #       `gate_failures`' own third field (range -10pp, cagr -1.5pp, 1Y -10pp, ...), i.e. the
                                 #       codebase's existing definition of "nearly passed", not one invented for this run.
                                 #
                                 #   (3) FAILS AT ALL SIX TABLES, and this is the finding that matters. Missed wealth by number of
                                 #       gates failed at once (gate-assessable winners; `unassessable` has no gate list):
                                 #         20y top 1%        1:13 2:7  3:28 4:15 5:20 6:18            -> 3+ = 81%, mode 3
                                 #         20y >=10x    0:0  1:15 2:11 3:26 4:17 5:18 6:12 7:1       -> 3+ = 74%, mode 3
                                 #         12y top 1%        1:6  2:6  3:7  4:19 5:33 6:24 7:4  8:1  -> 3+ = 88%, mode 5
                                 #         12y >=10x         1:10 2:6  3:11 4:16 5:29 6:20 7:6  8:1  -> 3+ = 83%, mode 5
                                 #          8y top 1%   0:2  1:16 2:14 3:19 4:15 5:16 6:10 7:4  8:1  -> 3+ = 65%, mode 3
                                 #          8y >=10x    0:2  1:16 2:10 3:20 4:15 5:19 6:11 7:4  8:2  -> 3+ = 71%, mode 3
                                 #       Winners failing exactly two gates hold only 6.0-13.6% of the missed wealth at any horizon,
                                 #       and the tail runs to EIGHT gates failed simultaneously. The buckets are true counts, not a
                                 #       clamp: a "7+" lumping was written first and dropped, because HOW FAR outside the gates these
                                 #       winners sit is the one thing this table exists to say.
                                 #
                                 #   WHAT THIS RETIRES, AND IT IS A WHOLE CLASS. Take the three ceilings together:
                                 #       one gate      <= 16.2% of missed wealth   ((#127))
                                 #       exactly two   <= 13.6% of missed wealth   (this receipt)
                                 #       and of that two-gate cohort, the near-miss share is ~0.
                                 #   The names this lane misses are not sitting just outside the boundary. They fail a MEDIAN of
                                 #   three to five growth gates SIMULTANEOUSLY and they fail them by wide margins. No loosening of
                                 #   any arity reaches them; one large enough to admit them would not be the growth heuristic any
                                 #   more, it would be an ungated momentum screen. THRESHOLD TUNING IS EXHAUSTED — that is the
                                 #   round's finding, and it should be cited to refuse the next "just loosen X" proposal the way
                                 #   (#127)'s `out-ranked` column (0.2%/0.0%/1.5%) is cited to refuse the next selection-weight one.
                                 #
                                 #   WHAT IS LEFT, STATED SO THE NEXT ROUND DOES NOT REDISCOVER IT: the reachable levers are no
                                 #   longer thresholds. They are (a) DATA COVERAGE — `history` is the largest genuinely single-gate
                                 #   cohort at every horizon (9.3%/4.1%/8.2% of missed wealth) and no threshold moves it, see
                                 #   `growth_min_leg_years`; and (b) POOL SIZE, which is blocked — see the (#128) correction on
                                 #   (#100 / finding C10) below.
                                 #
                                 #   NO KNOB WAS CHANGED. Goldens untouched. REVERT RULE: none applies — this receipt records a
                                 #   measurement and a refusal, not a value. Re-running it costs three `universe fund pit` runs with
                                 #   `print_recall_capture` on (tests/diag-ci-settings.yaml).
                                 #
                                 # (#129) THE GATES-OFF CEILING — MEASURED 2026-08-23, and it CLOSES the loosen-the-stack family
                                 # at every arity. Read this before proposing ANY gate loosening; it is the refusal of last resort.
                                 #
                                 #   WHAT WAS ASKED. (#127) and (#128) refused every threshold arm they could name and closed with
                                 #   "threshold tuning is exhausted at every arity", which leaves exactly one question: not WHICH
                                 #   gate, but whether the gate STACK AS A CONJUNCTION pays for itself at all. Two readings pointed
                                 #   at "no". The GATE AUDIT (#9) on the PIT pool comes back SPLIT at two of three horizons, and at
                                 #   20y the ADMITTED cohort averaged 260 points BELOW the pool it rejected. And the one-at-a-time
                                 #   GATE SWEEP flags `growth_maxdd_cap ->off` and `growth_min_cagr -4` TOO TIGHT at 8y/12y while
                                 #   both flip hard negative at 20y — the signature of gates cutting a tail.
                                 #
                                 #   HOW IT WAS READ, WITHOUT ONE LINE OF RUST. `tests/gatesoff-ci-settings.yaml` turns off every
                                 #   gate that BINDS in the backtest lane, each via its own documented off-value: range 80->0,
                                 #   min_cagr 19->-1e9 (this also governs the whole-life leg), min_1y 0->-1e9, min_5y 75->-1e9,
                                 #   above_ma 150->0, maxdd_cap 84->0, max_peg 1.6->0, require_peg true->false,
                                 #   require_lifetime_uptrend true->false. DELIBERATELY NOT LOOSENED: `growth_min_leg_years` and
                                 #   `fixed_cagr_years`, because they feed `long_leg_fixed` and so DEFINE THE CAGR WINDOW THE SCORE
                                 #   RANKS ON — moving them redefines the score instead of removing a gate, and the arm would stop
                                 #   being a read on the stack. Already inert here and so left alone: min_avg_turnover_eur and
                                 #   growth_min_age_years (LIVE-only), max_1m_drop_pct, min_8y/20y_pct, and every fundamentals floor.
                                 #
                                 #   THE ARM ARMED. `heuristic_drift()` named EXACTLY ten knobs on all three arm runs and none on the
                                 #   three control runs, and the admitted count went 117 -> 2084 (20y), 228 -> 6035 (12y),
                                 #   325 -> 11998 (8y).
                                 #
                                 #   THE GATE AUDIT (#9), BANKED — three horizons on the PIT pool, never written down before. This is
                                 #   the control arm, i.e. the SHIPPED gates:
                                 #     20y  accepted n=117  mean -246.4 med -391.2 | rejected n=2179  mean +13.2 med -408.3
                                 #                                                 | gap mean -259.7 med  +17.0  SPLIT
                                 #     12y  accepted n=228  mean  +17.0 med  -75.6 | rejected n=8122  mean  -0.5 med  -68.6
                                 #                                                 | gap mean  +17.5 med   -7.0  SPLIT
                                 #      8y  accepted n=325  mean  +48.7 med   -5.8 | rejected n=16480 mean  -1.0 med  -20.8
                                 #                                                 | gap mean  +49.7 med  +15.0  gates SELECT winners
                                 #   Two of three horizons SPLIT. That is what motivated the arm, and the arm explains it — see the
                                 #   MECHANISM paragraph, which is the reason a name-level peer-relative gap does not become a
                                 #   portfolio win.
                                 #
                                 #   PRIMARY PASSED AT ALL THREE HORIZONS, AND IT DOES NOT MATTER. top-10 book, excess mean|med
                                 #   pts/yr, and the worst-window guard:
                                 #     20y  control +0.8|+0.9  worst -10.0   ->   arm +2.7|+2.2  worst  -3.3
                                 #     12y  control +2.8|+2.8  worst  -9.5   ->   arm +4.1|+3.8  worst  -2.3
                                 #      8y  control +4.5|+4.5  worst  -6.2   ->   arm +5.0|+4.7  worst  -1.7
                                 #   Both PRIMARY clauses up at 20y AND 8y, and the worst-window GUARD improves at every horizon.
                                 #   This is precisely the trap the `growth_min_leg_years` receipt names in capitals: "THE TOP-10
                                 #   COLUMN IS THE TRAP: it RISES at every horizon, and that is the dilution signature, not a win...
                                 #   Grade rank-1 and h2h, not top-10." Ship Rule v2 encodes that as a GUARD for this exact reason.
                                 #
                                 #   THE RANK-1 H2H GUARD BREAKS AT ALL THREE HORIZONS -> REFUSE OUTRIGHT (GUARD > PRIMARY):
                                 #     20y  control #1 beat the 11-20 book 1/2 (50%)  ->  arm  7/20 (35%)
                                 #     12y  control                        2/4 (50%)  ->  arm 16/36 (44%)
                                 #      8y  control                        4/6 (67%)  ->  arm 18/44 (41%)
                                 #   READ THE DENOMINATORS — they cut the other way this time. The CONTROL h2h rests on 2, 4 and 6
                                 #   windows, barely a statistic, because a gated lane seldom admits 20 names in one window and so
                                 #   seldom has an 11-20 book to compare against. The ARM's 20/36/44 are the first genuinely readable
                                 #   rank-1 h2h numbers this lane has ever produced, and they say the top of the list loses to its own
                                 #   middle roughly six times out of ten.
                                 #
                                 #   THE MECHANISM, AND IT IS THE ROUND'S REAL FINDING. On the widened pool the score does not merely
                                 #   stop helping, it INVERTS. Disjoint rank slices, ARM, excess mean|med pts/yr:
                                 #     20y  rank 1 -0.3|+0.1   2-5 +2.0|+1.4   6-10 +1.2|+1.1   11-20 +1.4|+1.1   21-50 +2.4|+2.0
                                 #     12y  rank 1 +1.5|-0.3   2-5 +3.5|+3.3   6-10 +2.7|+3.1   11-20 +4.1|+2.9   21-50 +3.0|+2.4
                                 #      8y  rank 1 +2.9|+1.8   2-5 +4.3|+3.7   6-10 +4.2|+3.8   11-20 +3.6|+2.8   21-50 +2.6|+2.4
                                 #   Rank 1 is the WORST slice at every horizon. In the CONTROL at 8y it is the BEST (+4.6|+6.2 against
                                 #   2-5 at +3.8|+4.7). A second and independent instrument reads the same thing —
                                 #   Spearman(growth_score, peer-relative) on the GROWTH lane:
                                 #     20y  control -0.08 (n=117)  ->  arm -0.09 (n=2084)
                                 #     12y  control +0.10 (n=228)  ->  arm -0.03 (n=6035)    sign flip
                                 #      8y  control +0.25 (n=325)  ->  arm +0.03 (n=11998)   ~88% of the ranking power gone
                                 #   THE GATES ARE NOT A FILTER IN FRONT OF THE SCORE. THEY ARE THE PRECONDITION FOR THE SCORE TO RANK
                                 #   AT ALL. That also explains (#128)'s central puzzle — why the missed winners fail three to five
                                 #   gates at once: on names that fail gates the score carries no usable order, so admitting them
                                 #   cannot help. It destroys the ordering the top-10 book is built on.
                                 #
                                 #   ONE CAVEAT ON THE PRIMARY COLUMN, recorded because it weakens a clause that already lost: the arm
                                 #   is graded on MORE windows than the control (21 vs 20, 37 vs 34, 45 vs 42), and the extra windows
                                 #   are ones the control could not score at all for want of admitted names. The two columns are
                                 #   therefore not the same window set. Moot under GUARD precedence — but do not quote the PRIMARY
                                 #   delta as a clean comparison.
                                 #
                                 #   VERDICT: CLOSED, per the stop rule fixed in writing before any run. The rank-1 h2h GUARD is broken
                                 #   at 20y, 12y AND 8y, and a broken guard REFUSES OUTRIGHT — it is never softened into UNRESOLVED.
                                 #   The loosen-the-stack family is now finished at every arity: one gate ((#127)), exactly two
                                 #   ((#128)), and the whole conjunction (here). NO LATER ROUND MAY PROPOSE A GATE LOOSENING WITHOUT
                                 #   CITING THIS RUN.
                                 #
                                 #   AND IT REFUSES THE KNOB IT WAS RUN TO JUSTIFY. This ceiling existed to decide whether
                                 #   `growth_max_gate_failures` (0 = today's conjunction, k = admit up to k gate failures) was worth
                                 #   building across the ~25 `return None` sites in `picks::score_parts`. It is NOT. Every k>0 admits
                                 #   names the score ranks backwards, and the ceiling reading is what k would be climbing toward. Do
                                 #   not build it.
                                 #
                                 #   THE TIE-BACK, AND IT INVERTS (#127)'s HEADLINE. `print_recall_capture` was on in the arm, so the
                                 #   same missed-winner instrument (#127)/(#128) built reports on the UNGATED pool. Capture of the
                                 #   pool's extreme winners rose exactly as loosening should make it rise — GROWTH lane, top 1%:
                                 #     20y  recall 14% capture 12%    12y  recall 16% capture 11%    8y  recall 12% capture 15%
                                 #   against 0% / 2% / 4% for the gated control that (#128) recorded. So the gates WERE excluding real
                                 #   winners and turning them off DID admit them. The book still refused. Capture went up; the
                                 #   portfolio did not.
                                 #   And then the blame column re-files completely. On the ungated pool the first-failure table reads,
                                 #   at every horizon and both cuts:
                                 #     20y top 1%   out-ranked 87.1%   history 12.9%     20y >=10x   out-ranked 89.9%   history 10.1%
                                 #     12y top 1%   out-ranked 99.2%   history  0.8%     12y >=10x   out-ranked 95.2%   history  4.4%
                                 #      8y top 1%   out-ranked 87.6%   history  9.7%      8y >=10x   out-ranked 88.0%   history  9.7%
                                 #   `out-ranked` was 0.2% / 0.0% / 1.5% in the gated control, and (#127) cited exactly those three
                                 #   numbers to declare SELECTION DEAD. That verdict was true only INSIDE the gated pool. Remove the
                                 #   gates and essentially ALL of the missed wealth re-files as out-ranked: the winners are in the
                                 #   pool, the score is looking straight at them, and it does not put them in the top ten. THE GATES
                                 #   WERE HIDING THE SELECTION PROBLEM, NOT CAUSING IT. Do not cite (#127)'s `out-ranked` row to refuse
                                 #   a selection proposal without also citing this paragraph.
                                 #   Two more things the same block says, both worth having in writing. Even with EVERY gate off the
                                 #   lane still misses 83-90% of the extreme winners' wealth, so gating was never the main reason
                                 #   winners were missed. And at 20y the binary's own null-model line flags the arm outright:
                                 #   `vs null model (tuning off): rho -0.13  edge +8.0 pts  ->  tuning adds -311.2 pts  <- the tuning
                                 #   is NOT EARNING ITS KEEP`.
                                 #
                                 #   WHAT THIS LEAVES, AND IT IS NOT WHAT (#128) EXPECTED. (#128) named two surviving levers, DATA
                                 #   COVERAGE and POOL SIZE, on the assumption that admission was the binding constraint. Pool size is
                                 #   now measured and small — see the (#129) correction on (#100 / finding C10) below. Data coverage
                                 #   (`history`) survives and is real: it is the ONLY named blocker left standing on the ungated pool
                                 #   (0.8-12.9%), and no threshold moves it — it is names without enough record to score.
                                 #   But the lever this round actually uncovers is THE SCORE. Three instruments now say the same thing
                                 #   from three directions: the rank slices invert (rank 1 worst at every horizon), Spearman collapses
                                 #   (+0.25 -> +0.03 at 8y, +0.10 -> -0.03 at 12y), and 87-99% of the ungated missed wealth files under
                                 #   `out-ranked`. Note also that Spearman is -0.08 on the GATED pool at 20y: the ranking has never
                                 #   worked at the king horizon, gates or no gates, and no gate change was ever going to fix that.
                                 #   A round on the score is the next honest move, and it must be graded on rank-1 and h2h — this run
                                 #   is the standing proof that the top-10 column will happily rise while the ordering rots underneath.
                                 #
                                 #   NO KNOB WAS CHANGED. No Rust was touched at all, so the goldens cannot have moved. REVERT RULE:
                                 #   none applies — this receipt records a measurement and a refusal, not a value. Re-run: six
                                 #   `backtest {20,12,8} universe fund pit`, control on this file, arm on
                                 #   `tests/gatesoff-ci-settings.yaml`, which is kept in-tree for exactly that purpose.
  print_held_loser_factors: true # (#159) MEASURED 2026-08-29 — the instrument that finally asks what
                                 # the lane BOUGHT AND LOST. Ships OFF in config.rs (non-negotiable #1);
                                 # ON here, which is what the goldens are blessed on and what the
                                 # mutation gate needs: `report_lane` is not `mutants::skip`ped, so a
                                 # branch the fixture never enters has unkillable mutants and reds the
                                 # gate. A display knob that ships off must still be ON in this file.
                                 #
                                 #   THE GAP IT CLOSES: every other instrument here is a PRECISION or a
                                 #   RECALL metric about names the lane did NOT own. `recall_capture`
                                 #   and `missed_winner_reasons` price what the ranking MISSED;
                                 #   `gate_audit` and `gate_sweep` price what the gates REJECTED.
                                 #   Nothing priced the held book's own losers — and that cohort is the
                                 #   ONLY one a new gate can act on, since a gate cannot reach a name the
                                 #   book never held. (#151), (#128), (#152), (#153) and (#156) were all
                                 #   spent proposing gates from the literature instead of from it.
                                 #
                                 #   WHY IT SPLITS BY FACTOR AND NOT BY GATE: a held name cleared every
                                 #   gate by construction, so `picks::gate_failures` is empty for all of
                                 #   them and a gate-attribution column would print nothing at all. The
                                 #   split is over the same `FUND_FACTORS` registry `sweep_fund_factor`
                                 #   searches — (#157) widened it to 24 — and prints each factor's median
                                 #   among held names that MADE money against those that LOST it.
                                 #   READ THE GAP, NOT THE LEVEL: a gate can only act on a factor whose
                                 #   two cohorts differ. Same sign and similar size = a gate on it would
                                 #   reject winners and losers together, which is the shape of every
                                 #   refusal above.
                                 #
                                 #   A LOSER IS `realized < 0` — money lost outright, not merely trailing
                                 #   peers. The peer-relative reading already exists as `relative`, and
                                 #   half a book sits below its own median by definition, which would
                                 #   make every factor look like it separated something.
                                 #
                                 #   A factor missing from the table filled only one side and was
                                 #   dropped; the printed count says how many. That is a fact about what
                                 #   the point-in-time pool COVERS, never a fact about the factor.
                                 #
                                 #
                                 #   WHAT IT FOUND, `backtest {12,8} universe fund pit`, 2026-08-29.
                                 #   LEAN ON THE 8y COLUMN: the loser cohort is n=22 there against n=7 at
                                 #   12y. On the best-covered factors the split is 120 winners / 22 losers
                                 #   at 8y and 58 / 7 at 12y — the growth lane buys an outright money-loser
                                 #   about one hold in seven, so a gate aimed at this cohort is aiming at a
                                 #   thin slice to begin with. 23 of 24 factors filled both sides at 8y,
                                 #   20 of 24 at 12y (gap = median winner - median loser):
                                 #                        gap 12y    gap 8y    reading
                                 #     peg_yield          +468.92   +204.46   winners far CHEAPER
                                 #     ebitda_yield        +38.86    +21.66   winners CHEAPER
                                 #     earnings_yield      +14.48     +8.27   winners CHEAPER
                                 #     gross_margin         -8.74    -10.11   losers score HIGHER
                                 #     eps_growth           -8.75     -6.46   losers score HIGHER
                                 #     roe / quality        -3.94     -6.61   losers score HIGHER
                                 #     composite            -7.57     -6.84   losers score HIGHER
                                 #     interest_cover      -63.96     -4.35   losers score HIGHER
                                 #     accrual_gap          +0.00     -0.08   DEAD
                                 #     margin_stability     -0.08     -0.25   DEAD
                                 #     op_margin            -0.44     +0.39   DEAD
                                 #     roic                 -5.79     -0.06   collapses at 8y
                                 #     rev_yoy              +4.40     -7.69   SIGN FLIPS
                                 #     net_margin           -1.59     +0.92   SIGN FLIPS
                                 #     fcf_margin           -5.35     +0.77   SIGN FLIPS
                                 #     buyback_yield        -2.73     +0.42   SIGN FLIPS — see (#156) below
                                 #
                                 #   THE ONLY FACTORS THAT SEPARATE ARE VALUE FACTORS, and they agree at
                                 #   both horizons and by a margin nothing else approaches: the held book's
                                 #   losers are the EXPENSIVE names. The lane already ships the strongest of
                                 #   the three as its tilt (`growth_fund_factor: peg_yield`). So the answer
                                 #   to "what trait do our losers share" is a trait the score ALREADY reads.
                                 #
                                 #   THE PROPOSED-GATE FAMILY SEPARATES BACKWARDS. Every quality/growth
                                 #   factor — gross_margin, eps_growth, roe, quality, composite,
                                 #   interest_cover — gives the LOSERS the higher median at BOTH horizons. A
                                 #   gate on any of them rejects the winners. That is a measured explanation
                                 #   for five consecutive refusals, not another refusal: the family the
                                 #   literature keeps recommending is anti-correlated with this book's
                                 #   losses.
                                 #
                                 #   IT ALSO EXPLAINS (#156) INDEPENDENTLY, and this is the cross-check that
                                 #   makes the instrument worth its lines. `growth_max_dilution_pct` rejects
                                 #   on `-buyback_yield` above a ceiling — that is, it rejects the DILUTERS.
                                 #   At 12y the losers' median buyback_yield is +2.16 against the winners'
                                 #   -0.57: the money-losing holds were the ones SHRINKING their share count,
                                 #   and the diluters were on the winning side. The gate's rejection set is
                                 #   anti-correlated with the cohort it was proposed to remove, which is why
                                 #   every rung of that ladder cost edge. (#156) was run before this
                                 #   instrument existed and refused it on the numbers; this says WHY.
                                 #
                                 #   CONSEQUENCE — THE TIGHTENING DIRECTION IS NOW MEASURED SHUT, the way
                                 #   (#127) closed selection. There is no unexploited separating factor in
                                 #   the registry: the ones that separate are value and already shipped, the
                                 #   quality family separates the wrong way, four are dead and four flip sign
                                 #   between horizons. Read together with (#157) (ten new factors, ship
                                 #   nothing) and (#156) (the one unswept gate, refused at every rung), a
                                 #   NEW GATE PROPOSED FROM THE LITERATURE NOW NEEDS TO EXPLAIN ITS GAP IN
                                 #   THIS TABLE FIRST. That is the bar this receipt sets.
                                 #   REVERT RULE: set false to restore the pre-(#159) report, and
                                 #   re-bless — the goldens gained this block additively and nothing
                                 #   else in the report moved.
                                 # ── (#165) MEASURED 2026-08-30 — THE SAME TABLE, RE-READ ON A UNIT-FREE RULER ──
                                 #   WHAT CHANGED: each row gained an `AUC` column beside the gap. AUC = P(a random winner
                                 #   scores above a random loser), ties counting a half, so it is rank-based and INVARIANT
                                 #   under any monotone transform of the factor. Same two runs as (#159) —
                                 #   `backtest {12,8} universe fund pit`, control on this file. The gap column reproduced
                                 #   (#159) to the decimal (peg_yield +468.92, gross_margin -8.74), so both receipts read
                                 #   the SAME dataset and the columns are directly comparable.
                                 #   12y: windows scored 2737, census 35 buckets / median gated pool 6 / h2h n=4 (VACUOUS).
                                 #    8y: windows scored 6126, census 43 buckets / median gated pool 7 / h2h n=8.
                                 #
                                 #   WHY IT WAS NEEDED: (#159) ranked 24 factors by a gap carried in each factor's OWN
                                 #   units. `peg_yield` is `earnings_yield · CAGR` and runs in the hundreds; `accrual_gap`
                                 #   is a ratio near 0; `margin_stability` is single digits. Ranking by that gap ranks
                                 #   partly by SCALE — and the three factors (#159) crowned are the three largest-unit
                                 #   factors in the registry. That is a confound in the RULER, not a finding about the book.
                                 #
                                 #   ranked by |AUC-0.5|; dir = does the direction agree across the two horizons:
                                 #                      12y AUC   8y AUC   dir    vs (#159)
                                 #     peg_yield          0.91     0.70    SAME   holds
                                 #     ebitda_yield       0.91     0.70    SAME   holds
                                 #     earnings_yield     0.89     0.70    SAME   holds
                                 #     rev_cagr            —       0.21     —     NEW — see 4
                                 #     eps_growth         0.15     0.35    SAME   holds (backwards)
                                 #     interest_cover     0.20     0.46    SAME   12y only; 8y is noise
                                 #     asset_growth        —       0.69     —     8y only, n=41/10
                                 #     gross_margin       0.26     0.34    SAME   holds (backwards)
                                 #     composite          0.31     0.32    SAME   holds (backwards)
                                 #     buyback_yield      0.30     0.52    FLIP   holds (flips) — (#156) story is 12y-only
                                 #     margin_trend       0.68     0.47    FLIP   does not replicate
                                 #     rev_yoy            0.54     0.37    FLIP   holds (flips)
                                 #     roe / quality      0.38     0.41    SAME   holds (backwards)
                                 #     net_cash_rev       0.60     0.60    SAME   NEW — see 5
                                 #     net_margin         0.40     0.48    SAME   weak
                                 #     eps_yoy            0.53     0.41    FLIP   weak
                                 #     accrual_gap        0.55     0.45    FLIP   NOT dead — see 2
                                 #     op_margin          0.45     0.47    SAME   DEAD stands
                                 #     roic               0.54     0.50    flat   NEVER separated — see 2
                                 #     margin_stability   0.47     0.46    SAME   DEAD stands
                                 #     fcf_margin         0.49     0.50    flat   never separated
                                 #     rev_accel           —       0.50     —     8y only, flat
                                 #
                                 #   1. THE CORE CLAIM SURVIVES; THE SUPERLATIVE DOES NOT. The three value factors are the
                                 #      top three at 12y and hold DIRECTION at both horizons — the only family that does so
                                 #      at that strength. "The held book's losers are the EXPENSIVE names" therefore stands,
                                 #      now measured on a ruler units cannot move. But (#159) also wrote "by a margin
                                 #      nothing else approaches", and at 8y that is FALSE: `rev_cagr` reads 0.21 (|d| 0.29)
                                 #      against value's 0.70 (|d| 0.20) — and (#159) itself says LEAN ON THE 8y COLUMN.
                                 #      "THE ONLY FACTORS THAT SEPARATE ARE VALUE FACTORS" is RETRACTED AS WRITTEN and
                                 #      replaced by: value is the only family that separates CONSISTENTLY. It is not the
                                 #      strongest separator at every horizon.
                                 #
                                 #   2. THREE VERDICTS WERE UNIT ARTIFACTS — and not one of them promotes a factor.
                                 #      `roic` was "collapses at 8y" on a gap going -5.79 -> -0.06. On AUC it goes
                                 #      0.54 -> 0.50: it NEVER separated, so there was no collapse to explain.
                                 #      `accrual_gap` was DEAD on +0.00/-0.08 — numbers small because its UNITS are small.
                                 #      On AUC it is 0.55/0.45: a real but tiny separation that FLIPS SIGN. Not dead, just
                                 #      inconsistent. Same refusal, honest reason.
                                 #      `fcf_margin` was SIGN FLIPS on -5.35/+0.77; AUC 0.49/0.50 says it does not separate
                                 #      at all, so that flip was median noise on a flat factor.
                                 #      `interest_cover`'s -63.96 was the third-largest gap at 12y and IS a real reverse
                                 #      separator there (0.20) — but 0.46 at 8y. The MAGNITUDE was scale; the direction
                                 #      was not.
                                 #
                                 #   3. TWO DEAD VERDICTS ARE GENUINELY DEAD. `margin_stability` (0.47/0.46) and `op_margin`
                                 #      (0.45/0.47) sit on the half at BOTH horizons. The premise that motivated this
                                 #      instrument — that DEAD might be a units artifact — was WRONG for these two.
                                 #      Recorded because a receipt that only confirms its own hypothesis is not a measurement.
                                 #
                                 #   4. THE BACKWARDS FAMILY IS CONFIRMED AND WIDENED. gross_margin, eps_growth, roe/quality
                                 #      and composite are all below the half at BOTH horizons, same direction: (#159)'s
                                 #      measured explanation for five consecutive refusals holds unchanged. `rev_cagr` at
                                 #      0.21 is the STRONGEST instance of it on the 8y column and (#159) never saw it — the
                                 #      money-losing holds are the FAST REVENUE GROWERS. A gate there would ask the growth
                                 #      lane to reject growth. It fills only at 8y (n=41/10), so it cannot be confirmed at
                                 #      both horizons and is NOT proposed — recorded as the first thing to re-read when
                                 #      pre-2018 fundamental coverage improves.
                                 #
                                 #   5. ONE NEW CANDIDATE CLEARS (#159)'s BAR: `net_cash_rev` — 0.60 at BOTH horizons, same
                                 #      direction, on the best-covered rows in the table (n=120/22 at 8y, n=58/7 at 12y).
                                 #      The winners carry more net cash per unit of revenue. It is the ONLY non-value factor
                                 #      that replicates in BOTH direction and magnitude across the two horizons. |d| 0.10 is
                                 #      modest — half of value's — so this is a CANDIDATE, not a result.
                                 #
                                 #   AN AUC DOES NOT AUTHORISE A GATE. It says a threshold is possible, never that one pays.
                                 #   Ship Rule v2 decides in its own round: PRIMARY at 12y AND 8y on `pit` per (#139), every
                                 #   20y GUARD live, the (#160) h2h off the 6-10 book with its not-worse clause. NOTHING HERE
                                 #   SHIPS A PICK CHANGE — no knob moved and the goldens gained only the new column.
                                 #
                                 #   THE BAR (#159) SET IS AMENDED, NOT LIFTED: a gate proposed from the literature still has
                                 #   to explain itself in this table first, but it now explains its AUC at BOTH horizons
                                 #   rather than its gap. A GAP ARGUMENT ALONE IS NO LONGER ADMISSIBLE — it can be bought
                                 #   with units.
                                 #   REVERT RULE: none — this receipt records a measurement, not a value. The column itself
                                 #   reverts with `print_held_loser_factors: false`, exactly as (#159) already says.
  journal_core_list: false       # (#103 / finding C9) UNMEASURED — an unfitted judgement value,
                                 # shipping OFF, which is what every line already sitting in a user's
                                 # `.screen_snapshots.jsonl` holds. There is no sweep for this one and
                                 # there never will be: it does not change a number, it starts a record.
                                 #
                                 #   THE GAP: `screen` prints TWO recommendations — the momentum-ranked
                                 #   book and the buy-and-hold CORE shortlist — and journals ONE. Only
                                 #   `ranked_now` reaches the snapshot file, so `track` has been accruing
                                 #   live out-of-sample evidence for the momentum half since round 114
                                 #   while the CORE half, the part of the report a reader is most likely
                                 #   to actually act on for twenty years, has accrued nothing at all.
                                 #
                                 #   WHY IT SHIPS OFF ANYWAY: it widens an append-only file that no
                                 #   golden covers and no test owns — a user's own record. Off, the
                                 #   emitted line is byte-identical (`skip_serializing_if` drops the key,
                                 #   pinned by `an_empty_core_list_leaves_the_journal_line_byte_identical`);
                                 #   old lines stay readable either way via serde default.
                                 #
                                 #   WHAT IT DOES NOT DO: grade. `track` still reads only `rows`. That is
                                 #   deliberate and is the whole shape of this item — nothing can grade a
                                 #   history that was never written, so the RECORDING is the urgent half
                                 #   and the grader is the patient one. The field carries the same
                                 #   `(ticker, close EUR)` shape as `rows` precisely so `grade` can be
                                 #   pointed at it later with no format migration.
                                 #
                                 #   TURN IT ON WHEN: you want the CORE half gradeable, and accept that
                                 #   the evidence starts from the day you flip it, not retroactively.
                                 #   Every month it stays off is a month of CORE record that cannot be
                                 #   recovered. REVERT: set false; existing widened lines still parse.
  hold_max_ter: 0.25             # (#102 / finding C9) MEASURED ONLY AS THE STATUS QUO — this is the
                                 # literal that shipped inside `core::hold_miss_reason`, extracted
                                 # verbatim, so the value carries whatever justification it always had:
                                 # 0.25 admits FTSE All-World (VWCE/VWRL, 0.22%), the canonical one-fund
                                 # hold, and below it sits S&P/World territory (0.03-0.20%). Naming it
                                 # changes nothing; the printed reject reason now formats the cap from
                                 # this field, so the message cannot quote a number the check did not use
                                 # (pinned: "TER 0.30% > 0.25% cap" in `pure_logic`).
                                 #
                                 #   THE HALF THIS DOES NOT FIX: the CORE leg reads `ter_shown()` (BF
                                 #   `expense_ratio` OR the Yahoo `ter_fallback`) while the score's TER
                                 #   damp reads `expense_ratio` alone. Two sources for one number, so the
                                 #   H flag and the score can disagree about the same fund's cost on the
                                 #   same row — the display side sees a TER the ranking does not. That is
                                 #   a real violation of the one-definition rule, and it is NOT closed
                                 #   here on purpose: unifying them feeds the Yahoo fallback into a
                                 #   SCORING input, which moves the rank and needs the stress grid. Left
                                 #   as a named debt rather than a silent one.
                                 #
                                 #   REVERT: 0.25 is the shipped value; any other number is a deliberate
                                 #   choice about which funds wear the H flag, display-only either way.
  hold_ucits_or_domicile: false  # (#102 / finding C9) UNMEASURED — an unfitted judgement value, shipping
                                 # OFF, which is name-token-only, today's behaviour.
                                 #
                                 #   THE DEFECT: the CORE admission rule proves UCITS-ness by requiring
                                 #   the literal token "ucits" in the fund NAME. Feed names do not
                                 #   reliably carry it — "ISHARES III PLC ISHRS CORE MSCI" is an Irish
                                 #   UCITS fund by every fact on its row and fails this leg on spelling.
                                 #   A naming accident silently deletes a legitimate core, and because
                                 #   `hold_core_list` caps the table at 3 per tier, the deletion is not
                                 #   even visible as a shorter list — a worse fund refills the slot.
                                 #
                                 #   WHAT ON DOES: the leg passes on the name token OR a `domicile`
                                 #   prefix outside `core::NON_EU` — the same blocklist `firds_etf_isins`
                                 #   already screens the FIRDS dumps with, hoisted to a module const so
                                 #   the two readers cannot drift into two lists. Names WITH the token
                                 #   are untouched. `domicile: None` (watchlist-only rows) still falls
                                 #   back to the name, so this cannot turn missing data into a PASS —
                                 #   the house rule cuts the other way here and the knob respects it.
                                 #
                                 #   WHY UNMEASURED: display-only. `hold_suitable` drives the H flag and
                                 #   the CORE shortlist; neither is on the graded path, so no edge, rho
                                 #   or h2h number can move at any setting. The sweep that WOULD settle
                                 #   it is not a backtest at all: run `screen etfs` with the knob on and
                                 #   off and diff the CORE table — the honest question is how many
                                 #   admitted funds are EU-domiciled with no "ucits" token in the name,
                                 #   and whether any of them is genuinely not a UCITS wrapper. That needs
                                 #   a live BF-enriched fund universe, which is why it is not run here.
                                 #
                                 #   REVERT: set false. Nothing else reads the flag.
  hold_name_tokens: false        # (#102 / finding C9) UNMEASURED — an unfitted judgement value, shipping
                                 # OFF, which is bare `contains`, today's behaviour.
                                 #
                                 #   THE DEFECT: `core::geo_tier` decides both "is this a core?" and
                                 #   "which breadth tier?" by scanning the name for substrings, and the
                                 #   NARROW disqualifier list holds short generic English words —
                                 #   "value", "select", "small", "esg", "health", "quality", "paris".
                                 #   Any of them occurring INSIDE a longer word disqualifies the fund:
                                 #   the classic "N-etf-lix" substring bug, here with the power to delete
                                 #   a legitimate one-fund-forever hold from the shortlist silently.
                                 #   Two of the thirty tokens (" pab", "sri ") already carry a hand-rolled
                                 #   space guard, which is the previous author noticing this exact class
                                 #   of bug and fixing it one token at a time.
                                 #
                                 #   WHAT ON DOES: a token must sit at a WORD START to count. Word-START
                                 #   and deliberately not whole-word: half the list is an intentional
                                 #   PREFIX ("technolog" must catch Technology and Technologies, "financ"
                                 #   Financial and Finance, likewise "communicat"/"sustainab"/
                                 #   "semiconduct"), and whole-word matching would un-disqualify exactly
                                 #   the sector funds the list exists to bar. Tokens that already begin
                                 #   with their own separator pass through to plain `contains` untouched.
                                 #   Applies to the GEO table too, on the same call.
                                 #
                                 #   STRICTLY A TIGHTENING: every match it removes was a mid-word
                                 #   accident, so ON can only ADMIT names, never reject new ones. That is
                                 #   also why it can be flipped without a golden re-bless in any pool
                                 #   whose names carry no such accident — the goldens hold it off anyway.
                                 #
                                 #   WHY UNMEASURED: same reason as `hold_ucits_or_domicile` — display
                                 #   only, off the graded path. The settling sweep is again a diff of the
                                 #   `screen etfs` CORE table with the knob on and off, over a live
                                 #   fund universe. The matcher itself is pinned by fixtures in
                                 #   `pure_logic` (prefix tokens still bite; "equality"/"comparison"/
                                 #   "devalued" no longer do). REVERT: set false.
  growth_sector_cap: 0           # (#101 / finding C7) UNMEASURED — an unfitted judgement value, shipping
                                 # 0 (off), which is today's unconstrained table. It is a RISK
                                 # constraint, and the honest thing to say up front is that the
                                 # backtest CANNOT grade it: see below.
                                 #
                                 #   THE GAP: nothing in this tool limits SECTOR concentration in the
                                 #   printed book. `growth_corr_cap` ships 0.0 (off); (#126) turned
                                 #   `growth_value_floor_pct` on at 40.0, but that brake cuts by
                                 #   price-for-growth, not by sector, and a cheap semiconductor name
                                 #   clears it as easily as a dear one — so the gap below is unchanged
                                 #   and only the "ONLY surviving trim" wording needed fixing. The
                                 #   other surviving trim in `lane_split` is the ETF PEG ceiling. The stock table can
                                 #   legitimately be twenty semiconductor names, and the printed "sector
                                 #   mix" line is an OBSERVATION, not a constraint. Combined with
                                 #   `config::Sizing`'s risk budget — the one documented exception to the
                                 #   defaults-are-current rule — there is nothing between the score and
                                 #   the position size that limits concentration either.
                                 #
                                 #   WHAT > 0 DOES: keeps at most N stocks per GICS sector, the
                                 #   highest-scoring ones (the list arrives rank-ordered). Post-rank
                                 #   DISPLAY trim at the same seam as the redundancy skip and the value
                                 #   brake — after the score/sector cut, before the table is cut to `n`,
                                 #   so a dropped row REFILLS from below and the table does not shorten.
                                 #   Pinned tickers bypass. A name with no sector is KEPT: unjudgeable is
                                 #   not a verdict, and that is also what makes the trim inert in any pool
                                 #   where `sector` is unfilled.
                                 #
                                 #   WHY NO SWEEP CAN SETTLE IT. This is display-only and the backtest
                                 #   scores SAMPLES, not tables — `lane_split` is never on the graded
                                 #   path, so no edge, rho or h2h number moves at any value. Worse, the
                                 #   metric a concentration cap exists to improve (drawdown correlation
                                 #   across the held book) is not what the ship rule measures: the graded
                                 #   quantity is per-name peer-relative return, which is de-meaned WITHIN
                                 #   a bucket and therefore blind to whether the book's names move
                                 #   together. Grading this needs a portfolio-level measurement the tool
                                 #   does not have.
                                 #
                                 #   SO THE RULE FOR THIS KNOB IS DIFFERENT: it ships off because it is
                                 #   unmeasured, and if it is ever turned on that will be a DELIBERATE
                                 #   RISK CHOICE recorded as such — never a fitted value, and never
                                 #   justified by an edge number, because no edge number can move.
                                 #   3 is the value to try first if someone makes that choice; it is a
                                 #   judgement, not a measurement, and must not acquire a receipt that
                                 #   implies otherwise.
  growth_gate_on_tr_cagr: false  # (#99) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                                 # the price-only bar every golden and every gate receipt in this file
                                 # was measured under. Read with `growth_min_cagr` and `use_adjusted_close`.
                                 #
                                 #   THE DEFECT: closes are price-only (`use_adjusted_close: false`), so
                                 #   `long_cagr` is roughly the total-return CAGR MINUS the dividend yield.
                                 #   A name compounding at 19%/yr total while paying 3% measures ~15.5%/yr
                                 #   and is REMOVED FROM THE UNIVERSE by `growth_min_cagr: 19.0`. No
                                 #   `dividend_weight` can recover it: the reward is applied after the
                                 #   gate, to names that already survived it. Over a 20-year hold the
                                 #   reinvested yield the ranking never sees is 1.03^20 = 1.81x of
                                 #   terminal value.
                                 #
                                 #   SECOND-ORDER, same direction: ex-div drops depress the price series,
                                 #   so a payer's `range_pct` is pushed down against `growth_min_range_pct:
                                 #   80`. THIRD-ORDER and opposite in sign: understating `long_cagr` by the
                                 #   yield INFLATES `accel` (1Y − β·long_cagr) for the payers that do clear
                                 #   the gate. This knob addresses only the first.
                                 #
                                 #   WHAT true DOES: `picks::life_leg_cagr` adds `tr_cagr − life_cagr` —
                                 #   the dividend contribution in CAGR points over the same endpoints — to
                                 #   whatever leg it already returned. Deliberately an UPLIFT and not a
                                 #   switch to `tr_cagr` itself: switching would ALSO discard the
                                 #   `life_cagr_max_years` window that `capped_cagr` selected, and the knob
                                 #   would be two changes wearing one name. Approximation, stated rather
                                 #   than hidden: the uplift is measured whole-life and applied to a
                                 #   possibly-capped window, so it is wrong by whatever the yield did
                                 #   across the cut. LOWER BOUND either way — `tr_cagr` adds payouts
                                 #   without reinvesting them.
                                 #
                                 #   IT IS NOW SIGHTED, which it was not before (#99). The arithmetic lived
                                 #   inline in `fetch.rs`, so `tr_cagr` was live-only and this gate could
                                 #   never be A/B-ed. `core::tr_life_cagr` is now the one definition and
                                 #   `core::backtest_quote` fills the field from the same `[..=as_of]`
                                 #   slice and the same as-of dividends `div_eur` reads -> train == serve.
                                 #
                                 #   IT LOOSENS, SO BEFORE/AFTER ARE TWO UNIVERSES. Every payer's measured
                                 #   CAGR rises, so the pool grows and the edge numbers on either side are
                                 #   not an A/B on one sample. Read it the way the splice receipt reads its
                                 #   own coverage change, not as a tuning delta.
                                 #
                                 #   TO SETTLE: the ship-rule grid at 20y/12y/8y, plus a `growth_min_cagr`
                                 #   re-sweep underneath it — a bar fitted against price-only CAGRs is by
                                 #   construction too low once every CAGR rises. Judge the PAIR.
                                 #
                                 #   NOT THE SAME AS `use_adjusted_close`. That knob's recorded "no effect"
                                 #   verdict was measured with the BENCHMARK still price-only, i.e. on a
                                 #   broken instrument, and flipping it alone points a total-return book at
                                 #   a price-only ^GSPC and OVER-corrects. If that route is taken instead,
                                 #   the benchmark must move to ^SP500TR in the same change.
                                 #
                                 #   REVERT RULE: back to false unless the pair (knob + refitted
                                 #   `growth_min_cagr`) clears PRIMARY at 20y AND 8y with rank-1 h2h >=50%
                                 #   at all three horizons.
  backtest_calendar_cadence: false # (#117) MEASURED 2026-08-22 AND REFUSED. Held at false, which is
                                 #   the fixed 12/36/6 bar-count triple (and the daily path's
                                 #   252/750/126) that every golden in tests/fixture and every fitted
                                 #   receipt in this file was measured under. The defect below is REAL
                                 #   and the fix for it LOSES at all three horizons — see the verdict.
                                 #
                                 #   THE DEFECT: `backtest::run` chooses `cadence`, `min_history` and
                                 #   `step` ONCE per run and then counts them in BARS against each series
                                 #   in turn. The three constants only mean what their names claim — 12
                                 #   bars a year, 3 years of history before a cutoff, 6 months between
                                 #   cutoffs — if every series really is monthly. The long path ASKS for
                                 #   monthly (`fetch::chart_json_long` rewrites `interval=1d` to
                                 #   `interval=1mo`), Yahoo answers with whatever it likes for a thin
                                 #   line, and nothing in this repo reads `meta.dataGranularity` back.
                                 #   Census of the live long cache:
                                 #       1mo 1818 | 1wk 2059 | 1d 1155 | 1h 317 | 3mo 304 | 1y 172
                                 #   — 31% monthly. The fixture carries the same mix (58 of 200), which
                                 #   is why no golden has ever seen this.
                                 #
                                 #   WHAT IT COSTS TODAY. `step` is the sharp end: 6 bars on a daily
                                 #   series is 6 SESSIONS, so that name emits a cutoff every week and a
                                 #   half and enters the graded sample with ~40x the weight of a monthly
                                 #   one — purely from which granularity Yahoo happened to serve. Same
                                 #   arithmetic on `min_history: 36`: a daily row clears the "3 years of
                                 #   history" floor after 36 sessions, i.e. seven weeks. `cadence` is the
                                 #   other end: it annualises `volatility_pct`, and that field is read by
                                 #   ABSOLUTE thresholds (`sharpe_cap: 15.0`, `sharpe_cap_etf: 9.0`), so
                                 #   a daily row's vol is understated ~sqrt(252/12) = 4.6x against caps
                                 #   fitted on monthly rows. That units half is `vol_daily_equivalent`
                                 #   (#97), directly below — it fixes the SCALE of the bars, this knob
                                 #   fixes WHICH bars. They are independent and neither implies the other.
                                 #
                                 #   WHAT true DOES: `commands::backtest::walk_params` derives bars/year
                                 #   from the series' own date array — (n-1) * 365.25 / span_days, a SPAN
                                 #   and a COUNT rather than a median gap, so a record Yahoo spliced
                                 #   across granularities averages instead of snapping to whichever half
                                 #   is longer — and restates the triple as 1 year, 3 years and 6 months
                                 #   of THAT series' bars. NEAR-INERT WHERE THE DATA WAS ALREADY RIGHT,
                                 #   which is the property that makes it safe to reason about: a real monthly
                                 #   series measures 12.00 bars/yr and resolves to exactly 12/36/6, and a
                                 #   real daily one measures 252.0 and resolves to 252/756/126 against
                                 #   today's 252/750/126. Only the rows the constants were lying about
                                 #   move. Degenerate records (empty, single-date, zero-span, or any
                                 #   resolution that would round `step` to 0 and hang the walk) fall back
                                 #   to the run-wide triple, unit-tested by the `walk_params_refuses`
                                 #   case in `commands::backtest::tests`.
                                 #
                                 #   THE GRID, 2026-08-22. Six runs, `backtest {20,12,8} universe`, both
                                 #   arms from the SAME path (`probe117/arm.yaml`, one dir below the repo
                                 #   root so `config::anchored_path` keeps the gate's data dir) with only
                                 #   this boolean edited between them. The OFF arm reproduces the gate
                                 #   run bar for bar at 8y (edge +48.8, null lift +60.9, OOS +0.22/+0.15),
                                 #   which is the control: the probe path reads the same dataset.
                                 #     horizon   arm    GROWTH edge     top-3 excess   rank-1 h2h   worst
                                 #       20y     off    +780.8          +6.3 | +5.6    67% (12/18)   -0.5
                                 #       20y     ON      +37.6          +5.9 | +5.7    50% (17/34)   -3.9
                                 #       12y     off    +193.5          +6.8 | +6.9    58% (15/26)   -7.8
                                 #       12y     ON      +69.3          +6.3 | +6.7    48% (21/44)   -8.9
                                 #        8y     off     +48.8          +7.5 | +8.3    62% (21/34)   -9.2
                                 #        8y     ON       -4.0          +7.4 | +7.8    52% (27/52)  -10.0
                                 #   Null-model lift falls with it (+663.0 -> +353.4, +187.2 -> +114.7,
                                 #   +60.9 -> +43.7) and both OOS halves degrade at every horizon (20y
                                 #   +0.25/+0.23 -> -0.06/+0.08, 12y +0.19/+0.16 -> +0.04/+0.08, 8y
                                 #   +0.22/+0.15 -> +0.04/+0.10). At 8y the ON arm's edge band is
                                 #   [-33.6 … +21.2] — it straddles 0, i.e. the lane stops being real.
                                 #
                                 #   VERDICT: REFUSED, and not on a tie. Both GUARDs break — rank-1 h2h
                                 #   is 48% at 12y (the bar is 50%), and the top-3 worst window is worse
                                 #   at all three horizons. A broken guard refuses outright and is never
                                 #   softened into UNRESOLVED. PRIMARY fails too: top-3 excess is down on
                                 #   the mean at 20y AND down on both mean and median at 8y.
                                 #
                                 #   WHY IT LOSES, AND THE PART THAT WAS PREDICTED WRONG. The expectation
                                 #   written here on landing was that cutoff counts would FALL — daily
                                 #   rows losing their 40x weight. They ROSE: 4541 -> 11502 at 20y, 11835
                                 #   -> 21317 at 12y, 20800 -> 31010 at 8y, and growth-scored windows 337
                                 #   -> 1012 / 514 -> 1223 / 695 -> 1410. The dominant term is not the
                                 #   daily rows at all, it is the COARSE ones. `min_history: 36` bars is
                                 #   nine years of a quarterly series and thirty-six years of a yearly
                                 #   one, so the fixed floor was silently excluding almost every `3mo`
                                 #   and `1y` line in the cache (304 and 172 of them). Resolving per
                                 #   series drops those floors to 12 bars and 3 bars — three calendar
                                 #   years, as intended — and admits the whole cohort. Yahoo serves
                                 #   yearly bars for the lines it has least data on, so that cohort is
                                 #   the thin and obscure end of the universe, and injecting it triples
                                 #   the graded sample while collapsing the edge. Same species as the
                                 #   (#116) venue swap: the number that moved was the SAMPLE, not the
                                 #   score.
                                 #
                                 #   SO THE DEFECT IS STILL REAL AND THIS IS STILL NOT THE FIX. A daily
                                 #   row does contribute ~40x the cutoffs of a monthly one, and that is
                                 #   untouched at false. What the grid proves is that the 36-bar floor
                                 #   was doing a second, undocumented job — a granularity filter — and
                                 #   that removing it costs more than the weighting bug costs. Anything
                                 #   proposed here next has to keep that filter explicitly (a floor on
                                 #   bars/year, or on absolute bar count, ALONGSIDE the calendar
                                 #   restatement) and be graded on the same six runs. Do not re-propose
                                 #   the bare calendar restatement; it is measured and it loses.
                                 #
                                 #   REVERT RULE: stays false. It only moves if a variant that keeps a
                                 #   granularity floor clears SHIP RULE v2 outright — PRIMARY at 20y AND
                                 #   8y, rank-1 h2h >= 50% at 20y/12y/8y, top-3 worst window not worse.
                                 #   Conditions note for anyone re-running: the two arms saw 5043 and
                                 #   5044 tickers (one name resolved between them), which is cache drift
                                 #   and not a treatment; every effect above is orders of magnitude
                                 #   larger than one ticker in five thousand.
  vol_daily_equivalent: false    # (#97) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                                 # the raw per-bar scale every vol-reading receipt in this file was
                                 # fitted at. This is the one knob here that INVALIDATES OTHER RECEIPTS
                                 # when turned on, so read the whole block before touching it.
                                 #
                                 #   THE DEFECT: a stdev of per-bar returns scales with the square root
                                 #   of the bar length. `volatility_pct` is the stdev of `cadence` bars,
                                 #   so the SAME asset prints ~√21 = 4.58× more volatility on the monthly
                                 #   bars a long run walks than on the daily bars the screen walks. All
                                 #   three horizons the ship rule cites are monthly (`long || years >= 8`
                                 #   in `backtest::run`), so 20y, 12y and 8y are all at the wrong scale.
                                 #
                                 #   WHY THE OLD DEFENCE IS NOT ENOUGH. `backtest_quote`'s comment says
                                 #   monthly bars only APPROXIMATE the daily vol and that is fine because
                                 #   "a backtest run is single-cadence so the cross-sectional ranks stay
                                 #   consistent". The ranks do hold. But EVERY consumer of this field is
                                 #   an ABSOLUTE THRESHOLD, and a threshold does not care about ranks:
                                 #     sharpe_cap: 15 / sharpe_cap_etf: 9 — clamp `long_cagr / vol`. A
                                 #       4.58× larger denominator makes the ratio 4.58× smaller, so the
                                 #       cap barely binds in the run that fitted it while live it binds
                                 #       for nearly every name past the CAGR gate. Worked example pinned
                                 #       in `core.rs`: 15%/yr CAGR on a 6%-per-monthly-bar name reads
                                 #       sharpe 2.5 raw and 11.5 restated. The backtest measured a term
                                 #       that is very nearly a CONSTANT live (see C8).
                                 #     growth_max_vol / growth_max_vol_crypto — absolute % ceilings, so
                                 #       the monthly run rejects on a bar length the live gate never sees.
                                 #     normal_volatility_pct — the [FOIL] dip divisor. The knob is NOT
                                 #       touched here (non-negotiable 6); only what it divides moves.
                                 #
                                 #   WHAT true DOES: `core::daily_equivalent` multiplies the field by
                                 #   √(cadence/252) — and its `downside_dev_pct` twin with it, since
                                 #   leaving one of the pair in bar units would make the (r39) Sortino
                                 #   probe's ratio a pure cadence artefact. It is the AMPLITUDE half of
                                 #   the rule `span_to_bars` already enforces for every WINDOW: same
                                 #   calendar meaning on either cadence, train == serve.
                                 #
                                 #   LIVE IS BIT-IDENTICAL AT EITHER SETTING. At cadence 252 the factor
                                 #   is exactly 1.0 and `v * 1.0` is exactly `v` in IEEE — asserted, not
                                 #   assumed. Only a non-daily run can move, which is why this ships off
                                 #   rather than on despite being the correct units.
                                 #
                                 #   TURNING IT ON RETIRES FITTED VALUES. sharpe_cap, sharpe_cap_etf and
                                 #   growth_max_vol_crypto were all fitted against the raw monthly scale;
                                 #   under this knob their numbers describe a quantity that no longer
                                 #   exists. It is NOT a knob to flip and re-read the edge — the honest
                                 #   sequence is: turn it on, re-sweep those three, then judge the pair
                                 #   (knob + refitted caps) against the ship rule as ONE change.
                                 #
                                 #   TO SETTLE: the ship-rule grid at 20y/12y/8y with the three caps
                                 #   re-swept underneath it. A daily-cadence control run is the clean
                                 #   cross-check — at cadence 252 both arms must print identical numbers,
                                 #   and any difference is a bug in this knob rather than a result.
                                 #
                                 #   REVERT RULE: stays OFF until that paired sweep clears the ship rule.
                                 #   A result read WITHOUT re-sweeping the caps does not count either way
                                 #   — it measures the caps falling out of range, not the units fix.
                                 #
                                 #   (#176) GRADED 2026-08-31 on `backtest {20,12,8} universe fund pit`, plus a
                                 #   daily-lane bug check at 5y. VERDICT: STAYS OFF — and NOT because a number
                                 #   refused it. The knob is UNGRADEABLE under the ship rule, and (#175) has just
                                 #   closed the only route to grading it.
                                 #
                                 #   1. THE BUG CHECK PASSES, END TO END. This block asked for "a daily-cadence
                                 #   control run — at cadence 252 both arms must print identical numbers".
                                 #   `backtest 5 …` walks daily (`monthly = long || years >= 8` in `backtest::run`),
                                 #   and off-vs-on there is BYTE-IDENTICAL: md5 cbf1362b9cae15f63e19237c46c98372
                                 #   for both, the sole difference being the drift WARNING that names the overlay.
                                 #   `core.rs` already pinned the FUNCTION (`assert_eq!(daily_equivalent(v, 252,
                                 #   true), v)`); this pins the whole PATH through it. So LIVE IS UNAFFECTED BY
                                 #   THIS KNOB — now measured end to end, not only asserted.
                                 #
                                 #   2. ON THE GRADED LANE IT IS VACUOUS AT ALL THREE HORIZONS. Every ship-rule
                                 #   statistic is IDENTICAL off vs on, to the printed decimal:
                                 #     20y  top-10 +0.6 (med +0.0)  worst -10.0  h2h 4/8 50% (6-10), 1/2 50%
                                 #          (11-20)   census 21 buckets, median gated pool 5, 19 saturated
                                 #     12y  top-10 +3.7 (med +3.9)  worst  -6.4  census 35, pool 6, 31 saturated
                                 #     8y   top-10 +5.3 (med +5.6)  worst  -5.5  h2h 19/27 70% (6-10), 5/8 62%
                                 #          (11-20)   census 43, pool 7, 35 saturated
                                 #   PRIMARY, every GUARD, the worst window and BOTH h2h books move by not one
                                 #   tenth. Per (#149) that is recorded VACUOUS, NEVER "held": this is a RANKING
                                 #   knob and on the median bucket the gated pool IS the basket, so an equal-weight
                                 #   top-10 cannot see a reorder.
                                 #
                                 #   IT IS NOT A NO-OP — the reorder is real, it just cannot reach the graded
                                 #   basket. The THIN slices are proper subsets, hence NOT set-invariant, and they
                                 #   DO move: top-3 at 20y -0.3 -> -0.2; at 8y top-2 +6.1 -> +5.9 and top-3 +6.3 ->
                                 #   +6.4; top-2/top-3 likewise at 12y. Same run, same names, a visible reorder
                                 #   above rank 10 and exactly zero movement AT rank 10 — (#149)'s vacuity argument
                                 #   caught in the act, and the cleanest demonstration of it in this file.
                                 #
                                 #   3. WHY IT CANNOT BE GRADED — the durable finding. The revert rule above
                                 #   demands the three fitted caps be re-swept and the pair judged as ONE change.
                                 #   All three are unmeasurable on this lane:
                                 #     · sharpe_cap / sharpe_cap_etf — read in `picks::score_parts` as
                                 #       `(long_cagr / v).clamp(0.0, cap)`: a SCORE clamp, never a gate. Pure
                                 #       ranking -> VACUOUS on every column above.
                                 #     · growth_max_vol_crypto — crypto-only, and `universe fund pit` is an
                                 #       S&P-500-member pond with no coins in it. Inert.
                                 #     · growth_max_vol (P4b) ships 0.0 = off, so the knob's ONLY pool-changing
                                 #       consumer is not live either. Neither shows in the GATE SWEEP, which
                                 #       prints live gates only.
                                 #   So the sweep the revert rule requires is a RANKING sweep on a SATURATED
                                 #   column. The escape would be to de-saturate first — and (#175) has just priced
                                 #   that: `growth_min_cagr` 19 -> 12 is the SMALLEST rung that lifts the pool over
                                 #   the basket, and it REFUSED on the (#160) guard at 12y AND 8y and on PRIMARY at
                                 #   both. The column cannot be de-saturated at a price the ship rule will pay.
                                 #
                                 #   STRUCTURAL BLOCK, NOT AN UNRUN EXPERIMENT. Do not re-open by running the cap
                                 #   sweep — it would reproduce the VACUOUS reading this entry already records.
                                 #   RE-OPEN ONLY on a NON-VACUOUS column (median gated pool > VERDICT_TOP at 12y
                                 #   and 8y) reached by some route (#175) did not refuse. Measurement is also now
                                 #   consistent with this block's own C8 prediction: with the knob on, vol falls
                                 #   4.58x, `long_cagr/vol` rises 4.58x and the clamp pins nearly every name, so
                                 #   the sharpe term goes very nearly CONSTANT — the GROWTH lane headline is
                                 #   unchanged to the decimal (rho +0.13, edge +182.3, halves +137.7 / -44.6).
                                 #
                                 #   REVERT RULE UNCHANGED: stays OFF. The paired sweep has not cleared the ship
                                 #   rule and presently CANNOT. What this entry adds: a future attempt owes a
                                 #   non-vacuous column FIRST — a cap sweep run on today's lane is not evidence
                                 #   either way.
                                 #
                                 #   P4b `growth_max_vol` was blocked "on (#97)". It is now blocked on (#97) being
                                 #   structurally ungradeable rather than merely unrun. Its own receipt also
                                 #   demands a JOINT run with P4a `growth_max_daily_1m`, permanently blocked for
                                 #   want of a daily lane at any ship-rule horizon. Both halves of that pair are
                                 #   closed.
                                 #
                                 #   4. MEASUREMENT HYGIENE — found while doing (1), and worth more than the knob.
                                 #   The FIRST 5y control run printed `consistency_5y edge -231.2`; two later runs
                                 #   of the BYTE-IDENTICAL config both printed -237.2, as did the knob arm. That
                                 #   6.0-pt "difference" was THE CONTROL DISAGREEING WITH ITSELF. Cause is already
                                 #   documented at the fetch/walk split in `backtest.rs`: completion order "turns
                                 #   on fetch latency alone … neither is pinned", the following `sort_by_key(date)`
                                 #   is STABLE and inherits it, and a probe's `edge` splits top-half vs bottom-half
                                 #   via a STABLE sort over a TIE-HEAVY factor (`roll5y_pos_pct`, n=23). Rank
                                 #   statistics are tie-order-invariant, which is why rho and both OOS rhos held
                                 #   while ONLY `edge` moved — that is the signature to look for. RULE: a small-n
                                 #   probe `edge` is NOT reproducible run-to-run on a cache-served run; before
                                 #   attributing one to a knob, RUN THE CONTROL TWICE. The verdict block and the
                                 #   POOL CENSUS were stable across every run here — this bites the diagnostics,
                                 #   not the ship rule. Same cause, same day: two runs disagreed on the ETF pond
                                 #   (2022 vs 2032 regulatory extras), which is why the 12y BUY-lane rows differ
                                 #   between the arms and are NOT read here.
  print_edge_annualized: false   # (#96) UNMEASURED in the sense that no run has been read with it on —
                                 # but unlike its neighbours this one CANNOT change a decision, because
                                 # nothing scored reads it. Shipping OFF keeps the goldens byte-identical.
                                 #
                                 #   THE DEFECT: `edge` and `top-N excess` are both printed as "pts" and
                                 #   are not the same unit. `edge` is a spread of CUMULATIVE returns over
                                 #   the run's whole hold — `realized` is `close[i+hold]/close[i] − 1`,
                                 #   never divided by anything — while `excess` is per YEAR. At the 20y
                                 #   run that is a ~20× gap between two numbers printed in the same
                                 #   report under the same name, which a reader compares by eye.
                                 #
                                 #   WHAT true DOES: appends `[= X pts/yr over the Ny hold]` to the lane's
                                 #   headline. The restatement annualises each HALF and subtracts, because
                                 #   annualising the spread is a different (wrong) number — pinned by
                                 #   `a_cumulative_edge_and_a_per_year_excess_are_not_the_same_number`,
                                 #   which reads +8.68 pts/yr where annualising the spread would say +11.7.
                                 #
                                 #   TO SETTLE: nothing to sweep. Turn it on when reading a report by hand.
                                 #   REVERT RULE: none needed — a print-only knob has no result to protect.
  cost_per_rebalance: false      # (#96) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                                 # the single-charge arithmetic every golden and every fitted receipt in
                                 # this file was measured under. Read with `print_edge_annualized` above:
                                 # both are the same units bug, one in the label and one in the number.
                                 #
                                 #   THE DEFECT: `turnover_frac` is a PER-REBALANCE quantity — 1 − mean
                                 #   Jaccard of the held set between CONSECUTIVE ~6mo buckets — but the
                                 #   charge `turn × 20bps` was applied ONCE against an edge spanning the
                                 #   whole hold. A book re-formed every six months for twenty years paid
                                 #   for one round trip. The consequence is not just a small number: the
                                 #   "NET ≤ 0: too churny to trade" flag CANNOT fire on a long run at any
                                 #   churn level, because 20bps against a twenty-year cumulative spread is
                                 #   noise by construction. The guard is decorative at the horizons that
                                 #   ship.
                                 #
                                 #   WHAT true DOES: charges `2 × hold_years` round trips (~6mo buckets, so
                                 #   two a year), and prints the multiplier so the line's arithmetic
                                 #   reconciles. The HOLD-PERIOD SWEEP charges per ROW — a 1y row pays 2,
                                 #   a 10y row pays 20 — which is the point of that table: the cheap hold
                                 #   was previously getting the same one-trip discount as the expensive one,
                                 #   so "if they're flat, the longest (cheapest) wins" was advice the
                                 #   arithmetic underneath could not support.
                                 #
                                 #   EXPECTED TO LOOK WORSE. At 20y the charge grows 40×, and a lane whose
                                 #   net edge survives that is a different claim from one that survives
                                 #   0.15pt. A `net` that turns negative when this is ON is the FINDING,
                                 #   not a regression — it means the shipped number was flattered.
                                 #
                                 #   TO SETTLE: it is not a tuning sweep. Run the lane report at 20y/12y/8y
                                 #   with it on and read whether `net` still clears 0 and whether the churn
                                 #   flag fires. If it fires, the hold-period sweep is the follow-up: the
                                 #   answer is a longer hold or a smaller book, not a smaller charge.
                                 #
                                 #   REVERT RULE: this knob has NO edge-improving arm and must never be
                                 #   judged by one. It goes ON when someone accepts the honest cost, and OFF
                                 #   only to reproduce a pre-(#96) receipt. Never turned off because the
                                 #   number got worse.
                                 #
                                 #   (#177) READ 2026-08-31 on `backtest {20,12,8} universe fund pit` with this knob
                                 #   and `print_edge_annualized` both ON. NOT A SHIP — a VALIDITY READ, which is
                                 #   exactly the "TO SETTLE" above. Both knobs stay OFF.
                                 #
                                 #   HEADLINE: THE EDGE SURVIVES THE HONEST CHARGE AT EVERY HORIZON, and the churn
                                 #   flag fires NOWHERE. `net` clears 0 on both lanes at 20y, 12y and 8y. The
                                 #   multiplier reconciles as promised — 2 × hold_years, printed as "× 40 / × 24 /
                                 #   × 16 rebalances".
                                 #
                                 #     GROWTH lane (`growth_score` — the lane `screen` ranks on)
                                 #       horizon   gross     net OFF    net ON    charge   charge as % of gross
                                 #       20y        +15.7     +15.5      +9.0      6.7      43%
                                 #       12y       +182.3    +182.1    +178.5      3.8       2.1%
                                 #        8y        +87.4     +87.3     +85.0      2.4       2.7%
                                 #     BUY lane
                                 #       20y       +337.5    +337.3    +330.5      7.0       2.1%
                                 #       12y        +12.8     +12.6      +8.9      3.9      30%
                                 #        8y        +18.1     +17.9     +15.6      2.5      14%
                                 #
                                 #   SO THE SHIPPED NUMBERS WERE FLATTERED, BUT NOT FATALLY. Under the single charge
                                 #   every `net` sat within 0.2pt of its gross edge — that is the precise sense in
                                 #   which the old line carried no information. Corrected, two cells are materially
                                 #   thinner: GROWTH at 20y gives up 43% of its gross edge, and BUY at 12y gives up
                                 #   30%. Those are the two to watch; everywhere else the charge is ~2-3%.
                                 #
                                 #   THE CHURN GUARD IS NO LONGER DECORATIVE. This block says the flag "CANNOT fire
                                 #   on a long run at any churn level". That was right under the single charge: at
                                 #   20y it needed the gross edge to fall to 0.2pt. Under the corrected charge the
                                 #   20y bar is 6.7pt against a +15.7 gross edge — a factor of 2.3, not a factor of
                                 #   80. The guard is LIVE-BUT-NOT-TRIGGERED, which is a different statement from
                                 #   decorative, and a future gate that thins the 20y growth edge could trip it. No
                                 #   hold-period sweep is owed: this block makes that follow-up conditional on the
                                 #   flag firing, and it did not fire.
                                 #
                                 #   THE VERDICT IS UNTOUCHED, as designed. These knobs move only the printed lane
                                 #   `net`; the top-N book, the POOL CENSUS and every ship-rule statistic are
                                 #   unchanged. The single verdict-block digit that differs between control and arm
                                 #   (`abs_mom_5y` OOS -0.09 -> -0.08) is the run-to-run tie-order noise recorded at
                                 #   (#176) part 4 — same signature, a rank stat moving in the last digit with n and
                                 #   rho held — and NOT an effect of these knobs.
                                 #
                                 #   `print_edge_annualized` ADDED NOTHING VISIBLE HERE. `annualized_edge_tag` is
                                 #   reached from the hold-period sweep, which a plain lane run does not print, so
                                 #   the "× N rebalances" text above comes from THIS knob alone. Recorded so the next
                                 #   reader does not spend a run hunting a label that was never going to appear.
                                 #
                                 #   REVERT RULE UNCHANGED, and this entry does not satisfy it: the knob goes ON only
                                 #   when someone accepts the honest cost. What is now settled is the fact it was
                                 #   blocking — the edge DOES clear 0 under a 40× charge. Turning it on would restate
                                 #   every receipt in this file at a charge they were not fitted under, for no new
                                 #   information, which is why it still ships OFF.
  backtest_drop_lookahead_sector: false # (#95) UNMEASURED — an unfitted judgement value, shipping
                                 #   OFF, which is the stamped-sector behaviour every golden and every
                                 #   fitted receipt in this file was measured under. Sibling of (#94)
                                 #   below: that knob is look-ahead in the PRICE record, this one is
                                 #   look-ahead in the LABEL.
                                 #
                                 #   THE LOOK-AHEAD: `backtest::stamp_asset_class` fills `quote.sector`
                                 #   from the universe fetch, which returns TODAY's GICS label and only
                                 #   today's. The same label is stamped at every cutoff, so a 1995 walk
                                 #   grades a name on a classification that may postdate the decision by
                                 #   thirty years. It is not cosmetic: `picks::is_commodity` reads
                                 #   `sector` against COMMODITY_SECTORS (Energy, Materials, `picks.rs`
                                 #   (#44)) and drives `commodity_damp`, so a name reclassified since is
                                 #   damped — or spared — on a fact from after the bar it is scored at.
                                 #
                                 #   NO AS-OF LABEL EXISTS. No feed this tool reads carries GICS
                                 #   history; `.sp500_history.json` carries membership spans and no
                                 #   sector. So there is no third option to build: either today's label
                                 #   (look-ahead) or no label (damp inert). This knob is the second one.
                                 #
                                 #   REACH IS UNMEASURED AND CANNOT BE MEASURED FROM THIS REPO. The
                                 #   sector map arrives from the live universe fetch and is not cached
                                 #   here, so the count of pool names carrying Energy/Materials — and of
                                 #   those, how many were reclassified INTO or OUT OF it inside a
                                 #   window — is only knowable on a run. Stated as unknown rather than
                                 #   estimated.
                                 #
                                 #   TURNING IT ON IS A REACHABILITY CHANGE: with `sector` always None,
                                 #   `commodity_damp` goes INERT for every equity in the walk. That
                                 #   makes the on-run a TWO-part change — it removes the look-ahead AND
                                 #   removes the damp — so a worse result does not by itself convict the
                                 #   look-ahead. `growth_gate_reachability_pin` describes the OFF arm,
                                 #   which is the shipped state; if this ever ships ON, that pin moves
                                 #   with it.
                                 #
                                 #   NOT THE LIVE PATH. The knob is read only in the backtest stamp.
                                 #   `screen` and `check` score at today's cutoff, where today's sector
                                 #   IS the point-in-time sector, so nothing about the served book moves
                                 #   at either setting.
                                 #
                                 #   The sibling half of the finding — `picks::asset_class`
                                 #   (crypto/ETF/stock) — is deliberately left alone. An instrument does
                                 #   not stop being an ETF, so that label is time-invariant in practice
                                 #   and carries no look-ahead worth a knob.
                                 #
                                 #   TO SETTLE: the ship-rule grid at 20y/12y/8y on the PIT pool, on vs
                                 #   off, reading top-3 excess mean AND median plus rank-1 h2h. Pair it
                                 #   with a damp-off/sector-on control, otherwise the two effects above
                                 #   cannot be told apart.
                                 #
                                 #   REVERT RULE: stays OFF unless ON raises top-3 excess at 20y AND 8y
                                 #   with rank-1 h2h ≥50% at all three horizons. A bundled regression
                                 #   REFUSES — it is never read as "the look-ahead was helping".
  splice_trim_point_in_time: false # (#94) GRADED BY (#189) — LOOK-AHEAD PRICED, NOT SETTLED, still OFF, which
                                 #   is the parse-time trim every golden and every fitted receipt in this
                                 #   file was measured under. Read it together with `splice_max_weekly_rate`
                                 #   above: that knob decides WHAT is a splice, this one decides WHEN the
                                 #   answer is allowed to be known.
                                 #
                                 #   THE LOOK-AHEAD: `splice_trim_start` keeps the LAST qualifying step in
                                 #   the whole series and `parse_chart` drains everything before it. A 2015
                                 #   redenomination therefore deletes every pre-2015 bar when scoring a
                                 #   2005 cutoff — the walk grades a name on a record a 2005 observer had
                                 #   and a 2015 event retroactively removed. `age_years`, `life_cagr`,
                                 #   MAXDD and the trend R² are then all computed on the truncated series,
                                 #   so the name reads YOUNGER than it was and `growth_trust_young` docks
                                 #   it for a fact that had not happened. The splice receipt already
                                 #   measured the reach and read it as a cost worth paying: 138/3346
                                 #   non-crypto series (4.1%), median trimmed series losing 72% of its
                                 #   record. It is the same number, seen as look-ahead instead of coverage.
                                 #
                                 #   WHAT true DOES: `parse_chart` leaves the record whole and
                                 #   `core::backtest_quote` trims over `[..=as_of]` instead, asking what
                                 #   the cutoff could actually answer. At the LAST bar the two questions
                                 #   coincide — that identity is pinned by
                                 #   `a_splice_cannot_delete_history_that_had_not_happened_yet` — which is
                                 #   why the LIVE path does not move: `quote_one` re-trims the merged
                                 #   series over its whole span anyway, and its whole span ends at today.
                                 #
                                 #   ONE CAVEAT WORTH KNOWING BEFORE FLIPPING IT: with the drain deferred,
                                 #   the SHORT (daily-tail) chart inside `quote_one` is no longer trimmed
                                 #   by `parse_chart`, only by the merged-series pass. A splice inside the
                                 #   last year of daily bars is caught either way; one that lands exactly
                                 #   at a payload boundary is worth checking on a live `screen` before
                                 #   trusting the first run.
                                 #
                                 #   TO SETTLE IT: `backtest universe` at 8y and 12y against a control,
                                 #   and read the CLASS CENSUS and the YRS distribution first — if the
                                 #   trimmed 4% are not in the gated pool the knob is inert and the run
                                 #   says nothing. Expect the affected names to get OLDER at early cutoffs,
                                 #   which loosens `growth_trust_young` and lengthens their `life_cagr`
                                 #   window; both directions are possible for edge and neither is a bug.
                                 #
                                 #   REVERT RULE: this changes the INPUT record, so a before/after pair is
                                 #   two different measurements, not an A/B — the same caveat
                                 #   `splice_max_weekly_rate` carries. Judge it on whether the YRS column
                                 #   stops misstating a listing's age, not on whether edge went up.
                                 #
                                 #   ============================================================================================
                                 #   (#189) GRADED 2026-08-31 ON `universe fund pit`. THE LOOK-AHEAD IS NOW PRICED. STAYS OFF —
                                 #   BUT READ WHY BEFORE EVER CITING THIS AS A REFUSAL ON THE MERITS.
                                 #   ============================================================================================
                                 #   Same round, same lane and the same control as (#188): 3 arm runs on `backtest {20,12,8}
                                 #   universe fund pit` against a control re-run in the same session on the same binary, universe
                                 #   4788 resolved / 4788 from cache on all six runs. Single-key overlay, structurally verified
                                 #   against the shipped file (exactly one key moved: false -> true).
                                 #
                                 #     horizon  top-10 excess mean|med   worst          h2h 6-10      h2h 11-20   pool  subst
                                 #      20y     +0.6|+0.0 -> +0.6|+0.0   -10.0 -> -10.0  4/8 ->  4/8   1/2 -> 1/2  5->5   7-> 7
                                 #      12y     +3.7|+3.9 -> +3.6|+3.9    -6.4 ->  -6.4 12/19 -> 12/19 2/4 -> 2/4  6->6  16->18
                                 #       8y     +5.3|+5.6 -> +5.1|+4.9    -5.5 ->  -5.5 19/27 -> 19/27 5/8 -> 5/8  7->7  21->23
                                 #   growth-scored cutoffs 120/249/355 -> 120/255/361.
                                 #   EVERY GUARD HELD: the worst window is identical at all three horizons, and BOTH h2h books
                                 #   are identical at all three. AT 20y THE ARM IS IDENTICAL TO THE CONTROL ON EVERY GRADED LINE
                                 #   — the knob is inert there, which is the block above's own pre-registered inertness test
                                 #   ("if the trimmed 4% are not in the gated pool the knob is inert and the run says nothing")
                                 #   answering in the affirmative for 20y and in the negative for 12y and 8y.
                                 #   BY THE SHIP RULE: PRIMARY FAILS. Pit reads 12y AND 8y ((#139)); the mean is down at both
                                 #   (+3.7 -> +3.6, +5.3 -> +5.1) and the median is down at 8y (+5.6 -> +4.9). REFUSED.
                                 #
                                 #   AND NOW THE PART THAT MATTERS MORE THAN THE VERDICT. The block above closes with a
                                 #   PRE-REGISTERED closing rule: "Judge it on whether the YRS column stops misstating a
                                 #   listing's age, not on whether edge went up." THIS ROUND DID NOT TEST THE YRS COLUMN, so it
                                 #   did not answer the question the knob was actually posed. Refusing it on edge is precisely
                                 #   the reading its own receipt told the next session not to make. That pre-registration is NOT
                                 #   overruled here on the strength of a number seen afterwards — doing so would be moving a bar
                                 #   to fit a result, which is the move this file exists to prevent. The ship-rule verdict is
                                 #   recorded because it was pre-registered; it is not promoted into a finding about the knob.
                                 #
                                 #   SO RECORD WHAT WAS ACTUALLY BOUGHT: THE PRICE OF THE LOOK-AHEAD, MEASURED FOR THE FIRST
                                 #   TIME. `false` lets a 2015 redenomination delete every pre-2015 bar when scoring a 2005
                                 #   cutoff. Removing that costs the reported lane 0.1 pts/yr of top-10 excess at 12y, 0.2 mean
                                 #   and 0.7 median at 8y, and EXACTLY NOTHING at 20y. Read in the honest direction, that is not
                                 #   a cost of the correction at all — it is the INFLATION IN TODAY'S HEADLINE NUMBERS: the
                                 #   shipped lane's 8y +5.3|+5.6 is flattered by roughly 0.2|0.7 by a trim that knows the future.
                                 #   Small, which is the reassuring answer, and now a number instead of a worry.
                                 #   THE POND BEHAVES EXACTLY AS (#173) FOUND: substitutable names rise (16->18 at 12y, 21->23 at
                                 #   8y) and THE MEDIAN GATED POOL DOES NOT MOVE BY ONE NAME at any horizon. Supply is still not
                                 #   the binding constraint; the gates still bind.
                                 #
                                 #   WHAT THIS DOES NOT LICENSE. It does not ship the flip — PRIMARY refuses it, and the knob's
                                 #   own receipt says this instrument cannot adjudicate it either way, so there is no reading on
                                 #   which it goes on today. It does not CLOSE (#94): the YRS question is still open and still
                                 #   the right one. And it does not license anyone re-citing "the look-ahead costs nothing" —
                                 #   0.2|0.7 pts at 8y is small, not zero, and it is in the direction that flatters the record.
                                 #   TO REOPEN: build the YRS check the block above asks for — does the column stop misstating a
                                 #   listing's age for the 138/3346 trimmed series? — and judge it there, not on edge.
                                 #   REVERT RULE: it is at the revert value and stays there.
  demean_by_market: false        # (#93) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                                 #   the single pooled (bucket, class) peer group every edge, rho, OOS
                                 #   half and `tune` winner in this repo was measured against. Turning it
                                 #   on re-cuts the SELECTION METRIC itself, so it does not move one
                                 #   number, it re-measures all of them at once.
                                 #
                                 #   THE DEFECT: `realized` is `closes[fwd]/closes[i] - 1`, a ratio of two
                                 #   closes in the LISTING currency, and NOTHING on the return path
                                 #   converts it. `fx` is fetched, but only `earnings_yield`/`peg_yield`
                                 #   consume it. So a EUR UCITS line, a GBp LSE line and a USD S&P name
                                 #   land in one peer group with their FX returns stripped, and the mean
                                 #   subtracted from each is an average of three different currencies.
                                 #   Every `relative` in that group then carries the window's FX move as
                                 #   if it were selection — over a 12y hold that is tens of points on a
                                 #   quantity the whole growth lane is ranked by.
                                 #
                                 #   WHAT THIS KNOB FIXES, and exactly how far: splitting the group by
                                 #   market makes the FX move COMMON to the group, so subtracting the mean
                                 #   cancels it — no rate, no series, no fetch, because `Quote::stub`
                                 #   already fills `market` from the ticker suffix. It over-splits:
                                 #   `market_of` returns a COUNTRY, so Germany, France and the Netherlands
                                 #   are three groups trading one currency. That costs group size, never
                                 #   correctness — it can never MERGE two currencies — and a slice under
                                 #   MIN_PEER_GROUP (8) falls back to the pooled mean rather than
                                 #   de-meaning a name against one or two neighbours, or against itself.
                                 #
                                 #   WHAT IT DOES NOT FIX, stated plainly rather than left to be
                                 #   discovered: the ABSOLUTE leg. `report_vs_benchmark` compares a
                                 #   native-currency held book to ^GSPC, and no peer mean cancels anything
                                 #   there. Fixing that needs the closes themselves moved into one
                                 #   currency before the walk — `fetch::fx_factor_series` already returns
                                 #   a dated rate series, so the data exists, but it would have to be
                                 #   fetched per listing currency and applied ahead of `backtest_quote`,
                                 #   which re-bases CAGR, drawdown, trend and range_pct together. Same
                                 #   blast radius as `use_adjusted_close`, and it is NOT done here.
                                 #
                                 #   THE OTHER HALF OF THE SAME FINDING, also not fixed: both legs are
                                 #   price-only, which is honest only if the picks yield what the index
                                 #   yields. They do not — the lane selects near-high, low-yield growers
                                 #   (~0.3-0.8%) against an index yielding ~1.3% — so the reported excess
                                 #   is flattered by roughly 1.0-1.3 pts/yr on the dividend asymmetry
                                 #   alone. The correct fix is TR vs TR, which is exactly what
                                 #   `use_adjusted_close: true` already does (it swaps the benchmark to
                                 #   ^SP500TR at the same time, which is why the two are coupled and must
                                 #   stay coupled). That knob ships false and its own receipt requires a
                                 #   full re-validation to flip. Do NOT "fix" this by pointing the
                                 #   benchmark at ^SP500TR alone: price-only picks against a total-return
                                 #   index over-corrects, and would understate excess by the picks' own
                                 #   yield instead of overstating it by the gap.
                                 #
                                 #   TO SETTLE THIS KNOB: `backtest universe` at 8y and 12y with it on,
                                 #   read against a control run. Watch the CLASS CENSUS and the per-group
                                 #   sizes first — if the fallback is firing on most slices the knob is
                                 #   inert and the run says nothing. Expect edge to FALL: removing
                                 #   contamination removes dispersion the lane was scoring on.
                                 #
                                 #   REVERT RULE: an edge that only survives with this OFF was reading
                                 #   currency moves. If it improves, that is a smaller effective peer
                                 #   pool landing luckier — confirm at the second horizon before shipping.
  drop_dead_ticker_series: false # (#122) MEASURED 2026-08-22, POST-PIT. A real defect, demonstrated
                                 #   on named tickers, that moves ZERO of the numbers this repo grades. Ships OFF
                                 #   and KEPT rather than deleted — the (#47) precedent — so the null stays
                                 #   reproducible and nobody re-chases it from the census alone.
                                 #
                                 #   THE DEFECT. A dead company's ticker is not retired, it is REMAPPED. Yahoo
                                 #   returns it typed `MUTUALFUND` with a numeric registrant id where the name
                                 #   should be — `CFC` = "3847602", `BSC` = "1315901", `WYE` = "4595480" — and its
                                 #   bars KEEP RUNNING to today. `CFC` (Countrywide, index exit 2008-07) carries
                                 #   212 bars AFTER that exit, `BOL` 220, `CCU` 212, `CBE` 52, `MOLX` 49. A hold
                                 #   opened before the exit and closed `years` later reads its ENTRY from the real
                                 #   company and its EXIT from whatever now owns the ticker, then reports the ratio
                                 #   as a return. That is a WRONG number in the sample, not a missing one — which
                                 #   is exactly what separates it from the survivorship hole (#121) prints, where
                                 #   a dead name contributes nothing at all.
                                 #
                                 #   THE SIGNATURE IS EXACT ON THIS DATA. Across all 6149 cached series,
                                 #   `MUTUALFUND` + an all-digit name matches 134 tickers, and every one of the 134
                                 #   is an S&P 500 member: ZERO false positives among non-members, which include
                                 #   real mutual funds carrying real names. 34 of the 134 still carry bars and are
                                 #   the only ones that can contaminate a forward return; the other 100 return an
                                 #   empty payload and never produce a cutoff either way. The knob drops the WHOLE
                                 #   series, not its tail — the remap date is not in the payload, so there is no
                                 #   honest place to cut.
                                 #
                                 #   WHAT IT DOES NOT FIX. A ticker reused by another LIVE equity keeps a real name
                                 #   and an `EQUITY` type and is invisible to this test: `CCU` now resolves to
                                 #   Compania Cervecerias Unidas, `BEAM` to Beam Therapeutics, `GENZ` to a VanEck
                                 #   ETF. Three failure modes, one closed. It also does nothing for the 387 members
                                 #   served with zero bars, which never produce a cutoff at all.
                                 #
                                 #   MEASURED, NON-PIT (`backtest {20,12,8} universe`, two arms one boolean apart,
                                 #   same config path): FULLY INERT. Cutoff counts identical to the unit in both
                                 #   arms — 4541 / 11835 / 20806 — so not one series was dropped, and every printed
                                 #   figure matches to the decimal. CAUSE: the wide pool is the LIVE screen
                                 #   universe (5049 names) and contains none of these tickers. They enter ONLY
                                 #   through the PIT membership map (5738 names). THE LANE THAT EXISTS TO REMOVE
                                 #   LOOK-AHEAD IS THE ONLY LANE THAT ADMITS THE CONTAMINATED SERIES.
                                 #
                                 #   MEASURED, PIT (`backtest {20,12,8} universe --pit`, six runs):
                                 #     20y   cutoffs  2296 ->  2295   (-1)
                                 #     12y   cutoffs  8350 ->  8324   (-26)
                                 #      8y   cutoffs 16803 -> 16754   (-49)
                                 #   and NOT ONE GRADED NUMBER MOVES at any horizon. top-10 excess (mean AND
                                 #   median), win rate, worst window, the OOS split, the rank-1 slice and the
                                 #   rank-1 h2h are identical in both arms: 20y +0.8 (med +0.9) win 55% of 20 worst
                                 #   -10.0 h2h 1/2; 12y +2.3 (med +2.1) win 74% of 34 worst -9.5 h2h 3/6; 8y +3.3
                                 #   (med +2.8) win 81% of 42 worst -6.2. The contaminated cutoffs are real and are
                                 #   wrong, and not one of them ever reaches the top-10 book. NOISE CAVEAT: the
                                 #   live universe fetch drifted between arms (8y: 5738 tickers off vs 5740 on), so
                                 #   those cutoff deltas are a bound, not an exact count.
                                 #
                                 #   SHIP RULE v2 CANNOT GRADE THIS KNOB, and that is worth more than the knob. The
                                 #   rule reads the wide `universe` lane, where the knob provably does nothing; the
                                 #   only lane it touches is one the rule never looks at. This is NOT a TIE and NOT
                                 #   an UNRESOLVED — it is structurally ungradeable by the instrument as written.
                                 #   Recorded rather than worked around.
                                 #
                                 #   REVERT RULE: delete this knob the day the wide lane draws from the membership
                                 #   map, or the day the signature admits a single non-member — re-run the census
                                 #   before trusting it, `MUTUALFUND` + digits is a Yahoo naming artefact, not a
                                 #   contract. Turning it ON is only defensible on a PIT run, and on this data buys
                                 #   a cleaner sample and the same answer.
  print_selection_count: false   # (#92) NOTHING TO MEASURE in the display half — a display knob shipping
                                 #   OFF, byte-identical output, every golden pins it. But it names a
                                 #   real UNMEASURED debt, recorded here because the fix needs runs this
                                 #   repo has not made. Sibling of (#91): that one says how many trials a
                                 #   number rests on, this one says how many candidates it beat.
                                 #
                                 #   THE DEBT: `VERDICT_TOP: 3` — the basket the screen footer tells a
                                 #   reader to BUY — is the argmax over `TOP_LADDER`'s 13 rungs (1, 2, 3,
                                 #   5, 10, 15, 20, 25, 30, 35, 40, 45, 50), taken on the same data the
                                 #   report then quotes, with NO best-of-N haircut. Its own receipt says
                                 #   so in plain words and reads as a strength: "the measured peak of the
                                 #   top-N ladder at EVERY horizon (8y 16.1 vs 15.0/14.9/14.4 for
                                 #   top-1/5/10/20)". A 1.1-point gap, as a 13-way maximum, on the ~0.5
                                 #   effectively independent windows the 20y run has. This file carries
                                 #   exactly ONE multiple-testing correction — the Šidák-tightened band
                                 #   over 14 fund factors in `sweep_fund_factor` — and it covers a knob
                                 #   that ships at weight 0, while the basket size, which is the one
                                 #   number a reader acts on, has none.
                                 #
                                 #   WHAT THE KNOB DOES: prints " [best of 13, unhaircut]" beside the
                                 #   top-N table header and inside the screen footer, from one helper
                                 #   reading `TOP_LADDER.len()`, so adding a rung tightens the printed
                                 #   claim instead of making it a lie. It changes no number. It does NOT
                                 #   pay the debt — it stops the debt being invisible.
                                 #
                                 #   TO PAY IT, and this is the run nobody has made: bootstrap each of
                                 #   the 13 rungs' excess and read a 5/13-tightened tail, exactly the way
                                 #   `sweep_fund_factor` reads 5/14 for its factors. If top-3's band
                                 #   overlaps top-5's and top-10's under that haircut — and at 0.5
                                 #   effective windows it will — then the ladder never resolved the
                                 #   basket size and VERDICT_TOP should be FIXED A PRIORI instead, on
                                 #   grounds that are not a maximum: concentration risk, the worst-window
                                 #   number, and how many names a reader will actually hold. Note the
                                 #   existing receipt already offers one such ground ("roughly HALF
                                 #   top-1's worst window"), which is an argument that survives a haircut
                                 #   where "16.1 beats 15.0" does not.
                                 #
                                 #   DO NOT re-select VERDICT_TOP off a fresh ladder run and call it
                                 #   settled. Re-running the same argmax on the same universe reproduces
                                 #   the same selection bias with a newer date on it.
                                 #
                                 #   (#120) 2026-08-22 — THE DEBT IS PAID, AND NOT BY THE ROUTE THIS
                                 #   ENTRY PROPOSED. Nobody ran the 13-rung Sidak-tightened bootstrap
                                 #   described above; at 0.5 effective windows it would have resolved
                                 #   nothing, which this entry already predicted in its own words ("and
                                 #   at 0.5 effective windows it will"). Instead VERDICT_TOP was FIXED A
                                 #   PRIORI at 10 — the basket every candidate knob in this file was
                                 #   already compared on — exactly the "grounds that are not a maximum"
                                 #   this entry asked for. The paragraph above stands as written because
                                 #   its warning is still live: the fix was to STOP selecting, not to
                                 #   select more carefully. Full receipt in the SHIP RULE v2 block at
                                 #   the top of this file.
                                 #   THIS KNOB SURVIVES THE PAYMENT, with a narrower job: the caveat now
                                 #   prints only on the ladder TABLE header, warning the READER off an
                                 #   argmax the repo no longer makes. It came off the screen footer.
  print_n_eff: false             # (#91) NOTHING TO MEASURE — a display knob, shipping OFF, which is
                                 #   today's output byte-for-byte and is what every golden in
                                 #   tests/fixture pins. It adds no gate, no score term and no journal
                                 #   field; turning it on appends a tag to five lines and changes no
                                 #   number anywhere. Third of the (#89)/(#90) set: all three are about
                                 #   whether the numbers that JUDGE a knob are honest.
                                 #
                                 #   WHAT IT SAYS: a "window" is a ~6-month entry bucket and the hold is
                                 #   `years`, so two consecutive windows share (years−0.5)/years of ONE
                                 #   price path. They are near-copies, not repeats of an experiment, and
                                 #   the count worth quoting is windows/(2·years). Read off the goldens
                                 #   in this repo: backtest-20's headline row is "win 67% ... (windows
                                 #   21)" — 21 draws worth 0.5 of a trial; backtest-12's 33 windows are
                                 #   worth ~1.4; the 84-window journaled 12y verdict the screen footer
                                 #   quotes is worth 3.5. A win RATE over half a trial is a story, and
                                 #   nothing printed today distinguishes it from one over 21 trials.
                                 #
                                 #   WHERE IT PRINTS: the screen footer (`verdict_line`, the number a
                                 #   buy decision actually sees), the three `report_entry_state` rows
                                 #   including the JOURNALED one, and the rank-slice win rates. All five
                                 #   go through one helper so no site can drift to its own arithmetic.
                                 #
                                 #   NOT APPLIED to `windows scored: N` or the lane `n=` counts: those
                                 #   count CUTOFF ROWS (many tickers per bucket), not windows, so the
                                 #   bucket formula would be a wrong number wearing the right label. If
                                 #   an effective count is ever wanted there it needs its own derivation,
                                 #   not this one.
                                 #
                                 #   REVERT RULE: none needed — it cannot move a decision, only describe
                                 #   one. Turn it off if the extra column crowds the report. What it
                                 #   MUST NOT become is an input: the moment a gate or a ship rule reads
                                 #   n_eff, this stops being a display knob and needs a receipt with
                                 #   measurements in it.
                                 #
                                 # (#124) MEASURED 2026-08-23, `universe fund pit` at 20y/12y/8y, via
                                 # tests/diag-ci-settings.yaml. The arithmetic in this receipt was right, and the
                                 # numbers are worse than its own example suggested:
                                 #     20y   20 windows -> n_eff 0.5     (rank 1; rank 6-10 is 0.2)
                                 #     12y   34 windows -> n_eff 1.4
                                 #      8y   42 windows -> n_eff 2.6
                                 # EVERY 20-YEAR VERDICT IN THIS FILE RESTS ON HALF AN INDEPENDENT TRIAL. That is
                                 # not a caveat on one printed line, it is a property of the ship rule itself:
                                 # PRIMARY reads top-10 excess at 20y AND 8y, and one of those two horizons is 0.5
                                 # effective draws. A knob that "held at 20y" held across less than one effective
                                 # observation, which is the most likely reason so many sweeps in this file read as
                                 # noise-floor ties.
                                 # WHAT IT DOES NOT SAY: it does not say the 20y lane is wrong or should be dropped.
                                 # A 20-year hold IS the product, and 0.5 trials is the honest price of a 20y window
                                 # on the history available - not a defect to be tuned away. It says the 20y column
                                 # cannot ADJUDICATE between two knob values, and that knob-grading work belongs at
                                 # 12y/8y where n_eff clears 1. Anyone re-reading a 20y refusal in this file should
                                 # price it against 0.5.
                                 # Knob stays false: reading an instrument is not a reason to ship its output into
                                 # every golden. REVERT: set false.
  print_h2h_mid_ladder: true     # (#135) MEASURED 2026-08-24 — and it is the measurement the SHIP RULE v2
                                 # block at the top of this file asked for in writing: "any future rule that
                                 # grades on the PIT pool needs a guard whose sample size it can state".
                                 #
                                 # WHAT IT DOES: prints a SECOND head-to-head line — #1 and the 2-5 book
                                 # against the 6-10 book — BESIDE the shipped line, which compares them to
                                 # the 11-20 book. Never instead of it. Both tallies come from ONE pass over
                                 # the same buckets in `rank_slice_stats`, so the pair cannot differ by grid
                                 # drift the way a two-run comparison could. The ship rule still reads the
                                 # 11-20 line wherever that line exists.
                                 #
                                 # WHY IT EXISTS — THE GUARD IS STARVED BY THE GATES IT GRADES. The shipped
                                 # h2h needs a window holding >= 11 admitted names (`vv.len() > 10`), and
                                 # (#124) measured that "the book never holds more than ~10 names, because
                                 # the gates admit no more than that". So the winner's-curse GUARD that
                                 # exists to revalidate the gates is starved by the very gate values it
                                 # grades. That is one defect, not two: (#124)'s admission ceiling and
                                 # (#130)'s "the PIT universe was DECLINED, the h2h rests on 2/4/6 windows"
                                 # are the same fact seen from two sides. Its cost is concrete — P0b, the
                                 # re-sweep of the five lookahead-fitted knobs on a point-in-time pool, has
                                 # never been runnable, so those five stay frozen at values fitted on a pool
                                 # that could pick a name before it joined the index (see DEBT, above).
                                 #
                                 # THE BAR WAS FIXED IN WRITING BEFORE ANY RUN, because this touches a GUARD
                                 # and a broken guard REFUSES outright. Pre-registered: the 6-10 book may be
                                 # read as the guard ONLY if it reproduces the SAME verdict as the 11-20
                                 # book on every arm where both are computable. One disagreement and this is
                                 # a rule change rather than a denominator fix, and it is REFUSED.
                                 #
                                 # MEASURED: six wide runs, `backtest {20,12,8} universe fund pit` and
                                 # `... universe stress`, via a ONE-field overlay (historically tests/h2h-probe.yaml,
                                 # since DELETED; recreate per the rule at `print_book_deciles` above):
                                 #     buy_heuristic:
                                 #       print_h2h_mid_ladder: true
                                 # `heuristic_drift` named `print_h2h_mid_ladder` and nothing else on all
                                 # six. The runs authenticate themselves: the 11-20 denominators come back
                                 # 2/4/6 on PIT and 18 at 20y / 34 at 8y on stress, which is exactly what
                                 # (#130) recorded, so these are the lanes it measured.
                                 #
                                 #     rank-1 h2h        vs 11-20        vs 6-10       same side of 50%
                                 #     pit  20y           1/2   50%       4/8   50%      YES
                                 #     pit  12y           2/4   50%      11/18  61%      YES
                                 #     pit   8y           4/6   67%      17/26  65%      YES
                                 #     strs 20y          12/18  67%      15/22  68%      YES
                                 #     strs 12y          15/26  58%      19/36  53%      YES
                                 #     strs  8y          22/34  65%      27/44  61%      YES
                                 #
                                 #     the 2-5 book, same six arms:  pit 50->75, 50->61, 67->54;
                                 #                                   strs 61->68, 73->58, 71->66.  ALL SIX AGREE.
                                 #
                                 # VERDICT: 12 of 12 comparisons agree on the >= 50% line. The pre-registered
                                 # bar is MET, so re-basing the guard onto the 6-10 book is a DENOMINATOR FIX,
                                 # not a softening. On the PIT lane the denominator goes 2/4/6 -> 8/18/26
                                 # (4.0x, 4.5x, 4.3x) and the "one window flips it 50 points" objection at
                                 # line 404 falls to 12.5 / 5.6 / 3.8 points.
                                 #
                                 # WHAT THIS DOES NOT BUY, and the receipt says it rather than let a later
                                 # session discover it: 8 windows at 20y is still ~0.2 EFFECTIVE trials by
                                 # (#124)'s own n_eff arithmetic directly above. The 20y PIT column still
                                 # cannot ADJUDICATE between two knob values. This fix makes the guard
                                 # COMPUTABLE at 20y; it does not make it decisive there. Grade P0b at 12y
                                 # and 8y, where n_eff clears 1, exactly as (#124) instructs.
                                 #
                                 # KNOB STAYS false, matching every other print_* knob in this file: today's
                                 # output byte-for-byte, which is what the tests/fixture goldens pin. Turn it
                                 # on per-run via an overlay when grading a PIT arm. No golden can reach the
                                 # print (`hn == 0` on every fixture run, and no golden contains the string
                                 # "head-to-head"), so `rank_slice_ladder_and_head_to_head` is the ONLY
                                 # protection for this path — it pins both tallies, the 10-name boundary and
                                 # the short-pool clamp. Do not weaken it.
                                 #
                                 # REVERT RULE: this knob only prints, so reverting it reverts nothing that
                                 # was decided. What IS revertible is the re-basing it licenses — if a future
                                 # run finds the two books disagreeing on any arm where both are computable,
                                 # the re-basing is void and the guard goes back to the 11-20 book alone,
                                 # which puts P0b back where (#130) left it. Record the disagreeing arm here.
                                 #
                                 # *** THE REVERT RULE FIRED ON THE FIRST ARM GRADED, 2026-08-24. RECORDED HERE
                                 # AS IT REQUIRES, AND THE RE-BASING IS NOW LIMITED TO 12y AND 8y. ***
                                 # P0b arm 1 (`growth_min_cagr` 19->15 on PIT, full receipt at that knob) put the
                                 # two books on opposite sides of the 50% line at 20y: the 6-10 book reads 6/13
                                 # (46%, GUARD BROKEN) and the 11-20 book reads 3/6 (50%, passes). At 12y and 8y
                                 # they agreed again (68% vs 69%, and 76% vs 62% — same side, both pass).
                                 #
                                 # WHAT THIS COSTS AND WHAT IT DOES NOT. The rule was pre-registered mechanically
                                 # — "disagreeing on any arm where both are computable" — and it is being applied
                                 # as written rather than argued past, which is the whole reason to pre-register.
                                 # So: THE 20y RANK-1 h2h ON THE PIT POOL IS ONCE AGAIN UNCOMPUTABLE, because
                                 # the only book licensed there is the 11-20 one and (#130) measured its
                                 # denominator at 2. The re-basing SURVIVES at 12y and 8y, where the two books
                                 # have now agreed on four independent readings (control and arm, both horizons).
                                 #
                                 # That limit lands exactly where the independent evidence already pointed, which
                                 # is why it is a limit and not a defeat: (#124) measured 20y n_eff at 0.5 on the
                                 # live pool, this receipt put PIT 20y at ~0.2, and both said the 20y column
                                 # cannot ADJUDICATE between two knob values. A 6-window 50.0% and a 13-window
                                 # 46.2% are not a real contradiction — they are two draws of noise, and the
                                 # rule correctly refuses to let either one decide anything.
                                 #
                                 # DO NOT "FIX" THIS BY PREFERRING THE BIGGER DENOMINATOR. It is the obvious move
                                 # and it is exactly the softening this rule exists to block: the 6-10 book was
                                 # licensed BY agreement, so it cannot be promoted over the book it disagrees
                                 # with by citing its own sample size. TO RE-EXTEND IT TO 20y, an arm must show
                                 # the two books agreeing at 20y on a lane where the 11-20 denominator is not
                                 # starved — i.e. on `stress`, where (#135) already got 18 windows — AND the
                                 # PIT 20y denominator must clear the n_eff>=1 bar (#124) sets, which at ~0.2
                                 # today needs roughly a 5x deeper admitted book, not a different comparison.
                                 # #
                                 # # (#154) FLIPPED ON, 2026-08-29, AT MEASURED ZERO COST TO THE GOLDENS. This
                                 # # receipt predicted it: "no golden can reach the print (`hn == 0` on every
                                 # # fixture run)". Verified rather than trusted — with ONLY this knob flipped,
                                 # # 0 of 5 goldens move, and with it flipped beside print_recall_capture the
                                 # # golden diff is identical to that knob's alone. So "KNOB STAYS false ...
                                 # # today's output byte-for-byte, which is what the tests/fixture goldens pin"
                                 # # is SATISFIED BY the flip, not violated by it: the clause protected the
                                 # # goldens, and the goldens do not move. The general print_* policy is
                                 # # untouched — see (#91), which stays false, and (#154) at
                                 # # print_recall_capture, which pays a real 143-line cost for a different reason.
                                 # #
                                 # # WHY NOW: (#151) measured what shipping this off actually costs. It found the
                                 # # rank-1 h2h GUARD's denominator is IDENTICALLY the pool census's
                                 # # non-saturated bucket count, and that on the shipped lane it runs at n=4 at
                                 # # 12y pit — with nothing on the page saying so. The remedy was already built,
                                 # # already licensed at 12y/8y by the measurement above, and reachable only by
                                 # # remembering a per-run overlay. An instrument you must remember to switch on
                                 # # is one you will forget in exactly the run where it matters, which is what
                                 # # (#151) documented happening.
                                 # #
                                 # # THE LIMIT IS UNCHANGED AND THIS FLIP DOES NOT RELAX IT. The re-basing stays
                                 # # licensed at 12y and 8y ONLY; the 20y arm is void, the revert rule above
                                 # # fired and stays fired, and printing the line at 20y does not revive it. The
                                 # # line prints at every horizon because it reads a DENOMINATOR, not a verdict —
                                 # # it carries "denominator N here vs M for the 11-20 line above" for exactly
                                 # # that reason, and it cites (#135) inline so the reader lands on the limit.
                                 # # Anyone tempted by the bigger 20y denominator: read "DO NOT FIX THIS BY
                                 # # PREFERRING THE BIGGER DENOMINATOR" above first.
                                 # #
                                 # # `rank_slice_ladder_and_head_to_head` is STILL the only protection for this
                                 # # path and is unchanged — no golden reaches the print, so nothing else can pin
                                 # # it. Do not weaken it.
                                 # # REVERT RULE: set false; this knob only prints, so reverting it reverts
                                 # # nothing that was decided.
                                 # #
                                 # # (#160) 2026-08-29 — THAT REVERT RULE IS NO LONGER TRUE, AND THIS IS THE
                                 # # ONE PLACE A READER WOULD BE MISLED BY IT. The Ship Rule v2 GUARD now READS
                                 # # this line at 12y and 8y (>= 50% AND not below the control's tally on this
                                 # # same 6-10 book); 20y still reads the 11-20 line, unchanged and with no
                                 # # not-worse bar. So setting this false no longer "reverts nothing" — it
                                 # # removes the figure the guard adjudicates on, and the rule reverts with it.
                                 # # The knob still only PRINTS; what changed is who reads the print. Full
                                 # # receipt, the whole-record re-adjudication and the revert path: the (#160)
                                 # # block in this file's header. The 12y/8y-only limit above is unchanged and
                                 # # (#160) depends on it — if (#135)'s revert rule fires again at either
                                 # # horizon, (#160) goes with it.
  split_purge_months: 0          # (#90) UNMEASURED — an unfitted judgement value, shipping 0, which is no
                                 #   purge at all: today's behaviour byte-for-byte, and what every OOS rho,
                                 #   every "both halves positive" promotion in this file and every `tune`
                                 #   winner was read off. Sibling of (#89): both are about whether the
                                 #   numbers that JUDGE a knob are honest, not about the knobs themselves.
                                 #
                                 #   THE LEAK: every chronological split in backtest.rs cuts by ROW INDEX
                                 #   with no gap — `tune`'s 70/30 and the early/late OOS halves alike. A
                                 #   cutoff's label is its return over the NEXT `years`, so the last row on
                                 #   the earlier side and the first row on the later side share
                                 #   (years−0.5)/years of one realised path: 11 of 12 years at a 12y hold.
                                 #   The "held-out" half is then largely the outcome the earlier half was
                                 #   fitted on, which is precisely the failure an OOS number exists to
                                 #   catch. `tune`'s per-split `demean` does NOT cover this — it removes the
                                 #   PEER MEAN, and the leak is in the label. Any value >0 drops rows within
                                 #   that many 30-day months off the END of the earlier side; the later side
                                 #   is never trimmed, because it is the evidence being read.
                                 #
                                 #   WHERE IT APPLIES — all seven boundaries, through one helper
                                 #   (`purged_cut`), because a purge at some splits and not others is a
                                 #   purge nobody can reason about: `tune_growth`'s cut, `sweep_fund_factor`
                                 #   (its cut plus both of its OOS sub-splits), `report_lane`'s OOS line,
                                 #   `weight_curve`'s per-rung OOS, and `emit_probe` / `report_fund_lane`,
                                 #   whose rows are what a standalone factor's promotion bar is read from.
                                 #
                                 #   WHY NO NUMBER IS SHIPPED, same shape as (#89): the honest span is the
                                 #   purge (the forward window) plus an embargo of the same, so 24·years
                                 #   months — 192 at 8y, 288 at 12y, 480 at 20y. Against the fixture's own
                                 #   cutoff spans (1971-01..2018-08 at 8y, ..2014-07 at 12y, ..2006-07 at
                                 #   20y) 288 months eats 24 of the 43.5 years the 12y run has, and the
                                 #   train side is only 70% of it — so at 12y the honest purge leaves
                                 #   roughly nothing to tune on, and at 20y nothing at all. The correct
                                 #   value is larger than the data. Shipping a smaller one would dress that
                                 #   ignorance as rigour, so the knob ships off and the finding stands.
                                 #
                                 #   TO SETTLE IT: `backtest universe` at 8y over 0/12/24/48/96, reading
                                 #   how far the TEST edge and the two OOS rho's fall as the gap opens. A
                                 #   term whose OOS halves stay positive at 96 has evidence; one that only
                                 #   clears at 0 was reading its own training labels. Do NOT sweep 12y or
                                 #   20y — the honest span there exceeds the sample, so any rung is a
                                 #   compromise with no defensible stopping point.
                                 #
                                 #   REVERT RULE: this can only REMOVE rows, so it can only weaken an OOS
                                 #   claim or shrink a train set. If a value here ever appears to IMPROVE a
                                 #   held-out number, that is a smaller sample landing luckier, not a
                                 #   result — re-read it at a second horizon before believing it.
  bootstrap_block_buckets: 0     # (#119) MEASURED 2026-08-22 AND SHIPPED — 0 now means DERIVE FROM THE
                                 #   HOLD, and on this data that means NO BAND AT ANY HORIZON. This is a
                                 #   deliberate output change: five goldens moved, and what they lost was
                                 #   two "below 0 -> backwards" verdicts and three "STRADDLES 0" ones.
                                 #
                                 #   WHAT (#89) LEFT UNDONE. It added the block draw, worked out the honest
                                 #   length (the dependence is one hold, so 2·years buckets = 16 at 8y, 24
                                 #   at 12y, 40 at 20y) and then shipped one bucket anyway. The reason is
                                 #   visible in the code, not the statistics: both call sites are handed
                                 #   `samples` and `tuning`, neither carried the horizon, so the knob could
                                 #   only ever hold a FIXED count — and no fixed count is right for a knob
                                 #   read at three horizons in one run. `bootstrap_block` is that missing
                                 #   wire; `sweep_fund_factor` took a `years` argument to reach it.
                                 #
                                 #   THE MEASUREMENT, which is the part (#89) called for and did not run —
                                 #   frozen 8y fixture (64-71 buckets), sweeping the block, reading the
                                 #   WIDTH the revert rule asks about and not the edge:
                                 #
                                 #       block    1      2      4      8     16     24
                                 #       width  62.1   69.3   79.0   70.1   47.0   (refused)
                                 #
                                 #   It rises, PEAKS AT 4, then falls. At 16 — the honest length at 8y —
                                 #   the band is NARROWER than the one-bucket band it was added to widen.
                                 #   (#89)'s revert rule called a narrowing "a bug in the draw loop, not a
                                 #   result". It is neither: as the block approaches the record every draw
                                 #   resembles the whole record, the resample stops varying, and the
                                 #   interval shrinks toward zero. The draw loop is correct and the regime
                                 #   is wrong.
                                 #
                                 #   SO NEITHER END WAS HONEST. Below the dependence length the band is the
                                 #   i.i.d. width wearing a block bootstrap's name; at the dependence
                                 #   length the record holds too few blocks to vary. The peak at 4 is the
                                 #   maximum of a bias curve, not an estimate — shipping it would be
                                 #   fitting the artefact. `MIN_BLOCKS = 10` guards the RATIO instead: a
                                 #   block must be at most a tenth of the record. Clearing that needs 160
                                 #   buckets at 8y (80 years of cutoffs), 240 at 12y, 400 at 20y. Nothing
                                 #   does, so the band refuses everywhere — the same shape of finding as
                                 #   `split_embargo_months`: the correct value is larger than the data.
                                 #
                                 #   WHAT THIS COSTS: nothing the ship rule reads. tests/network.rs never
                                 #   parsed the band; SHIP RULE v2 is decided on excess, h2h and the worst
                                 #   window. What it costs is the habit of quoting "clears 0 -> real" off
                                 #   an interval that was too narrow by construction — every such verdict
                                 #   in this repo's history was easier to have earned than it looked.
                                 #
                                 #   REVERT RULE: `bootstrap_block_buckets: 1` restores the pre-(#119)
                                 #   one-bucket draw byte-for-byte — `block == 1` is exempt from the ratio
                                 #   guard (there is no blocking to collapse) and keeps the original
                                 #   `< 4 buckets` floor. Reverting brings the old bands back verbatim; it
                                 #   does not make them mean anything.
  backtest_anchor_windows: false # (#88) GRADED BY (#188) — NOT GRADEABLE ON THIS LANE, still OFF, which is
                                 #   today's behaviour byte-for-byte and is what every golden in
                                 #   tests/fixture and every receipt in this file was measured under.
                                 #   THE SKEW, and it is the twin of `inflation_adjust.score_on_nominal`:
                                 #   `core::backtest_quote` hardcoded an EMPTY anchor map, so every
                                 #   backtest leg fell back to `core::default_anchor_half`, while the live
                                 #   path is handed the `anchor_windows` block at the top of this file.
                                 #   They are not the same anchor:
                                 #       label   live (this file)   backtest (the default)
                                 #        1W            3                    7
                                 #        1M           15                   30
                                 #        6M           91                   90
                                 #        1Y          182                   90
                                 #        5Y/10Y/20Y  365                  365   <- these two agree
                                 #   So the long rungs are clean and the SHORT ones are not, which is the
                                 #   opposite of where you would look. `return_1y` is the estimator that
                                 #   moves most: live it is the mean of every close from t-547d to t-183d,
                                 #   in the backtest the mean over t-455d..t-275d. That number feeds
                                 #   `growth_min_1y_pct`, `accel` (weight 0.50, the heaviest score term in
                                 #   the lane) and `mom121`. `return_1m` feeds the 1M-knife gate, which is
                                 #   inert for equities at the shipped `max_1m_drop_pct: -1000000.0` but
                                 #   LIVE for crypto at `max_1m_drop_pct_crypto: -35.0` — so a coin is
                                 #   knifed on a half-width window in the backtest and a full-width one
                                 #   live.
                                 #   DIRECTION. A1 offers two reconciliations and this takes the one that
                                 #   is implementable: the BACKTEST adopts the live map. The inflation half
                                 #   had to go the other way (live -> nominal) because no as-of HICP series
                                 #   exists offline; the anchor map has no such problem, it is arithmetic
                                 #   over closes the run already holds. Resolved once in `backtest::run`
                                 #   and passed down as a map, so the validated walk and
                                 #   `hold_period_sweep` cannot read the knob differently.
                                 #   WHY NO NUMBER. Turning it on moves the INPUT to every perf-fed gate,
                                 #   so before/after is two different measurements and not an A/B — the
                                 #   same standing this file already gives `splice_max_weekly_rate`. There
                                 #   is no ship-rule verdict to quote because there is no comparable pair.
                                 #   REVERT RULE: it is at the revert value. Turning it on invalidates
                                 #   every perf-fed receipt at once — `growth_min_cagr`, `growth_min_1y_pct`,
                                 #   the 5Y/20Y rung floors, `max_1m_drop_pct_crypto`, `growth_accel_cap` — so it
                                 #   is a RE-SWEEP, not a flip. Do it as its own round, re-bless the
                                 #   goldens in the same commit, and mark every receipt it touches with the
                                 #   horizon it was re-measured at.
                                 #
                                 #   ============================================================================================
                                 #   (#188) GRADED 2026-08-31 ON `universe fund pit`. NOT GRADEABLE — THE ARM DESTROYS THE
                                 #   SAMPLE IT WOULD BE GRADED ON. IT STAYS OFF, NOW BY MEASUREMENT AND NOT ONLY BY CAUTION.
                                 #   ============================================================================================
                                 #   THE PREMISE THIS ROUND TESTED. The block above says "WHY NO NUMBER ... before/after is two
                                 #   different measurements and not an A/B ... There is no ship-rule verdict to quote because
                                 #   there is no comparable pair." That is RIGHT about the fitted knob receipts and WRONG about
                                 #   the ship rule. The graded quantity is `realized = closes[fwd]/closes[i] - 1`
                                 #   (backtest.rs:1181), a raw close-to-close ratio, and `benchmark_fwd` (:2348) is the same
                                 #   shape. `anchor_windows` is handed only to `core::backtest_quote` on the NEXT line (:1195),
                                 #   so it moves the score and gate INPUTS and cannot reach the number the ship rule grades. An
                                 #   arm picks a different book; the book is then graded on returns the knob cannot touch. A
                                 #   ship-rule A/B IS therefore available, and this round ran it — 3 arm runs against a control
                                 #   RE-RUN in the same session on the same binary (universe 4788 resolved / 4788 from cache on
                                 #   every one of the six runs, so the pond is identical and probe-vs-probe holds).
                                 #
                                 #   THE ANSWER IS NOT A VERDICT. IT IS A COLLAPSE.
                                 #       horizon   growth-scored cutoffs   median gated pool   substitutable names
                                 #        20y           120 ->    5            5 -> (no census printed)   7 -> (none)
                                 #        12y           249 ->   15            6 ->  1                   16 ->  0
                                 #         8y           355 ->   23            7 ->  1                   21 ->  0
                                 #   94-96% of the growth cohort disappears at every horizon. At 12y and 8y the median gated
                                 #   pool falls to ONE name against a graded basket of 10 and the substitutable count is ZERO,
                                 #   so by (#149) EVERY column is VACUOUS; the rank-1 h2h denominators are 0 and the GUARD line
                                 #   does not print at all. At 20y only 5 cutoffs survive and no census prints. A "top-10 book"
                                 #   drawn from a median pool of 1 is not a book, and setting it beside the control's 6-name
                                 #   book is not an A/B on the same instrument.
                                 #   RECORD THIS AS NOT GRADEABLE, NEVER AS "REFUSED": the arm did not lose a measurement, it
                                 #   removed the measurement. For completeness, on the numbers that do still print it ALSO fails
                                 #   PRIMARY at both graded horizons and breaks the worst-window GUARD — 12y +3.7|+3.9 ->
                                 #   -2.9|+1.8 with worst -6.4 -> -38.3; 8y +5.3|+5.6 -> +0.7|+3.4 with worst -5.5 -> -36.9.
                                 #   Both readings point the same way, so nothing turns on which one is quoted.
                                 #
                                 #   THE CAUSE — AND THE BLOCK ABOVE WAS WATCHING THE WRONG RUNG. The arm's missed-winner
                                 #   breakdown carries a blocker that appears in ALL THREE arm runs and in NONE of the three
                                 #   control runs: `artifact` (1 at 20y, 12 at 12y, 31 at 8y), while `history` is unchanged at
                                 #   4/3/15 on both sides. `artifact` is picks.rs:2386 and it fires on EXACT equality —
                                 #       d1.abs() > 0.5 && (d1 - w1).abs() < 1e-6 && (d1 - m1).abs() < 1e-6
                                 #   i.e. 1D, 1W and 1M all reading the same %: "single-bar repricing, not a price history". IT
                                 #   READS 1D/1W/1M AND NOTHING ELSE. The block above spends its entire analysis on 1Y (182 live
                                 #   vs 90 in the backtest, "the estimator that moves most"). 1Y CANNOT TRIP THIS GATE. The
                                 #   damage is at the two rungs where the live map is NARROWER than the backtest default —
                                 #   1W 3 vs 7, and 1M 15 vs 30 — which is the opposite of the direction the receipt reasoned in.
                                 #   WHY NARROWER HURTS: the live map is calibrated for DAILY bars, and history older than the
                                 #   ~10y daily window is MONTHLY (fetch.rs; core.rs:1650 states it, and it is the reason the
                                 #   (H-cov) guard carries 31 days of slack). A +-3d band around t-7d contains no monthly bar at
                                 #   all, so `asof_avg` returns None and core.rs:1656 falls through to `asof(target)` — the last
                                 #   bar on or before the target. At a historical cutoff 1D, 1W and 1M then resolve to the SAME
                                 #   monthly bar, the three percentages come out bit-identical, and the artifact gate fires on a
                                 #   name whose only defect is that its record is monthly. The wider DEFAULT windows straddle
                                 #   enough bars to differ, which is why the shipped lane never sees this.
                                 #
                                 #   WHAT THIS DOES TO THE PRESCRIPTION ABOVE. Its REVERT RULE orders a RE-SWEEP: "Do it as its
                                 #   own round, re-bless the goldens in the same commit, and mark every receipt it touches."
                                 #   THAT ROUND IS NOT EXECUTABLE AS WRITTEN. Re-fitting `growth_min_cagr`, `growth_min_1y_pct`,
                                 #   the 5Y/20Y rung floors, `max_1m_drop_pct_crypto` and `growth_accel_cap` on this lane would
                                 #   fit them to 15 growth-scored cutoffs at 12y and 23 at 8y, on a cohort selected in part by a
                                 #   gate misfiring on bar cadence. Every value it produced would be noise wearing a receipt.
                                 #   The re-sweep is not merely expensive here — it is invalid, and that is worth more than the
                                 #   verdict: this round's 6 runs bought the knowledge that the prescribed round must not be run.
                                 #
                                 #   TO REOPEN, AND IT IS A CODE FIX, NOT A KNOB. Make the anchor half RESPECT BAR CADENCE:
                                 #   widen (or skip) a short-horizon window where the local bar spacing is monthly, so
                                 #   `asof_avg` is never silently reduced to `asof` and 1D/1W/1M cannot collapse onto one bar.
                                 #   That is a change at core.rs:1655-1658, and it would also make the LIVE path honest on any
                                 #   name whose early record is monthly — so it is worth doing on its own merits, before any
                                 #   train-serve reconciliation is measured again. DO NOT reopen this by re-fitting
                                 #   `anchor_windows` until the backtest survives: that fits the ruler to the sample, which is
                                 #   (#120)'s argmax wearing a third hat.
                                 #   ON THE PRE-REGISTERED AMBIGUITY: this round fixed in advance that a REFUSAL on a train-serve
                                 #   arm is AMBIGUOUS in general — the skew may be harmless, or knobs fitted under the old
                                 #   estimator may simply fail to transfer. IT IS NOT AMBIGUOUS HERE. The arm does not lose on
                                 #   edge; it loses the sample, and the cause is identified in code rather than inferred from a
                                 #   number. That is why this closes as NOT GRADEABLE with a named remedy instead of as a null.
                                 #   REVERT RULE: it is at the revert value and stays there.
  use_adjusted_close: false      # (B) REVERTED 2026-06-28: backtest 12 universe edge unchanged (raw
                                 #   -100337371.5 vs adj -100337372.0), OOS halves identical (+0.09/-0.05).
                                 #   No improvement → golden rule reverts. Flip true only if a re-run lifts edge.
                                 #   *** THAT VERDICT IS NOT SAFE TO CITE (2026-08-07). READ THE NUMBERS: the
                                 #   edge it compared was -100,337,371 vs -100,337,372. Today the same metric on
                                 #   the same command reads +452.8. The sibling knob reverted on that SAME grid
                                 #   says what was wrong with it — see `use_trend_cagr` directly below: "Crypto's
                                 #   1e9-scale peer-rel swamps this sample → knob inert + edge straddles 0
                                 #   (noise)". `demean` now splits the peer mean by (bucket, ASSET_CLASS), which
                                 #   is what ended the swamping. An "unchanged" read at 1e8 scale is not evidence
                                 #   of no effect; it is evidence the instrument was broken. THE TOTAL-RETURN
                                 #   QUESTION IS THEREFORE OPEN, not closed, and this line is why.
                                 #   BUT DO NOT JUST FLIP IT — there is a trap that would manufacture a fake win.
                                 #   Adjusted close makes the BOOK total-return while the benchmark stays a PRICE
                                 #   index (^GSPC has no dividends to adjust, so its adjclose IS its close). The
                                 #   excess would jump by roughly the book's own yield and read as free alpha.
                                 #   A real test needs a total-return benchmark first, and ^SP500TR only starts
                                 #   1988 — which guts the 20y window range the ship rule grades on. That is the
                                 #   actual blocker, and it is a DATA problem, not a knob problem.
                                 #   MEANWHILE the cheaper half of the same question got answered instead: (#53)
                                 #   plumbed as-of dividends into the SCORE without touching realized returns, so
                                 #   `dividend_weight` is graded while both legs stay price-only and the excess
                                 #   stays apples-to-apples. See the dividend_weight receipt below.
  use_trend_cagr: false          # (E,#14) REVERTED 2026-06-28: slope CAGR edge -100337434.4 vs endpoint
                                 #   -100337336.3 (WORSE), OOS halves identical. Crypto's 1e9-scale peer-rel
                                 #   swamps this sample → knob inert + edge straddles 0 (noise). Keep endpoint.
  use_life_cagr: false           # (#3j) REVERTED 2026-07-26: ranking on the WHOLE-LIFE CAGR (the `cagr`
                                 #   column's number) instead of the 20/8/5Y leg read edge +125.6 vs +167.6,
                                 #   rho +0.17 vs +0.20, BOTH OOS halves down. Its ablation is the damning
                                 #   part: zeroing growth_trend_weight GAINS +3.8 under it (vs -37.6 under
                                 #   the leg) — the term stops carrying information. "Life" is an unbounded
                                 #   window (5y for one row here, 46y for another), so it mostly encodes
                                 #   listing age. Keep the leg. Full receipt: (#3j) below.
                                 #   (#3k) 2026-07-29 RE-CONDEMNED on the first stress grid — true at all
                                 #   FOUR horizons, corpses injected, splice-trimmed data: 20y-s edge
                                 #   +437.5 vs +552.5 (-115), 12y-s +144.6 vs +205.9 (-61), 8y-s top-10
                                 #   excess +5.7 vs +6.4, 5yL-s edge +31.0 vs +40.5. Loses EVERY view it
                                 #   was graded on. Do not re-propose. (It is also what ranked the corrupt
                                 #   0A08.L splice #1 pre-trim: an unbounded window maximizes exposure to
                                 #   glued-denomination history.) A local settings.yaml overlay carrying
                                 #   `use_life_cagr: true` silently reverses all of this — check yours.
                                 #   (#3m) 2026-07-29 WHY WHOLE ROWS MOVE WITH THIS FLAG (asked once,
                                 #   answered forever): it flips THE growth number (`long_cagr_from`,
                                 #   picks.rs) and by (#14)/(#37) design that ONE value feeds the score,
                                 #   the growth_min_cagr gate, trend, accel, sharpe, calmar, the LEG
                                 #   column AND the PEG divisor — so SCORE/S-8Y/PEG/LEG/rank all move
                                 #   together, on crypto too. Same-cache A/B: NVDA rank 2→13, PEG
                                 #   1.25→1.12, score 12.8→13.3; the rank drop is dilution — life CAGR
                                 #   walks MSFT/ORCL/GOOG/MA past the 19-floor that their 20Y rung
                                 #   fails, tripling the gated field. The CAGR column is whole-life
                                 #   DISPLAY (the "cagr" cell in picks.rs) and does not move with the
                                 #   flag; small CAGR/PE/daily diffs between two manual screen runs are
                                 #   live-quote drift between fetches, not the flag.
  life_cagr_max_years: 0.0       # (#3l) MEASURED OFF 2026-07-29: >0 swaps false-mode's 20/8/5Y rung ladder
                                 #   for a CAGR over the last min(age, N) years (true mode stays whole-life;
                                 #   a name the capped fn declines — <5y history — keeps its rung, so the
                                 #   pool only moves through the growth_min_cagr gate reading a different
                                 #   number). Motivation was the 8→20 dead band: an 18y-old name is ranked
                                 #   on its 8Y window. Two arms on the shipped (#3k) config, stress grid:
                                 #   N=20 20y-s: edge +536.6, top-10 +5.8(+5.6), rank1 +4.8|+3.7 win 81%,
                                 #     h2h 54% — the only >50% h2h ever recorded, but the rank-1 MEDIAN
                                 #     drops 0.7 and the lane edge 87 vs ship +623.5/+6.1(+5.8)/+4.4|+4.4/41%.
                                 #   N=20 12y-s +182.7/+6.0(+5.9)/+2.9|+3.7/28% and 8y-s +75.6/+5.9(+6.5)/
                                 #     +3.4|+3.0/47% lose EVERY metric vs ship (+221.7/+6.7(+6.8)/+3.2|+4.8/
                                 #     33% and +79.2/+6.5(+7.3)/+3.9|+4.5/48%).
                                 #   N=30: clamp never fires at the 20y-hold king (series ≤ ~25y), so its
                                 #     king run IS whole-life under the shipped weights: edge +557.9, still
                                 #     -66 vs the rung — (#3j)/(#3k) re-confirmed with modern weights.
                                 #     12y-s +180.4/28%, 8y-s +74.8/47%: same losses as N=20.
                                 #   Widening the window fixes the dead band and LOSES at every horizon
                                 #   that can see it — the recent-window rung is the signal, not a proxy.
                                 #   Keep 0.0. Inertness verified: knob-0 binary = ship metrics exactly.
                                 #
                                 #   (#73) 2026-08-12 — REPURPOSED, THEN GRADED AND HELD AT 0.0. Everything above this line
                                 #   describes a knob that no longer exists in that form. The RANKING branch it drove in
                                 #   `long_cagr_from` is DELETED — it was measured at -66 edge and shipped off, so this removes a
                                 #   branch that was never taken, and ranking is the 20/8/5Y rung ladder at every value now.
                                 #   The knob is repointed at `growth_min_cagr`'s WHOLE-LIFE REJECT BAR (leg 2), which is the
                                 #   round the two receipts below demanded (`fixed_cagr_years` ~line 777 and `growth_min_cagr`
                                 #   ~line 1524, both now discharged). One reader survives: `picks::life_leg_cagr`, which is
                                 #   `capped_cagr.or(life_cagr)` and is called from BOTH `score_parts` and `gate_failures` so the
                                 #   two can no longer drift. `capped_life_cagr` returns None at or below zero, so the `or` IS the
                                 #   switch: 0.0 = the uncapped lifetime this bar has always used.
                                 #   BEHAVIOUR BY AGE (from `core::capped_life_cagr`'s own guards, unit-tested at core.rs):
                                 #     under 5y  -> None, falls back to life_cagr — IDENTICAL (and unrankable anyway, no 5Y rung)
                                 #     5y..N     -> window clamps to whole life — IDENTICAL
                                 #     over N    -> last N years — THE ONLY GROUP THAT MOVES
                                 #   Two-sided by construction: it ADMITS a compounder whose dead first decade blocks it, and
                                 #   REJECTS a has-been whose early run still flatters a 20Y rung. That second half is why it
                                 #   needed a grid and not an argument.
                                 #   LADDER 0/6/8/10/12/16/20, `backtest {20,12,8} universe stress`, one-field probe arms (each
                                 #   printed `life_cagr_max_years 0->N` and nothing else; the CONTROL ran as a probe file too, so
                                 #   it is the same measurement). Conditions matched across all seven at every horizon — 8y
                                 #   20925 cutoffs 18/8971/11936, 12y 11989 cutoffs 0/2836/9153, 20y 4648 cutoffs 0/15/4633.
                                 #   The growth-scored count is the TREATMENT here and is expected to move; it is listed, not
                                 #   used as a drift check. (11 runs were discarded first: an upstream ETF-listing outage silently
                                 #   swapped the universe 4367 -> 4024 tickers and moved the cutoff denominators, which no config
                                 #   knob can do. Caught by the conditions check, re-run clean.)
                                 #     8y     t3 mean|med   h2h   r1 med   worst   scored   lane edge
                                 #      0.0    +7.7|+8.3    63%    +8.2    -9.2      713      +43.8
                                 #      6      +6.8|+7.1    57%    +9.4    -9.2      795      +40.0
                                 #      8      +7.0|+8.1    46%    +8.4    -9.2      815      +35.5
                                 #     10      +7.5|+8.2    63%    +9.9    -9.2      711      +35.0
                                 #     12      +7.8|+8.3    60%    +9.4    -9.2      677      +46.2
                                 #     16      +8.0|+8.3    57%    +8.2    -9.2      692      +51.3
                                 #     20      +7.7|+8.3    64%    +8.2    -9.2      726      +46.5
                                 #     12y    t3 mean|med   h2h   r1 med   worst   scored   lane edge
                                 #      0.0    +6.8|+6.9    63%    +7.7    -7.8      530     +176.9
                                 #      6      +6.1|+6.8    48%    +8.2    -7.8      538     +143.5
                                 #      8      +6.5|+7.0    48%    +7.2    -7.8      592     +159.5
                                 #     10      +6.7|+6.9    48%    +7.7    -7.8      540     +164.6
                                 #     12      +6.8|+6.9    59%    +7.7    -7.8      510     +173.8
                                 #     16      +7.0|+6.9    67%    +7.7    -7.8      520     +181.8
                                 #     20      +6.7|+6.9    68%    +7.7    -7.8      536     +169.7
                                 #     20y    t3 mean|med   h2h   r1 med   worst   scored   lane edge
                                 #      0.0    +6.3|+5.7    74%    +6.2    -0.5      353     +696.8
                                 #      6      +6.4|+5.9    58%    +6.2    -0.8      351     +577.8
                                 #      8      +6.2|+5.2    60%    +6.0    -0.5      384     +620.7
                                 #     10      +6.4|+5.9    72%    +5.6    -0.5      359     +717.7
                                 #     12      +6.3|+5.7    74%    +6.2    -0.5      351     +723.4
                                 #     16      +6.3|+5.7    74%    +6.2    -0.5      360     +701.9
                                 #     20      +6.3|+5.7    74%    +6.2    -0.5      357     +705.0
                                 #   SHIP RULE v2, PRECEDENCE GUARD > PRIMARY > TIE, STOP AT THE FIRST THAT FIRES:
                                 #      6  20y worst window -0.8 vs -0.5, and 12y h2h 48%. GUARD x2. NO SHIP.
                                 #      8  8y h2h 46%, 12y h2h 48%. GUARD x2. NO SHIP.
                                 #     10  12y h2h 48%. GUARD. NO SHIP. (It also has the round's best 8y rank-1 median, +9.9 —
                                 #         the guard is what stops that from being read as a win, which is the whole point of it.)
                                 #     12/16/20  guards all hold and PRIMARY holds (20y is byte-identical to control for all
                                 #         three; 8y t3 mean +0.1/+0.3/0.0 with the median dead flat at +8.3). They die on the
                                 #         ADDITION bar pre-registered for this round: the knob moves off its 0.0 default, so
                                 #         rank-1 median must RISE, not merely hold. It is FLAT at 12y AND 20y for all three,
                                 #         and flat at 8y for 16 and 20.
                                 #   VERDICT: HOLD 0.0. The code change ships; the value does not.
                                 #   THE ONE JUDGEMENT CALL, RECORDED RATHER THAN BURIED. The ADDITION bar did not say at WHICH
                                 #   horizon the rank-1 median must rise. Arm 12 DOES raise it at 8y (+8.2 -> +9.4) and is flat at
                                 #   the other two, so a lenient reading ("rises somewhere, falls nowhere") would ship 12. Refused,
                                 #   for a reason fixed before the data: the 20y book is byte-identical to control and the 8y t3
                                 #   median is exactly flat, so the entire "win" is one name at one horizon — precisely the argmax
                                 #   noise (#3k) and (#51) both measured and both refused. Shipping on it would be reading the
                                 #   ladder for the arm that flatters, which is what v2 exists to stop. Both readings are written
                                 #   down here so a later session cannot quietly adopt the convenient one.
                                 #   THREE FINDINGS WORTH MORE THAN THE VERDICT:
                                 #   (a) THE 8y t3 MEAN IS MONOTONE IN WINDOW LENGTH — 6.8 / 7.0 / 7.5 / 7.8 / 8.0 for N=6..16,
                                 #       then back to 7.7 at N=20 (= control, as it must be, since ~20y is whole life for nearly
                                 #       every name). SHORT WINDOWS HURT, long ones converge to today. Same shape as the (#51)
                                 #       dose-response and the same mechanism: a shorter leg is a noisier leg for the argmax.
                                 #   (b) THE ARMS THAT DO BEST ARE A NET TIGHTENING, WHICH INVERTS THE FRAMING THAT ORDERED THIS
                                 #       ROUND. The receipts argued the bar was too tight and named 18 blocked compounders. But
                                 #       8y scored counts run 795/815 at N=6/8 and 677/692/726 at N=12/16/20 against control 713 —
                                 #       the best arms admit FEWER names. The two-sided effect dominates once the window is long
                                 #       enough to still contain a has-been's dead years, so what leg 2 mostly buys is REJECTION.
                                 #       The 18-sole-blocks argument is now priced and it was pointing the wrong way.
                                 #   (c) ETF ADMISSION IS A SHORT-WINDOW ARTEFACT: 8y ETF scored 17 at control and at every arm
                                 #       from 10 up, but 31 at N=6 and 24 at N=8. Narrow funds clear a 19%/yr floor only on a
                                 #       recent window. Consistent with the ETF-BLIND paragraph at `growth_min_cagr`.
                                 #   LANE VS BOOK DIVERGED AGAIN, THE SIXTH TIME. Lane edge moves materially (8y +43.8 -> +51.3
                                 #   at N=16; 20y +696.8 -> +723.4 at N=12) while the graded book does not move at all. Lane
                                 #   metrics chose this round's target and were pre-committed NON-VOTING before the first run,
                                 #   which is the only reason that divergence is a footnote here instead of a shipped regression.
                                 #   DO NOT RE-RUN THIS LADDER. Seven arms x three horizons happened; this is their verdict. The
                                 #   knob is now pinned at 0.0 in `validated_tilt_pin` (config.rs) — the first pin it has ever had.
  # (#100 / finding C8) RE-TEST THIS AFTER growth_accel_beta (#98), NOT BEFORE. At 0 a 25-year-old name
  #   is judged on a full-cycle 20Y CAGR dragged through every crash and a 6-year-old on a bull-only 5Y
  #   CAGR, and the two are then ranked head to head — so the ladder is a comparability defect, not only
  #   a PEG-column one. But the v2 refusal on the rank-1 h2h guard graded a BUNDLE: `accel` is
  #   `1Y − β·long_cagr`, so changing WHICH WINDOW long_cagr measures also changes `accel` for every
  #   name in the pool. With β at its shipped 1.0 the two effects are the same size and opposite in
  #   sign, and no run so far can separate them. Under β = 0 the accel leg stops reading long_cagr at
  #   all, and a fixed_cagr_years grid then measures the window question ALONE. Order: β first.
  # (#130) β IS NOW SWEPT AND THE ORDER ABOVE IS DISCHARGED — C8 IS UNBLOCKED. The ladder REFUSED every
  #   rung and β stays 1.0 (receipt at `growth_accel_beta`), but that refuses β as a SHIPPED value, not
  #   as the INSTRUMENT this finding asks for — a β=0 arm still makes the accel leg stop reading
  #   long_cagr, which is the whole reason to run one here. ONE TRAP, and it is the same bundling error
  #   this block was written to avoid, one level up: a β=0 fixed_cagr_years arm must be graded against
  #   the β=0 CONTROL banked in (#130), NEVER against the shipped β=1.0 run. β=0 ALONE costs 20y rank-1
  #   +6.4|+7.2 -> +4.6|+4.3 and h2h 67% -> 56%, and that would otherwise be charged to the window knob.
  # fixed_cagr_years still 0, but the reason has CHANGED and the old one is dead. It said "#15 is redundant
  # because use_trend_cagr overrides a pinned window with the whole-series slope" — use_trend_cagr was then
  # REVERTED two lines up, so #15 sat untested on a stale rationale. (#3j) finally measured it: pinning every
  # name to its 10Y window reads edge +160.1 vs the leg's +167.6, i.e. -7.5, INSIDE the bootstrap band and
  # with a much steadier era table (worst era +111.2 vs +40.5). Best remaining candidate; golden rule still
  # reverts a drop. #16: sweep long_trend_cap (above) per run_me.sh, no flag.
  # (#51) THE DEFECT THIS KNOB FIXES, NAMED 2026-08-06, and it is NOT the one (#3j) measured. (#3j) asked
  #   whether a common window RANKS better and got -7.5 edge. The question nobody asked is whether the
  #   AGE-CHOSEN window makes the `peg` COLUMN UNREADABLE, and it does, because `growth_max_peg` divides
  #   by `long_cagr_pct` — the rung `long_leg` picked, i.e. 20Y for an old name and 8Y for a young one.
  #   TWO ROWS, ONE COLUMN, ONE CEILING, DIFFERENT QUESTIONS (live 2026-08-06, peg 2.5 / leg 5):
  #     VRT   8.0y -> 8Y rung  +47.9%/yr   its last 5y: +65.4%/yr   printed PEG 1.70
  #     AMZN 29.2y -> 20Y rung +23.4%/yr   its last 5y:  +4.7%/yr   printed PEG 1.63
  #   AMZN divides its P/E by a rate it last delivered around 2015 and prints CHEAPER than a name
  #   compounding six times faster today. That is not a valuation number, it is an age number.
  #   MEASURED LIVE AT 8 (screen-pin8.txt vs screen-peg2.5-leg5.txt, ONE-FIELD delta, verified by the
  #   off-validated warning line: `fixed_cagr_years 0->8` and nothing else):
  #     ceiling   no pin                        pin 8
  #     1.6         1  NVDA                       3  NVDA 0.84, EME 0.97, GOOG 1.53
  #     2.0         5                             6  + DELL 1.62, VRT 1.72, AAPL 1.94
  #     2.5         9                            14  + LRCX 2.01, AMAT 2.10, HWM 2.22, ASML 2.31,
  #                                                    APH 2.33, FTNT 2.39, ANET 2.42, AVGO 2.50
  #   MORE NAMES AT EVERY RUNG, INCLUDING THE MEASURED BASE 1.6 — this is the first thing tried in three
  #   rounds that widens the stock table without loosening a single gate. AMZN leaves at every rung (8Y
  #   +11.2%/yr, under growth_min_cagr) — dropped on its own record, not by a blunt cumulative floor.
  #   The entrants are the age cliff's own documented victims: GOOG was cut at 16.23%/yr on its 20Y rung
  #   against 21.4%/yr on 8Y (the GOOGL diagnosis ~line 1645 below), ASML at 18.5%/yr.
  #   ETF AND CRYPTO TABLES ARE BYTE-IDENTICAL (5 and 2 rows either way): growth_min_cagr 19 already
  #   walls funds out, so this is a STOCKS-ONLY effect. 3 ETF rows print `†` = no 8Y leg, pin fell back,
  #   correctly flagged; NO stock row fell back.
  #   THE PIN MOVES ONE OF growth_min_cagr's TWO LEGS AND CREATES A NEW BOTTLENECK. Leg 1 is `long_cagr`
  #   (pinned). Leg 2 is `quote.life_cagr` (whole-life) and NO config can pin it. `cagr-life` sole-blocks
  #   go 2 -> 18: CASY CAT DECK FICO FIX FLEX IBKR JBL KLAC LLY MCK PH STLD TPL TRGP TT URI WSM — names
  #   that now clear the 8-year bar and die on a lifetime average. That is the NEXT round and it is a
  #   CODE question, not this knob. Do not read those 18 as an argument against the pin.
  #   (#73) 2026-08-12 — THAT NEXT ROUND HAPPENED AND THIS PARAGRAPH IS DISCHARGED. Leg 2 can now read a
  #   window (`life_cagr_max_years`, repurposed; receipt at that knob ~line 744), it was graded on a
  #   0/6/8/10/12/16/20 ladder at three horizons, and the verdict is HOLD 0.0 — windowing leg 2 does not
  #   reach the graded book at any length. The 18 were counted under an arm that was REFUSED and the live
  #   count is 2; the grid then showed the useful arms ADMIT FEWER names, not more, so the sole-block list
  #   was arguing for the opposite of what the measurement supports. It is spent as a motivator either way.
  #   GRADED AND REFUSED THE SAME DAY — 5, 8 AND 10 ALL NO-SHIP. `backtest {20,12,8} universe stress`,
  #   one-field arms (each printed `fixed_cagr_years 0->N` and nothing else; control is this file, which
  #   prints no warning at all; baseline re-run at the end of the grid was byte-identical to the first
  #   control except the bootstrap band, which resamples). 20y KING, ctrl -> pin:
  #     pin    lane edge          rank-1 MEDIAN     h2h 20/12/8y      verdict
  #     10     +452.8 -> +437.7   +6.4 -> +6.0      76 / 61 / 64      NO-SHIP (nearest miss)
  #      8     +452.8 -> +432.3   +6.4 -> +4.6      71 / 63 / 50      NO-SHIP
  #      5     +452.8 -> +258.6   +6.4 -> +4.0      48 / 50 / 50      NO-SHIP (fails h2h too)
  #   IT IS THE (#3k) WINNER'S CURSE AGAIN, AND THE DOSE-RESPONSE PROVES THE MECHANISM. 20y rank-1
  #   median runs +6.4 / +6.0 / +4.6 / +4.0 for window length none / 10 / 8 / 5 — MONOTONE in how much
  #   the pin SHORTENS an old name's leg. A 20Y rung averages two decades and is smooth; hand `trend` and
  #   `accel` a shorter leg and the argmax rides the extra noise. Three points on a line, not a fluke.
  #   THE PIN IS GOOD FOR EVERY SLICE EXCEPT THE ONE THAT COUNTS — at 8: bootstrap LOWER bound +32.0
  #   /+13.9/+18.7 across the three horizons, rank 11-20 median +0.3/+1.1/+1.5, 12y and 8y lane edge
  #   +35.9/+8.4. Deeper pool, better downside, rotten argmax = NO-SHIP, the same asymmetry that sank
  #   rung 15 at growth_min_cagr and growth_max_peg 2.0. The bar is ALL clauses, not any.
  #   THE LEGAL SET IS NOW CLOSED: 1 (accel identically zero), 2 (cohort mean -811.0 fwd at 20y), 5, 8,
  #   10 all refused; 20 falls back for almost every name, so it is not a pin at all. NOTHING TO RE-RUN
  #   HERE. Do not re-open this knob without a NEW reason — an earlier session burned a round rebuilding
  #   a ladder that already existed (see growth_min_cagr), and this is exactly that trap's shape.
  #   *** THAT "CLOSED" WAS EARNED UNDER A RULE THAT NO LONGER EXISTS — amended SAME DAY, hours later,
  #   when the primary metric moved from rank-1 to top-3 (SHIP RULE v2, top of file). Every refusal
  #   above was decided on the rank-1 median. Re-graded on the basket actually bought, same files,
  #   no new runs (excess pts/yr, mean|median):
  #     arm      20y t3        12y t3        8y t3         verdict under v2
  #     control  +6.5|+6.6     +7.3|+7.8     +8.1|+8.5     —
  #     pin 10   +6.6|+6.9     +7.5|+8.0     +7.6|+8.2     UNRESOLVED (wins 20y AND 12y, loses 8y)
  #     pin 8    +6.5|+6.9     +7.1|+7.6     +7.7|+8.5     NO-SHIP (8y mean -0.4, 20y worst -1.0)
  #     pin 5    +6.0|+6.7     +6.9|+7.5     +7.6|+7.9     REFUSED — h2h guard broke (48/50/50)
  #   PIN 10 IS NOT THE DISASTER RANK-1 CALLED IT: it beats control on book, excess, median AND win%
  #   at 20y (+13.8 vs +13.7, 97% vs 97%, worst -0.5 = -0.5) and at 12y (+15.4 vs +15.2, 89% vs 87%).
  #   It fails ONLY at 8y (worst window -11.1 vs -9.2). Under v2 that is UNRESOLVED, so it STILL DOES
  #   NOT SHIP — the tie-break is no-ship, not king-wins.
  #   THE THREE ARMS LAND IN THREE DIFFERENT STATES, and the next session must not flatten them:
  #     pin 10  UNRESOLVED — the primary metric SPLITS by horizon (20y/12y up, 8y down). Re-openable
  #             by an arm that fixes the 8y worst window without giving back 20y/12y. None proposed.
  #     pin 8   NO-SHIP    — no split to resolve: 8y median is FLAT (+8.5 = +8.5), so the horizons
  #             never disagree; it simply loses on 8y mean and on the 20y worst window. Measured,
  #             refused, closed. (An earlier draft of this block called it UNRESOLVED. Wrong: a flat
  #             median is not a disagreeing one. The mechanical grade is the record.)
  #     pin 5   REFUSED    — the winner's-curse GUARD broke (rank-1 h2h 48/50/50, i.e. the argmax
  #             stops beating ranks 11-20 at all). A broken guard refuses OUTRIGHT; it is never
  #             softened into "unresolved" by the primary metric or the tie-break. Precedence:
  #             GUARD > PRIMARY > TIE.
  #   STILL NOTHING TO RE-RUN — the numbers above are the re-grade of the SAME output files, and
  #   re-running these three arms reproduces them.
  #   THE LIVE WIN IS STILL REAL, AND THAT IS THE POINT: the pin buys +5 stocks and a readable PEG, and
  #   costs argmax quality, because ONE knob moves BOTH. `fixed_cagr_years` pins the whole score —
  #   the gate, trend, accel, sharpe, calmar, trend_health AND the LEG column — but the defect at the
  #   top of this receipt is ONLY in `growth_max_peg`'s denominator. The measurement says the damage
  #   lives in trend/accel at rank 1, NOT in the PEG gate. So the surgical fix is a SEPARATE window for
  #   the PEG denominator alone, leaving the ranking legs on the age ladder that the backtest keeps
  #   voting for. That is next round's code change and it is the only version of this idea still alive.
  #   LEGAL VALUES ARE ONLY THOSE IN core::HORIZONS: 1, 2, 5, 8, 10, 20. A 3/4/6/7/12 misses the
  #   `perf_pct("{N}Y")` lookup, falls back to the age ladder, and the knob SILENTLY DOES NOTHING.
  #   1 and 2 are refused on prior measurement, not taste: at a 1Y pin `accel = 1Y - long_cagr` is
  #   identically zero, deleting the score's heaviest term (weight 0.65, picks.rs #49), and the 2Y
  #   cohort measures mean -811.0 forward at the 20y horizon (growth_min_leg_years below).
  #   BOUNDARY IS 7.91y, NOT 8.00 — `horizon_changes` (core.rs) blanks a leg only when the series starts
  #   more than 31 days after the 2920-day target. The slack is deliberate: pre-daily-window history is
  #   monthly, so an exact test would blank an old name's leg over bar PLACEMENT rather than missing data.
  growth_min_leg_years: 5.0      # (funnel) MEASURED AND REJECTED 2026-08-03 — shortest rung `long_leg` may use,
                                 #   on a 20/8/5/2Y ladder. 5.0 = the ladder as it has always been. The funnel
                                 #   (07a941f) says the GROWTH lane's whole volume dies on ONE gate no config
                                 #   reaches: `history`, 1949 of 4748 EU-buyable names with no 5y+ leg, hence
                                 #   no CAGR, hence unrankable. `fixed_cagr_years` above only PINS to a leg
                                 #   that exists; this is the only knob that creates one. Offline count over
                                 #   the 3435 cached charts (887 span <5y): a 2Y rung gives 828 of them a leg,
                                 #   ~120 clearing the 19%/yr floor. NOT a 3Y rung — core::HORIZONS has no 3Y
                                 #   and Quote.perf is a positional Vec aligned to it, so the knob has only
                                 #   three settings (5, 2, 1) and no gentler middle. (#49) added the 1Y rung;
                                 #   it is near-inert, see growth_trust_young below.
                                 #
                                 #   (#152) THE COVERAGE HYPOTHESIS IS REFUTED, 2026-08-29. MEASURED, OFFLINE,
                                 #   NO KNOB MOVED. (#127) closed on "the largest genuinely single-gate cohort at
                                 #   every horizon is `history` (no 5y+ record), which is a data-coverage ceiling
                                 #   ... that is the only rung this table endorses looking at next, and it is a
                                 #   COVERAGE problem, not a threshold to sweep." That framing contains a testable
                                 #   claim and nobody had tested it. A name can miss a 5y leg for two unrelated
                                 #   reasons and only ONE of them is recoverable:
                                 #     TRUNCATED  an old company whose price series we are served short. Fixable
                                 #                upstream, and fixing it admits real names AT THE SHIPPED GATE —
                                 #                the only pool widening that does not pay (#151)'s quality tax.
                                 #     YOUNG      a company that genuinely has not traded 5 years. Nothing to fix;
                                 #                the gate is correct and the ceiling is structural.
                                 #   THE DISCRIMINATOR WAS ALREADY ON DISK and needed no request. Yahoo's chart
                                 #   payload carries `meta.firstTradeDate` — when the security actually began —
                                 #   which is independent of how many bars the window returned. Compare it against
                                 #   the served first/last timestamp and the two causes separate exactly.
                                 #   OVER `.long_history_cache.json` (6450 tickers, 5935 carrying both fields):
                                 #     instrumentType    cached   span<5y   first traded >=5y ago (= TRUNCATED)
                                 #     ETF                 4613      2428        21
                                 #     EQUITY              1147       138         0
                                 #     CRYPTOCURRENCY        96        36         0
                                 #     MUTUALFUND            69        11         0
                                 #     CURRENCY               4         1         1
                                 #     ECNQUOTE / INDEX     4 / 2       0         0
                                 #   ZERO OF 138 SHORT-HISTORY EQUITIES ARE TRUNCATED. Not "few" — zero. And they
                                 #   are not near-misses either: their actual trading life is
                                 #     n=138  min 0.01y  p25 0.21y  MEDIAN 0.53y  p75 2.00y  max 4.96y
                                 #   Half have existed under SEVEN MONTHS. This cohort could not carry a 5y record
                                 #   under any fetch fix, because there is no history to fetch.
                                 #   THE 22 NON-EQUITY HITS ARE A DIFFERENT BUG, NOT THIS ONE. Every one is a
                                 #   ONE-BAR payload (served span 0.0y, a single timestamp) against a
                                 #   firstTradeDate 8-14y old — 21 ETFs and one FX pair. That is a fetch stub, not
                                 #   a truncated equity history, and the growth STOCK lane never ranks them. Filed
                                 #   as a live ETF-lane coverage bug and deliberately NOT fixed here; see the note
                                 #   at the end of this block.
                                 #   SAMPLE CAVEAT, STATED NOT BURIED: 1147 cached equities against the ~4748
                                 #   live EU-buyable names the 1949 figure above is drawn from — about a quarter,
                                 #   and it is the quarter this machine has actually fetched, so it over-weights
                                 #   names the screen already looks at. It is a SAMPLE. It is also a count of
                                 #   ZERO across 138, with a median life of six months, which is not the shape a
                                 #   sampling artifact takes.
                                 #   CONSEQUENCE — THE LAST ENDORSED RUNG IS CLOSED, and with it every documented
                                 #   lever on growth ADMISSION:
                                 #     selection            (#127)  out-ranked 0.2%/0.0%/1.5% of missed wealth
                                 #     one gate loosened    (#151)  both arms REFUSED on PRIMARY, 2026-08-29
                                 #     two gates loosened   (#128)  all three pre-registered bars failed
                                 #     wider universe       (#134)  blocked on a survivorship-free PRICE source
                                 #                          (#153)  block CONFIRMED on dead names, 0 of 30 and
                                 #                                  0 of 703 usable; dates are free, prices are not
                                 #     coverage             (#152)  refuted here — the rejects are genuinely young
                                 #   Do not re-open admission on argument alone. A future proposal needs a NEW
                                 #   measurement, not a re-reading of these.
                                 #   TO REPRODUCE (no network, no build, ~20s): the script is inlined so no
                                 #   instrument has to be built and maintained for a question that is now closed —
                                 #   the same standard (#127) set for itself ("NO KNOB WAS CHANGED ON THE STRENGTH
                                 #   OF THIS. The instrument is `print_recall_capture`, which still ships false").
                                 #     import json, datetime, collections
                                 #     d = json.load(open('.long_history_cache.json'))
                                 #     by, short, trunc = (collections.Counter() for _ in range(3))
                                 #     for t, v in d.items():
                                 #         try:
                                 #             r = v[1]['chart']['result'][0]; m = r['meta']
                                 #             ts = r.get('timestamp') or []; ftd = m.get('firstTradeDate')
                                 #             if not ts or not ftd: continue
                                 #             it = m.get('instrumentType', '?')
                                 #             f = datetime.date.fromtimestamp(ftd)
                                 #             s = datetime.date.fromtimestamp(ts[0])
                                 #             e = datetime.date.fromtimestamp(ts[-1])
                                 #             by[it] += 1
                                 #             if (e - s).days / 365.25 < 5.0:
                                 #                 short[it] += 1
                                 #                 if (e - f).days / 365.25 >= 5.0: trunc[it] += 1
                                 #         except Exception: pass
                                 #     for k, n in by.most_common(): print(k, n, short[k], trunc[k])
                                 #   REVERT RULE: none — nothing shipped. This receipt is the deliverable. It is
                                 #   INVALIDATED only by a price source that serves longer histories for the SAME
                                 #   tickers, which would change firstTradeDate for none of them but could change
                                 #   the served span; re-run the snippet against that cache before citing this.
                                 #   THE ETF ONE-BAR STUB (byproduct, filed not fixed): 21 ETFs return a single
                                 #   bar against an 8-14y firstTradeDate, e.g. 0Y07.L (first traded 2012-06-11),
                                 #   EMUM.L and 0MUL.L (2013-09-18), 020Y.L (2015-01-21), USCO.L (2016-02-17).
                                 #   The screen HAS an ETF lane, so this is a live coverage bug on shipped output,
                                 #   not a backtest curiosity. [CORRECTED BY (#158) DIRECTLY BELOW: the fetch is
                                 #   well-formed and the hole is upstream — there is no bug on our side to fix.]
                                 #   It is out of scope here because this block is about
                                 #   the growth STOCK gate; it wants its own round and its own receipt.
                                 #   (#158) ANSWERED 2026-08-29 — IT IS AN UPSTREAM HOLE. No code shipped,
                                 #   and this receipt CORRECTS the sentence above: "a live coverage bug on
                                 #   shipped output" is wrong in the part that matters. The fetch is not
                                 #   defective and there is nothing on our side to repair.
                                 #   THE STUB IS THE LIVE QUOTE, NOT A BAR. In every one of the 21 payloads
                                 #   the single timestamp carries close == meta.regularMarketPrice to the
                                 #   last digit and volume == meta.regularMarketVolume, dated the day before
                                 #   the cache was written:
                                 #     0Y07.L  close 223.8999938964844  rMP 223.9     vol 24     rMV 24
                                 #     EMUM.L  close  82.33000183105469 rMP  82.33    vol 24     rMV 24
                                 #     020Y.L  close   3.063800096511841 rMP  3.0638  vol 39     rMV 39
                                 #     USCO.L  close   7.346499919891357 rMP  7.3465  vol 0      rMV 0
                                 #     0A08.L  close 948.3275756835938  rMP 948.3276  vol 13336  rMV 13336
                                 #   Yahoo is echoing today's quote as a one-row series because it has no
                                 #   series to send. THE REQUEST IS WELL-FORMED: `range=max` is accepted and
                                 #   echoed back, `validRanges` advertises max and 10y, and the requested
                                 #   granularity is honoured (1mo, and 1wk for 0A08.L). So it is not a bad
                                 #   range/interval — the two candidate causes (#152) named are both excluded,
                                 #   and the third, a genuine upstream hole, is what is left.
                                 #   WHAT THE 21 ARE: secondary listings, overwhelmingly non-primary CURRENCY
                                 #   lines on LSE's international board — MXN (0A08.L, 0A3G.L), NZD (0A09.L),
                                 #   CHF (0VPX.L, 0VPY.L, 0Y2C.L, 0Y3O.L, 0DB5.L), plus GBp and TRY lines.
                                 #   Refreshed census: 51 cached ETF entries hold <=1 bar; 21 of them are
                                 #   >=5y old, which is the figure (#152) quoted and it still stands.
                                 #   NONE OF THE 21 IS IN `.eu_listing_cache.json` (519 entries, checked).
                                 #   THE REMEDY ALREADY EXISTS AND IS ALREADY CORRECT TO REFUSE THEM.
                                 #   `bridge_hint_lines` proposes a same-benchmark twin and `history_proxy`
                                 #   applies it, but the splice REJECTS a currency mismatch — and every
                                 #   available twin here is a different currency, which is precisely why
                                 #   nothing was bridged. Overriding that guard reproduces the failure this
                                 #   file already records: 0A08.L spliced an MXN head onto a GBP tail for a
                                 #   x19.59 move in 28 days. 0A08.L IS ONE OF THESE 21. The guard is not in
                                 #   the way of the fix; it is the reason there is no fix.
                                 #   WHY NO AUTO-BRIDGE WAS BUILT, stated as the measurement it was: matching
                                 #   a stub to a covered sibling by fund name was attempted twice and is
                                 #   unreliable in BOTH directions — exact normalised equality pairs 1 of 21,
                                 #   token-overlap pairs 18 of 21 while emitting obvious false pairs (two
                                 #   unrelated iShares funds scoring 1.0 because their names reduce to the
                                 #   same surviving token). THE MATCHER IS THE HARD PART, and a wrong match
                                 #   is not a visible error — it is a silent x19 splice into the price
                                 #   history. Do not build a retry layer, a bridge heuristic, or a symbol
                                 #   alias table for a hole that is not ours to fill.
                                 #   CONSEQUENCE: 21 ETF LINES ARE UNRANKABLE, AND THE LANE ALREADY HANDLES
                                 #   THAT CORRECTLY. No history means they fail the leg/CAGR gates and are
                                 #   never ranked; they are correctly EXCLUDED, not silently mispriced. The
                                 #   cost is that these listings cannot be screened, not that the screen is
                                 #   wrong about them.
                                 #   NOT ESTABLISHED, AND DELIBERATELY NOT CLAIMED: whether every affected
                                 #   FUND is reachable through some other covered listing. The name matcher
                                 #   above is exactly the tool that would answer it and is exactly the tool
                                 #   this receipt just showed to be untrustworthy. Anyone citing that claim
                                 #   must establish it by hand, per name.
                                 #   REVERT RULE: none — nothing shipped. INVALIDATED by a price source that
                                 #   serves a real series for these tickers; re-run the payload check above
                                 #   (close vs regularMarketPrice) before re-opening.
                                 #   THE 2.0 ARM WAS GRADED AND LOSES EVERY CLAUSE OF THE SHIP RULE. Six-run
                                 #   stress grid 2026-08-03, `backtest {20,12,8} universe stress`, baseline vs arm
                                 #   differing by THIS FIELD ALONE (verified: the drift warning names
                                 #   `growth_min_leg_years 5.0->2.0` and nothing else):
                                 #     horizon   lane edge        rho        rank-1 mean|med    h2h     top-10
                                 #     20y-s   +411.0 -> +244.2  +.14->+.11  +5.9|+6.0 -> +4.5|+4.0  67->39%  +5.8->+6.3
                                 #     12y-s   +121.9 -> +104.8  +.13->+.10  +5.5|+6.5 -> +5.4|+6.1  61->47%  +6.2->+6.7
                                 #      8y-s    +48.1 ->  +45.5  +.19->+.15  +6.9|+7.8 -> +7.4|+7.1  61->50%  +6.7->+7.4
                                 #   Rule was (max_1m_drop_pct's bar, 2026-08-02): rank-1 median up or held at the
                                 #   20y king, h2h >=50% at all three, king lane edge not down. Actual: king median
                                 #   -2.0, h2h BELOW 50 at two of three, king edge -41%. Three for three against.
                                 #   THE TOP-10 COLUMN IS THE TRAP: it RISES at every horizon, and that is the
                                 #   dilution signature, not a win. h2h says why — "#1 beats the 11-20 book" falls
                                 #   67->39% at the king, i.e. under the arm THE TOP OF THE LIST IS WORSE THAN ITS
                                 #   MIDDLE. A wider book of young momentum names lifts the average while destroying
                                 #   the ranking that the tool exists to produce. Grade rank-1 and h2h, not top-10.
                                 #   NOT a sample-size problem — the arm DEEPENS the pool (growth windows 517->686,
                                 #   +33%) and rho still falls at all three horizons. More data, worse selection.
                                 #   The baseline sweep row prices the same cohort and is MONOTONE IN HORIZON:
                                 #     20y-s n=169 mean -811.0 | med -793.9      <- the horizon this tool targets
                                 #     12y-s n=251 mean  -41.3 | med -184.7
                                 #      8y-s n=300 mean  +14.4 | med  -58.7
                                 #   The 2Y rung's damage GROWS with the holding period. An 8y reader could mistake
                                 #   it for noise; at the 20-year hold this tool is built for it is catastrophic.
                                 #   LIVE A/B same day (one universe fetch, 4707-4749 names): the arm works exactly
                                 #   as designed and that is the problem — `history` sole-blocked 11/1922/19 ->
                                 #   4/1185/7 (756 names gain a leg), cleared 9->22, ETF table 5->16 rows, and
                                 #   rank 1 became a 2-3y listing in ALL THREE classes (GEV 2.3y score 26.2 over
                                 #   NVDA 27.5y 10.8; HNSC.MI 3.4y 29.9 over IITU.L 10.7y 8.1; WBT-USD 3.9y).
                                 #   Rank stability 89% -> 33%. `trust_factor` HAD already halved them — 29.9 is the
                                 #   post-halving number — and their ABV-MA reads 0% with MAXDD -20..-38%, because
                                 #   the risk brakes cannot see a drawdown that has not happened yet. A short record
                                 #   collects score for the ABSENCE OF MEASURED BAD NEWS. That is the mechanism.
                                 #   Do not re-propose without a NEW damp aimed at that mechanism (a record-length
                                 #   term in the score itself, not a post-hoc multiplier); re-running this grid
                                 #   unchanged will reproduce these numbers. Knob KEPT, wired and probe-able, so the
                                 #   next attempt costs one config line instead of a patch to `long_leg`.
                                 #   0.0 IS NOT "OFF" HERE. Nearly every other knob in this file uses 0 = off;
                                 #   this one is a FLOOR, so 0.0 is the LOOSEST setting there is (every rung
                                 #   allowed, 2Y included) — identical in effect to 2.0. "Off" for this knob is
                                 #   5.0, the value that reproduces the historical 20/8/5 ladder. Set 0.0 by
                                 #   reflex on 2026-08-03 and `shipped_config_pin` caught it; that is what the
                                 #   pin is for.
                                 #   (#49) RE-PROPOSED AND REFUSED AGAIN 2026-08-05, this time WITH the record-
                                 #   length ladder underneath it. See growth_trust_young below for the grid.
                                 #   (#50) AND NOW MEASURED FROM THE OTHER END — 2026-08-05, on a live overlay that
                                 #   ships `growth_min_leg_years: 0`. Question was "why do only 2 stocks rank"; the
                                 #   answer is that THIS LINE IS THE ONE THAT MATTERS, and it is the whole fix:
                                 #     arm     what moved from LIVE       20y med|h2h|edge     8y med|h2h|edge
                                 #     LIVE    (overlay as shipped)      +4.3| 48%|+183.1    +7.1| 49%|+57.7
                                 #     E       leg 0 -> 5 ONLY           +6.7| 65%|+265.0    +8.2| 58%|+59.5
                                 #     A       E + range 50 -> 80        +6.7| 68%|+287.0    +8.4| 57%|+65.5
                                 #   LIVE's h2h is BELOW 50% at all three horizons (48/46/49) — its own #1 pick loses
                                 #   to its ranks 11-20. That is an INVERTED ranking, not a weak one, and it is the
                                 #   direct cost of the loose floor. Arm E — this line alone — does nearly all of
                                 #   arm A's work; the range revert adds ~3 h2h pts at 20y and nothing at 8y.
                                 #   THE COUNT TRADE DEPENDS ENTIRELY ON WHAT IT IS PAIRED WITH — corrected same day
                                 #   after the pairing was actually measured instead of assumed:
                                 #     leg 5 alone                 25 -> 8   (stocks 2 -> 1, ETFs 20 -> 5, coins 3 -> 2)
                                 #     leg 5 + peg 2.0             25 -> 12  (stocks 2 -> 5)
                                 #     leg 5 + peg off             25 -> 23  (stocks 2 -> 16)  <- NOT a recommendation,
                                 #                                                                see growth_max_peg
                                 #   The ETF loss (20 -> 5) is fixed at 15 young funds and is the real cost; the stock
                                 #   side is set by `growth_max_peg`, not by this knob. Reporting "this line costs you
                                 #   names" without naming the PEG rung it was paired with overstates it by 11 stocks.
                                 #   The peg-off row is here to make that arithmetic legible, NOT as a config: it is
                                 #   retracted at growth_max_peg (rank 1 becomes MPWR at P/E 96 with earnings -60.6%,
                                 #   and 0.0 also unarms the loss-maker screen). The shipped pairing is peg 2.0.
                                 #   Age is the tell — LIVE's
                                 #   ranked ages run 2.4, 27.5, 3.4, 2.1, 2.2, 2.3, 2.3, 2.3, 3.1, 3.4, 1.7, 2.1,
                                 #   16.6, 1.8, 5.7, 8.7, 11.8, 4.0 (median under 3 years) on a list meant to be
                                 #   held 20; leg 5 + peg 2.0 gives AAPL 41.7y, AMZN 29.2y, NVDA 27.5y, DELL 10.0y and
                                 #   VRT 8.0y — five names, all with growing earnings, none under 8 years of record.
                                 #   The floor removes ARM 2.9y, GEV 2.4y and SNDK 1.5y from the stock table.
                                 #   So the two symptoms — "only 2 stocks" and "rank 1 is a 2.4-year listing" — are
                                 #   ONE setting pulling both ways: it does not add stocks (it adds young ETFs) and
                                 #   it inverts the order. The count problem lives at `growth_max_peg`, not here.
                                 #
                                 #   (#151) SWEPT TO 2.0 AND REFUSED 2026-08-29 — admits names EQUAL to the book (+0.2 vs the accepted
                                 #   median) and improves the 8y worst window -5.5->-2.8, but top-10 mean falls
                                 #   +3.7->+2.7 (12y) and +5.3->+4.6 (8y). PRIMARY fails. See (#151) at the top.

  growth_trust_young: 0.5        # (#49) MEASURED AND NOT SHIPPED — the 2Y rung of `growth_trust_ladder`; the 1Y
                                 #   rung takes half of it. 0.5 reproduces the (#47) ladder's flat `none -> 0.50`
                                 #   bottom exactly, so this line is the DEFAULT written down, not a change.
                                 #   THE QUESTION: rank young assets too (so a promising new name can surface),
                                 #   but score a name higher the LONGER its proven record — 2+ < 5+ < 8+ < 20+,
                                 #   with a <=1y record ranked yet penalized. Motivation is the PT tax line: at
                                 #   28% on equities the real hold is 8+ or 20+ years, so the screen must rank
                                 #   for those horizons and nothing shorter.
                                 #   WHY IT WAS WORTH A RUN. Both halves had been measured and refused, but only
                                 #   SEPARATELY, and each refusal disabled the other half:
                                 #     - `growth_min_leg_years: 2.0` (above) ran with the ladder OFF, so every
                                 #       newly-admitted 2-year name took the old cliff's flat 0.5 — the SAME dock
                                 #       as a 7-year name. Its own receipt says so: "a 2Y-rung name arrives at
                                 #       half score — a damp, not a substitute for measuring this."
                                 #     - `growth_trust_ladder` arm D ran at floor 5, so NO young name existed in
                                 #       the pool for the ladder to dock. Its bottom rung never fired.
                                 #   Admit-and-grade together was the one arrangement nobody had run. To run it
                                 #   the ladder needed a bottom (this knob) and `long_leg` needed a 1Y rung
                                 #   (added; legal because 1Y is in core::HORIZONS, unlike the impossible 3Y).
                                 #   THE GRID — 9 runs, `backtest {20,12,8} universe stress`, 2026-08-05. Arms
                                 #   are full copies of this file differing by TWO lines (floor + ladder:true),
                                 #   diff-verified. Baseline re-run on the new binary as the drift control and
                                 #   reproduced every metric of the 2026-08-04 baseline EXACTLY (rho/edge/winsor/
                                 #   OOS identical at all three horizons; only the unseeded bootstrap band moved)
                                 #   — that is the proof the code change is byte-identical at the default.
                                 #     arm            horizon  lane edge   rho    rank-1 mean|med   h2h          top-10
                                 #     baseline         20y     +452.8   +0.16   +6.2|+6.4        16/21 (76%)   +5.8
                                 #     F2 floor2+ladder 20y     +250.9   +0.12   +4.7|+4.0         9/23 (39%)   +6.2
                                 #     F1 floor1+ladder 20y     +250.9   +0.12   +4.7|+4.0         9/23 (39%)   +6.2
                                 #     baseline         12y     +127.9   +0.07   +5.8|+7.2        19/30 (63%)   +6.4
                                 #     F2               12y      +94.4   +0.06   +5.4|+5.9        18/35 (51%)   +6.9
                                 #     F1               12y      +94.4   +0.06   +5.4|+5.9        18/35 (51%)   +6.9
                                 #     baseline          8y      +65.0   +0.17   +7.2|+8.2        25/38 (66%)   +7.0
                                 #     F2                8y      +50.8   +0.14   +7.0|+5.8        20/43 (47%)   +7.7
                                 #     F1                8y      +53.2   +0.14   +7.0|+5.8        20/43 (47%)   +7.7
                                 #   Pre-registered DUAL MANDATE (fixed before any number existed): at 20y AND 8y,
                                 #   rank-1 median >= baseline and h2h >= 50%, then top-N book up at both. Actual:
                                 #   rank-1 median -2.4 at 20y and -2.4 at 8y, h2h 39% and 47% — BOTH gates fail at
                                 #   BOTH mandated horizons. Nothing ships.
                                 #   THE LADDER DID NOT RESCUE THE FLOOR. Against the 2026-08-03 floor-2 arm that
                                 #   ran WITHOUT it (20y +244.2 edge, +4.5|+4.0, 39% h2h), adding the graded dock
                                 #   bought +6.7 lane edge and EXACTLY NOTHING on rank-1 (+4.0 -> +4.0) or h2h
                                 #   (39% -> 39%). The hypothesis this round was built on is refuted precisely: a
                                 #   post-hoc score multiplier does not repair a bad admission. That is what the
                                 #   growth_min_leg_years receipt above already demanded — "a record-length term in
                                 #   the score itself, NOT a post-hoc multiplier" — and this is the run that
                                 #   confirms the demand was right. Anything shaped like this knob will fail too.
                                 #   NO DOCK VALUE RESCUES IT EITHER. The curve sweeps 0.7..0.1 inside each arm
                                 #   (sound: trust is a pure score multiplier and every gate in `score_parts`
                                 #   returns before it, so admission is invariant to it — the FLOOR is what moves
                                 #   admission, which is why the floor moved in arms and only the dock was swept):
                                 #     w      20y edge   12y edge   8y edge    8y OOS 2nd half
                                 #     0.70    +249.9     +109.4     +59.0        +0.12
                                 #     0.50    +250.9      +94.4     +50.8        +0.10
                                 #     0.40    +294.7      +87.9     +58.2        +0.09
                                 #     0.30    +308.3      +90.1     +59.5        +0.07
                                 #     0.20    +298.5      +91.3     +57.1        +0.05
                                 #     0.10    +335.3      +76.6     +60.9        +0.04
                                 #   HORIZON-CONFLICTED and OVERFIT AT THE HARSH END: 20y wants the harshest dock
                                 #   (+335.3 at 0.10) while 12y wants the softest (+109.4 at 0.70), and at 8y rho
                                 #   decays monotonically +0.15 -> +0.12 while the SECOND OOS half collapses +0.12
                                 #   -> +0.04. The harsh rungs are fitting the first half of the sample. And the
                                 #   best cell in the whole table (+335.3) is still 26% BELOW the baseline it is
                                 #   trying to beat (+452.8), so no value of this knob buys back what admitting the
                                 #   young names costs. TIE-BACK EXACT: the 0.70 curve row reproduces the
                                 #   `growth_trust_young->0.7` ablation row at all three horizons (+249.9/+109.4/
                                 #   +59.0), and at the shipped floor the curve prints a FLAT line (no young-rung
                                 #   name survives the `history` gate to be docked) — the expected null.
                                 #   THE 1Y RUNG IS NEAR-INERT, which is its own finding. F1 and F2 are identical
                                 #   to the digit at 20y and 12y and differ by 5 scored windows out of 1158 at 8y.
                                 #   `horizon_changes` fills the 2Y leg whenever 2y of history exists, so the 1Y
                                 #   rung only ever catches a name with 1-2y of record that ALSO survives the full
                                 #   forward horizon — a cohort that barely exists. The rung is kept (it is what
                                 #   makes `growth_min_leg_years: 1.0` mean anything) but it buys no coverage.
                                 #   IT IS ALSO NOT THE ONLY DOCK ON A 1Y NAME, which matters for reading the
                                 #   curve: that name's long CAGR IS its 1Y return, so `accel` — weight 0.65, the
                                 #   heaviest term in the score — is structurally ZERO for it. Pinned by a test.
                                 #   THE TOP-10 TRAP REPRODUCED, third time now: top-10 excess RISES in every arm
                                 #   at every horizon (+5.8->+6.2, +6.4->+6.9, +7.0->+7.7) while rank-1 and h2h
                                 #   collapse. Read h2h. At 8y the arm's mean rank-1 is nearly flat (+7.2->+7.0)
                                 #   but its MEDIAN drops +8.2->+5.8 — fatter tails, worse typical outcome, which
                                 #   is the wrong trade for a buy-and-hold book.
                                 #   Knob KEPT at the default so the null stays reproducible via the ablation row
                                 #   and the curve, exactly as growth_trust_ladder is kept. Do not re-propose the
                                 #   admit-young idea as another multiplier; the next attempt has to change what
                                 #   gets ADMITTED or how the score is BUILT, not what it is multiplied by.

  # (B) Value-trap dock: a name bleeding for YEARS (1Y AND 5Y both deep red) is not a dip — dock it.
  sustained_decline_pct: -40.0   # [FOIL] both 1Y and 5Y must be <= this to count as a multi-year bleed
  sustained_decline_penalty: 0.4 # [FOIL] score ×this when that holds (0..1; lower = harsher)
  # Harsher tier for a 7y+ deep bleed (e.g. LTC: 5Y -73% riding an ancient 10Y pump): if the 5Y is
  # past deep_decline_pct, use the steeper deep_decline_penalty instead of the -40% one.
  deep_decline_pct: -70.0        # [FOIL] 5Y <= this = a years-deep bleed, not a recoverable dip
  deep_decline_penalty: 0.15     # [FOIL] score ×this then (lower than sustained_decline_penalty)

  # (A) Hide ranked rows scoring <= this so the per-class tables stop padding to top_picks with
  # near-the-high names (score ~1-2 = not really "on sale"). Fewer rows when few names qualify.
  min_score: -100.0              # [FOIL] on-sale trim. (The LIVE growth equivalent is growth_min_score below.) 0 = show all
                                 #   EVERY NEGATIVE VALUE IS THE SAME VALUE (read off the code 2026-08-02, no run needed):
                                 #   `ranked` trims with `*s > min_score.max(0.0)` (picks.rs), so the floor is CLAMPED AT 0
                                 #   and -20, -100 and -1e6 all mean the identical "show everything above 0". Turning this
                                 #   more negative is not a knob turn and no backtest can separate the settings. The -20 here
                                 #   is therefore cosmetic — it documents intent, not behaviour. To actually admit
                                 #   negative-scored on-sale rows, the clamp has to go first; that is a code change, not config.
                                 #   (Deliberately NOT done: the trim's whole job is dropping near-the-high padding rows.)

  # (C) Structural-cheap reward: how far below the ~200wk trend line (SMA = Simple Moving Average) the
  # price sits. Complements the OFF-HI drawdown (distance below the peak) with "cheap vs the long trend".
  cheap_weight: 0.07             # [FOIL] (#4) reward per % below the 200wk SMA — halved from 0.15: "structural cheap"
                                 #   is another dip term the backtest doesn't reward; demoted toward trend/quality.
  cheap_cap: 60.0                # [FOIL] cap the below-SMA % fed in

  # --- SCORE — SHARED / LIVE tilts (read by the growth lane `screen` prints) ---
  # (D) Dividend reward: REVIVED 2026-07-25 at 0.5, now scored NET of Portuguese tax (see tax_keep_*).
  #   HISTORY: shipped 1.5, demoted to 0 on 2026-07-15 because it is BACKTEST-BLIND and its rationale
  #   (price-only CAGR misses reinvested dividends) was tested DIRECTLY by the use_adjusted_close
  #   total-return probe, which came back NO-EDGE (reverted false 2026-06-28).
  #   (#53) RETRACTION 2026-08-07 — "IMPOSSIBLE" WAS FALSE, AND THE TERM IS NOW MEASURED.
  #   This block used to say a walk-forward grade was not merely pending but IMPOSSIBLE, because
  #   `backtest_quote` could not reconstruct as-of dividends, and it cited that to ship the term ungraded
  #   and to keep it out of the growth ablation list. It was never true. `Chart.divs` carries
  #   (ex-date, amount) for the whole history and arrives in the SAME response as the closes — it was
  #   simply dropped on the floor at the fetch destructure in commands/backtest.rs. `dividends_in_window`
  #   and `dividend_sums` already existed and already did the work. Plumbing it cost one extra parameter
  #   on `backtest_quote`, and look-ahead safety is STRUCTURAL: that function already slices
  #   `&dates[..=as_of]` and `dividends_in_window` anchors on `*dates.last()`, so an as-of yield cannot
  #   reach a future ex-date by construction. "Impossible" should have been read as "unplumbed" the first
  #   time; the price of not checking was a scoring term shipping on argument alone.
  #   THE MEASUREMENT (walk-forward, wide universe of 4982 tickers, `dividend_weight` 0.0 vs the shipped
  #   0.5, both arms on THIS config, differing in that one value):
  #                        20y (0.0 / 0.5)          8y (0.0 / 0.5)
  #     top-3 excess       +6.5 (med +6.6)  BOTH    +8.1 (med +8.5)  BOTH
  #     win of windows     97% of 39        BOTH    82% of 62        BOTH
  #     worst window       -0.5             BOTH    -9.2             BOTH
  #     OOS early/late     +5.0/+7.9        BOTH    +7.6/+8.7        BOTH
  #     rank-1 h2h guard   76% (16/21)      BOTH    63% (24/38)      BOTH
  #   Not "inside the noise band" — IDENTICAL, field for field, at both SHIP RULE v2 primary horizons.
  #   The term does not change which names reach the basket the screen footer quotes.
  #   IT IS LIVE, NOT INERT — the distinction that had to be settled before this could be written, since
  #   an unplumbed term and a useless one both print "no change". The `dividend_weight` CURVE separates
  #   (20y growth-lane edge +451.4 at 0.0 vs +459.5 at 0.5), so scores genuinely move; the movement is
  #   just all in the TAIL, below the top-3 anyone buys. Before the plumbing that curve was necessarily
  #   FLAT — no divs meant a 0% yield for every name, so every weight scored identically. It no longer is.
  #   And the tail movement FLIPS SIGN across horizons: the `growth_dividend` ablation (term OFF) reads
  #   edge -8.1 at 20y (term helps), +3.2 at 12y and +4.3 at 8y (term hurts), against a 20y bootstrap band
  #   of [+210.6 … +703.8]. Spearman moves +0.00 at all three. That is SHIP RULE v2's TIE arm: the
  #   horizons disagree -> UNRESOLVED -> nothing ships.
  #   VERDICT: 0.5 STANDS — not because it won, but because it provably costs NOTHING on the graded metric
  #   while keeping the after-tax yield visible in the ranking a human reads. It has moved from a BLIND
  #   judgment lever to a MEASURED-INERT one. DO NOT RE-PLUMB THIS: the answer is on file, and the next
  #   person to notice "dividends are missing from the backtest" should read this block instead of the
  #   module header they will otherwise trust.
  #   (#58) RETRACTED — three lines stood here claiming the tax split was "STILL UNGRADED, and NOT fixable
  #   by this plumbing: the backtest carries no `domicile`". Wrong the same way this block's own IMPOSSIBLE
  #   was wrong, one step further on: NOTHING on this path ever read a `domicile`. `tax_keep` branches on
  #   `picks::is_eu_payer`, i.e. `market` (filled by `core::market_of` from the ticker suffix inside
  #   `Quote::stub`) and `instrument_type` (filled by `backtest::stamp_asset_class` from Yahoo's meta plus
  #   the universe's `etf_set`). Both are present in the backtest, and both are the SAME inputs the live
  #   screen scores on — which is the whole bar for gradeability.
  #   THE MEASUREMENT: the `tax_split ->off` ablation — both arms set EQUAL, which is this knob's real "off"
  #   (0.0 would mean an EU dividend is worth nothing, a different question) — reads edge Δ+0.0 at 12y, 20y
  #   and stress, Δ+0.5 at 8y, rho Δ-0.00. The dividend WEIGHT the split scales ablates at Δ+142.8 in the
  #   same table. The split is a rounding detail next to the term it multiplies, which is what the SIZING
  #   note below already implied at ~0.06 pts. VERDICT: 0.76/0.72 STANDS, measured-inert, same as the weight.
  #   WHERE IT IS MEASURED, and why not in the growth lane: sample. The growth gates admit 4 EU payers out
  #   of 80 scored rows on the fixture, so a growth-lane sweep of a tax knob is flat for want of rows
  #   WHATEVER the truth; the on-sale lane scores 526 windows. Reading it across lanes is legitimate because
  #   `tax_keep` is shared by both by construction (see `picks::dividend_reward`). Caveat, stated honestly:
  #   the on-sale lane is a FOIL that ranks backwards on purpose, so a NON-ZERO result there would have
  #   needed care to interpret. A Δ+0.0 does not. A growth-lane `weight_curve` for this knob was built, run
  #   and DELETED — 17 lines of golden output to report the same Δ+0.0 that one ablation row reports.
  #   ALSO BY DESIGN: realized returns stay price-only on BOTH legs, so the excess vs ^GSPC stays
  #   apples-to-apples. This measured whether yield helps SELECT, never total-return performance.
  #   SIZING, unchanged: 0.5 peaks the term at +2.25 (6% yield × cap), under quality's +6 blind ceiling
  #   and far under the old 1.5's +9 — that magnitude is exactly what got it cut. The TAX scaling below is
  #   worth ~0.06 pts on a 5-19 pt score at a 4% yield. Read by BOTH lanes.
  #   Set 0.0 to go back to the 2026-07-15 state; the backtest says the top-3 will not notice.
  dividend_weight: 0.5           # LIVE: reward per % of trailing-1Y yield, AFTER the tax keep-rate (0 = OFF). GROWTH LANE ONLY since (#61)
  # (#62) MEASURED AT ZERO WIDE, ON PURPOSE, AND KEPT — so that nobody spends the six runs again. Same
  #   batch and same rule as the `growth_smoothness_weight` deletion above; this arm zeroed the GROWTH
  #   half only (post-(#61), so the on-sale flip is not mixed in) at 20/12/8y, n identical at 481/668/852:
  #     horizon   lane edge          rank-1 mean|med          h2h vs 11-20 book     null floor
  #     20y       +459.5 -> +451.4   +6.2|+6.4 -> +6.2|+6.4   16/21 -> 16/21 (76%)       93.3
  #     12y       +132.0 -> +135.2   +5.8|+7.2 -> +5.8|+7.2   17/30 -> 18/30 (57->60%)   15.4
  #      8y        +59.3 ->  +63.6   +7.2|+8.2 -> +7.2|+8.2   24/38 -> 24/38 (63%)       16.0
  #   BY THE LETTER OF THE DELETION RULE THIS ARM PASSES — the book never degrades, h2h is >=50% at all
  #   three, and the one negative edge move (-8.1 at 20y) is 11x under that horizon's floor while 12y and
  #   8y come back POSITIVE. It is kept anyway, for two reasons that are not "the number was close".
  #   ONE: the rank-1 column is BYTE-IDENTICAL at all three horizons. Not "held" — identical to the
  #   printed digit, twice over, mean and median. A term with zero rank-1 authority cannot be graded by a
  #   test whose verdict IS rank-1; the run did not find the term worthless, it found the instrument
  #   blind to it. (#60)'s symmetry rule applies unchanged: under-floor means UNRESOLVED, not disproved,
  #   and that cuts against deleting exactly as hard as it cuts against shipping.
  #   TWO: after (#61) zeroed the on-sale half, this weight is the LAST live consumer of the Portuguese
  #   englobamento model (tax_keep_eu / tax_keep_other / the 50% Art. 40.º-A EU exclusion). Zeroing it
  #   would retire that model by side effect of a metric that just told us it cannot see this term. The
  #   model's claim was never "after-tax yield predicts return" — it was "the yield shown to a human
  #   should be the yield that human keeps", which is (#58)'s standing verdict on the per-market map and
  #   is not a claim the backtest is entitled to refuse.
  #   So: 0.5 stays. If it is ever revisited, the number to beat is above and the argument to beat is the
  #   truthfulness one, not the edge one.
  onsale_dividend_weight: 0.0    # [FOIL] (#61) ON-SALE lane's own yield weight. ZEROED, and this is the
                                 #   largest single measured improvement in the file. Split from the growth
                                 #   lane for the same reason `onsale_sharpe_weight` was split from
                                 #   `sharpe_weight` — one shared knob serving two lanes that want opposite
                                 #   values — and the same fix, at ~6x the size (that one bought +23.7).
                                 #   MEASURED 2026-08-10, wide `12 universe` on 4997 tickers, n=4623 on-sale
                                 #   windows, admission byte-identical (cutoffs 11673 both runs):
                                 #     dial          shipped 0.5              this arm 0.0
                                 #     rho           -0.14                    +0.03
                                 #     edge          -65.0                    +85.7
                                 #     90% band      [-109.8 … -17.9] BELOW 0 [+50.6 … +126.7] CLEARS 0
                                 #     net of cost   -65.1 "too churny"       +85.6
                                 #     eras          -69.6/-7.0/-142.6/-85.5  +45.4/+123.7/+24.2/+83.5
                                 #   Read the eras row twice: ALL FOUR flip sign. This lane ranked BACKWARDS
                                 #   — its band sat entirely below zero, i.e. the names it liked underperformed
                                 #   the ones it did not — and paying for trailing yield was the reason.
                                 #   NOT A NEW DISCOVERY, WHICH IS THE UNCOMFORTABLE PART: the `dividend_weight*`
                                 #   ablation row printed Δ+150.7 in every wide on-sale table for as long as the
                                 #   row has existed, and this arm reproduces it to the decimal (-65.0 + 150.7 =
                                 #   +85.7). It went unread because a footnote asserted the term was inert in a
                                 #   price-only run; that footnote was stale from (#53) and is now fixed.
                                 #   WHY A YIELD TILT RANKS BACKWARDS HERE, offered as mechanism and not proof:
                                 #   this lane already rewards being cheap and beaten-down, and high trailing
                                 #   yield on a fallen price is the same fact a third time — it is a dividend
                                 #   YIELD, so the denominator is the very drop `discount_weight` is paying for.
                                 #   `discount_weight` ablates at Δ-88.9 (removing it HURTS), so the lane wants
                                 #   the dip; it just must not buy the dip twice.
                                 #   THE PORTUGUESE TAX MODEL IS UNAFFECTED. `tax_keep_eu`/`tax_keep_other` scale
                                 #   the GROWTH lane's dividend term, which keeps `dividend_weight: 0.5`, and
                                 #   growth is the lane `screen` ranks on. Nothing about Art. 40.º-A is retired.
                                 #   0.5 = the pre-split shared value, i.e. the arm's control.
  dividend_cap: 6.0              # LIVE: cap the yield % fed in — applied BEFORE the keep-rate (see below)

  # (D/PT) Portuguese dividend tax, as two after-tax KEEP fractions. Art. 40.º-A CIRS englobates only
  #   50% of a dividend from an EU-resident company meeting the Parent-Subsidiary Directive conditions,
  #   IF you opt for englobamento on the IRS return; a US/other payer is taxed in full. No tax law is
  #   encoded in the code — THESE TWO NUMBERS ARE THE WHOLE MODEL, and they are yours to compute from
  #   your own bracket, your englobamento choice, and the source withholding you actually eat after any
  #   reclaim. The values below are a worked example at a ~43.5% marginal rate, NOT advice: recompute
  #   them. 1.0/1.0 = tax off (the code default), lane byte-identical to the pre-tax version.
  #   ORDER MATTERS: the cap is applied to the GROSS yield, then the keep-rate scales it. Capping the
  #   scaled yield instead would saturate EU and non-EU onto the same value for every above-cap yielder,
  #   erasing the distinction exactly where yields are highest.
  #   EU set = the 12 members as core::suffix_country spells them (UK/Switzerland/Norway are NOT EU).
  #   Venue is the proxy for the payer: an Irish-domiciled S&P 500 name (ACN, ETN, MDT) reads "USA" and
  #   is under-credited — conservative, never the reverse. Funds take tax_keep_other at any domicile: an
  #   OICVM distribution is not a PSD company's *lucro*, so no exclusion.
  #   CEILING: one EU rate blends source withholding from 12.8% (France) to 35% (Finland). That 22-pt
  #   within-EU spread is ~3× the EU-vs-US gap this models — a real approximation, accepted knowingly.
  #   Upgrade path if it bites: a per-market map keyed on Quote.market. THAT PATH IS NOW PRICED AND IT DOES
  #   NOT PAY (#58): the coarse EU-vs-other split such a map would refine measures Δ+0.0 edge, so a finer cut
  #   of the same term cannot buy more than nothing. DO NOT BUILD IT FOR EDGE. The one justification left
  #   standing is truthfulness — a per-market map makes the after-tax yield a HUMAN reads closer to what
  #   they actually keep — and that argument has to be made on its own terms, not on the backtest's.
  tax_keep_eu: 0.76              # LIVE: fraction kept from an EU company's dividend (englobamento, 50% excluded)
  tax_keep_other: 0.72           # LIVE: fraction kept from every other payer, and from any fund

  # (E) Valuation tilt: cheap on EARNINGS, not just recently dropped. value = ref_pe / P/E, clamped
  # [0.5,1.5] (P/E = Price-to-Earnings ratio). Needs a trailing P/E -> set FMP_API_KEY (Financial
  # Modeling Prep) in the ENV (never here); free tiers are rate-limited so it populates at `check`
  # scale, stays neutral (×1.0) across the big `screen`. Read by BOTH lanes.
  ref_pe: 20.0                   # LIVE: "fair" trailing P/E; P/E 10 -> ×1.5 (cheap), P/E 40 -> ×0.5 (rich)

  # (F) Profitability/QUALITY tilt: reward trailing return-on-capital — ROE (Return On Equity, the
  # canonical quality factor; high-ROE firms out-compound long-run), or ROA where equity is negative
  # (`core::quality_return`). Same FMP_API_KEY opt-in as ref_pe for the LIVE screen (populates at
  # `check` scale). Read by BOTH lanes. 0 = off.
  # NO LONGER BACKTEST-BLIND, and the first measurement is a large one. This term was shipped for
  # rounds against a backtest that could not see it: `backtest_quote` builds from `Quote::stub` (roe
  # None) and fills only closes-derived fields, so `quality_reward` was 0.15 × 0 for EVERY sample in
  # EVERY lane — the ablation row read 0.0 by CONSTRUCTION, not by measurement. `4975f23` filled
  # `Quote.roe` from the as-of fundamentals in the backtest loop (same look-ahead guard as the three
  # yields), and this is its first wide run — `backtest universe fund 12`, 11690 cutoffs, 8458 with
  # as-of fundamentals:
  #     BUY lane    (full rho +0.04, edge +111.6): zeroing it -> rho -0.06 Δ-0.10, edge +62.9 Δ-48.7
  #     GROWTH lane (full rho +0.18, edge +181.6): zeroing it -> rho +0.18 Δ-0.00, edge +169.0 Δ-12.6
  #   The buy-lane Δ is the biggest RHO hit of any knob in that lane (bigger than cheap_weight, whose
  #   -50.4 edge comes with Δrho 0.00) — zeroing quality flips the lane's rank correlation NEGATIVE.
  #   Keep at 0.15. It is no longer a "small blind tilt"; it is load-bearing in the on-sale lane.
  # STANDALONE, though, it does NOT rank on its own: `quality(roe/roa) n=1826 rho -0.01 edge -115.1
  #   OOS +0.04 | -0.06`, and raw ROE alone is worse (rho -0.03, edge -123.2). It works as a MODIFIER
  #   on a price-led score, not as a selector. Held-book view agrees it is not junk (top-10 held 12y:
  #   +15.9%/yr, excess +4.3, win 100%, worst +2.5, OOS +4.7/+4.0 n=266) — the standalone edge is
  #   negative because ranking PURELY on ROE buys expensive quality.
  # NEITHER PIN GUARDS THIS TERM — do not read a green `cargo test` as a regression net for it:
  #   `walk_forward_edge_pin` builds samples with `core::backtest_quote` and `fund: None`, never
  #   routing through the run() loop's `quote.roe = ...`, so roe is None and the term is 0 for every
  #   pinned sample. `scoring_regression_pin` DOES set `stock.roe = Some(40.0)` by hand, so it pins
  #   `quality_reward`'s arithmetic — but never `core::quality_return`, which is where the ROE/ROA
  #   choice is actually made. That is why both pins sat still through `4975f23`. Expected, not lucky.
  quality_weight: 0.15           # LIVE + MEASURED: reward per % return-on-capital (×cap = max bonus, ~6 at 40%)
  # (#62) GRADED AT ZERO AND KEPT — and the interesting part is WHY the grade is thin and always will be.
  #   This term reads Δ+0.0 in every price-only run: `backtest_quote` fills no fundamentals, so the
  #   ablation row is measuring nothing and three of the four wide horizons are structurally blind to it.
  #   The ONLY run that can see it is `12 universe fund`, so that is the run, quality_weight 0.15 -> 0.0,
  #   n=655 growth windows in both (admission unmoved):
  #     dial        shipped 0.15                  arm 0.00
  #     rho         +0.15                         +0.14
  #     edge        +161.9                        +154.6      (Δ-7.3, vs that run's 37.3 null floor)
  #     90% band    [+103.6 … +234.8]             [+103.2 … +224.9]
  #     OOS         +0.08 | +0.15                 +0.08 | +0.15   (identical)
  #     rank-1      +6.3|+7.2, win 76% of 54      +6.2|+7.2, win 76% of 54
  #     h2h         17/29 (59%)                   17/29 (59%)     (identical)
  #   Reproduces the ablation row's -7.3 to the decimal, 5x under the floor, with the rank-1 MEDIAN, the
  #   win rate and the h2h all identical. On its own that reads like the smoothness result — free to
  #   remove — and the on-sale half agrees even harder: in the pre-(#61) fund baseline, ablating quality
  #   LIFTS the on-sale lane +28.1 (edge -93.1 -> -65.0), i.e. it was ranking that lane backwards too.
  #   IT IS KEPT ANYWAY BECAUSE ONE HORIZON IS NOT THREE, and one horizon is all this term can ever have.
  #   The 20y fund table is structurally EMPTY (SEC XBRL starts 2007; 20y cutoffs end 2006) and the 8y
  #   fund run was not made. The deletion rule was written as "holds at all three horizons" precisely so
  #   that a single lane-edge reading could not move a live term, and `quality_weight` is live on real
  #   fundamentals in every `screen` the operator runs — a population this backtest never sees.
  #   THE MISSING RUN, if anyone wants to settle it: `8 universe fund` for a second growth horizon, and a
  #   post-(#61) `12 universe fund` for the on-sale half, which is currently CONFOUNDED — the +28.1 above
  #   was measured before the dividend split, and the arm's own on-sale lane came back byte-identical to
  #   the price-only run (+85.7 / net +85.6 / [+50.6 … +126.7]) because quality was on-sale's only
  #   fundamental consumer, so zeroing it collapses that lane onto the fund-less one exactly.
  # (#72) GRADED UNDER SHIP RULE v2 — 2026-08-12, and it HOLDS at 0.15. (#62)'s MISSING RUN above is
  #   DISCHARGED: the 8y horizon was run, the 12y was re-run post-(#61), and the 20y half was measured
  #   rather than assumed. Ten runs, `backtest {8,12} universe fund` x four weights plus a 20y pair,
  #   one field moved (the tool's own drift line reads `quality_weight 0.15->X` and nothing else).
  #   Data conditions identical across all ten: 4997 tickers, growth-scored 351/511/637 at 20/12/8y,
  #   cutoffs with as-of fundamentals 4394/8456/10956, crumb fallback failed in every run.
  #   THE GRADED BOOK (top-N excess pts/yr, mean|median; the run's ENTRY-STATE block names it "the same
  #   top-10 growth book", i.e. this table is the GROWTH lane's):
  #     8y   n=59 windows, 35 h2h pairs           12y  n=51 windows, 27 h2h pairs
  #     arm     t1        t3        t5      t10   h2h    |  t1        t3        t5      t10   h2h
  #     0.00  +7.9|+8.6 +7.3|+7.8 +6.8|+7.4 +6.2|+6.0 63% | +6.8|+7.6 +6.8|+6.9 +6.5|+6.1 +6.1|+6.0 67%
  #     0.075 +7.9|+8.6 +7.3|+7.8 +6.8|+7.4 +6.3|+6.0 63% | +6.8|+7.6 +6.8|+6.9 +6.6|+6.1 +6.1|+6.0 67%
  #     0.15c +7.9|+8.6 +7.3|+7.8 +6.8|+7.4 +6.3|+6.0 63% | +6.8|+7.6 +6.8|+6.9 +6.6|+6.1 +6.1|+6.0 67%
  #     0.30  +7.9|+8.6 +7.3|+8.0 +6.8|+7.4 +6.2|+6.2 60% | +6.8|+7.6 +6.6|+6.9 +6.5|+6.1 +6.2|+6.0 67%
  #   Worst window is IDENTICAL in all four arms at both horizons (-9.2 at 8y, -7.8 at 12y), and rank-1
  #   never moves at all. THE TERM IS INERT ON THE BOOK: a 4x swing in the weight, from off to double
  #   the shipped value, moves no basket size at either graded horizon by more than 0.2.
  #   THE VERDICT, rule applied in order. Guards hold everywhere (h2h 60-67%, no worst window moves).
  #   0.00 and 0.075 HOLD the primary to the decimal but buy nothing, and 0.00 additionally owes the
  #   DELETION rule, which wants all three horizons and can only ever have two here — the same reason
  #   (#62) kept it, now with the second horizon actually run instead of merely named. 0.30 FAILS the
  #   primary (12y t3 mean +6.8 -> +6.6) and also fails the ADDITION bar pre-registered before the runs
  #   (rank-1 must RISE; it is flat at both horizons and the 8y h2h falls 63 -> 60). Nothing ships.
  #   THE 20y BLACKOUT IS NOW MEASURED FOR THIS KNOB, not inherited from (#71): the two EXTREME arms,
  #   0.00 and 0.30, come back identical on every graded number at 20y (t1 +6.4|+6.2, t3 +6.3|+5.7,
  #   t5 +6.2|+6.0, t10 +5.9|+6.1, h2h 74%, worst -0.5, OOS +4.7/+7.8). The only textual difference is
  #   the drift warning plus a transient Euronext-list fetch note in one run, which did not reach the
  #   graded pool (ETF counts 0/15 in both). So the widest possible arm gap is a no-op at 20y.
  #   AND IT IS NOT A COVERAGE PROBLEM AT THE GRADED HORIZONS — this is what separates this round from
  #   (#71). ROE is richly populated where it is graded: the standalone probe reads n=4096 at 8y and
  #   n=1845 at 12y. The term is being measured, and it is measured as powerless: standalone it scores
  #   rho -0.04 edge +1.3 OOS -0.01|-0.06 at 8y and rho -0.03 edge -166.9 OOS +0.02|-0.07 at 12y, i.e.
  #   NEGATIVE rank correlation with forward return at both. Novy-Marx's profitability premium does not
  #   show up in this pool at these horizons. It is kept anyway, on the same grounds as (#62): the
  #   deletion rule cannot be discharged on two horizons, and deleting it is worth nothing on the book.
  #   THE ON-SALE LANE IS WHERE THE MOVEMENT IS, and it flips (#3d)'s option (c) around. Ablating
  #   quality at the shipped weight LIFTS the on-sale lane at both horizons — 8y rho +0.02 -> +0.13
  #   (Δ+0.11), edge +25.6 -> +29.7; 12y rho -0.06 -> +0.03 (Δ+0.09), edge +60.6 -> +105.7 (Δ+45.0,
  #   about 3x that horizon's 15.4 null floor) — while the GROWTH lane reads Δ-3.6 edge at BOTH
  #   horizons, under each one's floor. (#3d) proposed zeroing quality in the GROWTH lane only; the
  #   measurement says the lane that wants it gone is the OTHER one.
  #   READ THE +45.0 CORRECTLY: it is a MODE SWITCH, not a dose-response. The on-sale lane edge across
  #   the four arms is 8y 29.7/31.9/25.6/26.0 and 12y 105.7/58.1/60.6/66.5 — NOT monotone, and 0.075
  #   is worse than the shipped 0.15 at 12y. Zero is discontinuous because quality is that lane's ONLY
  #   fundamental consumer ((#62)), so zeroing it drops the lane onto the price-only ranking entirely;
  #   verified here, every on-sale ablation row in the 0.00 arm reads Δ+0.0. The finding is "on-sale
  #   ranks better with no fundamentals at all", which (#62) also saw pre-(#61) at +28.1, reproduced
  #   post-(#61) and larger. It is not "less quality is better".
  #   WHICH IS WHY NOTHING SHIPPED FOR THE ON-SALE LANE EITHER, and the reason is structural: THE RUN
  #   PRINTS EXACTLY ONE top-N BOOK AND IT IS THE GROWTH ONE. Every number above about on-sale is lane
  #   edge, pre-committed as NON-VOTING before the runs and wrong five times already ((#63) accel,
  #   (#69) ablation Δ, (#70) edge curve, (#3d), (#71)). So Ship Rule v2 cannot grade an on-sale change
  #   at all today — not for lack of data, but because the metric it judges on does not exist for that
  #   lane. THE NEXT ROUND IS THEREFORE A CODE ROUND, and in this order: print an on-sale top-N book,
  #   THEN split the knob (`onsale_quality_weight: 0.0`, the shape `onsale_sharpe_weight` and
  #   `onsale_dividend_weight` already use — that split was made on exactly this evidence, "growth
  #   wants 0.15 but on-sale wants 0"), THEN grade the split arm on the new book. Splitting first would
  #   ship a knob on the one metric this file keeps catching out.
  quality_cap: 40.0              # LIVE: cap the ROE % fed in (a buyback-levered 200% ROE doesn't dominate)
  # (#42) "why does Apple print n/m as ROE" — asked 2026-07-27. The answer was a display rule reading the
  #   WRONG AXIS, and chasing it found the score doing the same thing. Two thresholds existed for one
  #   claim: the cell called ROE meaningless above 100, the score stopped believing it above quality_cap
  #   40 — and the looser one was the one the user saw, so a name could be hidden while collecting the
  #   full +6.0 quality reward.
  #   LEVEL DOES NOT SEPARATE "earned a lot on its assets" from "has no equity left". The discriminator is
  #   the EQUITY MULTIPLIER, ROE/ROA = assets/equity. Across the 447 cached filers with a positive ROE:
  #     CL    ROE +3948  ROA 13.1  mult 302x   equity is 0.33% of assets — artifact
  #     LYV   ROE  +183  ROA  2.2  mult  85x   artifact
  #     GDDY  ROE  +407  ROA 10.9  mult  37x   artifact
  #     BA    ROE   +41  ROA  1.3  mult  31x   artifact — and it PRINTED CLEAN under the old rule
  #     AAPL  ROE  +152  ROA 31.2  mult 4.9x   p78 leverage on an elite ROA — HIDDEN under the old rule
  #     ITW   ROE   +95  ROA 19.0  mult 5.0x   the SAME leverage as AAPL, printed clean
  #     APP   ROE  +156  ROA 45.9  mult 3.4x   hidden
  #   So `quality_return` now swaps ROE -> ROA on `roe/roa >= core::MAX_EQUITY_MULT` (20x, equity under
  #   1/20th of assets, p97) in ADDITION to the existing negative-equity sign test, and the |ROE|>100 n/m
  #   cell is DELETED — a number reaching the cell is now one that survived the multiplier check, and
  #   hiding a figure the score is already acting on was the inconsistency, not the cure.
  #   MEASURED, `backtest universe fund 12`, three binaries, one frozen config. All three scored the
  #   IDENTICAL universe (on-sale 4612, growth 1272), so (#40) noise did not fire and the deltas are real:
  #                        growth rho | edge | winsor      on-sale rho | edge | winsor
  #     off (baseline)       +0.19  +140.0  +120.2           +0.05  +121.2  +57.9
  #     mult >= 20 SHIPPED   +0.19  +140.0  +120.2           +0.04  +118.3  +55.0
  #     mult >= 40           +0.19  +140.0  +120.2           +0.04  +118.3  +55.0
  #   THE GROWTH LANE DOES NOT MOVE AT ALL — byte-identical, and the `quality(roe/roa)` held-book probe is
  #   identical too (n=236, +15.4%/yr, excess +3.8, win 100%). The whole cost is -2.9 on-sale edge, sitting
  #   entirely in one era (2011-05..2014-07: +179.7 -> +167.0), and it is CL/LYV/COR losing quality credit
  #   they were earning on a collapsed denominator.
  #   SHIPPED AT A MEASURED LOSS, deliberately, as a correctness fix rather than an edge claim — the
  #   mirror of (#39), which was refused while edge-POSITIVE because its mechanism was wrong. Here the
  #   mechanism is right and the price is 2.9 pts in a lane the fix was not aimed at.
  #   BONUS RESULT: the known misfire is unpriced. Balance-sheet businesses sit in the artifact band and
  #   are NOT artifacts (IBKR 37.9x, AMP 29.2x, PFG 28.7x, MET 26.2x, PRU 23.9x — for a broker the leverage
  #   IS the business and ROA is ~0.5% by construction), and 20x sweeps them up. But mult>=40 swaps ZERO
  #   financials and lands on the SAME numbers to the decimal, so those names never reach the scored pool.
  #   The accepted risk costs nothing measurable. Do not re-litigate it without new pool coverage.
  #   NOT A KNOB: `fund_factors(rows, cutoff, yrs)` takes no tuning, so a knob costs its signature and
  #   every caller, and 20x is an accounting fact. Measured by rebuilding the const (INFINITY / 20 / 40).
  quality_neutral: 0.0           # (#87) UNMEASURED — an unfitted judgement value, shipping at the value that IS
                                 #   the pre-(#87) behaviour, so this line changes nothing until it is raised.
                                 #   WHAT IT DOES: what an ABSENT ROE is worth, in ROE points, fed in before the
                                 #   weight and INSIDE the clamp(0, quality_cap) above — the same ordering (#43)
                                 #   chose for `growth_fund_extra[].neutral`, so a KNOWN-BAD ROE still floors at
                                 #   0 and ranks BELOW an unknown one. This is that field arriving, three
                                 #   receipts late, at the term that predates it.
                                 #   WHY: `quality_reward` read `roe.unwrap_or(0.0)` and its own doc called that
                                 #   "0 = neutral". The (N) note under `growth_fund_extra` refutes exactly that
                                 #   sentence in as many words — in a RANKING absent is not neutral, it is a
                                 #   demotion of up to weight x cap. Here that is 0.15 x 40 = 6.0 points, the
                                 #   LARGEST single miss any fundamental term in this file can charge, and it is
                                 #   not charged at random: `quote.roe` needs a CIK or an FMP key, so it lands on
                                 #   every non-US filer, every name past the 600-call SEC budget, and every ETF
                                 #   and coin — all ranked against US filers in the same table. (#43) called the
                                 #   equivalent gap on roic "a data gap must not be scored as the worst possible
                                 #   business" and fixed it; the older and three-times-larger one stayed open.
                                 #   WHY NO NUMBER IS QUOTED, i.e. why this ships at 0.0 and not at the fix. No
                                 #   sweep can settle it, for the reason (#59) recorded for the roic fill:
                                 #   `quote.roe` is None on every offline row before the fund path fills it, so
                                 #   at any non-zero value this term becomes a CONSTANT added to `base` across
                                 #   the whole sample — and a constant inside `base` is not rank-neutral, because
                                 #   `base` is then multiplied by trust x overext x proximity x value. A sweep
                                 #   would be grading the multiplier, not the fill. The only honest basis is the
                                 #   one (#43) used to get 17.3: a CENSUS of covered filers under this same
                                 #   clamp. That census has not been run for roe, so no value is asserted here.
                                 #   REVERT RULE: it is already at the revert value. To raise it, census the
                                 #   cached filers' roe under clamp(0, quality_cap) and take the MEDIAN, never
                                 #   the mean — the cap truncates the top, so the mean is dragged by the clamp
                                 #   ((#43): mean 20.2 vs median 17.3, 19% clamped). Then read a live `screen`
                                 #   both ways: if uncovered non-US names climb PAST measured US filers with a
                                 #   genuinely poor ROE, the fill is too generous and belongs back at 0.0.
                                 #   PAIRS WITH `growth_fund_neutral` / `growth_fund_scope_by_class` in the
                                 #   growth block — one policy, three terms, see the receipt there for the other
                                 #   half (a neutral is a covered-PEER median, so it is meaningless for a coin).

  # === GROWTH LANE — THIS is the ranking `screen` actually prints. =============================
  # Proven compounders the on-sale lane can't surface: a name AT/NEAR its own ~10y high, with a
  # strong proven long-term CAGR, still climbing. Gated hard (near-high + proven CAGR + positive 1Y +
  # not crashing) and P/E-tilted so it isn't just top-chasing. Every knob below is LIVE. ===========
  growth_min_range_pct: 80.0     # RE-CONFIRMED 2026-08-03 (5-rung stress ladder: 90/80/70/60/50). 80 stays, and BOTH directions lose:
                                 #     90 -> 20y edge +428.7 (up) BUT rank-1 median +6.0 -> +4.4 and h2h 67% -> 62% — fails the ship rule
                                 #                     on clause 1, so the higher lane edge is bought by a WORSE top of the list
                                 #     70 -> +381.8 | 60 -> +380.9 | 50 -> +381.0  (all ~-29 vs the shipped +410.8)
                                 #   Note 70/60/50 are the same config in practice (n 530/532/532): below 70 the bar admits essentially
                                 #   nothing further, so loosening past it pays the full -29 pts for no additional names. The 50 that was
                                 #   requested is therefore strictly dominated by 70, which is itself dominated by 80.
                                 #   ---- original note ----
                                 #   GATE: must sit in the top 20% of its own ~10y range. SETTLED 2026-07-02:
                                 #   A/B'd 70 vs 80 on `backtest 12 universe` — 80 WINS: edge +109.8 vs +103.2
                                 #   AND better on both OOS halves (+0.07/+0.09 vs +0.06/+0.08). 70 fell short of
                                 #   its own ">=117 both-OOS+" keep-rule; the gate-sweep "70 too tight" flag was a
                                 #   small-sample mirage. 80 is the validated base.
                                 #   THE MIRAGE IS NOW MEASURED, not just asserted (2026-08-01, sweep given a median): the
                                 #   `growth_min_range_pct -10` row was mean-only, and the median reverses its sign at two of three
                                 #   horizons — 20y-s mean +394.2 vs med -236.9 (n=10), 8y-s mean +54.6 vs med -3.1 (n=15). The
                                 #   names that row admits are a few big winners on top of a losing majority, which is precisely
                                 #   what "mirage" meant. It survives at 12y-s only (mean +76.6 | med +44.2, n=15) — still n=15,
                                 #   still below anything worth acting on, and against two A/B'd sweeps that both said 80.
                                 #   RE-CONFIRMED 2026-07-30 from the other side: 50 swept solo on the stress grid LOSES edge at
                                 #   every horizon (20y-s +573.0 vs +623.5, 12y-s +203.4 vs +221.7, 8y-s +76.0 vs +79.2) with no
                                 #   book metric improving. 80 -> 70 -> 50 is monotone damage; the 80 bar is load-bearing. An
                                 #   overlay running 50 should return to 80.
                                 #   AND KEEPING IT COSTS NOTHING — measured 2026-08-03 by the screen's gate
                                 #   funnel over 4718 EU-buyable names: this bar REJECTS 780 (220 stocks, 541
                                 #   ETFs, 19 coins) and is the SOLE blocker of exactly ZERO of them. Every one
                                 #   also fails `history` or `cagr`. Same story for its 8y twin (566 rejected /
                                 #   0 sole), `5Y+` (1009 / 0) and `lifetime` (612 / 0): four gates, 2967
                                 #   rejections, no rows owed to any of them. So "loosen the range gate to see
                                 #   more names" is answered — it returns nothing. The gates that DO cost rows
                                 #   are the ones with a non-zero sole column (2026-08-03: cagr 13, peg 8,
                                 #   maxdd 2, 1Y+ 2), and each of those has its own receipt below saying no.
                                 #   (#50) BUT "AN OVERLAY RUNNING 50 SHOULD RETURN TO 80" IS NOT UNCONDITIONAL — 2026-08-05.
                                 #   Every sweep above moved this bar at the SHIPPED `growth_min_leg_years: 5.0`. Reverted
                                 #   ALONE on a live overlay that also runs `growth_min_leg_years: 0` (arm D: range 50->80
                                 #   and its 8y twin, leg floor left at 0), it LOSES at both mandated horizons: 20y h2h
                                 #   48% -> 40% and edge +183.1 -> +173.8; 8y h2h 49% -> 47%. Rank-1 median is flat at 20y
                                 #   (+4.3) and up at 8y (+7.1 -> +8.1), so it fails the dual mandate outright. Live count
                                 #   unchanged: 2 stocks / 20 ETFs / 3 coins either way.
                                 #   WHY: with the leg floor at 0 the pool is dominated by 1Y/2Y-rung names, and the range
                                 #   bar cannot tell a 2-year name near its high from a 20-year one — it just deletes the
                                 #   older names' drawdown tolerance while the young cohort sails through. Revert both
                                 #   together (arm A) and it is fine (20y h2h 68%), but arm E — leg floor alone, range left
                                 #   at 50 — already gets 65%, so the range revert is worth ~3 h2h pts ON TOP of the leg
                                 #   floor and is NEGATIVE without it. Order matters: leg floor first, this second.
                                 #   Do not quote line 895 at an overlay without checking its `growth_min_leg_years`.
                                 # (#74) THE MIRAGE RE-CONFIRMED A THIRD TIME, AND ITS SIGNATURE IS SHARPER THAN "THE MEDIAN
                                 #   REVERSES". 2026-08-12, `backtest {20,12,8} universe fund`, `growth_min_range_pct -10`:
                                 #     20y  n=5   -164.8 |  -771.2            (both negative)
                                 #     12y  n=6    +39.3 |   -59.7   SPLIT
                                 #      8y  n=6    +74.6 |   +29.7   TOO TIGHT
                                 #   THE HORIZON THAT SURVIVES HAS MOVED. On 2026-08-01 (paragraph above) the row survived its
                                 #   median at 12y ONLY (+76.6 | +44.2); today 12y is SPLIT and 8y is the survivor instead. A real
                                 #   effect does not migrate between horizons — a handful of names crossing a threshold does, which
                                 #   is what n=5/6 buys you. That is the mirage stated more precisely than before: not merely "the
                                 #   median reverses at two of three horizons", but "WHICH horizon it reverses at is itself random".
                                 #   n also fell from 10/15/15 to 5/6/6 as intervening rounds tightened the gates upstream, so the
                                 #   row is thinner than when it was already judged too thin to act on. 80 stands, untouched.
                                 #   DO NOT RE-PROPOSE THIS GATE OFF A SWEEP ROW. Three independent refusals now (2026-07-02
                                 #   A/B, 2026-08-01 median, 2026-08-12 here) plus the live funnel's 780-rejected / ZERO-sole
                                 #   column. Loosening it returns no rows, which is the only question a "see more names" request
                                 #   is actually asking.
  growth_min_range_pct_crypto: 40.0 # GATE (crypto only): looser range floor (top 60% of its own range) so MORE coins
                                 #   surface — alts spend most of their life far below ATH yet still out-compound. The
                                 #   BTC-relative tilt below + the NUPL damp keep the wider crypto table honest. Stocks/ETFs
                                 #   keep the strict 80 gate above. Lower toward 0 for even more coins, raise toward 80 to tighten.
                                 #   MEASURED AND REJECTED 2026-08-03 at 50 (live A/B, both arms on the SAME cache dir, bracketed
                                 #   by a repeat of the shipped arm to size the noise): 50 blocks 7 MORE coins at this gate, and
                                 #   changes the ranked crypto table by NOTHING — BNB/BTC #1/#2 either way. Its sole-blocked
                                 #   column is EMPTY in both arms: no coin dies here alone, so the 7 are already dead upstream.
                                 #   Backtest cannot grade this knob (crypto is filtered out of every edge metric), which is why
                                 #   the evidence is a name diff and not an edge number. Keep 40: same table, looser bar.
  growth_min_range_pct_8y: 80.0  # GATE: the SAME 80 bar re-measured on the LAST 8 YEARS (`stats_8y.range_pct`).
                                 #   WHY BOTH: `range_pct` is a PERCENTILE of today's price over the window's closes, so a
                                 #   name whose old, much-lower closes sit in the ~10y chart clears 80 on the strength of
                                 #   history that has nothing to do with the last 8 years. PGR ranked #1 (22.3 vs 20.9 next)
                                 #   at 2Y -14.0%, 29.4% off its high, life CAGR +17%/yr powered by decades now outside the
                                 #   window. Measured 2026-07-28 on the live screen: PGR 76%, TDG 75% — the only two names
                                 #   the bar cut, and exactly the two worst 2Y in the lane (-14.0%, -10.9%; next is NXPI at
                                 #   -3.6%, which scrapes through). ETF and crypto lanes came out byte-identical.
                                 #   80 SPECIFICALLY, not "a bar": it is the level that makes this gate EQUAL the `S-8Y`
                                 #   column's bare `n/a`. That column re-scores under an 8Y pin which neutralizes the CAGR
                                 #   floors, and range is the only stat the pin swaps that any gate can newly reject on — so
                                 #   at 80 the gate fires exactly when the cell blanks, and a ranked row can no longer print
                                 #   `n/a` there. Any other level breaks that equality (picks.rs pins it in a test).
                                 #   BACKTEST-BLIND: `stats_8y` is set only in fetch.rs; `backtest_quote` builds from
                                 #   `Quote::stub`, so this gate cannot fire in a backtest — same standing as the age/AUM
                                 #   gates. It is armed on the live read alone and CANNOT be A/B'd like the 70-vs-80 sweep
                                 #   above. Code default is 0.0 (off), so this line is the whole of the shipped behaviour.
                                 #   MEASURED AND REJECTED 2026-08-03 at 50 (live A/B, shared cache dir, noise-bracketed): 50 is
                                 #   the BIGGEST funnel move of any knob tested all day — 96 fewer stocks and 82 fewer ETFs die
                                 #   here — and it still changes the ranked tables by NOTHING. Sole-blocked is EMPTY at this gate
                                 #   in both arms: all ~178 extra survivors are already dead at cagr/peg/1Y+/liquidity, so letting
                                 #   them past this bar only moves where they die. A gate whose whole effect is invisible should
                                 #   stay at the STRICTER value — 80 keeps the 8Y re-measure meaningful for the day the upstream
                                 #   gates loosen. Big funnel delta + zero table delta is the signature of a redundant bar.
  growth_min_range_pct_8y_crypto: 40.0 # GATE (crypto only): same split as the pair above, same 40. INERT on today's table —
                                 #   BNB and BTC keep an S-8Y (29.0, 18.5) at 43-53% off their highs, because a coin's normal
                                 #   life is far below ATH. Here so the crypto lane can never inherit the equity 80 by accident.
                                 #   MEASURED AND REJECTED 2026-08-03 at 50 (live A/B, shared cache dir, noise-bracketed): blocks
                                 #   4 more coins, sole-blocked EMPTY, ranked crypto table unchanged. Same verdict as its two
                                 #   siblings above and for the same reason — no coin dies here alone. Keep 40.
  growth_btc_outperf_weight: 0.3 # SCORE (crypto only): tilt a coin by its 1Y return vs BITCOIN's (the market base) — beats
                                 #   BTC -> boost, lags -> mild dock, bounded 0.5x..2x. BTC itself = 1.0x (the neutral anchor
                                 #   you read the others against). SCREEN/CHECK-only (backtest-blind, validated edge untouched). 0 = off.
  growth_min_cagr: 19.0          # GATE, TWO LEGS, one number: the long leg (20/8/5Y rung) AND (#3i) the whole-life CAGR
                                 #   must BOTH compound >=14%/yr to count as a proven grower. (STALE-FIXED
                                 #   2026-07-29: this note said the life leg was screen-only because
                                 #   "backtest_quote leaves life_cagr None" — false since 2026-07-27,
                                 #   backtest_quote fills life_cagr, so the gate's BOTH-legs form is what
                                 #   every grid below measured, train==serve.) The gate, not a
                                 #   weight, is what moves the signal (additive/multiplier knobs are inert; gates reshape the scored pool).
                                 #   12y MAX-monthly knee (the real 20y-hold horizon): 12->14 lifts GROWTH edge +91.1->+92.7 AND
                                 #   flips top-half peer-relative -5.2->+4.6 (the bought basket now BEATS its peers, not just loses less);
                                 #   16 overshoots (edge +86.3, top-half flat, pool thins 1987->1609). OOS both-positive. 5y proxy preferred
                                 #   12 (rho +0.15 sym) but the 12y horizon — what the 20y goal actually targets — peaks at 14.
                                 #   RAISED 14 -> 19 (2026-07-30, user-proposed, per-knob stress grid vs the (#3k) ship baseline):
                                 #   the SECOND winner's-curse cure. h2h >50% at ALL THREE horizons for the first time (20y-s 60%,
                                 #   12y-s 69%, 8y-s 60% vs 41/33/48) with rank-1 quality UP everywhere — 20y-s +5.4|+5.3 win 81%
                                 #   (vs +4.4|+4.4 78%), 12y-s +4.6|+5.3 (vs +3.2|+4.8), 8y-s +5.6|+6.1 with the worst window
                                 #   HALVED −15.8 -> −9.2 — AND king lane edge UP +677.4 vs +623.5. THE PRICE, stated: pool thins
                                 #   ~10% (36/51/59 windows vs 40/56/64), 12/8y lane edge −4/−7% (+212.9/+73.7), top-10 median
                                 #   −0.1..−0.4. Arm F (capped-CAGR) failed exactly where this wins (rank-1 median at king).
                                 #   The old 12y knee above ("16 overshoots") was measured under trend weight 0.35 PRE-(#3k);
                                 #   at 0.15 the argmax leans on the gate instead of the trend reward, and the knee moves.
                                 #   Intermediates 15-18: SWEPT — see the LADDER and RUNG-15 blocks below (16/17/18 on
                                 #   2026-08-01, 15 on 2026-08-02). Nothing below 19 ships. THIS LINE READ "UNSWEPT" FOR A DAY
                                 #   after its own discharge note landed 14 lines further down; a later session read this line,
                                 #   missed the discharge, and built a whole plan around re-running a ladder that already existed
                                 #   (caught before the redundant arms ran — but only just).
                                 #   If this gate is ever re-laddered, update THIS line too, not only the block below.
                                 #   Pair-checked with growth_max_peg 1.6
                                 #   (knob-K12 grid): interaction nil — king byte-identical to this arm solo, 12y-s +215.1 h2h 69%,
                                 #   8y-s +71.8 h2h 57%. The pair IS the new ship baseline.
                                 #   LADDER SWEPT 2026-08-01 (16/17/18/20/22 x {20,12,8} stress, cagr-*-s.txt) — 19 CONFIRMED,
                                 #   no change, and it is a TWO-SIDED knee that loses in each direction for the OPPOSITE reason:
                                 #   BELOW (16/17/18) the looser gate deepens the pool (38 vs 36 king windows) and holds lane edge
                                 #   on a flat plateau (+641.2/+672.6/+682.8 vs +677.4 — spread inside noise), but rank-1 quality
                                 #   degrades MONOTONICALLY: king h2h 45/48/50% vs 60%, rank-1 median +3.2/+2.5/+4.0 vs +5.3.
                                 #   ABOVE (20/22) rank-1 stops paying (h2h 61%/57% — noise, and 22 falls BACK) while lane edge
                                 #   craters −14%/−26% (+583.2/+503.8), top-10 mean −0.4/−0.6, and 12/8y degrade too (20 at 8y-s:
                                 #   edge +56.4, h2h 39%). The gate starves the pool — 31/30 windows, 14 h2h pairs at 22.
                                 #   NEAREST RIVAL IS 18, NOT 20: king edge +682.8 is nominally the ladder's best, 12y h2h 66%,
                                 #   and 8y rank-1 +5.9|+6.3 BEATS this arm's +5.6|+6.1. It loses ONLY the primary metric, king
                                 #   rank-1 (h2h 50% vs 60%, median +4.0 vs +5.3). Re-test 18 FIRST if the grading rule is ever
                                 #   re-weighted toward the 8y lane. The "15-18 unswept" caveat above is now discharged.
                                 #   *** THAT PRE-REGISTRATION FIRED 2026-08-06. The grading rule DID change — SHIP RULE v2
                                 #   at the top of this file makes top-3 primary at 20y AND 8y and retires "20y king
                                 #   overrules". 18 lost on exactly one clause, the 20y rank-1 argmax, which v2 demotes to a
                                 #   guard. So 18 is now a LIVE candidate on the file's own instruction, not a new idea.
                                 #   RE-RUN AND VERDICT: see the (#52) block appended below — it was measured the same day
                                 #   the rule changed, `backtest {20,12,8} universe stress`, one-field arm
                                 #   (`growth_min_cagr 19.0->18.0`, drift-verified). Do NOT re-run it from this line;
                                 #   read that block. THE WHOLE 15..22 LADDER above was swept on rank-1, so every rung is
                                 #   re-openable in principle — deliberately NOT re-opened wholesale, because re-sweeping
                                 #   an entire ladder against a fresh objective is how a file overfits itself. One arm at
                                 #   a time, verdicts recorded either way.
                                 #   RUNG 15 ADDED 2026-08-02 (bt_15_{20,12,8}.txt vs controls ctrl-20-s / bt_19_12 / ctrl-8-s,
                                 #   same 4945-ticker universe) — the interval is CLOSED 15..22, and 15 extends the below-19
                                 #   pattern to its end point. It wins the LANE at 12y and the DOWNSIDE everywhere, and still
                                 #   loses, because it gives back exactly what 14->19 bought:
                                 #     20y-s  edge +643.1 vs +653.9, boot LOWER +408.4 vs +341.7, winsor +590.6 vs +576.4,
                                 #            top-10 +6.1|+6.0 vs +6.1|+6.1 — but rank-1 +4.9|+4.3 vs +6.0|+6.6, h2h 50% vs 65%
                                 #     12y-s  edge +167.1 vs +148.1, boot LOWER +100.5 vs +87.3, winsor +139.2 vs +113.6,
                                 #            late-OOS +0.08 vs +0.01 — but rank-1 +4.3|+5.2, h2h 43% vs 66% (BELOW 50%: #1 is
                                 #            worse than the 11-20 book — the winner's curse (#3k) cures, walking back in)
                                 #     8y-s   edge +37.8 vs +42.0, winsor +13.3 vs +40.3 (collapses), late-OOS -0.16 vs +0.06
                                 #            (NEGATIVE), worst window -15.8 vs -9.2 — rank-1 +4.8|+5.5 vs +6.6|+7.2, h2h 56%
                                 #   King h2h below 19 is now monotone: 16/17/18/15 = 45/48/50/50% vs 65%. A DEEPER POOL WITH A
                                 #   BETTER DOWNSIDE AND A ROTTEN ARGMAX IS A NO-SHIP — the same asymmetry that sank growth_max_peg
                                 #   2.0, read from the other side: there the point estimate rose while the lower bound fell; here
                                 #   the lower bound rises while the argmax rots. Both fail because the bar is ALL clauses, not any.
                                 #   ETF-BLIND, and this gate is why (see class_census, backtest.rs): growth pass rate is
                                 #   ETF 0/14, 4/2618, 17/8628 at 20/12/8y vs stock 380/4619, 572/9134, 700/11907. A 19%/yr floor
                                 #   sits in the RIGHT TAIL of the stock return distribution while a diversified fund IS that
                                 #   distribution's mean, so no broad fund can clear it — live, the broad sleeves run +11..+15%/yr
                                 #   and only narrow tech/semis are over the line. At 15 the ETF counts only reach 0/14, 12/2618,
                                 #   128/8628. CONSEQUENCE: this ladder graded ~99% STOCKS. It never voted on funds and cannot.
                                 #   The ranked ETF table is a SECTOR-TILT lane by construction; broad buy-and-hold breadth lives
                                 #   in the hold CORE (picks.rs hold_core_list), which bypasses growth_score entirely — that is
                                 #   the design, not a gap to close by lowering this floor.
                                 #   ONLY ONE OF THE TWO LEGS IS PINNABLE, AND THE OTHER IS NOW THE BOTTLENECK (2026-08-06).
                                 #   Leg 1 is `long_cagr`, which `fixed_cagr_years` CAN pin to a chosen window. Leg 2 is
                                 #   `quote.life_cagr`, ALWAYS whole-life, and NO config reaches it. Pinning to 8 (the (#51)
                                 #   receipt at `fixed_cagr_years`) moved leg 1 and left leg 2 where it was, so the live
                                 #   `cagr-life` SOLE-BLOCK count went 2 -> 18: CASY CAT DECK FICO FIX FLEX IBKR JBL KLAC LLY
                                 #   MCK PH STLD TPL TRGP TT URI WSM. Those names clear 19%/yr over the last 8 years and die
                                 #   on a lifetime average that includes a decade they no longer resemble — the SAME stale-window
                                 #   defect the pin was built to remove, surviving on the leg the pin cannot reach.
                                 #   NOT A REASON TO DROP THE 19: the ladder above priced this number and it is a two-sided
                                 #   knee. It is a reason to ask whether leg 2 should read a WINDOW rather than a lifetime, and
                                 #   that is a CODE question (life_cagr is computed, not configured), so it is the next round's
                                 #   target, not this one's. THE 15..22 LADDER ABOVE STANDS UNTOUCHED: the pin was REFUSED
                                 #   the same day (5/8/10 all NO-SHIP), so this gate is still measured on the same
                                 #   age-chosen leg it was swept on. The 18 sole-blocks were a LIVE-table observation under
                                 #   an arm that did not ship — they describe the defect, not today's funnel. Only if a
                                 #   future round pins the ranking legs for real does this ladder need re-measuring.
                                 #
                                 #   (#73) 2026-08-12 — LEG 2 IS REACHABLE NOW, AND IT CHANGED NOTHING. "NO config reaches it"
                                 #   above is obsolete: `life_cagr_max_years` was repurposed off the rank and onto this bar, so
                                 #   leg 2 reads `capped_cagr.or(life_cagr)` (`picks::life_leg_cagr`) at both the score site and
                                 #   the footer site. Graded on a 0/6/8/10/12/16/20 ladder, `backtest {20,12,8} universe stress`,
                                 #   full Ship Rule v2 — full table and verdict at `life_cagr_max_years` ~line 744. HOLD 0.0:
                                 #   6/8/10 fail the h2h guard, 12/16/20 hold every guard and PRIMARY but leave rank-1 median flat.
                                 #   THIS 15..22 LADDER STILL STANDS UNTOUCHED, and now for a second, stronger reason: leg 2 ships
                                 #   at 0.0, i.e. on the same uncapped lifetime this gate was swept against. The pre-registration
                                 #   above ("only if a future round pins the ranking legs for real") has fired, been answered NO,
                                 #   and is spent. The 19 owes no re-measurement.
                                 #   WHAT THE GRID ACTUALLY REVEALED ABOUT THIS GATE, worth more than the knob: the arms that
                                 #   graded best are a net TIGHTENING of leg 2, not the loosening the sole-block story predicted.
                                 #   A window cuts both ways and the rejecting half dominates once it is long enough to still
                                 #   contain a has-been's dead years. Anyone re-opening "19 is too tight" starts from that, not
                                 #   from the 18 names.
                                 #
                                 #   (#52) 2026-08-06 — 18 RE-TESTED ON SHIP RULE v2, AND IT IS A WASH. NO-SHIP; 19 STANDS.
                                 #   The pre-registration above ("re-test 18 FIRST if the grading rule is ever re-weighted
                                 #   toward the 8y lane") HAS NOW FIRED AND IS SPENT. Three fresh wide runs,
                                 #   `backtest {20,12,8} universe stress`, one-field arm growth_min_cagr 19.0->18.0,
                                 #   drift-verified by the off-validated warning; a 4th run re-ran the BASELINE at 20y and
                                 #   reproduced control to the last digit (+13.7 book / +6.5|+6.6 / worst -0.5 / 39 windows),
                                 #   so nothing below is grid drift. Control -> arm:
                                 #     lane   t3 book      t3 mean|med    t3 worst   win%     r1 med    h2h      lane edge
                                 #     20y    13.7->13.6   6.5|6.6 -> 6.4|6.6   -0.5=-0.5   97=97   6.4->6.0  76->62  452.8->422.6
                                 #     12y    15.2->15.3   7.3|7.8 -> 7.4|8.0   -7.8=-7.8   87=87   7.2->6.5  63->61  127.9->143.8
                                 #     8y     16.1->15.9   8.1|8.5 -> 8.0|8.6   -9.2=-9.2   82->84  8.2->8.9  66->67   65.0= 65.0
                                 #   PRIMARY FAILS BY 0.1 AT BOTH GRADED HORIZONS (t3 mean -0.1 at 20y and at 8y) while the
                                 #   medians are flat/+0.1. Guards all hold: h2h 62/61/67 stays over 50 and no worst window
                                 #   moves. So this is NOT the rank-1 catastrophe the old rule saw — under v2 it is simply
                                 #   NOTHING. Say that plainly rather than dressing 0.1 up as a finding: those deltas are far
                                 #   inside the noise (the 20y bootstrap band is +/-78% of its own estimate on 21 h2h pairs).
                                 #   THE REFUSAL IS "18 BUYS NOTHING", NOT "18 IS BAD". It moves the basket by less than a
                                 #   rounding tick and pays for it in the guards' DIRECTION — 20y rank-1 h2h -14 points and
                                 #   lane edge -30 — which is the same below-19 pattern the 15..22 ladder already mapped,
                                 #   just muted. A knob that costs argmax quality to buy zero basket return is not a trade.
                                 #   12y IS THE ARM'S BEST LANE AND 12y DOES NOT VOTE — t3 median +7.8 -> +8.0 and lane edge
                                 #   +127.9 -> +143.8, its only real win. Recorded HERE so a future session finds it already
                                 #   known: v2 grades 20y AND 8y, 12y is a consistency read only, and picking the horizon that
                                 #   flatters an arm is the exact move v2 exists to stop.
                                 #   DO NOT RE-RUN 18. The runs happened; this is their verdict. What would be genuinely new
                                 #   is the leg-2 question two paragraphs up (should `life_cagr` read a window instead of a
                                 #   lifetime) — that is a CODE change, and no floor value can substitute for it.
                                 #   (#73) that leg-2 code change was made and graded; see the paragraph above. It is a wash too,
                                 #   which retires the last open lead on this gate. 18 stays refused and 19 stays shipped.
                                 # (#135) P0b ARM 1 — THE FIRST POINT-IN-TIME RE-SWEEP EVER RUN ON THIS KNOB,
                                 # 2026-08-24. REFUSED. 19.0 stands. This discharges the PIT half of the DEBT
                                 # block for `growth_min_cagr` specifically: it is no longer un-re-swept, it is
                                 # re-swept and it lost. Three runs, `backtest {20,12,8} universe fund pit`, via
                                 # a TWO-field overlay (historically tests/p0b-cagr15.yaml, since DELETED; recreate per
                                 # the rule at `print_book_deciles`):
                                 #     buy_heuristic:
                                 #       print_h2h_mid_ladder: true
                                 #       growth_min_cagr: 15.0
                                 # `heuristic_drift` named exactly those two fields on all three
                                 # (`growth_min_cagr 19.0->15.0` and the `print_h2h_mid_ladder` instrument).
                                 #
                                 # WHY 15.0 AND NOT A LADDER: 15 is the exact point `gate_sweep` probes as
                                 # `growth_min_cagr -4`, and on the PIT control that probe flags TOO TIGHT at 8y
                                 # (n=315, fwd peer-relative mean +66.3 | med +63.8). One arm at the flagged
                                 # point, verdict recorded either way — not a ladder re-fitted to a fresh
                                 # objective, which is how a file overfits itself.
                                 #
                                 #     horizon   top-10 exc|med      worst        h2h vs 6-10    vs 11-20   admitted
                                 #       20y     +0.8|+0.9 -> +1.0|+1.5   -10.0 -> -10.0   50% -> 46%     50% -> 50%   117 -> 183
                                 #       12y     +2.8|+2.8 -> +2.2|+3.1    -9.5 ->  -6.9   61% -> 68%     50% -> 69%   228 -> 374
                                 #        8y     +4.5|+4.5 -> +3.3|+3.8    -6.2 ->  -4.4   65% -> 76%     67% -> 62%   325 -> 640
                                 #
                                 # THE VERDICT DOES NOT DEPEND ON WHICH COMPARISON BOOK THE GUARD READS, which
                                 # is why it can be recorded as clean despite the 20y disagreement below:
                                 #   - re-based guard (6-10, per #135): rank-1 h2h 46% at 20y -> GUARD BROKEN ->
                                 #     REFUSED OUTRIGHT. Precedence GUARD > PRIMARY; it is not softened to a tie.
                                 #   - old guard (11-20): h2h 50/69/62 all pass, but PRIMARY fails at 8y with
                                 #     BOTH mean AND median DOWN (+4.5|+4.5 -> +3.3|+3.8). NO SHIP.
                                 # Both roads end at 19.0. The worst-window GUARD never bit — it improves at 12y
                                 # and 8y and holds exactly at 20y.
                                 #
                                 # THE INSTRUMENT VALIDATED ITSELF. The admitted-count deltas are +66 / +146 /
                                 # +315, which equal `gate_sweep`'s own n at 20y / 12y / 8y to the unit. Two
                                 # independent readings of the same quantity agreeing exactly, the same
                                 # cross-check (#74) used. The arm moved what it said it moved.
                                 #
                                 # *** READ THIS BEFORE ACTING ON ANY `gate_sweep` TOO TIGHT FLAG AGAIN. ***
                                 # At 8y this flag was as strong as flags get — n=315, mean AND median both
                                 # strongly positive — and the top-10 book still got WORSE (+4.5 -> +3.3). There
                                 # is no contradiction: `gate_sweep` measures the newly-admitted names' OWN
                                 # forward peer-relative return. It does NOT measure their effect on the book.
                                 # The book holds TEN names, so a newcomer who is above peer average can still
                                 # rank into the top-10 and displace someone BETTER. ADMISSION IS NOT SELECTION,
                                 # and a gate can be "too tight" by the sweep's measure while being exactly right
                                 # for the book. (#60) said "do not act on a short-horizon flag alone" about
                                 # `growth_maxdd_cap`; this generalizes it to the instrument — a TOO TIGHT flag
                                 # is a LEAD, never a verdict, at any horizon count. It costs three runs to
                                 # convert one into a verdict, and both of the two flags converted so far
                                 # (maxdd_cap #60/#74, and this one) converted to REFUSED.
                                 #
                                 # WHAT MOVED AND WHAT DID NOT. Loosening to 15 reliably improves the DOWNSIDE
                                 # (worst window better at 12y and 8y) and the MEDIAN at the two long horizons,
                                 # while cutting the MEAN at 12y and 8y. That is the shape of trading tail
                                 # winners for breadth: 19.0 is buying the right tail, and the right tail is what
                                 # an equal-weight terminal-wealth book is made of. A future arm that wants this
                                 # gate looser needs to keep the mean, not just the median.
                                 #
                                 # REVERT RULE: none to execute — nothing shipped. TO REOPEN, one of: (a) a rung
                                 # between 15 and 19 that holds the 8y MEAN, which is the clause that actually
                                 # failed on the old guard; or (b) evidence that the 20y rank-1 h2h disagreement
                                 # recorded at `print_h2h_mid_ladder` resolves in favour of the 6-10 book, since
                                 # under the 11-20 book this arm dies on PRIMARY rather than on the GUARD and the
                                 # two failures invite different repairs. Do not reopen on a `gate_sweep` flag.
                                 #
                                 #   (#151) SWEPT TO 15.0 AND REFUSED 2026-08-29 — admits +17.6 pts BETTER than the accepted cohort's
                                 #   median and cures the 8y vacuity (the only non-vacuous column ever produced),
                                 #   but top-10 mean falls +3.7->+2.9 (12y) and +5.3->+4.1 (8y). PRIMARY fails, no
                                 #   guard broke. Do not re-run the grid; see the (#151) block at the top.
                                 #
                                 #   (#187) 2026-08-31 — RUNG 17 GRADED. REFUSED. CLAUSE (a) ABOVE IS NOW CLOSED, and with it the
                                 #   last sanctioned reopen on this knob. 19.0 stands.
                                 #   PRE-REGISTERED BEFORE THE RUNS: ONE rung, the midpoint 17 of the pit-untested {16,17,18}, with the
                                 #   CORRECTION (#188): "pit-untested" is the accurate word; the original read "untested".
                                 #   16/17/18 WERE swept, on 2026-08-01, on the `stress` lane — recorded at the LADDER block
                                 #   (:5848 and the rungs at :5857-5867). They were untested ON PIT, which is the lane clause
                                 #   (a) names and the lane this receipt graded. The rung-17 verdict below is unaffected.
                                 #   closing rule fixed in advance — if 17 does not hold the 8y MEAN, (a) is closed for the WHOLE
                                 #   interval, because 15 and 17 both failing leaves only 18, one notch off the incumbent, which is a
                                 #   rounding error and not a mechanism. Not a ladder re-fitted to a fresh objective, which is the
                                 #   failure mode (#135) named in its own block.
                                 #   THE OVERLAY (tests/mincagr17-ci-settings.yaml, deleted after the round per (#131)): a full copy of
                                 #   this file with ONE key moved, `growth_min_cagr: 19.0 -> 17.0`. `print_h2h_mid_ladder` already
                                 #   ships true here, so it needed no second field — and `heuristic_drift` names exactly
                                 #   `growth_min_cagr 19.0->17.0` on all three runs, nothing else. Control REUSED from (#183), run
                                 #   TWICE that day on this same binary and byte-identical on every graded line; its numbers match
                                 #   (#175)'s control column to the decimal. `windows scored` 1119 / 2737 / 6126, matching control.
                                 #     horizon  top-10 exc|med          worst           h2h 6-10             h2h 11-20          pool  admitted
                                 #       20y    +0.6|+0.0 -> +0.4|+0.0  -10.0 -> -10.0  4/8 50% -> 5/11 45%  1/2 50% -> 1/5 20%  5->6  120->147
                                 #       12y    +3.7|+3.9 -> +3.0|+3.0   -6.4 ->  -6.4  12/19 63% -> 17/23 74%  2/4 50% -> 6/9 67%  6->7  249->305
                                 #        8y    +5.3|+5.6 -> +4.2|+4.3   -5.5 ->  -3.9  19/27 70% -> 23/31 74%  5/8 62% -> 7/17 41%  7->8  355->484
                                 #   VERDICT: REFUSED TWICE OVER, on two independent halves of the rule.
                                 #     1. THE 20y rank-1 h2h GUARD BREAKS. The arm reads 1/5 = 20% on the 11-20 book, the licensed book
                                 #        at 20y — far under the absolute 50% bar. A broken guard REFUSES OUTRIGHT (GUARD > PRIMARY);
                                 #        it is never softened into unresolved.
                                 #     2. PRIMARY FAILS AT BOTH GRADED HORIZONS, ON BOTH MOMENTS. 12y +3.7|+3.9 -> +3.0|+3.0 and
                                 #        8y +5.3|+5.6 -> +4.2|+4.3. Every one of the four numbers is DOWN, so this is not a TIE and
                                 #        not an UNRESOLVED — the horizons agree.
                                 #   CLAUSE (a) ANSWERED DIRECTLY: the 8y MEAN goes +5.3 -> +4.2. It does not hold. Combined with
                                 #   (#135)'s 15.0 (+4.5 -> +3.3 on the config of the day) the interval is walked from both ends, and
                                 #   the closing rule pre-registered above fires. DO NOT PROBE 16 OR 18.
                                 #   (#186) WAS EXERCISED AND WAS NOT LOAD-BEARING — recorded because a floor that only ever excuses
                                 #   things is worthless. The arm de-saturates, so the 20y 11-20 denominator GROWS 2 -> 5 and the column
                                 #   becomes READABLE for the first time on this lane. Above the floor, it answers, and it answers
                                 #   REFUSE. The floor did not soften a single thing here; the first arm graded under it dies on exactly
                                 #   the column it governs. (The CONTROL's 1/2 is still vacuous by (#186); the n that counts is the
                                 #   arm's, and the arm's is 5.)
                                 #   *** THE FINDING WORTH KEEPING, AND IT IS ABOUT THE RULER, NOT THIS KNOB. ***
                                 #   THE (#160) NOT-WORSE BAR CAN BE PASSED BY DEGRADING THE COMPARISON BOOK. This arm CLEARS both
                                 #   (#160) guards — 6-10 h2h 63% -> 74% at 12y and 70% -> 74% at 8y, above 50% and above the control —
                                 #   while making rank 1 itself WORSE at both horizons:
                                 #       12y  rank 1 +1.8|+5.1 -> +0.7|+4.3     rank 6-10 -1.9|-0.1 -> -2.2|-0.3
                                 #        8y  rank 1 +5.6|+6.5 -> +5.2|+5.8     rank 6-10 -0.0|+1.7 -> -0.9|-1.0
                                 #   The h2h asks "does #1 beat the 6-10 book". It rises here because the 6-10 book SANK FASTER than #1
                                 #   did, not because #1 improved. This is adjacent to (#160)'s own reading note about a rising fraction
                                 #   on a flat numerator, but it is NOT that case and the note does not cover it: both numerator and
                                 #   denominator rise (12 -> 17 of 19 -> 23; 19 -> 23 of 27 -> 31), so the counts look healthy. A
                                 #   reader checking the numerator, as (#160) instructs, would be reassured and would be wrong.
                                 #   WHAT THIS DOES NOT LICENSE: no rule changes on this receipt. The verdict here does not depend on it
                                 #   (PRIMARY and the 20y guard each refuse on their own), and amending a bar on the strength of the arm
                                 #   that just failed is exactly the move this file forbids. It is written down so the NEXT arm that
                                 #   passes (#160) on a falling rank-1 is caught. THE CHEAP CHECK, available on every run already: read
                                 #   the rank-1 and rank-6-10 SLICES beside the h2h before believing a rising tally.
                                 #   WHY IT COSTS WHAT IT COSTS — the same mechanism (#175) found at rung 12, in smaller doses. The
                                 #   admitted names are the wrecks: the 12y top-10 zero list gains AIG@b3994(-96%) and FOSL@b4028(-96%)
                                 #   and the 8y list gains FOSL@b4028(-93%) — two of the exact names (#175) named. Zeros ridden go
                                 #   9 -> 10 (12y) and 3 -> 4 (8y). A 19%/yr compounder bar is a distress filter, and 17%/yr already
                                 #   lets the wreckage back in.
                                 #   DE-SATURATION IS REAL BUT DOES NOT PAY. Median gated pool 5/6/7 -> 6/7/8, substitutable names
                                 #   7/16/21 -> 20/40/95. Every column is still VACUOUS for a RANKING knob (pool still <= basket 10),
                                 #   which does not impede this verdict: (#149) exempts pool-changing GATES, and this is one.
                                 #   INSTRUMENT CROSS-CHECK, in the weaker form this arm allows. `gate_sweep` probes `growth_min_cagr
                                 #   -4` (= 15), NOT 17, so the exact admitted-count-equals-sweep-n check (#74)/(#135)/(#138) used is
                                 #   NOT available here — stated rather than fudged. What is available is the bracket: the 8y sweep row
                                 #   on the control reads n=335 for rung 15, and 17 admits +129. 0 < 129 < 335, and the deltas are
                                 #   monotone in the rung at all three horizons (+27/+56/+129). The arm moved what it said it moved.
                                 #   NET: this knob is CLOSED. (a) is spent here; (b) — the 20y book disagreement — is unchanged and is
                                 #   a question about the ruler, not about 19.0. Nothing shipped, so there is nothing to revert.
  growth_min_cagr_crypto: 0.0    # GATE (crypto only): any positive long trend qualifies, so the crypto table shows ALL
                                 #   potential growers ranked vs Bitcoin (the base), not just >8%/yr names. Raise toward 8 to tighten.
  growth_min_cagr_etf: 0.0      # (#192) GATE (ETFs only), TWO LEGS, one number — the fund lane's OWN CAGR floor, same units
                                 #   and same two legs as growth_min_cagr (the long rung, and the whole-life bar via life_leg_cagr).
                                 #   0.0 = INHERIT the equity floor and NOT "off": this is a MIN bar, so 0-as-off would make the
                                 #   DEFAULT a relaxation. SHIPPED AT THE SENTINEL — the lane is byte-identical to before this knob,
                                 #   and every golden proves it. The knob was MEASURED AND REFUSED; it stays for the two reasons below.
                                 #   WHAT WAS ASKED. Widen ADMISSION so the book holds more names worth holding 20+ years. (#124)'s
                                 #   decile ladder is why that was worth trying: more names improved the median AND the left tail
                                 #   monotonically (top-1 d10 0.05x / med 3.78x / P(book<1.0x) 40% -> top-10 1.30x / 5.51x / 5%), and
                                 #   went flat only because the book never holds more than ~10 names. The ETF lane was the obvious
                                 #   place to look: growth_min_cagr 19.0 sits in the RIGHT TAIL of the stock return distribution while
                                 #   a diversified fund IS that distribution's mean, so structurally no broad sleeve can ever clear it,
                                 #   and six other ETF-specific relaxations already ship on exactly that argument (growth_max_peg_etf
                                 #   2.0 vs equity 1.6, sharpe_cap_etf 9.0 vs 15.0, growth_min_score_etf, growth_min_aum_etf,
                                 #   budget_etf, name_etf). The CAGR gate was the conspicuous hole. It is now measured, not a hole.
                                 #   THE CLAIM I SHIPPED THIS ON FIRST, AND IT WAS FALSE. The arm was drafted "NOT BACKTEST-GRADEABLE:
                                 #   the graded pool is S&P 500 constituents and holds no ETFs", to be shipped on judgement plus a live
                                 #   screen diff. That is WRONG and the repo already said so: `class_census` (backtest.rs) records the
                                 #   pool as ~4311 ETFs of 4954 and names this exact sentence as the rotted receipt `sharpe_cap_etf`
                                 #   once shipped on. Arming the knob moved five FROZEN goldens (ETF growth-scored 1/184 -> 4/184),
                                 #   which is the proof, and it is why the fixture is a gate and not a formality. Graded properly below.
                                 #   MEASURED 2026-09-01 — six `backtest <y> universe fund pit` runs, control (0.0 = inherit) vs arm
                                 #   (15.0), one pond (4791-4792 resolved, ALL from cache), windows scored 1119/2737/6126 at 20y/12y/8y
                                 #   so no FOLIOMAN_OFFLINE collapse. Ship Rule v2 on the (#139) PIT LANE: PRIMARY and TIE read 12y AND
                                 #   8y, every 20y GUARD live. The knob is a GATE (pool-changing), so (#149)'s vacuity exemption does
                                 #   NOT excuse it from PRIMARY — that exemption covers RANKING knobs on a saturated column.
                                 #     PROOF OF ARM (the pool actually moved, per class_census growth-scored/cutoffs):
                                 #       20y  ETF   0/12   ->   0/12      stock 120/2276 both       <- see 20y note below
                                 #       12y  ETF   0/3043 ->  12/3043    stock 249/5444 both
                                 #        8y  ETF   7/9586 -> 138/9586    stock 348/7649 both
                                 #       The stock column is IDENTICAL at all three horizons: an ETF floor that moved the equity lane
                                 #       would be a bug, and this is the check that would have caught it.
                                 #     PRIMARY — top-10 held book, excess mean (median) pts/yr, control -> arm:
                                 #       12y  +3.7 (+3.9)  ->  +3.7 (+3.3)     mean held, MEDIAN DOWN 0.6      REFUSES
                                 #        8y  +5.3 (+5.6)  ->  +4.8 (+4.3)     BOTH DOWN, 0.5 and 1.3          REFUSES
                                 #       20y  +0.6 (+0.0)  ->  +0.6 (+0.0)     unmoved, and unmovable — see below
                                 #       Both graded horizons move the SAME WAY, so this is not a TIE and not UNRESOLVED. It is an
                                 #       outright PRIMARY refusal, at the two horizons the pit lane adjudicates on.
                                 #     GUARDS all PASS — which is the point of reporting them: the arm was refused on its merits, not
                                 #     on a broken guard, and nothing here was softened to get there.
                                 #       worst window (top-10)  20y -10.0 = -10.0   12y -6.4 = -6.4   8y -5.5 = -5.5   not worse
                                 #       rank-1 h2h  12y 12/19 (63%) = control, >= 50% and NOT BELOW it, on the 6-10 book per (#160)
                                 #       rank-1 h2h   8y 22/27 (81%) vs control 19/27 (70%) — clears both bars, and IMPROVES
                                 #       rank-1 h2h  20y 1/2 (50%) on the 11-20 book, n=2 -> VACUOUS BY RESOLUTION per (#186): one
                                 #         window flip moves the tally the full 50 points, so it can neither refuse nor license. Vacuous
                                 #         is not a pass; the arm still cleared 20y's live MAX guard (worst window) unchanged.
                                 #     WHERE THE HARM IS, from the disjoint rank slices — the useful part of this refusal:
                                 #       rank 1     IDENTICAL at every horizon (12y +1.8|+5.1, 8y +5.6|+6.5). No admitted fund ever
                                 #                  reaches rank 1, so the top of the book is untouched.
                                 #       rank 6-10  12y -1.9|-0.1 -> -2.0|-0.1     8y -0.0|+1.7 -> -1.7|+0.2
                                 #       The admitted ETFs land in the MIDDLE of the book and dilute it, and an equal-weight top-10
                                 #       pays 1/N for every one of them. That is the mechanism, and it is not a scoring artifact: the
                                 #       same names that widen the pool are the ones that occupy slots 6-10.
                                 #     THE 20y COLUMN CANNOT SPEAK ABOUT FUNDS AT ALL, and not for (#124)'s n_eff reason. There are 12
                                 #     ETF cutoffs in the whole 20y lane and NONE scores at 15.0 or at the inherited 19.0, so the arm
                                 #     and the control are the same run. Forward-window survival scales with listing age; the funds
                                 #     simply are not old enough. Do not read a 20y ETF conclusion off this harness — `class_census`
                                 #     carries that warning and this is it firing.
                                 #   WHAT THE LIVE SCREEN SAID, and why it is NOT the ship signal. Five `screen` runs the same day
                                 #   (control/18/15/12/10) put ETF rows at 8/10/19/23/23 and cleared names at 11/13/22/~27/35, with the
                                 #   stock table byte-identical at every arm (md5 323c3eb1366de4979091f1418627ded4). 15.0 was chosen
                                 #   as the arm because it was the lowest floor whose new rows were 11 DISTINCT benchmarks rather than
                                 #   share-class twins. All of that is a POOL CENSUS — it counts what gets admitted, and says nothing
                                 #   about what the admitted names then RETURN. The backtest answers that, and it answered no.
                                 #   WHY THE KNOB STAYS AT ALL rather than being deleted with its receipt:
                                 #     (1) it collapsed THREE hand-copied spellings of the CAGR floor (score_parts, gate_failures, and
                                 #         the long_leg_floors tail view) into `picks::min_cagr_floor` — non-negotiable #4. The #2
                                 #         mirror was previously held only by the three copies happening to be typed alike.
                                 #     (2) its ETF-only scope is now a TEST RESULT, not a comment: `growth_gate_reachability_pin` reads
                                 #         etf LIVE / crypto INERT / stock INERT for it. Re-measuring costs one number, not a refactor.
                                 #   RE-ARMING RULE, so this is not re-run blind. Any future arm must clear PRIMARY at 12y AND 8y on
                                 #   `universe fund pit`. The measured direction is that ADMITTING FUNDS DILUTES RANKS 6-10, and it is
                                 #   monotone in how many get admitted (8y admits 138 names and loses 0.5|1.3; 12y admits 12 and loses
                                 #   0|0.6), so a floor picked high enough not to hurt is a floor that admits nothing. Do not fit this
                                 #   knob to the grade — that is a search for the value where the harm rounds to zero, not a finding.
                                 #   The honest read is that this lever is CLOSED, on the same evidence the other five closed on.
                                 #   REVERT RULE: none needed — nothing shipped. 0.0 IS the shipped value and it is the INHERIT
                                 #   sentinel, so deleting the key entirely is byte-identical too.
  growth_trend_weight: 0.15      # SCORE: reward per %/yr of the (now long_trend_cap-clamped) leg CAGR.
                                 #   (#3k) 2026-07-29 LOWERED 0.35 -> 0.15 — the WINNER'S-CURSE CURE. New
                                 #   rank-slice/head-to-head instrumentation proved rank 1 was the WORST
                                 #   slice of the top 20 at EVERY horizon (20y-stress: rank-1 med excess
                                 #   +2.6 vs +6.4 for ranks 2-5; #1 beat the 11-20 book in only 5/22
                                 #   windows, 23%): the argmax was owned by overextended names riding an
                                 #   uncapped CAGR reward. Weight ladder at 20y-stress: 0.35 h2h 23% |
                                 #   0.15 h2h 41%, rank-1 med +4.4 | 0.00 h2h 45%, rank-1 med +4.4 but
                                 #   lane edge sags at 12y (+199 vs +218) and top-10 dips everywhere.
                                 #   0.15 (bottom of the 12y plateau, see below) keeps ~80% of the cure
                                 #   AND beats baseline lane edge at every stress horizon: 20y +593.0 vs
                                 #   +552.5, 12y +218.3 vs +205.9, 8y +82.0 vs +62.8 (h2h 50%), 5yL +38.4
                                 #   vs +40.5 (h2h 52%). Top-10 excess never below baseline (+6.1/+6.7/
                                 #   +6.6/+7.6). CURVED at 12y (#3g): 0.15..0.45 is a flat plateau
                                 #   (+184.6/+185.9/+181.3/+183.3) inside the bootstrap band, cliffs above
                                 #   (0.55 +153.8, 1.00 +125.1) — so 12y cannot tell 0.15 from 0.35; the
                                 #   20y-stress argmax quality can, and it picks 0.15. Zeroing outright
                                 #   still costs edge (load-bearing), the term just must not DOMINATE.
                                 #   RE-FIT ON THE cagr-19 POOL 2026-08-01 (Phase B, pb-tw*-*-s.txt): 0.15 HOLDS, and
                                 #   (#3k) is independently RE-VALIDATED on a pool it was not fitted on. 0.25 brings the
                                 #   winner's curse straight back — king rank-1 median +3.4 vs +5.3, h2h 55% vs 60%, and
                                 #   12y rank-1 +3.8|+4.5 vs +4.6|+5.3 — for king edge that is still DOWN (+662.9). 0.10
                                 #   is a plateau wash: king rank-1 identical (+5.5|+5.3 81% h2h 60%) with lane edge -5%
                                 #   (+645.1), 12y -3, 8y +3. No honest room in either direction; do not re-propose
                                 #   without a NEW metric, the way growth_accel_weight earned its raise.
                                 #   RE-PROPOSED AND REFUSED 2026-08-03 (#47, arm C2, 0.15 -> 0.40, `{20,12,8} universe
                                 #   stress`). A new metric WAS brought — 20y-king rank-1 median on the stress pool — and it
                                 #   comes back NEUTRAL (+6.4, identical to baseline) while the curse shows up one horizon
                                 #   down exactly as this receipt predicts: 12y rank-1 median -1.4, 8y h2h 66% -> 53%. Lane
                                 #   edge is UP at all three (+28/+32/+25) and that is not enough, per the rank-1-wins rule
                                 #   this knob was shipped under; the gains are inside the bootstrap band besides. 0.15 STANDS,
                                 #   now bracketed at 0.10 / 0.15 / 0.25 / 0.40. Full grid at growth_accel_weight (#47).
                                 #   (#70) RE-GRADED UNDER SHIP RULE v2 ON 2026-08-11. ALL THREE RUNGS REFUSED; 0.15 STANDS.
                                 #   THE ROUND WAS OWED, and by this knob more than any other: (#47) refused 0.40 on 12y
                                 #   rank-1 MEDIAN and 8y h2h 66->53%, and v2 demoted the first to a non-judge and set the
                                 #   second's bar at >=50%, which 53% clears. This is the one knob in the file refused
                                 #   PRECISELY on what v2 retired. Its config had also moved twice since: (#62) zeroed
                                 #   smoothness, (#63) cut accel 0.65->0.50, and (#63) recorded that the accel cut shifted
                                 #   this knob's whole sweep up one row. And both (#62) and (#63) logged its wide curve as an
                                 #   explicit lead ("a lead for a wide run, not a licence to move the knob").
                                 #   THE ARM WAS STRUCTURAL, NOT FITTED. accel subtracts long_cagr, so for any name inside
                                 #   the accel clamp — the accelerating cohort this lane exists to hunt — d(base)/d(CAGR) =
                                 #   growth_trend_weight - growth_accel_weight = 0.15 - 0.50 = -0.35 per %/yr. Past CAGR 30
                                 #   the trend term saturates on long_trend_cap and it worsens to -0.50. Term ceilings agree:
                                 #   trend maxes at 0.15x30 = 4.5 pts, accel at 0.50x50 = 25. THE LANE PENALISES A HIGHER
                                 #   LONG-RUN CAGR, and raising this weight to 0.55 flips the slope to +0.05.
                                 #   TWELVE WIDE RUNS, {0.15,0.35,0.55,0.70} x `backtest {20,12,8} universe`, 4997 tickers,
                                 #   this knob the only thing changed, rule fixed BEFORE the first run. Control is 0.15 run as
                                 #   its own probe. Data conditions identical per horizon (windows 36/51/59; growth-scored
                                 #   351/524/692 in every arm). DRIFT CONTROL: the 0.15 arm reproduces (#69)'s cap-100 control
                                 #   at 20y BYTE-FOR-BYTE across the entire top-3 line, OOS and hold count included.
                                 #     horizon  w     top3 mean  top3 med  worst  win%  rank-1 h2h  lane edge  winsor   rho
                                 #     20y      0.15      +6.3      +5.7   -0.5    97%     74%        +707.3   +627.2  +0.23  <- ctl
                                 #     20y      0.35      +6.3      +6.4   -3.5    94%     58%        +734.1   +654.0  +0.25
                                 #     20y      0.55      +6.1      +6.4   -3.5    94%     53%        +779.2   +694.2  +0.25
                                 #     20y      0.70      +6.1      +6.4   -3.5    94%     58%        +782.3   +697.2  +0.25
                                 #     12y      0.15      +6.8      +6.9   -7.8    84%     63%        +177.5   +148.5  +0.17  <- ctl
                                 #     12y      0.35      +6.7      +6.9   -7.8    84%     67%        +185.9   +156.2  +0.18
                                 #     12y      0.55      +6.6      +6.9   -7.8    84%     63%        +180.2   +150.6  +0.19
                                 #     12y      0.70      +6.5      +6.9   -7.8    84%     59%        +187.4   +157.7  +0.19
                                 #      8y      0.15      +7.7      +8.3   -9.2    81%     60%         +43.9    +43.3  +0.18  <- ctl
                                 #      8y      0.35      +7.3      +7.9   -9.2    81%     46%         +70.1    +69.4  +0.21
                                 #      8y      0.55      +7.3      +8.0   -9.2    81%     51%         +78.9    +75.8  +0.21
                                 #      8y      0.70      +6.9      +7.3   -9.2    81%     51%         +86.6    +83.4  +0.21
                                 #   REFUSED, all three rungs, on the TOP-3 WORST-WINDOW GUARD at 20y: -0.5 -> -3.5 at every
                                 #   rung. A broken guard refuses outright and is never softened, so the round stops there.
                                 #   0.35 breaks a SECOND guard — 8y rank-1 h2h 46%, under the 50% bar — which is (#47)'s own
                                 #   66->53% finding reproduced and deepened on the new pool, at a rung BELOW the 0.40 it
                                 #   refused. The primary would have failed anyway: 8y top-3 loses BOTH moments at every rung,
                                 #   monotonically past 0.35 (+7.7|+8.3 -> +7.3|+7.9 -> +7.3|+8.0 -> +6.9|+7.3).
                                 #   THE SIZE OF THE BREAK, stated because a receipt that hides it is worthless: win% 97 -> 94
                                 #   on n=36 is EXACTLY ONE WINDOW flipping from win to loss, and that one window loses 3.5.
                                 #   The rule was fixed before the runs and a broken guard is a refusal, so this closes the
                                 #   question — but the honest magnitude is one window, not a collapse. What makes it a real
                                 #   effect rather than noise is that it lands at -3.5 IDENTICALLY at 0.35, 0.55 and 0.70:
                                 #   one name enters the top 3 as soon as the weight clears some threshold and stays there.
                                 #   THE MECHANISM, and it is the part worth keeping. THE CURVE WAS RIGHT ABOUT WHAT IT
                                 #   MEASURES AND STILL DOES NOT GRADE. Lane edge rose at all three horizons exactly as the
                                 #   (#62)/(#63) curve promised (20y +707->+782, 12y +178->+187, 8y +44->+87), winsor tracked
                                 #   it point for point so it is not leaning on extreme rows, rho rose at all three, and
                                 #   OOS-early rose. Every column that motivated this round did what it said. The graded book
                                 #   got WORSE anyway. Lane edge is a top-half-vs-bottom-half spread over ~350-700 windows and
                                 #   is BLIND TO THE TOP-3 BOOK'S TAIL: rewarding CAGR harder pulls high-CAGR names up the
                                 #   whole list, which widens that spread, and simultaneously puts one name in the top 3 that
                                 #   gives back 3.5 pts/yr in the worst window. At 20y it even improves the top-3 MEDIAN
                                 #   (+5.7 -> +6.4) while costing the mean and the tail — better typical window, worse bad one.
                                 #   THIRD TIME A LANE-LEVEL METRIC HAS DISAGREED WITH BOOK QUALITY HERE, and the pattern is
                                 #   now worth naming: (#63) accel trades lane edge for rank-1 quality; (#69) an ablation Δ
                                 #   tracks a term's MAGNITUDE contribution to `base`, not its ranking authority; (#70) an edge
                                 #   CURVE tracks lane spread, not the graded basket's tail. None of the three is a proxy for
                                 #   the book. Use them to CHOOSE an arm, never to grade one.
                                 #   AND THE NEGATIVE SLOPE IS NOT, BY ITSELF, A DEFECT. d(base)/d(CAGR) = -0.35 is real
                                 #   arithmetic and correcting it does exactly what theory says — rho and edge both rise. It
                                 #   just does not buy a better basket. Do not re-propose this knob on the slope argument
                                 #   alone; it has now been brought, measured, and refused on the metric that ships.
                                 #   (#178) RE-CHECK AT THE NEW BASKET SIZE — REFUSED, AND NOW DEAD PERMANENTLY.
                                 #   (#70) refused 0.35 on a TOP-3 worst window; (#120) moved the graded basket
                                 #   to top-10, which CLAUDE.md says makes any such refusal re-checkable, so it
                                 #   was re-checked on `universe fund stress` — the lane (#70) set it on, and
                                 #   non-vacuous (median gated pool 11 > basket 10 at both horizons). It loses
                                 #   on PRIMARY at BOTH horizons (20y median +6.0 -> +5.4, 8y mean +6.9 -> +6.8)
                                 #   and ALSO breaks the (#160) not-worse bar (8y 6-10 h2h 68% -> 66%, below the
                                 #   control's tally), so it REFUSES OUTRIGHT. A refusal on the survivor-
                                 #   flattered lane does not get re-litigated on the honest one. 0.15 STANDS.
                                 #   NOTE (#130)(d): this knob and `growth_accel_weight` are near-substitutes,
                                 #   so this is a partial view of ONE slope — sweeping accel independently of
                                 #   `growth_accel_beta` is still open. Full receipt: the (#178) block on
                                 #   `growth_value_weight`.
  growth_accel_weight: 0.50      # SCORE: reward per pt the last year outpaced the long CAGR.
                                 #   (#137) P0b arm 2 — RE-SWEPT ON THE POINT-IN-TIME POOL 2026-08-25. NO-SHIP; 0.50 STANDS.
                                 #   This discharges the PIT half of the DEBT block for `growth_accel_weight`, the LAST of the five
                                 #   and the only one that is a WEIGHT: no `gate_sweep` row can ever reach it, because a weight moves
                                 #   no admission (`growth_min_score` is display-only — `print_lane`, not the backtest — and ships at
                                 #   -100.0 where it cannot fire). Every reading in the (#70)/(#47) receipts below is `stress` at
                                 #   top-3, the basket (#120) retired on 2026-08-22. Two independent staleness claims, both answered.
                                 #
                                 #   METHOD. Six runs, `backtest {20,12,8} universe fund pit`, two overlays under tests/ (recreate per
                                 #   the rule at `print_book_deciles`; DELETED after the round per (#131)):
                                 #       buy_heuristic:                          buy_heuristic:
                                 #         print_h2h_mid_ladder: true              print_h2h_mid_ladder: true
                                 #                                                 growth_accel_weight: 0.35
                                 #   `heuristic_drift` named exactly those fields and nothing else on all six. windows scored
                                 #   1123/2731/5941 — no OFFLINE collapse. The control reproduces (#135)'s PIT control to the decimal
                                 #   (top-10 +0.8|+0.9 / +2.8|+2.8 / +4.5|+4.5, worst -10.0/-9.5/-6.2, admitted 117/228/325), so this
                                 #   is demonstrably that lane and the two rounds are comparable.
                                 #
                                 #   WHY 0.35 AND NOT A LADDER: it is (#70)'s OWN RUNNER-UP, chosen by prior evidence rather than
                                 #   fitted here. On that grid it beat 0.50 at 12y on BOTH moments (+7.2|+7.8 vs +7.1|+7.4) with the
                                 #   better h2h (67% vs 60%) and lost only the 8y median — on the retired top-3 basket. Round 10's
                                 #   precedent, in its words: one arm at a point prior evidence already flags, "not a ladder re-fitted
                                 #   to a fresh objective, which is how a file overfits itself".
                                 #
                                 #     horizon   top-10 exc|med             worst           h2h vs 6-10   vs 11-20   admitted
                                 #       20y     +0.8|+0.9 -> +0.8|+0.9   -10.0 -> -10.0     50% -> 50%   50% -> 50%   117 -> 117
                                 #       12y     +2.8|+2.8 -> +2.8|+2.8    -9.5 ->  -9.5     61% -> 67%   50% -> 50%   228 -> 228
                                 #        8y     +4.5|+4.5 -> +4.4|+4.5    -6.2 ->  -6.2     65% -> 69%   67% -> 67%   325 -> 325
                                 #
                                 #   VERDICT: NO-SHIP, and deliberately NOT "unresolved". BOTH GUARDS PASS — rank-1 h2h clears 50% on
                                 #   BOTH comparison books at all three horizons and IMPROVES by 4-6 points where it moves, and the
                                 #   worst window is byte-identical everywhere. PRIMARY FAILS: the 8y top-10 MEAN falls +4.5 -> +4.4
                                 #   while 12y and 20y hold exactly. No horizon is UP, and "a flat horizon is not a disagreeing one",
                                 #   so this is a plain NO-SHIP rather than a TIE. Precedence does not rescue it either: GUARD > PRIMARY
                                 #   means a broken guard refuses outright, never that a passing guard makes a failed primary shippable.
                                 #
                                 #   *** THE FINDING THAT OUTLIVES THE VERDICT: ON THE PIT POOL, PRIMARY IS NEARLY BLIND TO RANKING.
                                 #   READ THIS BEFORE RUNNING ANY WEIGHT ARM ON `pit`. ***
                                 #   The arm REORDERS the book substantially and the graded basket barely registers it:
                                 #     20y   18 of 19 graded lines BYTE-IDENTICAL. The only line that moves at all is top-3
                                 #           (+0.1|+0.9 -> +0.4|+1.1). top-10, rank-1 and BOTH h2h lines are unchanged.
                                 #     12y   top-1 +0.6|+4.1 -> +0.7|+4.4, top-2 +1.7 -> +2.2, top-3 median +3.5 -> +4.5 (+1.0)
                                 #           — and EVERY RUNG FROM top-10 TO top-50 IS BYTE-IDENTICAL.
                                 #      8y   top-2 +5.2 -> +5.8, top-3 +5.3 -> +5.6, and top-10 moves 0.1 on the MEAN alone.
                                 #   The ranking demonstrably changed: at 12y the top-10 zero-names list comes back in a different
                                 #   ORDER with the same members. The book moved; the basket could not see it.
                                 #   MECHANISM, read off the HOLDS column of these same runs rather than inferred: the graded book
                                 #   holds 107 / 196 / 267 name-slots over 20 / 34 / 42 windows = 5.4 / 5.8 / 6.4 NAMES PER WINDOW,
                                 #   against a basket of 10, because the gates admit that few. `book_multiples` takes `n.min(vv.len())`,
                                 #   so in every window admitting <=10 the top-10 book IS the entire admitted set and reordering it is
                                 #   a no-op. Only slots beyond rank 10 can move, and the WHOLE SAMPLE holds 7 of them at 20y (holds
                                 #   107 -> 114 at top-15), 14 at 12y (196 -> 210) and 16 at 8y (267 -> 283).
                                 #   CONSEQUENCE, and it is a rule rather than an observation: the PIT lane CAN grade a GATE — round
                                 #   10's arm moved admitted 117 -> 183 / 228 -> 374 / 325 -> 640 — but it CANNOT grade a WEIGHT to
                                 #   better than ~0.1 pt. Here 0.1, at one horizon, on one moment, was the entire signal, and it
                                 #   decided the verdict. Do NOT run another weight arm on `pit` expecting PRIMARY to answer; it will
                                 #   read "held" almost whatever the weight does. This is the structural twin of (#127)'s `out-ranked`
                                 #   column (0.2% / 0.0% / 1.5% of missed wealth): that measured selection dead on RECALL, this measures
                                 #   it near-ungradeable on the BOOK — opposite direction, same pool, same conclusion.
                                 #
                                 #   WHAT THE ARM DOES BUY, recorded because it is real and because it is not enough: rank-1 h2h rises
                                 #   61 -> 67% (12y) and 65 -> 69% (8y) on the licensed 6-10 book, i.e. 0.35 makes the TOP of the list
                                 #   beat its own middle MORE often. The 11-20 book is unchanged at 50 / 67%, so the two books sit on
                                 #   the same side of 50% on every arm and horizon here and (#135)'s revert rule does NOT fire again.
                                 #   A future round wanting this weight lower needs a graded statistic that reads rank 1 and is NOT the
                                 #   guard. The guard cannot be promoted to judge — that is (#135)'s standing warning against preferring
                                 #   the bigger denominator, applied to a different column.
                                 #
                                 #   THE 20y COLUMN IS REPORTED AND NOT GRADED, per (#126)'s substitution precedent, and this is stated
                                 #   as a DEVIATION from PRIMARY's letter rather than passed over: (#124) puts PIT 20y n_eff at ~0.2,
                                 #   the 6-10 book is unlicensed there because (#135)'s revert rule fired on it, and the 11-20
                                 #   denominator is 2. The verdict rests on 12y and 8y. It would not change if 20y were graded — 20y
                                 #   holds exactly, on both books and both moments.
                                 #
                                 #   REVERT: none to execute; nothing shipped and 0.50 is the value in this file. TO REOPEN, one of:
                                 #   (a) a lane whose admitted book EXCEEDS 10 names per window, which today means `stress` and so
                                 #   re-inherits the lookahead the PIT arm exists to remove; or (b) a graded statistic that reads rank 1
                                 #   and is not the h2h guard. Do NOT reopen on the h2h improvement above — it is a guard, and a guard
                                 #   that passes is not evidence for shipping.
                                 #   (#70) RE-GRADED UNDER SHIP RULE v2 ON 2026-08-11 AND CONFIRMED AT 0.50, zero new runs.
                                 #   (#63) below shipped this value on 2026-08-10 graded on (#47)'s rank-1 rule — the primary
                                 #   v2 had retired FOUR DAYS EARLIER. Re-grading its twelve surviving run files on top-3, the
                                 #   way v2 itself re-graded `fixed_cagr_years` (same files, no new compute):
                                 #     arm        20y t3 mean|med   12y          8y            h2h 20/12/8   worst
                                 #     0.65 ctl   +6.4|+6.6         +7.0|+6.9    +7.9|+8.4     67/60/58      -0.5/-7.8/-9.2
                                 #     0.35       +6.5|+6.6         +7.2|+7.8    +7.9|+8.3     76/67/58      identical
                                 #     0.50 <--   +6.4|+6.6         +7.1|+7.4    +8.0|+8.7     76/60/63      identical
                                 #     0.80       +6.5|+6.6         +7.0|+6.9    +7.7|+8.2     71/63/55      identical
                                 #   0.50 holds the primary at 20y and takes BOTH moments up at 8y, with both guards passing
                                 #   and the worst window byte-identical everywhere. 0.35 fails the primary on 8y median, 0.80
                                 #   on both 8y moments. So the knob survives a rule it was not graded under.
                                 #   IT ALSO SURVIVES ITS OWN ESCAPE CLAUSE, and that is the part to read: (#63) hedged that
                                 #   0.35 "owns lane edge, rho, OOS and top-3 at most horizons" and that a re-measurement
                                 #   failing to reproduce its +7.0 rank-1 spike should trigger a revert. On top-3 — the metric
                                 #   that now decides — 0.35 does NOT own it: it loses the 8y median outright. The clause is
                                 #   discharged, not merely unexercised.
                                 #   (#47) "DOES A HIGHER CAGR DESERVE A BETTER RANK?" — asked 2026-08-03. Answer, measured:
                                 #   NO, on all four levers proposed. NOTHING SHIPPED. Values below are UNCHANGED; this is a
                                 #   null receipt, recorded so the question is not re-opened without a new metric.
                                 #   THE ARITHMETIC IS REAL. trend = 0.15·min(cagr,30) rewards CAGR; accel =
                                 #   0.65·clamp(1Y−cagr,0,50) subtracts it. For any name whose last year outpaced its long
                                 #   run — the whole cohort this lane hunts — d(base)/d(cagr) = 0.15 − 0.65 = −0.50 per %/yr.
                                 #   Above long_trend_cap the trend term saturates and the slope worsens to −0.65; at the
                                 #   accel clamp it flattens to 0 (where NVDA sits, per the LIVE A/B below). peg_yield offsets
                                 #   ~+0.28/pt at a 4% earnings yield for stocks with fundamentals; nothing offsets it for
                                 #   ETFs or coins. Term ceilings say the same: trend maxes at 4.5 pts, accel at 32.5.
                                 #   MEASURED: 27 runs, `backtest {20,12,8} universe stress`, 9 arms one field apart, each
                                 #   arm a full copy of THIS file under FOLIOMAN_CONFIG (its grandparent is the repo root, so
                                 #   the warm cache serves all of them). A drift-control repeat of baseline came back
                                 #   BYTE-IDENTICAL at all three horizons, so every delta below is the knob, not the pipeline.
                                 #   Primary metric = rank-1 MEDIAN excess at the 20y king (lottery-ticket-immune), with h2h
                                 #   #1-vs-11-20 >= 50% required at all three horizons. Baseline: 20y +6.2|+6.4 87% h2h 76%
                                 #   edge +452.8 | 12y +5.8|+7.2 74% 63% +127.9 | 8y +7.2|+8.2 76% 66% +65.0.
                                 #     arm                                 20y r1med (Δ) h2h edge |  12y Δ  |  8y Δ
                                 #     A  accel 0.65->0 + mom121 0->0.65     +4.3 (-2.1) 57% +389 |  -1.4   |  -1.2
                                 #     B1 mom121 0->0.20 (additive)          +4.6 (-1.8) 71% +371 |  -0.7   |  -0.4
                                 #     B2 mom121 0->0.40                     +4.6 (-1.8) 71% +338 |  -0.7   |  -0.8
                                 #     B3 mom121 0->0.65                     +4.3 (-2.1) 62% +298 |  -1.4   |  -1.7
                                 #     C1 long_trend_cap 30->60              +6.4 ( 0.0) 81% +337 |   0.0   |   0.0
                                 #     C2 growth_trend_weight 0.15->0.40     +6.4 ( 0.0) 76% +481 |  -1.4   |   0.0
                                 #     D  growth_trust_ladder off->on        +4.6 (-1.8) 71% +407 |  -0.3   |  -1.7
                                 #   VERDICT. Not one arm RAISES rank-1 median at the king; five lower it at all three
                                 #   horizons. The slope is real arithmetic with NO realised cost at the shipped clamp — the
                                 #   subtraction is not what is limiting argmax quality, so removing it buys nothing and costs
                                 #   the accel term's measured (#3k-era) rank-1 authority. A and B are the direct disproof:
                                 #   12-1 momentum is a strictly WORSE substitute (A) and a strictly worse ADDITION (B1..B3,
                                 #   monotone damage in the weight), which also re-answers "is accel just momentum?" — no.
                                 #   C1/C2 are the two halves of "reward CAGR harder", kept separate so a win could be
                                 #   attributed; both come back rank-1 NEUTRAL at the king. C1 lifts king h2h to 81% (best in
                                 #   the grid) for -115 edge — which RECONFIRMS (#3g)/(#3h): the cap's edge wants to come DOWN,
                                 #   not up, and 60 is on the far side of the measured optimum. C2 lifts lane edge at all three
                                 #   (+28/+32/+25) and is still refused, because it reproduces the exact winner's curse (#3k)
                                 #   shipped 0.15 to cure — 12y rank-1 median -1.4 and 8y h2h 66% -> 53% — and (#3k) closes with
                                 #   "do not re-propose without a NEW metric". The new metric was applied and it says no. Every
                                 #   C2 gain also sits deep inside the baseline's own [+199.8 … +691.4] bootstrap band.
                                 #   D is the graded record ladder (see growth_trust_ladder): the binary 10Y trust cliff really
                                 #   does throw away resolution, and grading it costs rank-1 at every horizon anyway.
                                 #   (#100 / finding C4) TWO CORRECTIONS TO HOW D WAS DESCRIBED, neither of which
                                 #   overturns the refusal. FIRST, THE DOCK IS NOT 0.5. `combine_damps` is a 2-slot
                                 #   geometric mean, so the shipped cliff delivers sqrt(0.5) = 0.707 — a 29.3% dock,
                                 #   not 50% — and arm D was graded with that square root in place. Anyone reasoning
                                 #   about "half the score" is reasoning about a number this lane never applies.
                                 #   SECOND, THE CLIFF IS A STEP ON A LEG THE SCORE OTHERWISE IGNORES. A listing of
                                 #   ten years and one day scores 1.0 identically to a 46-year compounder, and a
                                 #   9.9-year one takes the full dock, while `long_leg` ranks on the 20/8/5 ladder —
                                 #   so the 10Y leg's ONLY function in the whole growth lane is this cliff, and an
                                 #   8Y-rung name is ranked on 8Y and docked for not having 10Y.
                                 #   STANDING VERDICT UNCHANGED: D was measured under v2 and lost rank-1 at every
                                 #   horizon. What is now recorded is that the thing measured was a 29.3% step, so a
                                 #   future re-grade should move the DOCK SIZE, not re-run the same ladder.
                                 #   THE FOURTH LEVER — "rank a name better the FARTHER it sits below its high" (OFF-HI, i.e.
                                 #   drawdown_pct) — was NOT run, because two shipped receipts already answer it and a third
                                 #   arm would only re-buy them: rewarding dip depth IS the on-sale lane, which this repo keeps
                                 #   as a FOIL precisely because `backtest` shows dip-buying has negative multi-decade edge
                                 #   (see discount_weight, "deepest-dip ranking is BACKWARDS on peer-relative selection", and
                                 #   growth_min_range_pct, "80 -> 70 -> 50 is monotone damage"). The softer reading — loosen the
                                 #   near-high gate so a dipped compounder can appear — is answered by the funnel instead: the
                                 #   range gate rejects 780 names and is the SOLE blocker of exactly zero, so loosening it
                                 #   returns nobody. That funnel claim is now pinned offline (`sole_blocking_gates_are_pinned`).
                                 #   RAISED 0.5 -> 0.65 (2026-08-01, Phase B re-fit on the cagr-19 pool, pb-aw*-*-s.txt).
                                 #   THE THIRD WINNER'S-CURSE CURE, and the only knob of the three re-fitted that moved.
                                 #   Rank-1 quality UP at ALL THREE horizons: 20y-s +6.0|+6.6 win 83% h2h 65% (vs +5.4|+5.3
                                 #   81% 60%), 12y-s +5.6|+5.9 69% (vs +4.6|+5.3 67%), 8y-s +6.6|+7.2 73% (vs +5.6|+6.1 69%).
                                 #   King top-10 MEDIAN +6.1 vs +5.7; 8y lane edge +77.0 vs +71.8 (+7%).
                                 #   THE PRICE, stated: king lane edge -29 (+648.2 vs +677.4, -4%, deep inside the ±260
                                 #   bootstrap band), 12y flat (-2), 12/8y h2h -3/-2, 8y top-10 -0.2 mean / -0.5 med.
                                 #   Same trade the cagr 19 ship took (it paid 12/8y edge -4/-7% for rank-1) — precedent, not
                                 #   a new standard.
                                 #   BRACKETED BOTH WAYS, unlike the 0.5 knee below: 0.3 / 0.5 / 0.65 / 0.8 / 1.0 give king
                                 #   h2h 40 / 60 / 65 / 65 / 60% and rank-1 median +2.2 / +5.3 / +6.6 / +6.6 / +5.3. Rank-1
                                 #   PLATEAUS across [0.65, 0.8] — byte-identical there — and 0.65 is the CHEAP end of that
                                 #   plateau: king edge +648.2 vs 0.8's +627.3, and 8y +77.0 vs +68.4 (rank-1 med +7.2 vs
                                 #   +6.1). At 1.0 rank-1 falls back AND edge craters (+542.3). Same "top of the plateau, at
                                 #   its cheapest point" rule the PEG tilt shipped under.
                                 #   SUPERSEDES the "0.65 regresses, the knee is 0.5" note below, and says why rather than
                                 #   overwriting it: that hill-climb graded 12y LANE EDGE on the pre-(#3k), cagr-14 pool.
                                 #   This grid grades 20y RANK-1 on the cagr-19 pool, where 12y edge is FLAT (+212.8 vs
                                 #   +215.1), not regressed. Different objective, different pool, opposite verdict.
                                 #   MECHANISM (reconciles the "accel HURTS" note further below): accel TRADES LANE EDGE FOR
                                 #   RANK-1 QUALITY. At 0.3 king edge is the best of the accel arms (+668.6) and rank-1 the
                                 #   worst measured anywhere (median +2.2, h2h 40%); at 0.65 edge gives back 29 pts and rank-1
                                 #   is the best measured anywhere. Both old notes were right about their own metric.
                                 #   ORTHOGONALITY (Phase B's other finding): growth_overext_floor is a pure LANE-EDGE lever
                                 #   with ZERO rank-1 authority, this knob is the reverse. Tune them independently.
                                 #   LIVE A/B (2026-08-01, full `screen` at 0.5 and 0.65 back to back): SAME NAMES, SAME ORDER
                                 #   in every table — stocks (NVDA alone, 10.8 at both: its accel term sits at a clamp) and ETFs
                                 #   (IITU.L / XLKS.L / SMH.L / SEMI.AS). What DID move is the ETF SCORES: 8.1->8.2, 7.8->7.9,
                                 #   and the two semi funds 5.1->6.2 / 5.0->6.0. That second pair is the point: at 0.5 they cleared
                                 #   growth_min_score_etf (5.0) by 0.1 and 0.0 — one datapoint from being trimmed off the table
                                 #   entirely — and at 0.65 they carry a full point of margin. So this ship is backtest-measurable,
                                 #   live-RANK-neutral today (the cagr-19 gate leaves one stock row, so rank-1 has nothing to
                                 #   reorder), and live-STABILITY-positive on the ETF lane. The rank-stability and CORE-shortlist
                                 #   deltas printed between the two runs are the state file the first run wrote, not the knob.
                                 #   PRIOR RECEIPT, kept for the 12y-edge history: accel INVERTS with horizon —
                                 #   the 5y proxy wanted it trimmed (mean-reverts: removing it +41.7 edge), but the 12y MAX-monthly
                                 #   horizon (the 20y-hold target) makes it a STRONG helper: removing it costs -55.5..-65.9 edge.
                                 #   Hill-climbed at 12y: 0.2->0.35->0.5 edge +65.8->+84.5->+91.1; 0.65 regresses (+87.0, OOS-early
                                 #   +0.02), so the knee is 0.5. Persistent compounding (last year still beating the long CAGR) PREDICTS
                                 #   over a decade where it merely chased a hot streak over 5y. ORIG 5y note below:
                                 #   rho LIKES accel (rank helper) but the 5y EDGE ablation showed it HURTS the 5y profit
                                 #   spread — recent 1Y-vs-CAGR pop is the noisiest, most mean-reverting growth signal at 5y
                                 #   (hot-streak chasing). 0.2 lifts wide 5y edge +43.7->+50.5 / 3y +24.3->+28.5.
  # (#63) 0.65 -> 0.50 on 2026-08-10. NOT a new opinion about this knob — a re-measurement forced by the
  #   fact that 0.65 was fitted as one term of a JOINT fit whose other term no longer ships. Phase B
  #   (2026-08-01) moved growth_accel_weight 0.5->0.65, growth_trend_weight, and growth_overext_floor
  #   TOGETHER, with growth_smoothness_weight at 5.0. (#62) zeroed smoothness yesterday. The two terms
  #   pull OPPOSITE ways — smoothness paid trend_r2 (straight-line), accel pays a recent pop (lumpy, low
  #   R²) — so 0.65 was the value that survived a counterweight that has since been removed. This round
  #   re-verifies the plateau on the config that actually ships.
  #   THE GRID — `backtest {20,12,8} universe`, 4997 tickers, 12 runs, this knob the only thing changed.
  #   Graded on (#47)'s rule, FIXED BEFORE ANY RUN: PRIMARY = rank-1 MEDIAN at the 20y king; GATE = h2h
  #   #1-vs-11-20 >= 50% at all three; re-weights must make rank-1 RISE, not merely hold; lane edge and
  #   rho read against each arm's OWN null floor and never vote.
  #     20y king   rank-1 mean|MED   win%   h2h #1 vs 11-20   rho    lane edge   own null floor
  #     0.35       +6.1|+6.4         87%    76%               +0.17  +466.5      -216.7
  #     0.50  <--  +6.6|+7.0         90%    76%               +0.15  +414.4      -185.3
  #     0.65 ctl   +6.4|+6.4         90%    67%               +0.13  +418.1      (baseline)
  #     0.80       +6.4|+6.4         90%    71%               +0.11  +329.7      -157.4
  #     12y: ctl +6.5|+7.8 60% +125.5 · 0.50 +6.7|+7.8 60% +146.4 · 0.35 +6.3|+7.8 67% +158.0 · 0.80 +6.5|+7.8 63% +121.6
  #      8y: ctl +7.4|+8.2 58%  +45.2 · 0.50 +7.6|+8.4 63%  +56.9 · 0.35 +7.2|+8.2 58%  +70.1 · 0.80 +7.4|+8.2 55%  +44.9
  #   0.50 is the only arm that raises the primary metric (+6.4 -> +7.0), passes the gate at all three
  #   horizons (76/60/63%), and is BRACKETED BOTH WAYS — Phase B's own standard. The control reproduced
  #   the committed baseline to the digit (481/668/852 windows, +418.1/+125.5/+45.2), so this is a batch,
  #   not drift.
  #   THE FRAGILITY, stated because a receipt that hides it is worthless: the primary metric is a
  #   SINGLE-POINT MEDIAN SPIKE on n=39 king windows. 0.35, 0.65 and 0.80 all read +6.4; only 0.50 reads
  #   +7.0. One window moving reverses it. What supports it is that it is not alone — rank-1 MEAN, win%,
  #   h2h, OOS-early and top-3 all move WITH it at multiple horizons, while 0.35 has rank-1 mean DOWN at
  #   all three. A spike with five corroborating columns is worth more than a spike alone, and less than
  #   a plateau. If this is ever re-measured and +7.0 does not reappear, revert on that.
  #   THE COLUMN THAT ARGUES FOR 0.35: it owns lane edge (+466.5/+158.0/+70.1), rho (+0.17), OOS
  #   (+0.16|+0.21) and top-3 at most horizons. That is not a contradiction, it is the MECHANISM already
  #   documented above — accel TRADES LANE EDGE FOR RANK-1 QUALITY — reproduced a third time. A reader
  #   who valued the held book over the single top pick would ship 0.35. The rule says rank-1 primary,
  #   the rule was fixed before the runs, so 0.50 ships. Both readings are defensible; only one is ours.
  #   THE OLD 12y HILL-CLIMB WAS RIGHT ABOUT THE NUMBER. The "knee is 0.5" note above was superseded by
  #   Phase B for a good reason (different pool, different objective). This round lands back on 0.5 from
  #   a third direction — 20y rank-1 on the cagr-19 pool — so all that is left of the supersession is
  #   its explanation, not its verdict.
  #   LIVE A/B (full `screen`, back to back, state file restored between runs so the rank-stability and
  #   CORE-shortlist deltas are readable). Stocks: NVDA #1 at 5.6 in BOTH — unchanged. Crypto: BNB 7.3,
  #   BTC 6.8, unchanged. The ETF lane REORDERS, and the reorder is the whole point:
  #     ETF        1Y      leg CAGR   raw accel      0.65   0.50    Δ
  #     SMH.L      +89.2%  +27%       62 -> CAPPED    5.6    4.6   -1.0
  #     SEMI.AS   +102.1%  +25%       77 -> CAPPED    5.5    4.5   -1.0
  #     IITU.L     +21.0%  +19%        2               4.9    4.8   -0.1
  #     XLKS.L     +22.3%  +20%        2               4.6    4.4   -0.2
  #   Both semis sit AT growth_accel_cap (50), so cutting the weight 0.15 strips 7.5 raw points from each
  #   — the most this term can ever give — and they lose a full point, identically. The two names whose
  #   1Y is within 2pts of their long CAGR lose ~0.1, which is the drift between two runs minutes apart
  #   (prices and the 1H/6H/12H columns all moved). Hot-streak names lose lift, steady compounders do
  #   not. That is this knob's job description.
  #   AND IT RESTORES THE ORDER PHASE B SHIPPED. Phase B ran a live A/B at THIS SAME PAIR (0.5 vs 0.65)
  #   and recorded "SAME NAMES, SAME ORDER" with IITU.L #1 at both. Today at 0.65 that order is INVERTED
  #   (SMH.L first). At fixed accel 0.65, across the smoothness removal: IITU.L 8.2->4.9 and XLKS.L
  #   7.9->4.6, but SMH.L only 6.2->5.6 and SEMI.AS 6.0->5.5. Killing a 5.0 x trend_r2 term cost the two
  #   straight-line names FIVE TIMES what it cost the two lumpy semis, which is what flipped the lane.
  #   So the live reorder here is not new instability, it is the Phase B ordering coming back. CAVEAT,
  #   because those two runs are 9 days apart and not a controlled pair: read the -3.3/-3.3 vs -0.6/-0.5
  #   split as direction and rough magnitude, not as a measurement.
  #   PHASE B'S TRIMMING CONCERN IS MOOT AND ITS RECEIPT ABOVE SHOULD BE READ WITH THIS: it worried that
  #   at 0.5 the semis "cleared growth_min_score_etf (5.0) by 0.1 and 0.0 — one datapoint from being
  #   trimmed off the table entirely". growth_min_score and growth_min_score_etf BOTH ship at -100.0
  #   today. The floors are disarmed; nothing is trimmable at any of these scores. If either floor is
  #   ever re-armed above ~4.5, re-read that concern, because it becomes live again.
  #   CI COLLAPSE GATE checked on this arm's own output before shipping: growth edge +414.4/+146.4/+56.9
  #   all > 0, top-3 excess +6.4/+7.1/+8.0 all > 0, and both OOS halves positive at every horizon
  #   (+0.15|+0.19, +0.07|+0.17, +0.15|+0.14 — up on all six against the control's +0.12|+0.17,
  #   +0.05|+0.17, +0.13|+0.13).
  #   NOT RE-OPENED THIS ROUND, recorded so the runs are not spent twice. accel -> 0: never swept, but
  #   the sweep above collapses monotonically toward zero (rank-1 median +6.6/+5.3/+2.2 and king h2h
  #   65/60/40% at 0.65/0.5/0.3), so zero is off the end of a cliff, and the ablation's "removing accel
  #   lifts edge" is the documented trade working as recorded, not a finding. growth_trend_weight
  #   0.15->0.35: wide curve strongly favourable (edge +418->+540, +125->+160, +45->+77, both OOS halves
  #   rising) but the knob is defended at four bracket points and curve rows print no rank-1 and no h2h,
  #   so they cannot settle a ship rule. sharpe_weight still owes the re-sweep (#62) named.
  #   THE GOLDENS MOVED, and they prove the change rather than merely absorbing it. The fixture config
  #   deep-merges over this file, so all six move; every one kept its exact line count, values only.
  #   (a) The growth_accel_weight ABLATION ROW IS BYTE-IDENTICAL at all three horizons — rho
  #   +0.21/+0.46/+0.19, edge +83.9/+414.2/+24.2, not one digit. That row is "this knob at ZERO", which
  #   cannot depend on what the shipped weight is, so it is a fixed point and it held. Only the Δ shrank,
  #   and the identity endpoint − headline = Δ closes exactly (+83.9 − +11.0 = +72.9, +24.2 − −10.3 =
  #   +34.5). A code bug would have moved the endpoint.
  #   (b) Predicting the headline by LINEAR INTERPOLATION toward that endpoint — 0.65->0.50 is 23.1% of
  #   the way to zero — gives rho +0.15/+0.30/+0.16. Measured: +0.16/+0.30/+0.15. Edge is the noisy
  #   column on 80/52/102 windows and lands looser (12y -5.5 -> +11.0, 8y -54.3 -> -10.3, 20y unmoved).
  #   (c) The ON-SALE lane is byte-identical in all three full reports. accel is growth-only, and that is
  #   now shown structurally rather than asserted.
  #   (d) `12 stress` carries a growth_trend_weight sweep, and cutting accel SHIFTED THAT WHOLE SWEEP UP
  #   EXACTLY ONE ROW, edge and winsor columns reproducing BIT-IDENTICALLY: new 0.15 (+11.0/+5.6) IS old
  #   0.25; new 0.25 (+90.2/+84.8) IS old 0.35; new 0.35 (+108.8/+103.4) IS old 0.55; new 0.55
  #   (+67.9/+62.5) IS old 0.70. On frozen data these two terms are NEAR-SUBSTITUTES — base =
  #   growth_trend_weight·CAGR + growth_accel_weight·accel, and cutting accel 0.15 buys what raising
  #   trend ~0.10 buys. READ THE REFUSED trend 0.15->0.35 LEAD IN THAT LIGHT: it is not independent of
  #   this ship, and re-running it now would be re-measuring a distance that has already partly closed.
  #   Stress OOS moves +0.29|+0.02 -> +0.32|+0.04, both halves up.
  #   THE TWO FIXTURE COLUMNS THAT ARGUE AGAINST, logged because the previous receipt praised one of them
  #   by name: `12 tune`, the only honestly out-of-sample instrument here, drops the shipped default's
  #   TEST rho +0.31 -> +0.24 at an unchanged edge (+144.5) and byte-identical terciles — giving back
  #   more than the +0.26 -> +0.31 that (#62) claimed as a gain. And `12 halflife` is mixed, not good:
  #   3y +0.2 -> +22.0, 5y +49.7 -> +62.2 and 8y -54.3 -> -10.3 improve, but 1y +15.9 -> +13.7, 2y
  #   +37.7 -> +30.2 and 10y -4.2 -> -43.3 get worse, the last of those badly. Neither column votes —
  #   80 hand-picked large caps at 52-102 windows is not the grader, the wide universe is — but a reader
  #   re-opening this knob should know the fixture did not agree with the wide run everywhere.
  #   ONE CLEAN MECHANICAL WIN in the same file: halflife TURNOVER falls at EVERY hold length
  #   (60->59, 62->61, 61->60, 63->61, 62->57, 68->61). accel is the most time-varying input in the
  #   score, so damping it makes the ranking steadier across rebalances. That is a cost reduction that
  #   needs no horizon to be true.
  #   THE PIN MOVED WITH IT: src/picks.rs shipped_config_pin() asserts this value by parsing this file.
                                 #
                                 #   (#179) RE-GRADED ON THE NON-VACUOUS LANE — REFUSED IN BOTH DIRECTIONS. 0.50
                                 #   STANDS, and it is now bracketed on a lane that can actually answer. (#137)
                                 #   above returned NO-SHIP for 0.35 on `universe fund pit`, and the very same
                                 #   receipt records why that verdict is not safe: it says the pit basket CANNOT
                                 #   resolve a weight arm better than ~0.1pt and closes with "Do NOT run another
                                 #   weight arm on `pit` expecting PRIMARY to move". Its verdict then turned on
                                 #   exactly 0.1pt (8y top-10 mean +4.5 -> +4.4). That is a verdict decided by the
                                 #   resolution floor of the instrument. (#178) had just validated `universe fund
                                 #   stress` as the one non-vacuous ranking lane in this file, so the arm was re-
                                 #   run there.
                                 #
                                 #   PRE-REGISTERED BEFORE ANY ARM RAN, and the ladder was fixed in advance: rungs
                                 #   0.35 and 0.65, both reported whatever they read, no rung added after. Neither
                                 #   is fitted here — 0.35 is (#70)'s own runner-up and the exact arm (#137) ran,
                                 #   0.65 is the value Phase B shipped before (#63) reverted it to 0.50. Ship Rule
                                 #   v2 UNCHANGED: PRIMARY reads 20y AND 8y (the (#139) 12y/8y substitution is
                                 #   PIT-LANE-ONLY and was not borrowed); GUARD rank-1 h2h off the 6-10 book at
                                 #   8y, >=50% AND not below the control's tally ((#160)), off the 11-20 book at
                                 #   20y at >=50% absolute with no not-worse bar; GUARD top-10 worst window not
                                 #   worse. Reading rule as in (#178): a refusal on this survivor-flattered lane
                                 #   is PERMANENT, a pass would have proved nothing.
                                 #
                                 #   LANE INTEGRITY. `backtest {20,8} universe fund stress`, six runs.
                                 #   `heuristic_drift` named `growth_accel_weight` and nothing else on all four
                                 #   arm runs. windows scored 2377 / 8608, identical on every run — no OFFLINE
                                 #   collapse. The control byte-reproduces (#178)'s banked control (0 changed
                                 #   lines at both horizons). The POOL CENSUS is IDENTICAL across all six runs —
                                 #   20y 37 buckets, median gated pool 11 > basket 10, 18 saturated; 8y 59
                                 #   buckets, median pool 11, 28 saturated — which is the proof this is a PURE
                                 #   REORDER with no pool effect, as a weight must be. The zero-names profile is
                                 #   identical everywhere too (top-10 rode 0 zeros at both horizons, all three
                                 #   arms), so nothing here is a value trap.
                                 #
                                 #   horizon  arm    top-10 exc|med      worst    h2h 6-10       h2h 11-20      rank-1 exc|med
                                 #     20y    ctl    +5.9|+6.0           -0.5     14/23  61%     12/19  63%     +6.0|+6.0
                                 #     20y    0.35   +5.9|+5.4           -0.5     12/23  52%     10/19  53%     +5.3|+4.2
                                 #     20y    0.65   +5.9|+6.2           -0.5     14/23  61%     12/19  63%     +6.0|+6.0
                                 #      8y    ctl    +6.9|+7.8           -9.2     30/44  68%     18/31  58%     +8.1|+8.5
                                 #      8y    0.35   +6.9|+8.0           -9.2     27/44  61%     17/31  55%     +7.0|+7.5
                                 #      8y    0.65   +6.8|+8.2           -9.2     28/44  64%     18/31  58%     +8.1|+8.5
                                 #
                                 #   VERDICT 0.35: REFUSED, twice over. PRIMARY fails at 20y — the median goes
                                 #   +6.0 -> +5.4 while the mean only holds, and PRIMARY needs mean AND median up
                                 #   or held at 20y AND 8y. AND the (#160) not-worse bar BREAKS at 8y: the 6-10
                                 #   h2h goes 68% -> 61%, above the absolute 50% line but BELOW the control's
                                 #   tally. A broken guard REFUSES OUTRIGHT and is never softened to unresolved.
                                 #   Note what would have happened without (#160): at 20y this arm drops the 11-20
                                 #   h2h 63% -> 53% and the 6-10 book 61% -> 52% and STILL clears the 20y guard,
                                 #   because 20y is an absolute >=50% bar with no not-worse condition. That is
                                 #   exactly the blindness (#160) was written for, and here the 8y not-worse bar
                                 #   is what does the work. (#137)'s pit NO-SHIP for this rung is therefore
                                 #   CONFIRMED, but on evidence that can carry it.
                                 #
                                 #   VERDICT 0.65: REFUSED. PRIMARY fails at 8y — the mean goes +6.9 -> +6.8 DOWN,
                                 #   and a down mean is a fail however good the median looks (+7.8 -> +8.2). The
                                 #   20y column is genuinely the best reading in the table (median +6.0 -> +6.2
                                 #   with every guard flat), so this is a horizons-disagree shape; it does NOT
                                 #   reach the TIE rule, because the (#160) not-worse bar ALSO breaks at 8y (6-10
                                 #   h2h 68% -> 64%), and GUARD outranks both PRIMARY and TIE. This re-confirms
                                 #   (#63)'s revert of 0.65 -> 0.50 on a lane (#63) did not have. Worst window is
                                 #   byte-identical at -0.5 / -9.2 for every arm, so no guard ever turned on
                                 #   drawdown.
                                 #
                                 #   WHAT THIS CLOSES. (#130)(d) found this knob and `growth_trend_weight` are
                                 #   near-substitutes on frozen data — cutting accel ~0.15 buys what raising trend
                                 #   ~0.10 buys — and warned the refused trend lead "is not independent of this
                                 #   ship". Both halves are now dead on the SAME gradeable lane: (#178) killed
                                 #   trend-UP 0.15 -> 0.35, and accel-DOWN 0.50 -> 0.35 dies here. accel-UP dies
                                 #   too, so the one slope those two knobs multiply into is BRACKETED IN BOTH
                                 #   DIRECTIONS. The substitution question (#130) left open is closed with it:
                                 #   there is no rung of either knob, in either direction, that survives on a lane
                                 #   whose basket can see a reorder. Do not re-open this pair without a NEW
                                 #   measurement — not a new rung, and not a new lane weaker than this one.
                                 #
                                 #   AND A SECOND CONFIRMATION OF (#178)'s BLINDNESS FINDING, free from these
                                 #   runs: at 20y the 0.35 arm moves rank-1 from +6.0|+6.0 to +5.3|+4.2 — a 1.8pt
                                 #   median collapse at the king — while the graded top-10 mean does not move at
                                 #   all (+5.9 both). An equal-weight top-10 book is set-invariant, so it cannot
                                 #   price a reorder inside itself; the rank slices and the h2h can. Grade a
                                 #   ranking knob on those, never on the top-10 mean alone.
                                 #
                                 #   MEASUREMENT HYGIENE, second instance after (#176) part 4 and worth the line:
                                 #   `diff A B | grep -c '^[<>]'` reported 0 changed lines for all four arm-vs-
                                 #   control pairs, while `md5sum` and `diff -q` proved every one of them differs
                                 #   (200-354 lines once the drift WARNING is stripped). The 0 was taken as false
                                 #   and the checksums believed. Settle a same-or-different question with `md5sum`
                                 #   or `diff -q`, never with a piped line count.
  growth_accel_beta: 1.0         # (#98) UNMEASURED — an unfitted judgement value, shipping 1.0, which is
                                 #   the original `clamp(1Y − long_cagr, 0, cap)` expression BIT FOR BIT
                                 #   (multiplying by 1.0 is exact in IEEE, so this is not an approximation
                                 #   of the old line, it IS the old line). Read it with `growth_accel_weight`
                                 #   above and `growth_trend_weight`: the three multiply out to one slope.
                                 #
                                 #   THE DEFECT, which this file already half-records at (#70): inside the
                                 #   unclamped accel band — the majority of the pool — the lane's derivative
                                 #   in the quantity it exists to hunt is
                                 #       growth_trend_weight − β·growth_accel_weight = 0.15 − 1.0×0.50 = −0.35
                                 #   per %/yr. A 30%/yr compounder scores BELOW an identical 20%/yr one at
                                 #   the same 1Y return, in a lane whose stated thesis is 20-year compounding.
                                 #
                                 #   WHY THE (#70) GRID DID NOT SETTLE IT. That grid raised growth_trend_weight
                                 #   (0.35/0.55/0.70) and every rung REFUSED on the top-3 worst window, with
                                 #   0.35 also breaking the 8y h2h guard — correctly read there as "do not
                                 #   paper over it". But it moved only ONE factor of the product. β is the
                                 #   other, and it has never been swept. Below β = 0.15/0.50 = 0.30 the slope
                                 #   turns POSITIVE with growth_trend_weight left exactly where the grid put it.
                                 #
                                 #   SECOND EFFECT, deliberate: the clamp floor is at 0, so a smaller β lifts
                                 #   more names off the floor and the leg discriminates across a wider slice
                                 #   of the pool. A β sweep is therefore not a pure slope experiment and the
                                 #   read-out must not be reported as one.
                                 #
                                 #   NOT BUILT: the `ratio` (1Y/long_cagr − 1) and `pctile` (1Y against the
                                 #   name's own rolling 1Y history) forms. `ratio` needs an ε guard at
                                 #   long_cagr ≈ 0 and re-scales the leg against growth_accel_cap, so it
                                 #   cannot share this knob's sweep; `pctile` needs per-name rolling-return
                                 #   history no Quote carries. β is the form that is one line, continuous,
                                 #   and reduces exactly to today at its default.
                                 #
                                 #   TO SETTLE: β over {1.0, 0.5, 0.3, 0.15, 0.0} at 20y/12y/8y under the ship
                                 #   rule. THEN re-grid growth_trend_weight underneath the winning β — the
                                 #   (#70) receipt's own reading is that rho, edge and OOS all want that knob
                                 #   higher and only the worst-window guard refuses, and removing the
                                 #   counterweight is the most plausible way that guard stops binding. The two
                                 #   sweeps are ordered, not independent.
                                 #
                                 #   REVERT RULE: back to 1.0 unless the paired (β, trend_weight) pass clears
                                 #   PRIMARY at 20y AND 8y with rank-1 h2h ≥50% at all three horizons. A β that
                                 #   only lifts LANE EDGE does not ship — that is exactly the trap (#70) names.
                                 #   `picks::shipped_config_pin` asserts both this value and the −0.35 slope it
                                 #   implies; that assert failing is the feature.
                                 #
                                 #   ══ (#130) SWEPT 2026-08-23 — β IS ALREADY AT ITS OPTIMUM, AND THE "DEFECT" ABOVE IS WHY. ══
                                 #   REVERT RULE ABOVE FIRED: β STAYS 1.0. No rung cleared PRIMARY at 20y AND 8y, and two broke the
                                 #   rank-1 h2h guard outright. β = 1.0 is the BEST rung on every column that votes — h2h, rank-1
                                 #   slice, rho, 20y lane edge — at both horizons.
                                 #
                                 #   THE LANE, AND WHY NOT PIT. `backtest {20,8} universe stress`, 5078 tickers, one arm per rung
                                 #   (tests/beta{050,030,015,000}-ci-settings.yaml, kept in-tree), with `heuristic_drift()` naming
                                 #   growth_accel_beta AND NOTHING ELSE on all four arms and nothing on the control. This is NOT the
                                 #   PIT lane and it is NOT "PRE-PIT" either — the PIT universe exists and was DECLINED: on
                                 #   `universe fund pit` the rank-1 h2h rests on 2/4/6 windows at 20y/12y/8y, so a single window
                                 #   swings it 50 points and the revert rule above cannot be executed there at all. `universe stress`
                                 #   is the lane (#47)/(#70) set these values on, h2h denominators 18 (20y) and 34 (8y). Survivor bias
                                 #   is the price paid; an ungradeable guard was the alternative. The (#47)/(#62) stress baselines
                                 #   elsewhere in this file are TOP-3 and predate (#120), so β=1.0 was RE-RUN as rung 1, not quoted.
                                 #
                                 #   THE LADDER. top-10 excess mean|med = PRIMARY; rank-1 h2h = GUARD (denominator shown); rank-1 =
                                 #   the disjoint #1 slice; rho/edge = lane screens, NON-VOTING.
                                 #     20y — n=339, 36 windows
                                 #     β      slope    top-10      worst  h2h        rank-1       rho     edge
                                 #     1.0    -0.35    +6.0|+6.1    -0.5  12/18 67%  +6.4|+7.2   +0.24   +769.4   [SHIPPED]
                                 #     0.5    -0.10    +6.0|+6.1    -0.5   9/18 50%  +5.0|+4.4   +0.18   +477.4
                                 #     0.3     0.00    +5.8|+5.1    -0.5   9/18 50%  +5.0|+4.4   +0.18   +394.2
                                 #     0.15   +0.075   +5.9|+5.6    -0.5  10/18 56%  +4.9|+4.3   +0.17   +392.1
                                 #     0.0    +0.15    +5.9|+5.6    -0.5  10/18 56%  +4.6|+4.3   +0.18   +528.5
                                 #     8y — n=699, 59 windows
                                 #     1.0    -0.35    +6.6|+7.3    -9.2  22/34 65%  +6.9|+7.8   +0.20    +48.7   [SHIPPED]
                                 #     0.5    -0.10    +6.4|+7.3    -9.2  16/34 47%  +5.0|+5.4   +0.17    +61.6
                                 #     0.3     0.00    +6.3|+6.8    -9.2  16/34 47%  +5.3|+5.8   +0.16    +51.5
                                 #     0.15   +0.075   +6.3|+6.8    -9.2  19/34 56%  +5.4|+5.4   +0.17    +66.4
                                 #     0.0    +0.15    +6.5|+7.2    -9.2  17/34 50%  +5.4|+5.4   +0.17    +79.2
                                 #
                                 #   THE VERDICT, CLAUSE BY CLAUSE. GUARD worst-window: NEVER BIT — every rung reproduces the
                                 #   control's -0.5 / -9.2 exactly. GUARD rank-1 h2h: β=0.5 and β=0.3 come back 47% at 8y -> REFUSED
                                 #   OUTRIGHT, never softened to unresolved. PRIMARY: β=0.15 and β=0.0 keep their guards but are down
                                 #   on mean AND median at BOTH graded horizons -> dead. β=0.5 holds 20y exactly (+6.0|+6.1) and loses
                                 #   the 8y mean, so it fails PRIMARY as well as its guard.
                                 #   12y WAS DELIBERATELY NOT RUN, and that is a decision rather than a gap: the pass was ordered
                                 #   20y+8y first so that dead rungs stop costing runs, and NO rung reached the third horizon
                                 #   PRIMARY-clean. Five runs saved. A rung down on PRIMARY at both graded horizons cannot be rescued
                                 #   by a 12y guard.
                                 #
                                 #   WHY β = 1.0 WINS, AND WHY THE -0.35 ARITHMETIC ABOVE IS THE WRONG READ OF IT. At β = 1.0 the leg
                                 #   is `1Y − long_cagr`: 1Y return measured against THE NAME'S OWN long-run rate, i.e. a
                                 #   self-normalising RESIDUAL. That is the only β at which the baseline is the name's own rate; any
                                 #   β < 1 makes it an arbitrary FRACTION of that rate, which is neither a level nor a residual. The
                                 #   -0.35 above is a PARTIAL derivative AT FIXED 1Y RETURN — a quantity no name in the pool holds
                                 #   fixed. What ranks the pool is the TOTAL derivative, 0.15 + 0.50·(d(1Y)/d(CAGR) − β): a faster
                                 #   compounder also tends to return more in a typical year, so d(1Y)/d(CAGR) ≈ 1 and β = 1.0 is
                                 #   precisely the value that ZEROES the accel term's exposure to the CAGR LEVEL. The lane then pays
                                 #   for compounding at a clean +0.15/%yr through `growth_trend_weight`, while `accel` ranks the
                                 #   residual. Lowering β does not fix a slope, it RE-INJECTS the level into the accel term and
                                 #   double-counts it (at β=0 the total derivative is +0.65) — buying a level the trend term already
                                 #   prices and discarding the residual. rho falling +0.24 -> +0.17/+0.18 at 20y is that signature.
                                 #   MEASURED: the ladder. INFERRED: the d(1Y)/d(CAGR) ≈ 1 mechanism, offered because it predicts the
                                 #   sign and the shape — this run did not measure that slope.
                                 #
                                 #   THE TIE-BACK. At β = 0 the accel leg is `clamp(1Y, 0, cap)` — plain 1Y momentum, no baseline at
                                 #   all. (#47) arm A reached the same place by the other road (accel 0.65->0 WITH
                                 #   growth_mom121_weight 0->0.65) and landed on 20y rank-1 median +4.3 at h2h 57%. This ladder's β=0
                                 #   rung: +4.3 at 56%. Different pool and different date, so read it as corroboration and not as a
                                 #   controlled comparison — but two independent routes to one number say the accel leg's value is
                                 #   THE SUBTRACTION, not the 1Y term.
                                 #
                                 #   THE LADDER IS NOT MONOTONE IN β and must not be reported as a slope response: 20y lane edge runs
                                 #   +769.4 / +477.4 / +394.2 / +392.1 / +528.5 down the rungs, turning back UP at the bottom. The
                                 #   clamp is why, and IT BINDS AT BOTH ENDS — (#98) above names only the floor half. `growth_accel_cap`
                                 #   is 50.0, so at high β the CAGR subtraction pins compounders ON THE 0 FLOOR, and as β -> 0 the leg
                                 #   becomes raw 1Y return and every name with 1Y > 50% SATURATES THE CEILING. The ends of this ladder
                                 #   are therefore not commensurable with its middle. Reading the β=0 end honestly would mean moving
                                 #   `growth_accel_cap` in the same breath — a two-knob run in which neither result is readable. NOT
                                 #   DONE, deliberately.
                                 #
                                 #   AND IT REFUSES THE PREMISE OF (#98)'s ORDERED SECOND SWEEP. That receipt prescribes re-gridding
                                 #   `growth_trend_weight` UNDERNEATH the winning β, reasoning that removing the counterweight is what
                                 #   stops the worst-window guard binding. Every run above prints the `growth_trend_weight CURVE`
                                 #   computed AT ITS OWN β, so that grid came back FREE — and it says the opposite. 20y lane edge by
                                 #   (β, trend weight):
                                 #       β      0.00    0.15    0.25    0.35    0.45    0.55    0.70    1.00      max
                                 #       1.0   551.4   769.4   780.6   830.5   876.8   875.8   843.1   797.1    876.8
                                 #       0.5   276.3   477.4   518.1   639.0   678.8   710.5   737.7   724.8    737.7
                                 #       0.3   350.1   394.2   467.7   591.2   577.3   671.8   700.2   710.9    710.9
                                 #       0.15  383.0   392.1   550.7   573.4   604.9   620.3   630.9   720.1    720.1
                                 #       0.0   356.9   528.5   554.3   554.5   580.4   609.8   633.9   742.3    742.3
                                 #   At 20y β=1.0 DOMINATES POINTWISE — every other β is worse at every trend rung. At 8y dominance is
                                 #   not pointwise, but the ceiling is still β=1.0 (97.5 at trend 0.70; next best 89.9). Lowering β
                                 #   does not free the trend weight to climb, it LOWERS THE CURVE the trend weight would climb.
                                 #   READ AS A SCREEN ONLY: no rank-1 and no h2h here, so it cannot settle the ship rule. It refuses
                                 #   (#98)'s PREMISE, not the trend knob — a real trend re-grid still needs its own graded runs.
                                 #
                                 #   WHAT THIS CLOSES. β was the last unswept factor of the score's CAGR slope, and it is at its
                                 #   optimum. The -0.35 arithmetic may not be cited as an unaddressed defect again without citing this
                                 #   ladder. NO KNOB WAS CHANGED: src/ and tests/fixture/ are byte-identical across this round.
  growth_accel_cap: 50.0         # SCORE: cap that 1Y-minus-CAGR acceleration term
  growth_smoothness_weight: 0.0  # (E) SCORE: reward per unit of trend_r2 (R² of the log-price trend fit, 0..1) — pays the
                                 #   straight-line compounder over the equal-CAGR rollercoaster. Reconstructed in backtest_quote,
                                 #   so validated END-TO-END (not backtest-blind): 2/5/10/20 sweep gave Δedge +5.4/+13.2/+10.3/-38.0
                                 #   same-batch, rho intact at +0.13 — clean hump, knee at 5. A 6M-momentum twin swept in the same
                                 #   run was null at every weight and was NOT wired. 0 = off.
  # (#62) ZEROED 2026-08-10, on the first measurement this term has ever had AT ZERO. Read the sweep
  #   above once more: it is 2/5/10/20. The knee at 5 is a local optimum AMONG NONZERO WEIGHTS — that
  #   sweep asked how heavy the term should be and never asked whether it should exist, and the answer
  #   to the second question is not contained in the answer to the first.
  #   THE ARM — `backtest {20,12,8} universe` on 4997 tickers, this knob the only thing changed, run on
  #   the post-(#61) binary so the on-sale split is not moving underneath it. Growth `windows scored`
  #   came back identical at 481/668/852 in all three pairs, so admission did not shift:
  #     horizon   lane edge          rank-1 mean|med          win%     h2h vs 11-20 book        null floor
  #     20y       +459.5 -> +418.1   +6.2|+6.4 -> +6.4|+6.4   87 ->90  16/21 -> 14/21 (76->67%)      93.3
  #     12y       +132.0 -> +125.5   +5.8|+7.2 -> +6.5|+7.8   74 ->76  17/30 -> 18/30 (57->60%)      15.4
  #      8y        +59.3 ->  +45.2   +7.2|+8.2 -> +7.4|+8.2   76 ->74  24/38 -> 22/38 (63->58%)      16.0
  #   Every lane-edge cost (-41.4 / -6.5 / -14.1) sits UNDER its own horizon's (#60) null floor — under
  #   what deleting an information-free CONSTANT costs at that horizon — so the edge column carries no
  #   information here and does not vote. What votes is the book, and the book gets BETTER: rank-1 mean
  #   rises at all three horizons, the median rises at 12y and holds at 20y and 8y, win% rises at two of
  #   three, and every h2h move is ±2 windows out of 21/30/38.
  #   THE ONE COLUMN THAT ARGUES THE OTHER WAY, recorded here because a receipt that logged only the
  #   favourable half would be worthless — the TOP-3 held book, which is also what ci.yml's collapse gate
  #   grades, moves DOWN at all three horizons while rank-1 moves up:
  #     horizon   top-3 excess mean|med          win%        worst
  #     20y       +6.5|+6.6 -> +6.4|+6.6         97% -> 97%  -0.5 -> -0.5
  #     12y       +7.3|+7.8 -> +7.0|+6.9         87% -> 87%  -7.8 -> -7.8
  #      8y       +8.1|+8.5 -> +7.9|+8.4         82% -> 82%  -9.2 -> -9.2
  #   -0.1 / -0.3 / -0.2 on the mean, inside the rule's own ~0.3 pts/yr tolerance, and the win rate and
  #   the worst drawdown are BYTE-IDENTICAL at all three — the book's risk shape did not move at all. The
  #   single excursion is the 12y median at -0.9. So the honest summary is not "strictly better": it is
  #   sharper at rank 1 and a hair flatter across the top 3, bought for a lane-edge cost that is under
  #   the noise floor. If this is ever reverted, that -0.9 is the number to revert on.
  #   THIS IS THE SECOND R² TERM TO LOSE HERE. The old R²-steadiness DAMP was deleted on the same kind of
  #   evidence — see sharpe_weight's block below, "disabling it had nearly DOUBLED wide 5y edge
  #   +50.5->+95.6" — and picks.rs (#1) recorded the mechanism: "R2 docks exactly the parabolic
  #   compounders this lane exists to surface". This knob is that damp's MIRROR IMAGE: same input, sign
  #   flipped, paying the straight line instead of docking the lumpy one. It loses for the same reason.
  #   A lane whose entire thesis is the parabolic compounder should not be pricing path shape in either
  #   direction, and it now prices it in neither.
  #   WHAT THIS OWES sharpe_weight, and do not skip this. sharpe_weight was cut 0.15 -> 0.05 on 2026-07-04
  #   EXPLICITLY BECAUSE this term had just shipped ("trend_r2 carries the same calm-compounder signal,
  #   so Sharpe's marginal value shrank"). That justification is now gone, and the 0.05/0/0.10-vs-0.15
  #   sweep that set the value was run WITH smoothness at 5.0 and has never been repeated without it.
  #   Re-sweeping sharpe_weight is the obvious next run. It is an ADDITION and so owes the addition ship
  #   rule (rank-1 must RISE, not merely hold), which is why it is NOT being done on this receipt.
  #   THE GOLDENS MOVED AND THE FIXTURE LOOKS WORSE — read this before reacting to that diff. The 12y
  #   fixture growth edge goes +66.7 -> -5.5 and its net turns "too churny to trade". That is NOT a second
  #   opinion contradicting the wide runs above; it is the SAME measurement the old golden already
  #   printed. Each new headline reproduces, BIT-IDENTICALLY, the row the previous golden carried for
  #   exactly this change:
  #     20y  old ablation `growth_smoothness_weight rho +0.25  edge -39.8`  ->  new headline rho +0.25  edge -39.8
  #     12y  old ablation `growth_smoothness_weight rho +0.13  edge  -5.5`  ->  new headline rho +0.13  edge  -5.5
  #      8y  old ablation `growth_smoothness_weight rho +0.15  edge -54.3`  ->  new headline rho +0.15  edge -54.3
  #   and the knob's own ablation row now reads Δ+0.00, as it must once the knob is already zero. The
  #   ON-SALE lane is byte-identical in all four full reports. One knob moved, nothing else did.
  #   WHY THE FIXTURE NUMBER IS NOT THE GRADE: (#59) proved that in THIS fixture `growth_smoothness_weight`
  #   was indistinguishable from a constant — its Δ tied the `null: base −4.3` row to the tenth of a point
  #   at 20y AND 12y (-261.8 / -72.2), because 5.0 × trend_r2 ≈ 4.33 and trend_r2 barely varies across 80
  #   hand-picked large caps. Removing a constant from `base` rescales everything `base` is multiplied by
  #   (trust × overext × proximity × value), which is why the headline moved. Across 4997 real tickers
  #   trend_r2 is NOT near-constant, the term is not a constant, and the wide table above is the grade.
  #   WHAT THE FIXTURE GAINED, which the same diff shows: its null floor COLLAPSED 72.2 -> 7.6 at 12y and
  #   261.8 -> 43.9 at 20y, so every remaining term is now measured against a 9.5x / 6.0x quieter ruler.
  #   `overext_brake` — (#60)'s sole surviving carrier of rho — goes from 1.4x its floor to 2.6x at 12y and
  #   2.1x to 5.8x at 20y. And `12 tune`, the only honestly out-of-sample instrument in the fixture (889
  #   train / 382 test cutoffs, 500 draws), IMPROVED: shipped-default TEST rho +0.26 -> +0.31 at an edge
  #   unchanged at +144.5, while the overfit search winner collapsed +115.3 -> -48.1, so "SHIP NOTHING"
  #   now wins by 192.6 points instead of 29.2.
  #   A LEAD THIS OPENED, not acted on: with the pseudo-constant gone, the `growth_trend_weight` curve
  #   stops being a monotone ramp and grows a knee — 0.35/0.45 +90.2, 0.55 +108.8, then DOWN to +67.9 at
  #   0.70 and +63.5 at 1.00, against +5.5 at the shipped 0.15. That is a weight_curve row on n=80 with a
  #   band straddling 0: it prints no rank-1 and no h2h, so it CANNOT settle a ship rule and is a lead for
  #   a wide run, not a licence to move the knob.
  #   THE FIELD STAYS WIRED, deliberately. config.rs already defaults it to 0.0, so this line was the only
  #   thing switching it on anywhere and one value is the entire revert. The sweep's hump is still true of
  #   nonzero weights: an operator who wants a calm-path tilt back should set 5, not 2 and not 20.
  #   THE CI COLLAPSE GATE STAYS GREEN with margin — checked against ci.yml's own three conditions on the
  #   live universe before shipping: GROWTH edge +418.1/+125.5/+45.2 (>0), top-3 excess +6.4/+7.0/+7.9
  #   (>0), and BOTH out-of-sample halves positive at every horizon (+0.12|+0.17, +0.05|+0.17, +0.13|+0.13).
  growth_underwater_weight: 0.3  # SCORE: additive penalty per year of the LONGEST below-prior-peak stretch
                                 #   (drawdown DURATION; the maxdd cap #26 only sees depth). MONTHLY-BLIND
                                 #   (#3k) 2026-07-29: underwater_yrs is /252-session-based so core.rs keeps
                                 #   it DAILY-ONLY — every monthly backtest (8y+, or `long`) leaves it None
                                 #   and the term contributes 0 to every name. Proven empirically: zeroing
                                 #   this weight reproduced the 20y-stress grid metric-for-metric (rho/edge/
                                 #   top-10/rank-1/h2h all identical to the printed digit). It CANNOT be
                                 #   validated at the 8/12/20y goal horizons; it still fires on the LIVE
                                 #   screen (daily closes). Kept at 0.3 on the original daily receipt below —
                                 #   a tie is not evidence against — but do not cite the monthly grids for it.
                                 #   Rebuilt in backtest_quote (daily cadence). Standalone probe (universe fund, 2026-07-19):
                                 #   n=8571 rho +0.26 edge +27.9 OOS +0.40|+0.14 both +. Candidate lane run at 0.3 same-batch:
                                 #   edge +25.5->+27.5, winsorized +15.7->+17.7, rho intact +0.12, OOS -0.03|+0.26 -> -0.02|+0.27,
                                 #   worst era -11.7->-4.8 (nothing degraded). 0 = off.
  growth_allow_negative_scores: false # (#86) UNMEASURED — an unfitted judgement value, and it ships OFF, so
                                 #   the two lines below keep behaving exactly as they have. What it would fix,
                                 #   stated first because it is the reason the two knobs under it are inert:
                                 #   `ranked` trims on `min_score.max(0.0)`, so EVERY negative floor is the
                                 #   same floor. Both values below are -100.0. Neither can fire. -100, -5 and
                                 #   0 are one setting, and the ~40-line ETF receipt on the second line —
                                 #   arguing 4.0 against 5.0, citing wrapper scores of 4.5-5.6 — is describing
                                 #   a knob that has not been able to reach those rows since the clamp went in.
                                 #   Read that receipt as ARGUMENT, never as a measurement of this config.
                                 #   AND IT CANNOT BE FIXED ALONE, which is why this is one knob and not two.
                                 #   The score is `base x damps + liq_bonus` and every damp lies in 0..1, so a
                                 #   NEGATIVE base is raised toward zero by them: base -2.0 x damp 0.22 = -0.44.
                                 #   Among the negatives the ranking is inverted — the most overextended, least
                                 #   trusted, longest-underwater name scores highest. `growth_underwater_weight`
                                 #   (0.3, three lines up) is the only term that can drive base below zero and
                                 #   it is unbounded below. Honour the floor without fixing that and the rows
                                 #   newly admitted arrive in the wrong order, which is worse than hiding them.
                                 #   ON = the floor is used as written AND a negative base takes no damp at all.
                                 #   Not `2 - damp`, not a division: the only claim that can be defended without
                                 #   a sweep is that a penalty must not become a reward. How much MORE to punish
                                 #   a stretched name already below zero is a magnitude nobody has measured.
                                 #   WHY NO NUMBER IS QUOTED HERE. No sweep can settle it: the backtest pool
                                 #   scores no negative-base rows (`underwater_yrs` is rebuilt in
                                 #   `backtest_quote`, but a name that survives the gates to be scored is at or
                                 #   near its own high by construction), and `min_score` is DISPLAY-only and
                                 #   never reached by `backtest`. So this is a live-tables change end to end,
                                 #   and no golden covers it — which is also why the default had to be `false`
                                 #   rather than the fix: off is byte-identical, on is unmeasured.
                                 #   REVERT RULE: it is already at the revert value. Before turning it on, run a
                                 #   live `screen` both ways and read the BOTTOM of the stock table, not the top
                                 #   — that is the only place the two settings differ. If a name that is deeply
                                 #   underwater and far above its trend appears ABOVE a name that is merely
                                 #   underwater, the inversion is still there and the change is not done.
  growth_min_score: -100.0       # hide growth rows scoring <= this; 0 = show all top_picks
                                 #   (#86) INERT AT ANY NEGATIVE VALUE while growth_allow_negative_scores is
                                 #   false — see the receipt above. -100.0 here means "no floor", the same as 0.
  growth_min_score_etf: -100.0   # ETF-only floor. ETF scores structurally cap ~5.6 (accel~0: a diversified basket doesn't accelerate; quality/liq/fund all 0) so the stock-calibrated 5.0 floor (fine at 26% of the ~19 stock ceiling) sits at ~89% of the ETF ceiling and shows only a sliver. 4.0 = surface the diversified broad-tech tier (Nasdaq-100 / World-IT / S&P-IT wrappers score 4.5-5.6; TER column lets you pick the cheapest wrapper) while still trimming the young single-sector padding (Europe-Financials 3.2, 6y). DISPLAY-only -> validated edge untouched. Does NOT surface broad S&P500 core (VUAA/SPYL young=no 5y leg, AUM5 ~8% CAGR=market-beta<growth_min_cagr) — history/thesis wall, not this floor
  # (funnel) THE ETF WALL IS `history`, NOT THIS FLOOR — the line above said it in words, now it is a
  #   number. Screen gate funnel, 2026-08-03, 4718 EU-buyable names: 1842 ETFs fail `history` (no 5y+ leg
  #   to rank) and that is the ONLY gate they fail, against 12 ETFs sole-blocked by `cagr` and 0 by `peg`.
  #   No ETF-side floor can reach those 1842 — they have nothing to score, so lowering this knob (or
  #   growth_max_peg_etf, or the range bars) cannot surface them.
  #   A KNOB CAN, AND IT WAS MEASURED AND REJECTED same day: `growth_min_leg_years` (above) adds a 2Y rung
  #   to `long_leg`'s ladder, which GIVES them something to score — 1185 of the 1922 ETF wall cleared at
  #   2.0, ETF table 5->16 rows. It loses every clause of the ship rule and gets worse the longer the
  #   horizon (20y-s rank-1 median -2.0, h2h 67->39%, lane edge -41%). Read that receipt before treating
  #   this wall as an unsolved problem: it is solvable and the solution is worse than the wall.
  #   THE BRIDGE DOES NOT SCALE EITHER, so do not re-propose auto-bridging on a name match: an offline
  #   name-match over the 3467 cached fund names (a CACHE SCAN, not a funnel number — treat as an
  #   estimate) found 124 young listings with an older same-index twin and 58 whose twin also shares its
  #   currency, i.e. splice-legal. All 58 are broad World/S&P500/EM wrappers, which then meet
  #   growth_min_cagr 19.0 and die there anyway. ~58 candidates, ~0 new rows, and a wrong twin silently
  #   corrupts CAGR for good — `history_proxy` stays hand-curated.
  # (#41) REDUNDANCY skip — ARMED 2026-07-28, receipt written here for the first time (the knob shipped
  #   built, probe-shared and measured, then sat at the code default 0 = off with nothing recording why).
  #   WHAT IT IS: walk the ranked STOCK rows and drop one whose 36-month trailing-return correlation with
  #   an already-kept row reaches the cap, refilling from below so the table stays full length. Stocks
  #   only — the ETF lane is diversified by construction and crypto is one bet already. Not a gate and not
  #   a score term: every row it removes passed everything, which is why it runs AFTER the score/sector
  #   trim rather than inside score_parts. It removes the SECOND COPY of a bet. Pinned tickers bypass it,
  #   exactly as they bypass the score trim.
  #   WHY THIS IS TRUSTWORTHY WHERE CROSS-RUN NUMBERS ARE NOT: `core::decorrelate_keep` is called by BOTH
  #   the live skip (picks.rs) and the CORR-CAP probe (backtest.rs), so probe and served rule cannot
  #   drift; and `report_corr_cap` runs on EVERY backtest, making this a WITHIN-run sweep — immune to the
  #   (#40) live-universe noise that invalidates comparing two runs.
  #   THE SWEEP, 2026-07-28, top-10 held 12y:
  #     cap off  book +14.2%/yr  excess +6.1  worst -7.8  OOS +5.6/+6.7   [= plain top-10]
  #     cap 0.8  book +14.2      excess +6.1  worst -7.8  OOS +5.6/+6.7   [indistinguishable from off]
  #     cap 0.6  book +14.3      excess +6.2  worst -7.8  OOS +5.8/+6.6   [late OOS half DIPS below off]
  #     cap 0.4  book +14.4      excess +6.3  worst -7.8  OOS +5.9/+6.8   [both halves up, worst flat]
  #   The probe's own ship rule is "a cap lifting book or worst with OOS both + -> ship". Only 0.4 clears
  #   it on this run. An EARLIER run put the knee at 0.6 (+13.9->+14.3, win 86->88%, OOS +4.9/+6.8 ->
  #   +5.1/+7.2) and called 0.4 "the same plateau". Needs >=12 overlapping months to judge a pair; an
  #   unjudgeable pair never blocks.
  #   ...AND IT STAYS OFF ANYWAY. 0.4 was armed on that sweep and REVERTED the same day on live evidence:
  #   the probe's number does not transfer to the live table, because the two run on differently-shaped
  #   populations. The probe fills a top-10 book by walking a DEEP ranked list, so dropping a correlated
  #   name pulls the next one up. The live stock table after the score floor and the sector caps is ~38
  #   rows of heavily intra-correlated US large-cap growth, so `decorrelate_keep` exhausts its candidates
  #   and returns FEWER than n. Measured live, same binary, same day, rows kept out of 38:
  #     cap off 38   cap 0.8 -> 37 (ORLY)   cap 0.6 -> 35 (+GOOG MSFT)
  #     cap 0.4 -> 29 (+AMZN CARR EBAY HCA META NXPI PGR)
  #   So config.rs's "refilling from below" is true of the probe and FALSE of the live table. Arming 0.4
  #   would hide MSFT/AMZN/META — and PGR/HCA, the two names the roic `neutral` fix in this same round
  #   just repaired — to buy +0.2%/yr measured on a book that is not this table. 0.8 costs a row and the
  #   probe calls it indistinguishable from off, so it buys nothing either.
  #   AND THE JOB IS ALREADY DONE, NON-DESTRUCTIVELY: the "Holdings overlap" footer already tells you
  #   which picks are effectively one concentrated bet, without deleting rows from a ranking whose whole
  #   purpose is to show you the candidates. Re-open this only with a refill that walks past n.
  growth_corr_cap: 0.0
  # (#140) 2026-08-25 — GRADED ON THE BOOK FOR THE FIRST TIME, AND REFUSED THREE WAYS. (#41) above is
  #   a LIVE-TABLE measurement; the knob had never been graded at all, because nothing read it in the
  #   graded path. That gap is closed in this commit and the value below is unchanged.
  #
  #   THE WIRING GAP, and why no earlier round could have run this arm. `growth_corr_cap` had exactly
  #   one non-test call site — `picks::lane_split`, the SERVED table. `report_vs_benchmark` never read
  #   it. The CORR-CAP probe swept the axis on its own ladder and never read this field either. So
  #   arming the knob in this file moved the screen and left every verdict number byte-identical: a
  #   vacuous discharge, not a verdict. `report_vs_benchmark` now applies the same skip through
  #   `corr_cap_rung` -> `core::decorrelate_keep`, the two-site shape (#75) uses, so served rule, probe
  #   and verdict cannot drift ((#3j)). PER LADDER RUNG (n = the rung being built), not on the cohort:
  #   a correlation cap's answer depends on how many names you are buying. At 0.0 it is the identity
  #   and the seven tests/fixture/*.golden did not move — that is the proof, and it is why this commit
  #   ships the wiring with the knob still off.
  #
  #   THE LEAD. The CORR-CAP probe on the post-(#138) config met its own printed bar ("a cap lifting
  #   book or worst with OOS both + -> ship as a book-construction rule") at both graded horizons:
  #   12y book +9.7->+9.9, excess +3.3->+3.6, win 74->83%, worst -6.4->-5.6; 8y worst -5.5->-3.8.
  #   Converted per the standing rule at the head of this file — a flag is a LEAD, never a verdict.
  #
  #   THE ARM. `backtest {20,12,8} universe fund pit`, one arm, overlay inlined here per (#131):
  #       buy_heuristic:
  #         print_h2h_mid_ladder: true
  #         growth_corr_cap: 0.4
  #   0.4 is the only rung the probe prices as moving (0.8 and 0.6 are inert). Pre-registered as a
  #   KNOWN WEAKNESS that it is the GRID EDGE — nothing is measured beyond it — the opposite of the
  #   interior-plateau shape (#126) demanded of itself. Verification passed before any verdict was
  #   read: drift named exactly `print_h2h_mid_ladder false->true, growth_corr_cap 0.0->0.4`;
  #   windows scored 1123/2731/5941 and 5766 tickers, matching control to the unit.
  #
  #     horizon  top-10 exc|med           worst            h2h vs 6-10   vs 11-20   holds
  #       20y    +0.6|+0.7 -> +0.8|+1.4   -10.0 -> -10.6    50% -> 50%   50% -> 50%   110 -> 102
  #       12y    +3.7|+4.0 -> +3.9|+3.0    -6.4 ->  -5.6    63% -> 63%   50% -> 50%   209 -> 188
  #        8y    +5.3|+5.6 -> +5.2|+4.6    -5.5 ->  -3.8    70% -> 70%   62% -> 62%   284 -> 259
  #
  #   REFUSED, and any ONE of these is sufficient:
  #     1. GUARD BROKEN at 20y — top-10 worst window -10.0 -> -10.6. Precedence is GUARD > PRIMARY,
  #        and a broken guard refuses outright; it is never softened into unresolved.
  #     2. PRIMARY fails at 12y — the MEDIAN falls +4.0 -> +3.0. Mean-and-median is the bar.
  #     3. PRIMARY fails at 8y — BOTH moments fall, +5.3|+5.6 -> +5.2|+4.6.
  #   Not a TIE: PRIMARY fails at both adjudicating horizons rather than disagreeing across them.
  #
  #   WHAT IT ACTUALLY DOES: trades RETURN for RISK. Worst window improves at 12y and 8y (-6.4->-5.6,
  #   -5.5->-3.8) and win-rate rises 74->83% at 12y, while the excess median falls at both. Ship Rule
  #   v2's PRIMARY is a return bar, so a risk-only gain cannot carry it. Anyone re-opening this should
  #   argue for changing the RULE, not for re-fitting the cap.
  #
  #   THE MECHANISM, and it confirms (#41) rather than contradicting it. Holds fall at every horizon
  #   (284->259 at 8y). `decorrelate_keep` DOES walk the whole ranked list, so this is not a missing
  #   refill — the per-bucket candidate list is exhausted first. The PIT book never fills the basket:
  #   12y control holds 209 over 35 windows = 6.0 names against a basket of 10 (the round-12 finding).
  #   YOU CANNOT DEDUPE A BOOK THAT IS ALREADY SHORT — removing a duplicate does not swap in a
  #   replacement, it just makes the book emptier. (#41) measured this live (38 rows -> 29) and
  #   predicted the deep backtest pool would behave differently. It does not. That is the transferable
  #   result of this round and it outlives the 0.4 arm.
  #
  #   THREE INSTRUMENT LESSONS, all worth more than the arm:
  #   a) THE PROBE'S BAR HAS NO MEDIAN. It reported 12y as a clean win; the verdict's median is what
  #      refused it. (#75) already said that bar is "worth a grid, never ship this" — this is the
  #      demonstration, not the quote. The probe also grades a HELD book while the verdict rebalances.
  #      Cross-check that the wiring is honest: the graded top-10 reproduces the probe's 0.4 row
  #      EXACTLY at 20y (+7.3 book, +0.8 excess, -10.6 worst), which is what it should do at n=10.
  #   b) THE h2h GUARD IS VACUOUS FOR THIS KNOB. The skip is applied per rung inside the top-N ladder
  #      and deliberately does NOT reach `rank_slice_stats`, so h2h is measured on the same cohort
  #      armed or off and came back byte-identical at all three horizons. It PASSED and that pass is
  #      worth nothing. Read it as vacuous, never as evidence — the (#126) precedent for a horizon
  #      discharged vacuously, applied to a guard.
  #   c) AN `if knob > 0.0` ALLOCATION GUARD AROUND INERT CODE IS SIX UNKILLABLE MUTANTS. The first
  #      cut of this wiring guarded both the `trail_of` build and the per-rung `trails` build so the
  #      shipped 0.0 lane would allocate nothing. The gate graded 14 mutants and SEVEN survived: at
  #      `cap: 0.0` the guarded bodies are never read, so `>` `==` `<` `>=` all print byte-identical
  #      output, and `report_vs_benchmark` returns `()` and only prints, so no unit test can reach the
  #      branch either ((#75)). Six of the seven were that one micro-optimisation. Deleting both
  #      guards took the diff to 8 mutants with the goldens killing the rest. THE RULE: a branch whose
  #      only effect at the default is to skip work nothing reads is not an optimisation — it is dead
  #      weight the suite is then required to pretend it covers. Write the unguarded version. Applies
  #      to every future knob wired into `report_vs_benchmark`, which is why it sits here and not in a
  #      commit message. (Grade it the way CI does — `-- --lib --test backtest_fixture`. A local run
  #      with `--lib` alone hides every mutant the goldens are the only witness for, and this round's
  #      first pass reported an eighth survivor for exactly that reason.)
  #
  #   REVERT RULE: none needed — the value never moved. The src wiring STAYS either way: it is inert
  #   at 0.0, proven by the untouched goldens, and it closes a knob that was ungradeable by
  #   construction. Do NOT ladder 0.3/0.2 to rescue this: that is fitting a rung to a failed arm, and
  #   the 20y guard breaks in the direction a tighter cap makes worse. (#41)'s standing reopen — "only
  #   with a refill that walks past n" — is NOT met by this round and stays open; but note it would not
  #   help, because the candidates are exhausted, not skipped.
  #
  #   PROBE SURFACE SWEPT THE SAME DAY, so round 15 does not re-derive it. All ten printed decision
  #   bars in `backtest` were read against banked post-(#138) output. Every one is closed:
  #     VALUE / EV-VALUE / PEG-VALUE gates  the lift IS `growth_value_floor_pct`, already shipping at
  #       40.0 since (#126). The probes run on RAW samples (brake-off baseline) while `value_floor_trim`
  #       is applied inside `report_vs_benchmark`, so 0%->40% re-measures the shipped knob. earnings_yield
  #       40% returns the IDENTICAL book to peg_yield 40% (+10.1/+3.7) = the same names; ebitda_yield is
  #       strictly weaker than what ships.
  #     SURVIVAL-GATE                       every factor flat or negative. net_cash_rev 25% is +0.5 at
  #       8y and FLAT at 12y; PRIMARY needs both.
  #     held-book by FACTOR                 (#136)'s coverage mirage, now proven mechanically: NINE
  #       factors print byte-identical books (12y +18.0/+6.4, n=65) because "ey rows/window mean 1.9" —
  #       the "top-10" holds ~2 names. Not a ranking result at all.
  #     growth_fund_weight knee             flat 0.1->2.0 at both horizons; shipped 0.07 sits on it.
  #     TWO-STYLE combo                     its own bar says "Never a score/gate/knob change".
  #     CORR-CAP                            this receipt.
  #
  #   (#134) RE-TESTED, NOT ASSUMED. Its one named open thread — stooq.com — was re-run 2026-08-25 and
  #   still returns a JavaScript proof-of-work bot check on every symbol INCLUDING the live AAPL
  #   control. Per that receipt's own instruction the line stays "untested rather than refuted"; this
  #   is a second dated attempt and nothing more.
  #   AND A DEAD END, recorded so nobody walks it. The caveat block says closing PIT survivorship needs
  #   "delisting DATES plus a terminal return, not a price backfill", which reads as unblocked. It is
  #   not. `backtest.rs` skips a cutoff whose forward window runs off the end of the series, so a
  #   mid-hold delisting contributes no loss — and the last close IS the terminal price, so salvaging
  #   it needs no new data. But (#121) measured "ZERO of 1206 members have a series ending more than a
  #   year before today": there are no truncated series to salvage. The 387 zero-bar names cannot be
  #   SCORED at all (no pre-cutoff history -> no score -> never picked), and the rest run to today as
  #   recycled tickers. A terminal-return stub is dead twice over.
  # (#75) VALUE BRAKE — the cross-sectional PEG floor: built, wired, graded and REFUSED 2026-08-12.
  #   Sibling of the (#41) skip above by construction (same site, same shape, same "not a gate, not a
  #   score term" standing) and now its sibling in outcome too: a cohort-aware trim that measures well
  #   on a probe's own book and does not survive contact with the graded one.
  #   THE LEAD. The PEG-VALUE-GATE probe prints its own ship bar — "a reject-% that lifts book with OOS
  #   both + and worst no deeper than off -> ship as a real growth gate" — and MET it: 12y book
  #   +13.9->+14.2%/yr, excess +6.1->+6.4, OOS late +7.0->+7.5, worst -7.8 unmoved; 8y +13.9->+14.1,
  #   +6.3->+6.5, late +6.4->+6.9, worst -9.2 unmoved. The code's own "MEASURED DEAD 2026-07-25 ... flat
  #   +6.8/+6.9/+6.9/+6.9" annotation was STALE — the (#74) perishability lesson running BACKWARDS. A
  #   flag banked as dead expires exactly like one banked as REAL, and nothing re-reads a death
  #   certificate. That annotation is now corrected in place, in both directions.
  #   WHY NO SWEEP EVER FOUND IT. `gate_sweep` loosens each gate one notch and ONLY loosens, so a gate
  #   that should be TIGHTER is structurally invisible to it. The probe's cohort has already passed
  #   `growth_max_peg: 1.6`, so "drop the cheapest 40% by peg_yield" IS the tightening direction — never
  #   measured under the current PEG definition, because every PEG ladder ever run went 1.6/2.0/2.5/off.
  #   That blind spot is general, outlives this knob, and is the most reusable thing here.
  #   THE GRID. 12 runs, `backtest {12,8} universe fund` x {0,25,40,55,70} plus a 20y {0,70} pair. One
  #   field moved per arm (self-verified by the off-validation warning); identical data conditions inside
  #   every horizon — 4999 tickers, cutoffs 4588/11885/20791, as-of coverage 4394/8456/10956.
  #   WIRING PROVEN BEFORE ANY GRADING, and this step is not optional for a knob with a probe twin: the
  #   graded top-10 line reproduces the probe's own row TO THE DECIMAL at every shared rung (12y 0%
  #   +13.9/+6.1/-7.8/+5.3,+7.0; 25% +14.0/+6.2/+5.3,+7.2; 40% +14.2/+6.4/+5.3,+7.5). Same cohort, same
  #   `< floor` boundary -> probe and knob are measuring one thing, so the disagreement below is real.
  #   THE VERDICT, top-3 under Ship Rule v2 with the ADDITION bar (off its default => must RISE):
  #     12y top-3 mean|med   off +6.8|+6.9   25 +6.8|+6.9   40 +6.9|+7.1   55 +6.8|+7.1   70 +6.6|+6.7
  #      8y top-3 mean|med   off +7.3|+7.8   25 +7.2|+7.8   40 +7.3|+7.8   55 +7.1|+7.8   70 +6.8|+7.5
  #   40 is the best arm at both horizons; it RISES at 12y and merely HOLDS at 8y (+7.3|+7.8 on both
  #   sides, to the decimal). ADDITION wants a rise at BOTH graded horizons -> NO SHIP. Every other arm
  #   is flat or worse. The guards passed and did NOT decide it: worst window identical at every arm and
  #   horizon (-7.8 / -9.2 / -0.5), rank-1 h2h >=50% everywhere (12y 65-67%, 8y 52-63%, 20y 74%).
  #   WHY THE PROBE AND THE GRADE DISAGREE, which is the finding. The probe grades a TOP-10 held-Ny
  #   NO-SELL book; Ship Rule v2 grades the REBALANCED TOP-3. The +0.3/+0.2 the probe reports is a
  #   top-10 effect that decays to +0.1 at 12y top-3 and to 0.0 at 8y top-3 — the brake works by
  #   improving the AVERAGE of a wide book, and there is nothing left of it by the time the book is
  #   three names. A probe's printed ship line is a reason to LOOK, never a grade. Six receipts already
  #   say that about lane metrics ((#63), (#69), (#70), (#3d), (#71), (#73)); this is the first time it
  #   has been measured about a probe that prints an explicit ship rule of its own.
  #   THE 70 RUNG WAS A DESIGNED SANITY CHECK, and it passed. Had the book kept climbing there this
  #   would be "buy the cheapest", not a brake. It does not: 70 is the WORST arm on top-3 at both
  #   horizons (+6.6|+6.7 and +6.8|+7.5) while lifting 12y top-1 to +7.0, i.e. noise at the argmax.
  #   20y IS PROVEN INERT, not assumed. Control and reject-70 are IDENTICAL on every book and every h2h
  #   line (top-1 +6.4|+6.2, top-3 +6.3|+5.7, top-10 +5.9|+6.1, h2h 14/19 74%): no 20y cutoff carries a
  #   peg_yield (SEC XBRL starts 2007, 20y cutoffs end ~2006) and the PEG probe prints nothing there. A
  #   70% cut that removes ZERO names is the strongest available statement of that blackout.
  #   COST OF GRADING ANY COHORT TRIM, general and worth knowing before the next one: the h2h
  #   DENOMINATOR SHRINKS as the brake tightens (12y 27->23 windows, 8y 35->29), because that test needs
  #   >10 names in a window and trimming pushes windows under the bar. A tighter arm is graded on fewer
  #   windows than the control, so its h2h is not quite the same measurement. Never read a trim's h2h
  #   without reading its denominator.
  #   THREE LEADS DIED AT ZERO RUN COST in the same round, recorded so they are not re-proposed:
  #   (a) re-laddering `growth_fund_weight` on a "strengthened" peg_yield — (#71)'s own control run and
  #       today's are BYTE-IDENTICAL (n=1424 rho +0.23 edge +399.8 OOS +0.27|+0.18), so (#71) graded
  #       with exactly this evidence and held 0.07; (b) `op_margin` as a second negatively-tilted axis —
  #       its rho near-mirrors peg_yield's at both horizons (+0.23/-0.24 at 12y, +0.19/-0.17 at 8y),
  #       the signature of an inverse-valuation proxy the shipped tilt already carries; (c) a
  #       `quality_weight` sign audit — (#72) already proved every arm 0..0.30 ties on the book.
  #   KEPT OFF, NOT DELETED — the (#47) precedent. The knob is what stops this loop repeating: without
  #   it the next session re-reads the probe's "ship as a real growth gate" line and re-proposes the
  #   whole round, which is precisely what a stale annotation caused this time. Re-open only on a run
  #   where top-3 RISES at BOTH 12y and 8y.
  #   MUTATION AUDIT — the round's first gate came back RED, 7 missed of 86, and all 7 were this change:
  #   six operators on the brake's own two comparisons plus a whole-function stub of the neighbouring
  #   `report_book_by_factor`, which a COMMENT correction inside its body was enough to drag into scope.
  #   Every one was unkillable rather than unkilled — the knob ships at 0.0, the brake needs `fund` to
  #   see a single peg_yield, and `fund` cannot run offline, so no test could reach the comparisons
  #   where they stood. Closed by splitting the trim into `value_floor_trim` (pure, argument-taking,
  #   value-returning, tested on two non-overlapping bucket cohorts) and dropping the redundant
  #   `pct > 0.0` guard, whose three survivors existed only because `pct_floor` already decides that.
  #   The printer is `#[mutants::skip]` with the reason at its head. GENERAL LESSON, third recording:
  #   a knob defaulted OFF is a knob whose live path no offline test executes — plan the pure split
  #   with the feature, not after the gate reds.
  # (#120) 2026-08-22 — THIS REFUSAL IS NOW HALF-VACATED, AND THE HALF THAT DIED IS NAMED HERE SO
  #   NOBODY CITES IT AGAIN. The grid above refused 40 because its lift "is a TOP-10 held-book effect"
  #   that "decays to +0.1 at 12y top-3 and to 0.0 at 8y top-3" — i.e. it pays on the wide basket and
  #   vanishes on the basket the verdict graded. THE VERDICT NOW GRADES TOP-10. That specific argument
  #   is therefore measured against a basket this repo no longer ships and CANNOT be re-cited.
  #   WHAT SURVIVES: the probe's bar grades a HELD-N-YEARS NO-SELL book while the verdict grades a
  #   REBALANCED one, so the probe's ship line is still not the rule; and the 20y blackout is still
  #   proven inert. WHAT DOES NOT: the decay argument, and with it the refusal's primary reason.
  #   STATUS: RE-GRADEABLE, NOT RE-GRADED. Nobody has run 0/25/40/55/70 against a top-10 verdict, and
  #   (#120) deliberately did not re-open it — a basket change is not a licence to re-litigate every
  #   refusal it touched. It SHIPS 0.0 (unchanged) until that grid is run under Ship Rule v2.
  #   The (#71) receipt is the contrast worth noting: it recorded its arm ordering "at every basket
  #   size", so it survives the move untouched. Receipts that name the basket age better.
  # (#126) RE-GRADED UNDER SHIP RULE v2 ON THE PIT POOL — 2026-08-23 — AND IT SHIPS AT 40.0.
  #   (#75) refused this knob 2026-08-12 on a top-3 basket. Half that refusal was that the lift lived
  #   at top-10 while the verdict graded top-3; (#120) moved the graded basket to top-10 on 2026-08-22
  #   and backtest.rs marked the knob RE-GRADEABLE in those words. This is that re-grade. It is ALSO a
  #   fresh grid, not a comparison against (#75)'s numbers: that round was PRE-PIT, this one is on the
  #   point-in-time pool, so the absolute levels are lower throughout and only the DELTAS transfer.
  #   METHOD. 12 runs, `backtest {12,8} universe fund pit` x {0,25,40,55,70} plus a 20y {0,70} pair.
  #   One field moved per arm, self-verified by the off-validation warning naming exactly five knobs
  #   (this one + the four display-only diagnostics). Data conditions identical inside every horizon:
  #   cutoffs with a forward window 8350/16804/2296, growth-scored stock 228/318/117. The TICKER count
  #   drifted 5745->5749 across runs as the ETF fetch picked up a few ISINs; the growth-scored counts
  #   are identical, so the graded book never saw it. Recorded rather than smoothed over.
  #   WIRING PROVEN BEFORE GRADING, as (#75) requires of any knob with a probe twin: the graded top-10
  #   row reproduces the PEG-VALUE-GATE probe's own row TO THE DECIMAL at every shared rung, at BOTH
  #   horizons (12y 40% -> +2.8, worst -9.5, OOS +2.1/+3.4 on both; 8y 40% -> +4.5, worst -6.2, OOS
  #   +3.5/+5.4 on both). Probe and knob are measuring one thing, so the grid below is about the knob.
  #   THE GRID, top-10 excess mean|med (the basket Ship Rule v2 now grades):
  #     horizon   off        25         40         55         70        worst (all arms)
  #      12y     +2.4|+2.5  +2.6|+2.7  +2.8|+2.8  +2.6|+2.9  +2.5|+2.7   -9.5 identical
  #       8y     +3.7|+3.4  +4.0|+4.0  +4.5|+4.5  +4.2|+4.7  +4.2|+4.8   -6.2 identical
  #      20y     +0.8|+0.9      -          -          -      +0.8|+0.9  -10.0 identical
  #   VERDICT. 40 RISES on BOTH moments at BOTH graded horizons (12y +0.4|+0.3, 8y +0.8|+1.1), which is
  #   what the ADDITION bar wants off a 0.0 default. Guards passed and did NOT decide it: the worst
  #   window is IDENTICAL at every arm and horizon, and rank-1 h2h is >=50% everywhere. 40 is the
  #   mean-argmax at both horizons AND interior to the ladder, with decay on both sides — a plateau
  #   peak, not a grid-edge artifact. Every rung 25..70 also clears the primary; 40 is the best of them
  #   on the mean at both horizons, which is why it and not a wider cut is the shipped value.
  #   THE 20y DEVIATION, STATED PLAINLY BECAUSE IT IS A DEVIATION. PRIMARY as written reads "20y AND
  #   8y". This knob CANNOT be judged at 20y: control and reject-70 came back byte-identical on every
  #   field (+0.8|+0.9, win 55% of 20, worst -10.0, OOS -1.6/+3.2, h2h 1/2) because no 20y cutoff
  #   carries a peg_yield — SEC XBRL starts 2007, 20y cutoffs end ~2006. A 70% cut that removes ZERO
  #   names is the strongest available statement of that blackout, and it reproduces (#75)'s finding on
  #   a different pool. (#71) warns in writing that such an arm reads "held at 20y" for free and that
  #   this must not be mistaken for evidence. SO THIS ROUND SUBSTITUTED 12y FOR THE BLIND HORIZON and
  #   required the rise at 8y AND 12y — STRICTER than the letter, not looser. NOTE THE LITERAL RULE
  #   REACHES THE SAME VERDICT (20y vacuously held, 8y rises), so the deviation did not buy the ship.
  #   WHAT WOULD MAKE THIS WRONG, recorded because the evidence is THIN and saying so is the receipt:
  #   (a) n_eff is 1.4 at 12y (34 windows) and 2.6 at 8y (42) — one to three independent trials, not
  #   thirty-four. (b) The rank-1 h2h DENOMINATOR is tiny and SHRINKS as the brake tightens (8y 9->8->
  #   6->4->4 windows, 12y 4 throughout), exactly the cost (#75) flagged; that guard passed on almost
  #   nothing and should not be cited as support. (c) The MEDIAN at 8y keeps climbing past 40 to +4.8
  #   at the 70 rung while the mean turns over at 40 — that is the "buy the cheapest, not a brake" tell
  #   the 70 rung exists to catch, and it half-fired. It does NOT fire at 12y, where the median peaks
  #   at 55 and falls at 70, which is why 40 ships and 70 does not. Re-read this before widening the
  #   cut. (d) Eight arm-horizon cells from ONE grid, no multiple-testing haircut applied.
  #   REVERT RULE: set back to 0.0 on any future `universe fund pit` run where top-10 excess at 40 fails
  #   to beat off on BOTH moments at 8y AND 12y, or where the worst window at 40 comes in deeper than
  #   off at either. A single horizon regressing is a re-open, not a revert.
  growth_value_floor_pct: 40.0
  # (#190) 2026-08-31 — SECTOR-RELATIVE VALUE BRAKE: NOT GRADEABLE. STOP RULE FIRED, NOTHING BUILT.
  #   THE PROPOSAL. This brake ranks each cutoff bucket by `peg_yield` and drops the cheapest-ranked
  #   40% — RAW, across all sectors at once. A peg level is a SECTOR property before it is a stock
  #   property (banks/energy/utilities structurally cheap; software/healthcare/consumer-discretionary
  #   growth structurally expensive), so a cross-sector bottom-40% cut does not remove the expensive
  #   NAMES, it removes the expensive SECTORS — in a GROWTH heuristic, whose target population is
  #   those sectors. The fix would key the floor on (bucket, sector) instead of bucket.
  #   IT CLEARED (#169)'s BAR ON PAPER, which is why it was worked up rather than waved off: it adds
  #   NO factor and re-aims the one family measured to separate (peg_yield AUC 0.91 at 12y / 0.70 at
  #   8y, (#165)). (#93) tried LISTING MARKET as the peer-split axis; SECTOR had never been tried.
  #
  #   PRE-REGISTERED STOP RULE, written before the census: if the median (bucket, sector) cell holds
  #   FEWER THAN 5 ROWS, a 40% within-cell cut is trimming 1-2 names off a 3-name cell and the arm is
  #   NOT GRADEABLE. It fired on all three counts below, and no backtest run was needed to see it.
  #
  #   1. SECTOR COVERAGE CEILING ON THE PIT POOL IS 41.7%, AND THE MISSING 58% IS NOT RANDOM.
  #      `pit_pool` (backtest.rs:719) keeps a ticker only if it is ABSENT from `sector_of`, then adds
  #      `spans.keys()` — so every pit stock comes from the membership map, while `sector_of` carries
  #      only names in a CURRENT constituent CSV. `.sp500_history.json` holds 1206 spans of which 503
  #      still have an OPEN span; (#129) independently measured the `sp500_csv` pond at exactly 503.
  #      The two numbers agreeing is the proof, not a coincidence. So 703 of 1206 (58.3%) carry NO
  #      sector, and they are precisely the DELISTED/REMOVED names — the survivorship-relevant half.
  #      Missing data passes (non-negotiable #5), so those rows would be exempt from the brake and the
  #      `on` arm would confound "sector-relative" with "trims 58% fewer names". A CONFOUNDED ARM, not
  #      merely a thin one: it would have produced a number, and the number would have meant nothing.
  #
  #   2. THE CELLS ARE ~1 NAME. (#165)'s census: 12y 35 buckets / median gated pool 6; 8y 43 buckets /
  #      median 7. Those are POST-trim (`report_vs_benchmark` trims at :2505 before reading pools at
  #      :2510), so pre-trim is ~10. Ten names across 11 GICS sectors is ~1 per cell against a floor
  #      of 5 — the rule fires by an order of magnitude, not marginally.
  #
  #   3. LIVE IS NO BETTER, so this is not a backtest-only limit. `.screen_web.json` 2026-08-31: the
  #      stock lane prints FIVE rows. A five-row book cannot be partitioned by sector either.
  #
  #   AND THE KEY WOULD BE LOOK-AHEAD ANYWAY — recorded so a future round does not re-derive it.
  #   (#95): the backtest stamps TODAY's GICS label at every cutoff, there is no as-of source in any
  #   feed this tool reads, and `backtest_drop_lookahead_sector` ships false. The LIVE path is clean
  #   ("today's sector IS the point-in-time sector"), but the GRADING key is contaminated by
  #   construction. Even a passing arm could not have been read as a clean result.
  #
  #   THE TRANSFERABLE RESULT, and it outlives this arm. (#41)'s finding at the `growth_corr_cap`
  #   block — "YOU CANNOT DEDUPE A BOOK THAT IS ALREADY SHORT: removing a duplicate does not swap in
  #   a replacement, it just makes the book emptier" — is the same arithmetic one step earlier. YOU
  #   CANNOT PARTITION A BOOK THAT IS ALREADY SHORT. Any proposal that SUBDIVIDES the per-bucket pool
  #   — by sector, market, size, style or anything else — meets this wall, and the wall is POOL SIZE,
  #   never the axis chosen. Cite this before working up another partitioning idea: the axis is not
  #   the thing to argue about. Pool size is, and (#134) records that the pond is blocked on a price
  #   source for names that stopped trading, not on membership data or on effort.
  #
  #   REVERT RULE: none — nothing shipped, no knob was added and no source line changed; this block is
  #   the whole artifact. Re-opening requires a per-bucket pool that can CARRY a partition, which
  #   means the pond, not another axis and not another re-reading of (#159)/(#165).
  growth_value_weight: 0.0     # (#1) authority of the P/E multiplier in the growth lane: 1.0 = full x0.5..1.5 swing (default), 0.0 = OFF. Set 0 because the walk-forward never saw pe_ratio. (#147) FILLED IT AND MEASURED — 0 STANDS, see the block below
  # (#147) THE BACKTEST'S P/E IS NO LONGER BLIND, AND THIS KNOB IS STILL 0 — MEASURED 2026-08-28.
  #   SUPERSEDES (#1)'s reason. (#1) zeroed this knob SOLELY because "the walk-forward never saw
  #   pe_ratio (None in backtest), so this +-50% reorder was pure guess" — an unmeasurABLE, not a
  #   refusal. (#145) then found the same blindness had hollowed `growth_er_weight`'s composite and
  #   pre-registered this round to fix it. The fix was a FILL, not a formula.
  #
  #   THE FILL. `core::pe_from_earnings_yield(ey) = 100/ey for ey > 0, else None`, called at
  #   backtest.rs's cutoff right after `f.earnings_yield` is set, writing `quote.pe_ratio`. The loop
  #   ALREADY held the number: earnings_yield = eps_ttm / as-of close, in the FILER's currency under
  #   the existing no-look-ahead FX discipline. A P/E is that inverted, and a ratio carries no
  #   currency, so no rate is applied and none is needed. Non-positive yields answer None rather than
  #   a negative P/E — a loss-maker has no meaningful multiple, and inventing one would put a
  #   "cheap" name at the top of the lane this feeds. `fund` LANE ONLY: an as-of P/E needs as-of EPS.
  #
  #   PROOF IT IS LIVE, because "no effect" and "dead code" print the same bytes and this is the
  #   trap the whole round could have fallen into. The ON-SALE `buy_score` foil reads `value_factor`
  #   at FULL authority, unmediated by this knob. Same lane, same config, same shipped values, zero
  #   drift warnings on either run — only the binary differs:
  #     12y ON-SALE      pre-fill ((#145)'s control)   post-fill
  #       edge             +3.9 pts                     +12.8 pts
  #       top/bottom half  -8.1 / -12.0                 -3.6 / -16.3
  #       winsorized edge  -44.5                        -38.2
  #       tuning adds      +76.2                        +61.9
  #     The fill triples the foil's raw edge. READ THE WINSORIZED ROW BEFORE QUOTING THAT: it is
  #     -38.2, still deeply negative, so the on-sale edge leans on extreme rows either way and this
  #     is evidence the P/E REACHES the score, NOT evidence it is worth shipping. Non-negotiable #6
  #     forbids tuning this lane; observing it is what proved the fill.
  #
  #   PROOF IT MOVES NOTHING THAT SHIPS. The growth lane is BYTE-IDENTICAL pre- and post-fill
  #   (12y top-10 +3.7|+4.0, win 74%, worst -6.4, 9 zeros/209 holds on both), because this weight
  #   ships at 0 so the P/E cannot reach `growth_score` at all. Every golden stayed green WITHOUT
  #   FOLIOMAN_BLESS — the goldens are price-only, where `pe_ratio` stays None. (#145)'s claim that
  #   this round "changes the shipped lane, so it needs its own receipt and re-blessed goldens" was
  #   HALF WRONG and is corrected there: the receipt yes, the re-blessing no.
  #
  #   THE ARM. Body, for re-creation as `tests/pe-ci-settings.yaml` (per (#131) — an arm overlay,
  #   not a tracked lane; it MUST sit at tests/<name>-ci-settings.yaml so `config::data_path`
  #   resolves the same caches, and only a file literally named ci-settings.yaml takes an empty
  #   base — (#125)):
  #     buy_heuristic:
  #       growth_value_weight: 0.25   # the dose; 0.0 for the control
  #   `heuristic_drift()` named exactly ONE knob on every arm. The control ran THROUGH THE SAME
  #   OVERLAY at 0.0. Lane `universe fund pit`, shipped gates, 12y and 8y per (#139); never with
  #   FOLIOMAN_OFFLINE=1; windows scored 2737 pool / 249 growth-lane, `stock 249/5444` on every run.
  #
  #   THE NOISE FLOOR IS ZERO ON THE GRADED COLUMNS, measured rather than assumed, because the whole
  #   A-verdict is a claim about identical output. Two 0.0 controls run hours apart fetched DIFFERENT
  #   universes (6082 vs 6081 tickers, 233 vs 353 Yahoo fund-facts fallbacks, 129 vs 87 justETF
  #   answers) and still printed byte-identical rank slices, zero-names and h2h. The header's warning
  #   that live-fetch drift is the noise floor is about the SCREEN; on this lane's graded columns it
  #   is 0, so an identity here is a real identity.
  #
  #   RESULT — VACUOUS ON EVERY SHIP-RULE SURFACE, AT BOTH HORIZONS.
  #     12y: at w = 0.25, 0.5 AND 1.0 every printed column is BYTE-IDENTICAL to
  #          control — not just top-10 (set-invariant, so it reads "held" for free,
  #          (#126)/(#145)) but top-1, top-2, top-3, top-5, top-15..top-50, the
  #          `top-10 zero names` list, `rode 9 zeros/209 holds`, and the rank-1 h2h
  #          (2/4 = 50% vs the 11-20 book, 2/4 vs the 2-5 book).
  #      8y: the pool is bigger (355 growth-scored cutoffs vs 249, `stock 348/7649`),
  #          score gaps are tighter, and the ladder moves EXACTLY TWO CELLS in the
  #          whole grid: w=0.25 lifts the rank 2-5 MEAN +5.3 -> +5.4, and w=1.0 flips
  #          rank 6-10's mean -0.0 -> +0.0 (a sign-of-zero print, the smallest move
  #          representable). NON-MONOTONE — 0.5 sits back at control on both — which
  #          is the signature of one marginal pair swapping in and out, not of a term
  #          with authority. top-10 (+5.3|+5.6, win 86%, worst -5.5), the rank-1 h2h
  #          (5/8 and 6/8) and the `top-10 zero names` list are identical at every dose.
  #     Both GUARDs therefore HOLD at both horizons and PRIMARY reads "held" — and that
  #     is exactly the free pass (#145) warned about. Recorded VACUOUS, NEVER PASSED.
  #
  #   WHY — RETRACTED BY (#148) ON 2026-08-29. THE MECHANISM THIS RECEIPT GAVE WAS WRONG.
  #   What stood here explained the vacuous ladder as ARITHMETIC: that `value` reaches the score
  #   through `combine_damps(&[trust, overext, proximity, value])`, `product().powf(1/n)` with n = 4,
  #   so the knob's real authority is the FOURTH ROOT of its advertised swing — 1.32x total spread at
  #   w = 1.0 rather than 3x — under a three-row table of folded ranges sized on that.
  #   THAT IS THE WRONG ARM OF `compose_score`. It has three, and the 4-slot `combine_damps` is the
  #   `growth_geomean_fold` arm, which (#8) reverted on 2026-07-02 and which ships FALSE (line 1172).
  #   The SHIPPED arm is the one line 754 has carried all along:
  #     score = base · proximity · value · geomean(trust, overext_brake) · ter · commodity · fx · acc
  #             + liq_bonus
  #   `proximity` and `value` multiply RAW there, at full advertised authority — at w = 1.0 `value`
  #   really does span 0.5..1.5, and (#1)'s "+-50% reorder" was the honest description of the knob.
  #   `damp` is a TWO-slot `combine_damps(&[trust, overext_damp])`, a square root over trust and
  #   overextension only. Lines 754, 1172, 4854 and 7472 all recorded this BEFORE (#147) contradicted
  #   them, so the receipt was wrong against its own register — which is why this retraction is loud
  #   and in place rather than a quiet rewrite. Anything downstream that sized a rung on the folded
  #   table is sized wrong by up to 3x; nothing shipped on it, because the verdict was 0.0 either way.
  #   The same retraction covers the claim that this fold is why (#48) found `growth_proximity_weight`
  #   argmax-blind: proximity multiplies raw too, so that receipt's blindness is ALSO unexplained, not
  #   explained by a shared mechanism. Two open questions now, not one solved one.
  #
  #   ALSO WITHDRAWN: "Coverage is NOT the cause — 240 of the 249 growth-scored cutoffs carry
  #   fundamentals (96%)". The number is right and the inference is wrong: that is `quote.fund`
  #   presence, and `value_raw` never reads `quote.fund`. It reads `quote.pe_ratio`, which (#147)
  #   fills ONLY from a POSITIVE `earnings_yield` (`core::pe_from_earnings_yield` filters y > 0);
  #   every other row falls back to `value_factor`'s `_ => 1.0` and is INERT at every weight. Coverage
  #   of that predicate on the growth lane was never measured. The nearest measured bound is pool-wide
  #   off this same run: 4789 of 8487 cutoffs carry as-of fundamentals, but only 1377 of those carry
  #   an `earnings_yield` at all (29%), and that count is SIGNED — so positive-only coverage is at
  #   most 29%, and a term inert on three rows in four is a live hypothesis for the vacuity the fourth
  #   root pretended to have settled.
  #
  #   SO THE MECHANISM IS OPEN. Two candidates, and they are separable: (a) COVERAGE, above — one
  #   instrumented run counting growth-lane rows with `value_raw != 1.0` decides it; (b) AUTHORITY vs
  #   GAP — even a raw 0.5..1.5 multiplier may be smaller than the score gaps between adjacent names
  #   in a pool this thin. (b) is now known to be untestable on this column: the graded top-10 basket
  #   holds ~93% of a pool that saturates near 6.4 names per bucket, so it could not have moved for
  #   ANY ranking knob at ANY authority (see (#149), which makes the ruler say so). MEASURE (a) FIRST.
  #
  #   THE DOSE THAT DOES MOVE IT, run as a DIAGNOSTIC and never a ship candidate: w = 10.0, ten times
  #   the top rung. Even there top-1, top-2, top-10, top-15..50, the zero-names list and the h2h stay
  #   byte-identical; only top-3 (+10.4 -> +10.5%/yr, excess +4.1 -> +4.2) and top-5 (+10.6 -> +10.7,
  #   median +4.5 -> +4.9) move at all. A knob that needs 10x its documented maximum to nudge two
  #   middle slices by 0.1pp has no business carrying valuation for this lane.
  #
  #   AND THE FILL EXPOSED A LATENT NaN — fixed in this same commit, in `picks::score_parts`.
  #   `value` had NO clamp while its structural twin `proximity` has carried `.max(0.0)` since (#48)
  #   for exactly this reason: a negative factor inside `combine_damps` is NaN under a fractional
  #   exponent, and NaN does not sink the name, it poisons the whole score silently. Any weight above
  #   2.0 drives a name at VALUE_TILT_MIN negative: 1 + 3x(0.5-1) = -0.5. This was UNREACHABLE while
  #   the backtest was blind (`value_raw` was 1.0 on every row, so every weight gave exactly 1.0) and
  #   the FILL is what made it reachable. Guarded at the boundary rather than in a comment, (#48)'s
  #   reasoning verbatim, and pinned by an assert in the `growth_value_weight` unit test. NO SHIPPED
  #   VALUE MOVES: the 0.0 lane and the whole 0.25..1.0 ladder keep `value` in [0.5, 1.5], and the
  #   post-clamp control reproduced the pre-clamp control byte-for-byte on the live lane.
  #
  #   VERDICT: 0.0 STANDS, now on EVIDENCE rather than on ignorance. Ship Rule v2 never got to
  #   adjudicate — every horizon is VACUOUS in (#126)'s sense, and a column that cannot move reads
  #   "held" for free. Recorded VACUOUS, never passed.
  #   REVERT: already at the reverted value. (#148) rewrote this rule too, because the old one asked
  #   for "a mechanism that gives this term authority the geomean does not quarter" — the geomean
  #   never quartered it. Re-open only after the coverage count in (a) above, and only on a lane
  #   (#149) does NOT tag VACUOUS. If coverage is the cause, the fix is upstream data, not a weight.
  #   Re-run: the arm body above at w in {0.25,0.5,1.0} plus a 0.0 control through the SAME overlay,
  #   `backtest {12,8} universe fund pit`, ~2.5 min per run.
  # (#50) WHAT THIS 0.0 COSTS DOWNSTREAM, recorded 2026-08-06 because no receipt said it: with this
  #   term off, `growth_max_peg` is the GROWTH LANE'S ONLY VALUATION CONTROL. Nothing else in
  #   `score_parts` reads a P/E. That is why the PEG rung has to be chosen on the raw P/E column and
  #   not on the PEG number alone — there is no soft brake behind the hard gate to catch what it lets
  #   through. Two knobs, coupled, in different halves of this file; neither receipt mentioned the
  #   other until now. See `growth_max_peg` for the ladder and the 2.0 recommendation.
  #   NOTE ALSO that `value_factor` SATURATES: (value_ref_pe / pe).clamp(0.5, 1.5) at ref 20.0 is
  #   already pinned at the 0.5 floor by P/E 40, so even at weight 1.0 it cannot separate a P/E 70
  #   compounder from a P/E 300 one. Re-enabling this term does NOT by itself make a loose PEG rung
  #   safe; the clamp or the ref would have to move too, and both are backtest-blind (pe_ratio is
  #   None in backtest_quote, which is the whole reason this ships at 0).
  # (#2) VALUATION TILT — ENABLED 2026-07-12 after re-validation. History: the 2026-07-02 wide run
  #   (8175 cutoffs) had the earnings_yield lane LOSING to price-only (+140.4 vs +151.2) and the tilt
  #   shipped OFF. Three later independent-pool wide runs (2× full S&P 500 on 8320/8380 cutoffs, then
  #   the whole live screen universe) all reversed that: the earnings_yield lane beats the price-only
  #   baseline with BOTH out-of-sample halves positive every time (+156.9 vs +120.5, +156.4 vs +115.4,
  #   +136.9 vs +124.1), and the swing traced to baseline variance, not the factor (its lane edge held
  #   +137..+157 in all four runs). Ship test on the serving pool, tilt on-vs-off same batch:
  #   GROWTH edge +170.3 vs +156.9, rho +0.19 vs +0.15, OOS +0.14|+0.20 vs +0.14|+0.12, winsorized
  #   +139.2 vs +126.8, held top-10 book +15.0 vs +14.7 (excess +7.0, OOS +5.6/+8.4), worst drawdown
  #   -7.8 unchanged. Weight sweep knee = 1.0 (monotone up to 1, falls at 2). Every OTHER factor
  #   (margins, rev/eps growth, buyback, ROE, insider, composite) still fails the both-OOS+ keep-rule —
  #   the sweep's nominal per-run "WINNER" line bounces between factors (searched-weight overfit);
  #   earnings_yield is the only one positive on every view. Revert rule: revert (weight -> 0) only
  #   on TILT-SPECIFIC degradation in a future wide run: the growth-lane ablation shows removing this
  #   weight HELPS (Δ edge >= 0), or standalone earnings_yield loses an OOS half. A lane OOS half that
  #   price-only ALSO loses is regime weather, not the tilt (2026-07-17 early audit: lane early half
  #   -0.03 with price-only -0.04, ablation removal cost -8.6 edge, standalone ey +0.20|+0.23 -> kept;
  #   receipts .claude/bt_audit_2026-07-17.txt).
  #
  #   (#178) RE-RUN ON A NON-VACUOUS LANE — AND THE KNOB IS STRUCTURALLY INERT THERE. UNGRADEABLE,
  #   NOT "held", and the backlog item stays OPEN. (#147) above measured this knob twice on
  #   `universe fund pit` and BOTH columns read VACUOUS, so the 0.0 verdict rested on a ruler that
  #   (#149) says cannot move for a reorder. This round took it to `universe fund stress` — the lane
  #   (#47)/(#70) originally set these weights on, and the lane (#130) chose for exactly this reason.
  #   Pre-registered before any arm ran: `backtest --universe fund stress --years {20,8}`, ship rule
  #   v2 UNCHANGED (PRIMARY reads 20y AND 8y; the (#139) 12y/8y substitution is PIT-LANE-ONLY and was
  #   deliberately NOT borrowed). Ladders fixed in advance, every rung reported, no rung added after.
  #   `fund` is a deliberate divergence from (#130)'s bare `universe stress`: without it the gate
  #   stack is silently incomplete AND this knob has no P/E to act on at all. It took — 8y coverage
  #   10941/21488 with a live `growth_max_peg` sweep row (n=65); 20y coverage 4394/4608. (The 20y
  #   `admits 0 new names` on that row is a non-binding gate, NOT (#174)'s missing-`fund` signature.)
  #
  #   VOIDING GATE, RUN FIRST, AND IT IS THE REASON THIS ROUND EXISTS. (#130) predates (#149)'s POOL
  #   CENSUS, so "stress is non-vacuous" was an INFERENCE from a 2026-08-23 table, never a
  #   measurement. Measured now, on the control:
  #     20y  37 buckets, median gated pool 11 > basket 10, 18 saturated, rank-1 h2h denom 19
  #      8y  59 buckets, median gated pool 11 > basket 10, 28 saturated, denom 31
  #   NON-VACUOUS at both horizons — a ranking knob CAN move this column, which is true of no other
  #   lane in this file. Control run TWICE per horizon ((#176) part 4): 0 changed lines either time,
  #   byte-reproducible — cleaner than the pit lane, whose `edge` diagnostic is not. Worst windows
  #   reproduce (#130) exactly (-0.5 at 20y, -9.2 at 8y). `windows scored` healthy at both; no
  #   FOLIOMAN_OFFLINE anywhere near these runs.
  #   THE PRICE, stated here and not in a footnote: `STRESS_TICKERS` is 27 names (backtest.rs:594),
  #   so the fat denominators do NOT come from the loser injection — they come from membership not
  #   being point-in-time. Today's index is a set of proven compounders, so far more of them clear a
  #   19% CAGR gate and the buckets fill. SURVIVOR BIAS IS WHAT MAKES THIS LANE GRADEABLE. Hence the
  #   reading rule, also pre-registered: a rung that REFUSES on this friendly, pool-full,
  #   survivor-flattered lane is DEAD PERMANENTLY — it will not win on the honest one, and that
  #   refusal is publishable. A rung that CLEARS here proves NOTHING and earns only an instrument run.
  #
  #   CONTROL BARS.  20y top-10 +5.9|+6.0, worst -0.5, h2h 6-10 14/23 61%, 11-20 12/19 63%.
  #                   8y top-10 +6.9|+7.8, worst -9.2, h2h 6-10 30/44 68%, 11-20 18/31 58%.
  #
  #   THIS KNOB (w -> 0.5 and w -> 1.0). At 20y BOTH rungs are BYTE-IDENTICAL to the control: 0
  #   changed lines in the entire report, every rank slice included. That is NOT "held". The run
  #   prints `earnings_yield n/a (only 0 cutoffs carry this factor)` at 20y, and `pe_ratio` is filled
  #   ONLY from `earnings_yield` (backtest.rs:1233 — an as-of P/E needs as-of EPS), so
  #   `value_factor == 1.0` for every name and the knob is STRUCTURALLY INERT at 20y. RECORD IT AS
  #   NOT TESTED. This is a SECOND cause of a non-answer, distinct from (#149)'s saturation: there
  #   the ruler cannot move, here the knob never touches the ruler. Both are untested, neither is
  #   evidence, and a byte-identical column must never be written up as a "hold" on either cause.
  #   At 8y the knob IS live. w = 1.0: top-10 +6.9|+8.0 (median +7.8 -> +8.0), worst -9.2 unchanged,
  #   h2h 68% and 58% both unchanged; rank-1 +8.1|+8.5 -> +8.2|+8.6, 2-5 +7.1|+7.9 -> +7.0|+7.9,
  #   6-10 +5.9|+5.2 -> +5.9|+5.4, 11-20 +5.6|+5.5 -> +5.5|+5.0. w = 0.5: verdict block IDENTICAL to
  #   control (310 changed lines, all diagnostics). So the only rung that moves anything moves ONE
  #   horizon, and it is the horizon PRIMARY cannot decide on alone.
  #   VERDICT: UNGRADEABLE. Stress PRIMARY needs 20y AND 8y and 20y cannot answer; `pit` may drop 20y
  #   under (#139) but is VACUOUS for ranking knobs, which (#147) measured twice. The knob falls
  #   BETWEEN the two lanes and NO EXISTING LANE CAN GRADE IT. 0.0 stands by default, not by
  #   measurement. Anyone re-opening it must FIRST pre-register a (#139)-style 12y-AND-8y amendment
  #   for the stress lane, before running anything — proposing it now, having seen the favourable 8y
  #   median, would be moving a pre-registered bar after seeing a number.
  #
  #   AND A FINDING THE SHIP RULE CANNOT SEE. At w = 1.0, 8y, the knob pulls a TOTAL LOSS into the
  #   graded book: `top-10 zero names (8y): FRCB@b4034(-100%)`, and the top-10 book goes `rode 0
  #   zeros` -> `rode 1 zeros/444 holds` — while every ship-rule statistic stays flat or improves. A
  #   cheap trailing P/E on a bank weeks before it is seized is the textbook value trap, and
  #   mean|median|worst-window|h2h are all blind to a single -100% inside a 10-name equal-weight
  #   basket. Not itself the refusal (the knob is untested at 20y regardless) — a warning attached to
  #   any future re-open: read the zero-names line, not just the verdict block.
  #
  #   TWO OTHER RANKING RUNGS WERE KILLED ON THIS LANE IN THE SAME PRE-REGISTERED BATCH. Verdicts
  #   below; they are repeated in short at each knob.
  #   (a) `growth_trend_weight` 0.15 -> 0.35 — the (#70) lead (#120) made re-checkable, since (#70)
  #       refused it on a TOP-3 worst window and the graded basket is now top-10. REFUSED TWICE OVER.
  #       PRIMARY fails at BOTH horizons: 20y median +6.0 -> +5.4 DOWN, 8y mean +6.9 -> +6.8 DOWN.
  #       AND the (#160) not-worse bar breaks — 8y 6-10 h2h 68% -> 66% is BELOW the control's tally —
  #       so it REFUSES OUTRIGHT and is never softened to unresolved. 20y h2h collapses on both books
  #       (61% -> 52%, 63% -> 53%) and 20y rank-1 goes +6.0|+6.0 -> +5.1|+4.2. DEAD PERMANENTLY:
  #       (#70)'s trend lead is CLOSED, on the very lane it was set on, at the basket size (#120)
  #       moved it to. Caveat quoted rather than dropped: (#130)(d) found trend and
  #       `growth_accel_weight` are near-substitutes on frozen data (cutting accel 0.15 buys what
  #       raising trend ~0.10 buys), so the two multiply into ONE slope and this is a PARTIAL view of
  #       it. Sweeping accel INDEPENDENTLY of beta stays OPEN; (#130) swept only beta.
  #   (b) `sharpe_weight` 0.05 -> 0.15 — (#70)'s own unasked question, verbatim: "the open question is
  #       whether to RAISE this knob, never whether it is currently earning its place." REFUSED; THE
  #       ANSWER IS NO. 8y mean +6.9 -> +6.8 DOWN, so PRIMARY fails. 20y top-10 byte-identical, worst
  #       window unchanged at both horizons, and both h2h books drift DOWN rather than up (20y 11-20
  #       63% -> 58%; 8y 11-20 58% -> 55%; 8y 6-10 flat at 68%). Nothing gained to trade against the
  #       loss. DEAD PERMANENTLY. 0.05 stands.
  #   NO SURVIVOR, so the planned ship-instrument phase (full surface at both horizons plus the pit
  #   column printed beside it) was SKIPPED — there was nothing left to instrument. No shipped value
  #   changed in this commit: all three are RANKING knobs, so the goldens do not move, no gate
  #   LIVE/INERT reachability is in play, and `growth_gate_reachability_pin` is untouched.
                                 # (#185) GRADED, 2026-08-31, ON THE AMENDMENT WRITTEN FOR IT — AND IT REFUSES. Rungs {0.5, 1.0}
                                 #   pre-registered and the amendment committed BEFORE any run (`a167527`). Lane `universe fund
                                 #   stress`, three horizons, control + both arms = 9 runs.
                                 #   THE INERTNESS EVIDENCE `(#185)` DEMANDS, QUOTED FROM THE RUN: 20y prints `earnings_yield n/a
                                 #   (only 0 cutoffs carry this factor)`. 12y prints `winners n=66 med 17.48 losers n=8 med 5.64 gap
                                 #   11.84 AUC 0.77` and 8y `n=137/18 gap 3.42 AUC 0.60` — so the factor is genuinely absent at 20y
                                 #   and genuinely present, and strongly discriminating, at the other two. The amendment applies.
                                 #   IT STILL CANNOT BE ADJUDICATED, AND NOW THE REASON IS COMPLETE. Column by column:
                                 #     20y VACUOUS by `(#185)` — the factor is absent, so every arm returns the control by
                                 #       construction (confirmed: +5.9|+6.0, worst -0.5, h2h 12/19 identical on all three arms).
                                 #     12y VACUOUS by `(#149)` — median gated pool 10 against a graded basket of 10, and the run
                                 #       PRINTS the VACUOUS tag. This was not foreseen when the amendment was drafted; `stress` was
                                 #       believed non-saturated on the strength of `(#178)`'s 20y/8y census (median pool 11 both),
                                 #       and 12y had never been run on this lane. It is saturated by one name.
                                 #     8y is the ONLY column that can answer a ranking knob here.
                                 #   `(#185)`'s PRIMARY reads 12y AND 8y, and `(#149)` says a VACUOUS column is recorded VACUOUS and
                                 #   NEVER "held". So the 12y leg cannot be supplied BY ANY ARM, and PRIMARY is unsatisfiable on this
                                 #   lane. REFUSED — not on a number, on the structure. Two vacuous columns cannot be voted with.
                                 #   WHAT 8y SAYS ANYWAY, since a refusal on structure invites "but was it winning?": no.
                                 #     rank-1 h2h on the 6-10 book is 30/44 (68%) on ALL THREE arms — the GUARD statistic does not
                                 #     move at all. rank-1 excess +8.1|+8.5 control, +8.1|+8.5 at w=0.5, +8.2|+8.6 at w=1.0.
                                 #     top-10 +6.9|+7.8 control, IDENTICAL at w=0.5, +6.9|+8.0 at w=1.0. Census byte-identical on
                                 #     every arm at every horizon (37/51/59 buckets, median pool 11/10/11) — a PURE REORDER, as a
                                 #     ranking weight must be. Everything that moves moves by 0.1pt, at or below the resolution
                                 #     `(#137)` measured for a weight arm.
                                 #     AND THE ONE GAIN IS THE ZERO-NAMES LINE `(#178)` WARNED ABOUT: w=1.0's +7.8 -> +8.0 median
                                 #     arrives with `top-10 zero names (8y): FRCB@b4034(-100%)` — the arm buys 0.2pt of median by
                                 #     admitting a total loss to the book, while its 11-20 slice falls +5.6|+5.5 -> +5.5|+5.0.
                                 #   VERDICT: `growth_value_weight` stays 0.0 and `(#181)`'s "one genuinely open item" is CLOSED —
                                 #   closed as PERMANENTLY UNGRADEABLE ON TODAY'S LANES, which is a result, not a to-do. `pit` is
                                 #   vacuous for ranking knobs (`(#180)`: median pool 5/6/7); `stress` is structurally inert at 20y
                                 #   and saturated at 12y. No lane in this file has two columns that can adjudicate it.
                                 #   REVERT RULE: re-open ONLY if a lane appears whose median gated pool clears the graded basket at
                                 #   TWO horizons that both carry `earnings_yield`. Widening the pond is the known lever and
                                 #   `(#173)` already measured that it moves the median gated pool by zero names. Do not re-run
                                 #   these rungs on these lanes; that question is spent.
  fund_source: "sec"         # SEC EDGAR XBRL: free, keyless, uncapped — the source the tilt was validated on
                             #   (train-serve: FMP's 250/day cap can't even cover one wide run).
                             #   NO LONGER US-only as of 2026-07-25 (#3c): 20-F/40-F forms, the ifrs-full
                             #   taxonomy and non-USD reporting currencies all parse now. Non-covered names
                             #   still get fund_factor None -> tilt neutral, price terms decide.
  # (#3) GROWTH-AT-PRICE — earnings_yield SUPERSEDED by peg_yield 2026-07-25. peg_yield = 1/PEG =
  #   earnings_yield · as-of CAGR (100 <=> PEG 1; >100 <=> PEG < 1 = cheap for its growth). The axis sat
  #   wired-but-unmeasured for rounds, annotated "expected dead — redundant with earnings_yield x CAGR
  #   already in the score". It is not dead; the annotation was never tested. Two blockers hid it:
  #   (a) peg_yield was None on the LIVE path (only earnings_yield got the native-close backfill), so
  #   selecting it would have silently zeroed the fund term; (b) it was absent from BOTH the standalone
  #   probe list and the weight-searching sweep, so no view could see it. Both fixed.
  #   SCALE IS LOAD-BEARING: peg_yield runs ~0-500 where earnings_yield runs ~0-15, so the old
  #   growth_fund_cap 30 clamps every PEG < 3.3 name to one value. Measured at cap 30 peg_yield reads
  #   +151.9 and LOSES to earnings_yield +158.7; at cap 300 it reads +195.1 and wins. Same run, same
  #   samples — the +43.2 IS the clamp releasing. Never read this factor at a cap below ~300.
  #   Wide same-batch 12y, 4913 tickers / 11717 cutoffs / 8393 with as-of fundamentals:
  #     standalone lane (ranks the RAW factor -> immune to the searched-weight overfit the block below
  #       warns about; the decisive view): peg_yield n=1489 rho +0.14 edge +291.7 OOS +0.16|+0.13
  #       vs earnings_yield n=1716 rho +0.11 edge +207.4 OOS +0.12|+0.09 — better on ALL FOUR dials.
  #     sweep held-out TEST (each factor at its own best cap): peg_yield +195.1 OOS +0.23|+0.11 vs
  #       earnings_yield +158.7 OOS +0.24|+0.12, price-only baseline +116.9. WINNER survives the
  #       Sidak best-of-11 haircut: band [+89.3 ... +283.3], reproduced [+98.8 ... +295.1].
  #     reproducibility: two independent cap-300 runs returned peg_yield +195.1 and earnings_yield
  #       +147.4 to the decimal; only the bootstrap band jittered (RNG resampling).
  #   CAVEATS, recorded honestly — this is NOT a clean sweep:
  #     - held-book by FACTOR (absolute, top-10 held 12y): peg_yield excess +4.8 (n=245) vs
  #       earnings_yield +5.0 (n=250). PEG LOSES this view, marginally, on a slightly different
  #       subsample. Both trail plain growth_score (+6.9) — neither is a better standalone selector.
  #     - (#3k) 2026-07-29 rank-source arm, STRESS grid on trimmed data: composite stays the ranker.
  #       12y-s composite +6.6 beats every factor (best gross_margin +5.1, n=120); 8y-s peg_yield
  #       +6.6 vs composite +6.4 (n=425) = statistical tie; 20y-s factor table structurally EMPTY
  #       (cutoffs end ~2006, SEC XBRL starts ~2007 — factors CANNOT be graded at the goal horizon).
  #       An earlier "+6.9 factor vs +6.1 composite, ship the winner" reading came from the
  #       contaminated config (use_life_cagr true, no splice trim, no stress) and is void.
  #     - as a GATE it is dead: the PEG-VALUE-GATE reject-bottom sweep is flat +6.8 -> +6.9 across
  #       0/10/25/40%. Shipped as a rank TILT only. The brake framing is closed on measurement.
  #     - the WINNER line genuinely does bounce with the cap (composite at 30, peg_yield at 300),
  #       exactly as the block below warns. That is why the cap-free standalone view decides here.
  #   Keep-rule inherited from (#2): revert to earnings_yield only on TILT-SPECIFIC degradation in a
  #   future wide run — the growth-lane ablation shows removing this weight HELPS, or standalone
  #   peg_yield loses an OOS half. A half that price-only also loses is regime weather, not the tilt.
  growth_fund_factor: "peg_yield"  # 1/PEG = as-of TTM EPS / native close x as-of CAGR — same definition in backtest and live
  growth_fund_weight: 0.07   # ON — the TOP of the measured plateau (curve receipt (#3b) below). Raised from
                             #   the sweep's argmax 0.018 on 2026-07-25 to give PEG < 1 more say in the rank.
                             #   NOT transferable from the pre-2026-07-25 1.0: that was earnings_yield's knee on
                             #   a ~0-15 factor. Shipping peg_yield (~0-500) at 1.0 would run the tilt ~55x hotter
                             #   than anything measured — up to 300 pts against a ~24 pt trend term — and nothing
                             #   would flag it, since the factor NAME would still match this receipt.
                             #   What the score sees is weight x clamp(value, 0, cap): 0.07 x 300 = 21 pts max,
                             #   7.0 pts at PEG 1, 3.5 at PEG 2 (was 1.8 / 0.9 at the argmax weight).
                             #   Move weight and cap TOGETHER or the magnitude drifts silently.
  # (#71) GRADED UNDER SHIP RULE v2 — 2026-08-11, and it HOLDS at 0.07. Read this before (#3b)-(#3e)
  #   below: those are the pre-v2 history, measured on the fund lane's own held-out TEST edge. This is
  #   the first time the knob has been put to the rule that decides everything else in this file.
  #   WHY THE ROUND WAS OWED. The wide `universe` run that graded (#69) and (#70) carries NO
  #   fundamentals — `fund_lane_on` (backtest.rs:501) gates the as-of fill, so without the `fund`
  #   keyword `quote.fund`, `quote.fund_factor` and `quote.roe` are None on every row. This knob,
  #   growth_fund_factor, growth_fund_cap, growth_max_peg and growth_require_peg therefore contributed
  #   NOTHING to any number v2 had ever judged. Twenty-one graded wide runs were blind to all of them.
  #   METHOD. Twelve runs, `growth_fund_weight in {0, 0.05, 0.07, 0.12}` x `backtest {20,12,8} universe
  #   fund`, one knob moved, rule fixed before the first run, ~2.5 min each off a warm .sec_cache.
  #   Data conditions identical in all twelve: 4997 tickers, growth-scored 351/511/637, cutoffs with
  #   as-of fundamentals 4394/8456/10956, and the Yahoo crumb fallback failed in every run alike.
  #     horizon   arm      top-3 mean|med    win%   h2h    worst
  #     20y       ALL FOUR   +6.3|+5.7        97%   74%    -0.5     <- byte-identical, see BLACKOUT
  #     12y       0.00       +6.7|+6.9        84%   63%    -7.8
  #     12y       0.05       +6.6|+6.9        84%   67%    -7.8
  #     12y       0.07 ctl   +6.8|+6.9        84%   67%    -7.8
  #     12y       0.12       +6.9|+7.1        86%   67%    -7.8
  #     8y        0.00       +7.1|+8.0        81%   57%    -9.2
  #     8y        0.05       +7.3|+8.0        81%   63%    -9.2
  #     8y        0.07 ctl   +7.3|+7.8        83%   63%    -9.2
  #     8y        0.12       +7.3|+7.8        83%   63%    -9.2
  #   VERDICT. No guard is broken by any arm — the worst window is identical at every horizon (the tilt
  #   never touches the tail) and h2h stays >=57% throughout. 0.00 FAILS THE PRIMARY: 8y top-3 mean
  #   +7.1 vs +7.3. That is the round's one robust result, and it is robust in the strong sense — off
  #   loses the mean at EVERY basket size at 8y (top-1 -0.2, top-3 -0.2, top-5 -0.3, top-10 -0.2), loses
  #   both moments at 12y top-1 (+6.5|+7.7 vs +6.8|+7.6), drops 8y rank-1 h2h 63% -> 57%, and drops the
  #   8y top-3 late OOS half +8.0 -> +7.7. THE TILT EARNS ITS PLACE.
  #   0.05 and 0.12 pass the letter of the primary and are NOT shipped, for two stated reasons.
  #   (1) Neither survives a basket-size robustness check. 0.05's whole case is 8y top-3 median +0.2 —
  #   but the same comparison is +0.1 at top-1 mean, flat at top-5, -0.1 at top-10 mean, and it COSTS a
  #   winning window (83% -> 81%). 0.12's gains sit entirely at 12y, which the primary does not judge;
  #   at both judged horizons it is flat. Signs that flicker across basket sizes are noise, and every
  #   difference here is <=0.2 pts/yr at one-decimal printing precision.
  #   (2) Half the primary could not be evaluated at all — see the blackout below. An arm cannot
  #   discharge "up or held at 20y AND 8y" on 8y evidence alone.
  #   THE 20y FUNDAMENTAL BLACKOUT, and it is a RULE-LEVEL finding, not a knob one. All four arms are
  #   byte-identical at 20y down to the OOS split, because at 20y EVERY ONE of the 17 standalone factor
  #   probes reports "n/a (only 0 cutoffs carry this factor)" — peg_yield included. The run's own
  #   ablation confirms it independently: `growth_fund_weight rho +0.23 D+0.00 edge +707.3 D+0.0`,
  #   exactly zero, against D+0.03/+15.9 at 12y and D+0.01/-2.6 at 8y. Note the header two lines above
  #   the probes still reads "cutoffs with as-of fundamentals: 4394 / 4588": that counter is
  #   `s.fund.is_some()` (backtest.rs:1431), i.e. a FundRow was ATTACHED, while the probes count
  #   samples whose derived field is Some. At 20y-era cutoffs the attached rows carry no usable
  #   derived values, so 96% "coverage" and zero usable factors are both true and not in conflict.
  #   CONSEQUENCE, which outlives this knob: Ship Rule v2's PRIMARY requires 20y AND 8y, so it can
  #   never be discharged non-vacuously by ANY fundamental knob — growth_fund_factor, growth_fund_cap,
  #   growth_max_peg, growth_require_peg and quality_weight all inherit this. Every such arm will read
  #   "held at 20y" for free. Do not mistake that for evidence. See the note beside Ship Rule v2 above.
  #   (#136) THE SAME BLACKOUT ONE RUNG MORE DANGEROUS, measured 2026-08-25 on `universe fund pit` 12y.
  #   The 20y case above is HONEST: zero cutoffs carry the factor, so the probe prints "n/a" and no
  #   one is fooled. At 12y on the PIT lane coverage is non-zero but TINY, and the table then prints a
  #   confident-looking row instead. In the `held-book by FACTOR` block, SIX unrelated factors return
  #   BYTE-IDENTICAL rows:
  #     earnings_yield / peg_yield / buyback_yield / roe_raw / fcf_margin / net_cash_rev
  #     all: book +16.6%/yr  excess +5.0  win 100%  worst +0.8  OOS +4.7/+5.2  (n=52)
  #     (roic is the same set minus three: n=49, worst +0.7)
  #   against growth_score's baseline +8.9%/yr, excess +2.7, win 74%, worst -9.5, n=220.
  #   THE PROOF IT IS COVERAGE AND NOT SKILL, and it is airtight because the same run contains the
  #   control. Those factors are NOT the same signal: on the large standalone sample the same run
  #   gives peg_yield rho +0.24 (n=1142), earnings_yield +0.20 (n=1400), roe_raw -0.02 (n=1438).
  #   Factors that rank differently on n~1400 cannot rank identically on n=52 unless the ranking is a
  #   NO-OP. It is: the block ranks FUND-COVERED gated picks and holds the top 10, and n=52 over 34
  #   windows is ~1.5 covered names per window. "Top-10 of 1.5 names" is every covered name, for every
  #   factor. win 100% and worst +0.8 across 34 windows should have been read as the tell.
  #   WHY THIS MATTERS RIGHT HERE: backtest.rs:2975 prints, immediately beneath that table, "a factor
  #   beating growth_score's held-book excess with OOS both + is a better held-book selector -> ship
  #   it." Six factors clear that line by +2.3 pts/yr with both OOS halves positive and a positive
  #   worst window. Taken at face value it is a standing invitation to ship a coverage artifact.
  #   BEFORE SHIPPING ANY FACTOR OFF THAT TABLE: check its n against the window count. If n/windows is
  #   below the basket size, the row is not a ranking result and the comparison to growth_score is not
  #   like-for-like — growth_score is graded on n=220 there, a real book, and the factor on ~1.5 names.
  #   THE SHIPPED VALUE IS NOT CONTAMINATED. `growth_fund_factor: "peg_yield"` leads the STANDALONE
  #   probe (rho +0.24, edge +506.3, TEST edge +555.0, OOS +0.72|+0.50 — all on n=1142, where the
  #   factors do discriminate), which is the evidence (#3b)-(#71) actually used. Nothing here reopens
  #   that choice; this is a guard on how the held-book table is read from now on.
  #   Sixth entry in the (#71) list of lane-level metrics disagreeing with book quality — except this
  #   one disagrees with ITSELF at two sample sizes in one run.
  #   THE FUND LANE'S OWN COLUMNS DISAGREED AGAIN, and were pre-committed as non-voting before the runs.
  #   At 8y the sweep verdict prints "no factor beats price-only with both OOS halves + — keep
  #   growth_fund_weight 0. SHIP NOTHING." and its curve reads tilt-off -75.0 vs shipped -77.4, i.e. the
  #   tilt COSTS 2.4 edge. The graded top-3 book says the exact opposite at the same cutoffs in the same
  #   run: off is the worst arm at every basket size. That is the FIFTH time a lane-level metric has
  #   disagreed with book quality, after (#63) accel, (#69) an ablation delta, (#70) an edge curve, and
  #   (#3d) itself. Lane edge is a top-half-vs-bottom-half spread over hundreds of windows and is blind
  #   to the top-3 book. USE THESE COLUMNS TO CHOOSE AN ARM, NEVER TO GRADE ONE.
  #   THIS RESOLVES (#3d)'s OPEN DECISION. That receipt left three options and favoured (a)
  #   `growth_fund_weight -> 0` as "the best measured corner, +202.3, and the sweep's own verdict". On
  #   the graded book, option (a) is the WORST of the four arms and the only one that fails the primary.
  #   (#3d)'s finding stands as measured — PEG and quality DO double-count on lane edge — but the
  #   conclusion it pointed at does not survive top-3. Option (c), zeroing quality in the growth lane
  #   only, is still untested and is now the live lead; it is a code change, not a tuning one.
  #   (#3b)'s REVERT RULE IS EXPLICITLY NOT TRIPPED. It says drop to 0.05 "at the first sign of
  #   late-half degradation in a future wide run". This is that wide run, and the late half IMPROVES
  #   with the tilt on: the 8y curve's OOS halves go -0.06|-0.07 at weight 0 to -0.05|+0.10 at 0.07, a
  #   sign flip in the late half, and the graded 8y top-3 late OOS is +8.0 shipped vs +7.7 off.
  #   AND THE 0.08 CLIFF DOES NOT REPRODUCE. (#3b) measured -71 edge in one step at 0.08 and (#3c)
  #   warned the cliff is a ceiling on weight x value that moves with the sample. It moved: the 12y
  #   curve now reads 0.050/0.060/0.070/0.080/0.090/0.100 all at +227.6 exactly, and the graded 0.12 arm
  #   is flat at 8y and 20y and UP at 12y. Nothing was shipped on this, but the standing warning that
  #   0.08 is one step from a cliff is no longer supported on the current sample.
  #   NOT RE-MEASURED HERE, deliberately: (#3d) warns the (#3b) curve is now a slice through a two-term
  #   interaction with quality_weight and must not be re-run to "re-find the plateau". This round did
  #   not re-run it as evidence — it graded the shipped configuration as it actually ships, one knob
  #   moved, which is the question v2 asks. The arms are therefore PEG's marginal value at
  #   quality_weight 0.15, not PEG's standalone value. Do not read them as the latter.
  # (#3b) WEIGHT CURVE — wide same-batch 12y, `backtest universe fund 12`, at growth_fund_cap 300. The
  #   factor sweep only ever reported its argmax, which cannot tell a peak from a plateau; the curve
  #   print added the same day shows the whole shape (held-out TEST edge):
  #     0.000 +117.1 [off]    0.050 +203.9 (peak)     0.080 +127.1  <- CLIFF, -71 in one step
  #     0.005 +181.4          0.060 +200.5            0.090 +109.3
  #     0.018 +195.3          0.070 +198.2 [SHIPPED]  0.250  +81.2 / 0.500 +71.4
  #   0.018..0.07 is ONE BROAD PLATEAU (+195..+204). Past 0.07 the edge falls BELOW switching the tilt
  #   off entirely (+127 vs +117 at zero, then worse) — this is a ceiling, not a gentle roll-off.
  #   0.07 was chosen deliberately for maximum PEG expression, eyes open on the cost:
  #   HEADROOM IS THIN. 0.08 is one step away and costs -71 edge. Two consequences, both binding:
  #     - anyone changing `growth_fund_cap` MUST re-run the curve before trusting this weight. The cliff
  #       is really a ceiling on weight x value, not on weight alone, so it MOVES when the cap moves.
  #     - OOS at 0.07 is +0.24|+0.09 vs +0.23|+0.11 at 0.018: early half UP, late half DOWN. Not a
  #       unanimous win — it is an expression choice taken on a flat-edge plateau, not a validated lift.
  #   Revert rule for THIS knob: drop to 0.05 (the measured optimum, 60% headroom to the cliff) at the
  #   first sign of late-half degradation in a future wide run, before touching the factor itself.
  # (#3c) FOREIGN FILERS ADMITTED 2026-07-25 — re-validation of the above, MANDATORY because it changed
  #   the sample the weight was chosen on. Until this date `parse_sec_facts` required form 10-K, unit USD,
  #   the us-gaap taxonomy and a "USD/shares" EPS key, so every 20-F/40-F/IFRS/non-USD filer produced ZERO
  #   rows and was ranked price-only with no error anywhere (ASML: seven n/a columns, no `#`). All four now
  #   parse, and any price-joined ratio converts the close into the FILER's reporting currency first —
  #   a EUR EPS over a USD ADR close is the Item 16 trap, wrong by the whole FX rate and plausible-looking.
  #   Same conversion in backtest (as-of, dated rate series) and live, so train and serve agree.
  #   RESULT — same wide 12y batch, 4913 tickers / 11717 cutoffs, curve reproduced against (#3b):
  #     0.000 +116.9   0.050 +203.8   0.070 +198.1 [SHIPPED]   0.080 +127.0   0.250 +81.2 / 0.500 +71.4
  #   Every rung within 0.2 edge of (#3b). Plateau, peak and the -71 cliff at 0.08 all sit where they did;
  #   OOS at 0.07 +0.23|+0.09 vs +0.24|+0.09. Sweep winner still peg_yield +195.1 to the decimal.
  #   -> revert rule NOT triggered; weight stays 0.07.
  #   READ THAT NARROWLY. Fund coverage moved only 8393 -> 8458 cutoffs (+0.8%): a 12y forward window
  #   needs cutoffs from 2014 or earlier and foreign SEC XBRL mostly starts after the 2011 20-F mandate,
  #   so this lane STRUCTURALLY cannot hold many of the newly-admitted names. It proves the change did not
  #   disturb the validated edge. It does NOT prove the foreign names carry it.
  #   Second lane that can see them — `backtest universe fund 5` (daily, 8594 cutoffs, 2389 fund-covered):
  #     0.000 +28.0   0.070 +47.7 [SHIPPED]   0.080 +48.7   0.250 +66.4   0.500 +52.7 — NO cliff at 0.08,
  #     exactly as (#3b) warns (the cliff is a ceiling on weight x value, so it moves with the sample).
  #     peg_yield is the best of 11 factors here too (+47.7 vs price-only +28.0) and the only one that
  #     lifts the early OOS half (-0.09 -> -0.03). That lane's headline prints "SHIP NOTHING" because its
  #     early half is negative — but it is negative at weight 0 as well, which (#3)'s own keep-rule calls
  #     regime weather, not tilt degradation. Not a revert signal; not an independent confirmation either,
  #     since there is no pre-change 5y run to difference against.
  #   Known gaps, measured not assumed: HSBC yields NO rows (neither IFRS revenue tag, and revenue is the
  #   row ANCHOR); banks lack ProfitLossFromOperatingActivities so op_margin/interest_cover stay None for
  #   RY/TD. Verified live: ARM/BABA (20-F us-gaap), NVS/AZN/SHEL (ifrs-full USD), NVO (DKK), RY/TD (40-F
  #   CAD), TM (dual-currency -> USD, matching its ADR).
  # (#3d) THE PLATEAU IS GONE — measured 2026-07-26, and the cause is NOT this knob. First wide run
  #   after `4975f23` un-blinded `quality_weight` (see (F) above), same `backtest universe fund 12`
  #   batch as (#3c) to the cutoff (11690 vs 11717; fund-covered 8458, identical):
  #     weight   0.000    0.050   0.070 [SHIPPED]   0.080    0.250    0.500
  #     (#3c)   +116.9   +203.8       +198.1       +127.0    +81.2    +71.4
  #     now     +202.3    +85.2        +95.9        +93.6    +78.2    +69.5
  #   Tilt-OFF gained +85.4 and every rung on the old plateau lost ~-105. The tails CONVERGE (0.250
  #   -3.0, 0.500 -1.9), which is the tell: at a weight where PEG swamps the score, quality's presence
  #   barely registers; at the shipped weight, where the two terms are comparable in size, they
  #   DOUBLE-COUNT. Both say the same thing — a good business you are not overpaying for.
  #   Four corners, held-out TEST edge:  quality only +202.3 | PEG only +198.1 (#3c) | both +95.9 |
  #   neither +116.9. Either term ALONE is worth ~+85; together they are worth -100 against either.
  #   Corroborated in the same run, independently of the curve:
  #     - growth-lane ablation: zeroing growth_fund_weight -> edge +187.0 Δ+5.4 (rho +0.17 Δ-0.02).
  #       Removing it HELPS on edge, hurts slightly on rho.
  #     - factor sweep verdict: "no factor beats price-only with both OOS halves + — keep
  #       growth_fund_weight 0. SHIP NOTHING." peg_yield's searched-best TEST +199.1 vs price-only
  #       +202.3; it is now the best of 11 factors and still cannot clear the bar.
  #   THIS TRIPS (#3)'s OWN REVERT RULE — "the growth-lane ablation shows removing this weight HELPS"
  #   — but read what it actually indicts. PEG did not decay: standalone it is unchanged and still the
  #   best single fundamental factor (n=1506 rho +0.14 edge +283.1 OOS +0.15|+0.13, vs the recorded
  #   n=1489 +0.14 / +291.7 / +0.16|+0.13 — sample up 17 names from the EPS tag fix, every dial flat;
  #   earnings_yield n=1737 rho +0.11 edge +203.9). What changed is that its ROOMMATE woke up.
  #   OPEN DECISION, deliberately NOT taken here: at 0.07 the two terms cost ~-106 TEST edge against
  #   running quality alone. The measured options are (a) growth_fund_weight -> 0 (best measured
  #   corner, +202.3, and the sweep's own verdict), (b) drop to ~0.005 (+194.0, PEG keeps a whisper of
  #   say), (c) keep 0.07 and zero quality in the GROWTH lane only — untested, and it would cost the
  #   BUY lane nothing since the knob is shared. Do NOT re-run the (#3b) curve to "re-find the
  #   plateau" without first fixing which of the two terms is being kept: the curve is a slice through
  #   a two-term interaction now, not a one-knob shape.
  # (#3e) SAME-FILING COMPARATIVES + the lookback fix — measured 2026-07-26, two horizons. Two changes
  #   landed together; read them apart.
  #   (i) THE JOIN. `collect` (fetch.rs) keeps the EARLIEST-filed value per period end, which is right
  #   for a LEVEL and wrong for every RATIO: each 10-K restates its comparatives at the CURRENT split
  #   basis, so taking each year from its own filing divides two different bases. TPL's stored series
  #   read 7.69M / 23.02M / 69.03M shares — three bases in one column, from two 3-for-1s (2024, 2025).
  #   Rows now carry `prior_eps`/`prior_shares` AS THE SAME FILING STATED THEM, so a YoY never crosses
  #   a basis. TPL 5y EPS CAGR: chained same-filing +22.6%/yr vs endpoint-naive -21.0%/yr, same payload.
  #   No share-ratio test can replace this — ORLY's ratio is 14.58, not 15.0, because buybacks drift it
  #   off the split ratio, and a jump cannot say WHY it jumped. Three classes the old >40% rule
  #   conflated: real split (8 names, comparatives restated), real dilution (6 — the guard was HIDING a
  #   fact: COF's EPS genuinely fell 11.59 -> 4.03), scale error (PDD, WRB). Effect on the shipped
  #   factors, `backtest universe fund 12`: peg_yield UNCHANGED n=1506 rho +0.14 edge +283.2 OOS
  #   +0.15|+0.13 (vs recorded +283.1 / +0.15|+0.13) — it reads quote.trend_cagr (a PRICE cagr), so the
  #   join cannot touch it; (#3)'s keep-rule not tripped. buyback_yield REPOPULATED n=1183 -> 1737
  #   (+554 samples, the restated names rejoining), edge -276.5 -> -262.1, rho +0.04 -> +0.05. Still a
  #   losing factor, still unshipped.
  #   (ii) THE LOOKBACK. `backtest.rs` passed the run's FORWARD hold horizon as the BACKWARD fundamental
  #   lookback, so `fund 12` demanded 12y of statements before every cutoff. rev_cagr / rev_accel /
  #   eps_growth were None on EVERY sample and printed "n/a (only 0 cutoffs)", while their sweep lines
  #   read EXACTLY the price-only baseline — which reads as "measured, no edge" and was "never
  #   measured". Now a constant 5, the value both live call sites already pass. NOTE the residual
  #   boundary: `fund_as_of` filters on FILED, and XBRL filings only start 2009-2010 (54 rows filed in
  #   2009, 651 in 2010), so at horizon 12 (cutoffs end 2014-07) rev_cagr/rev_accel STILL read 0 —
  #   cutoff-5y lands before the filings exist. eps_growth fills (n=265) only because it filters on
  #   period_end, and one filing carries several comparative years. `fund 5` clears the boundary
  #   entirely (cutoffs to ~2021): rev_cagr n=2094, rev_accel n=2092.
  #   THE ANSWER TO "should REV-YoY / EPS-YoY / NET% raise growth_score", standalone probes, both runs:
  #                    fund 12 (n / rho / OOS)          fund 5 (n / rho / OOS)         verdict
  #     eps_growth      265  +0.09  +0.17|+0.05        1810  +0.13  +0.18|+0.09       BOTH halves + at BOTH
  #     rev_yoy        1316  +0.08  +0.08|+0.07        2277  +0.05  +0.18|-0.08       fails at 5y
  #     eps_yoy        1719  -0.02  +0.03|-0.09        2236  +0.05  +0.13|-0.05       fails at both
  #     net_margin     1824  -0.19  -0.16|-0.21        2327  -0.12  -0.12|-0.12       NEGATIVE on all four
  #     rev_cagr        n/a (filed-date boundary)      2094  +0.06  +0.16|-0.03       fails at 5y
  #   NET% is the clear one: negative rho on all four halves, matching its above-the-line twin op_margin
  #   (n=1516 rho -0.23 edge -326.7 OOS -0.22|-0.24). Rewarding a high margin LEVEL subtracts edge. What
  #   works is the margin RISING (margin_trend +0.02|+0.08), which is a different knob.
  #   rev_yoy WINS the fund-12 sweep — TEST +214.9 vs price-only +202.2, OOS +0.24|+0.11, and it clears
  #   the now-stricter best-of-14 Sidak band [+73.4 … +291.8], the first factor ever to beat that
  #   baseline on this sweep. DO NOT SHIP IT ON THAT ALONE: its own standalone late half goes -0.08 at
  #   horizon 5, and the fund-5 sweep's verdict is "no factor beats price-only with both OOS halves +
  #   — SHIP NOTHING" (every factor there has a negative EARLY half, price-only included, so that sweep
  #   discriminates nothing — read its standalone rows instead). A factor that wins at one horizon and
  #   loses at the other is horizon-dependent, not an edge.
  #   (#157) THE SWEEP WAS WIDENED 14 -> 24 AND MEASURED 2026-08-29. SHIP NOTHING.
  #   `core::select_fund_factor` has always registered and computed ten factors this array never
  #   asked about — they were reachable from every OTHER read site (the held-book-by-factor table,
  #   the SURVIVAL-GATE probe, `growth_fund_weight`) and absent only here, which is the one place
  #   that applies a multiple-testing haircut. So four of them had (#124)'s percentile-cut reading
  #   and no honest band, and the other six had neither. The array also moved to module scope as
  #   `FUND_FACTORS` so (#159) reads the SAME registry — non-negotiable #4, one definition.
  #   THE HAIRCUT GOT STRICTER, NOT WEAKER. `sidak_tail(n)` returns (5/n, 100-5/n), so the band's
  #   tails move from the 0.357th/99.643rd percentile at 14 to the 0.208th/99.792nd at 24. ANY band
  #   quoted in this file must be read against the arity it was computed at — check the length before
  #   calling a wider band a regression.
  #   Runs: `backtest {12,8} universe fund pit`, this file unmodified.
  #                     12y (baseline +309.7)        8y (baseline -48.6)        verdict
  #     roe             +376.2  OOS +0.31|-0.03     -70.3  OOS -0.18|+0.01     below baseline at 8y
  #     quality         +376.2  OOS +0.31|-0.03     -50.0  OOS -0.18|+0.02     below baseline at 8y
  #     roic            +349.5  OOS +0.20|+0.00     -22.7  OOS -0.12|-0.00     early half - at 8y
  #     ebitda_yield    +400.5  OOS +0.50|+0.08     -50.7  OOS +0.04|+0.14     below baseline at 8y
  #     fcf_margin      +326.7  OOS +0.48|-0.07     -17.8  OOS -0.13|+0.10     a - half at BOTH
  #     interest_cover  +180.8  OOS +0.33|-0.18     -18.3  OOS -0.09|+0.08     below baseline at 12y
  #     net_cash_rev    +349.5  OOS +0.50|-0.06      +0.7  OOS -0.10|-0.06     a - half at BOTH
  #     margin_stability +309.7 OOS +0.47|-0.00     -48.6  OOS -0.13|-0.02     INERT (see below)
  #     accrual_gap     +403.1  OOS +0.47|+0.07     -53.2  OOS -0.12|-0.00     below baseline at 8y
  #     asset_growth    +309.7  OOS +0.47|-0.00     -48.6  OOS -0.13|-0.02     INERT (see below)
  #   NOT ONE OF THE TEN clears price-only with both OOS halves + at BOTH horizons, which is the bar
  #   this sweep has always applied. `accrual_gap` is the 12y ARGMAX (+403.1 against a +309.7
  #   baseline, both halves +) and drops BELOW baseline at 8y (-53.2 vs -48.6) — horizon-dependent,
  #   the identical disqualification `rev_yoy` already carries above. DO NOT RE-POINT
  #   `growth_fund_factor` AT IT: the WINNER row is an argmax, `peg_yield` stays shipped.
  #   `margin_stability` and `asset_growth` print EXACTLY the price-only baseline at both horizons.
  #   That is not "weak" — the weight search returned 0, so the tilt is inert and the factor cannot
  #   move the score at any weight the search would pick.
  #   >>> (#168) 2026-08-30 REFUTES THE THREE LINES DIRECTLY ABOVE. The weight search did NOT return 0.
  #   It returned w 0.249 for BOTH factors at BOTH horizons. The sentence was an INFERENCE from a
  #   number the table did not print; the table now prints it (backtest.rs:2234) and the inference is
  #   wrong. What the signature actually means is settled by a control the same table supplies:
  #   `insider_net_buys_90d`, which `report_fund_lane` independently reports as "only 0 cutoffs carry
  #   this factor", prints the IDENTICAL row at 12y — TEST edge +309.7 (w 0.249) OOS +0.47 | -0.00,
  #   byte-for-byte what `margin_stability` and `asset_growth` print. So a baseline-identical row is
  #   consistent with THE FACTOR FILLING NOTHING ON THE SWEEP SAMPLE — a NON-MEASUREMENT — and is NOT
  #   evidence that a real tilt was applied and found neutral. A real, large weight was applied and the
  #   book did not move one basis point; that is a coverage statement, not a search verdict.
  #   AND THE TAG IS UNDER-APPLIED. SIX rows carry the signature at 12y, not two: `rev_cagr`,
  #   `rev_accel`, `insider_net_buys_90d`, `margin_stability`, `asset_growth` (all w 0.249) and
  #   `buyback_yield` (w 0.249, OOS +0.50|+0.01). At 8y six carry it too — those plus `margin_trend`
  #   (w 0.071) and `composite` (w 0.046), with `buyback_yield` at w 0.498. Whatever the tag means it
  #   applies to all of them or to none, and w 0.249 ALONE is not the tell: present factors draw it too
  #   (12y `gross_margin` +225.8, `eps_growth` +232.4, `interest_cover` +180.8, all at w 0.249). The
  #   tell is the PAIR — a weight the search actually picked AND an edge equal to the baseline.
  #   The VERDICT on those two factors is untouched: a factor that cannot move the book is not earning
  #   the tilt either way. Only the stated mechanism changes, and it changes from a result to a
  #   coverage hole. Do not cite "the weight search returned 0" again; cite this.
  #   ALSO RECORDED, because it re-prices the WINNER row: the 12y winner is NOT stable run-to-run on
  #   this lane. It read `ebitda_yield` (+646.5) on the (#165) run and `accrual_gap` (+403.1, w 0.446)
  #   on this one, same file, same command, same 2737 windows scored. The 8y winner reads
  #   `earnings_yield` (-5.8 against a -48.6 baseline, w 0.421). Three different names from three runs.
  #   The caveat below already says TEST edges move between runs; this says the ARGMAX OVER THEM moves
  #   too, which is the stronger claim and the one that matters — the WINNER line names an argmax over
  #   a re-searched, re-sampled table and must never be read as a standing recommendation.
  #   NO n/a ROWS AT EITHER HORIZON: 24 of 24 populate. The widening was expected to expose factors
  #   the point-in-time pool cannot fill, and it exposed none. Coverage is not the limit here.
  #   CAVEAT THAT LIMITS EVERY NUMBER ABOVE — WITHIN-RUN COMPARISONS ONLY. The pre-existing factors'
  #   TEST edges MOVED between the 14-factor run and this one (eps_yoy +338.3 -> +402.5,
  #   earnings_yield +371.3 -> +400.5, gross_margin +192.0 -> +225.8) even though the graded top-10
  #   columns of the two runs were byte-identical (+3.7 med +3.9, worst -6.4, 209 holds). The sweep
  #   re-searches a weight per factor over a fund sample that shifts as the universe cache warms, and
  #   it is not a graded column, so it does not inherit (#156)'s determinism result. COMPARE EACH
  #   FACTOR TO THE price-only BASELINE PRINTED IN ITS OWN RUN, never to a TEST edge from another.
  #   THE SIDAK BAND DID NOT PRINT AT EITHER HORIZON — `bootstrap_edge_ci` returned None — and it did
  #   not print in the 14-factor run either, so the widening did not cause this. Recorded because the
  #   consequence is easy to miss: this file calls that band "exactly ONE multiple-testing
  #   correction", and on the PIT lane it is currently not produced at all, leaving the WINNER row
  #   unguarded there. Do not read a WINNER on ANY lane as if banded.
  #   (#162) 2026-08-30 CORRECTS THE SENTENCE THAT STOOD HERE. It read "Fixing that is its own round",
  #   which files this as a PIT-LANE DEFECT awaiting repair. It is not one, and the round it invites
  #   would be wasted. The None is (#119)'s guard at src/commands/backtest.rs:3860 firing exactly as
  #   designed:
  #       if block > 1 && keys.len() < MIN_BLOCKS * block { return None; }
  #   and that guard's own comment already states the consequence in full: "On this data no horizon
  #   clears it - 8y would need 160 buckets (80 years of cutoffs), 12y 240, 20y 400 - so the band
  #   refuses everywhere, which is the finding ... the statistically correct value is larger than the
  #   data." So the band refuses on EVERY lane at EVERY horizon, wide and stress included; `pit` is
  #   simply where (#157) happened to notice. Nothing about the point-in-time pool causes it, and no
  #   amount of pool would cure it: 20y would need 400 half-year buckets, i.e. 200 years of cutoffs.
  #   THE TWO AVAILABLE "FIXES" ARE BOTH THE OVERFITTING SHAPE THIS FILE BANS. Shrinking MIN_BLOCKS
  #   weakens the one multiple-testing correction the sweep has, which exists precisely to stop a
  #   best-of-N winner shipping on selection luck. Forcing `block == 1` is worse and is already
  #   measured: (#89) records that a one-bucket block "reproduces the i.i.d. width almost exactly",
  #   because consecutive cutoffs of the SAME ticker share (2*years-1)/(2*years) of their forward
  #   path - 95.8% at 12y, 97.5% at 20y. A band widened by either route would print a number that
  #   LOOKS like a correction and is not one, which is strictly worse than printing nothing.
  #   WHAT SURVIVES UNCHANGED is the operative warning, and it is load-bearing: DO NOT READ A WINNER
  #   AS IF BANDED, on any lane. The band is UNAVAILABLE, not merely absent here. The honest reading
  #   of a WINNER row remains (#157)'s own: it is an ARGMAX over FUND_FACTORS.len() tried factors, and
  #   the file's standing bar - clear price-only with both OOS halves + at BOTH horizons - is the only
  #   guard actually in force. That bar is what refused all ten factors, so NO VERDICT IN (#157) RESTS
  #   ON THE MISSING BAND; the widening's conclusion stands exactly as written.
  #   NO CODE CHANGE, and that is the deliverable: this entry exists so the next reader finds the
  #   round ALREADY ADJUDICATED instead of spending it. REVERT RULE: none - nothing was changed.
  #   REVERT RULE: shrink `FUND_FACTORS` to restore the looser band; nothing else moves. Goldens are
  #   untouched either way — the fixture runs offline, so `sweep_fund_factor` returns early on its
  #   `< 8 samples with fund` guard and never prints a row.
  #   Only eps_growth clears both halves at both horizons — and it is exactly the factor (i) made
  #   split-safe and (ii) made visible. Its held-book is unremarkable though (fund 5: +16.7%/yr, excess
  #   +4.4, win 50%, OOS -0.3/+9.0, n=269) vs peg_yield +31.9 / +19.5 / 100% — so it is a candidate for
  #   `growth_fund_extra`, not a replacement for the shipped tilt.
  #   MECHANISM SHIPPED INERT: `growth_fund_extra` (a list of {factor, weight, cap}) now lets several
  #   fundamentals tilt at once, each with its OWN cap because the scales are incompatible (peg_yield
  #   ~0-500, net_margin ~0-60). Empty by default -> contributes exactly 0.0 -> every receipt above is
  #   still numerically exact, and `report TPL` reproduced growth_score 14.8 unchanged. Pinned empty in
  #   config.rs::validated_tilt_pin. Before adding a term, read (#3d): quality and peg_yield each pay
  #   ~+85 alone and -100 TOGETHER, and REV-YoY / EPS-YoY / NET% are three more readings of that same
  #   latent. The per-term ablation rows (`fund_extra:<factor>`) exist to catch exactly that.
  #   DEFERRED, evidence recorded so it is not re-derived: `shares_ttm` still carries scale errors into
  #   ev_ebitda_yield's market-cap leg. Cross-checking shares against net_income/eps flags 105 rows /
  #   35 tickers (24 of them 2020+); 50x/100x/200x thresholds select the IDENTICAL 105 rows, so it is
  #   not a tuning knob. Not fixed because the check says the PAIR disagrees, not which side is wrong:
  #   MCD tags shares in millions EVERY year (716 for FY2025) — self-consistent, so no ratio test ever
  #   fires and its market-cap leg is 10^6x light; HAL's shares (883-908M) are CORRECT and its EPS is
  #   the mis-tagged side. Nulling on disagreement would break HAL's four good rows. Same-filing
  #   comparatives neutralise these rows for every RATIO column regardless.
  # (#3f) HORIZON COVERAGE — measured 2026-07-26, A/B on the SAME day and universe. Found from one row:
  #   JEDG.L (VanEck Space Innovators) printed YRS 4 and a filled 5Y +234.5%. A 4-year-old fund has no
  #   5-year return. Tell: its 5Y sat BELOW its own 2Y (+258.3%) — both anchors were in the same era.
  #   MECHANISM. `horizon_changes` anchors each leg via
  #     asof_avg(dates, closes, target, half).or_else(|| asof(dates, closes, target))
  #   `asof` is correct ("None if before history"), but `asof_avg` runs FIRST and averages every close
  #   in [target-half, target+half]. A series that merely STARTS inside that window still returns Some,
  #   so the correct None never happens and the leg is built from the name's earliest bars wearing a
  #   label they never earned. A leg of D days with window +/-H is fabricable for ANY history length in
  #   [D-H, D) — and when H >= D (1W, 1M at their defaults) for a series of any length at all.
  #   Bands at the LIVE windows (config/settings.yaml sets no `anchor_windows`; the field is
  #   serde(default) -> empty -> core::default_anchor_half. NOTE the block below in THIS file overrides
  #   1Y to 182, so CI's 1Y band is 0.50-1.00y, wider than live's):
  #     6M  +/-90d  -> 0.25-0.50y | 1Y +/-90d -> 0.75-1.00y | 2Y +/-90d -> 1.75-2.00y
  #     5Y  +/-365d -> 4.00-5.00y (JEDG) | 8Y -> 7-8y | 10Y -> 9-10y | 20Y -> 19-20y
  #   NOT display-only, and identical in TRAIN and SERVE — the only two call sites are fetch.rs (live)
  #   and core::backtest_quote, which passes an EMPTY windows map and so always used the defaults.
  #   Consumers of a possibly-fabricated leg: `long_leg`'s 20Y/8Y/5Y CAGR ladder, the 5Y+ gate (written
  #   `is_some_and` precisely to SKIP names with no 5Y record — the fabrication defeated that intent),
  #   `spy_premium` (whose own doc warns a young listing's leg "would flatter it") and `twin_groups`
  #   (whose doc claimed "<5y twins drop via perf None" — that claim was simply false).
  #   FIX: one guard, `first > target + 31d -> None`. 31 and not 0 because history older than the ~10y
  #   daily window is MONTHLY, so an exact test would blank an old name's 20Y leg over bar PLACEMENT
  #   rather than missing data. Residual lie is bounded at 31d: 0.4% of a 20Y leg, 1.7% of a 5Y.
  #   A/B, `backtest universe fund 12`, guard OFF vs ON, same day / same 11717 cutoffs / 4913 tickers:
  #                                   OFF (fabricating)   ON (guarded)    delta
  #     GROWTH windows scored              1488               1338         -150  (10.1% were fabricated)
  #     ON-SALE windows scored             5101               4616         -485  ( 9.5%)
  #     GROWTH edge                      +181.6             +181.3         -0.3  <- the shipped lane is FLAT
  #     GROWTH rho / OOS            +0.18 +0.14|+0.16   +0.18 +0.13|+0.15  flat
  #     GROWTH winsorized edge           +148.1             +150.9         +2.8  <- BETTER
  #     GROWTH bootstrap band     [+111.4 … +242.6]   [+136.3 … +237.3]    tighter
  #     fund-sweep price-only baseline   +202.2             +112.4        -89.8
  #   CONTROL: all 15 standalone fundamental probes are BIT-IDENTICAL across the pair (peg_yield n=1506
  #   rho +0.14 edge +283.2 OOS +0.15|+0.13; eps_growth n=265 +0.09 +16.8; rev_yoy n=1316 +0.08 +60.2;
  #   net_margin n=1824 -0.19 -62.5; op_margin n=1516 -0.23 -326.7). They read fundamentals vs forward
  #   returns and never touch a perf leg, so their invariance proves the universe and the forward
  #   returns did NOT drift — the deltas above are the guard and nothing else. The OFF run also
  #   reproduces (#3e)'s recorded +202.2 / rev_yoy +214.9 exactly, so (#3e) is reproducible too.
  #   THE FINDING THAT MATTERS: the fund sweep's WINNER FLIPS, rev_yoy -> peg_yield.
  #     rev_yoy    +214.9 OOS +0.24|+0.11  ->  +131.8 OOS +0.12|+0.07   (-83.1, OOS roughly halved)
  #     peg_yield  +199.2 OOS +0.24|+0.11  ->  +186.7 OOS +0.15|+0.09   (-12.5)
  #   (#3e) recorded rev_yoy as "the first factor ever to beat the price-only baseline" and declined to
  #   ship it on horizon-dependence alone. That caution was right for a better reason than was known:
  #   the win was substantially an ARTIFACT of fabricated legs. The sweep now names the factor that is
  #   already SHIPPED. The -89.8 baseline drop is the same artifact leaving — the old number was partly
  #   harvesting phantom legs on young names, so it was never a defensible bar to clear.
  #   COST, stated plainly: `long_leg`'s shortest rung is 5Y, so a non-crypto name now needs 5.0y (less
  #   the 31d grace) to be RANKED AT ALL, up from 4.0y — picks.rs bails with `?` when no rung resolves.
  #   JEDG.L therefore leaves the ranking entirely, not just the cell. The sanctioned way to rank a
  #   young fund stays `history_proxy` (curated older twin, spliced and marked `~`, e.g. VUAA.DE), which
  #   genuinely extends the series and is untouched by the guard. Adding a 2Y rung to the ladder would
  #   re-admit these names but is a separate ranking change needing its own receipt — NOT done here.
  #   `walk_forward_edge_pin` moved with this (scored 30 -> 26): backtest_quote slices to [..=as_of], so
  #   its EARLY cutoffs hold only a year or two of bars and were scored on legs fabricated from the
  #   slice's own first days. The guard can only turn Some into None, so `scored` can only FALL there.
  # (#3g) "CAN WE REWARD A GREATER CAGR MORE?" — asked 2026-07-26. Answer, measured: NO on both levers.
  #   First, the question could not be read off the table at all, because the table's CAGR is not the
  #   CAGR the rank uses. `cagr` prints `quote.life_cagr` (whole-life, listing-to-date); the growth score
  #   runs on `long_cagr` — the CAGR of `long_leg_fixed`'s 20Y/8Y/5Y rung, then CAPPED at long_trend_cap.
  #   CF Industries (rank #1): column +20% over 21y, scored 14.28%/yr (the 20Y leg, +1344% cumulative)
  #   -> 0.35 × 14.28 = 5.00 pts, ~16% of its 30.81 base. `life_cagr` is not unused in scoring, but it
  #   appears ONLY as a negative guard (the value-trap dock when it is <= 0), never as a positive reward.
  #   FIXED by adding the `leg` COLUMN = min(long_cagr, long_trend_cap), i.e. exactly what `trend_term`
  #   multiplies, sourced from the score's own `long_cagr_from` rather than re-derived. Shown CAPPED on
  #   purpose: a row reading LEG +30% against CAGR +45% is the ceiling binding, which IS the answer below,
  #   made visible per row. Four "20/10/5" ladder comments were stale too (it has been 20/8/5 since the
  #   8Y-rung commit) and are corrected; `yrs` prints listing AGE, which also PICKS the rung.
  #   MEASURED with a new `weight_curve` (backtest.rs), swept on `universe fund 12` — n=1338 growth
  #   windows, the same batch behind (#3e)/(#3f), reproducing their headline exactly (rho +0.18, edge
  #   +181.3, winsor +150.9). Rows are held fixed at the shipped tuning and re-scored, like the ablation;
  #   sound because NEITHER swept knob touches a gate (the growth_min_cagr floor reads the UNCAPPED
  #   long_cagr, so the cap cannot change who is admitted). Two exact tie-backs are printed as a
  #   self-check and both held: the SHIPPED row reproduces the lane headline, and the weight-0 row
  #   reproduces the ablation's growth_trend_weight line (+0.15 / +119.9).
  #     growth_trend_weight   0.00 +119.9 [off] | 0.15 +184.6 | 0.25 +185.9 | 0.35 +181.3 [SHIPPED]
  #                           0.45 +183.3 | 0.55 +153.8 | 0.70 +134.7 | 1.00 +125.1   (winsor tracks all)
  #     long_trend_cap        15 +175.6 | 20 +185.1 | 25 +191.1 | 30 +181.3 [SHIPPED] | 40 +177.3
  #                           50 +168.1 | 60 +168.1
  #   VERDICT. The stale rationale at config.rs ("raw long-CAGR is mildly HARMFUL, Δ+0.03/+0.10") is
  #   REFUTED — zeroing the term costs -61.4 edge, it is load-bearing. But the conclusion it produced
  #   (keep the weight low) survives for a different reason: 0.15..0.45 is a FLAT PLATEAU whose entire
  #   spread (+181.3..+185.9) sits far inside the [+133.9 … +236.5] bootstrap band, and above 0.45 the
  #   edge cliffs. The shipped 0.35 already sits in that plateau, so there is no honest room to raise it.
  #   The old "0.35->0.55 cut edge +1.9->-0.6" note came from a ±2-edge sample; its DIRECTION now
  #   reconfirms at ±180 scale (+181.3 -> +153.8).
  #   The cap says the same thing harder: it wants to come DOWN, not up. Monotone decline above 25, and
  #   the +9.8 hump at 25 is both inside the band and only half-measured (the cap is SHARED with the
  #   on-sale lane), so it is recorded, not shipped. Both levers agree — the fastest compounders mean
  #   revert, and the ceiling is protective. NOTHING SHIPPED HERE: 0.35 and 30.0 both unmoved, this pass
  #   changed a column and two rationales only. SUPERSEDED IN PART by (#3h) below, which removed the cap
  #   anyway on direction — including this receipt's "half-measured, so recorded not shipped" caveat.
  #   NOT RE-RUN, and worth knowing: the default `backtest universe` (5y holdout, 10y daily) is USELESS
  #   for this question — its 238 growth windows all land in a ~3-week slice of July 2021 (10y back plus
  #   5y forward leaves only that sliver), so its era table repeats one date and its OOS split splits
  #   within a day. It reported the curve as monotone-decreasing from 0. Use `12` (>=8 switches to the
  #   MAX-monthly series) for anything read as a horizon result.
  # (#3h) LEG CLAMP REMOVED — 2026-07-26, BY DIRECTION, AGAINST THE CURVE. long_trend_cap 30 -> 0 (off).
  #   The golden rule would revert this. It is shipped anyway because it was asked for twice, and it is
  #   recorded as a conflict rather than quietly softened. Re-measured on the SAME batch as (#3g)
  #   (`backtest universe fund 12`, n=1338):
  #     BEFORE (cap 30)  rho +0.18  edge +181.3  winsor +150.9
  #     AFTER  (cap 0)   rho +0.16  edge +169.8  winsor +139.8   -> COST -11.5 edge, -11.1 winsor, -0.02 rho
  #   Predicted -13.2 before running (on the reasoning that 50 and 60 read an identical +168.1, so nothing
  #   in the sample tops a 50%/yr leg and OFF should land there). Prediction HELD: -11.5 actual, and the
  #   +1.7 above the cap-50 row is the handful of names that DO exceed 50%/yr.
  #   The full ladder, with 0 now on it and reproducing (#3g) to +-0.1 across every rung:
  #     long_trend_cap  0 +169.8 [SHIPPED/off] | 15 +175.7 | 20 +185.2 | 25 +191.2 (peak) | 30 +181.3
  #                     40 +177.3 | 50 +168.1 | 60 +168.1
  #   So OFF is -21.4 from the measured optimum (25) and -11.5 from where it sat. HONEST CAVEAT IN BOTH
  #   DIRECTIONS: that whole spread lies inside the 90% bootstrap band ([+121.7 … +214.1] this run), so
  #   the cost is a real point estimate but not separable from noise. The mechanism is not noise, though —
  #   the decline above 25 is monotone and has now reproduced across three independent runs.
  #   0 = OFF is the mechanism, not a deletion: `capped_trend` (picks.rs) guards the zero, because
  #   `long_cagr.min(0.0)` is 0.0 for every positive CAGR and an unguarded 0 would silently ZERO both
  #   trend terms instead of uncapping them. One helper feeds all three readers (growth score, on-sale
  #   foil, `LEG` column) so the printed cell cannot drift from the arithmetic it claims to show.
  #   RESTORE = one line: put any positive number back (30 = the long-time value, 25 = the measured peak).
  #   Also gone: the cap was an ARTIFACT guard as well as a mean-reversion belief (cf. growth_fund_cap's
  #   "+9000% rev-accel artifact"). Uncapped, a bad split or a near-zero anchor price can hand core::cagr
  #   an absurd number and that name takes #1 until someone notices. Checked on the live screen the same
  #   day — highest life CAGR is BNB at +98%/yr, nothing runaway — but that check has to be re-done by eye,
  #   because no gate does it now.
  # (#3i) "WHY DOES SCREEN SHOW ROWS UNDER growth_min_cagr?" — asked 2026-07-26. It didn't. The COLUMN
  #   and the GATE read different numbers: `cagr` prints life_cagr (whole-life, listing->today), the gate
  #   reads long_cagr (the 20/8/5Y rung). So a 46y name is admitted on its LAST 20 YEARS and the first 26
  #   never touch a gate — nothing stopped `life +2%/yr / leg +20%/yr`. The only whole-life check in
  #   scoring was the value-trap dock at life_cagr <= 0. (Two other honest causes of a low `cagr` cell,
  #   unchanged: `*` PINNED rows bypass every gate via the f64::MIN_POSITIVE sentinel in `growth_scorer`,
  #   and crypto uses growth_min_cagr_crypto: 0.0.)
  #   SHIPPED: growth_min_cagr now bars BOTH — the leg AND life_cagr, same number, one field.
  #
  #   *** RETRACTED THE SAME DAY BY (#3j). READ THIS BLOCK AS AN ERROR, NOT AS GUIDANCE. ***
  #   What it said: "THIS IS UNMEASURABLE BY CONSTRUCTION, and that is a design property, not a gap.
  #   life_cagr is None in every backtest (backtest_quote builds from a point-in-time SLICE, which has no
  #   whole-life history), so the new leg CANNOT FIRE there... NO BACKTEST NUMBER CAN EVER CONFIRM THIS."
  #   The premise is FALSE. `backtest_quote` slices `[..=as_of]` — from the FIRST BAR OF THE FULL SERIES,
  #   not from some later window — so the whole-life history was present at every cutoff and the field was
  #   simply never filled. `trend_cagr` had been derived from that identical slice for two receipts.
  #   "Unmeasurable by construction" was a property of ONE unwritten assignment, dressed up as a law.
  #   (#3j) fills it, and the gate is now measured like everything else. Two lessons kept on purpose:
  #     - an inert knob and a sound knob BOTH report "no change"; never accept the second reading without
  #       proving the first is false. Here the proof is a test asserting backtest_quote fills the field.
  #     - "X is unmeasurable" is a claim about the code, so it needs the same evidence as any other. This
  #       one was asserted from a field doc rather than from the function, and the function disagreed.
  #   What SURVIVES from the block: `is_some_and` means the leg only ever REJECTS, never admits, so the
  #   live pool stays a strict subset of the leg-only pool — divergence in the safe direction. And the
  #   12->14 receipt above does measure the long leg alone, because it predates the fill.
  #   MEASURED where it was visible AT THE TIME — the live screen, same day, same universe. 5 rows cut,
  #   tables still
  #   full at 20/20/2 (backfilled from below the cut: SEMI.AS, XUTC.L, FLXK.L):
  #     FSLR +10% (was stock #4) | IAUP.L +3% | ETLX.DE +7% | ITWN.MI +11% | GDX.L +13.4%
  #   Four of the five are the GOLD-MINER pattern the bar was built for — crashed 2011-15, ran since, so a
  #   great 8Y leg sits on a mediocre life (IAUP.L: +3%/yr since listing vs a +16%/yr leg). GDXJ.L clears
  #   both bars and stayed. The three rows displaying "+14%" (TJX, TPL, URI) all survived, so their true
  #   life_cagr is >= 14.0 — the column rounds, the gate does not.
  #   gate_review gained a `cagr-life` leg beside `cagr` (same 1.5pp near-miss margin), so a rejection
  #   prints a reason instead of vanishing: "GDX.L … cagr-life 13.4%/yr since listing (need >=14.0%)".
  #   Label is deliberately NOT `lifetime` — that belongs to the <= 0 dock, and a footer that can't tell
  #   "mediocre" from "negative" is the confusion this receipt exists to end.
  #   NOTE: life_cagr comes from the MERGED series (fetch.rs:510), so a `~` history_proxy row is barred on
  #   its TWIN's whole life, not its own. Correct for the strategy thesis, surprising if unstated.
  # (#3j) "MAKE growth_min_cagr THRESHOLD THE CAGR INSTEAD OF THE LEG. CAN THE HEURISTIC USE CAGR TOO?
  #   WHY WOULD WE WANT LEG AT ALL?" — asked 2026-07-26 after `growth_min_cagr: 17` dropped Alphabet,
  #   whose `cagr` column reads +23%. DIAGNOSIS FIRST: GOOGL is 22y old, so the 20/8/5 ladder hands it the
  #   20Y rung — +1924% = 16.23%/yr — while its 8Y (21.4%/yr) and 5Y (19.4%/yr) legs BOTH clear 17. The two
  #   excluded years are 2004-06, +370% on their own. It was cut for being OLD ENOUGH to earn the long
  #   rung: a 19y-old peer is judged on 8 years against the same floor. The age cliff is real and it is the
  #   honest half of the complaint.
  #   ANSWER, MEASURED: keep the leg. Both alternatives lose. Same code, same day, same universe,
  #   `backtest universe fund 12`, the operator's live config (growth_min_cagr 16, tax_keep_eu 0.777):
  #
  #     run                        scored   rho    edge     winsor   OOS early|late
  #     A  leg (SHIPPED)              860  +0.20  +167.6   +139.5   +0.15 | +0.18
  #     B  use_life_cagr: true       1290  +0.17  +125.6   +108.7   +0.12 | +0.12    edge -42.0
  #     C  fixed_cagr_years: 10       830  +0.19  +160.1   +133.5   +0.12 | +0.19    edge  -7.5
  #
  #   B fails on every metric and on BOTH OOS halves. Its ABLATION says why, and it is worse than the
  #   headline: zeroing growth_trend_weight costs -37.6 edge under A (load-bearing) but GAINS +3.8 under B.
  #   Ranking on whole-life CAGR does not weaken that term, it EMPTIES it — the score does marginally better
  #   ignoring the number altogether. The mechanism is visible in the pool: 860 -> 1290 windows admitted,
  #   because a name's whole life carries its early explosive years, so nearly everything clears a 16%/yr
  #   floor. Whole-life CAGR largely encodes HOW LONG AGO A NAME LISTED, not how well it compounds now.
  #   THAT is the answer to "why LEG at all": the ladder BOUNDS THE WINDOW. Life does not bound it at all —
  #   "life" is 5 years for one row of this table and 46 for another, and they are ranked head-to-head.
  #   C — the rival fix, pinning every name to the SAME 10Y window — loses too, by -7.5, and that was NOT
  #   the predicted result (it was expected to win). The drop sits inside A's bootstrap band
  #   [+118.6 … +228.8], so it is noise-level rather than a refutation, and C's ERA table is markedly
  #   steadier (194.0/139.7/111.2/122.4 vs A's 148.1/205.3/40.5/74.6 — worst era +111.2 vs +40.5) with
  #   OOS-late +0.19 > A's +0.18. Recorded as the strongest remaining candidate, NOT shipped: the golden
  #   rule reverts on a drop, and "steadier across eras" is a DIFFERENT claim from "more edge" — it needs
  #   its own measurement, not a favourable re-reading of this one.
  #   BOTH KNOBS STAY OFF. `use_life_cagr` is kept in code, inert and documented, so this is not re-proposed.
  #   The age cliff therefore SURVIVES, deliberately, with its price now known: removing it costs edge.
  #
  #   SHIPPED ANYWAY, because none of it is about the ranking basis:
  #     - `core::life_cagr` — ONE definition, called by the live fetch AND by `backtest_quote`. The formula
  #       had been inlined at the fetch site alone while the backtest left the field None.
  #     - `peg` now divides by the score's OWN CAGR (`long_leg_fixed` + `long_cagr_from`). It had been
  #       calling `long_leg` + `core::cagr` by hand, so it silently ignored fixed_cagr_years AND
  #       use_trend_cagr — the last display site re-deriving the score's arithmetic, which is the exact
  #       defect the `LEG` column was added to end.
  #     - the (#3i) retraction above.
  #   CONSEQUENCE, stated because it is a real behaviour change: filling life_cagr turns the (#3i) whole-life
  #   bar ON inside the backtest, where it had never run. `walk_forward_edge_pin` moved (scored 26 -> 24,
  #   the only direction that bar can move it) and run A's +167.6 is measured WITH it live. What is STILL
  #   not isolated is that bar's own cost: no knob separates the two legs of one `growth_min_cagr` field,
  #   so pricing it needs one. Named here rather than left implicit — (#3i) shipped a gate that called
  #   itself unmeasurable, and half-measuring it now would be the same mistake in a quieter voice.
  # (#37)/(#38) "THRESHOLD ASSETS WITH PEG > 2 AND NET% < 10" — asked 2026-07-26. Answer splits: the PEG
  #   ceiling SHIPS (+17.3 edge), the margin floor is REJECTED at every value tested. Two knobs, not one
  #   AND-ed pair, precisely so they could be priced apart — and they had to be, because together they are
  #   WORSE than the ceiling alone.
  #   THERE IS NOW ONE PEG (2026-07-27). There used to be two, and the history matters because every
  #   number in the table below was measured against the OLD one:
  #     was: printed `peg` cell = pe_ratio / long_cagr_from  |  gated `fund.peg_yield` = ey x TREND cagr
  #     now: BOTH are `fund.peg_yield` = ey x `picks::long_cagr_pct` — the score's own CAGR, so the gate
  #          follows use_life_cagr / use_trend_cagr / fixed_cagr_years with the rest of the lane.
  #   peg_yield == 100 <=> PEG == 1, so the knob stores PEG and applies `peg_yield < 100/growth_max_peg`.
  #   WHY IT CHANGED — this file used to say "do not file it as a bug", and that call was overruled on
  #   2026-07-27. `long_cagr_from` is a THREE-WAY switch (endpoint / trend / life); the column followed
  #   whichever arm the config picked while the gate hardcoded the trend arm, so under the live
  #   `use_life_cagr: true` they were two different numbers wearing one name. Observed cost, one run at
  #   ceiling 2.0: APH cut at PEG 2.02 and ULTA at 2.01, while ODFL (printed 2.51) and JBL (2.10) stayed
  #   RANKED. Rejecting 2.02 and ranking 2.51 in the same pass is not a subtlety to document, it is a
  #   defect. Gating on `pe_ratio` was and remains rejected — it is None in every backtest, and a gate no
  #   run can price is a gate no run can defend — so the COLUMN moved to the gate's number, not the
  #   reverse. Nothing may re-derive a PEG: route through `picks::long_cagr_pct`.
  #   NUMERATOR, still two and deliberately so: PEG uses the ANNUAL 10-K eps_ttm (the only basis the as-of
  #   path can rebuild — fetch.rs fences the TTM roll to `earnings_yield` for that reason) while the `P/E`
  #   COLUMN prefers the fresher TTM roll. So P/E ÷ CAGR is CLOSE to the printed PEG but not equal, widest
  #   for mid-ramp growers. Feeding TTM into peg_yield would manufacture train-serve skew; dragging the P/E
  #   column back to annual would print 2319 where the truth is ~150. Two columns, two jobs, both true.
  #   THE NET% TRAP IS UNCHANGED AND STILL LIVE — it was never the same defect, only the same paragraph:
  #     printed NET% = net_margin_fy (live only)   |  gated `fund.net_margin` (as-of joined, both paths)
  #   MEASURED AGAINST THE OLD DEFINITION (peg_yield = earnings_yield x TREND cagr). Every row below, and
  #   the sweep under it, priced a PEG the tool no longer computes — life CAGR runs BELOW trend CAGR, so
  #   every PEG got bigger and every ceiling got stricter. RE-RUN before quoting any of it as current.
  #   `backtest universe fund 12`, one frozen base (use_life_cagr true, growth_min_cagr 15) —
  #   frozen on purpose, the live file moved three times mid-run and a moving base makes a table of lies:
  #
  #     run                            scored   rho    edge     winsor   OOS early|late   worst era
  #     A  both off                      1407  +0.17  +122.4   +107.7   +0.13 | +0.10      +44.0
  #     B  growth_max_peg 2.0            1378  +0.17  +139.7   +120.4   +0.12 | +0.12      +63.0   SHIPPED
  #     C  growth_min_net_margin 10      1303  +0.16  +125.9   +109.5   +0.12 | +0.12      +26.4
  #     D  both                          1294  +0.16  +125.0   +108.2   +0.12 | +0.12       -7.7
  #
  #   D is the reason these are two knobs. The floor CANCELS the ceiling's entire +17.3 and drives the most
  #   recent era (2008-11..2014-07, n=324) NEGATIVE — the only negative era in any run here. `peg_yield`
  #   terms interacting non-additively is already on the record at backtest.rs:446 ("+85 alone, -100
  #   together"); this is the second instance, so stop assuming this factor composes.
  #   SWEEP, growth_max_peg [OLD DEFINITION] — the point is the SHAPE, not the winner:
  #     0 off +122.4 | 1.5 +148.9 | 2.0 +139.7 | 2.5 +148.1 | 3.0 +135.9 | 4.0 +145.4
  #   Read as two clusters: off = +122, ANY ceiling = +136..+149. Off-to-on (+13.5..+26.5) dwarfs the
  #   spread among ceilings (13 pts, non-monotonic — 2.0 and 3.0 dip, 1.5/2.5/4.0 sit high), and every
  #   value lifts rho, edge, winsorized edge, BOTH OOS halves and the worst era at once. So the gate is
  #   real and the threshold is NOT resolvable at this n. 2.0 ships because it is what was asked for and
  #   sits inside the plateau; taking 2.5 for its extra +8.4 would be fitting the noise, and the 13-pt
  #   spread IS the noise floor — quote it before claiming any future PEG threshold beat another.
  #   Note what this sweep never tested: anything below 1.5. A live config asking for 1.0 is off the
  #   bottom of the measured range, and under the new definition it is stricter still.
  #   SWEEP, growth_max_peg [NEW DEFINITION, 2026-07-27] — seven `backtest universe fund 12` runs on
  #   ONE frozen base, but NOT the base above: use_life_cagr true, growth_min_cagr 16 (was 15) and
  #   growth_min_1y_pct -1e9 (the 1Y floor switched off live after the #41 work). So the absolute
  #   points here are NOT comparable to run B's +139.7 — only the SHAPE within this table is.
  #
  #     ceiling  scored   edge     winsor   OOS e|l        90% band on edge     worst era   ->off n / fwd
  #     off      1665    +137.4   +111.7   +0.11 | +0.10   [+79.4 … +172.2]      +37.7      —  (not binding)
  #     1.0      1562    +143.0   +115.3   +0.11 | +0.11   [+97.9 … +189.3]      +60.9      103 / +72.5   LIVE
  #     1.25     1596    +146.4   +117.4   +0.11 | +0.12   [+89.7 … +188.2]      +40.0       69 / +82.2
  #     1.5      1625    +148.5   +119.9   +0.11 | +0.12   [+86.8 … +182.2]       +4.4       40 / +115.7
  #     2.0      1642    +146.5   +118.3   +0.11 | +0.11   [+81.6 … +181.5]      +31.0       23 / +245.9
  #     2.5      1650    +145.0   +117.4   +0.11 | +0.11   [+77.7 … +177.4]      +33.3       15 / +229.6
  #     3.0      1652    +146.4   +118.6   +0.11 | +0.11   [+81.5 … +179.9]      +31.8       13 / +162.2
  #
  #   THE SHAPE SURVIVED THE REDEFINITION, and that is the headline: off is still the floor and still
  #   alone down there (+137.4 vs +143..+148.5), exactly as the old curve read it (off +122.4 vs
  #   +136..+149). Redefining the denominator moved every number and changed no conclusion.
  #   THE THRESHOLD IS LESS RESOLVABLE THAN BEFORE, not more. The spread among ceilings COLLAPSED from
  #   13 pts to 5.5 (+143.0..+148.5) — under the old noise floor this file already warned about, and
  #   the seven 90% bands overlap almost completely. Nobody may claim a winner off this table.
  #   1.5 peaks under BOTH definitions (+148.9 old, +148.5 new). Do not read that as confirmation: the
  #   plateau is 3.5 pts wide across 1.25..3.0 and 1.5's own WORST ERA is +4.4, the weakest cell in the
  #   whole table. A peak whose worst quarter nearly touches zero is not a robust peak.
  #   THE LIVE 1.0 IS THE LOWEST *ON* VALUE (+143.0, 5.5 under peak) AND SIMULTANEOUSLY THE MOST ROBUST
  #   ONE: the highest bootstrap lower bound (+97.9, the only one clearing +90) and the highest worst
  #   era (+60.9, where every other run sits +4..+40). Headline edge and downside both matter and they
  #   disagree, so this sweep does NOT justify moving the knob in either direction. LEFT AT 1.0.
  #   CAVEAT ON THE ERA COLUMN before anyone leans on it: eras are QUARTILES of each run's own scored
  #   set, so the boundaries move with n. 1.0's third era is 2004-11..2008-09; 1.5's is 2005-04..2009-10.
  #   1.0 spreads the GFC across eras 3 and 4 while 1.5 concentrates it in era 3. Some of that +60.9
  #   vs +4.4 is window alignment, not skill. Cross-run era comparison here is soft evidence, no more.
  #   EVERY CEILING READS "TOO TIGHT" ON ITS OWN ->off ROW, and the excluded names' forward return RISES
  #   with the ceiling: +72.5 (n=103) at 1.0 -> +115.7 (n=40) at 1.5 -> +245.9 (n=23) at 2.0. The more
  #   extreme the PEG a run excludes, the better that excluded set did. Two honest readings and this
  #   sweep cannot separate them: either high-PEG names genuinely carry return this gate throws away, or
  #   n=13..40 windows of survivor-heavy extremes is a number too small to mean anything. Note the
  #   direction contradicts the edge column, which prefers HAVING a ceiling — the gate improves the
  #   RANKING among what it keeps while excluding names that then outperform. Both can be true.
  #   WIDER CONTEXT, so nobody reads the PEG row as the main event: at the live config the GATE SWEEP
  #   also shows growth_maxdd_cap ->off at +250.7 (n=71) and growth_min_range_pct -10 at +102.4 (n=123).
  #   PEG is the THIRD-tightest gate, not the first. And the GATE AUDIT has read "gates ADD NOTHING"
  #   (accepted -12.5 vs rejected +1.9, gap -14.5) both before and after this change — -13.6 / -24.9 /
  #   -14.3 on the three preceding runs on record — so it is a standing property of the whole stack,
  #   NOT something the PEG unification caused. Do not cite it as evidence about this knob.
  #   RESOLVED 2026-08-01 — and the answer is that the "gates ADD NOTHING" reading was an ARTIFACT.
  #   Both the audit and the sweep were comparing BARE MEANS of forward peer-relative return; given a
  #   median (backtest.rs `cohort_stats`), the audit's gap FLIPS SIGN at both long horizons: 20y-s mean
  #   -531.5 vs med +76.3, 12y-s mean -23.4 vs med +78.2, 8y-s agreeing positive (+26.0 | +39.4). The
  #   gates SELECT winners at every horizon on the typical name; the negative means came from a rejected
  #   pool that is a mass of losers plus a few enormous survivors (rejected 20y-s: mean +43.6, med
  #   -766.3). The "standing property of the whole stack" was a property of the STATISTIC. Every reading
  #   above that leans on "gates ADD NOTHING" is void; the sentence telling you not to cite it as
  #   evidence about this knob was right for the wrong reason.
  #   THE TWO HONEST READINGS ABOVE ARE NOW SEPARATED, which is what the sweep could not do before. The
  #   `->off` row is mean-only no longer: at 12y-s it reverses (mean +22.9 vs med -131.3) — the "high-PEG
  #   names carry return" story is a tail there. At 8y-s it SURVIVES with both stats positive (mean
  #   +195.3 | med +57.5, n=54) and 20y-s does not bind at all (0 new names). So the surviving claim is
  #   narrow and specific: the 1.6 ceiling may be too tight WITHIN A DECADE, at no cost to the king
  #   horizon, which is the ladder this knob has been owed since "the threshold half must be re-measured"
  #   was written below. NOT the general "every ceiling reads TOO TIGHT" claim — that one died with the
  #   mean. Companion flags that died the same way: growth_maxdd_cap ->off (12y mean +159.3 vs med
  #   -154.2; 8y +129.1 vs -4.9) — its 2026-07-03 receipt was never contradicted after all — and
  #   growth_min_cagr -4 at 12y (mean +9.4 vs med -127.8).
  #   The on/off question is now priced EVERY run by the `growth_max_peg ->off` GATE SWEEP row
  #   (backtest.rs), so the part this sweep found decisive no longer needs six hand-edited configs.
  #   SWEEP, growth_min_net_margin — a clean monotone loser once you look past the headline:
  #     0 off +122.4 (worst era +44.0) | 5 +128.7 (+7.7) | 10 +125.9 (+26.4) | 15 +110.1 (-1.5)
  #   The +6.3/+3.5 bumps at 5 and 10 are bought ENTIRELY from the pre-2000 era while the modern era caves;
  #   15 loses outright on every metric and drops 182 windows. Single-digit net margin is a BUSINESS MODEL
  #   fact for retail, distribution and industrials, not a quality signal — the floor cuts EME (7.5%) and
  #   CASY (4.1%) for their industry. Predicted to fail before the run, and it failed. STAYS 0.
  #   SIDE EFFECT worth more than the headline: three ablations FLIP SIGN between A and B. Zeroing
  #   growth_trend_weight is +8.5 under A but -6.3 under B; growth_fund_weight +4.9 -> -4.5; quality_weight
  #   +3.0 -> -4.5. Under A those terms were dead weight the score did better ignoring. The PEG ceiling
  #   removes the cohort that was scrambling them. A gate that RESTORES information to unrelated terms is a
  #   different (and better) claim than one that merely raises edge.
  #   LOSS-MAKERS: `core::peg_yield` returns None for negative EPS *and* for negative growth alike, so None
  #   is ambiguous. The gate splits it on the one case identifiable from the data — `eps_ttm <= 0` is
  #   rejected, genuinely absent fundamentals still pass. Deliberate departure from the house "None passes"
  #   rule and the only one: letting the cohort with no earnings at all walk through a VALUATION ceiling
  #   would make the gate mean the opposite of its name.
  #   No !crypto guard on either gate, unlike (#24)/(#26): coins and ETFs carry no `quote.fund`, so the
  #   None arms already scope both to equities. Do not add one "for consistency" — it is a second thing to
  #   keep true for no gain.
  #   CAVEAT ON THE BASE: measured with use_life_cagr TRUE, which (#3j) records as a -42.0 loser. The
  #   ceiling was not re-priced on the shipped (leg) basis, so its +17.3 is established on that base only.
  # (#39) "IMPROVE THE RANKS — CF/MPC/ODFL sit above NVDA/ANET" — asked 2026-07-26 off a live table where
  #   ODFL ranked 4th with revenue AND EPS shrinking at P/E 48, while NVDA (rev +65%, eps +67%, net 55.6%)
  #   ranked 10th. Three candidates came out of the backtest's OWN probe tables. Result: one rejected on
  #   MECHANISM despite good numbers, one rejected on ABLATION despite good numbers, one still open.
  #   growth_max_margin_swing — REJECTED, and this is the instructive one. `fund.margin_stability` is
  #   -std(net_margin) and core.rs:1386 sells it as catching the "peak-cycle name (fertilizer, refiner)
  #   that hides the cycle behind a good level". It is edge-POSITIVE at every ceiling tested (3/5/8/12 ->
  #   edge +154.0/+149.6/+147.0/+149.0 vs +139.7 off; rho +0.17 -> +0.19 at ALL of them, the only thing
  #   all session to move rho; both OOS halves up). By the house rule it ships. IT DOES NOT SHIP, because
  #   the live screen shows it cutting the OPPOSITE names from the ones it advertises:
  #     NVDA  net margin 36% -> 56%  (rev +68%/yr)  swing 16.4pp  CUT at ceiling 5 AND 12
  #     ANET  net margin 29% -> 39%  (rev +32%/yr)  swing  5.6pp  CUT at ceiling 5
  #     CF    net margin 14% -> 21%  (rev  +2%/yr)                SURVIVES at 12
  #     MPC   net margin  8% ->  3%  (rev  +3%/yr)                SURVIVES at 12
  #   Standard deviation has NO DIRECTION. A margin CLIMBING 36->56 disperses more than a fertilizer
  #   oscillating 14<->21, so the "cyclical detector" cuts the margin-EXPANDING compounders and keeps the
  #   actual cyclicals. The era table says the same thing from the other side: 2010->2014 collapses
  #   +64.4 -> +14.7..+27.2 at EVERY ceiling, which is where those compounders' returns live.
  #   A gate measuring the right quantity for the wrong reason is worse than no gate: the number would
  #   have shipped it and the receipt would have taught the next reader something false. The knob stays in
  #   code at 0, inert and documented, because it is the substrate for the real fix — dispersion must be
  #   DOWNSIDE-ONLY (std of negative deviations), or paired with `margin_trend` so improvement is not
  #   punished. That is a different gate and needs its own measurement.
  #   growth_fund_extra: interest_cover — REJECTED. Best row in the held-book-by-factor probe (+17.4%/yr,
  #   excess +5.8 TYING the full growth_score, win 100%, worst +1.8 vs -7.8, OOS +5.7/+5.9), so it looked
  #   like the strongest signal on record and costs zero code. Its WITHIN-RUN ablation says otherwise at
  #   every weight (cap 20): removing the term is Δ+0.0 at 0.1, Δ+0.0/+1.2 at 0.25, Δ+5.4 at 0.5. The term
  #   never pays, and at 0.5 it costs. A factor that selects a good BOOK on its own is not the same claim
  #   as a factor that adds information to a score that already reads price, momentum and PEG.
  # (#40) METHODOLOGY, worth more than either result above and it invalidates a class of comparison used
  #   throughout these receipts. `backtest universe` FETCHES THE LIVE UNIVERSE EVERY RUN, and exchange
  #   endpoints fail differently each time ("Euronext ETF list failed 504 / venue list unavailable — using
  #   last-good copy (793 ISINs)" on one run, "Euronext Lisbon failed 500" on the next). The universe is
  #   therefore NOT constant across runs. Same config, same binary, same day, back to back:
  #     interest_cover w=0.25   run 1: scored 1385  rho +0.19  edge +145.9
  #                             run 2: scored 1378  rho +0.18  edge +138.6
  #   ~7 edge points and 0.01 rho of pure run-to-run noise, and it was caught only because a SCORE tilt
  #   cannot legally change `windows scored` — the pool moving 1378 -> 1385 was the tell. Without that
  #   impossible-by-construction check the +145.9 would have shipped as a +6.2 win.
  #   RULE: a cross-run edge delta under ~7 pts is NOT evidence. Two instruments are immune because they
  #   compare inside ONE universe: (a) the ablation lines, (b) the sweeps a single run prints itself
  #   (CORR-CAP, VALUE-GATE, SURVIVAL-GATE, held-book-by-factor, the weight CURVEs). Prefer those. Where a
  #   cross-run comparison is unavoidable — every GATE, since a gate changes the pool by design and cannot
  #   be ablated — require a SWEEP whose values all sit above baseline, not a single pair. (#37) survives
  #   exactly that test: 1.5/2.0/2.5/3.0/4.0 all landed +136..+149 against off's +122.4, five draws with
  #   none falling back. (#38)'s floor straddled baseline and was rejected; consistent.
  growth_fund_cap: 300.0     # (#3) clamp in the factor's OWN pts before weighting. 300 <=> PEG >= 0.33 stays
                             #   orderable; above that a data artifact would run away with the rank. The code
                             #   default stays 30 (earnings_yield's scale) per the house rule that defaults are
                             #   the neutral value and this fixture carries the live one.
  growth_fund_neutral: 0.0   # (#87) UNMEASURED — an unfitted judgement value, shipping at the pre-(#87)
                             #   behaviour, so this line changes nothing. What an ABSENT `growth_fund_factor` is
                             #   worth, in the factor's own pts, before the 0.07 weight and inside the clamp(0,
                             #   growth_fund_cap) above. The third and last of the three fundamental terms to
                             #   get the field, so all three now answer "no data" through one helper
                             #   (`picks::missing_fundamental`) instead of in three places, two directions.
                             #   THIS TERM IS THE EXPENSIVE ONE: weight x cap = 0.07 x 300 = 21 pts, so the
                             #   miss it charges an uncovered row is more than three times `quality`'s 6.0 and
                             #   twice roic's 10. It is also the term (#43) argued should KEEP the hard 0 —
                             #   "the primary's misses are CROSS-CLASS, an ETF or a coin has no PEG, and filling
                             #   it from a filer median would flatter it, so all-or-nothing is right there".
                             #   That argument is correct about coins and ETFs and WRONG about the rest of the
                             #   misses, which are the same within-class coverage gap it fixed for roic: a
                             #   non-US filer has a PEG, we just cannot fetch it. All-or-nothing was the right
                             #   answer only because the knob to separate the two cases did not exist. It does
                             #   now, on the line below, and (#43)'s objection is the reason a neutral must
                             #   never be raised here without it.
                             #   REVERT RULE: already at the revert value. Raising it requires BOTH a census of
                             #   the covered filers' factor values under this same clamp (median, per (#43)) AND
                             #   growth_fund_scope_by_class true. A neutral alone, at this weight, hands every
                             #   coin and every ETF up to 21 points for having no income statement.
  growth_fund_scope_by_class: false # (#87) UNMEASURED — an unfitted judgement value, shipping OFF, which is
                             #   today's behaviour byte-for-byte. The OTHER half of the missing-data policy, and
                             #   the half a neutral fill alone gets backwards.
                             #   A NEUTRAL IS A COVERED-PEER MEDIAN. That is a defensible stand-in for a row
                             #   that COULD have been covered and was not, and it is meaningless for an asset
                             #   with no income statement in existence. Today the fill is chosen by symptom
                             #   (`None`), not by cause, so `growth_fund_extra: roic, weight 0.25, neutral 17.3`
                             #   below pays every ETF, every coin and every non-SEC stock 0.25 x 17.3 = 4.325
                             #   points for having no fundamentals — against a `growth_trend_weight` term whose
                             #   ENTIRE discriminating span above the growth_min_cagr floor is 0.15 x (30 - 19)
                             #   = 1.65 points, and against a measured filer with a genuine 5% ROIC who banks
                             #   1.25. Coverage outranks merit by ~3 points inside the term that exists to
                             #   measure merit. (#59) already recorded the constant-in-`base` half of this
                             #   ("NOT backtest-blind ... a constant inside `base` is NOT rank-neutral"); this
                             #   is the cross-class half of the same finding.
                             #   ON = a coin or an ETF takes 0.0 from ALL THREE fundamental terms (nothing to be
                             #   missing), while a STOCK we merely failed to fetch still takes the configured
                             #   neutral. Split by cause, which is what the fills were always documented to be.
                             #   NAME: it reads `growth_*` because it sits in this block, but it governs
                             #   `quality_neutral` in the on-sale block too — one policy, three terms. Renaming
                             #   it costs a settings break for no behaviour, so the scope is documented instead.
                             #   WHY NO NUMBER. There is nothing to sweep: it is a boolean, and at the values
                             #   this file ships (both neutrals 0.0) it is INERT — with nothing to fill, there
                             #   is nothing to withhold from a coin. It exists so that the day a census sets a
                             #   neutral, the cross-class leak is already closed rather than shipped alongside.
                             #   REVERT RULE: it is at the revert value. Turn it on ONLY in the same change that
                             #   raises a neutral above 0.0, and check it on a live `screen` by watching the
                             #   CRYPTO and ETF tables, not the stock one — those are the only rows it moves.
  # (#43) ROIC — the FIRST non-empty `growth_fund_extra` entry, so the mechanism stops being inert here.
  #   WHAT IT IS: pre-tax EBIT / (equity + net debt), derived in core::roic_return from levels already
  #   cached (no refetch, no cache-key bump). Equity is reconstructed as NI / ROE, which is signed right
  #   by construction — a loss-maker has NI<0 AND ROE<0 so equity comes out positive, and genuine
  #   negative equity is where those signs DISAGREE.
  #   WHY, when `quality` already scores return-on-capital: ROE divides by equity ALONE. Over a 20-year
  #   hold a business compounds at what it earns on ALL capital, borrowed included, and quality_cap 40
  #   means everything above 40% ROE already scores identically — so the discriminating band is exactly
  #   where the leverage gap lives.
  #   ABLATION, three runs, weights 0.1/0.25/0.5 at cap 40. THE DECISIVE INSTRUMENT (within-run, so #40
  #   immune) and all three scored 1098 windows — an IDENTICAL pool, which is the (#40) tell coming back
  #   clean, so even the cross-run read is legal this time:
  #        weight   fund_extra:roic      quality_weight (SHIPPED, 0.15)
  #        0.1      edge Δ-2.8           Δ-1.9
  #        0.25     edge Δ-6.2   <- peak Δ-5.6
  #        0.5      edge Δ-2.3           Δ+4.5   <- the ROE term COSTS here; roic still pays
  #   Negative Δ = removing the term costs edge = the term pays. It pays at EVERY weight, peaks at 0.25,
  #   and beats the shipped ROE-based term at all three. Contrast the rejected `interest_cover` directly
  #   above: Δ+0.0 / +0.0 / +5.4 — never paid, and cost at 0.5. Opposite result on the same instrument.
  #   HELD-BOOK (same runs, identical n=178 across all three): book +15.4%/yr, excess +3.9, win 100%,
  #   worst +0.1, OOS +3.3/+4.3 — beats quality(roe/roa) (+15.1/+3.5, OOS +2.7/+4.1) and roe_raw
  #   (+15.1/+3.4, OOS +2.5/+4.1) on every column, within-run. It does NOT beat growth_score's +6.5
  #   excess, so it is not a better standalone book SELECTOR; the ablation says it ADDS information to a
  #   score that already reads price, momentum and PEG, which is the different and stronger claim — and
  #   the exact claim interest_cover failed while topping this very table.
  #   MECHANISM (live screen, roic on vs off, the check that rejected growth_max_margin_swing). Promotes
  #   AZO 10->3 and ORLY 12->7: buybacks drove BOTH to negative equity, so ROE is garbage (-73%, -332%),
  #   quality_return falls back to ROA (12.9%, 15.3%) and ROA understates them 4-5x. ROIC reads 70.6% and
  #   68.4%. MA 4->1 (116.9%, capped). GOOGL 6->9 at an honest 29.8%, passed by names that gained more.
  #   The direction is right everywhere the factor applies — the opposite of margin_swing, which cut the
  #   names it advertised protecting.
  #   THE COVERAGE ASYMMETRY — recorded here as "accepted", now FIXED by `neutral` below. 114 of 509
  #   cached filers (22.4% — banks, insurers, REITs, and also CVX/COP/DE/ADM/BMY/BIIB/HCA) file no
  #   operating margin, so roic is None, contributed 0, and in a RANKING a 0 is a relative demotion of up
  #   to weight x cap = 10 pts, not neutrality. PGR fell 1->10 and HCA 5->13 on ABSENT DATA, not on
  #   leverage. Re-censused on the `_facts10` cache: `op_margin` is the ONLY missing leg (77.6%) — revenue
  #   and net_margin and roe and roa all read 100%, net_debt 99.6% — so `core::roic_return`'s ROA fallback
  #   branch fires ZERO times, because the bail is `op_margin?` and sits before it.
  #   The old defence ("smaller than the primary tilt's 0.07 x cap 300 = 21 pts, identical shape") compared
  #   unlike things and is withdrawn. The primary's misses are CROSS-CLASS — an ETF or a coin has no PEG,
  #   and filling it from a filer median would flatter it — so all-or-nothing is right there. This term's
  #   misses are WITHIN-class: US filers, same lane, same peers, one absent line item. That is a data gap,
  #   and a data gap must not be scored as the worst possible business.
  #   NO credibility multiplier like MAX_EQUITY_MULT: only 6 names clear 150%, and a bar cutting MCK's
  #   artifact (1,639% on invested capital = 0.5% of assets) would take DECK (213% on 16%) with it, which
  #   is genuinely capital-light. The term's own `cap` already clamps both to any excellent business.
  growth_fund_extra:
    - factor: roic
      weight: 0.25           # the ablation peak above. 0.1 and 0.5 both still pay; 0.25 pays most
      cap: 40.0              # matches quality_cap — the band where return-on-capital still discriminates
      # (N) what a MISSING roic scores, instead of the old hard 0. 17.3 is the MEDIAN roic of the 395
      # covered filers under this same clamp(0,40) (mean 20.2; 19% clamp at the cap; 24 read negative).
      # "Unknown" therefore ranks as TYPICAL rather than as worst, while a KNOWN-BAD roic still floors at
      # 0 and ranks BELOW an unknown one — the clamp is outside the fill precisely to keep that ordering.
      # roic builds NO book on the as-of path (the held-book loop prints `roic n=0 rows` instead of
      # silently skipping it), so there is no OOS split to read and the census of today's filers is the
      # only honest basis. If a later run gives roic as-of coverage, sweep this value the way (#43) swept
      # the weight.
      # (#87) "covered filers" is the load-bearing word and this fill does not check it. A coin and an
      # ETF have no filer at all, so the 4.325 they collect here is a stock median handed to something
      # that cannot have the number — see `growth_fund_scope_by_class` above, which is the switch that
      # withholds it. It ships OFF, so this line still pays every class today.
      # (#59) NOT "BACKTEST-BLIND", which is what this block used to call it, filed alongside
      # growth_min_age_years / growth_min_aum_etf / growth_turnover_weight. Those three are inert. This
      # one is not, and the distinction is the DATA is absent while the FILL is live: `quote.fund` is None
      # on every offline row, so `unwrap_or(neutral)` fires every time and the term adds a hard constant
      # 0.25 × 17.3 = 4.325 to `base` for the whole sample. A constant inside `base` is NOT rank-neutral,
      # because `base` is then multiplied by trust × overext × proximity × value — it reaches the score as
      # 4.325 × multiplier and so ranks by the multiplier stack. The `fund_extra:roic` ablation row has
      # been measuring that fill, never roic.
      # PROVEN, not argued: the `null: base −4.3 (calibration)` row removes the SAME 4.325 through a
      # factor name that matches nothing, and the two rows print identical rho and identical Δedge at all
      # four horizons — Δ-72.2 (12y, stress), Δ-261.8 (20y), Δ-56.1 (8y). Identical to the tenth of a
      # point is what "carries no information" looks like.
      # WHAT THAT COSTS THE TABLE ABOVE IT, and this is the uncomfortable part: those Δs are the NOISE
      # FLOOR, and most of the growth ablation sits under it. At 20y `growth_trend_weight` — the lane's
      # headline long-CAGR reward — reads Δ-251.6 against a null of Δ-261.8, i.e. SMALLER than a constant,
      # and `growth_smoothness_weight` reads Δ-261.8, i.e. exactly the null (smooth = 5.0 × trend_r2 ≈
      # 4.33, the same magnitude by arithmetic accident). At 8y NOTHING in the table clears the null. At
      # 12y only `overext_brake` does (Δ-101.1 vs Δ-72.2).
      # READ THAT AS A STATEMENT ABOUT THE FIXTURE, NOT A VERDICT ON THE TERMS. This harness cannot
      # resolve them: n=80 at 12y, a 90% band of [-40.6 … +175.1] that straddles 0, and OOS decaying
      # +0.35 -> +0.06. "Indistinguishable from a constant of its own size HERE" is not "worthless" — it
      # is the fixture declining to answer. A term's real grade needs `backtest universe`.
      neutral: 17.3
  # (1) Overextension brake: how far ABOVE its own 200wk SMA a growth name has run. The P/E tilt
  # can't brake crypto/ETFs (no earnings); this can — it's price-only. Taper the score toward the
  # floor as price stretches above trend, killing blow-off-top chasing.
  growth_overext_cap: 100.0      # % above the 200wk SMA where the brake maxes out. Biting harder (100->60) cut edge +1.9->-9.5: over-penalizes good compounders that keep running above trend. Keep 100
                                 # (#69) RE-GRADED UNDER SHIP RULE v2 ON 2026-08-11 AND HELD AT 100. This closes the question
                                 #   `growth_max_above_ma` opened above ("ONE MECHANISM WORTH THE NEXT ROUND'S ATTENTION ...
                                 #   Read `growth_overext_cap` in that light"): the answer is that the hard gate does NOT
                                 #   substitute for the soft brake, and the ablation collapse that suggested it was measuring
                                 #   the wrong thing.
                                 #   WHY THE ROUND WAS OWED, beyond that: every rung of this knob was graded on rank-1, the
                                 #   primary v2 retired, and the receipt one line above is a pure lane-edge argument. So the
                                 #   knob had never been judged on the slice that now judges it.
                                 #   THE ARM WAS STRUCTURAL, NOT FITTED, and it is worth stating because it was WRONG. The
                                 #   brake is one linear ramp, damp = 1 - (min(above_ma, cap)/cap) x (1 - floor), so at cap 100
                                 #   with the gate at 150 EVERY name in the 100..150 band saturates at floor 0.05: a +101% name
                                 #   and a +149% name score identically, though (#24) keeps that band in the table on purpose.
                                 #   Setting cap = gate would have graded the band instead of flattening it. It measured worse.
                                 #   NINE WIDE RUNS, cap {100,150,200} x `backtest {20,12,8} universe` on 4997 tickers, floor
                                 #   held at 0.05 so exactly one knob moved. Data conditions identical within each horizon
                                 #   (cutoffs 4588 / 11885 / 20791, same window counts 36 / 51 / 59). Ship rule fixed BEFORE
                                 #   the first run. Control is cap 100 run as its own probe, never a bare-config run.
                                 #     horizon  cap   top3 mean  top3 med  worst  rank-1 h2h  win%
                                 #     20y      100       +6.3      +5.7   -0.5      74%       97%   <- control
                                 #     20y      150       +6.3      +5.2   -0.5      74%       97%
                                 #     20y      200       +6.3      +5.0   -0.5      47%       97%
                                 #     12y      100       +6.8      +6.9   -7.8      63%       84%   <- control
                                 #     12y      150       +6.5      +6.3   -7.8      52%       82%
                                 #     12y      200       +6.3      +6.3   -7.8      41%       82%
                                 #      8y      100       +7.7      +8.3   -9.2      60%       81%   <- control
                                 #      8y      150       +7.1      +7.8   -9.2      54%       80%
                                 #      8y      200       +6.7      +7.9   -9.2      40%       80%
                                 #   cap 200 is REFUSED: the rank-1 h2h guard breaks at ALL THREE horizons (47/41/40, under the
                                 #   50% bar), and a broken guard refuses outright rather than softening to unresolved.
                                 #   cap 150 is a plain NO-SHIP, not UNRESOLVED: the primary loses at 20y (median +5.7 -> +5.2)
                                 #   and at 8y (mean +7.7 -> +7.1, median +8.3 -> +7.8), and NO horizon goes up, so there is no
                                 #   real disagreement to be unresolved about. Both guards actually pass for 150 — h2h 74/52/54
                                 #   and worst window identical at every horizon — it simply does not earn the primary.
                                 #   THE MECHANISM, which is the part worth keeping: weakening the cap degrades rank-1 h2h
                                 #   MONOTONICALLY at every horizon (20y 74/74/47, 12y 63/52/41, 8y 60/54/40) while top-3
                                 #   median falls with it. The gate and the brake are NOT redundant — the gate TRUNCATES above
                                 #   150 and the brake ORDERS what remains, and the 100..150 band still needs crushing for the
                                 #   top of the list to hold. The (#64) note read an ablation Δ collapse (-35.5 -> -5.6 at 8y)
                                 #   as evidence of redundancy; it is not, for the reason recorded at the head of this file —
                                 #   an ablation Δ tracks how much MAGNITUDE a term contributes to `base`, largely independent
                                 #   of how much it KNOWS. Ranking authority is what the brake carries, and only a rank metric
                                 #   can see it.
                                 #   AND THE FLOOR'S "ZERO RANK-1 AUTHORITY" DOES NOT GENERALIZE TO THE CAP. That receipt
                                 #   (`growth_overext_floor`) found rank-1 byte-identical at every floor swept. The cap moves
                                 #   h2h by 27pp. They are different levers on one ramp: the floor sets how deep the penalty
                                 #   goes at saturation, the cap sets how much of the admitted range is saturated at all.
                                 #   Do not cite one knob's rank-blindness as the other's.
  growth_max_above_ma: 150.0     # (#24) HARD gate above the brake: reject equities >150% above the 200wk SMA (crypto exempt).
                                 #   Past the brake cap the damp saturates, so a +486% blow-off (WDC 2026-07) scored like a +100%
                                 #   name; the brake can floor it but never remove it. Same-batch triple 2026-07-03:
                                 #   off +106.6 (wins +84.1, OOS +0.11|+0.06) / 100 +106.9 (edge-flat) / 150 +115.9
                                 #   (wins +88.1, OOS +0.13|+0.08) — and the names the 150 gate excludes average -125.1 pts
                                 #   forward vs peers (n=267). NOT the same cohort as the rejected R² gate (round 4: low-R²
                                 #   names near a high BEAT the field +61/+64): an ugly PAST is fine, an extreme PRESENT
                                 #   stretch is a measured loser. 150 > brake cap on purpose — the 100..150 band stays in the
                                 #   table (marked `!`), only the blow-off tail is cut. 0 = off.
                                 # (#64) RESTORED to 150 on 2026-08-10 after seven days switched off. `d58db7a` (2026-08-03)
                                 #   set this to 1000.0 BY HAND, in a commit whose subject is about wiring a 2Y CAGR rung, and
                                 #   said so in its own message: "set by hand and NOT graded by any run above ... two of the
                                 #   three carry same-file receipts arguing the other way". This block was one of the two. The
                                 #   repo's own instrument could not catch it: the GATE SWEEP only ever LOOSENS a knob and asks
                                 #   what the loosening newly admits, so a knob moved in the loosening direction is the one case
                                 #   it structurally cannot flag. Nor did any test — `shipped_config_pin` covered eight knobs and
                                 #   not this one. Both holes are closed in the same commit; the pin is the real fix.
                                 #   NINE wide runs, three configs x `backtest {20,12,8} universe` on 4997 tickers, this knob the
                                 #   only thing changed (the falling-knife arm ran beside it and was refused; see max_1m_drop_pct).
                                 #   Ship rule fixed BEFORE any run, and deliberately the mirror of the sweep's own published bar
                                 #   ("too tight needs mean AND median positive — one survivor can carry a mean on its own"):
                                 #   PRIMARY is the EXCLUDED cohort's forward peer-relative return, which must be negative on BOTH
                                 #   moments to justify excluding it. GUARD is that rank-1 MEDIAN at the 20y king must not FALL —
                                 #   not "must rise", because a gate protects the tail rather than the argmax. Lane edge, rho and
                                 #   OOS are recorded against each arm's own null floor and never vote. A window-count drop is
                                 #   EXPECTED here and is a result, not a discard trigger: a gate admits fewer windows by design.
                                 #   The control reproduced the committed post-(#63) baseline to the digit at all three horizons
                                 #   (20y n=481 edge +414.4 rank-1 +6.6|+7.0; 12y n=668 +146.4 +6.7|+7.8; 8y n=852 +56.9 +7.6|+8.4),
                                 #   so this is one batch rather than drift.
                                 #   PRIMARY, all six numbers negative on real n — 20y n=127 mean -786.0 | med -778.1;
                                 #   12y n=134 -154.6 | -209.7; 8y n=140 -79.3 | -120.7. The 2026-07-03 measurement said -125.1
                                 #   average on n=267; this reproduces the sign and the order of magnitude three separate times on
                                 #   a pool rebuilt by (#61)'s on-sale split, (#62)'s smoothness zeroing and (#63)'s accel cut.
                                 #   GUARD, rank-1 mean|MED: 20y +6.6|+7.0 -> +6.8|+8.0, h2h 76% -> 76%. It rises rather than
                                 #   merely holding. 12y +6.7|+7.8 -> +6.6|+7.7 with h2h 60% -> 66%; 8y +7.6|+8.4 -> +7.6|+8.6
                                 #   with h2h 63% -> 62%.
                                 #   THE COSTS, recorded rather than dropped. Scored windows fall 481->355, 668->535, 852->713.
                                 #   Three of the 39 king windows lose their rank-1 name outright (39 -> 36): 7.7% of the time this
                                 #   gate hands back an empty top slot. The 12y rank-1 median slips -0.1 and its top-3 median drops
                                 #   +7.4 -> +6.9. The 8y lane edge falls +56.9 -> +41.3, and that -15.6 is far ABOVE the 8y null
                                 #   floor of 8.7, so it is a real degradation and not noise; the 8y final era flips +3.5 -> -11.0.
                                 #   Against that, the 20y edge gain (+236.5) sits UNDER its own null floor of 290.6 and therefore
                                 #   does NOT count as a win, and the rho gain (+0.07) only just clears the 0.06 floor. Both OOS
                                 #   split dates moved (20y 1999-12-01 -> 2000-07-01, 8y 2005-04-01 -> 2006-08-01) because the
                                 #   window count changed, so no OOS figure here is strictly like-for-like.
                                 #   ONE MECHANISM WORTH THE NEXT ROUND'S ATTENTION: at 8y the `overext_brake` ablation Δ collapses
                                 #   from -35.5 to -5.6. The hard gate is now doing the work the soft brake was doing — they price
                                 #   the same stretch — so shipping this partly SUBSTITUTES for the one term that clears its null
                                 #   floor on both columns at all three horizons. Read `growth_overext_cap` in that light.
                                 #   LIVE A/B 2026-08-10, both runs against the same restored `.screen_state.json`: every shown row
                                 #   is identical. NVDA holds rank 1 at score 5.6 in both, the ETF lane keeps IITU.L/SMH.L/SEMI.AS/
                                 #   XLKS.L in order, BNB and BTC do not move; the only deltas are price drift between two runs
                                 #   minutes apart. The gate funnel gains one row reading `stretch 20 / 0` stocks, `0 / 0` ETFs,
                                 #   `0 / 0` crypto — equity-only, exactly as the line above claims, which is why SMH.L survives at
                                 #   +183% above its own 200wk SMA. Named rejects: VRT +163%, LRCX +183%, FIX +221%, DELL +306%,
                                 #   LITE +368%, MU +392% — every one of them ALSO fails `peg` today, so on the live pool this gate
                                 #   currently removes nothing that was not already removed. It is bought for the backtested tail,
                                 #   not for anything it does to today's table. (The rank-stability line differs 17% vs 100% between
                                 #   the two runs; that is the documented state-file artifact, not this knob.)
                                 #   THE GOLDENS DIAGNOSE THE CHANGE RATHER THAN ABSORBING IT, and they hand over the single
                                 #   most on-thesis result of the round. Cutoffs are byte-identical in every report (552 / 1186 /
                                 #   1546), as are the ETF counts (0/11, 0/85, 1/184) and crypto (0/0, 0/9) — a third independent
                                 #   confirmation, after the wide runs and the live funnel, that this gate is equity-only. Stock
                                 #   growth-scored drops by EXACTLY 16 in all four full reports (52->36, 80->64, 80->64, 101->85):
                                 #   the same 16 of the fixture's 80 large caps, at every horizon. And the row that vanishes from
                                 #   three of them is the ZERO-NAMES row. `top-10 zero names (20y): AIG@b3997(-94%), NOK@b4000(-92%),
                                 #   C@b4000(-91%), AIG@b4000(-98%)` is gone, `rode 4 zeros/51 holds` becomes `rode 0 zeros/35`, and
                                 #   the 12y and stress reports lose the same four. The gate deletes the pre-2008 blow-offs that
                                 #   later lost 90%+ of their value. That is the exact cohort this block was written for, named.
                                 #   Everything downstream follows: 20y top-1 excess +2.1 -> +3.2 with worst -17.3 -> -3.2 and OOS
                                 #   early +3.5 -> +6.8; 12y top-10 +0.2 -> +0.9; 8y top-10 +4.6 -> +5.2 (median +3.8 -> +6.9); the
                                 #   tune split's held-out TEST rho flips sign, -0.12 -> +0.12, though it still says SHIP NOTHING.
                                 #   The 8y report keeps its two zeros (FRCB, -100% in 2023) — a bank that failed without ever
                                 #   running 150% above its own trend, which is the honest limit of what a stretch gate can catch.
                                 #   ONE FIXTURE COLUMN ARGUES BACK, AND IT IS THE ROUND'S STRONGEST COUNTER-SIGNAL. The half-life
                                 #   table degrades at EVERY hold length, and badly at the long end: 1y +13.7 -> +11.8, 2y +30.2 ->
                                 #   +23.6, 3y +22.0 -> -0.1, 5y +62.2 -> +55.4, 8y -10.3 -> -107.3, 10y -43.3 -> -168.3. Under the
                                 #   rule fixed before the runs it does not vote — 80 hand-picked large caps, and this arm scores
                                 #   only 64 of them — but it is not dismissed either, because it comes with a mechanism that is
                                 #   plausible and untested: a ceiling removes the names that STAY stretched for years, and a long
                                 #   hold is precisely what pays for those. The wide runs rebalance annually and cannot see it.
                                 #   Note the after-tax deferral edge moves the other way at all three horizons (+1.3 -> +1.5,
                                 #   +0.8 -> +1.0, +1.1 -> +1.3), so the two long-hold readings in this repo disagree. THAT is the
                                 #   first thing to test next, ahead of anything else listed here.
                                 #   TO REOPEN: NVDA sits at +147% — THREE POINTS under the fence. When it crosses, the live stock
                                 #   table goes EMPTY, because it is the only row in it. That is the accepted, known cost of this
                                 #   ship and not a surprise to diagnose later; the 7.7% empty-slot rate above is the same fact
                                 #   measured historically. Do not raise the ceiling to keep one name — re-run the nine.
                                 # (#65) THE HALF-LIFE DISSENT ABOVE IS CLOSED, AND IT WAS WRONG. The receipt above named the
                                 #   fixture's long-hold collapse as the first thing to test: 3y +22.0 -> -0.1, 8y -10.3 -> -107.3,
                                 #   10y -43.3 -> -168.3 when this gate went on. Tested 2026-08-10 on 4997 tickers, and it does not
                                 #   reproduce. Four runs, `backtest 12 universe halflife` at {this config, gate off} x {plain,
                                 #   stress}. `wide` and `halflife` have always been independent flags and nobody had ever combined
                                 #   them; `years` reaches that path only through `monthly = long || years >= 8` and HOLDS is a
                                 #   const, so 8/12/20 are byte-identical there and one run per arm is the whole experiment.
                                 #   FIRST, THE INSTRUMENT WAS UNREADABLE AND NOW IS NOT. The sweep printed edge/turnover/net and
                                 #   no floor at all, which under (#60) means none of its numbers could be sized. It now prints the
                                 #   same (#59) null per hold. That mattered more than expected here, because a gate changes the
                                 #   scored POOL, so the two arms are not measuring the same names and each needs its own ruler.
                                 #   net edge, and each arm's own null, per hold:
                                 #     hold    ship edge | null      gate-off edge | null      ship exc   off exc     Δ
                                 #      1y        +4.2 | +2.9            +3.8 | +2.3              +1.3      +1.5    -0.2
                                 #      2y        -0.7 | -2.4            -0.8 | -2.4              +1.7      +1.6    +0.1
                                 #      3y        -3.1 | -4.6            -1.9 | -4.7              +1.5      +2.8    -1.3
                                 #      5y       +28.9 | +21.8          +28.3 | +18.6             +7.1      +9.7    -2.6
                                 #      8y       +41.3 | +26.8          +56.9 | +48.2            +14.5      +8.7    +5.8
                                 #     10y       +69.0 | +58.6          +90.4 | +73.4            +10.4     +17.0    -6.6
                                 #   READ THE RAW GAP AGAINST THE NULL GAP, WHICH IS THE WHOLE POINT OF ADDING THE COLUMN. At 8y
                                 #   the gate's edge is 15.6 below gate-off — but the information-free constant's edge is 21.4
                                 #   below it on the same two pools. The score fell LESS than a constant did, so at 8y the gate is
                                 #   ahead once the pool change is priced out. At 10y the raw gap is 21.4 against a null gap of
                                 #   14.8, leaving a genuine residual of -6.6. Those two residuals have OPPOSITE SIGNS on adjacent
                                 #   hold lengths, and both are an order of magnitude under their own floors (27 to 73). Under
                                 #   (#60) that is unresolved, not a finding. The fixture's version was ~3x its own floor (-97.0
                                 #   and -125.0 against 39.0/41.2), which is why it looked real at n=86 and n=73.
                                 #   THE STRESS ARMS REPLICATE IT TO 0.1 pts (+5.7 at 8y, -6.5 at 10y), so this is not sampling
                                 #   noise in the usual sense — it is a stable feature of two different pools that happens to carry
                                 #   no consistent direction. Survivorship was the hypothesis this round was built to test and it
                                 #   is not the explanation either way: injecting the 27 losers moves every number by under a point.
                                 #   THE FIXTURE HAD THE SIGN OF THE WHOLE PHENOMENON WRONG, which is the part worth remembering.
                                 #   It reported long holds as the WORST and deeply negative (net -107.4 and -168.4). Wide, net edge
                                 #   rises MONOTONICALLY with hold length in all four arms — +4.1/+28.7/+41.1/+68.9 here, and
                                 #   +3.6/+28.2/+56.7/+90.2 with the gate off — and 10y is the best hold by a wide margin, clearing
                                 #   its floor in every arm. The sweep's own header says to pick the highest net edge and prefer the
                                 #   longest on a tie; wide, that answer is unambiguous and it agrees with the after-tax deferral
                                 #   column rather than contradicting it. The two long-hold readings in this repo no longer disagree.
                                 #   Middle holds carry nothing: 2y and 3y sit under floor in all four arms. Turnover is flat at
                                 #   77-81% across every hold and arm, so `net` never differs from `edge` by more than 0.2 and the
                                 #   cost correction decides nothing here.
                                 #   INSTRUMENT CROSS-CHECK, exact and unplanned: this sweep's 8y bucket scores n=713 with the gate
                                 #   on and n=852 with it off — the same two window counts (#64)'s `backtest 8 universe` reported for
                                 #   the stretch arm and the control. Two unrelated code paths, agreeing to the unit on both configs.
                                 #   NOTHING CHANGED THIS ROUND. The rule was fixed before the runs: revert only if the gate is worse
                                 #   at BOTH ends, keep if it trades short holds for long ones, and neither fired — there is no
                                 #   above-floor long-hold gap to trade against. 150 stands on (#64)'s evidence, undisturbed.
  # (#138) 2026-08-25 — THIS GATE NOW SHIPS OFF (0.0). It carried 84.0 from (#26) until then; the
  # receipt for the change is at the END of this block, after (#74). The (#26) head text below is
  # left as originally written and describes the gate as it behaved while it was armed.
  growth_maxdd_cap: 0.0          # (#26) reject equities whose worst-EVER peak-to-trough loss exceeds 83% (magnitude; crypto
                                 #   exempt — BTC itself is -83%). A >83% wipeout survivor is a lottery ticket, not a compounder.
                                 #   Same-batch pair 2026-07-03: off +119.5 (wins +91.5, OOS +0.13|+0.08) / on +128.4 (wins
                                 #   +98.3, OOS +0.14|+0.11 — BOTH halves up); the n=171 names it excludes average -16.5 pts
                                 #   fwd vs peers. Second gate-beats-damp case (after #24): Calmar as a continuous damp was
                                 #   measured harmful, yet the hard tail cut pays — damp-verdicts don't transfer to gates.
                                 #   (#60) FLAGGED "TOO TIGHT" AT TWO HORIZONS AND REFUSED, 2026-08-09, `{20,12,8} universe`
                                 #   on ~4997 tickers. The gate sweep's own flag fires at 12y (n=34, newly-admitted fwd
                                 #   peer-relative mean +125.9 | med +62.3) and at 8y (n=57, +324.2 | +7.0) — both mean AND
                                 #   median positive, which is the sweep's bar for "loosen this". At the 20y king the SAME
                                 #   loosening admits n=10 that run mean -1079.5 | med -983.9. That is not a narrow
                                 #   disagreement to be split, it is a sign flip of ~1000 pts, and it is the horizon this
                                 #   gate exists for: over 20 years a >84% wipeout marks a broken company, over 8 it can
                                 #   mark a recovery. SHIP RULE v2 TIE arm — nothing ships, 84.0 stands. Recorded because
                                 #   the flag WILL fire again on any 12y or 8y sweep and reads like a free win on its own.
                                 #   TO REOPEN: an ETF/equity-split bar, or 20y evidence that the n=10 tail is survivorship
                                 #   rather than signal. Do not act on a short-horizon flag alone.
                                 # (#74) BOTH REOPEN CONDITIONS ARE DISCHARGED, AND THE ETF SPLIT IS DEAD ON MEASUREMENT
                                 #   RATHER THAN ON JUDGEMENT. 2026-08-12, six runs, `backtest {20,12,8} universe fund` x
                                 #   {this config, cap off}. `fund` is not optional: `fund_lane_on` gates the as-of fill, so a
                                 #   `stress` run is structurally blind to every fundamental knob and cannot be used to reason
                                 #   about which names reach this gate. One field moved per arm, each run self-verifying via
                                 #   `growth_maxdd_cap 84.0->0.0` and nothing else. Data conditions identical inside every pair
                                 #   (4999 tickers; cutoffs 4588 / 11885 / 20791; as-of coverage 4394 / 8456 / 10956).
                                 #     horizon   ETF scored on->off   stock scored on->off   sweep `growth_maxdd_cap ->off`
                                 #       20y         0/15  ->  0/15      351 -> 357  (+6)      n=6   -1175.9 | -1238.0
                                 #       12y         4/2836 -> 4/2836    511 -> 536 (+25)      n=25   +167.7 |   +63.1  TOO TIGHT
                                 #        8y        17/8971 -> 17/8971   637 -> 678 (+41)      n=41    +82.4 |   -44.3  SPLIT
                                 #   THE ETF COLUMN DOES NOT MOVE BY ONE NAME AT ANY HORIZON. `gate_sweep` loosens exactly one
                                 #   gate off the shipped tuning (`newly_admitted_stats`, backtest.rs), so its n IS the sole-block
                                 #   count for this knob — and that n equals the STOCK delta to the unit at all three horizons
                                 #   (6, 25, 41). Two independent readings of the same quantity, agreeing exactly, the way (#65)'s
                                 #   n=713/852 cross-check did. Conclusion: this cap sole-blocks ZERO ETFs. A `growth_maxdd_cap_etf`
                                 #   sibling in the `sharpe_cap_etf` shape would be INERT at every horizon that has any ETF coverage
                                 #   at all, and 20y has none to begin with (0 of 15). There is nothing for it to admit. Do not
                                 #   propose that field again; it is not unproven, it is measured empty.
                                 #   MECHANISM, PLAUSIBLE BUT NOT MEASURED: every ETF that fails this cap must also fail an earlier
                                 #   gate, and `growth_min_cagr` 19 is the obvious candidate since no broad fund clears it. NOT
                                 #   established here — the runs prove the sole-block count is zero, not which gate takes the block.
                                 #   THE SECOND CLAUSE FAILS IN THE OPPOSITE DIRECTION TO THE ONE IT SUSPECTED. The 20y tail is not
                                 #   a survivorship-inflated positive to be discounted; it is n=6 at -1175.9 | -1238.0, and it is
                                 #   the MOST-correct gate on the whole 20y board — every gate reads negative there (range -164.8,
                                 #   cagr -466.8, 1y -689.0, above_ma -772.2, leg_years -846.7) and this one is worse than the next
                                 #   by over 300 pts. Magnitudes are NOT comparable across horizons (20y peer-relative returns
                                 #   compound for 20 years, which is why that whole column is in the hundreds); only signs are.
                                 #   THE CASE FOR LOOSENING HAS WEAKENED SINCE (#60), NOT STRENGTHENED. (#60) recorded the flag at
                                 #   TWO horizons; today it fires at ONE. 8y went +324.2 | +7.0 (TOO TIGHT) -> +82.4 | -44.3 (SPLIT,
                                 #   median NEGATIVE) as intervening rounds changed which names reach the gate. That is the same way
                                 #   `growth_max_peg`'s 8y flag died — see its own block. A sweep flag surviving on a thin positive
                                 #   median is the thing that expires; check its age before ever acting on one.
                                 #   84.0 STANDS. (#60)'s SHIP RULE v2 TIE re-affirmed on a fresh dataset, both reopen conditions
                                 #   now closed. NOTHING on the asset-class axis can reopen this. The only route left is a change
                                 #   to WHICH names reach this gate at 12y — e.g. closing the live-vs-backtest as-of fundamentals
                                 #   skew — and that is a different round with a different instrument.
                                 #   (#138) SHIPPED OFF, 2026-08-25 — THE FIRST TIME THIS KNOB WAS EVER GRADED ON THE BOOK.
                                 #   (#26)/(#60)/(#74) ARE ALL PRE-PIT, and not one of them reports a top-10 excess, a worst window or
                                 #   an h2h. Every previous refusal rested on the GATE SWEEP row plus the 20y sign flip — a PRE-BOOK
                                 #   instrument — and all three predate (#120)'s move of the graded basket to top-10. (#74) named the
                                 #   instrument that reopens it: "the only route left is a change to WHICH names reach this gate at
                                 #   12y ... a different round with a different instrument". `pit` is that change.
                                 #   THE LEAD, RE-READ FRESH RATHER THAN BANKED — `{20,12,8} universe fund pit`, this same day:
                                 #     20y n=3  -703.6 | -483.3      12y n=21 +388.1 | +252.3  TOO TIGHT
                                 #                                    8y n=30 +209.6 |  +41.7  TOO TIGHT
                                 #   The 12y median is 4x the largest this flag has ever printed (+63.1 (#74), +62.3 (#60)). Per the
                                 #   perishability rule in `growth_max_peg`'s block, the flags that expire are the ones whose median
                                 #   sits just above zero: 8y's +41.7 is in that band, 12y's +252.3 is not.
                                 #   THE OVERLAY, inlined per the recreation rule (tests/p0b-maxdd-off.yaml, deleted after the round):
                                 #     buy_heuristic:
                                 #       print_h2h_mid_ladder: true
                                 #       growth_maxdd_cap: 0.0
                                 #   THE INSTRUMENT SELF-VALIDATED. `gate_sweep` loosens exactly one gate off the shipped tuning, so
                                 #   its n IS this knob's sole-block count. Admitted went 117->120, 228->249, 325->355 — equal to the
                                 #   sweep n (3/21/30) TO THE UNIT at all three horizons, the cross-check (#74) and (#65) both used.
                                 #   Drift named exactly `print_h2h_mid_ladder false->true, growth_maxdd_cap 84.0->0.0` on every run;
                                 #   windows scored 1123 / 2731 / 5941. With the gate off its own sweep row reads "admits 0 new names".
                                 #     horizon  top-10 exc|med           worst           h2h vs 6-10   vs 11-20   admitted
                                 #       20y    +0.8|+0.9 -> +0.6|+0.7   -10.0 -> -10.0    50% -> 50%   50% -> 50%   117 -> 120
                                 #       12y    +2.8|+2.8 -> +3.7|+4.0    -9.5 ->  -6.4    61% -> 63%   50% -> 50%   228 -> 249
                                 #        8y    +4.5|+4.5 -> +5.3|+5.6    -6.2 ->  -5.5    65% -> 70%   67% -> 62%   325 -> 355
                                 #   PRIMARY passes at 12y and 8y on BOTH moments. Every guard passes: rank-1 h2h clears 50% on both
                                 #   comparison books at all three horizons, and the worst window HELD at 20y and IMPROVED at 12y/8y.
                                 #   The gain is uniform across the WHOLE ladder — top-10 through top-50 all move ~+0.9 — so it is not
                                 #   a basket-size artifact. The GATE AUDIT agrees from the other side: accepted-cohort mean
                                 #   +17.0 -> +48.3 (12y) and +48.7 -> +62.3 (8y).
                                 #   *** THE SHIP TURNS ON ONE PRE-REGISTERED DEVIATION, STATED HERE RATHER THAN BURIED. *** 20y was
                                 #   graded as a GUARD ONLY, never as a PRIMARY adjudicator, and that was decided BEFORE the runs. 20y
                                 #   PIT is n=3 on this cohort and (#124) puts its n_eff at ~0.2: it cannot adjudicate a MEAN, but a
                                 #   worst-window guard is a MAX and stays answerable. UNDER THE LITERAL RULE (PRIMARY at 20y AND 8y)
                                 #   THIS IS A TIE AND REFUSES, exactly as (#60) and (#74) did — 20y top-10 excess reads +0.8 -> +0.6
                                 #   and rank-1 -3.3 -> -3.9. Same substitution precedent as (#126)/(#135)/(#137). Anyone who rejects
                                 #   the deviation should revert on that ground alone; the numbers above are not in dispute.
                                 #   THE SURVIVORSHIP OBJECTION, TESTED RATHER THAN WAVED THROUGH. A drawdown cap is the knob MOST
                                 #   exposed to a survivor-biased pool: every >84% faller still in the pool recovered by construction.
                                 #   `pit` already fixes membership — its pond is 5764 names that were EVER in the index, not the ~503
                                 #   that survived — leaving delisted-name PRICES, which is (#134)'s open blocker. `pit` and `stress`
                                 #   CANNOT be combined (backtest.rs: "opposite treatments of the same bias ... would silently conflate
                                 #   them"), so the cross-check ran on the `stress` lane, arm vs control, same day:
                                 #     12y  +6.5|+6.1 -> +6.7|+7.1   worst -7.8 -> -7.8   OOS +5.3/+7.6 -> +5.4/+8.0   51 windows both
                                 #      8y  +6.5|+7.1 -> +6.9|+7.8   worst -9.2 -> -9.2   OOS +6.0/+6.9 -> +6.1/+7.6   59 windows both
                                 #   Same sign, both OOS halves still positive — the bar backtest.rs itself sets for "the edge survives
                                 #   the loser-inclusive pool". And at 8y the arm rode FEWER wipeouts, 1/434 -> 0/444, dropping FRCB
                                 #   (-100%): turning the cap off did not import lottery tickets. The stress gain (+0.2/+0.4 mean) is
                                 #   SMALLER than the pit gain (+0.9/+0.8), so read the pit number as an UPPER BOUND, not the estimate.
                                 #   THREE THINGS THAT ARE NOT CLEAN, recorded so nobody has to rediscover them:
                                 #     (1) the graded window count rose 34->35 (12y) and 42->43 (8y) — a window that admitted nobody
                                 #         now admits somebody. A new window can only make a WORST worse, so the worst-window
                                 #         improvement proves EXISTING windows improved, and the stress lane shows the same direction
                                 #         at IDENTICAL window counts. But the two means are not strictly like-for-like.
                                 #     (2) the gain sits in the LATE OOS half (12y +3.4->+5.5, 8y +5.4->+7.1) while the early half
                                 #         slips marginally (+2.1->+1.9, +3.5->+3.4). Both halves stay positive at both horizons.
                                 #     (3) 20y accepted-cohort quality got WORSE (mean -246.4 -> -257.9), consistent with (#74)'s
                                 #         reading that over 20 years a >84% wipeout marks a broken company.
                                 #   OFF-BY-ONE IN THE (#26) TEXT ABOVE, left as written and corrected here: the comparator in
                                 #   `picks::score_parts` is `>` (pinned by picks.rs "the cap itself is survivable"), so 84.0 rejected
                                 #   a name whose maxdd exceeded 84, not 83. Do not re-quote "exceeds 83%" as the rule it enforced.
                                 #   COST OF SHIPPING OFF: the `growth_maxdd_cap ->off` GATE SWEEP row is a no-op from here on, so this
                                 #   knob loses its own standing alarm. Watch the `top-10 zero names` line instead — that is the direct
                                 #   symptom this gate existed to prevent.
                                 #   REVERT RULE: `growth_maxdd_cap: 84.0`. Revert on ANY of — (a) (#134)'s survivorship-free prices
                                 #   land and the arm does not reproduce (this is the FIRST knob to re-grade when they do); (b) the
                                 #   20y-as-guard-only deviation is rejected as policy; (c) `top-10 zero names` grows on a later run.
                                 #   DO NOT ladder 70/78/90 to "improve" this — `off` is the only rung the evidence prices.
                                 #   0 = off.
  growth_maxdd_cap_crypto: 84.0  # (#26) crypto's own maxdd bar = "reject coins that wiped out WORSE than Bitcoin" (BTC -83%,
                                 #   so 84 keeps BTC and cuts the deeper-wipeout alts: ZEC -98, TRX -96, XMR -93, XRP -96,
                                 #   SOL -96...). Coins crash >90% every cycle, hence a separate looser bar than the equity 83.
                                 #   BACKTEST-BLIND in practice (almost no coin has the full 12y window, so the pool carries
                                 #   ~none) -> judgment gate like the turnover floor, validated by live-screen eyeball.
                                 #   CONFIRMED 2026-08-02, and this block had the mechanism RIGHT where its two crypto
                                 #   siblings had it wrong: it is the forward window, not absence from the pool. The census
                                 #   line now prints the number — 12y-s: crypto 0 cutoffs of 11758; 8y-s: 18 of 20575, 0 scored.
                                 #   If a future bear drags BTC below -84, raise this just above BTC's new mark. 0 = off.
  growth_max_vol_crypto: 3.0     # (#36) crypto-only VOL cap = "reject coins swinging WILDER than Bitcoin day-to-day" — same
                                 #   philosophy as the maxdd bar, applied to daily-return stdev (the VOL column) instead of the
                                 #   worst-ever tail. BTC ~2.4%/day, so 3.0 keeps the base with headroom and cuts the wild alts
                                 #   (OKB 3.4, LEO 3.1). Edge-blind by construction — CORRECTED 2026-08-02: not because
                                 #   "crypto is absent from the backtest pool" (it is NOT; fetch_universe pushes SYMBOL-EUR
                                 #   tickers and nothing filters them out), but because ~11y of coin history leaves zero
                                 #   cutoffs with a scored forward window: 20y-s 0/4633, 12y-s 0/11758, 8y-s 18/20575 with 0
                                 #   growth-scored. Judgment gate validated by live-screen eyeball. 0 = off.
  growth_max_peg: 1.6            # (#37) HARD valuation ceiling: reject an equity whose PEG exceeds this. Stored as PEG
                                 #   (reads like the column and like the request), APPLIED to `fund.peg_yield` — which is
                                 #   1/PEG x 100 — as `peg_yield < 100/growth_max_peg`, so a HIGH peg_yield is cheap. Since
                                 #   2026-07-27 this IS the printed `peg` cell: one PEG, both dividing by the score's own
                                 #   `picks::long_cagr_pct`. It was TWO numbers before (cell on pe_ratio/long_cagr_from, gate
                                 #   on a hardcoded trend_cagr) and they disagreed in both directions — see (#37) above for
                                 #   why that was overruled. Also rejects `eps_ttm <= 0` (no earnings to be cheap against);
                                 #   genuinely absent fundamentals still pass. THE NUMBERS BELOW PRICED THE OLD DEFINITION:
                                 #   +122.4 -> +139.7 edge, worst era +44.0 -> +63.0, every value in [1.5, 4.0] beating off
                                 #   without separating. The on/off finding is the durable half; the threshold half must be
                                 #   re-measured, because life CAGR sits below trend CAGR so every ceiling now bites harder.
                                 #   2.0 stays as the asked-for value inside a plateau, not a fitted optimum. 0 = off.
                                 #   THE OWED RE-MEASURE IS DONE (2026-08-01, mp{20,25,off}-*-s.txt): 1.6 / 2.0 / 2.5 / off x
                                 #   {20,12,8} stress. VERDICT: 1.6 HOLDS. No arm clears the bar, and two structural facts came
                                 #   out of the ladder that matter more than the threshold:
                                 #   (a) REDUNDANT AT THE KING HORIZON. At 20y-s all four arms are identical to the decimal —
                                 #   accepted n=380, edge +648.2, top-10 +6.1 (+6.1), rank-1 +6.0|+6.6 win 83%, h2h 65% — and the
                                 #   sweep row reads "admits 0 new names". NOT a coverage gap (20y as-of fundamentals cover
                                 #   4458/4633 cutoffs): every name this ceiling would cut is ALREADY cut by growth_min_cagr 19 or
                                 #   growth_min_range_pct 80, so PEG is never the sole blocker there. The king CANNOT vote on this
                                 #   knob, which is why the locked grading rule can't decide it and 12/8y have to.
                                 #   (b) ZERO RANK-1 AUTHORITY anywhere. Rank-1 is byte-identical across all four arms at all three
                                 #   horizons (+6.0|+6.6 / +5.6|+5.9 / +6.6|+7.2). This is a pool-SIZE knob, not a ranking one —
                                 #   same orthogonality lesson as growth_overext_floor vs growth_accel_weight.
                                 #   WHAT THE 12/8y ARMS ACTUALLY SAY: 12y lane edge humps at 2.0 (+212.9 / +219.6 / +216.6 /
                                 #   +204.3) but the top-10 book is IDENTICAL at every arm and the bootstrap LOWER bound gets worse
                                 #   (+147.0 -> +143.7) — point estimate up, downside down, book unchanged, so no ship. 8y is the
                                 #   mirror: lane edge falls monotonically (+77.1 / +76.5 / +72.1 / +71.9) while the BOOK improves
                                 #   (top-10 med +6.2 / +6.7 / +6.7 / +6.8, OOS-late +5.9 / +6.0 / +6.0 / +6.2). Both bootstrap
                                 #   lower bounds fall as the ceiling loosens.
                                 #   That 8y split is EXACTLY the trade the paragraph above predicted — "the gate improves the
                                 #   RANKING among what it keeps while excluding names that then outperform" — now measured with a
                                 #   median instead of inferred from a mean. The sweep's 8y TOO TIGHT flag survives its median
                                 #   (+195.3 | +57.5, n=54) and is REAL; it simply does not convert into a shippable number,
                                 #   because what it buys (a slightly better 8y book) it pays for in lane edge and downside.
                                 #   DO NOT RE-PROPOSE 2.0/2.5/off on sweep evidence alone: the sweep row measures the names a
                                 #   looser ceiling admits, not what admitting them does to the book. The ladder is the answer.
                                 #   TIGHTENED 2.0 -> 1.6 (2026-07-30, user's live value, per-knob stress grid): 8y-s edge +83.1
                                 #   vs +79.2 with h2h 52% vs 48%; 12y/20y flat (the gate is near-blind at the 20y king — SEC
                                 #   fundamentals start 2007, its cutoffs end 2006). Same [1.5, 4.0] plateau as above, but the
                                 #   cheap end now measures mildly better and costs nothing anywhere. Shipped as a pair with
                                 #   growth_min_cagr 19 (knob-K12 grid — interaction nil, see that receipt).
                                 #   (#50) THE BACKTEST CANNOT VOTE ON THIS KNOB UNDER A LOOSE LEG FLOOR — measured
                                 #   2026-08-05 on the live overlay (growth_min_leg_years 0, range 50). 1.6 -> 2.0 came
                                 #   back BYTE-IDENTICAL at all three horizons: rank-1 +4.7|+4.3, h2h 48%, edge +183.1,
                                 #   n=914 — not "small delta", the same digits. Repeated on top of the leg-floor revert
                                 #   (arm E+C vs E at 20y): identical again, +6.2|+6.7 h2h 65% edge +265.0 n=723.
                                 #   AND THE REASON IS PRINTED IN THE RUN, so do not read those nulls as a green light —
                                 #   corrected 2026-08-06 after I did exactly that. The gate audit says:
                                 #     growth_max_peg ->off   admits 0 new names (gate not binding on this sample)
                                 #   Fundamentals ARE loaded (backtest.rs:446 sets quote.fund; the fund_extra:roic probe
                                 #   moves edge -61.0 in the same run), so this is NOT structural blindness — it is that
                                 #   the backtest's sample contains no name this gate was rejecting at 1.6. Loosening
                                 #   admits nobody THERE while admitting 15 on the live universe. Byte-identical is
                                 #   therefore ZERO EVIDENCE, not a measured null, and it is evidence in NEITHER
                                 #   direction. (b) "zero rank-1 authority" has the same caveat and always did.
                                 #   MEANWHILE IT IS THE LARGEST SOLE-BLOCK IN THE LIVE FUNNEL. Same day, same overlay:
                                 #   peg fails 411 stocks and SOLE-blocks 17 — AAPL, AMZN, ANET, ARM, AVGO, CRWD, DELL,
                                 #   FTNT, HWM, LITE, LRCX, MPWR, PANW, SNDK, TSLA, VRT, VST — against cagr 512/1 and
                                 #   cagr-life 484/2. The CAGR gates fail more names but almost never alone; PEG is the
                                 #   marginal binding gate on the live universe. AMZN misses by 0.02 (PEG 1.62 vs 1.60).
                                 #   Raising to 2.0 took the live stock table 2 -> 6 names. This is the "HOW MUCH IT BITES
                                 #   DEPENDS ON A KNOB 60 LINES AWAY" warning above, now measured from the other side:
                                 #   quoting (a) "PEG is never the sole blocker" as a LIVE fact is wrong. It is a
                                 #   backtest-pool fact, and the live funnel disagrees with it flatly.
                                 #   CONFIRMED A THIRD TIME AT FULL-OFF, 2026-08-05. `growth_max_peg: 0.0` +
                                 #   `growth_max_peg_etf: 0.0` on top of the leg-floor revert, vs the same arm at 1.6:
                                 #   the two backtests differ by ONE line, the drift warning naming the arm file. 20y
                                 #   rank-1 +6.2|+6.7 h2h 65% n=723; 8y +6.5|+8.2 h2h 58% n=1221 — identical both
                                 #   horizons. So the whole range 1.6 -> off is un-sampled, not just the 1.6 -> 2.0 step.
                                 #   `growth_max_peg_etf` is not binding at a 5y leg floor either (ETF table stays at 5
                                 #   rows across every rung below).
                                 #   THE LIVE LADDER, measured at `growth_min_leg_years: 5.0` — the only evidence this
                                 #   knob has. Read the P/E and EPS-YoY columns, NOT the count:
                                 #     peg    stocks   name it lets in       P/E     PEG    EPS-YoY
                                 #     1.6      1      NVDA                  33.6    1.40    +66.7%
                                 #     2.0      5      AMZN                  32.6    1.63    +29.7%
                                 #                     DELL                  36.9    1.69    +36.1%
                                 #                     VRT                   69.8    1.70   +166.4%
                                 #                     AAPL                  37.7    1.85    +22.7%
                                 #     2.5      9      HWM                   67.6    2.21    +32.0%
                                 #                     FTNT                  63.6    2.46     +7.1%
                                 #                     AVGO                  69.6    2.47   +287.8%
                                 #                     ANET                  67.8    2.48    +23.3%
                                 #     3.0     10      VST                   23.5    2.78    -68.9%
                                 #     off     16      LRCX                  58.1    3.74    +43.1%
                                 #                     MPWR                  96.2    4.54    -60.6%
                                 #                     TSLA                 297.7    7.52    -47.1%
                                 #                     PANW                 315.4    9.26    -56.0%
                                 #                     LITE                 145.5   62.93   -104.6%
                                 #                     CRWD                   n/a     n/a   +983.3%
                                 #   OFF IS NOT AN ENDPOINT — RECOMMENDED ON 2026-08-05 AND RETRACTED 2026-08-06. At off
                                 #   the RANK-1 STOCK IS MPWR: P/E 96.2, earnings down 60.6%, score 22.2. A valuation gate
                                 #   set to 0.0 puts the worst-priced name in the book at the top of it. Every name above
                                 #   PEG 2.5 except LRCX has earnings falling 47-105%; every name at or below 2.5 is
                                 #   growing. The count knee at 3.0 is real but it is the WRONG reading of this table.
                                 #   OFF ALSO UNARMS THE LOSS-MAKER SCREEN. `None if eps_ttm <= 0 => return None`
                                 #   (picks.rs) sits INSIDE the `if growth_max_peg > 0.0` guard, so 0.0 disables the
                                 #   no-earnings rejection as a side effect of disabling the ceiling. CRWD — PEG `n/a`,
                                 #   admitted at no other rung — is the proof. Two gates, one switch. Never fires at a
                                 #   non-zero rung, so it is recorded here rather than fixed in code.
                                 #   THE DENOMINATOR IS A PRICE CAGR, WHICH IS WHY THIS NUMBER MISLEADS. core::peg_yield
                                 #   is (P/E) / long_cagr_pct, and long_cagr_pct is the score's LONG-LEG PRICE CAGR, not
                                 #   an earnings growth rate. So the textbook "PEG > 1 is expensive" does not transfer,
                                 #   and a name is rated CHEAPER for having already run up: VRT at P/E 69.8 with a +51%
                                 #   CAGR prints 1.70, while AAPL at P/E 37.7 with +21% prints 1.85. Apple at 38x
                                 #   earnings scores as pricier than Vertiv at 70x. Partly a momentum term in valuation
                                 #   clothing — pair any reading of this column with the raw P/E next to it.
                                 #   2.0 IS THE DEFENDABLE RUNG, AND THE REASON IS THAT THIS GATE IS THE ONLY VALUATION
                                 #   CONTROL THE GROWTH LANE HAS. `growth_value_weight` ships 0.0 (below) — the P/E
                                 #   multiplier is OFF, deliberately, because the walk-forward never saw pe_ratio. So
                                 #   nothing downstream docks a rich name: `value_factor` is written to saturate at
                                 #   P/E 40 anyway ((value_ref_pe/pe).clamp(0.5, 1.5) at ref 20.0, so P/E 70 and P/E 300
                                 #   would score the same dock), but at weight 0 it never even runs. THIS KNOB IS THE
                                 #   WHOLE BRAKE. Set it where the raw P/E is still defensible on its own, because
                                 #   nothing else is looking: at 2.0 four of five names are under P/E 38; at 2.5 five of
                                 #   nine are P/E 63-70. Not that the 2.5 names are bad — all four have growing earnings
                                 #   — but there is no second line of defence behind them. Raising this knob above ~2.0
                                 #   is only defendable together with a working P/E term, which today does not exist.
                                 #   CONSEQUENCE FOR ANY FUTURE ROUND: this knob's cost cannot be graded by `backtest` —
                                 #   the gate audit line above tells you so directly, in any run. Decide it on a live
                                 #   funnel plus the P/E column, never on a grid, and never on a byte-identical null.
                                 #   EXTENDED TO FUNDS 2026-08-02 — but on a SEPARATE number, `growth_max_peg_etf`
                                 #   below, not this one. This knob and every receipt above it are equity-only and
                                 #   unchanged; see that knob for the measurement that forced the split.
                                 #   PARTIAL RETRACTION 2026-08-06 — "at 2.0 four of five names are under P/E 38"
                                 #   ABOVE IS WITHDRAWN AS AN ARGUMENT FOR 2.0. Two of those five, AAPL and AMZN, were
                                 #   admitted on a STALE DENOMINATOR. This gate divides by `long_cagr_pct`, which is the
                                 #   rung the AGE ladder picked, so AMZN (29.2y) was priced on its 20Y rate +23.4%/yr
                                 #   while its actual last 5 years ran +4.7%/yr. It printed PEG 1.63 — CHEAPER than VRT
                                 #   at 1.70, which compounds six times faster today. The P/E half of that sentence was
                                 #   true; the PEG half was measuring how long ago the company was good. What survives:
                                 #   this gate is still the growth lane's ONLY valuation control (growth_value_weight
                                 #   0.0), so it is still the whole brake, and 2.0 is still the rung. What dies: do not
                                 #   cite the AAPL/AMZN admissions as evidence a ceiling is calibrated.
                                 #   THE DENOMINATOR, NOT THE CEILING, IS THE BINDING PROBLEM — see the (#51) receipt
                                 #   at `fixed_cagr_years` (~line 588). Pinning every name to one 8Y window admits MORE
                                 #   names at EVERY rung of this knob, 1.6 included (1 -> 3), without loosening it, and
                                 #   drops AMZN on its own record.
                                 #   BUT THE PIN DID NOT SHIP — 5, 8 and 10 all failed the ship rule the same day, on
                                 #   rank-1 quality, because `fixed_cagr_years` moves the ranking legs too. SO THIS KNOB
                                 #   IS STILL GRADED ON THE AGE LADDER, and the stale denominator above is still LIVE in
                                 #   every PEG this file produces. Read the column knowing an old name's PEG may be
                                 #   divided by a rate it stopped delivering a decade ago; do NOT tune this ceiling to
                                 #   compensate, because a ceiling cannot fix a denominator — it only trades away names.
                                 #   The fix is a separate window for the PEG denominator ALONE (next round, code).
                                 # (#74) THE OWED 8y LADDER IS CANCELLED: ITS PREMISE EXPIRED. 2026-08-12. The paragraph above
                                 #   banks one number — "the sweep's 8y TOO TIGHT flag survives its median (+195.3 | +57.5, n=54)
                                 #   and is REAL" — and that is the entire case for re-laddering this ceiling. Re-read today on
                                 #   `backtest {20,12,8} universe fund`, that row is `n=55  +82.1 | -45.8  <- SPLIT`. The MEDIAN
                                 #   FLIPPED, by 103 pts, while n barely moved (54 -> 55). A stable n with a reversed median is
                                 #   not the pool changing size; it is the same-sized cohort's forward returns going the other
                                 #   way. 12y reads n=13 `-210.8 | -421.1` and 20y still `admits 0 new names`, so after the flip
                                 #   there is no horizon left where loosening looks good on both stats. NOTHING TO LADDER — the
                                 #   ladder was owed to a flag, and the flag is gone. 1.6 stands on the 2026-08-01 verdict.
                                 #   THE GENERAL LESSON, and it cost this round its first two candidate targets: A SWEEP FLAG IS
                                 #   PERISHABLE. Both leads that died on inspection here died the same way — a TOO TIGHT row
                                 #   banked months ago, re-read today, now SPLIT with a negative median. `growth_maxdd_cap`'s 8y
                                 #   flag went +324.2 | +7.0 -> +82.4 | -44.3 over the same period (see its block). The flags that
                                 #   expire are the ones whose median sat just above zero: +57.5 and +7.0 both did. BEFORE ACTING
                                 #   ON ANY BANKED SWEEP ROW, RE-READ IT — one `universe fund` run, ~2 min, prints every gate.
  # (#100 / finding C6) VALUATION DISCIPLINE REACHES A MINORITY OF THE UNIVERSE, and that is worth
  #   stating in one place rather than being spread across three knobs that each look fine alone:
  #     - `growth_value_weight: 0.0` — the P/E multiplier IS computed in `score_parts` and then
  #       multiplied by a weight of zero. Dead arithmetic on the shipped config, kept as a knob.
  #     - `growth_max_peg` / `growth_require_peg` read `quote.fund`, which is None for EVERY ETF, EVERY
  #       coin and every non-SEC-covered stock. So for the majority of rows there is no valuation input
  #       at all — while valuation is the input with the strongest long-horizon evidence behind it.
  #     - the free-neutral half of the complaint is ALREADY FIXED and shipped off: see
  #       `growth_fund_scope_by_class` (#87), which is exactly "stop paying uncovered names for the
  #       absence". Turning THAT on is the cheap first move, and it has its own receipt.
  #   THE ETF HALF IS BLOCKED BY FETCH ECONOMICS, not by taste. `fund_peg_yield` exists and would be the
  #   natural scored term — but its input arrives from `yahoo_top_holdings`, called for the ranked picks
  #   plus a refill bench (~50 symbols against a ~4300-fund universe). A UNIVERSE-WIDE fund valuation
  #   term would mean thousands of crumbed quoteSummary calls, which is why that fetch documents itself
  #   as display-only and why this stays a post-rank TRIM rather than a score input. `lane_split` also
  #   warns in code against "fixing" it by attaching a synthetic FundFacts, which would switch the
  #   equity `growth_fund_weight` tilt on for funds as a plumbing side effect. Scoring funds on
  #   valuation needs a bulk source this tool does not have; it is not one knob away.
  growth_max_peg_etf: 2.0        # (#37 funds) the ETF TABLE's valuation ceiling — same PEG units and same arithmetic as
                                 #   growth_max_peg, applied to a fund's look-through equity-book P/E (parse_fund_pe ->
                                 #   picks::fund_peg_yield, dividing by the same long_cagr_pct). A POST-RANK TRIM in
                                 #   picks::lane_split, not a gate in growth_score, and the asymmetry is forced by data:
                                 #   that P/E comes only from yahoo_top_holdings, called for the ranked picks + a refill
                                 #   bench (~50 symbols of a ~4300-fund universe), so a universe-wide fund gate would mean
                                 #   thousands of crumbed quoteSummary calls. Sitting before the cut to n it refills from
                                 #   below instead of shortening the table (contrast the (#41) growth_corr_cap receipt,
                                 #   where stopping at n is exactly why that knob ships 0). Funds with no P/E AND no
                                 #   resolvable index twin are never trimmed; pinned tickers bypass it. Fund P/Es are
                                 #   propagated across a fund's sibling VENUES (Xetra/Euronext/Lisbon all list the same
                                 #   UCITS fund) — without that the ceiling would cut one listing and keep its twin.
                                 #   WHY IT EXISTS: growth_max_peg reads `quote.fund`, which is None for every fund, so
                                 #   funds walked past the tool's only valuation ceiling untested — the same missing-data
                                 #   hole growth_require_peg was added to close for equities.
                                 #   WHY 2.0 AND NOT THE EQUITY 1.6 — MEASURED, not assumed. The shared 1.6 was built
                                 #   first and run live 2026-08-02 against the full 4940-ticker universe. It cut EVERY
                                 #   fund that reports the datum:
                                 #     LYBK.DE  P/E 12  PEG 0.44  kept
                                 #     SMH.L    P/E 44  PEG 1.69  cut   (semis, ~26%/yr)
                                 #     SEMI.AS  P/E 42  PEG 1.76  cut   (~24%/yr)
                                 #     IITU.L   P/E 34  PEG 1.78  cut   (US tech, ~19%/yr)
                                 #     CSSPX.MI P/E 27  PEG 2.57  cut   (S&P 500, ~10%/yr)
                                 #     VWRA.L   P/E 23  PEG 3.29  cut   (all-world, ~7%/yr)
                                 #   The ETF table collapsed to ONE row, and that row (XLKS.L) survived only because it
                                 #   has no look-through P/E — selection by ABSENT DATA, the precise inversion this trim
                                 #   exists to prevent, and the same pathology the crypto half was rejected over.
                                 #   A diversified basket compounds ~7%/yr against a ~23 book P/E; it cannot clear an
                                 #   equity-grade PEG bar, and the ~12%/yr estimate used while designing this was wrong.
                                 #   2.0 keeps the concentrated growth funds (semis 1.69, SEMI 1.76, US tech 1.78) and
                                 #   cuts the broad expensive ones (S&P 2.57, all-world 3.29). That is the growth lane's
                                 #   job — the buy-and-hold CORE shortlist is where broad cheap-forever funds belong, and
                                 #   it is a separate list that this trim does not touch.
                                 #   TRIMMED vs PASSED-FREE at 2.0 (same universe, live): the ETF lane held 10 funds after
                                 #   the score/sector cut — 6 trimmed on a measured PEG, 3 kept on one (1.69/1.76/1.78),
                                 #   and 1 kept for want of a P/E (XLKS.L). 9 of 10 verdicts rest on real data, so this is
                                 #   NOT a mostly-decorative gate. Table went 1 row -> 4; the equity lane came out
                                 #   byte-identical and crypto moved only on live prices, as it must — neither path reads
                                 #   fund_pe. (Wider pool, for scale: 40 funds fetched, 14 served a P/E.)
                                 #   THE "KEPT FOR WANT OF A P/E" ROW WAS THE HOLE, AND IT IS NOW CLOSED (2026-08-02).
                                 #   XLKS.L did not lack the datum by accident: it is a SWAP-BASED (synthetic) ETF. It
                                 #   holds a total-return swap, not equities, so Yahoo answers stockPosition 0.0 /
                                 #   otherPosition 1.0 / holdings [] / every equityHoldings ratio a literal 0.0, and
                                 #   parse_fund_pe rightly refuses to invert a zero. That is PERMANENT for such a fund —
                                 #   it would have ridden the free pass forever, at rank #2, in the same run this trim
                                 #   cut seven funds at PEG 2.57-3.80. Selection by absent data, one level down.
                                 #   FIX: borrow the look-through P/E from a PHYSICAL fund tracking the same index
                                 #   (screen::borrow_index_twins). XLKS.L <- SXLK.L 33.93 -> PEG 1.70, which PASSES 2.0
                                 #   with room, so no table moved on the day it shipped; the gain is that the next swap
                                 #   fund gets TESTED. Borrowed values act exactly like fetched ones and print with a `~`
                                 #   plus the source ticker — an inference that can cut a fund must be traceable.
                                 #   MATCHED BY NAME, NEVER BY COMPOSITION, and that is measured, not stylistic: a
                                 #   synthetic fund's reported basket looks like COLLATERAL, not exposure. XLKS.L reads
                                 #   Technology 0.912 / Financial Services 0.072 against twin SXLK.L's Technology 0.994 /
                                 #   Communication Services 0.006, and PANX.PA misses ANX.PA the same way — a sector
                                 #   fingerprint REJECTS both real pairs. Do not re-propose it; it was tried and it fails
                                 #   on exactly the funds that need it.
                                 #   SAFETY: bench-only (never the ~4250 unfetched funds, or this display trim silently
                                 #   becomes the universe-wide gate the paragraph above rules out); sources must be
                                 #   MEASURED, so no chaining; and candidates that DISAGREE refuse rather than guess.
                                 #   Gate: screen::index_key_never_fuses_two_different_books groups all 46 P/E-bearing
                                 #   funds by name key against real Yahoo longNames and fails if any key covers two
                                 #   different ratios — 5 multi-fund keys, 0 heterogeneous. Live orphans that correctly
                                 #   resolve to NOTHING and keep their n/a: JEDG.L (physical, Yahoo just has no ratio),
                                 #   PANX.PA (a screened index, genuinely unique), XACT-BULL-2.ST (2x leveraged),
                                 #   VBTC.DE (bitcoin ETP, no equity book at all).
                                 #   NOT BACKTEST-GRADEABLE, ever: the look-through P/E is a live snapshot with no
                                 #   history, so there is no as-of value to reconstruct at a cutoff. Judgement + live-run
                                 #   diff, like growth_min_age_years and growth_min_aum_etf. Unit-pinned instead
                                 #   (picks::etf_peg_ceiling_trims_and_refills, screen::fund_pe_line_semantics), and
                                 #   growth_gate_reachability_pin's golden string is UNCHANGED by it — backtest_quote
                                 #   fills no fund P/E, so nothing leaked into the price-only path.
                                 #   CRYPTO WAS REJECTED ON MEASUREMENT (2026-08-02 DefiLlama spike). The plan was a
                                 #   per-coin revenue proxy (api.llama.fi/overview/fees, dataType=dailyRevenue, joined to
                                 #   CoinGecko ids via /v2/chains) feeding the same ceiling. The payload is fine —
                                 #   `total1y` is a true trailing-365d figure, 365 of 461 chains carry a gecko_id — but
                                 #   the ARITHMETIC kills it:
                                 #     * coverage: 23 of the top 100 coins map to a revenue-bearing chain; 6 of those
                                 #       (BTC, DOGE, ZEC, HBAR, ATOM) report zero, which None-outs. 83/100 pass FREE.
                                 #     * magnitude: chain revenue/market-cap runs 0.001-1%, i.e. P/E in the thousands.
                                 #       CAGR needed to clear a 1.6 ceiling: ETH 1,629 %/yr, SOL 828, SUI 1,860,
                                 #       ADA 43,933, ALGO 55,480. Only TRX (49), ARB (27), POL (19) are in reach.
                                 #   So it would reject essentially every major L1 that reports data and admit BTC, DOGE,
                                 #   ZEC and the 77 that report none. Not a coverage problem a second source fixes: equity
                                 #   PEG compares price to EARNINGS, and chain fee revenue is not that quantity. Do not
                                 #   re-propose without a crypto-native cheapness measure AND a way to grade it (crypto
                                 #   has 0 growth-scored cutoffs at every horizon: 20y-s 0/4633, 12y-s 0/11758, 8y-s
                                 #   18/20575 with 0 scored — no sweep can ever vote on it). 0 = off.
                                 #   SUPERSEDED IN PART 2026-08-02, and read the split carefully: crypto DOES have a
                                 #   valuation gate now, `crypto_max_mvrv` above, on MVRV from CoinMetrics. Exactly HALF
                                 #   the bar written on the line above was cleared. The measure arrived — MVRV is
                                 #   crypto-native, runs 0.26-1.37 instead of the thousands this revenue proxy produced,
                                 #   and cross-checks against the tool's own NUPL footer. The GRADING did not, and
                                 #   cannot: the backtest still scores zero crypto rows. So `crypto_max_mvrv` ships on
                                 #   judgement, and the sentence above still stands for anything built on fee revenue.
  growth_min_net_margin: 0.0     # (#38) net-margin floor on `fund.net_margin`. MEASURED AND REJECTED, kept at 0 with the
                                 #   receipt so it is not re-proposed: 5/10/15 all cost edge or era-stability, and combined
                                 #   with growth_max_peg it cancels that gate entirely and turns the newest era negative.
                                 #   Single-digit net margin is an INDUSTRY fact (retail, distribution, industrials), not a
                                 #   quality signal. Raising this cuts sectors, not bad companies. 0 = off.
  growth_max_margin_swing: 0.0   # (#39) MEASURED, EDGE-POSITIVE, AND DELIBERATELY NOT SHIPPED — kept at 0 with the receipt
                                 #   so the good numbers don't get it re-proposed. Rejects on net-margin std, which has no
                                 #   direction, so it cuts NVDA (36%->56%) and ANET (29%->39%) while CF and MPC survive: it
                                 #   punishes margin EXPANSION as readily as cyclicality. Needs downside-only dispersion, or
                                 #   pairing with margin_trend, before it means what its name says. 0 = off.
  # ---- (P1) SURVIVAL GATES: four rejections, ALL OFF, NONE MEASURED YET ----
  # These four factors already exist as `FundFactors` fields and have only ever been priced as additive
  # TILTS. As GATES they make a different claim — not "this name scores lower" but "this filer may not
  # survive the hold" — and no scoring term in the lane can express that. Each is equity-only for free
  # (coins and ETFs carry no `fund`), and missing fundamentals PASS, per the house rule.
  #
  # EVERY VALUE BELOW IS AN UNFITTED JUDGEMENT VALUE — specifically, the OFF state. Nothing here has
  # been swept, so nothing here has a receipt yet. They reject OVERLAPPING cohorts, so their individual
  # deltas will NOT add and the survivors must be run JOINTLY before any of them ships. The sweep also
  # has to report each rung's REJECTED cohort forward return vs kept peers: a gate that cuts names which
  # then outperform is a bad gate whatever it does to the edge, which is the standard the #24 receipt set.
  # REVERT RULE for all four: they are off; turning one on without a run in this file is the revert.
  #
  # (#124) PARTIALLY MEASURED 2026-08-23 — the "NONE MEASURED YET" in the heading above is now stale
  # for THREE of the four, and still exactly true for the fourth. Read on `universe fund pit` at
  # 20y/12y/8y (tests/diag-ci-settings.yaml), from the SURVIVAL-GATE probe the report already prints:
  #
  #                    reject 0% (off)      reject 10%           reject 25%
  #   fcf_margin       excess +2.4          +2.4  (null)         +2.2  (WORSE)
  #   interest_cover   excess +2.4          +2.4  (null)         +2.2  (WORSE)
  #   net_cash_rev     excess +2.4          +2.4  (null)         +2.3  (WORSE)
  #   margin_stability excess +2.4          +2.4  (null)         +2.2  (WORSE)   [not a P1 knob]
  #                                          (12y; `worst` and OOS unmoved at every rung)
  #
  # READ IT AS EVIDENCE ABOUT THE FACTORS, NOT AS A SWEEP OF THESE KNOBS. The probe drops the weakest
  # P% by factor and re-ranks; these knobs set an ABSOLUTE threshold. A percentile cut and a bar are
  # not the same experiment, so this does NOT retire the 5-rung ladders below. What it does do is move
  # the prior hard: cutting the weakest tenth by any of the three changed nothing at all, and cutting
  # the weakest quarter cost edge every time. That is the opposite of the shape a gate worth shipping
  # makes, and it is the same answer at 8y.
  #
  # P1a (growth_max_dilution_pct) HAS NO READING AT ALL — and it is the one with the highest prior.
  # The SURVIVAL-GATE probe covers fcf_margin, interest_cover, net_cash_rev and margin_stability.
  # Dilution is absent from it, so nothing above speaks to it and nothing may be inferred about it
  # from its neighbours. It is still UNFITTED in the strict sense: sweep it FIRST, as this block
  # already said.
  #
  # THE FINDING THAT OUTRANKS ALL FOUR, from (#124) on print_recall_capture/print_book_deciles in the
  # same runs: the held book NEVER EXCEEDS ~10 NAMES (the top-N ladder is identical from top-10 to
  # top-50 at every horizon) and it captures only 5-24% of the pool's extreme wealth. The lane's
  # problem is ADMISSION, not selection. Four more rejections is the wrong direction against that
  # measurement, whatever any individual rung scores. Do not open this block before reading (#124).
  #
  # (#156) MEASURED 2026-08-29 — the sweep this block demanded, run at last. REFUSED AT EVERY RUNG.
  # Keep 0.0 (off). Nothing to revert: the knob never moved off its default, and this receipt exists
  # so the next reader does not pay for the same five runs.
  #
  # Runs: `backtest {20,12,8} universe fund pit`, arms at tests/dil{0,1,2,5,10}-ci-settings.yaml, the
  # pre-registered ladder off/10/5/2/0. The `fund` keyword is LOAD-BEARING: `fund_lane_on`
  # (backtest.rs:501) is what fills `quote.fund`, and without it `buyback_yield` is None on every row,
  # missing data passes (non-negotiable #5) and all five arms print byte-identical. An arm that reads
  # exactly like its control is the symptom of a forgotten keyword, not of an inert gate.
  #
  #   arm    12y excess (med)  ws   h2h(#135)   |  8y excess (med)  ws   h2h(#135)  |  20y
  #   off    +3.7 (+3.9)       249  12/19 63%   |  +5.3 (+5.6)      355  19/27 70%  |  all arms
  #   10     +3.7 (+3.9)       247  11/18 61%   |  +5.5 (+5.7)      349  18/26 69%  |  byte-
  #    5     +3.5 (+3.9)       243  10/18 56%   |  +5.3 (+5.6)      344  18/26 69%  |  identical
  #    2     +3.2 (+3.0)       232  10/17 59%   |  +4.8 (+4.8)      328  17/25 68%  |  to control
  #    1     +3.0 (+2.9)       225   9/16 56%   |  +4.3 (+4.6)      309  13/24 54%  |
  #   (ws = `windows scored`. worst window UNCHANGED at every arm and horizon: -6.4 / -5.5 / -10.0.)
  #
  # Rungs 1, 2 and 5 fail PRIMARY outright at both horizons — down on mean AND median. That is
  # (#124)'s shape reproduced exactly on the one gate its probe never covered: tightening this family
  # is null and then costly. The block above may now be read as covering all four, not three.
  #
  # Rung 10 is the one that needs care, because BY THE LETTER IT PASSES: 12y held (byte-identical on
  # every graded column), 8y up on mean and median, worst window unmoved, and the rank-1 h2h GUARD
  # apparently IMPROVED 5/8 (62%) -> 5/7 (71%). It is refused anyway, on three independent reads:
  #   1. That h2h "gain" is the (#151) residue artifact in its purest form — the NUMERATOR IS
  #      IDENTICAL AT 5. Only the denominator moved, 8 -> 7, because the tighter gate saturated one
  #      more bucket and deleted a window the shipped lane was LOSING. A count improved by removing
  #      one of its losses is not a win.
  #   2. `print_h2h_mid_ladder` (#135) — on in this file, off in the shipped config — settles it on a
  #      real denominator, and REVERSES the sign: 19/27 (70%) -> 18/26 (69%) at 8y, 12/19 (63%) ->
  #      11/18 (61%) at 12y. The numerator FALLS at both horizons. Rung 10 does not win a single
  #      head-to-head it was not already winning. This is what (#135) was built for; read it before
  #      leaning on any rank-1 GUARD whose n is single-digit.
  #   3. The +0.2 at 8y is a mean over a DIFFERENT SAMPLE: ws 355 -> 349, six growth-scored names and
  #      six windows gone. See the general finding below.
  #
  # GENERAL FINDING — (#151) EXTENDS FROM THE h2h TO PRIMARY ITSELF, FOR POOL-CHANGING KNOBS.
  # (#149) exempts gates from the vacuity rule because they change the pool; (#151) then amended the
  # rank-1 h2h out of that exemption, its denominator being the residue count. The same reasoning
  # reaches PRIMARY: a gate that empties a bucket removes a window from `windows scored`, so its mean
  # and median are taken over a different sample than the control's. Across all five arms at BOTH
  # horizons the top-10 excess is a near-monotone function of ws (225->249 tracks +3.0->+3.7;
  # 309->355 tracks +4.3->+5.3). So: A GATE'S PRIMARY IS A LIKE-FOR-LIKE READ ONLY WHILE `windows
  # scored` HOLDS. When it moves, the delta must clear what the composition change alone explains —
  # rung 1's -1.0 on a 13% smaller sample is still plainly a loss, rung 10's +0.2 on a 1.7% smaller
  # sample is not a gain. Print ws for both sides of every future gate comparison.
  #
  # At 20y the gate is entirely INERT — all five arms byte-identical, worst -10.0 throughout. As-of
  # fundamentals are too sparse pre-2010 for `buyback_yield` to be populated, and missing data passes.
  # The 20y GUARDs are therefore satisfied trivially, not meaningfully, and no 20y column here is
  # evidence for or against the gate.
  #
  # DETERMINISM, checked rather than assumed: the control was re-run LAST, after all ten other runs.
  # It differed from its own first run in the header counts ONLY — 6083 -> 6087 tickers, 4764 -> 4768
  # resolved, as Börse Frankfurt ISIN resolution warmed the cache — and was byte-identical on every
  # graded column: excess, median, census, h2h, worst window, holds. Universe drift inside that band
  # is graded-inert, every arm sat inside it, and the ladder above is a like-for-like comparison.
  # This CONFIRMS the determinism claim this file already makes; run-order drift was the first thing
  # suspected when the ladder came out monotone, and it is not the explanation.
  growth_max_dilution_pct: 0.0   # (P1a) reject a filer whose as-of 1y share count grew more than this %.
                                 #   Reads `-fund.buyback_yield` (that field is sign-flipped, + = shrinking),
                                 #   so a 3% share raise reads +3.0 here. Serial issuance is how a compounder's
                                 #   per-share result is taken from the holder, and no margin or growth term in
                                 #   the lane can see it. SWEPT AND REFUSED at every rung — see (#156)
                                 #   directly above. Do not re-run this ladder without a new reason.
                                 #   The ladder it ran was off / 10 / 5 / 2 / 0. 0 = off.
  growth_min_interest_cover: 0.0 # (P1b) reject a filer whose as-of operating income ÷ interest expense is
                                 #   under this ×. `fund.interest_cover` is None when NO interest expense was
                                 #   filed, so a debt-free balance sheet reads NEUTRAL and passes — deliberate:
                                 #   scoring "carries no debt" as "cannot service its debt" inverts the gate.
                                 #   UNFITTED. Suggested ladder: off / 2 / 3 / 5 / 8. 0 = off.
  growth_min_fcf_margin: -1e9    # (P1c) reject a filer whose as-of (op cash flow − capex) ÷ revenue is under
                                 #   this %. NOTE THE OFF SENTINEL: it is -1e9, NOT the house 0, because a bar
                                 #   of exactly 0 ("reject anyone burning cash") is the single most interesting
                                 #   setting this knob has — a knob whose off-state collides with its best
                                 #   candidate value cannot be swept at all.
                                 #   UNFITTED. Suggested ladder: off / -10 / 0 / 5 / 10. -1e9 = off.
  growth_min_net_cash_rev: -1e9  # (P1d) reject a filer whose as-of (cash − debt) ÷ revenue is under this %.
                                 #   Same -1e9 off sentinel and the same reason as P1c: negative values are
                                 #   meaningful here (a levered balance sheet), so 0 is a legitimate bar.
                                 #   Revenue-scaled rather than EBITDA-scaled so loss-makers stay defined
                                 #   instead of None-ing straight out of the gate.
                                 #   UNFITTED. Suggested ladder: off / -100 / -50 / -25 / 0. -1e9 = off.
                                 #
                                 # (#169) 2026-08-30 PRE-REGISTRATION — WRITTEN BEFORE THE RUNS WERE READ,
                                 # as (#139) requires on the pit lane. The result is appended below it;
                                 # this half is not edited afterwards.
                                 #   WHY NOW: (#165) re-read (#159)'s held-loser table on a unit-free AUC
                                 #   and `net_cash_rev` is the ONE name it surfaced — AUC 0.60 at BOTH 12y
                                 #   and 8y, same direction (12y winners med 5.72 vs losers 3.48, n=58/7;
                                 #   8y winners med 3.35 vs losers -0.60, n=120/22). At 8y the MEDIAN LOSER
                                 #   CARRIES NET DEBT. Both other shapes for that name are already closed:
                                 #   as a tilt it fails the sweep's own both-halves-+ bar at both horizons
                                 #   (12y +349.5 OOS +0.42|-0.10, 8y +0.7 OOS -0.10|-0.06), and the banked
                                 #   "net_cash_rev 25%" refusal is a DIFFERENT INSTRUMENT — the SURVIVAL-GATE
                                 #   probe (backtest.rs:3275) drops the weakest 25 PERCENT of names, while
                                 #   this knob is an absolute % floor. The floor itself has never been graded.
                                 #   LADDER: off / -100 / -50 / -25 / 0. Exactly the five rungs written on
                                 #   the line above, before today. NO SIXTH RUNG. 0 is the a-priori bright
                                 #   line ("no net debt") — the config.rs:327 comment records that 0 is why
                                 #   the off-sentinel is -1e9 and not 0, so it is not a fitted value.
                                 #   GRADE: Ship Rule v2 unchanged. PRIMARY = top-10 excess, mean AND median,
                                 #   up-or-held at 12y AND 8y per (#139). Every 20y GUARD live: h2h >=50% on
                                 #   the 11-20 book at 20y with no not-worse bar; h2h on the 6-10 book at 12y
                                 #   and 8y at >=50% AND not below the control, per (#160); top-10 worst
                                 #   window not worse. GUARD > PRIMARY > TIE, and a broken guard REFUSES.
                                 #   METHOD: five overlay configs in tests/ (NOT $TMP — config::anchored_path
                                 #   takes the config's GRANDPARENT, so a $TMP probe silently moves the data
                                 #   root), each merging over this file, one knob apart. The `off` arm is the
                                 #   CONTROL and every comparison is probe-vs-control, never vs a fixture run.
                                 #   TRIM, stated so a later reader sees a decision and not a missing horizon:
                                 #   8y first for all five arms; 12y and 20y only for arms still alive on
                                 #   PRIMARY at 8y. A rung down at 8y is dead whatever its other columns say.
                                 #   READ-GUARDS: no FOLIOMAN_OFFLINE (it cuts scored windows 699->32); check
                                 #   `windows scored` on every run; read the POOL CENSUS before the top-N
                                 #   table and record the h2h n per (#151). A gate is POOL-CHANGING, so it
                                 #   still answers on a VACUOUS column where a ranking knob would not.
                                 #   FRAMING, fixed in advance: the AUC surfaced this name across the whole
                                 #   record, horizon-agnostically. That 7 of the 9 zero-return 12y holds are
                                 #   levered mortgage-credit names bought in 2007-08 is a COHERENCE CHECK,
                                 #   NOT THE JUSTIFICATION — fitting a gate to erase one macro event is the
                                 #   overfitting shape this file bans. An AUC does not authorise a gate; it
                                 #   says a threshold is possible. Ship Rule v2 decides.
                                 #
                                 # (#169) RESULT — REFUSED, ON A GUARD, AT EVERY RUNG. Runs 2026-08-30,
                                 # `backtest 8 universe fund pit` per arm, five overlays in tests/ one knob
                                 # apart. INTEGRITY: all five report `windows scored: 6126` identically, each
                                 # arm's heuristic_drift() WARNING names ITS OWN knob and no other, and the
                                 # control emits no WARNING at all (it restates the shipped -1e9).
                                 #                 top-10 excess     worst   rank-1 h2h (6-10 book)  median pool
                                 #   off (CONTROL)  +5.3 (med +5.6)   -5.5    19/27  70%             7
                                 #   -100           +5.6 (med +6.3)   -5.5    16/27  59%             7
                                 #   -50            +5.6 (med +6.4)   -5.5    16/27  59%             7
                                 #   -25            +5.5 (med +6.2)   -5.5    14/26  54%             7
                                 #   0              +6.0 (med +6.4)   -5.5    12/21  57%             5
                                 #   PRIMARY PASSES ON ALL FIVE — mean AND median up at 8y, every rung — and
                                 #   the worst window is -5.5 on all six columns, so that GUARD holds too.
                                 #   THE (#160) 8y GUARD BREAKS ON ALL FIVE. Every rung clears the absolute
                                 #   >=50% bar and every rung lands BELOW the control's 70% tally on the same
                                 #   6-10 book, which is exactly the not-worse clause (#160) added at 12y/8y.
                                 #   GUARD > PRIMARY, and a broken guard REFUSES OUTRIGHT — it is never
                                 #   softened to unresolved. So the ladder is dead at 8y and NO RUNG WAS RUN
                                 #   AT 12y OR 20y. That is the pre-registered trim firing, not a missing
                                 #   horizon: an arm that cannot pass 8y cannot pass, whatever 12y says.
                                 #   AND IT IS NOT A DENOMINATOR ARTIFACT, which is the first objection to
                                 #   raise given (#151). A gate thins the pool and the h2h denominator IS the
                                 #   census's non-saturated bucket count, so a gate mechanically shrinks its
                                 #   own guard's n — at rung 0 the denominator falls 27 -> 21. But at -100 and
                                 #   -50 THE DENOMINATOR IS 27, IDENTICAL TO CONTROL, and the count still
                                 #   falls 19 -> 16. Same book, same n, three head-to-heads lost. The
                                 #   degradation is real.
                                 #   WHAT THE GATE ACTUALLY DOES: it raises the mean and median of the top-10
                                 #   book while making the #1 pick less reliably better than the 6-10 names
                                 #   beneath it. That is a coherent effect, not noise — cutting levered
                                 #   filers lifts the basket's centre and flattens the ranking inside it —
                                 #   and it is precisely the trade (#160)'s clause was written to refuse.
                                 #   REVERT RULE: none to revert; the knob stays -1e9 and nothing shipped.
                                 #   Re-opening it requires a NEW measurement in which the 6-10 h2h is NOT
                                 #   below control at 8y. Re-running these five rungs is not that. A SIXTH
                                 #   rung is FORBIDDEN — the ladder was fixed in writing above before the
                                 #   runs, and hunting a threshold that clears the guard is (#120)'s argmax.
                                 #
                                 # (#169) ALSO CLOSES THE DIRECTION (#159) OPENED, since this was its last
                                 # live candidate. (#159)'s standing bar — "a new gate proposed from the
                                 # literature now needs to explain its gap in this table first" — was re-read
                                 # by (#165) on a unit-free AUC because the original ranking was in each
                                 # factor's own units. That re-read surfaced exactly ONE name, `net_cash_rev`
                                 # at AUC 0.60 on both horizons, and all three shapes for it are now closed:
                                 # TILT refused (fails the sweep's own both-halves-+ bar at 12y and 8y),
                                 # SURVIVAL-GATE refused (the percentile probe: +0.5 at 8y, FLAT at 12y),
                                 # ABSOLUTE FLOOR refused here on the (#160) guard at all five rungs.
                                 # `ebitda_yield`, the other name the AUC ranked top, is refused twice over
                                 # (below baseline at 8y, and the WINNER row that named it is an unstable
                                 # argmax — see (#168) at the sweep block). THE TABLE IS SPENT. The next gate
                                 # proposal needs a NEW measurement, not another re-reading of (#159).
  # ---- (P2)/(P3) ACCRUALS AND ASSET GROWTH: MEASURED, AND NEITHER EARNS A KNOB ----
  # (#124) 2026-08-23. There is no knob here to hang a receipt on, deliberately — this block IS the
  # receipt, and it records why `growth_max_accrual_gap` and `growth_max_asset_cagr` were NOT added.
  # docs/heuristic-v2-tasks.md P2 and P3 both say: STOP and run the standalone factor probe before
  # writing the gate. That probe needs no code — `report_book_by_factor` already prints a line for all
  # 20 registered factors, `accrual_gap` and `asset_growth` included, on any `fund` run. It was run:
  # `universe fund pit` at 20y/12y/8y via tests/diag-ci-settings.yaml.
  #
  #   asset_growth   20y n=0    12y n=0    8y n=44, excess +3.1 (below its peers in the same table)
  #   accrual_gap    20y n=0    12y n=52   8y n=108, excess +5.1
  #
  # (P3) asset_growth: the stop rule FIRES, unambiguously. n=0 means the factor built no book at all at
  # the two horizons the ship rule grades hardest. There is nothing to gate on. Do not write the knob.
  #
  # (P2) accrual_gap: the 12y row is DEGENERATE and must not be quoted as a win. Its n=52 line came back
  # byte-identical to three unrelated factors in the same table — same +16.6%/yr, same excess +5.0, same
  # win 100%, same worst +0.8, same OOS +4.7/+5.2. Four different factors cannot genuinely agree to the
  # decimal; they are selecting the SAME handful of names out of a pool too thin to separate them. Read
  # it as one pool, not four confirmations. The 8y row (n=108) is distinct, and it is the only real
  # reading either factor produced.
  #
  # AND THE EVIDENCE IS WEAKER THAN P1'S, STRUCTURALLY — say so rather than comparing the two. This is
  # `report_book_by_factor`, an ABSOLUTE held-book stat. It is NOT `sweep_fund_factor`, whose hardcoded
  # 14-name `FACTORS` array (src/commands/backtest.rs) omits `accrual_gap` and `asset_growth` along with
  # `fcf_margin`, `interest_cover`, `net_cash_rev`, `margin_stability`, `roic`, `quality`, `roe` and
  # `ebitda_yield`. So neither factor has a train/test held-out edge or OOS halves at all. Adding those
  # 10 names to `FACTORS` is the cheap follow-up IF either factor ever looks worth the trouble; on this
  # reading neither does, which is why that work was not done either.
  #
  # (#168) 2026-08-30 SUPERSEDES THE PARAGRAPH ABOVE, on its facts and not on its verdict. The "hardcoded
  # 14-name `FACTORS` array" no longer exists: `sweep_fund_factor` (backtest.rs:2228) and
  # `held_loser_factors` (:4810) both iterate the SAME `FUND_FACTORS` (:2138, 24 names), so all ten
  # listed omissions — `accrual_gap` and `asset_growth` included — DO now carry a train/test held-out
  # edge and OOS halves on every `fund` run. The cheap follow-up was done. What does NOT change is the
  # verdict: read the sweep rows for those two and they still refuse. Cite this correction, not the
  # paragraph above it, before claiming any factor is unswept — the claim is now false for all 24.
  #
  # REVERT RULE: this is a null result, not a threshold. Adding either gate requires a NEW run in this
  # file that beats these numbers — not an appeal to the literature, which is what put them on the list.
  # ---- (P4) PRICE-PATH GATES: two rejections, BOTH OFF, NEITHER MEASURED YET ----
  # Unlike the (P1) four above, these two are SWEEPABLE ON DAY ONE. They read `volatility_pct` and
  # `max_daily_1m`, both built by `core::backtest_quote` from the close series every run already walks,
  # and `growth_gate_reachability_pin` pins them LIVE for stock and ETF (INERT for crypto, by scope).
  # No `fund` lane, no as-of attach — a plain `backtest <h> universe` grades them.
  #
  # (#127) "SWEEPABLE ON DAY ONE" IS FALSE FOR BOTH, 2026-08-23. The paragraph above is right that
  #   neither knob needs the `fund` lane and right that both pin LIVE. It is wrong that a plain
  #   `backtest <h> universe` GRADES them, and the block contradicts itself: P4a's own paragraph
  #   below already carries the cadence ceiling that kills it. Two separate structural reasons, both
  #   read off `core::backtest_quote` rather than measured, because neither needs a run to settle.
  #
  #   P4a — `core.rs` fills `max_daily_1m = max_daily_pct(c, (cadence / 12).max(1))`. The 20y/12y/8y
  #   `universe` lanes walk MONTHLY (`cadence` 12), so that window is ONE BAR and the field stops
  #   being "the largest single-bar move last month" and becomes "that month's own return". core.rs
  #   states this in its own comment ("a monthly sweep of it is measuring something else and must not
  #   be pooled with a daily one"), and the run's caveat block agrees: "vol/MA are monthly-bar
  #   approximations". A monthly grid of this knob would produce numbers, and they would be numbers
  #   about a different signal wearing the same field name. THERE IS NO DAILY LANE AT THESE HORIZONS.
  #
  #   P4b — `volatility_pct` is passed through `daily_equivalent(v, cadence, rescale)`, and `rescale`
  #   is `vol_daily_equivalent` (#97), which is UNMEASURED and SHIPS OFF. So on the shipped config a
  #   monthly run's field is in per-MONTHLY-bar units while the suggested ladder (off/4/3/2.5/2) and
  #   the LIVE screen are both per-day — a ~√21 mismatch. core.rs already names this exact hazard:
  #   "every knob that reads this field is an ABSOLUTE threshold in per-bar % units ... and a
  #   threshold does not care that the ranks held". Fitting the ladder in monthly units instead would
  #   ship a value that means something else the moment the live daily screen reads it, which is the
  #   train==serve skew this file spends four other receipts avoiding.
  #
  #   STATUS CHANGE: both are BLOCKED, not merely unswept. P4b is blocked ON (#97) — grade that units
  #   fix first and P4b becomes gradeable behind it. P4a is blocked on there being no daily lane at a
  #   ship-rule horizon at all, which is not a knob problem and may never be worth fixing.
  #   DO NOT open either on the strength of the "SWEEPABLE ON DAY ONE" line above; it is superseded.
  #   The ladders below are left exactly as written — they are still the right ladders IF the lane
  #   ever exists to run them on.
  #
  # BOTH ARE UNFITTED JUDGEMENT VALUES — specifically, the OFF state. They are NON-CRYPTO on purpose:
  # coins already carry three tail knobs of their own, and a coin's ordinary week clears every rung
  # either of these would be set to, so an unscoped version would be a class filter wearing a threshold.
  # They overlap each other (a name with a +30% day usually has the vol to match), so the two survivors
  # must be run JOINTLY before either ships, and each rung must report its REJECTED cohort's forward
  # return against kept peers — the #24 standard, and the one that matters most here: cutting the
  # sharpest movers is exactly how a momentum lane can be made to look safer while earning less.
  # REVERT RULE for both: they are off; turning one on without a run in this file is the revert.
  growth_max_daily_1m: 0.0       # (P4a) reject a non-crypto name whose LARGEST single-bar move in the
                                 #   trailing month exceeded this %. The one tail the averages cannot see:
                                 #   `volatility_pct` divides a +30% session across the whole window and
                                 #   `max_drawdown_pct` only ever looks down, so a name can clear both while
                                 #   its month was one gap and twenty flat days.
                                 #   CADENCE CEILING, and a sweep must respect it: on a MONTHLY-cadence run
                                 #   the source window collapses to a single bar and the field becomes that
                                 #   month's own return — a different signal under the same name. Grade this
                                 #   knob on the DAILY lane only, and never pool a monthly arm with a daily one.
                                 #   UNFITTED. Suggested ladder: off / 25 / 20 / 15 / 12. 0 = off.
  growth_max_vol: 0.0            # (P4b) reject a non-crypto name whose daily-return stdev exceeds this %.
                                 #   The equity/ETF twin of growth_max_vol_crypto (3.0, shipped). That knob's
                                 #   comment says "equities never reach multi-% daily stdev, so no equity twin";
                                 #   that is true of ITS value and false of the concept — high-beta single names
                                 #   run 3-4%/day and small-cap biotech well past it — so THE EQUITY LADDER SITS
                                 #   BELOW THE CRYPTO ONE, and copying 3.0 across would ship an inert knob.
                                 #   Also unlike its crypto sibling it is not edge-blind: the pool is
                                 #   overwhelmingly equities, so a sweep here has the samples to vote.
                                 #   UNFITTED. Suggested ladder: off / 4 / 3 / 2.5 / 2. 0 = off.
  growth_require_lifetime_uptrend: true # (#25) reject equities whose WHOLE-LIFE log-trend CAGR is <= 0: moon-crash-partial-recovery
                                 #   names whose 10Y leg looks great but never reclaimed their long-run trend (the ranked CAGR uses
                                 #   the longest 20Y/10Y/5Y leg, so it can't see this). Same-batch pair 2026-07-03: off +115.7
                                 #   (wins +87.9) / on +119.4 (wins +91.5), OOS +0.13|+0.08 both + either way. CAVEAT: barely binds
                                 #   in the backtest (n=10 excluded, mean fwd +30.2 -> sweep prints TOO TIGHT) because the 12y pool
                                 #   is survivors-only — the dot-com-wreck cohort this targets is mostly delisted and never enters.
                                 #   Shipped on live-table semantics + measured-harmless; a strongly POSITIVE sweep row on a future
                                 #   maintenance run is the signal to re-question it. Crypto exempt. false = off.
                                 #   STILL NOT BINDING 2026-08-01: the `->off` row admits 0 new names at ALL THREE stress horizons,
                                 #   so it prints "gate not binding on this sample" rather than any flag. Every name it would reject
                                 #   is already rejected by something else. The re-question signal has not fired; it also cannot fire
                                 #   on a survivors-only pool, so this stays a live-semantics judgment gate, not a measured one.
                                 # (#184) THE SURVIVORS-ONLY EXCUSE IS NOW TESTED, AND RETIRED, 2026-08-31. Both notes above explain
                                 #   the null the same way: the gate barely binds "because the 12y pool is survivors-only -- the
                                 #   dot-com-wreck cohort this targets is mostly delisted and never enters", and the 2026-08-01 line
                                 #   goes further, that the re-question signal "cannot fire on a survivors-only pool". Every one of
                                 #   those runs was on `stress`. `pit` is the POINT-IN-TIME lane with as-of membership, which is
                                 #   precisely the pool that excuse claims does not exist -- so it can answer, and it was never asked.
                                 #   ASKED NOW, off the `(#183)` control runs at zero extra cost (the GATE SWEEP prices every gate on
                                 #   every run): `growth_require_lifetime_uptrend ->off admits 0 new names (gate not binding on this
                                 #   sample)` at 20y AND 12y AND 8y on `universe fund pit`. Identical verdict to the stress lane.
                                 #   AND THE COHORT IS DEMONSTRABLY PRESENT, which is what makes this a test rather than a repeat:
                                 #   the same 20y control prints its top-10 zero names as FMCC@b3995(-97%), FMCC@b3998(-95%),
                                 #   RIG@b4000(-98%), RIG@b4001(-96%), FNMA@b4003(-99%) -- near-total wrecks that not only enter the
                                 #   pit pool but reach the BOOK. The names this gate was written for are in the sample and it still
                                 #   rejects none of them, so the null is a property of the GATE, not of the pool's survivor bias.
                                 #   CONCLUSION: every name it would reject is already rejected by something else, on the honest lane
                                 #   too. The knob stays shipped `true` -- it costs nothing, and its live-table semantics are still
                                 #   the reason it is on -- but it must no longer be described as untestable or as awaiting a probe.
                                 #   NOTE the `->off` sweep row IS the false-arm: turning the gate off changes the admitted pool by
                                 #   zero names, so there was no separate arm left to run. NO ARMS WERE SPENT.
                                 #   REVERT RULE unchanged and now precise: a strongly POSITIVE sweep row (mean AND median, read `vs
                                 #   shipped`, not the bare sign) on a future maintenance run re-opens it. It has still never fired.
  growth_min_age_years: 0.0      # (#33) hard age floor on quote.age_years (whole-life listing age, the YRS column): reject a name
                                 #   younger than this many years. Shipped OFF (0.0) to DETECT EARLY WINNERS: a young strong grower
                                 #   (e.g. a fresh S&P 500 ETF, +21%/yr) still surfaces in the ranking, DOCKED by trust_factor (which
                                 #   already down-weights short-history names) instead of hard-cut — early-but-unproven, ranked below the
                                 #   proven compounders, not hidden. BACKTEST-BLIND either way: age_years is None in the backtest pool, so
                                 #   the gate never touches the validated edge; it only shapes the LIVE screen. Set >0 (e.g. 3) for a hard
                                 #   floor that trims the freshest listings entirely; pins always bypass (shown with a gate-review reason).
                                 # (#182) RETRACTION, 2026-08-31: the 'BACKTEST-BLIND either way' clause above is WITHDRAWN. It was
                                 #   never a property of the walk -- it was an unfilled field. `core::backtest_quote` already held the
                                 #   `[..=as_of]` date slice and read `life_cagr` off it; `age_years` was simply never assigned. It is now
                                 #   filled there by the SAME arithmetic `fetch.rs` uses at its own `age_years` binding, off the SAME
                                 #   splice-trimmed slice, so train==serve holds exactly as it does for `life_cagr`. This is the `(#3i)`
                                 #   mistake one field over, and the fix is the same fix.
                                 #   CONSEQUENCE: this gate is now GRADEABLE, and `growth_gate_reachability_pin` moved it INERT -> LIVE in
                                 #   all three lanes (crypto/etf/stock) in the same commit -- non-negotiable #3, done deliberately.
                                 #   The SHIPPED VALUE DOES NOT MOVE: still 0.0, still a no-op (`picks.rs` gates on `> 0.0`), and the
                                 #   `tests/fixture/*.golden` files did not move -- which also CONFIRMS the doc claims at `perf_fill`
                                 #   (`picks.rs`) and the `twin_groups` hint that those two `?`-bail sites are display-only: both woke and
                                 #   neither reached a golden. Grading of the ladder {3, 5, 10} is `(#183)`; until that lands this knob is
                                 #   MEASURABLE BUT UNMEASURED, not validated. Revert rule for THIS entry: none -- filling a field the
                                 #   walk already had is not a knob and has no arm to revert; if the fill itself is ever wrong, the pin
                                 #   and the goldens are the tripwires.
                                 #   ONE MORE THING THE GATE FOUND, worth the line because it generalises: with the days-to-years
                                 #   span written inline at the fill, replacing its `/` with `%` or `*` SURVIVED the whole suite.
                                 #   Nothing pins a field that is display-only while its gate ships 0.0 -- so the act of making a
                                 #   knob gradeable does NOT by itself make its input tested. The span is now `core::age_years`,
                                 #   read by five of the six sites that used to spell it out (the sixth, `capped_life_cagr`, is a
                                 #   documented equivalent-mutant exception on its own doc comment), and pinned by
                                 #   `age_years_is_days_over_365_25`. Gate after: 22 mutants, 18 caught, 4 unviable, 0 missed.
                                 # (#183) MEASURED, 2026-08-31, and the answer is NO. `backtest {20,12,8} universe fund pit`, the
                                 #   honest lane, Ship Rule v2 under the `(#139)` PIT substitution (PRIMARY 12y AND 8y; every 20y
                                 #   GUARD live). Ladder {3, 5, 10} pre-registered in the `(#182)` block ABOVE, before any run; all
                                 #   three rungs reported, none added after. Control run TWICE: every graded line byte-identical, the
                                 #   only delta in either file being `4777 from cache` vs `4778 from cache` on the fetch-provenance
                                 #   line -- so this lane IS reproducible at grading resolution and the deltas below are real.
                                 #   CONTROL  20y +0.6|+0.0 worst -10.0 | 12y +3.7|+3.9 worst -6.4 | 8y +5.3|+5.6 worst -5.5
                                 #            6-10 h2h 4/8 (50%) | 12/19 (63%) | 19/27 (70%);  11-20 h2h 1/2 (50%) | 2/4 (50%) | 5/8 (62%)
                                 #            census buckets 21/35/43, median gated pool 5/6/7, substitutable 7/16/21.
                                 #   age 3 -> INERT. NOT TESTED, never "held" (`(#178)`'s second cause). Every number at every
                                 #     horizon is byte-identical to control: census, median pool, substitutable count, top-10
                                 #     mean|median, worst window, and BOTH h2h books. A 3-year floor rejects nobody on this pool who
                                 #     is not already rejected by another gate. No arm was graded here, and none can be at this rung.
                                 #   age 5 -> REFUSED on the `(#160)` GUARD. Its PRIMARY actually PASSES (12y mean +3.7->+3.8 with the
                                 #     median held at +3.9; 8y both held at +5.3|+5.6) and its worst window HOLDS or improves
                                 #     (20y -10.0, 12y -6.4, 8y -5.5->-5.1). It refuses anyway, because GUARD > PRIMARY: the rank-1
                                 #     h2h on the 6-10 book falls 63% -> 59% (10/17) at 12y and 70% -> 64% (16/25) at 8y. Both clear
                                 #     50% in absolute terms; both are BELOW THE CONTROL'S TALLY, which is exactly the not-worse bar
                                 #     `(#160)` added at 12y/8y. A broken guard REFUSES outright and is never softened to unresolved.
                                 #     The mechanism is visible in the census: median gated pool 6->5 at 12y, substitutable 16->13,
                                 #     21->16 at 8y -- the gate thins the bench, so fewer buckets can even field an 11th name and the
                                 #     h2h denominators shrink 19->17 and 27->25 along with the tally.
                                 #   age 10 -> REFUSED on the WORST-WINDOW GUARD, at all three horizons, and not narrowly:
                                 #     20y -10.0 -> -17.8, 12y -6.4 -> -33.9, 8y -5.5 -> -9.6. Its PRIMARY looks spectacular
                                 #     (8y +5.3|+5.6 -> +6.8|+7.9, 12y +3.7|+3.9 -> +4.0|+6.2) and its 6-10 h2h reads 80% and 89%.
                                 #     ALL OF THAT IS BOUGHT BY STARVATION, and this is the arm the pre-registration existed to catch.
                                 #     Median gated pool collapses 5/6/7 -> 2/3/4 against a graded basket of 10, with ZERO
                                 #     substitutable names at every horizon: the "top-10" book is a FORCED top-2/3/4 with no bench.
                                 #     The 20y 11-20 h2h line is not printed at all -- the pool cannot form an 11-20 book, so that
                                 #     GUARD cannot be satisfied even in principle. And it is not like-for-like: bucket counts drop
                                 #     21->18, 35->32, 43->40, so the gaudy means are scored over FEWER windows than the control.
                                 #     A 10-year floor does not pick better names; it deletes the pool and reports the remainder.
                                 #   VERDICT: `(#33)` stays 0.0 and this closes it. Not one rung of a pre-registered ladder shipped:
                                 #     3 cannot move anything, 5 breaks a guard, 10 starves the book. The `(#33)` rationale above --
                                 #     keep early winners visible and let `trust_factor` DOCK them rather than hard-cut them -- now
                                 #     has a measurement behind it on the honest lane instead of only live-table semantics.
                                 #     REVERT RULE: re-question only if the pit pool's median gated pool rises ABOVE the graded
                                 #     basket (census stops printing VACUOUS), since every refusal above is entangled with a starved
                                 #     book. Re-running the same ladder on today's pool is spent -- do not re-grade it.
  growth_min_aum_etf: 100000000  # (AUM) ETF-only hard fund-size floor in EUR (quote.aum_eur, BF etp_search AUM, EUR-approximate —
                                 #   fund currency is EUR/USD mixed; ±10% FX is immaterial vs an order-of-magnitude line): reject a
                                 #   fund smaller than €100M, the industry viability line under which issuers liquidate/merge — the
                                 #   one way a 20y hold ends WITHOUT the strategy failing (a forced taxable exit + re-buy). Measured
                                 #   2026-07-05: median pond AUM €82M, 1791/3391 funds (53%) under this line. None-AUM names are NOT
                                 #   gated (missing data is not a small fund; BF only covers its venue — same stance as #33 age).
                                 #   BACKTEST-BLIND: aum_eur is None in the backtest pool -> gate inert there, validated edge
                                 #   untouched; live-only. 0 = off. Opt-in `aum` column (widths.columns) shows the number per row.
  growth_ter_drag: true          # (#34) ETF cost drag: dock the growth score by the ACTUAL 20-year wealth multiple the expense
                                 #   ratio eats, (1 - TER)^20. The TER is the one cost certain to compound against a decades hold, so
                                 #   two near-identical index ETFs (e.g. iShares vs Amundi Nasdaq-100) rank by NET return instead of
                                 #   momentum noise, and a 0.65% thematic ETF loses ground to a 0.10% broad one it only led on a
                                 #   reverting recent year. ETF-ONLY (expense_ratio is None for stocks/crypto -> x1.0). BACKTEST-BLIND:
                                 #   expense_ratio is None in the backtest pool -> x1.0 there, so the validated edge is byte-identical;
                                 #   this shapes only the LIVE ETF lane. false = off. (Magnitude: 0.10% TER -> x0.980, 0.65% -> x0.878.)
  growth_commodity_damp: 0.8     # (#44) COMMODITY dock: scale the growth score of a row whose earnings are a SPREAD on a
                                 #   traded input price — GICS Energy/Materials, or a fund whose name carries a commodity token
                                 #   (picks::is_commodity). Such a name's long CAGR is a spot-price snapshot, not a compounding
                                 #   record. 1.0 = off. The `c` rank flag prints either way; only the dock is knob-gated.
                                 #
                                 #   WHAT PROMPTED IT (live screen 2026-07-27, top-12 stocks):
                                 #     rank 1  CF Industries   Materials  CAGR +20%  maxdd -62%  R2 0.76  score 22.3
                                 #     rank 8  Marathon Petro  Energy     CAGR +19%  maxdd -81%  R2 0.68  score 10.1
                                 #   CF is a nitrogen producer (P&L = gas cost vs urea price); MPC is a refiner on the crack
                                 #   spread. They held the table's two LOWEST R2 and MPC its worst drawdown.
                                 #
                                 #   WHY NOT JUST READ R2, WHICH IS ALREADY A COLUMN: Amazon's R2 was 0.79 — BETWEEN the two.
                                 #   R2 measures the SYMPTOM (a lumpy climb) and cannot tell a commodity cycle from a dot-com
                                 #   crash. The sector names the CAUSE. Nor does the score already cover it: it prices lumpiness
                                 #   TWICE (growth_smoothness_weight 5.0, growth_underwater_weight 0.3, both swept) and CF
                                 #   cleared both and still ranked first.
                                 #
                                 #   THE ADJACENT NEGATIVE RESULT — the reason to hold this loosely. picks.rs records that (#1)
                                 #   an R2-steadiness damp @0.5 and (#3) a momentum-confirm damp @0.3 were BOTH measured
                                 #   edge-NEGATIVE (backtest universe ablation, 467 win: removing R2 lifted edge +45.6->+68.0,
                                 #   rho +0.18->+0.20) because "R2 docks exactly the parabolic compounders this lane exists to
                                 #   surface". A punish-the-lumpy-name damp has been tried here and it LOST. What differs: this
                                 #   docks a SECTOR, not a SHAPE, so it fires on 2 of 12 rows and never touches NVDA/AMZN — the
                                 #   cohort that killed the R2 version. That is an argument, not a measurement.
                                 #
                                 #   UNMEASURABLE BY CONSTRUCTION: `core::backtest_quote` builds every pool row from
                                 #   `Quote::stub(tk, "", "", tk)` — sector None (nothing outside screen.rs ever writes the
                                 #   field) and name == THE TICKER, not the fund's real name. So the damp is x1.0 in every
                                 #   walk-forward -> the validated edge is untouched AND this knob can NEVER be swept, unlike
                                 #   every weight around it. Same edge-blind class as growth_min_aum_etf/growth_max_vol_crypto.
                                 #   Proven exhaustively, not sampled: `quote_is_etf` falls back to `is_etf(&quote.name)`, and
                                 #   is_etf is a SUBSTRING match, so a ticker could in principle open the fund path. Ran the
                                 #   real predicate over both caches — 3551 backtest-pool tickers (.long_history_cache.json)
                                 #   and 8556 .isin_cache.json entries: 11 tickers DO satisfy quote_is_etf (the Polish
                                 #   "ETFB*.WA" / "TVBETETF.RO" family, which literally contain "etf"), but ZERO satisfy
                                 #   is_commodity — none carries a commodity TOKEN, and is_commodity token-splits where is_etf
                                 #   substring-matches. Both leak vectors closed over the WHOLE pool.
                                 #
                                 #   AND THE A/B THAT WOULD HAVE LIED: ran `backtest universe` at 0.8 and 1.0 anyway. They
                                 #   disagree — edge +58.3 vs +59.9, winsor +47.4 vs +49.0 — which reads as "the damp moved the
                                 #   edge" and is NOT that. `backtest universe` re-fetches the live universe per run and the
                                 #   venue feeds 504 at random: the 0.8 run lost Euronext Lisbon, the 1.0 run lost the Euronext
                                 #   ETF list, so they scored 4878 vs 4868 tickers over 8421 vs 8408 cutoffs. Different pools.
                                 #   A two-run A/B CANNOT prove inertness on this command — the pool noise is larger than any
                                 #   effect being tested. Anyone re-testing this knob: read the fetch header and the ticker
                                 #   count before believing a delta, on this row or any other.
                                 #
                                 #   WHY 0.8: it moves CF out of rank 1 (the thing that prompted the ask) without erasing it —
                                 #   CF 22.3 -> 17.84 (rank 1 -> 4), MPC 10.1 -> 8.08 (rank 8 -> 9). Both sit under EUR 1B
                                 #   turnover so liq_bonus is 0 and x0.8 on the pre-liq product IS x0.8 on the printed score.
                                 #   0.5 was rejected: it puts MPC on 5.05 against a growth_min_score of 5.0, i.e. one tick from
                                 #   vanishing — which defeats a flag whose whole purpose is to be SEEN. Set by judgement; if the
                                 #   ranks stop looking right, return this to 1.0 and keep the flag, which costs nothing.
                                 #
                                 #   ACCEPTED NOISE: Materials is ~50/50. CF/Mosaic/Freeport/Newmont/Nucor/Dow/Lyondell ride the
                                 #   cycle; Sherwin-Williams and PPG (branded paint), Linde and Air Products (industrial gas on
                                 #   15y take-or-pay), Ecolab, Vulcan/Martin Marietta (aggregates, local monopoly pricing) do not.
                                 #   Taken by direction: Energy-only would MISS CF, the rank-1 name this exists for, and the
                                 #   branded/contracted Materials names are slow compounders this momentum lane never surfaces.
  growth_fx_damp: 0.98           # (#45) FX/VENUE dock: scale the growth score of an ETF whose LIVE quote currency is not EUR
                                 #   (Yahoo `quote_currency` — GBp/GBP/USD/SEK/CHF lines all count; picks::is_noneur_etf).
                                 #   Prices what the ranking is otherwise blind to for a EUR buyer: broker FX conversion
                                 #   (~0.25% each way) plus the off-home-venue spread — a cost the EUR line of the SAME
                                 #   multi-listed fund does not carry. Currency, not country (user decision 2026-07-30): a
                                 #   EUR-quoted line on a non-eurozone venue costs no FX and is NOT docked. ETF-only, also a
                                 #   user decision — the stock lane is all-USD by nature, a uniform dock reorders nothing;
                                 #   crypto's -EUR legs already quote EUR. The `x` rank flag prints either way; only the dock
                                 #   is knob-gated, same split as `c`.
                                 #   WHY 0.98: tie-break strength, the user's pick over 0.95/0.90 — ~0.5-0.8% round trip
                                 #   amortized over a long hold is real but small, so it reorders near-equal scores (prefers
                                 #   the EUR twin) without burying a genuinely stronger foreign listing. TER is NOT this
                                 #   knob's job — growth_ter_drag above already prices the recurring cost.
                                 #   UNMEASURABLE BY CONSTRUCTION, same class as (#44): `quote_currency` is written only by
                                 #   the live fetch (fetch.rs chart meta); `core::backtest_quote` stubs leave it None, so the
                                 #   damp is x1.0 in every walk-forward — validated edge untouched, knob can never be swept.
                                 #   Set by judgement; if the ETF ranks stop looking right, return to 1.0 and keep the flag.
                                 #   LIVE EFFECT (screen A/B 2026-07-30, warm cache): IITU.L 8.4->8.2, XLKS.L 8.2->8.1,
                                 #   SEMI.AS 5.1->5.0 — note SEMI.AS is a Euronext AMSTERDAM line quoted in USD, the
                                 #   currency-not-country rule catching what a venue rule would miss. EUR rows (VVSM.DE,
                                 #   VUAA/SPYL/AUM5) and crypto identical; ranks unchanged at today's gaps (tie-break sized).
  growth_overext_floor: 0.05     # (#100 / finding C5) READ THIS FIRST: the number written here is NOT
                                 #   the multiplier delivered. `damp = combine_damps(&[trust, overext])`
                                 #   is a 2-slot GEOMETRIC MEAN, so it square-roots the floor. The
                                 #   documented tightening 0.2 -> 0.15 -> 0.05 actually delivered
                                 #   0.447 -> 0.387 -> 0.224: a fully-stretched name keeps 22.4% of base,
                                 #   not 5%. Every reading below is correct AS MEASURED and mis-stated in
                                 #   its units. `picks::growth_term_knob_flips` now pins the square root.
                                 #   STRUCTURAL LIMIT, unrelated to the value: damps are PROPORTIONAL, so
                                 #   they cannot reorder across bases. A 0.224 damp costs a base-30
                                 #   blow-off 9 points and a base-5 steady name 1.1 — the brake exists to
                                 #   demote parabolic names and structurally cannot demote one past a name
                                 #   with a much smaller base. An additive penalty could; that has never
                                 #   been graded and is not proposed here.
                                 #   AND THE SLOT COUNT IS A FREE PARAMETER: `picks.rs` records that adding
                                 #   or removing a CONSTANT-1.0 slot moved edge +98 -> +109. Wiring an
                                 #   inert term reorders the book, so `combine_damps`' arity is itself an
                                 #   unswept knob nobody declared.
                                 # growth-score multiplier at full stretch. Tightened 0.2->0.15->0.05, each step a harder blow-off-top brake:
                                 #   buying right after a parabola = poor long-hold entry. The walk-forward sweep ranks 0.05 best: wide
                                 #   5y rho +0.26->+0.28 AND OOS-late rho +0.09->+0.14 (durability up 55%) with edge flat (+108.5->+106.8).
                                 #   The old "0.1 docks NVDA out" worry was regime-bound — at 0.05 today NVDA still scores 6.7 > min_score
                                 #   5 (it's -10.6% off-hi, not parabolic) and the displayed order is unchanged. 1.0 = brake off.
                                 #   RE-FIT ON THE cagr-19 POOL 2026-08-01 (Phase B, pb-of*-*-s.txt): 0.05 HOLDS.
                                 #   THE FINDING IS THE SHAPE, not the winner — this knob has ZERO rank-1 authority. King
                                 #   rank-1 is byte-identical at EVERY value swept (0.02 / 0.05 / 0.10 / 0.20 all read
                                 #   +5.3 median, win 81%, h2h 60%; 0.02 is +5.5 mean vs +5.4, noise) while lane edge falls
                                 #   monotonically as the brake weakens: +684.4 / +677.4 / +631.6 / +553.8. It is a PURE
                                 #   LANE-EDGE lever, and growth_accel_weight is its mirror (pure rank-1, edge-costing).
                                 #   Tune the two independently — that orthogonality is why Phase B could ship accel
                                 #   without re-opening this knob.
                                 #   0.20 costs 18% of king edge and 19% at 8y (+58.4) for rank-1 that never moved —
                                 #   consistent with the ablation table (removing the brake outright = -457 at 20y).
                                 #   0.02 buys +7 at king (noise, inside the band) and SELLS 8% of 12y edge (+198.3 vs
                                 #   +215.1) and 5pp of 8y h2h. 0.05 is the balance point; 0.10 just loses 7% of king.
                                 #   NOTE the 0.10 king run first ABORTED (rc=134, `free(): invalid size` — glibc heap
                                 #   corruption, half the report lost mid-file). Re-run ALONE it exited clean and
                                 #   reproduced edge +631.6 EXACTLY, so the abort hit output teardown, not the maths.
                                 #   Same session had rustc SIGSEGV under memory pressure: environmental, not config.
  growth_turnover_weight: 0.0    # (L) ZEROED 2026-07-02 after a live A/B (screen at 0.5 vs 0.0): the ln(turnover/€1B) tilt is
                                 #   BACKTEST-BLIND (backtest quotes carry no turnover) and the A/B showed it does NOT do its
                                 #   claimed job — it's reward-only, so illiquid junk (ROX.DE €11K/day) sat in the ETF table
                                 #   WITH the tilt on; all it did was float mega-caps (AAPL/AMZN/TSLA) a few rows and swap
                                 #   AMD in over FTNT at the tail on turnover alone. A blind term that reorders with no
                                 #   validated edge gets neutralized (same rule as growth_value_weight). Thin listings are a
                                 #   GATE problem: use min_avg_turnover_eur (e.g. 1000000) to drop them outright. 0.5 = old tilt
  growth_overext_cap_crypto: 100.0 # (#4) crypto's own brake cap; =equity (no change). Raise (e.g. 200) to brake crypto later — unvalidated (crypto windows too sparse to isolate)

  # (4) Crypto market-greed damp from Bitcoin NUPL (already fetched for the screen footer). High NUPL
  # = whole-market euphoria/top -> shrink ALL crypto scores (thins the crypto tables in a frothy top,
  # fattens them after a flush). Applies to both lanes, crypto rows only.
  nupl_euphoria: 0.5             # NUPL above this starts damping crypto (~0.5 = greed/"belief" zone)
  nupl_damp_floor: 0.5           # crypto-score multiplier at NUPL 1.0 (peak euphoria); 1.0 = damp off
  nupl_capitulation: 0.25        # NUPL below this starts BOOSTING crypto (~0.25 = fear/accumulation); 0 = boost off
  nupl_boost_ceiling: 1.3        # crypto-score multiplier at NUPL 0 (deep capitulation); 1.0 = boost off. BACKTEST-BLIND judgment lever — keep mild
  crypto_max_mvrv: 1.6           # (#45) CRYPTO's valuation CEILING — reject a coin whose MVRV (market cap / realized cap)
                                 #   exceeds this. MVRV is the price the market pays over the price its holders actually
                                 #   paid; <1 = the whole market sits below its own cost basis. Printed in the new MVRV
                                 #   column, crypto tables only.
                                 #   IT IS NOT A PEG, and is deliberately NOT in the PEG column: realized cap has no
                                 #   earnings term, so MVRV reads as a P/B, not a P/E ÷ growth. Folding it into `peg`
                                 #   would recreate "two numbers, one name" — see the picks.rs `peg` cell receipt for
                                 #   what that cost last time (the tool cut APH at 2.02 in the run it ranked ODFL at 2.51).
                                 #   WHY 2.0 — AND NOTE THE VALUE ON THE LINE ABOVE IS 1.6. This paragraph argues a
                                 #   threshold that has not shipped since 2026-08-03; the (#64) block below records the
                                 #   hand edit, what the disagreement costs, and why the stricter number stands. Read
                                 #   them as one receipt — this half is kept because it is still the argument.
                                 #   IT IS NOT A NEW JUDGEMENT. `nupl_euphoria: 0.5` above already declares
                                 #   where crypto greed starts, and NUPL = 1 - 1/MVRV, so NUPL 0.5 IS MVRV 2.0. The damp
                                 #   and this ceiling now share ONE threshold. Consequence, accepted deliberately: a coin
                                 #   above 2.0 is both score-damped AND rejected, i.e. one fact charged twice. The
                                 #   alternative was two numbers that both mean "expensive" and drift apart.
                                 #   DO NOT LOWER TOWARD 1.0. Below ~1.0 the ceiling inverts into dip-buying — it would
                                 #   cut BTC (1.19 live) and keep the most bled alts (ICP 0.28, CRO 0.26) — and the
                                 #   `discount_weight` receipt records that the walk-forward backtest found deepest-dip
                                 #   ranking BACKWARDS. At 2.0 nothing in the live top 100 is cut (dearest is XLM 1.37),
                                 #   so this ships INERT and bites in the next bull. That inertness is the design.
                                 #   SOURCE: CoinMetrics community tier, `CapMVRVCur`, daily. Two requests per run and
                                 #   the first is mandatory: the timeseries endpoint 400s the ENTIRE batch over one
                                 #   unsupported asset (measured on cc, pyusd, rlusd, usde), so the catalog is fetched
                                 #   first as an allowlist. It must be the /v4/catalog/ path, NOT /v4/catalog-all/ —
                                 #   the latter lists pro-tier assets and re-admits the ones that break the call. Ids
                                 #   are the bare lowercase symbols, so there is no id table to join and keep current.
                                 #   TWO URL DETAILS THAT ARE LOAD-BEARING, both found by a live run returning n/a for
                                 #   BTC ITSELF. (1) `limit_per_asset=1`: the series runs oldest-first from 2011-12-31
                                 #   and the endpoint has no `reverse` param, so without it the page is spent on 2012
                                 #   rows and no current value survives. (2) A DATE CHECK IN CODE on every row, because
                                 #   `limit_per_asset=1` returns each asset's LAST row — for an asset CoinMetrics
                                 #   STOPPED covering that is a fossil, not a quote: bnb ends 2019-04-22 at 1.88 and dot
                                 #   ends 2022-06-03 at 0.52, and both come back looking exactly as current as btc's.
                                 #   Rows older than 10 days are dropped, so a dead-coverage coin reads as no data (and
                                 #   passes free) instead of being gated on a seven-year-old valuation.
                                 #   VALIDATED AGAINST THE TOOL'S OWN FOOTER: BTC MVRV 1.19 -> NUPL 1 - 1/1.19 = 0.160,
                                 #   against a live `Bitcoin NUPL: 0.166` line the same run. Two independent sources,
                                 #   same quantity — this knob is the per-coin form of a gauge already shipped.
                                 #   COVERAGE IS THIN AND THE GATE IS BUILT AROUND THAT: 17 of the live CoinGecko top
                                 #   100 non-pegged coins carry a value. Missing MVRV PASSES (house missing-data rule).
                                 #   Misses include BNB, SOL, TRX, AVAX, SUI, HBAR, NEAR, XMR — and BNB is currently
                                 #   RANK 1 of the crypto table, so today the printed table shows one n/a and one value.
                                 #   The screen prints the hit rate each run ("Crypto valuation: N of M coins ...").
                                 #   UNSWEEPABLE, and NOT for the usual reason. The data has daily history to
                                 #   2011-12-31, so it is not backtest-blind for want of a past value; the backtest POOL
                                 #   growth-scores zero crypto rows at every horizon (20y-s 0/4633, 12y-s 0/11758, 8y-s
                                 #   18 cutoffs with 0 scored). No sweep can vote on any crypto knob, this one included.
                                 #   `growth_gate_reachability_pin` reads it INERT for all three classes: `backtest_quote`
                                 #   never fills `quote.mvrv`, so it is cause (a) — field never filled. 0 = off (default).
  # (#64) THE SHIPPED VALUE IS 1.6, NOT THE 2.0 EVERYTHING ABOVE ARGUES FOR. Recorded 2026-08-10,
  #   seven days late. It was set by hand on 2026-08-03 in d58db7a — a commit about wiring a 2Y CAGR
  #   rung — alongside two gates that were switched OFF the same way. That commit's own message named
  #   all three as "set by hand and NOT graded by any run above", and said "two of the three carry
  #   same-file receipts arguing the other way". This is the third. The other two are graded and
  #   answered in their own blocks; this one cannot be graded and so gets prose instead.
  #   WHAT IT COSTS TO BE AT 1.6. The "WHY 2.0" argument above is an argument for AGREEMENT, not for
  #   2.0 as such: `nupl_euphoria: 0.5` marks where crypto greed starts, NUPL = 1 - 1/MVRV, so NUPL
  #   0.5 IS MVRV 2.0, and at 2.0 the damp and the ceiling were ONE threshold. At 1.6 they are two.
  #   The thing that block set out to prevent — "two numbers that both mean expensive, drifting
  #   apart" — has now happened, and 1.6 is the drift. That is the honest cost and it is not paid for
  #   by any measurement.
  #   WHY IT STANDS ANYWAY. It is STRICTER, and every stated risk on this knob is a LOOSENING risk:
  #   "DO NOT LOWER TOWARD 1.0" guards the inversion into dip-buying, and 1.6 moves away from 2.0 but
  #   is still 0.41 clear of BTC's live 1.19 rather than the 0.81 it had at 2.0. Reverting to 2.0
  #   would LOOSEN a ceiling on judgement alone, in the same round that TIGHTENS two other gates on
  #   evidence. Two opposite directions in one commit, neither measured, is worse than one documented
  #   inconsistency.
  #   IT IS INERT TODAY, WHICH IS WHY THE CHANGE WAS INVISIBLE. The live gate funnel carries NO mvrv
  #   row at all (2026-08-10): zero coins blocked at 1.6, exactly as the block predicted for 2.0
  #   ("nothing in the live top 100 is cut ... so this ships INERT and bites in the next bull").
  #   Both values are inert on today's pool, so nothing observable distinguishes them and the hand
  #   edit could not have been caught by looking. Coverage is the reason: the run prints "19 of 77
  #   coins carry an MVRV (the rest pass the ceiling free)", and missing MVRV PASSES by house rule.
  #   NO RUN CAN EVER SETTLE THIS, and that is measured, not assumed: 0 crypto rows are growth-scored
  #   at every horizon (20y-s 0/4633, 12y-s 0/11758, 8y-s 18 cutoffs with 0 scored), so the arms this
  #   round spent on the two sibling gates would have been wasted here. Settled the way its three
  #   siblings were — `max_1m_drop_pct_crypto`, `growth_maxdd_cap_crypto`, `growth_max_vol_crypto` —
  #   by live screen eyeball plus a receipt, which is the most this class of knob can ever have.
  #   TO REOPEN: the next bull, when the ceiling stops being inert. At that point the 1.6-vs-2.0 gap
  #   becomes observable in the funnel for the first time, and the coins landing between them are the
  #   evidence this block has never had. Until then, do not spend runs on it.

  # RISK-ADJUSTED-RETURN tilts (B/C) — Sharpe ratio (return ÷ volatility) and Calmar ratio (return ÷
  # worst peak-to-trough drawdown), from the SAME closes already fetched, ZERO extra network cost.
  # Reward a calm, shallow-drawdown compounder over a knife-y one with the same headline CAGR.
  # (The old R²-smoothness "consistency" damp was deleted: it read pure edge-harm with no rho gain —
  # disabling it had nearly DOUBLED wide 5y edge +50.5->+95.6. Past path-smoothness doesn't predict return.)
  sharpe_weight: 0.05            # (B) GROWTH lane: reward per unit CAGR/volatility. History: 0.3->0.15 halving was peak edge
                                 #   PRE-smoothness. Re-swept 2026-07-04 AFTER growth_smoothness_weight shipped: trend_r2 carries
                                 #   the same calm-compounder signal, so Sharpe's marginal value shrank — 0.05/0/0.10 plateau at
                                 #   edge +148.0/+147.0/+145.8 vs +137.2 at 0.15 (same batch, rho +0.13 flat). 0.05 = measured max;
                                 #   kept NON-zero deliberately: the ETF lane (absent from backtest) leans on the Sharpe leg for its
                                 #   risk reward and the sharpe_cap_etf fix, so zeroing would be an unmeasured live ETF regression. 0 = off
                                 #   (#62) THE 0.15 -> 0.05 CUT'S PREMISE IS NOW STALE, so do not read 0.05 as a settled optimum:
                                 #   the re-sweep above cut this weight because `growth_smoothness_weight` "carries the same
                                 #   calm-compounder signal", and that knob was ZEROED on 2026-08-10 (see its block). The whole
                                 #   0.05/0/0.10-vs-0.15 plateau was measured with smoothness at 5.0 and has never been repeated
                                 #   without it. Re-sweeping this is the obvious next run; it is an ADDITION, so it owes the
                                 #   addition ship rule (rank-1 must RISE, not merely hold), and 0.05 stands until it clears that.
                                 #   (#70) MEASURED INERT AT THE SHIPPED VALUE, which sharpens what that re-sweep is asking.
                                 #   Read off the (#63)/(#70) wide runs: this term's ablation — the whole knob to ZERO — moves
                                 #   rho by Δ-0.00 at all three horizons and edge by Δ +0.0 / +2.7 / -5.1, every one of those
                                 #   far under its own horizon's (#60) null floor (93.3 / 15.4 / 16.0), i.e. under what deleting
                                 #   an information-free CONSTANT costs there. At 0.05 this term is carrying nothing the wide
                                 #   backtest can see. That does NOT settle the sweep — inert at 0.05 is not inert at 0.15, and
                                 #   the ETF lane it was deliberately kept nonzero for is absent from `backtest` entirely — but
                                 #   it does mean the open question is whether to RAISE this knob, never whether it is currently
                                 #   earning its place. Nobody needs to spend runs re-confirming that it is quiet today.
                                 #   (#178) THE RAISE QUESTION (#70) LEFT OPEN IS NOW ANSWERED: NO. (#70)
                                 #   ablated this to zero, found the delta under the null floor, and said "the
                                 #   open question is whether to RAISE this knob, never whether it is currently
                                 #   earning its place." 0.05 -> 0.15 on `universe fund stress` (non-vacuous at
                                 #   both horizons; see the (#178) block on `growth_value_weight`) fails PRIMARY
                                 #   at 8y — top-10 mean +6.9 -> +6.8 — with the 20y column BYTE-IDENTICAL, the
                                 #   worst window unchanged at both, and both h2h books drifting DOWN rather
                                 #   than up (20y 11-20 63% -> 58%, 8y 11-20 58% -> 55%, 8y 6-10 flat at 68%).
                                 #   Nothing bought, so nothing to trade for the loss. REFUSED, DEAD PERMANENTLY
                                 #   — a refusal on the survivor-flattered lane is binding. 0.05 STANDS.
  onsale_sharpe_weight: 0.0      # [FOIL] (B) ON-SALE lane's own Sharpe weight, split from growth because the shared knob conflicted:
                                 #   ablation showed growth wants 0.15 but on-sale wants 0 — zeroing it lifts the on-sale 12y edge
                                 #   +39.2->+62.9 (Δ+23.7) while growth keeps 0.15. 0 = off (the swept on-sale optimum)
  sharpe_cap: 15.0               # (B) cap that CAGR/volatility ratio
                                 #   (#100 / finding C3) THIS TERM IS VERY NEARLY A CONSTANT ON THE LIVE
                                 #   PATH, and the receipt should say so. At a typical 1.3-1.5%/day
                                 #   large-cap vol this cap binds above ~20%/yr CAGR — i.e. above
                                 #   `growth_min_cagr: 19.0`, the bar every scored name has already
                                 #   cleared. So `risk_reward` contributes a fixed number for nearly the
                                 #   whole live pool and ranks nothing, while `calmar_weight: 0.0` makes
                                 #   the other half of the term dead code.
                                 #   It is also TRIPLE-COUNTED: `sharpe`, `calmar` and the trend term are
                                 #   all affine in `long_cagr`, and the vol/maxdd denominators make the
                                 #   marginal reward for compounding depend on the asset's VOLATILITY —
                                 #   which is not what any of the three claims to measure.
                                 #   THE UNITS HALF IS ALREADY BUILT: `vol_daily_equivalent` (#97) is the
                                 #   "annualise the vol so the ratio lands in a discriminating range" fix,
                                 #   and its receipt records that this cap must be RE-SWEPT underneath it
                                 #   rather than carried across. Deleting the term and its two caps is the
                                 #   other honest option and has never been graded.
                                 #   UNMEASURED either way; nothing here moves until that paired run.
  sharpe_cap_etf: 9.0            # (#37) ETF-only Sharpe cap: cross-listed lines of the SAME fund print different daily
                                 #   stdev (NASD.L 1.8%/day vs CSNDX.SW 1.1% for identical Nasdaq-100 holdings — thin-line
                                 #   prints + FX conversion), so ETF CAGR/vol above ~9 measures listing-line noise, not
                                 #   fund risk. Uncapped, that noise (0.15×Δsharpe ≈ 0.7 score) outvotes the real TER
                                 #   difference (2.2% over 20y) and ranks the COSTLIER identical wrapper higher. 9.0 ≈ the
                                 #   noise floor observed across same-index wrapper trios. Stocks keep sharpe_cap.
                                 #   CORRECTED 2026-08-02: "the backtest pool holds stock constituents only" was FALSE — the
                                 #   pool is ~4311 ETFs of 4954 tickers. The cap read inert only because backtest_quote left
                                 #   instrument_type/name blank so quote_is_etf never fired (see the ETF-CLASSIFICATION
                                 #   receipt at the head of this file). Since the class stamping this cap DOES apply in the
                                 #   backtest, at whatever ETF density the horizon carries: 20y-s 14 ETF cutoffs of 4633,
                                 #   12y-s 2624 of 11758, 8y-s 8650 of 20575. 0 = off
  calmar_weight: 0.0             # (C) ZEROED. Calmar (CAGR/max-drawdown) is harmful at the 12y horizon: 1.0->0 lifts GROWTH
                                 #   edge +92.7->+98.0, rho +0.06->+0.07, top-half +4.6->+7.3 — all three improve. Over a 20y hold the
                                 #   deepest-drawdown names are often the biggest compounders; penalizing path-pain deselects them.
                                 #   sharpe (0.15) stays — removing it COSTS edge (Δ-3.1), so volatility-awareness still helps. 0 = off
  calmar_cap: 2.0                # (C) cap that CAGR/max-drawdown ratio

  prefer_eur: true               # dedup currency twins (BTC-EUR/BTC-USD): keep EUR leg if true, else USD
  growth_require_peg: true       # require a PEG to score a name. The growth_score reads PEG (1/PEG x 100) and the gate rejects PEG > growth_max_peg, so a missing PEG is a free pass. This knob forces a missing PEG to fail the gate. Reads `fund.eps_never_reported` (filer-level, never as-of), NOT `peg_yield.is_none()` — the cohort is filers whose 10-K states no per-share element at all, 8 of 509 cached US names, seven of them now FIXED by the XBRL-instance fallback. Residue today: ARES alone. false = off

# --- live-universe fetch breadth + pacing (so the backtest gate scans the same wide pool `screen` does) ---
# How many symbols PER CLASS (crypto + stocks/ETFs) `screen` pulls from the live sources
# (coingecko_markets + sp500_csv URLs below). Bigger = more to scan = slower (one history
# fetch per ticker, in parallel) + more rate-limit risk. `screen TICKER...` overrides everything.
universe_size: 1234000

# Max fetches in flight = CPU cores × this multiplier. In-flight memory holds the large 10y + monthly
# chart payloads, so this is the MEMORY/stability knob, not the speed knob (the pacer below sets speed).
# 8 cores × 8 = 64 was stable across full 3847-name screens; conc=128 (mult 16) got SIGKILLed mid-run.
# Keep cores×mult ≈ 64: on a 16-core box use mult 4, on 4-core use 16. 0 is treated as 1.
fetch_concurrency_multiplier: 8

# Global outbound-request pacer (requests/second) — THE speed knob. Spaces request LAUNCHES so the
# fan-out can't burst-429 (concurrency bounds in-flight count but not the launch burst). Yahoo's limit
# is undocumented. Measured at full universe (3847 names, ~3 Yahoo calls/name): rps 60 = 0 valid names
# lost in 4m27s; rps 40 = 0 lost in 6m37s. 60 nearly saturates conc 64, so higher rps needs more
# concurrency (which risks the memory kill above) for little gain. Lower if you see real names drop,
# 0 disables pacing (a 64-wide burst then drops valid names — e.g. NVDA — so don't).
fetch_requests_per_second: 60.0

# Worker threads for the CPU-bound half of `backtest` — the per-ticker walk, the knob sweeps and the
# `tune` search. NOT the two knobs above: those size in-flight NETWORK requests and want to be far
# larger than the core count, this one sizes actual compute and wants to be at most it. 0 (the
# default, and what CI uses) = every logical core; `RAYON_NUM_THREADS=n` still overrides for one run
# whenever this is 0. Set it to cap FolioMan while you use the machine for something else.
# It must never change a printed number — the walk collects in ticker order and every seeded PRNG
# stream stays serial — and tests/backtest_fixture.rs pins that: the goldens are generated
# single-threaded and must reproduce byte-for-byte at any setting.
compute_threads: 0

# Quote the live-universe crypto in EUR (BTC-EUR) if true, else USD (BTC-USD). Mirrors
# buy_heuristic.prefer_eur but for what `screen` fetches. US stocks/ETFs have no EUR listing.
universe_prefer_eur: true

# (#76) EQUITY VENUE SWAP — replace a constituent-CSV stock with its Xetra twin (GOOGL -> ABEA.DE) so a
# EUR investor is shown the line they can actually buy, priced in the currency they actually pay. BUILT
# AND WIRED 2026-08-15, SHIPPED OFF. Off is byte-identical to no swap at all (goldens unmoved), so this
# `false` is the shipped behaviour, not a CI-only override.
#
# WHY AN IDENTIFIER SERVICE AND NOT A SUFFIX. The German ticker is genuinely a different string: AAPL is
# APC.DE, KO is CCC3.DE, V is 3V64.DE, CAT is CAT1.DE. A naive `AAPL.DE` returns no chart at all. Yahoo's
# own search cannot do the job either — `?q=GOOGL` returns a 2x ETF, options and a Korean ETF but no
# European venue; `?q=AAPL` returns Thai DRs, a Brazilian DRN and an Argentine CEDEAR, none of which are
# the same security. Resolution is OpenFIGI (Bloomberg's keyless open service), two hops:
# TICKER+exchCode US -> shareClassFIGI -> every listing of that share class, then the `GY` (Xetra) row
# with securityType `Common Stock`. Measured 2026-08-15: 10 of 10 S&P names resolved to a Xetra ticker
# whose Yahoo chart came back EUR, exchangeName GER, 219-320 monthly bars, zero nulls. Cost is 10 jobs
# per request unkeyed (20 returns HTTP 413), 25 requests/min, two hops over ~500 names = ~102 requests
# paid once and cached forever in .eu_listing_cache.json.
#
# WHAT TURNING IT ON COSTS, ALL SEVEN STATED PLAINLY:
#   1. It re-prices the entire equity lane. A Xetra line's avg_turnover_eur is a fraction of NASDAQ's,
#      and turnover feeds liq_bonus and the rank tie-break, so every US name's score moves.
#   2. The CAGR becomes EUR-denominated and carries EUR/USD drift. growth_min_cagr and every threshold in
#      this file were calibrated on USD series. From a EUR investor's view the EUR CAGR is the CORRECT
#      number; it still invalidates the calibration.
#   3. Xetra history starts 2007-12-31 for nearly every name — 18.6y, under the 20Y rung. Harmless at a
#      fixed_cagr_years of 8 or less, but on the default 20/8/5 ladder every swapped name drops a rung and
#      its CAGR changes for that reason alone. trust_factor's 10Y requirement still clears.
#   4. train != serve against the backtest, structurally. Both lanes read this same knob, so they agree
#      with each other, but the frozen fixture pool is US/USD and no Ship Rule run can grade the swap.
#   5. Owned matching breaks. Owned.stocks matches by yahoo_base, and yahoo_base("ABEA.DE") == "abea"
#      will not match a held GOOGL_US_EQ, so owned names stop being flagged until that match goes
#      through ISIN.
#   6. A PINNED US TICKER CANCELS THE SWAP FOR ITS OWN NAME. Found on the first real run (2026-08-16), by
#      a NVIDIA row that printed NVDA/USA with the knob on. Nothing failed: NVDA resolved to NVD.DE, the
#      EUR/XETRA chart was fetched (225 bars), and then `screen` re-appended the watchlist VERBATIM after
#      the pond was already swapped, putting the US leg back beside the twin that had replaced it. Both
#      carry the byte-identical Yahoo name "NVIDIA Corporation", so the dual-class collapse in `ranked`
#      fires — and it exempts pinned tickers, so the US leg survives and the EU one is deleted. Which leg
#      sorts first is decided by trust_factor: at fixed_cagr_years 20 NVDA's 332 bars clear the 20Y rung
#      at 1.0 while NVD.DE's 225 do not and take the flat 0.5, i.e. cost 3 above is what settles it. Both
#      rules are deliberate and neither was changed; the interaction is now REPORTED on stderr
#      (`eu_shadowed_pins`) instead of being silent, since rewriting a ticker the user typed on purpose is
#      worse than naming the shadow. The fix is the user's: drop the US pin, or pin the EU symbol too.
#   7. IT ORPHANS EVERY SWAPPED NAME FROM ITS SEC FILINGS — found 2026-08-16 on the same run, and the
#      largest cost of the seven by a wide margin. The SEC is keyed by US ticker (`sec_cik`), and the swap
#      renames a constituent BEFORE any fundamentals fetch, so `NVD.DE` has no CIK, no company-facts, no
#      TTM EPS roll and no Form-4s. Every SEC-derived cell for that name goes n/a AT ONCE: P/E, PEG,
#      ROE/A, REV-YoY, EPS-YoY, NET% and BUYBK. The run said so and it was misread as thin coverage —
#      `fund tilt (peg_yield): 222 of 549 stocks carry the factor`, against 266 constituents swapped.
#      FIXED (round 78) rather than accepted: `sec_cik` now looks the ticker up through an EU->US alias
#      inverted from the resolver's own cache (`invert_eu`), so a swapped name reaches its filer's CIK.
#      The FX discipline this needs was already there — `fetch_ratios_sec`/`enrich_fund_factor` put the
#      native close into the filer's reporting currency before dividing (the ASML-ADR case), so a EUR
#      close over a USD EPS was never the problem. Cost 3 above is the one that CANNOT be fixed this way:
#      short Xetra history is a property of the listing, not of a lookup key.
#
# FIRST REAL RESOLVER RUN (2026-08-16, the run this knob's rollout was waiting on): the stderr tally
# reads `338 of 604 constituents on Xetra, 266 kept on their US line` (56.0%). Read the denominators
# before comparing: 604 is the pond list WITH cross-pond duplicates, while the cache behind it holds 517
# UNIQUE symbols, of which 266 resolved and 251 are remembered misses. The 10-of-10 probe above was not
# representative — it sampled mega-caps, and roughly half the pond has no Xetra common line at all. That
# miss rate is now a number rather than a guess, which was the precondition for the venue-ladder question
# (NA/FP/IM/SW beyond GY) that was deliberately deferred when this shipped.
#
# MUTATION AUDIT (round 76), CI's targets exactly (--lib --test backtest_fixture, no --test cli):
# 83 mutants, 79 caught, 1 unviable, 3 missed. Two of the survivors were real and are fixed: the stderr
# swap tally computed inside its own `eprintln!` (extracted to `eu_swap_tally` — an inverted count would
# have misreported the very Xetra hit rate this rollout is meant to be decided on), and the whole-
# function stub on `fetch_universe`, whose only test asserted the TOTAL-OUTAGE case and so was satisfied
# perfectly by a universe builder that had stopped building.
#
# THE OTHER THREE WERE SHIPPED AS "DELIBERATELY UNPINNED" AND THAT WAS A MISTAKE (round 77). They were
# one comparison — the `i > 0` request pacer in `resolve_eu_listings` surviving `==`, `<` and `>=` —
# left alive on the reasoning that all three move only WHEN the sleeps happen, never what the function
# returns or writes, so killing them needs a flaky wall-clock assertion costing real suite time. That
# reasoning priced the LOCAL cost correctly and ignored the gate's shape: `.github/workflows/ci.yml`
# ends with `if [ "${missed}" -gt 0 ]; then ... exit 1`, and there is no allowlist anywhere in it.
# "Accepted survivor" is not a concept the CI gate has, so shipping three was shipping a permanently red
# main — which is exactly what run 31934809294 reported (`MISSED src/fetch.rs:3803:14: replace > with
# >= in resolve_eu_listings`).
#
# THE LESSON, and it generalises past this knob: a local audit may not accept a survivor, because the
# thing that decides is a gate with no allowlist. Either kill it or `#[mutants::skip]` the function.
#
# The fix was to DELETE the comparison, not to test it. Hoisting it into a testable helper would not
# have worked: every usize comparison against zero has a semantically equivalent partner (`i > 0` is
# `i != 0`, `i == 0` is `i <= 0`), so an unkillable mutant follows the comparison wherever it is moved.
# The pacing delay is now carried in a `pace` variable that starts at zero — same behaviour, one line
# shorter, no operator left to mutate, no test and no suite time bought. Round 77 also removed a second
# sleep at the END of each hop-2 chunk, which had been spacing that hop's requests 5s apart against a
# 2.4s rate limit: ~2 minutes of pure waiting on a cold ~52-chunk resolve.
#
# UNGRADEABLE BY THE BACKTEST — no fixture carries a Xetra twin, so no golden can move either way, and
# no horizon can be quoted for it. The resolver prints its hit/miss tally to stderr on every real screen.
# A name whose twin fails to resolve, is not on Xetra, or whose chart is not EUR with at least 60 bars
# keeps its US row — the pond never silently shrinks.
#
# (#114) ON, and the receipt is HONESTY ABOUT WHO WAS ALREADY RUNNING IT rather than a measurement.
# This sat at `false` while BOTH live lanes — the author's terminal overlay and the Pages overlay —
# set it `true`, so the committed "off" described nothing that actually ran. Collapsing the two
# overlays onto this file (see the header note on `widths` below) forced the question, and the answer
# that keeps ONE lane is to commit the value the lanes were using. UNMEASURED at every horizon.
#
# (#116) CORRECTED. The sentence that stood here said this knob "changes which VENUE a live row is
# fetched from, never a backtest score". THAT WAS FALSE, and it is why this flip shipped as a
# display-only change with no horizon measured: `backtest`'s wide path handed this very setting to
# `fetch_universe`, so the walk-forward gate — which pins THIS FILE as its config — graded whatever
# venue the knob selected. Turning it on swapped 266 of 519 S&P constituents onto Xetra twins that are
# a MEDIAN 18.6 YEARS YOUNGER than their US primaries (218 of 261 by more than five years), and since
# a cutoff needs history behind it PLUS the forward window, the 20y lane keeps almost none of them
# while the 8y lane fills with them. CI run 32456448769 read that as a scoring collapse: 20y edge
# +775.8, 12y +58.2 (about half its ~117 baseline), 8y -22.8 — RED, on a universe change no line of
# the diff mentioned. The last wide run before the flip (.backtest_verdict.json, 2026-08-18) has 8y at
# top-3 excess +6.4 pts/yr over 53 windows with both OOS halves positive.
#
# The call site now passes a hardcoded `false`, so the corrected claim is true BY CONSTRUCTION rather
# than by review: the live lanes read this knob, the backtest cannot. The test
# `the_backtest_never_reads_the_venue_knob` (src/commands/backtest.rs) counts the mentions in that file
# and fails at one. Every threshold in THIS file was fitted on the US lines, which is the sample the
# backtest still grades.
#
# REVERT RULE: set `false` if a real screen's stderr tally shows the swap shrinking the pond or
# promoting a thin Xetra line over a liquid US one. That tally is the only evidence this knob has —
# and it is a LIVE-side tally, because no horizon can be quoted for a knob the backtest never reads.
prefer_eu_listing: true

# (#191) MEASURED AND REFUSED 2026-09-01 — the venue skew is REAL, it is NOT the admission
# bottleneck, and correcting it makes the live book SMALLER. Two `screen` runs, one boolean apart,
# overlays merging over this file, the watchlist pinned in both so each arm mirrors the real screen.
# Both arms read `4790 resolved to Yahoo tickers (4790 from cache)`, so the pond is one experiment.
#
# THE PROPOSAL: mirror e1e6a36 into the live lane. That commit forced this knob false for `backtest`
# ("grade the deepest series, not the venue a row is bought on") because 266 of 519 constituents
# resolve to a Xetra twin a MEDIAN 18.6 YEARS YOUNGER, and the graded 8y edge fell +117 -> -22.8.
# It left the live screen swapping, on the stated ground that "every threshold in ci-settings.yaml
# was fitted on the US lines anyway" — which is the argument FOR insulating the screen too, not
# against. The screen book collapsed 2026-08-21, the day before the backtest was insulated, and has
# been frozen since: 12 days, 11 of them an identical name list (.screen_snapshots.jsonl).
#
# PRE-REGISTERED STOP RULE, written before the runs: if the two arms' CLEARED counts differ by
# fewer than 3 names, the swap is not what binds admission -> record it, ship nothing, stop.
#
#   arm            scanned   refused   failed   CLEARED   stocks printed   unpinned non-dup
#   on (shipped)      5186       560     4615        11         5                1  (KLA.DE)
#   off               5191       558     4622        11         4                0
#
# IT FIRED AT ZERO. Not 3, not 1 — the cleared count is IDENTICAL, and the printed book moves the
# WRONG WAY: off loses KLA.DE, the only company the stock lane discovers, and prints NVIDIA twice.
#
# WHY THE GATES DO NOT CARE, and this is the transferable part. The swap moves names between gate
# columns in BULK and past the finish line NOT AT ALL (stocks, failed / sole-blocked):
#   gate        on            off          reading
#   history     57 / 57       13 / 13      the young twins — 44 stocks, every one sole-blocked
#   liquidity  183 /  1       17 /  0      min_avg_turnover_eur 500k on a Xetra line: 10.8x more fails
#   cagr       418 /  0      504 /  0      RESTORING history EXPOSES 86 more CAGR failures
#   peg        318 /  6      409 /  6      ...and 91 more PEG failures
#   5Y+        392 /  1      423 /  2      ...and 31 more 5Y failures
# The short Xetra record was HIDING those, exactly the mechanism growth_record_weight's receipt
# names: "a short record COLLECTS SCORE FOR THE ABSENCE OF MEASURED BAD NEWS". Give a name its true
# 27-year record back and the gates finally get to fail it on evidence. Sole-blocked stocks total
# 69 (on) and 24 (off), so even a PERFECT single-gate fix recovers a handful. Admission dies in
# DEPTH — 4615 of 5186 fail, 11 clear — not on any one axis. Same shape as the (#190) refusal and
# the "pool supply is not the constraint" result: the gate STACK binds, and no one knob is it.
#
# THE SKEW IS STILL REAL, and one row of the off-arm prints it better than any table could:
#   NVIDIA  NVDA    USA      +36%/yr  YRS 27.6
#   NVIDIA  NVD.DE  Germany  +14%/yr  YRS 18.7
# Same company, same day, 22 points of CAGR and 9 years of record, purely from venue and currency —
# against a growth_min_cagr bar of 19.0 the US line clears and the Xetra line misses by five. That
# is the train-serve skew as a single fact, and it is worth more than this round's verdict.
#
# LEFT OPEN, NOT FIXED HERE (each needs its own round and its own receipt):
#  - The fundamentals alias LEAKS. fetch.rs:1507 says aliasing a Xetra symbol back to its filer
#    "restores all of it"; measured, it does not — peg_yield coverage is 345/549 stocks with the
#    swap on against 404/552 with it off, so 59 names still lose their fundamentals to the swap.
#    Missing data PASSES, so those names are admitted on a gate that never got to run.
#  - liquidity at 500k is a US-calibrated bar applied to Xetra turnover. It sole-blocks 1, so it is
#    not worth a round on its own, but it is the wrong number for the venue it is being read on.
#
# REVERT RULE: none — nothing shipped. This knob stays `true`, which is both the shipped state and,
# on this measurement, the BETTER of the two for the live book. Do not re-propose insulating the
# screen without first widening what the gates admit; the venue is not what is holding the book to
# five rows. Nothing in buy_heuristic was re-fitted, no golden moves, and the backtest cannot see
# any of this — it hardcodes false and `the_backtest_never_reads_the_venue_knob` enforces that.

# `screen` SECTOR FILTER — which type of companies/ETFs to fetch. Case-insensitive substring
# keywords matched against a stock's GICS sector AND an ETF's fund name. Empty list (or omitted) =
# fetch ALL sectors (the default). Stocks are filtered BEFORE fetch (by their GICS sector, so other
# sectors are never even pulled); ETFs carry no GICS, so they're filtered by fund name. Crypto is
# never sector-filtered. `screen TICKER...` (explicit tickers) ignores this. Examples:
#   tech only:  [Technology, Communication, Semiconductor]   ("Technology" catches GICS "Information Technology")
#   healthcare: [Health]                                     energy: [Energy]   financials: [Financ]
#   ALL sectors: []   (empty list — or comment out the `sectors:` line below entirely)
sectors: []

# ntfy.sh topic for `alert`. Anyone who knows it can read your alerts (= your watchlist), so treat it
# like a password: this COMMITTED base carries a DUMMY only. Put a real, long, random private topic in
# the gitignored config/settings.yaml overlay (NEVER here — this file is committed + pushed).
ntfy_topic: "ci-smoke-tests-unused"

# Data-source URLs. Templates use {placeholder} tokens filled at fetch time: {ticker} {range} {n}
# {topic} {key}. Swap a source by editing the URL here, no rebuild of logic needed (only the response
# shape must match what the parser expects). The keys with a built-in code default (constituents_csv,
# euronext_lisbon, fundamentals*) are listed here for documentation — omit
# any and it falls back to the SAME URL, so the backtest gate already fetches them either way.
urls:
  yahoo_chart: "https://query1.finance.yahoo.com/v8/finance/chart/{ticker}?range={range}&interval=1d&events=div,split"
  # hourly bars over ~5 days -> screen 1h/6h/12h columns (counted as 1/6/12 bars back, not wall-clock,
  # so stocks fill too despite overnight gaps; 5d ensures >=13 bars even for a 6.5h trading day).
  yahoo_intraday: "https://query1.finance.yahoo.com/v8/finance/chart/{ticker}?range=5d&interval=60m"
  yahoo_search: "https://query1.finance.yahoo.com/v1/finance/search?q={ticker}&newsCount=3&quotesCount=0"
  yahoo_quote: "https://finance.yahoo.com/quote/{ticker}"
  euribor: "https://www.euribor-rates.eu/en/current-euribor-rates/2/euribor-rate-3-months/"
  us_cpi: "https://api.bls.gov/publicAPI/v1/timeseries/data/"  # BLS CPI-U index (monthly); seriesid + year window POSTed by fetch (path-GET ignores years). v1 = no key, 10y/call
  pt_cpi: "https://bpstat.bportugal.pt/data/v1/domains/12/datasets/7f13efcd65fc6bd0c5adb0e8d29d9b44/?lang=EN&series_ids=5721550"
  # COICOP-2018 successor (Feb 2026): prc_hicp_manr was TERMINATED at 2025-12; minr carries the
  # same annual-rate series (unit=RCH_A, all-items now coicop18=TOTAL) but only back to 2000.
  eu_hicp: "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/prc_hicp_minr?format=JSON&lang=EN&unit=RCH_A&coicop18=TOTAL&geo=EU27_2020"
  # the frozen pre-2026 dataset, still served: merged UNDER the live series for the 1997-1999 tail
  eu_hicp_old: "https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/prc_hicp_manr?format=JSON&lang=EN&coicop=CP00&geo=EU27_2020"
  # {n} = top-N crypto by market cap; `screen` maps each .symbol to SYMBOL-USD (Yahoo form)
  coingecko_markets: "https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page={n}&page=1"
  # S&P 500 constituents CSV (first column = Symbol); `screen` maps '.'->'-' for Yahoo
  sp500_csv: "https://raw.githubusercontent.com/datasets/s-and-p-500-companies/main/data/constituents.csv"
  # (Item 18/32) extra equity ponds APPENDED to the S&P 500 set, to widen the grower pool the proven
  # ranking draws from. Each entry is a CSV (col0=Symbol; col2=GICS Sector, or 2-column Symbol,Name ->
  # sector "other") OR a Wikipedia "List of S&P N companies" page (host picks the parser). A dead URL
  # or unrecognizable page just drops that pond — never crashes the screen.
  # Nasdaq-100 (yfiua/index-constituents, auto-updated monthly): ~13 large growers not (yet) in the
  # S&P 500 (the PDD/ARM profile — exactly what the lane ranks). VALIDATED 2026-07-03, same-batch 12y:
  # baseline edge +126.7 (OOS +0.14|+0.10) -> with n100 +126.8 (OOS identical) — free supply, no dilution.
  # S&P MidCap 400 (Wikipedia page) MEASURED-REJECTED same batch: +400 mid-caps CUT the edge to +85.0
  # and halved rho (+0.13 -> +0.07) — the persistence signal the lane rides is weaker in mid-caps
  # (they mean-revert more). Do NOT re-add without a new same-batch run that clears the bar.
  # FTSE 100 + DAX (yfiua CSVs, the free STOXX-600 proxy) MEASURED-REJECTED 2026-07-04, same-batch
  # 12y: baseline edge +127.6 (rho +0.13, OOS +0.14|+0.10) -> with 140 EU large-caps +117.0
  # (rho +0.11, OOS +0.11|+0.09, winsorized 97.7 -> 89.1) — every dial moved down, same direction
  # as the MidCap rejection: the US-momentum persistence the lane rides is weaker in EU large-caps.
  # RE-TESTED AND REJECTED AGAIN 2026-07-25, this time with FTSE MIB added (the one pond never
  # measured before) — DAX 40 + FTSE MIB 40 + FTSE 100 100, same-batch 12y: baseline 4913 tickers
  # edge +156.8 (rho +0.15, OOS +0.14|+0.12, winsorized +126.7) -> with the three ponds 5077 tickers
  # edge +136.0 (rho +0.13, OOS +0.11|+0.12, winsorized +106.4). Every dial down or flat, none up,
  # and the -20.8 falls well inside the 90% bootstrap bands ([+99.1 … +197.7] vs [+87.6 … +187.2]),
  # so it is not even distinguishable from noise — an indistinguishable result does not overturn a
  # prior measured rejection. Three runs, three ponds, one direction; Italy did not rescue it.
  # Do NOT re-add without a new same-batch run that clears the bar.
  # NOTE for whoever tries a fourth time: the dilution is a POOL effect, not just extra rows being
  # ranked — forward returns are de-meaned per ~6mo cutoff, so +164 EU names move every bucket's
  # mean. Widening the pond can therefore only pay if the new names rank as well as the incumbents,
  # and by three measurements EU large-caps do not.
  # (#100 / finding C10) ALL THREE REJECTIONS ARE PRE-PIT AND MEASURED ON A SURVIVOR POOL. Every run
  # quoted above — MidCap 400, FTSE 100 + DAX, and the DAX/FTSE MIB/FTSE 100 re-test — was 12y-only,
  # on today's index membership walked backwards, before the peer-mean fix. The DEBT block already
  # flags 12y/8y numbers from that era as needing a re-sweep, and these are those numbers.
  # THE SURVIVOR BIAS RUNS THE WRONG WAY HERE. Today's S&P 500 is the set of names that WON; today's
  # MidCap 400 is largely the set that did not get promoted. Judging mid-caps by attaching them to a
  # survivor-selected large-cap incumbent pool is close to guaranteed to read "the newcomers rank
  # worse", whether or not mid-caps carry signal — which is exactly what all three runs read.
  # So the standing verdict is UNCHANGED (do not re-add on argument alone) but the EVIDENCE behind it
  # is now marked stale, not settled. A re-run on the point-in-time pool at 20y/12y/8y is the only
  # thing that can either confirm or overturn it. Until then this file should not be cited as having
  # measured that mid-caps, EU large-caps, EM or small-caps are useless — it measured them once, on a
  # pool that could not answer the question.
  # (#128) THAT RE-RUN IS BLOCKED, 2026-08-23, and the block is in this repo, not in the data.
  # `backtest::pit_pool` keeps a ticker only if it is ABSENT from `sector_of` (i.e. not supplied by
  # any `constituents_csv` pond) or present in the S&P 500 membership spans. So under `pit` — the
  # only mode that can answer the question above — every name from an EXTRA pond is dropped, because
  # `member_spans` covers the S&P 500 alone and there is no historical twin to put the others back.
  # `pit_pool`'s own doc comment says it: "So `pit` shrinks a multi-pond universe to one pond. Add a
  # second membership source before adding a second pond, or the comparison is unfair to the pond
  # that keeps its names."
  #   CONSEQUENCE: adding MidCap 400 / FTSE / DAX back to `constituents_csv` and re-running at
  #   20y/12y/8y `pit` does NOT re-test the rejection. It produces the SAME S&P-500 pool plus
  #   whatever the new pond contributes that the S&P spans already carry, and reads as "the newcomers
  #   changed nothing" — a null that is manufactured by `pit_pool`, not measured. That is a WORSE
  #   failure than the survivor bias this finding already flags, because it looks like a clean answer.
  #   RE-FILED: the standing verdict is unchanged (do not re-add on argument alone) and the evidence
  #   stays marked stale, but the prescribed re-run is now BLOCKED PENDING A SECOND MEMBERSHIP
  #   SOURCE — point-in-time constituents for whichever pond is being tested — not merely unrun.
  #   Cite this before quoting the "a re-run on the point-in-time pool is the only thing that can
  #   confirm or overturn it" line above as though the re-run were available.
  #   WHY IT MATTERS NOW: (#128) retired threshold tuning at every arity (one gate <=16.2% of the
  #   missed wealth, exactly two <=13.6%, near-miss share ~0), which leaves POOL SIZE as one of the
  #   two remaining levers on growth admission. This is the thing standing in front of it.
  # (#129) TWO CORRECTIONS TO THE PARAGRAPH ABOVE, 2026-08-23. The BLOCK STANDS; its framing does not.
  # (1) THE SECOND MEMBERSHIP SOURCE DOES NOT EXIST FOR FREE — the block is on DATA, not on effort, and
  # (#128) left that ambiguous. The CODE side is small and was checked: `core::sp500_spans` is already
  # an index-agnostic `ticker,start_date,end_date` parser despite the name, and `Urls.sp500_history` is
  # a single String where `constituents_csv` is already a Vec — so a second source is a Vec, a map
  # merge, and `pit_pool` unchanged. The SOURCE is what is missing. fja05680/sp500, the repo
  # `default_sp500_history` points at, publishes the S&P 500 alone. yfiua/index-constituents — the repo
  # the pond below is fetched from — covers Nasdaq-100/DAX/FTSE/CSI, but its history BEGINS 2023-07 and
  # is MONTHLY SNAPSHOTS, not spans. Neither can produce a point-in-time twin at 8y, let alone 20y.
  # RE-FILE from "blocked pending a second membership source" to "blocked pending a source that is not
  # available free": this needs a paid vendor (CRSP/EODHD-class), not a session of work. Do not open
  # this again on the assumption that someone simply has not looked.
  #   (#153) NARROWS THIS, 2026-08-29: true for PRICES, false for DATES. Alpha Vantage LISTING_STATUS
  #   serves delisted US symbols with ipoDate and delistingDate on the free tier. The dates alone
  #   unblock nothing (no terminal price), but do not cite this line for them. See CORRECTION 2.
  # (2) THE POND ACTUALLY BEING DROPPED TODAY IS 15 NAMES. "One of the two remaining levers on growth
  # admission" was written without measuring it. Measured 2026-08-23 against `.sp500_history.json`
  # (1206 spans) and both configured pond CSVs:
  #       sp500_csv pond                                503
  #       constituents_csv pond (nasdaq100)             101
  #       nasdaq100 names not already in sp500_csv       14
  #       names `pit_pool` actually drops           15 of 517   = 2.9% of the live pond, ~1% of the PIT pool
  #       of those, carrying >=20y of price history        6   ALNY ASML INSM MSTR TRI VMRK
  #       >=12y / >=8y                                  9 / 12
  # So POOL SIZE, as this repo is configured today, is a SIX-NAME lever at 20y — the same order as the
  # `out-ranked` column (0.2%/0.0%/1.5%) that (#127) used to declare selection dead. Treat it as
  # measured and immaterial, not as a lever awaiting a session.
  # THIS DOES NOT RETIRE THE C10 RE-RUN, and the distinction matters: C10 is about MidCap 400 / FTSE /
  # DAX ponds, which share almost nothing with the S&P 500 spans and would be dropped very nearly
  # ENTIRELY under `pit`. The 15 measures the nasdaq100 pond that is configured now. Two different
  # claims — never cite the 15 for the other ponds.
  # (#134) THE POND RE-RUN IS BLOCKED BY THE PRICE SOURCE, NOT BY MEMBERSHIP DATA. Measured 2026-08-24.
  # (#128) filed this as "BLOCKED PENDING A SECOND MEMBERSHIP SOURCE"; (#129) refiled it as "blocked
  # pending a source that is not available free ... a paid vendor (CRSP/EODHD-class)" and closed with
  # "do not open this again on the assumption that someone simply has not looked". Someone has now
  # looked, and both receipts name the WRONG HALF. Membership is the CHEAP half. The half that cannot
  # be had here is PRICES FOR NAMES THAT STOPPED TRADING.
  #
  # 1. MEMBERSHIP IS NEARLY FREE, AND (#129) MISSED THE SOURCE IT CHECKED FOR. Wikipedia serves any
  #    revision's wikitext, and the constituent tables carry {{NyseSymbol|X}} / {{NasdaqSymbol|X}}
  #    templates, so a monthly walk of a page's revision history reconstructs `ticker,start_date,
  #    end_date` directly — the shape `core::sp500_spans` already parses.
  #    `tools/membership_from_wikipedia.py` does exactly that, and is deliberately NOT wired into the
  #    binary; `--selftest` runs its parser checks offline.
  #    Measured depth, per pond, 2026-08-24:
  #      S&P 400 MidCap   USABLE 2012-12 -> today. 399/400/400/401/400 names parsed at 2013-01, 2016-06,
  #                       2020-06, 2023-06, today.
  #      FTSE 100         USABLE ~2015 ONLY. The Ticker column appears between 2014-06 and 2015-06;
  #                       before that the page lists company NAMES ("# [[3i]]", 2005), and the
  #                       "Past constituents" section is an undated, ticker-less name list.
  #      S&P 600          2018. Too shallow.
  #      DAX / CAC 40 / IBEX 35   No machine-readable ticker table at any depth.
  #    The S&P 400 page's own "Selected past ... changes" table IS dated and DOES carry added/removed
  #    tickers — 508 dated rows — but its earliest row is 2012-01, so it does not extend the walk back.
  #    CONSEQUENCE ON ITS OWN: no free pond reaches 20y. Ship Rule v2 PRIMARY needs 20y AND 8y, so no
  #    Wikipedia-sourced arm can carry a verdict. (12y needs membership by 2014-08 — S&P 400 clears
  #    that, FTSE 100 does not. 20y needs 2006-08 and nothing clears it.)
  #
  # 2. A 20y SOURCE DOES EXIST, AND IT FAILS FOR A SECOND, SEPARATE REASON. iShares Trust (CIK
  #    0001100663) files the IJH schedule of investments on EDGAR: N-Q filings carrying "S&P MidCap 400
  #    Index Fund" run from 2007-08, N-CSR from 2003. Parsed live — the 2007-06-30 IJH schedule yields
  #    393 holdings, inside a 350-420 band. But those filings identify holdings by COMPANY NAME ONLY:
  #    no ticker, no CUSIP, no ISIN, checked in a 2004 and a 2013 filing alike. Resolving those names
  #    against the Wikipedia name<->ticker pairs (754 keys, unioned over eight revisions) matched
  #    234 of 393 = 59.5%. The 40% that missed are systematically the DEPARTED names — Catalina
  #    Marketing, DRS Technologies, Sequa, ArvinMeritor, Colonial BancGroup, Greater Bay Bancorp.
  #    Dropping them re-introduces precisely the survivor bias a point-in-time universe exists to
  #    remove, so the shortfall cannot be waved through as coverage noise.
  #
  # 3. AND IT WOULD CHANGE NOTHING IF EVERY NAME RESOLVED. THIS IS THE ACTUAL FINDING. (#121) already
  #    measured it on the S&P 500, where membership is perfect: of the 734 members whose span carries
  #    an exit date, 387 come back as a MUTUALFUND stub with ZERO BARS, and 193 more hold fewer than 36
  #    bars inside their own span — 154 of 734 are scoreable. ZERO of 1206 members have a series ending
  #    more than a year before today. Re-confirmed 2026-08-24 against `.long_history_cache.json`:
  #      BSC TWX SVU ROH ENRNQ  MUTUALFUND stub, numeric shortName, 0 bars.
  #      CFC (left 2008-07)     320 bars, 2000-01..2026-08 — not Countrywide's history.
  #      MOLX (acquired 2013)   215 bars, 1973-01..2026-07 — likewise.
  #      ARM                    "Arm Holdings plc", 2023-09..2026-08. ArvinMeritor was a 2007 IJH
  #                             holding; that ticker now belongs to a company that listed in 2023.
  #    A perfect MidCap membership file would therefore hand the walk zero-bar stubs and RECYCLED
  #    tickers carrying a different company's prices. The second failure is worse than the first,
  #    because it scores silently instead of dropping out.
  #
  # WHAT WOULD ACTUALLY REOPEN THIS: a survivorship-free PRICE source. That is the vendor (#129) was
  # reaching for, described correctly for the first time. ONE free candidate is UNTESTED rather than
  # refuted — stooq.com is reported to retain delisted US symbols, but on 2026-08-24 every request
  # from this machine, INCLUDING a live AAPL control, returned a JavaScript proof-of-work bot check
  # instead of CSV. Nothing about its delisted coverage was measured either way. Run the live control
  # first before citing this line as "stooq does not have it".
  #   (#153) SUPERSEDES THIS LEAD, 2026-08-29: the control was re-run (same wall) and robots.txt reads
  #   `User-agent: * / Disallow: /`. stooq is refused on PERMISSION, so its coverage is moot and this
  #   is no longer an open candidate. See CORRECTION 1 below.
  # DO NOT re-open the pond question by reconstructing membership: that half is done and it is not the
  # blocker. (#100)'s evidence stays STALE and the standing verdict stays UNCHANGED — do not re-add a
  # pond on argument alone — but the block is now filed against the right half of the problem.
  # (#153) THE BLOCK STANDS AND IS NOW MEASURED ON DEAD NAMES — plus THREE CORRECTIONS to the two
  # receipts above. Measured 2026-08-29, entirely offline. (#134) closed by naming the one free
  # candidate it had not tested and asking for a live control first. That control has been run, a
  # dated third-party list of genuinely delisted symbols has been obtained free, and the coverage
  # metric (#134) reported turns out to count the wrong population. The CONCLUSION does not move:
  # there is still no survivorship-free price source here. The EVIDENCE is now much sharper.
  #
  # CORRECTION 1 — STOOQ IS REFUTED, NOT UNTESTED. Delete the "run the live control first" line
  # above as an open lead. stooq.com's robots.txt is `User-agent: * / Disallow: /`, exempting only
  # Bingbot and Googlebot, so ALL automated access is disallowed site-wide regardless of what the
  # coverage turns out to be. Re-tested 2026-08-29: a live AAPL control still returns the same
  # JavaScript proof-of-work wall, so the 2026-08-24 observation was not a transient. Its delisted
  # coverage remains unmeasured and will stay that way — this is a PERMISSION refusal, not a
  # capability one, and it is not reopened by a better client.
  #
  # CORRECTION 2 — DELISTING DATES ARE FREE. (#129) refiled this whole block as "needs a paid vendor
  # (CRSP/EODHD-class), not a session of work". That is right for PRICES and wrong for DATES.
  # Alpha Vantage's LISTING_STATUS endpoint returns delisted US symbols with `ipoDate` AND
  # `delistingDate`, on the free tier, one request, no per-ticker loop. Verified live on the public
  # demo key, which serves one canned date: `state=delisted&date=2014-07-10` -> 425 symbols, 419 of
  # them assetType=Stock carrying both dates (e.g. `ZIGO,Zygo Corp,NASDAQ,Stock,1992-03-03,
  # 2014-06-20,Delisted`). Other dates return 0 rows on the demo key; a real free key serves the
  # full history. NOTHING WAS BUILT ON THIS — see the verdict below for why the dates alone buy
  # nothing — but stop citing "the dates need a vendor". They do not.
  #
  # CORRECTION 3 — AND THIS IS THE FINDING: `.sp500_history.json` EXIT DATES CANNOT IDENTIFY A DEAD
  # NAME, so (#134)'s coverage number counts the wrong population. LEAVING AN INDEX IS NOT
  # DELISTING. A name dropped from the S&P 500 for size or a sector rebalance keeps trading, keeps a
  # real forward return, and carries NO survivorship bias at all. A name that stopped trading is the
  # entire problem. The span file records one exit date for both and does not distinguish them.
  # (#134) reported "154 of 734 are scoreable" off that file. Recounted 2026-08-29 over the 703
  # members whose spans are ALL closed (i.e. never re-entered; the 734/703 gap is that definition,
  # not a data change):
  #       no price data at all            387   = 186 zero-bar payloads + 201 "No data found"
  #                                             (this half REPRODUCES (#134)'s 387 exactly)
  #       has bars                        316
  #         feed runs >1y past the exit   300   median overrun 6476 days = 17.7 YEARS
  #         feed ends within 1y of exit    16
  #       (#134)'s ">=36 bars in-span"    123 of 703
  # AND ALL 16 ARE AN ARTIFACT OF THE WINDOW, NOT TERMINATED FEEDS. Every one exited between
  # 2025-09-22 and 2026-06-30 and every one's last bar is 2026-08-25 — they are still trading; they
  # pass a 365-day test only because their exit is recent. None holds 5y of in-span history.
  #   => ZERO of 703 exited members have a price feed that actually stops when the name stopped, and
  #      the 123 "scoreable" are live tickers sliced to a past window. For a name that merely left
  #      the index that slice is legitimate; for a recycled ticker it is a different company's
  #      prices, which is the silent-scoring failure (#134) 3. already named. The coverage is
  #      BINARY: Yahoo serves either nothing or a feed running to today. There is no middle.
  #
  # THE CROSS-CHECK ON GENUINELY DEAD NAMES. The recount above is still an S&P-membership sample, so
  # it was re-run against the LISTING_STATUS list, where every row is a real delisting with a real
  # date. 30 of the 419 delisted stocks are already in `.long_history_cache.json`:
  #       zero-bar payload                 17   (AAMRQ ACS AYE BJS BRL CVH HNZ IKN JNY MFE NLC NYX
  #                                              OMX PALM ROH SOV, mostly numeric shortName)
  #       recycled / continuation          11   last bar >1y after the delisting date
  #       "No data found, symbol may be delisted"  2   (FRX, HMA)
  #       USABLE IN-SPAN BARS               0
  #   CEG delisted 2013-03-20, serves 2022-01-17..2026-08-27 — Constellation Energy, the 2022
  #   spin-off, on the same ticker. MER (Merrill Lynch) delisted 2008-12-31, serves 28 bars dated
  #   2026. Same three failure modes as the membership sample, on an independently sourced list.
  #   SAMPLE CAVEAT, STATED NOT BURIED: those 30 are the names this machine happens to have fetched
  #   already, not a random draw. A live census over 40 UNCACHED delisted symbols was attempted the
  #   same day and is UNMEASURED, not zero: Yahoo returned HTTP 429 to a live AAPL control, so every
  #   row would have been rate limiting rather than coverage. That is the same control discipline
  #   (#134) demanded for stooq, applied to this run's own result. The control was then retried 30
  #   times at 60s and returned 429 every time, so the run aborted having probed ZERO delisted
  #   symbols — the cooldown is longer than 30 minutes, not a transient. Whoever retries this should
  #   pace it in hours and keep the control gate first; a census that fetched nothing is the correct
  #   outcome of that gate, not a failure of it.
  #
  # VERDICT — NO CODE, AND THE DATES FILE IS NOT WIRED IN. A delisting date without a terminal price
  # cannot correct survivorship bias; it can only label a name the backtest already drops when its
  # span ends. `pit_pool` gains nothing from knowing WHY a span closed. Building a LISTING_STATUS
  # fetcher, a `stress_source` knob and a cache for that would be an instrument for a question these
  # numbers close, which is the build (#127) warned against ("NO KNOB WAS CHANGED ON THE STRENGTH OF
  # THIS"). `STRESS_TICKERS` stays the hand-picked 27 and its doc comment stays honest about it.
  # WHAT WOULD REOPEN THIS is unchanged from (#134) and is now the ONLY thing that would: a
  # ---------------------------------------------------------------------------------------------
  # (#161) THE VENDORS ARE NOW PRICED, 2026-08-29. DESK RESEARCH ONLY — NOTHING BOUGHT, NOTHING BUILT.
  # (#129) said "a paid vendor (CRSP/EODHD-class)" and named no price; (#134) and (#153) both close by
  # asking for a survivorship-free PRICE source without saying what one costs. Four candidates, priced
  # against THIS pool's requirement so the next reader does not repeat the search. PRICES ARE PUBLISHED
  # LIST PRICES READ OFF VENDOR PAGES ON 2026-08-29 AND NOT OFF A CHECKOUT — treat them as indicative.
  #
  # THE REQUIREMENT, restated from (#153)'s own numbers so a vendor can be judged against it:
  #   (a) prices for names that STOPPED TRADING, ending AT the delisting rather than running to today;
  #   (b) a terminal return (Bessembinder's median delisted return is -91.95%, and this dataset holds
  #       not one such return);
  #   (c) depth to ~2006-08 for a 20y PRIMARY, ~2014-08 for 12y;
  #   (d) A PERMANENT SECURITY ID, NOT A TICKER. This is the one (#153) proved is binding: CEG, MER and
  #       ARM are all recycled, and a ticker-keyed feed scores a DIFFERENT COMPANY silently.
  #
  #   vendor            list price            delisted  depth   platform          keyed on
  #   EODHD             EUR 19.99/mo          yes, all  US from REST, Linux OK    Isin, "string or null";
  #                     EUR 199.90/yr         packages  ~2000                     NO delisting date
  #   Sharadar SEP      ~USD 69/mo, 499/yr    21k act+  1998    REST via Nasdaq   delist reasons, ticker
  #                     (bundle price, see below)  delisted             Data Link, Linux OK   changes, security ids
  #   Norgate Platinum  USD 630/yr            yes, AND  1990    WINDOWS ONLY      n/a here
  #   Norgate Diamond   USD 787.50/yr         historical index consts 1950  (see below)
  #   CRSP              no individual license — academic/government/practitioner contracts only
  #
  # NORGATE IS THE BEST DATA FIT AND THE WORST PLATFORM FIT. Platinum/Diamond carry delisted securities
  # AND historical index constituents, i.e. BOTH halves (#134) split apart, for USD 630-787.50/yr. But
  # the Norgate Data Updater is a Windows-only .NET application (Win 10/11 64-bit, .NET 4.7.2, up to
  # 63 GB local store); Mac/Linux is "medium-term plans", with a Windows VM or WSL2 as the documented
  # workaround. This project is a Linux Rust CLI whose fetch layer is HTTP-plus-JSON-cache. Standing a
  # Windows VM up to feed it is a bigger change than anything the data would buy. There is a 21-day
  # free trial, but it carries TWO YEARS of history, which cannot answer a 20y question.
  #
  # EODHD IS THE CHEAPEST THING THAT COULD ANSWER, AND ITS DOCS DO NOT ANSWER THE ONE QUESTION THAT
  # MATTERS. Delisted symbols come from `exchange-symbol-list/US?delisted=1`, returning `Code` `Name`
  # `Country` `Exchange` `Currency` `Type` `Isin` — Isin nullable, and NO delisting date, which is fine
  # because (#153) CORRECTION 2 already found the dates free at Alpha Vantage LISTING_STATUS. The docs
  # say prices "keep their full history up to the delisting date" and say NOTHING about recycled
  # tickers, which is requirement (d) — the one that actually decides it.
  #   AND IT CANNOT BE PROBED FREE. The free tier is 20 calls/day and PAST YEAR ONLY; the DEMO key
  #   serves six fixed tickers (AAPL, TSLA, VTI, AMZN, BTC-USD, EURUSD). Neither can fetch a name that
  #   died in 2008. So the control discipline (#134) demanded for stooq and (#153) applied to itself
  #   CANNOT be run before paying. That is a finding, not an obstacle to route around.
  #   ONE LIMIT WORTH PRICING NOW: for names delisted BEFORE 2018 EODHD serves EOD ONLY — no
  #   fundamentals, dividends or splits. Prices and a terminal return are what survivorship needs, so
  #   this does not block the correction. It DOES mean the `fund` lane — which (#156), (#157) and
  #   (#159) all read — could not be extended to pre-2018 dead names on this vendor.
  #
  # SHARADAR advertises exactly the fields requirement (d) needs — "delist reasons, ticker changes" and
  # security identifiers, 21k active-and-delisted to 1998. Its price is quoted here from a BUNDLE page
  # (USD 69/mo, 499/yr full history; 29/mo for 5 years, too shallow) and was NOT confirmed as the price
  # of SEP alone; SF1, the fundamentals half, is a separate product. Verify before citing this figure.
  #
  # CRSP is the gold standard and is not purchasable here: academic, government and practitioner
  # contracts only, signed and paid annually. (#129)'s "CRSP/EODHD-class" pairs a product this project
  # cannot buy with one it can, and only the second half was ever actionable.
  #
  # WHAT ANY OF IT WOULD ACTUALLY BUY, MEASURED RATHER THAN ASSUMED. The survivorship correction in
  # this repo is `STRESS_TICKERS`: 27 hand-picked names, of which only FIVE are bankruptcies whose
  # series ends near zero (BBBYQ, FRCB, SIVBQ, SBNY, WEWKQ) — the doc comment calls those "the gold
  # correction". Everything this file says about whether the edge survives a loser-inclusive pool rests
  # on those five. A vendor replaces the hand-picked list with the real population — (#153) already
  # holds 419 dated delistings from a free endpoint, waiting only on prices. THE INSTRUMENT ALREADY
  # EXISTS: `stress` injects and dedups the list today, so the integration is a bigger list, not a new
  # lane. That is the cheapest shape this could take and it is why the question is worth a small bet.
  #
  # RECOMMENDED NEXT STEP, AND IT IS NOT A PURCHASE OF A YEAR: buy ONE month of EODHD's cheapest EOD
  # plan (EUR 19.99) and run the probe (#153) could not — fetch the names it proved Yahoo fails on
  # (CEG delisted 2013-03-20, MER 2008-12-31, plus ROH BSC TWX ENRNQ AAMRQ PALM NYX SOV) and check
  # three things: does the series STOP at the delisting date; is there a real terminal return; and is a
  # RECYCLED ticker isolated from its predecessor. Yahoo fails all three. If EODHD passes, the year is
  # worth EUR 199.90; if it fails on recycling, the next rung is Sharadar at ~USD 69 for one month, not
  # Norgate. PRE-REGISTER that as the bar before the month is bought, and record the result here
  # whichever way it lands — a refuted vendor is as reusable a finding as a working one.
  #
  # (#163) THAT PURCHASE IS VOID, 2026-08-30 — NO-SPEND CONSTRAINT. The recommendation above is kept
  # as written (its pricing survives if the constraint ever lifts) but it MUST NOT be acted on: this
  # project buys no data. What follows is the free-only re-run of the same question, and it closes it.
  #
  # FMP WAS THE ONE VENDOR (#161) NEVER PROBED, and it was the obvious one to probe: the repo already
  # holds an FMP_API_KEY and already fetches four /stable/ endpoints with it (src/config.rs 822, 849,
  # 856, 862), so asking costs nothing. Probed read-only 2026-08-30, key never printed:
  #
  #   endpoint                              result
  #   /stable/delisted-companies            WORKS — symbol, companyName, exchange, ipoDate, delistedDate
  #   /stable/delisted-companies?page=1     REFUSED — "Special Parameters : The values for 'page' can
  #                                         only be 0 based on your current subscription"
  #   /stable/historical-price-eod/full     REFUSED for every dead name — "Special Endpoint : This value
  #                                         set for 'symbol' is not available under your current
  #                                         subscription": MER, TWX, ENRNQ, AAMRQ, SIVBQ, FRCB, and
  #                                         BCAR (delisted 2026-08-28, taken straight off the free list)
  #   /stable/historical-price-eod/full     AAPL returns full history in the same session
  #
  # SO THE DATES ARE FREE AND THE PRICES ARE NOT — the identical split (#153) found at Alpha Vantage,
  # now confirmed at a second vendor. Two details matter for anyone re-reading this. (1) The price
  # refusal is SYMBOL-CLASS, not rate-limiting and not age: the live control passes in the same
  # session, and a name delisted two days earlier is refused exactly like one delisted in 2008. (2) The
  # free delisted LIST is page 0 only — 100 rows, the most recent delistings — so it is not a census
  # and cannot enumerate what a 20y backtest is missing. It is a daily-refreshed sliver, and building a
  # census by accumulating it going forward would take as many years as the backtest is long.
  # THE LEGACY /api/v3/ PATHS ARE ALSO GONE for this key ("only available for legacy users who have
  # valid subscriptions prior August 31, 2025"), so any future reader copying a v3 URL from an old
  # tutorial will get an error that reads like an auth failure and is not one. The repo's own four
  # endpoints are unaffected — all /stable/, all live-symbol.
  #
  # THE FREE LANDSCAPE IS NOW COMPLETE, AND THE DIRECTION IS CLOSED:
  #   stooq            REFUSED ON PERMISSION — robots.txt is "User-agent: * / Disallow: /"
  #   Yahoo            0 usable delisted names, 0-for-30 and 0-for-703 on the END-at-delisting test (#153)
  #   EODHD  free      past-year only; the demo key serves 6 tickers — cannot probe the question (#161)
  #   Alpha Vantage    LISTING_STATUS free (#153); prices need a free key at 25 req/day and are
  #                    UNADJUSTED only (adjusted daily is premium), so returns break across splits —
  #                    and 25/day cannot fetch a universe even if they did not. The public demo key
  #                    returns "The demo API key is for demo purposes only" for TIME_SERIES_DAILY on
  #                    every symbol including IBM, so it cannot answer this either.
  #   FMP              dates free (page 0), prices paywalled — this entry
  #
  # VERDICT: THE WIDER-UNIVERSE DIRECTION IS CLOSED ON FREE DATA. Every free source serves membership
  # or delisting DATES and none serves survivorship-free PRICES. (#129)'s "this needs a paid vendor,
  # not a session of work" is confirmed a third time, and under no-spend that is the end of it — not a
  # backlog item, not a smaller probe, and not a reason to re-examine Yahoo. The 27 STRESS_TICKERS
  # (22 crashed-and-alive, 5 bankrupt) remain the only dead-name evidence this repo has, and (#161)'s
  # note that `stress` already injects and dedups that list still stands as the cheapest integration
  # shape IF a price source ever arrives.
  # WHAT WOULD REOPEN IT: a price source — free or paid — shown to serve bars that END at a delisting
  # date. That test is unchanged since (#134) and it is still the only test. Nothing above passes it.
  # REVERT RULE: none — receipts only, no code, no knob, no golden.
  # ---------------------------------------------------------------------------------------------
  # survivorship-free PRICE source. Membership is done, dates are free, prices are the block. A
  # candidate must be shown to serve bars that END at a delisting date — that is the one test, and
  # both samples above fail it 0-for-30 and 0-for-703.
  # TO REPRODUCE (no network, no build, ~15s):
  #   import json, datetime
  #   sp = json.load(open('.sp500_history.json')); ch = json.load(open('.long_history_cache.json'))
  #   for t, spans in sp.items():
  #       if any(e is None for _, e in spans): continue          # still a member -> not an exit
  #       ex = max(datetime.date.fromisoformat(e) for _, e in spans)
  #       try: ts = ch[t][1]['chart']['result'][0].get('timestamp') or []
  #       except Exception: continue                             # "No data found" payload
  #       if not ts: continue                                    # zero-bar stub
  #       print(t, ex, datetime.date.fromtimestamp(ts[-1]))      # last bar vs the exit date
  #   # every printed line has a last bar at today, or within a year of a 2025/2026 exit.
  #
  # ============================================================================================
  # (#173) POOL SUPPLY ON MERIT — THE S&P MIDCAP 400 POND. GRADED, AND REFUSED. 2026-08-30.
  # *** RETRACTED IN PART BY (#174), 2026-08-30 — READ THAT BLOCK FIRST. These six runs were graded on
  # *** `universe pit`, NOT the canonical `universe fund pit`, so the PEG gate was inert and the gate
  # *** stack incomplete. THE VERDICT BELOW IS WRONG: the (#160) guard is NOT broken at 12y (the
  # *** canonical lane reads 63%, not 48%) and the shipped lane is NOT guard-broken. The correct
  # *** verdict is UNRESOLVED on TIE. The POOL-SUPPLY FINDING SURVIVES and was reconfirmed on the
  # *** canonical lane. Everything below is left as written, per this file's convention.
  # ============================================================================================
  # (#172) named the last open lever: "pool supply on merit, and widening the universe rather than
  # the gates is its untried half." This is that run. It is the first GRADED answer on the
  # wider-universe direction — (#163) closed it on a vendor argument, this closes it on a number.
  #
  # WHY IT WAS RE-OPENED AT ALL. Two receipts stood against it and neither covers this case.
  #   * (#134) refused because "no free pond reaches 20y. Ship Rule v2 PRIMARY needs 20y AND 8y."
  #     (#139) later made the PIT lane's PRIMARY read 12y AND 8y with every 20y GUARD live, so that
  #     objection was written under a rule that no longer governs this lane. (#134)'s own census
  #     puts S&P 400 membership USABLE from 2012-12, which clears the 2014-08 that 12y needs.
  #   * (#163) closed "the wider-universe direction on free data", but its own reopening test is
  #     "a price source shown to serve bars that END at a delisting date" — i.e. prices for names
  #     that STOPPED TRADING. A MidCap pond of names that still trade needs no such source.
  #   * (#153) CORRECTION 3 is what makes the slice legitimate: "LEAVING AN INDEX IS NOT DELISTING
  #     ... 300 of 316 with bars have feeds running >1y past the exit, median overrun 6476 days =
  #     17.7 YEARS." S&P 400 departures are dominated by index MIGRATION (promotion to the 500,
  #     demotion to the 600), not by delisting.
  #
  # PHASE A — the kill gate, pre-registered BEFORE execution, run fully OFFLINE against the 165
  # cached `List of S&P 400 companies` Wikipedia revisions (no network, so no repeat of (#153)'s
  # 429 wall). Bar: N_12y >= 200 or the round stops and Phase B is never built.
  #   N_new (MidCap ever-members absent from the 1206 S&P spans) ....... 785
  #   dead — no price bars at all (THE SURVIVOR-BIAS HOLE) ............. 261  (33%)
  #   recycled ticker — span and feed do not overlap (ARM, CEG, MER) ...  51  (6%)
  #   feed ends before the span ends ...................................   0
  #   N_usable (feed covers the span) .................................. 473  (60%)
  #   N_12y (membership alive at 2014-08) .............................. 457  -> PASS, bar was 200
  # The 261 dead names are the survivorship hole this arm cannot close and it is recorded here as a
  # number, per the plan, whatever the outcome. NOTE the usable count was WRONG on the first pass:
  # it was computed as ">=36 in-span bars", which flagged live long-history names (ALLY, ENTG,
  # PINS, TWLO, TOST) as thin. Cause: fetch.rs rewrites `interval=1d` to `interval=1mo` and Yahoo
  # caps `range=max` at ~168 points, so cached AAPL/MSFT/JNJ all read 4.0 bars/yr. BAR COUNT IS A
  # DENSITY ARTIFACT, NOT A COVERAGE MEASURE — classify on first-bar/last-bar dates instead.
  #
  # PHASE B — the knob. `urls.membership_csv: Vec<String>`, defaulted EMPTY (non-negotiable #1), a
  # sibling of `sp500_history` rather than a migration of it. Parsed by the EXISTING
  # `core::sp500_spans` (index-agnostic despite the name) and merged by `core::merge_spans` into
  # the S&P map. `backtest::pit_pool` builds its pool as `spans.keys()`, so merged names arrive
  # with NO edit to backtest.rs at all and no second pond — which is exactly the precondition
  # pit_pool's own doc comment states. An entry not starting with `http` is read from disk, so
  # `data/sp400_spans.csv` (1085 names, 27 KB, generated by tools/membership_from_wikipedia.py)
  # reproduces this offline. Deliberately NOT cached: a filename-keyed cache would serve one graded
  # arm's membership file to the other.
  #
  # GRADED, pre-registered per (#139): control (`membership_csv: []`) vs arm (+ the CSV), at 20y,
  # 12y and 8y, `backtest N universe pit`, six runs. PRIMARY reads 12y AND 8y; every 20y GUARD live.
  #
  #   horizon | scoreable index members | windows scored | MEDIAN GATED POOL | VACUOUS
  #           |      ctl  ->  arm       |   ctl -> arm   |    ctl -> arm     |
  #     20y   |      289  ->  289       |  1119 -> 1119  |      5 -> 5       |  yes
  #     12y   |      396  ->  591       |  2737 -> 3013  |      7 -> 7       |  yes
  #      8y   |      478  ->  736       |  6126 -> 7181  |      9 -> 9       |  yes
  #   pond size 1206 -> 1991 names (+785, +65%).
  #
  #   top-10 excess (mean | median), worst window:
  #     20y   ctl +0.6 | +0.0  worst -10.0    arm +0.6 | +0.0  worst -10.0   (identical)
  #     12y   ctl +3.2 | +3.0  worst  -6.4    arm +3.1 | +3.0  worst  -6.4
  #      8y   ctl +4.3 | +3.8  worst  -5.5    arm +4.5 | +4.3  worst  -5.5
  #   rank-1 h2h, 6-10 book ((#160) at 12y/8y): 20y 4/8 (50%)  12y 10/21 (48%)  8y 16/29 (55%)
  #   rank-1 h2h, 11-20 book ((#160) at 20y):   20y 1/2 (50%)  12y  3/8 (38%)   8y  8/16 (50%)
  #   BOTH ARMS READ IDENTICALLY ON EVERY h2h TALLY AT EVERY HORIZON.
  #
  # VERDICT: REFUSED ON A GUARD. Precedence is GUARD > PRIMARY > TIE, and a broken guard refuses
  # outright — it is never softened into unresolved. The (#160) 12y bar is the 6-10 book rank-1 h2h
  # at >= 50% absolute AND not below the control; the arm reads 48%, so the ABSOLUTE half fails.
  # Read honestly: the CONTROL also reads 48%, so the not-worse half holds and the break is a
  # property of the shipped lane today, not something this arm caused. The arm neither broke it nor
  # fixed it. PRIMARY would not have carried it either — 12y mean -0.1 (DOWN) against 8y +0.2 /
  # +0.5 (UP) is horizons disagreeing, i.e. TIE -> UNRESOLVED -> NO SHIP.
  #
  # AND THIS IS THE FINDING, which is larger than the verdict: SUPPLY IS NOT THE BINDING
  # CONSTRAINT. THE GATES ARE. The arm added 785 pond names, lifted scoreable index members by 54%
  # at 8y and 49% at 12y, and scored 1055 more windows at 8y — and the MEDIAN GATED POOL DID NOT
  # MOVE BY ONE NAME at any horizon (9->9, 7->7, 5->5). Every column stayed VACUOUS under (#149).
  # Stronger still: of the 785 new names, ZERO appear in any printed rank slice at any horizon.
  # Names arrive, get scored, and are gated out before the book. That is the mechanism, measured.
  #
  # REVERT RULE / REOPENING TEST: this direction reopens ONLY on a run where the median gated pool
  # MOVES with pond size. A bigger pond is not evidence; a deeper gated pool is. Anyone proposing
  # another index (S&P 600, Russell, FTSE) must show that first, and (#134) already measured
  # FTSE/DAX/CAC/IBEX as unusable at the depth 12y needs. The knob stays SHIPPED OFF: it costs one
  # defaulted-empty field and it is what makes this close re-runnable in one line instead of a
  # rebuild. This supersedes (#163)'s argument-based close with a measurement, and it retires
  # (#172)'s "one lever left" — pool supply on merit is now closed on a graded run.
  #
  # 20y IS THE BUILT-IN POSITIVE CONTROL AND IT PASSED. S&P 400 membership only starts 2012-12, so
  # a correct point-in-time implementation MUST read byte-identical at 20y. It did: 289 -> 289
  # scoreable, 1119 -> 1119 windows, every number equal. Pre-registered as expected before the run
  # so it could not be mistaken for a manufactured null on the day.
  #
  # A MANUFACTURED NULL WAS ACTUALLY HIT, AND IS THE WARNING WORTH KEEPING. The first six runs came
  # back EXACTLY zero-difference at all three horizons — the clean-looking null (#128) warns about.
  # Cause: `target/mutants/folioman` was a leftover cargo-mutants `--in-place` artifact whose
  # `fetch_sp500_history` had been mutated to return empty, so `pit_swaps_pool` was false and the
  # pool swap never ran. The tells were `COVERAGE (#121): 0 of 0` and a missing `PIT —` line on a
  # run that was passed `pit`. AFTER ANY cargo-mutants RUN, REBUILD BEFORE GRADING, and confirm
  # with `strings target/mutants/folioman | grep <a string only the new code has>`.
  #
  # CENSUS DRIFT — BANKED FLAGS ARE PERISHABLE. Today's shipped control reads median gated pool 7
  # at 12y, 9 at 8y, 5 at 20y, and the 12y rank-1 h2h on the 11-20 book is n=8, not the n=4 (#151)
  # banked. Same direction, different magnitudes. Re-read a census line before leaning on a banked
  # one; do not cite (#151)'s n=4 as current.
  #
  # (#173) also documents the sibling key that does NOT appear below because it defaults empty:
  #   membership_csv: ["data/sp400_spans.csv"]   # extra point-in-time membership, off by default
  # ============================================================================================
  #
  #
  # ============================================================================================
  # (#174) (#173) WAS GRADED ON THE WRONG LANE. RE-RUN, RETRACTED, RE-ADJUDICATED. 2026-08-30.
  # ============================================================================================
  # (#173)'s six runs used `backtest N universe pit`. THE CANONICAL GRADED LANE IN THIS FILE IS
  # `backtest {20,12,8} universe fund pit` — about twenty receipts use that spelling; the no-fund
  # spelling appears once, and only for (#121)'s COVERAGE CENSUS, which is a pool diagnostic and not
  # a graded verdict. Without `fund` the fundamental lane is empty, so `growth_max_peg` never binds
  # and the gate stack that decides the pool is INCOMPLETE.
  #
  # THREE INDEPENDENT CONFIRMATIONS THAT THE FLAG, NOT DATA DRIFT, IS THE CAUSE:
  #   1. (#173)'s COVERAGE read 289/396/478 of 1206 — the banked NO-FUND census reads 291/399/481.
  #   2. (#173)'s sweep printed `growth_max_peg ->off admits 0 new names`. On the canonical lane the
  #      same row reads n=42 at 8y. A gate that admits nobody is a gate with no data behind it.
  #   3. (#172)'s control, canonical lane, reads 12y 18/26 (69%) and 8y 25/34 (74%).
  #
  # THE SHIPPED LANE MEASURED PROPERLY, `universe fund pit`, control arm, 2026-08-30:
  #     horizon | rank-1 h2h 6-10 book | 11-20 book | median gated pool | (#173) said
  #       20y   |      4/8  (50%)      |  1/2 (50%) |         5         |  4/8 (50%),  pool 5
  #       12y   |     12/19 (63%)      |  2/4 (50%) |         6         | 10/21 (48%), pool 7
  #        8y   |     19/27 (70%)      |  5/8 (62%) |         7         | 16/29 (55%), pool 9
  #
  # RETRACTION, STATED PLAINLY: (#173) asserted that the SHIPPED LANE IS GUARD-BROKEN AT 12y, its
  # 6-10 tally sitting at 48% against (#160)'s 50% absolute bar. THAT IS FALSE. The canonical lane
  # reads 63% at 12y and 70% at 8y, both comfortably clear, and in the same regime as (#172)'s banked
  # 69%/74%. No guard is broken on the shipped lane, no round is pre-refused at 12y, and nothing in
  # the grading apparatus needs repair. The 48% was an artifact of a missing word on a command line.
  # Note also that `fund` TIGHTENS the pool (5/6/7 against 5/7/9): the live PEG gate removes names.
  #
  # THE POND ARM, RE-ADJUDICATED ON THE CANONICAL LANE. Same two overlays, same `data/sp400_spans.csv`,
  # six fresh runs.
  #
  #   PRE-REGISTERED CONFOUND, MEASURED AND CLEARED. 0 of the 785 new names were in `.sec_cache`
  #   before the round. Non-negotiable #5 is "missing data PASSES a gate", so a name SEC does not
  #   serve would sail through `growth_max_peg` while the incumbents are genuinely gated — "wider pond
  #   PLUS a free pass", which is not a result. Declared in writing before the runs. MEASURED AFTER:
  #   SEC serves 478 of 785 new names (61%) against 694 of 1206 incumbents (58%). The rates are
  #   SYMMETRIC — the new names are served slightly BETTER than the pond they join — so the asymmetry
  #   does not exist and the arm is a valid result. 307 names do still ride #5; that is the residue.
  #
  #   horizon | scoreable members | windows scored | MEDIAN GATED POOL | VACUOUS
  #           |    ctl  ->  arm   |   ctl -> arm   |    ctl -> arm     |
  #     20y   |    289  ->  289   |  1119 -> 1119  |      5 -> 5       |  yes
  #     12y   |    396  ->  591   |  2737 -> 3013  |      6 -> 6       |  yes
  #      8y   |    478  ->  736   |  6126 -> 7181  |      7 -> 7       |  yes
  #
  #   top-10 excess (mean | median), worst window:
  #     20y   ctl +0.6 | +0.0  worst -10.0    arm +0.6 | +0.0  worst -10.0   (identical)
  #     12y   ctl +3.7 | +3.9  worst  -6.4    arm +3.8 | +4.1  worst  -6.4
  #      8y   ctl +5.3 | +5.6  worst  -5.5    arm +4.9 | +4.3  worst  -5.5
  #   rank-1 h2h 6-10 book:   20y 4/8 = 4/8   12y 12/19 = 12/19   8y ctl 19/27 -> arm 20/27
  #   rank-1 h2h 11-20 book:  20y 1/2 = 1/2   12y  2/4  =  2/4    8y ctl  5/8  -> arm  5/10
  #
  # VERDICT: UNRESOLVED ON TIE -> NO SHIP. Every GUARD PASSES this time, which is what changes.
  # (#160) at 12y: 63% >= 50% and 12/19 is NOT BELOW the control's 12/19. At 8y: 74% >= 50% and 20/27
  # is ABOVE the control's 19/27. At 20y the 11-20 book is 1/2 = 50%, meeting the absolute bar. The
  # worst window is unchanged at all three horizons. With the guards clear, PRIMARY is reached and it
  # SPLITS: 12y mean +0.1 / median +0.2 (UP) against 8y mean -0.4 / median -1.3 (DOWN). Horizons
  # disagreeing is a TIE, and a TIE is UNRESOLVED, and UNRESOLVED IS NO SHIP.
  # SO THE BOTTOM LINE IS UNCHANGED — the pond does not ship — BUT THE REASON IS DIFFERENT AND THIS
  # ONE IS CORRECT. (#173) recorded a guard break that was not there.
  #
  # READING NOTE on the 8y 11-20 row, so nobody reads a fall into it: the numerator is FLAT at 5 and
  # the DENOMINATOR grows 8 -> 10, because the wider pond desaturates two more buckets. That is
  # (#160)'s READING NOTE artifact in its falling form. It is also not the guard book at 8y ((#160)
  # reads 6-10 there), so it does not bite. The 8y census likewise moves 35 -> 33 saturated: the pond
  # is not entirely inert, it just never reaches the basket.
  #
  # THE (#173) FINDING SURVIVES INTACT AND IS NOW CONFIRMED ON TWO DIFFERENT GATE STACKS. Pond 1206
  # -> 1991 (+65%), scoreable members +54% at 8y and +49% at 12y, 1055 more windows scored at 8y —
  # and THE MEDIAN GATED POOL DID NOT MOVE BY ONE NAME at any horizon, on either lane. Every column
  # stayed VACUOUS. ZERO of the 785 new names appear in any printed rank slice at any horizon on the
  # canonical lane, exactly as on the no-fund lane. SUPPLY IS NOT THE BINDING CONSTRAINT; THE GATES
  # ARE. (#173)'s reopening test stands unchanged: this direction reopens only on a run where the
  # median gated pool MOVES with pond size.
  #
  # THE GATE SWEEP, READ ON THE CANONICAL LANE FOR THE FIRST TIME — AND IT NAMES NO LEVER.
  #     gate                    8y (n, mean|med, vs shipped)      12y (n, mean|med, vs shipped)
  #     growth_min_cagr -4      335, +60.6|+54.9, +56.5  TIGHT    151, +12.3|-55.6,  +17.6  SPLIT
  #     growth_max_peg ->off     42, +44.4|+11.7, +13.3  TIGHT     14, -20.1|-212.1, -138.9
  #     growth_min_1y_pct -10    57, +21.0|-44.3, -42.7  SPLIT     41,  +3.2|-138.7, -65.5  SPLIT
  #     growth_max_above_ma off  93, +97.4|-75.6, -73.9  SPLIT     86, -94.5|-129.0, -55.7
  #     growth_min_leg_years 2   93,  +0.3| -3.2,  -1.5  SPLIT     68, -60.6|-73.0,   +0.2
  #   TWO gates carry a TOO TIGHT flag (mean AND median positive) and BOTH DO SO AT 8y ONLY. At 12y
  #   the same two have medians of -55.6 and -212.1. NOTHING IS FLAGGED AT BOTH HORIZONS. Since the
  #   pit lane's PRIMARY reads 12y AND 8y ((#139)), an 8y-only lead is the exact signature that
  #   produces a TIE, which is how (#151) refused BOTH gates it flagged. `growth_max_peg` is newly
  #   PRICED here — it was invisible on the no-fund lane — and it is 8y-only too.
  #   CONCLUSION: THE GATE STACK HAS NO LIVE LEAD ON THE CANONICAL LANE. Read this as permission to
  #   measure, never as a result ((#151)); a green ADMITS NO WORSE flag is a pool diagnostic.
  #
  # THE LESSON, AND IT IS THE REUSABLE PART: A MISSING `fund` SILENTLY CHANGES THE GATE STACK, AND
  # NOTHING IN THE OUTPUT SAYS SO. There is no warning, no error, and every number still prints. The
  # tell is `growth_max_peg ->off admits 0 new names` in the GATE SWEEP — a gate that admits nobody
  # is a gate with no data behind it. GRADE ON `universe fund pit`, and check that row before
  # believing any verdict. The sibling trap from (#173) still applies: after any cargo-mutants run,
  # rebuild before grading, because `--in-place` leaves a MUTATED binary in `target/mutants/`.
  # ============================================================================================
  #
  #
  # ============================================================================================
  # (#175) THE VACUITY IS STRUCTURAL. THE POOL CAN BE DE-SATURATED, AND IT COSTS THE EDGE. 2026-08-31.
  # ============================================================================================
  # THE QUESTION. (#149)'s vacuity rule has now refused FOUR consecutive ranking-knob rounds, and the
  # fourth (`growth_er_weight`, this file) closed with a standing invitation: "RE-OPEN ONLY ON A
  # NON-VACUOUS COLUMN. That is a POOL problem, not a score problem ... no ranking knob whatsoever is
  # measurable here." Nobody had tried to COLLECT on it. This round does, and the answer is that the
  # invitation cannot be honoured at a price worth paying.
  #
  # It also matters live, not just for grading: the median gated pool is 5/6/7 against a graded basket
  # of 10, so THE SHIPPED HEURISTIC CANNOT FILL ITS OWN BOOK. That is the framing this round tested.
  #
  # PRE-REGISTERED BEFORE THE RUNS, and none of it moved afterwards:
  #   DE-SATURATED := median gated pool >= 11 at BOTH 12y and 8y (strictly above VERDICT_TOP = 10).
  #   VERDICT_TOP is NOT touched — re-fitting the basket to meet the pool is (#120)'s forbidden argmax.
  #   ANTI-ARGMAX: report the census for EVERY rung, grade ONLY the SMALLEST arm that de-saturates,
  #     and if it refuses, STOP. Do not walk up the ladder hunting a rung that passes.
  #   PREDICTION: the de-saturating arm REFUSES on PRIMARY at 12y, because (#174)'s canonical sweep
  #     priced `growth_min_cagr`'s newly-admitted cohort at a 12y median of -55.6 and flagged TOO
  #     TIGHT at 8y only. HIT, and overshot — it refused at BOTH horizons, same direction.
  #
  # ---- PHASE A: THE CENSUS LADDER. `backtest {12,8} universe fund pit`, seven arms, census only ----
  # Control reproduces (#174) exactly (12y pool 6, 31/35 saturated; 8y pool 7, 35/43), so probe-vs-
  # probe holds. `windows scored` is IDENTICAL at 2737 / 6126 on every arm — the gate moves who is in
  # the pool, not how many windows exist, which is the control that says these columns are comparable.
  #
  #   arm                                    12y pool (sat)   8y pool (sat)   clears >= 11 both?
  #   control  growth_min_cagr 19.0             6 (31/35)       7 (35/43)      --
  #   A2  growth_value_floor_pct 40 -> 0        7 (28/35)       8 (29/43)      NO
  #   A3  growth_max_above_ma 150 -> off        7 (27/36)       8 (31/44)      NO
  #   A4  growth_min_leg_years 5 -> 3           6 (31/35)       7 (35/43)      NO — moves NOTHING
  #   A1a growth_min_cagr 19 -> 15              9 (22/36)      11 (22/44)      NO (12y = 9)
  #   A1b growth_min_cagr 19 -> 12             11 (16/36)      14 (16/44)      YES  <-- smallest
  #   A1c growth_min_cagr 19 -> 8              14 (12/36)      19 (12/44)      yes, but larger
  #
  # ONLY THE CAGR GATE DE-SATURATES. Three of the four non-CAGR arms move the median by ONE name and
  # `growth_min_leg_years` moves it by ZERO. That is a real result about the shape of the gate stack:
  # it is not five gates each taking a slice, it is ONE gate doing essentially all of the binding.
  #
  # (A2) IS ITS OWN FINDING AND CLOSES THE SECOND SUPPLY EXPLANATION. `growth_value_floor_pct` runs at
  # backtest.rs:2505 and the POOL CENSUS reads its output at :2519 — so every "median gated pool 6"
  # ever quoted in this file is measured AFTER a trim that discards the dearest 40% of each bucket.
  # That looked like the obvious cause. It is not: removing the ENTIRE brake buys ONE name at each
  # horizon. It roughly doubles the SUBSTITUTABLE count at 8y (21 -> 44) while the MEDIAN barely
  # moves, and `basket_vacuity` adjudicates on the median. Note this also answers (#75)'s standing
  # "RE-GRADEABLE, not re-graded" line for the census question, though NOT for the knob's own edge.
  # With (#173)'s pond result, BOTH supply-side explanations are now eliminated: not the pond, not the
  # trim. The gates bind, and one gate binds.
  #
  # ---- PHASE B: GRADE A1b, `growth_min_cagr: 19.0 -> 12.0`, the smallest de-saturating rung ----
  # Six runs, `backtest {20,12,8} universe fund pit`. THE COLUMNS ARE GENUINELY NON-VACUOUS ON THE ARM
  # (12y 16 saturated of 36, 8y 16 of 44), so PRIMARY ADJUDICATES A RANKING-RELEVANT BOOK FOR THE
  # FIRST TIME IN THIS FILE. That is what the round bought, and it is worth stating separately from
  # the verdict, because it is the only run on record where the ship rule was not answering vacuously.
  #
  #   horizon | top-10 excess mean|med | worst window |  h2h 6-10 book  | h2h 11-20 book | pool
  #     20y   | +0.6|+0.0 -> +1.7|+1.4 | -10.0 -> -3.9 | 4/8 50% -> 7/19 37% | 1/2 50% -> 5/10 50% | 5 -> 10
  #     12y   | +3.7|+3.9 -> +3.0|+3.8 |  -6.4 -> -5.3 | 12/19 63% -> 17/32 53% | 2/4 50% -> 11/20 55% | 6 -> 11
  #      8y   | +5.3|+5.6 -> +4.1|+4.3 |  -5.5 -> -4.2 | 19/27 70% -> 22/40 55% | 5/8 62% -> 19/28 68% | 7 -> 14
  #
  # VERDICT: REFUSED ON THE (#160) GUARD AT 12y AND 8y. The 6-10 tally is 53% and 55% — ABOVE the 50%
  # absolute bar and BELOW the control's 63% and 70%. That is exactly the case (#160) was built for and
  # says so in its own words: "an ABSOLUTE bar cannot see a knob that makes the book worse while
  # staying above the line." A broken guard REFUSES OUTRIGHT; it is never softened into unresolved.
  # PRIMARY REFUSES INDEPENDENTLY, and not as a TIE: top-10 excess falls at BOTH graded horizons
  # (12y -0.7 mean / -0.1 median, 8y -1.2 / -1.3). Two independent halves of the rule, same answer.
  #
  # "TALLY" MEANS THE RATE, NOT THE COUNT — DISAMBIGUATED HERE BECAUSE THIS IS THE FIRST ARM THAT
  # COULD TELL THEM APART. A de-saturating arm GROWS the h2h denominator (12y 19 -> 32, 8y 27 -> 40),
  # so the count RISES (12 -> 17, 19 -> 22) while the rate FALLS. (#160) did not anticipate an arm
  # that moves its own denominator. It is settled by (#160)'s own worked example, which quotes (#138)
  # as "67% -> 62%" and "65% -> 70%" — percentages. So: RATE. The verdict is robust either way (PRIMARY
  # fails at both horizons on its own), but the NEXT de-saturating arm needs this written down.
  #
  # ---- WHY IT COSTS WHAT IT COSTS, which is the part worth keeping ----
  # THE ADMITTED NAMES ARE THE WRECKS. The 12y top-10 zero list gains exactly AIG@b3994(-96%),
  # RIG@b4015(-95%) and FOSL at b4024/b4026/b4028 (-98%/-99%/-96%) — the GFC bailout, post-Macondo
  # Transocean, and three Fossil Group windows. Zeros ridden go 9 -> 13 and holds 209 -> 308. A 19%/yr
  # compounder bar is, among other things, a distress filter, and 12%/yr does not clear the wreckage.
  #
  # AND THE BOOK REALLY DID GET DEEPER — the cost is not an artifact. The rank slices inverted:
  #   12y rank 6-10   ctl -1.9|-0.1 (win 47% of 19)  ->  arm +2.1|+1.7 (win 72% of 32)
  #    8y rank 6-10   ctl -0.0|+1.7 (win 63% of 27)  ->  arm +2.7|+2.2 (win 72% of 40)
  # The control's 6-10 slice is largely PADDING drawn from saturated buckets; the arm's is real names,
  # and it scores better. So de-saturation did what it claimed. The top-10 aggregate still falls,
  # because the marginal names are worse than the index even when they are better than padding.
  # Read those two facts together — they are the whole finding.
  #
  # 20y IS THE DISSENTING COLUMN AND IS NOT PROMOTED. The arm IMPROVES there (+0.6 -> +1.7, worst
  # -10.0 -> -3.9) and its 20y guards PASS (11-20 book 5/10 = 50%, worst not worse). It does not
  # rescue the arm: (#139) puts PRIMARY on 12y AND 8y for the pit lane and (#124) puts pit 20y n_eff
  # at ~0.2. Quoting the 20y improvement as the result would be picking the column the rule excludes
  # BECAUSE it cannot resolve. Recorded, not promoted. Note its 6-10 tally is 37% — well under 50% —
  # which does not bite only because 20y reads the 11-20 book by (#160).
  #
  # ---- WHAT THIS SETTLES ----
  # THE FOUR-ROUND INVITATION IS ANSWERED AND SHOULD NOT BE RE-OPENED AS WRITTEN. A non-vacuous column
  # is REACHABLE — the pool goes 6 -> 11 and 7 -> 14 — but only by loosening the gate that defines a
  # compounder, and the loosened book fails both halves of the ship rule. So the vacuity is NOT a
  # tuning oversight that a cleverer knob setting escapes: IT IS THE SHAPE OF A POOL THAT A 19%/yr
  # CAGR BAR PRODUCES ON THE S&P 500. Any future ranking knob inherits this, and re-opening on "just
  # de-saturate first" is now a MEASURED refusal rather than an untried idea.
  #
  # THE LIVE DEFECT IS REAL AND IS NOT FIXED BY THIS. The heuristic still cannot fill a 10-name book
  # from its own gated pool. This round establishes only that LOOSENING THE CAGR GATE is the wrong
  # price for fixing it. A basket smaller than 10 is the untested alternative and is BLOCKED by
  # (#120)'s forbidden argmax; anyone re-opening that owes a receipt against (#120), not this one.
  #
  # PER THE ANTI-ARGMAX RULE, A1c WAS NOT GRADED. It de-saturates further (14/19) and admits strictly
  # more marginal names than the rung that already refused. Its census is banked above; that is all it
  # is for.
  #
  # REVERT RULE: nothing shipped, so there is nothing to revert — `growth_min_cagr` stays 19.0 and
  # `growth_value_floor_pct` stays 40.0. RE-OPEN ONLY on a de-saturating arm that does NOT loosen a
  # gate: something that ADDS names the current stack would already accept. (#173) closed the pond
  # version of that, so a re-opener needs a third mechanism, not a fourth pond.
  # ============================================================================================
  #
  constituents_csv:
    - "https://yfiua.github.io/index-constituents/constituents-nasdaq100.csv"
  # Euronext Lisbon equities (POST, DataTables JSON; mics=XLIS) -> Portugal `.LS` stock universe
  euronext_lisbon: "https://live.euronext.com/en/pd_es/data/stocks?mics=XLIS"
  # latest Bitcoin NUPL (net unrealized profit/loss), free on-chain sentiment gauge; `screen` prints it after the crypto table
  nupl: "https://bitcoin-data.com/v1/nupl/last"
  # (E) trailing-P/E source for the valuation tilt. {ticker} + {key} (key from FMP_API_KEY env, NOT
  # here). Equities only, and only when the env key is set — free tiers are rate-limited, so use it
  # for `check` (~55 tickers), not the ~750-ticker `screen` (where P/E stays None -> neutral tilt).
  fundamentals: "https://financialmodelingprep.com/stable/quote?symbol={ticker}&apikey={key}"
  # (F) ROE/quality source for the profitability tilt — FMP stable ratios-ttm (carries returnOnEquityTTM).
  # Same FMP_API_KEY opt-in / `check`-scale profile as `fundamentals` above. (v3 legacy died 2025-08-31.)
  fundamentals_quality: "https://financialmodelingprep.com/stable/ratios-ttm?symbol={ticker}&apikey={key}"
  ntfy: "https://ntfy.sh/{topic}"
