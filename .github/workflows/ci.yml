name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 4 * * *"   # nightly 04:00 UTC — drives the backtest-gate job below (guarded by `if:`)
  workflow_dispatch: {}    # let it be run on demand from the Actions tab

# Tame flaky crates.io fetches. The "[16] Error in the HTTP2 framing layer" curl failure is HTTP/2
# multiplexing choking mid-download; CARGO_HTTP_MULTIPLEXING=false forces HTTP/1.1 and dodges it.
# CARGO_NET_RETRY bumps cargo's own retry (default 3) so a transient blip self-heals instead of
# failing the job. (Cargo.lock is untracked, so every job re-resolves + re-downloads — more exposure.)
env:
  CARGO_NET_RETRY: "10"
  CARGO_HTTP_MULTIPLEXING: "false"

# note: ubuntu-latest ships a stable Rust toolchain, so no install step needed.
# rust-cache with a shared key: all jobs restore ONE common warm cache from prior runs
# (deps compiled once, not N×). Parallel jobs can't share THIS run's cache — GitHub saves it
# at job end — so cross-run reuse is the win; within-run hand-off would require `needs: build`.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # one warm cache shared across all jobs (across runs)
      - run: cargo build --verbose

  # lint gate. clippy -D warnings turns every lint into an error (needless clone, dead code,
  # unwrap-heavy footguns…). NO `cargo fmt --check`: this codebase keeps a deliberate dense style
  # (single-line method chains, aligned trailing comments) that rustfmt-default would explode across
  # hundreds of lines — cosmetic churn, no bug caught. clippy stays; it finds real defects.
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: rustup component add clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # one warm cache shared across all jobs
      - run: cargo clippy --all-targets -- -D warnings

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # one warm cache shared across all jobs (across runs)
      - run: cargo test --lib   # in-module #[cfg(test)] unit tests

  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # one warm cache shared across all jobs (across runs)
      - run: cargo test --test '*'   # everything under tests/ (tests/network.rs cases are #[ignore]d -> skipped here, stays offline)

  # live API smoke tests — hit Yahoo/FX to catch the API changing shape under the parsers. The tests
  # self-classify (see tests/network.rs): a 429/5xx/transport hiccup SKIPS (green), only a 200-OK-but-
  # -broken response FAILS. BLOCKING (no continue-on-error): because a throttle just skips, the only way
  # this job goes red is a real regression — API drift or broken code — which SHOULD fail the pipeline.
  # config/settings.yaml is gitignored (private ntfy topic), so FOLIOMAN_CONFIG points config::load() at
  # the checked-in, secret-free tests/ci-settings.yaml fixture.
  network-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # same warm cache as the other jobs
      - name: live API smoke tests (skip-on-throttle, fail-on-drift)
        run: FOLIOMAN_CONFIG=tests/ci-settings.yaml FOLIOMAN_NET_TESTS=1 cargo test --test network -- --ignored

  # Nightly walk-forward gate. Runs the REAL `backtest 12 universe` over the live universe and checks
  # the committed default tuning still yields a POSITIVE validated edge. NOT on push/PR: the wide fetch
  # is slow, Yahoo-heavy, and drifts by the day, so a per-PR gate would go red on the market, not the
  # code. Same skip-vs-fail contract as network-smoke: a throttle (nonzero exit / <500 tickers resolved)
  # SKIPS green, only a genuine COLLAPSE (GROWTH edge <= 0, or both out-of-sample halves negative) FAILS.
  # ci-settings.yaml mirrors the real settings.yaml `buy_heuristic`, so the gate scores with the tuning
  # actually shipped — a red means a scoring-code change or a knob edit broke the edge. Its own
  # FOLIOMAN_BACKTEST_GATE opt-in keeps the multi-minute backtest out of the per-PR network-smoke sweep.
  backtest-gate:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 45   # cold release build + the wide-universe walk-forward fetch
    steps:
      - uses: actions/checkout@v4
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: folioman-ci   # same warm cache as the other jobs
      - name: walk-forward edge gate (skip-on-throttle, fail-on-collapse)
        run: FOLIOMAN_CONFIG=tests/ci-settings.yaml FOLIOMAN_BACKTEST_GATE=1 cargo test --release --test network backtest_edge_holds -- --ignored --nocapture
